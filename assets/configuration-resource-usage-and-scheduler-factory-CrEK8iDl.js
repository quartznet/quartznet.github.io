import{a as e,c as t,i as n,t as r}from"./app-CuLqJq2G.js";var i=JSON.parse(`{"path":"/documentation/quartz-4.x/tutorial/configuration-resource-usage-and-scheduler-factory.html","title":"Configuration, Resource Usage and SchedulerFactory","lang":"en-US","frontmatter":{"title":"Configuration, Resource Usage and SchedulerFactory"},"git":{"updatedTime":1786192546000,"contributors":[{"name":"Marko Lahma","username":"","email":"marko.lahma@gmail.com","commits":1},{"name":"Claude Fable 5","username":"","email":"noreply@anthropic.com","commits":1}],"changelog":[{"hash":"c34c908bdde656401030090c8e751abc1f473565","time":1786192546000,"email":"marko.lahma@gmail.com","author":"Marko Lahma","message":"Bump docs npm dependencies to clear Dependabot alerts (#3255)","coAuthors":[{"name":"Claude Fable 5","email":"noreply@anthropic.com"}]}]},"filePathRelative":"documentation/quartz-4.x/tutorial/configuration-resource-usage-and-scheduler-factory.md"}`),a={name:`configuration-resource-usage-and-scheduler-factory.md`};function o(r,i,a,o,s,c){return t(),n(`div`,null,[...i[0]||=[e(`<h1 id="configuration-resource-usage-and-schedulerfactory" tabindex="-1"><a class="header-anchor" href="#configuration-resource-usage-and-schedulerfactory"><span>Configuration, Resource Usage and SchedulerFactory</span></a></h1><p>Quartz is designed in modular way, and therefore to get it running, several components need to be &quot;snapped&quot; together. Fortunately, some helpers exist for making this happen.</p><p>The major components that need to be configured before Quartz can do its work are:</p><ul><li>ThreadPool</li><li>JobStore</li><li>DataSources (if necessary)</li><li>The Scheduler itself</li></ul><p>Thread pooling has changed a lot since the Task-based jobs were introduced. Now the default implementation, <code>DefaultThreadPool</code> uses <a href="https://docs.microsoft.com/en-us/dotnet/standard/threading/the-managed-thread-pool" target="_blank" rel="noopener noreferrer">CLR&#39;s managed thread pool</a> to execute jobs as tasks. You can configure the pool that have max concurrency, which effectively limits how many concurrent tasks can be scheduled to the CLR&#39;s thread pool. See configuration reference for more details on how to configure the thread pool implementation.</p><p>JobStores and DataSources were discussed in Lesson 9 of this tutorial. Worth noting here, is the fact that all JobStores implement the <code>IJobStore</code> interface - and that if one of the bundled JobStores does not fit your needs, then you can make your own.</p><p>Finally, you need to create your Scheduler instance. The Scheduler itself needs to be given a name and handed instances of a JobStore and ThreadPool.</p><h2 id="building-a-scheduler-without-a-container" tabindex="-1"><a class="header-anchor" href="#building-a-scheduler-without-a-container"><span>Building a scheduler without a container</span></a></h2><p>An application with no host — a console application, or a test — builds a scheduler with <code>QuartzSchedulerBuilder</code>. It takes the same configuration API as <code>AddQuartz</code>, creating a container of its own and building from it, so what works in one works in the other:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token class-name"><span class="token keyword">var</span></span> builder <span class="token operator">=</span> QuartzSchedulerBuilder<span class="token punctuation">.</span><span class="token function">Create</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">builder<span class="token punctuation">.</span><span class="token function">ConfigureScheduler</span><span class="token punctuation">(</span>options <span class="token operator">=&gt;</span> options<span class="token punctuation">.</span>InstanceName <span class="token operator">=</span> <span class="token string">&quot;reporting&quot;</span><span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">.</span><span class="token function">UseDefaultThreadPool</span><span class="token punctuation">(</span><span class="token named-parameter punctuation">maxConcurrency</span><span class="token punctuation">:</span> <span class="token number">10</span><span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">.</span><span class="token function">UseInMemoryStore</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line"><span class="token class-name">IScheduler</span> scheduler <span class="token operator">=</span> <span class="token keyword">await</span> builder<span class="token punctuation">.</span><span class="token function">BuildScheduler</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The builder is kept in a variable rather than built in one expression: its configuration methods are the same ones <code>AddQuartz</code> hands out, so they return that interface rather than the builder and cannot be chained into <code>BuildScheduler()</code>. Use <code>Build()</code> instead of <code>BuildScheduler()</code> when you want the factory rather than the scheduler it produces. It returns a <code>StandaloneSchedulerFactory</code>, which owns the container it built — dispose it, preferably with <code>await using</code>, to shut the scheduler down.</p><h2 id="configuring-from-properties" tabindex="-1"><a class="header-anchor" href="#configuring-from-properties"><span>Configuring from properties</span></a></h2><p>A scheduler can also be configured from a set of flat <code>quartz.*</code> properties (<code>NameValueCollection</code>) instead of in code. The properties are generally stored in and loaded from a file, but can also be created by your program and handed to the builder:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token keyword">await</span> <span class="token keyword">using</span> <span class="token class-name">StandaloneSchedulerFactory</span> schedulerFactory <span class="token operator">=</span> QuartzSchedulerBuilder<span class="token punctuation">.</span><span class="token function">Create</span><span class="token punctuation">(</span><span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">.</span><span class="token function">UseProperties</span><span class="token punctuation">(</span>properties<span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">.</span><span class="token function">Build</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The keys are translated into the same options and registrations the code-based API produces, so a scheduler configured this way is the same scheduler, and the two can be mixed — what is written in code wins. Keys are checked against the ones Quartz reads, so a misspelling is reported rather than silently leaving a setting at its default.</p><p>You can find complete documentation in the &quot;Configuration Reference&quot; section of the Quartz documentation.</p><h2 id="logging" tabindex="-1"><a class="header-anchor" href="#logging"><span>Logging</span></a></h2><p>Quartz logs through <code>Microsoft.Extensions.Logging</code>. Under a host it uses whatever logging the application has configured, with no extra setup. Quartz does not log much: some information while starting, and then only serious problems while jobs run.</p><h3 id="microsoft-extensions-logging" tabindex="-1"><a class="header-anchor" href="#microsoft-extensions-logging"><span>Microsoft.Extensions.Logging</span></a></h3><p>You can configure Microsoft.Extensions.Logging.Abstractions either manually or using services found in <a href="https://www.nuget.org/packages/Quartz" target="_blank" rel="noopener noreferrer">Quartz</a>.</p><h4 id="manual-configuration" tabindex="-1"><a class="header-anchor" href="#manual-configuration"><span>Manual configuration</span></a></h4><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token comment">// obtain your logger factory, for example from IServiceProvider</span></span>
<span class="line"><span class="token class-name">ILoggerFactory</span> loggerFactory <span class="token operator">=</span> <span class="token range operator">..</span><span class="token punctuation">.</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line"><span class="token comment">// Quartz 3.1</span></span>
<span class="line">Quartz<span class="token punctuation">.</span>LogContext<span class="token punctuation">.</span><span class="token function">SetCurrentLogProvider</span><span class="token punctuation">(</span>loggerFactory<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line"><span class="token comment">// Quartz 3.2 onwards</span></span>
<span class="line">Quartz<span class="token punctuation">.</span>Logging<span class="token punctuation">.</span>LogContext<span class="token punctuation">.</span><span class="token function">SetCurrentLogProvider</span><span class="token punctuation">(</span>loggerFactory<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h4 id="configuration-using-microsoft-di-integration" tabindex="-1"><a class="header-anchor" href="#configuration-using-microsoft-di-integration"><span>Configuration using Microsoft DI integration</span></a></h4><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line">services<span class="token punctuation">.</span><span class="token function">AddQuartz</span><span class="token punctuation">(</span>q <span class="token operator">=&gt;</span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    <span class="token comment">// this automatically registers the Microsoft Logging</span></span>
<span class="line"><span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div>`,24)]])}var s=r(a,[[`render`,o]]);export{i as _pageData,s as default};