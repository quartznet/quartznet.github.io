import{a as e,c as t,d as n,i as r,o as i,r as a,s as o,t as s,u as c}from"./app-D6FlNkEn.js";var l=JSON.parse(`{"path":"/documentation/quartz-4.x/how-tos/custom-job-store.html","title":"A Job Store of Your Own","lang":"en-US","frontmatter":{"title":"A Job Store of Your Own"},"git":{"updatedTime":1787413150000,"contributors":[{"name":"Marko Lahma","username":"","email":"marko.lahma@gmail.com","commits":1},{"name":"Claude Fable 5","username":"","email":"noreply@anthropic.com","commits":1}],"changelog":[{"hash":"99c1c648b015bd52dedc833fab86ff9aa7316a5f","time":1787413150000,"email":"marko.lahma@gmail.com","author":"Marko Lahma","message":"Give the schema-changes page anchors VuePress actually emits","coAuthors":[{"name":"Claude Fable 5","email":"noreply@anthropic.com"}]}]},"filePathRelative":"documentation/quartz-4.x/how-tos/custom-job-store.md"}`),u={name:`custom-job-store.md`};function d(s,l,u,d,f,p){let m=c(`RouteLink`);return t(),r(`div`,null,[l[16]||=a(`h1`,{id:`a-job-store-of-your-own`,tabindex:`-1`},[a(`a`,{class:`header-anchor`,href:`#a-job-store-of-your-own`},[a(`span`,null,`A Job Store of Your Own`)])],-1),l[17]||=a(`p`,null,[a(`code`,null,`IJobStore`),i(` is where scheduling data lives. Quartz ships two implementations — in memory, and ADO.NET over a relational database — and the interface is public so a third can keep it somewhere else: a document database, a key-value store, a service.`)],-1),l[18]||=a(`p`,null,`Before writing one, be clear about which of three jobs you are doing, because they have different answers.`,-1),a(`table`,null,[l[6]||=a(`thead`,null,[a(`tr`,null,[a(`th`,null,`You want to`),a(`th`,null,`Do this`)])],-1),a(`tbody`,null,[l[4]||=a(`tr`,null,[a(`td`,null,`Add behaviour around an existing store — logging, metrics, tenant routing, fault injection`),a(`td`,null,[i(`derive from `),a(`code`,null,`DelegatingJobStore`)])],-1),a(`tr`,null,[l[3]||=a(`td`,null,[i(`Support a `),a(`strong`,null,`relational`),i(` database Quartz does not ship a dialect for`)],-1),a(`td`,null,[l[1]||=i(`write an `,-1),o(m,{to:`/documentation/quartz-4.x/how-tos/dialect-delegate.html`},{default:n(()=>[...l[0]||=[a(`code`,null,`IDriverDelegate`,-1)]]),_:1}),l[2]||=i(`, not a store`,-1)])]),l[5]||=a(`tr`,null,[a(`td`,null,`Keep scheduling data somewhere that is not a relational database`),a(`td`,null,[i(`implement `),a(`code`,null,`IJobStore`),i(` directly`)])],-1)])]),l[19]||=e(`<h2 id="decorating-a-store" tabindex="-1"><a class="header-anchor" href="#decorating-a-store"><span>Decorating a store</span></a></h2><p><code>DelegatingJobStore</code> forwards every operation to another store, and every member is <code>virtual</code>:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token keyword">public</span> <span class="token keyword">sealed</span> <span class="token keyword">class</span> <span class="token class-name">MetricsJobStore</span><span class="token punctuation">(</span><span class="token class-name">IJobStore</span> inner<span class="token punctuation">,</span> <span class="token class-name">IMeterFactory</span> meters<span class="token punctuation">)</span> <span class="token punctuation">:</span> <span class="token function">DelegatingJobStore</span><span class="token punctuation">(</span>inner<span class="token punctuation">)</span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    <span class="token keyword">private</span> <span class="token keyword">readonly</span> <span class="token class-name">Histogram<span class="token punctuation">&lt;</span><span class="token keyword">double</span><span class="token punctuation">&gt;</span></span> acquireDuration <span class="token operator">=</span> meters</span>
<span class="line">        <span class="token punctuation">.</span><span class="token function">Create</span><span class="token punctuation">(</span><span class="token string">&quot;App.Quartz&quot;</span><span class="token punctuation">)</span></span>
<span class="line">        <span class="token punctuation">.</span><span class="token generic-method"><span class="token function">CreateHistogram</span><span class="token generic class-name"><span class="token punctuation">&lt;</span><span class="token keyword">double</span><span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span><span class="token string">&quot;app.quartz.acquire.duration&quot;</span><span class="token punctuation">,</span> <span class="token string">&quot;s&quot;</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line">    <span class="token keyword">public</span> <span class="token keyword">override</span> <span class="token keyword">async</span> <span class="token return-type class-name">ValueTask<span class="token punctuation">&lt;</span>List<span class="token punctuation">&lt;</span>IOperableTrigger<span class="token punctuation">&gt;</span><span class="token punctuation">&gt;</span></span> <span class="token function">AcquireNextTriggers</span><span class="token punctuation">(</span></span>
<span class="line">        <span class="token class-name">TriggerAcquisitionRequest</span> request<span class="token punctuation">,</span></span>
<span class="line">        <span class="token class-name">CancellationToken</span> cancellationToken <span class="token operator">=</span> <span class="token keyword">default</span><span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">{</span></span>
<span class="line">        <span class="token class-name"><span class="token keyword">long</span></span> start <span class="token operator">=</span> Stopwatch<span class="token punctuation">.</span><span class="token function">GetTimestamp</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">        <span class="token keyword">try</span></span>
<span class="line">        <span class="token punctuation">{</span></span>
<span class="line">            <span class="token keyword">return</span> <span class="token keyword">await</span> <span class="token keyword">base</span><span class="token punctuation">.</span><span class="token function">AcquireNextTriggers</span><span class="token punctuation">(</span>request<span class="token punctuation">,</span> cancellationToken<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">        <span class="token punctuation">}</span></span>
<span class="line">        <span class="token keyword">finally</span></span>
<span class="line">        <span class="token punctuation">{</span></span>
<span class="line">            acquireDuration<span class="token punctuation">.</span><span class="token function">Record</span><span class="token punctuation">(</span>Stopwatch<span class="token punctuation">.</span><span class="token function">GetElapsedTime</span><span class="token punctuation">(</span>start<span class="token punctuation">)</span><span class="token punctuation">.</span>TotalSeconds<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">        <span class="token punctuation">}</span></span>
<span class="line">    <span class="token punctuation">}</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line">q<span class="token punctuation">.</span><span class="token function">UseJobStore</span><span class="token punctuation">(</span>sp <span class="token operator">=&gt;</span> <span class="token keyword">new</span> <span class="token constructor-invocation class-name">MetricsJobStore</span><span class="token punctuation">(</span></span>
<span class="line">    ActivatorUtilities<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">CreateInstance</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>RAMJobStore<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span>sp<span class="token punctuation">)</span><span class="token punctuation">,</span></span>
<span class="line">    sp<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">GetRequiredService</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>IMeterFactory<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>protected IJobStore InnerJobStore</code> reaches the real store through however many layers are in the way.</p><div class="hint-container tip"><p class="hint-container-title">Tips</p><p>The shipped stores are sealed, and decoration is why. <code>RAMJobStore</code> holds a lock while it mutates several indexes in a fixed order and raises notifications after releasing it — none of which an override can be asked to preserve. Wrap it, and change what you meant to change.</p></div><p>A store that keeps scheduling data somewhere new should implement <code>IJobStore</code> directly rather than derive from this.</p><h2 id="registering-a-store" tabindex="-1"><a class="header-anchor" href="#registering-a-store"><span>Registering a store</span></a></h2><p>Four overloads, all singleton and all keyed by scheduler name for a named scheduler:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line">q<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">UseJobStore</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>MyStore<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>                          <span class="token comment">// container-constructed</span></span>
<span class="line">q<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">UseJobStore</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>MyStore<span class="token punctuation">,</span> MyStoreOptions<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span>o <span class="token operator">=&gt;</span> …<span class="token punctuation">)</span><span class="token punctuation">;</span>    <span class="token comment">// plus its own options type</span></span>
<span class="line">q<span class="token punctuation">.</span><span class="token function">UseJobStore</span><span class="token punctuation">(</span>existingInstance<span class="token punctuation">)</span><span class="token punctuation">;</span>                   <span class="token comment">// one you built</span></span>
<span class="line">q<span class="token punctuation">.</span><span class="token function">UseJobStore</span><span class="token punctuation">(</span>sp <span class="token operator">=&gt;</span> <span class="token keyword">new</span> <span class="token constructor-invocation class-name">MyStore</span><span class="token punctuation">(</span>…<span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>               <span class="token comment">// a factory, e.g. for a decorator</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The generic forms construct the store with <code>ActivatorUtilities</code> through a <em>scheduler-scoped</em> view of the container, so a store written against the scheduler&#39;s own collaborators behaves the same under a named scheduler as under the default one. Take what you need:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token keyword">public</span> <span class="token keyword">sealed</span> <span class="token keyword">class</span> <span class="token class-name">DocumentJobStore</span><span class="token punctuation">(</span></span>
<span class="line">    <span class="token class-name">ISchedulerSignaler</span> signaler<span class="token punctuation">,</span></span>
<span class="line">    <span class="token class-name">ITypeLoader</span> typeLoader<span class="token punctuation">,</span></span>
<span class="line">    <span class="token class-name">TimeProvider</span> timeProvider<span class="token punctuation">,</span></span>
<span class="line">    <span class="token class-name">IObjectSerializer</span> serializer<span class="token punctuation">,</span></span>
<span class="line">    <span class="token class-name">IOptions<span class="token punctuation">&lt;</span>MyStoreOptions<span class="token punctuation">&gt;</span></span> options<span class="token punctuation">,</span></span>
<span class="line">    <span class="token class-name">ILogger<span class="token punctuation">&lt;</span>DocumentJobStore<span class="token punctuation">&gt;</span></span> logger<span class="token punctuation">)</span> <span class="token punctuation">:</span> IJobStore</span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    <span class="token comment">// ...</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><div class="hint-container warning"><p class="hint-container-title">Warning</p><p>Registration is <code>TryAdd</code>, so <strong>first wins</strong>. <code>UseInMemoryStore()</code> and <code>UsePersistentStore(…)</code> register a store too — call <code>UseJobStore&lt;MyStore&gt;()</code> instead of them, not after them.</p><p>A <code>TOptions</code> resolved through <code>IOptions&lt;TOptions&gt;</code> must keep its public parameterless constructor when the application is trimmed.</p></div><h2 id="initialize-and-identity" tabindex="-1"><a class="header-anchor" href="#initialize-and-identity"><span>Initialize and identity</span></a></h2><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token return-type class-name">ValueTask</span> <span class="token function">Initialize</span><span class="token punctuation">(</span><span class="token class-name">SchedulerIdentity</span> identity<span class="token punctuation">,</span> <span class="token class-name">CancellationToken</span> cancellationToken <span class="token operator">=</span> <span class="token keyword">default</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>Nearly everything a store needs — the type loader, the signaler, the time provider — is supplied through its constructor. What remains here is the scheduler&#39;s identity, which is not settled until the container has built the graph, plus work that has to happen before the scheduler runs and cannot be done during construction: verifying a schema, opening a connection, starting a background scan.</p><p><code>SchedulerIdentity</code> carries <code>SchedulerName</code> and <code>InstanceId</code>, both required. <strong>Record the instance id against the firings this node owns</strong>, so <code>QueryFireInstances</code> can say which node is running what.</p><p>It is called once, after the scheduler is built and before plugins initialize.</p><h2 id="the-contract-that-is-easy-to-get-wrong" tabindex="-1"><a class="header-anchor" href="#the-contract-that-is-easy-to-get-wrong"><span>The contract that is easy to get wrong</span></a></h2><h3 id="the-fire-cycle" tabindex="-1"><a class="header-anchor" href="#the-fire-cycle"><span>The fire cycle</span></a></h3><p>Three members run in a fixed sequence, once per acquisition batch:</p><ol><li><strong><code>AcquireNextTriggers(TriggerAcquisitionRequest request, ct)</code></strong> — reserve triggers for this node. Never return a trigger that would fire later than <code>request.NoLaterThan</code>, and never return more than <code>request.MaxCount</code>.</li><li><strong><code>TriggersFired(triggers, ct)</code></strong> — the scheduler is about to run them. <strong>The returned list must be the same length as the input and index-aligned with it.</strong> The caller reads <code>results[i]</code> against <code>triggers[i]</code>. Return <code>TriggerFiredResult.NotFired</code> for a trigger that should not fire after all and <code>TriggerFiredResult.Failed(exception)</code> for one that could not be processed; both are handled, a ragged list is not.</li><li><strong><code>TriggeredJobComplete(trigger, jobDetail, instruction, ct)</code></strong> — the firing is over. This is what releases a <code>[DisallowConcurrentExecution]</code> job&#39;s siblings, and the scheduler calls it even on paths where the job never ran. <code>ReleaseAcquiredTrigger</code> is only for a trigger that was acquired and never fired.</li></ol><p>Also implement <code>TimeSpan GetAcquireRetryDelay(int failureCount)</code>, called when <code>AcquireNextTriggers</code> fails more than once in succession. Return something between 20 milliseconds and 10 minutes.</p><h3 id="trigger-state" tabindex="-1"><a class="header-anchor" href="#trigger-state"><span>Trigger state</span></a></h3><p>Every store keeps its triggers in one vocabulary — <code>StoredTriggerState</code>, nine members — and resolves to the <code>TriggerState</code> callers see through one function, so two stores cannot report different states for the same situation:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token class-name">TriggerState</span> reported <span class="token operator">=</span> TriggerStateResolver<span class="token punctuation">.</span><span class="token function">Resolve</span><span class="token punctuation">(</span>stored<span class="token punctuation">,</span> isExecuting<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>The precedence is <strong><code>None &gt; Error &gt; Paused &gt; Executing &gt; Blocked &gt; Complete &gt; Normal</code></strong>. Paused and error outrank executing because they are the facts an operator has to act on, and both remain true while a previously started execution finishes. Executing outranks blocked so that the trigger which actually started the running job stays distinguishable from the siblings gated behind it.</p><p>Two more rules to inherit rather than reinvent:</p><ul><li>A stored value this version does not recognise reads as <code>Waiting</code>, and is reported as <code>Normal</code> — schedulable.</li><li>A trigger that does not exist reads as <code>Deleted</code>, which resolves to <code>TriggerState.None</code>.</li></ul><p><code>StoredTriggerStates.ToStoredValue()</code> / <code>FromStoredValue()</code> map to and from the persisted strings, and are public for exactly this.</p><h3 id="queries" tabindex="-1"><a class="header-anchor" href="#queries"><span>Queries</span></a></h3><p>The six <code>Query…</code> members are abstract, and three rules keep them consistent with the shipped stores:</p><ul><li><strong>Order by group, then name, ordinal.</strong> Fire instances add fire instance id as a third key, because one trigger can have several firings in flight and group plus name would not order them.</li><li><strong><code>HasMore</code> is exact.</strong> Read one row past <code>Take</code>.</li><li><strong><code>TotalCount</code> only when asked.</strong> <code>Take = 0</code> with <code>IncludeTotalCount = true</code> must skip the row query entirely — that is the counting idiom.</li></ul><p><code>QueryFireInstances</code> answers for the whole cluster if the store keeps firings durably, and for its own process otherwise, which is the whole of an in-memory store&#39;s world. <code>FireInstance.JobKey</code> is <code>null</code> while a firing is only <code>Acquired</code> — the job is not loaded until it starts.</p><h3 id="bulk-members" tabindex="-1"><a class="header-anchor" href="#bulk-members"><span>Bulk members</span></a></h3><p>Many key-set members — <code>PauseJobs(keys)</code>, <code>ResumeTriggers(keys)</code>, <code>DeleteJobs(keys)</code> and so on — have default interface implementations that loop the single-key member. Correct for any store, and one lock or round trip per key. Override the ones your store can do in one pass, and keep the default for the rest.</p><h3 id="two-properties-that-are-answers-not-settings" tabindex="-1"><a class="header-anchor" href="#two-properties-that-are-answers-not-settings"><span>Two properties that are answers, not settings</span></a></h3><p><code>bool Clustered</code> and <code>bool SupportsPersistence</code> are read-only because they describe what the store <em>is</em>. A store that cannot cluster answers <code>false</code> and means it.</p><h2 id="deriving-from-adojobstorebase" tabindex="-1"><a class="header-anchor" href="#deriving-from-adojobstorebase"><span>Deriving from AdoJobStoreBase</span></a></h2><p>If your storage is relational but the <em>transaction</em> model differs — you manage transactions elsewhere, or lock differently — derive from <code>AdoJobStoreBase</code> rather than writing a store. It has exactly two abstract members:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token keyword">protected</span> <span class="token keyword">abstract</span> <span class="token return-type class-name">ValueTask<span class="token punctuation">&lt;</span>ConnectionAndTransactionHolder<span class="token punctuation">&gt;</span></span> <span class="token function">GetLocalTransactionConnection</span><span class="token punctuation">(</span><span class="token class-name">CancellationToken</span> ct <span class="token operator">=</span> <span class="token keyword">default</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line"><span class="token keyword">protected</span> <span class="token keyword">abstract</span> <span class="token return-type class-name">ValueTask<span class="token punctuation">&lt;</span>T<span class="token punctuation">&gt;</span></span> <span class="token generic-method"><span class="token function">ExecuteInLock</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>T<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span></span>
<span class="line">    <span class="token class-name">SchedulerLock<span class="token punctuation">?</span></span> lockKind<span class="token punctuation">,</span></span>
<span class="line">    <span class="token class-name">Func<span class="token punctuation">&lt;</span>ConnectionAndTransactionHolder<span class="token punctuation">,</span> ValueTask<span class="token punctuation">&lt;</span>T<span class="token punctuation">&gt;</span><span class="token punctuation">&gt;</span></span> txCallback<span class="token punctuation">,</span></span>
<span class="line">    <span class="token class-name">CancellationToken</span> ct <span class="token operator">=</span> <span class="token keyword">default</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>LocalTransactionJobStore</code> and <code>ExternalTransactionJobStore</code> are the two shipped answers. An override of <code>GetLocalTransactionConnection</code> has to start with <code>GetEnlistedConnection</code>.</p><p>Four members are <code>protected virtual</code>, and one of them is a real extension point:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token keyword">protected</span> <span class="token keyword">virtual</span> <span class="token return-type class-name">TriggerAcquisitionCriteria</span> <span class="token function">CreateAcquisitionCriteria</span><span class="token punctuation">(</span><span class="token class-name">TriggerAcquisitionRequest</span> request<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>It maps the store-level request onto the criteria the driver delegate reads. Start from the base and return a <code>with</code> copy — the criteria are a record, so <code>with</code> leaves everything the base decided in place:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token keyword">protected</span> <span class="token keyword">override</span> <span class="token return-type class-name">TriggerAcquisitionCriteria</span> <span class="token function">CreateAcquisitionCriteria</span><span class="token punctuation">(</span><span class="token class-name">TriggerAcquisitionRequest</span> request<span class="token punctuation">)</span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    <span class="token class-name">TriggerAcquisitionCriteria</span> criteria <span class="token operator">=</span> <span class="token keyword">base</span><span class="token punctuation">.</span><span class="token function">CreateAcquisitionCriteria</span><span class="token punctuation">(</span>request<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">    <span class="token keyword">return</span> criteria <span class="token keyword">with</span> <span class="token punctuation">{</span> MaxCount <span class="token operator">=</span> Math<span class="token punctuation">.</span><span class="token function">Min</span><span class="token punctuation">(</span>criteria<span class="token punctuation">.</span>MaxCount<span class="token punctuation">,</span> <span class="token keyword">this</span><span class="token punctuation">.</span>nodeBudget<span class="token punctuation">)</span> <span class="token punctuation">}</span><span class="token punctuation">;</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><div class="hint-container warning"><p class="hint-container-title">The MaxCount rule</p><p>An override may <strong>lower</strong> <code>MaxCount</code> but must never raise it above the request&#39;s. The choice between lock-free and locked acquisition was already made from the request before this factory runs, so a raised count is only caught by post-acquisition validation, and the surplus is released and retried — a performance hazard rather than corruption, but a silent one.</p></div><p>It is called once per acquisition <em>attempt</em>, inside the store&#39;s internal retry loop, so an override runs again for every retry rather than once per <code>AcquireNextTriggers</code> call. Anything time-derived is recomputed, which is deliberate.</p><p><code>TriggerAcquisitionCriteria</code> is the designated place for future acquisition filtering, so a property added later will default to &quot;no additional filtering&quot; — an override that starts from <code>base</code> and adjusts one field keeps working.</p><h2 id="rebuilding-jobs-and-triggers" tabindex="-1"><a class="header-anchor" href="#rebuilding-jobs-and-triggers"><span>Rebuilding jobs and triggers</span></a></h2><p>A store that reads its data back has to reconstruct <code>IJobDetail</code> and <code>IOperableTrigger</code>. The two are not symmetric:</p><ul><li><strong>Jobs go through <code>JobBuilder</code>.</strong> <code>JobDetailImpl</code> is internal, so <code>JobBuilder</code> is the only supported construction path — which is what the ADO store does too.</li><li><strong>Triggers can be constructed directly.</strong> <code>Quartz.Impl.Triggers.*TriggerImpl</code> are public, and <code>TriggerBase</code> is public and abstract. Note that three of the five are <code>sealed</code> (<code>CalendarIntervalTriggerImpl</code>, <code>DailyTimeIntervalTriggerImpl</code>, <code>RecurrenceTriggerImpl</code>); only <code>SimpleTriggerImpl</code> and <code>CronTriggerImpl</code> can be subclassed.</li></ul><h2 id="testing-one" tabindex="-1"><a class="header-anchor" href="#testing-one"><span>Testing one</span></a></h2><ul><li><strong>Behaviour</strong>: run a real scheduler over your store with <code>UseJobStore&lt;MyStore&gt;()</code> and assert through <code>IScheduler</code>. That is the only way to exercise the fire cycle&#39;s ordering.</li><li><strong>The contract</strong>: the query rules above — ordering, <code>HasMore</code>, the <code>Take = 0</code> count — are all testable against the store directly, with no scheduler.</li><li><strong>Fault handling</strong>: <code>DelegatingJobStore</code> wrapping <em>your</em> store lets a test make one member fail.</li></ul>`,54),a(`p`,null,[l[8]||=i(`See `,-1),o(m,{to:`/documentation/quartz-4.x/tutorial/testing.html`},{default:n(()=>[...l[7]||=[i(`Testing`,-1)]]),_:1}),l[9]||=i(`.`,-1)]),l[20]||=a(`h2`,{id:`see-also`,tabindex:`-1`},[a(`a`,{class:`header-anchor`,href:`#see-also`},[a(`span`,null,`See also`)])],-1),a(`ul`,null,[a(`li`,null,[o(m,{to:`/documentation/quartz-4.x/tutorial/job-stores.html`},{default:n(()=>[...l[10]||=[i(`Job Stores`,-1)]]),_:1}),l[11]||=i(` — the shipped stores and what they guarantee`,-1)]),a(`li`,null,[o(m,{to:`/documentation/quartz-4.x/how-tos/dialect-delegate.html`},{default:n(()=>[...l[12]||=[i(`A Driver Delegate for a New Database`,-1)]]),_:1}),l[13]||=i(` — the right seam for a relational database`,-1)]),a(`li`,null,[o(m,{to:`/documentation/quartz-4.x/tutorial/querying-jobs-and-triggers.html`},{default:n(()=>[...l[14]||=[i(`Querying Jobs and Triggers`,-1)]]),_:1}),l[15]||=i(` — the query contract, from the caller's side`,-1)])])])}var f=s(u,[[`render`,d]]);export{l as _pageData,f as default};