import{a as e,c as t,d as n,l as r,m as i,p as a,s as o,t as s,u as c}from"./app-BsOuDHI-.js";var l=JSON.parse(`{"path":"/documentation/quartz-4.x/how-tos/custom-job-store.html","title":"A Job Store of Your Own","lang":"en-US","frontmatter":{"title":"A Job Store of Your Own"},"git":{"updatedTime":1788441191000,"contributors":[{"name":"Marko Lahma","username":"","email":"marko.lahma@gmail.com","commits":1},{"name":"Claude Fable 5.1","username":"","email":"noreply@anthropic.com","commits":1}],"changelog":[{"hash":"8dee9f767ead84b6e47c023533864d1d3e3178ae","time":1788441191000,"email":"marko.lahma@gmail.com","author":"Marko Lahma","message":"The release post carries the day 4.0.0 shipped","coAuthors":[{"name":"Claude Fable 5.1","email":"noreply@anthropic.com"}]}]},"filePathRelative":"documentation/quartz-4.x/how-tos/custom-job-store.md"}`),u={name:`custom-job-store.md`};function d(s,l,u,d,f,p){let m=a(`RouteLink`);return n(),o(`div`,null,[l[38]||=e(`h1`,{id:`a-job-store-of-your-own`,tabindex:`-1`},[e(`a`,{class:`header-anchor`,href:`#a-job-store-of-your-own`},[e(`span`,null,`A Job Store of Your Own`)])],-1),l[39]||=e(`p`,null,[e(`code`,null,`IJobStore`),r(` is where scheduling data lives. Quartz ships two implementations — in memory, and ADO.NET over a relational database — and the interface is public so a third can keep it somewhere else: a document database, a key-value store, a service.`)],-1),l[40]||=e(`p`,null,`Before writing one, be clear about which of three jobs you are doing, because they have different answers.`,-1),e(`table`,null,[l[7]||=e(`thead`,null,[e(`tr`,null,[e(`th`,null,`You want to`),e(`th`,null,`Do this`)])],-1),e(`tbody`,null,[l[4]||=e(`tr`,null,[e(`td`,null,`Add behaviour around an existing store — logging, metrics, tenant routing, fault injection`),e(`td`,null,[r(`derive from `),e(`code`,null,`DelegatingJobStore`)])],-1),e(`tr`,null,[l[3]||=e(`td`,null,[r(`Support a `),e(`strong`,null,`relational`),r(` database Quartz does not ship a dialect for`)],-1),e(`td`,null,[l[1]||=r(`write an `,-1),c(m,{to:`/documentation/quartz-4.x/how-tos/dialect-delegate.html`},{default:i(()=>[...l[0]||=[e(`code`,null,`IDriverDelegate`,-1)]]),_:1}),l[2]||=r(`, not a store`,-1)])]),l[5]||=e(`tr`,null,[e(`td`,null,`Keep scheduling data somewhere that is not a relational database`),e(`td`,null,[r(`implement `),e(`code`,null,`IJobStore`),r(` directly`)])],-1),l[6]||=e(`tr`,null,[e(`td`,null,`Manage transactions differently from either shipped ADO.NET store`),e(`td`,null,[r(`implement `),e(`code`,null,`IJobStore`),r(` directly — see `),e(`a`,{href:`#the-ado-net-store-is-not-a-base-class`},`The ADO.NET store is not a base class`)])],-1)])]),l[41]||=t(`<h2 id="decorating-a-store" tabindex="-1"><a class="header-anchor" href="#decorating-a-store"><span>Decorating a store</span></a></h2><p><code>DelegatingJobStore</code> forwards every operation to another store, and every member is <code>virtual</code>:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token keyword">public</span> <span class="token keyword">sealed</span> <span class="token keyword">class</span> <span class="token class-name">MetricsJobStore</span><span class="token punctuation">(</span><span class="token class-name">IJobStore</span> inner<span class="token punctuation">,</span> <span class="token class-name">IMeterFactory</span> meters<span class="token punctuation">)</span> <span class="token punctuation">:</span> <span class="token function">DelegatingJobStore</span><span class="token punctuation">(</span>inner<span class="token punctuation">)</span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    <span class="token keyword">private</span> <span class="token keyword">readonly</span> <span class="token class-name">Histogram<span class="token punctuation">&lt;</span><span class="token keyword">double</span><span class="token punctuation">&gt;</span></span> acquireDuration <span class="token operator">=</span> meters</span>
<span class="line">        <span class="token punctuation">.</span><span class="token function">Create</span><span class="token punctuation">(</span><span class="token string">&quot;App.Quartz&quot;</span><span class="token punctuation">)</span></span>
<span class="line">        <span class="token punctuation">.</span><span class="token generic-method"><span class="token function">CreateHistogram</span><span class="token generic class-name"><span class="token punctuation">&lt;</span><span class="token keyword">double</span><span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span><span class="token string">&quot;app.quartz.acquire.duration&quot;</span><span class="token punctuation">,</span> <span class="token string">&quot;s&quot;</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line">    <span class="token keyword">public</span> <span class="token keyword">override</span> <span class="token keyword">async</span> <span class="token return-type class-name">ValueTask<span class="token punctuation">&lt;</span>List<span class="token punctuation">&lt;</span>IOperableTrigger<span class="token punctuation">&gt;</span><span class="token punctuation">&gt;</span></span> <span class="token function">AcquireNextTriggers</span><span class="token punctuation">(</span></span>
<span class="line">        <span class="token class-name">TriggerAcquisitionRequest</span> request<span class="token punctuation">,</span></span>
<span class="line">        <span class="token class-name">CancellationToken</span> cancellationToken <span class="token operator">=</span> <span class="token keyword">default</span><span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">{</span></span>
<span class="line">        <span class="token class-name"><span class="token keyword">long</span></span> start <span class="token operator">=</span> Stopwatch<span class="token punctuation">.</span><span class="token function">GetTimestamp</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">        <span class="token keyword">try</span></span>
<span class="line">        <span class="token punctuation">{</span></span>
<span class="line">            <span class="token keyword">return</span> <span class="token keyword">await</span> <span class="token keyword">base</span><span class="token punctuation">.</span><span class="token function">AcquireNextTriggers</span><span class="token punctuation">(</span>request<span class="token punctuation">,</span> cancellationToken<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">        <span class="token punctuation">}</span></span>
<span class="line">        <span class="token keyword">finally</span></span>
<span class="line">        <span class="token punctuation">{</span></span>
<span class="line">            acquireDuration<span class="token punctuation">.</span><span class="token function">Record</span><span class="token punctuation">(</span>Stopwatch<span class="token punctuation">.</span><span class="token function">GetElapsedTime</span><span class="token punctuation">(</span>start<span class="token punctuation">)</span><span class="token punctuation">.</span>TotalSeconds<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">        <span class="token punctuation">}</span></span>
<span class="line">    <span class="token punctuation">}</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line">q<span class="token punctuation">.</span><span class="token function">UseJobStore</span><span class="token punctuation">(</span>sp <span class="token operator">=&gt;</span> <span class="token keyword">new</span> <span class="token constructor-invocation class-name">MetricsJobStore</span><span class="token punctuation">(</span></span>
<span class="line">    ActivatorUtilities<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">CreateInstance</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>RAMJobStore<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span>sp<span class="token punctuation">)</span><span class="token punctuation">,</span></span>
<span class="line">    sp<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">GetRequiredService</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>IMeterFactory<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>protected IJobStore InnerJobStore</code> reaches the real store through however many layers are in the way.</p><div class="hint-container tip"><p class="hint-container-title">Tips</p><p>None of the shipped stores can be derived from — <code>RAMJobStore</code> is sealed, the two ADO.NET stores are internal — and decoration is why. <code>RAMJobStore</code> holds a lock while it mutates several indexes in a fixed order and raises notifications after releasing it, none of which an override can be asked to preserve. Wrap it, and change what you meant to change.</p></div><p>A store that keeps scheduling data somewhere new should implement <code>IJobStore</code> directly rather than derive from this.</p><h2 id="registering-a-store" tabindex="-1"><a class="header-anchor" href="#registering-a-store"><span>Registering a store</span></a></h2><p>Four overloads, all singleton and all keyed by scheduler name for a named scheduler:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line">q<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">UseJobStore</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>MyStore<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>                          <span class="token comment">// container-constructed</span></span>
<span class="line">q<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">UseJobStore</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>MyStore<span class="token punctuation">,</span> MyStoreOptions<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span>o <span class="token operator">=&gt;</span> …<span class="token punctuation">)</span><span class="token punctuation">;</span>    <span class="token comment">// plus its own options type</span></span>
<span class="line">q<span class="token punctuation">.</span><span class="token function">UseJobStore</span><span class="token punctuation">(</span>existingInstance<span class="token punctuation">)</span><span class="token punctuation">;</span>                   <span class="token comment">// one you built</span></span>
<span class="line">q<span class="token punctuation">.</span><span class="token function">UseJobStore</span><span class="token punctuation">(</span>sp <span class="token operator">=&gt;</span> <span class="token keyword">new</span> <span class="token constructor-invocation class-name">MyStore</span><span class="token punctuation">(</span>…<span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>               <span class="token comment">// a factory, e.g. for a decorator</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The generic forms construct the store with <code>ActivatorUtilities</code> through a <em>scheduler-scoped</em> view of the container, so a store written against the scheduler&#39;s own collaborators behaves the same under a named scheduler as under the default one. Take what you need:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token keyword">public</span> <span class="token keyword">sealed</span> <span class="token keyword">class</span> <span class="token class-name">DocumentJobStore</span><span class="token punctuation">(</span></span>
<span class="line">    <span class="token class-name">ISchedulerSignaler</span> signaler<span class="token punctuation">,</span></span>
<span class="line">    <span class="token class-name">ITypeLoader</span> typeLoader<span class="token punctuation">,</span></span>
<span class="line">    <span class="token class-name">TimeProvider</span> timeProvider<span class="token punctuation">,</span></span>
<span class="line">    <span class="token class-name">IObjectSerializer</span> serializer<span class="token punctuation">,</span></span>
<span class="line">    <span class="token class-name">IOptions<span class="token punctuation">&lt;</span>MyStoreOptions<span class="token punctuation">&gt;</span></span> options<span class="token punctuation">,</span></span>
<span class="line">    <span class="token class-name">ILogger<span class="token punctuation">&lt;</span>DocumentJobStore<span class="token punctuation">&gt;</span></span> logger<span class="token punctuation">)</span> <span class="token punctuation">:</span> IJobStore</span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    <span class="token comment">// ...</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><div class="hint-container warning"><p class="hint-container-title">Warning</p><p>Registration is <code>TryAdd</code>, so <strong>first wins</strong>. <code>UseInMemoryStore()</code> and <code>UsePersistentStore(…)</code> register a store too — call <code>UseJobStore&lt;MyStore&gt;()</code> instead of them, not after them.</p><p>A <code>TOptions</code> resolved through <code>IOptions&lt;TOptions&gt;</code> must keep its public parameterless constructor when the application is trimmed.</p></div><h2 id="initialize-and-identity" tabindex="-1"><a class="header-anchor" href="#initialize-and-identity"><span>Initialize and identity</span></a></h2><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token return-type class-name">ValueTask</span> <span class="token function">Initialize</span><span class="token punctuation">(</span><span class="token class-name">SchedulerIdentity</span> identity<span class="token punctuation">,</span> <span class="token class-name">CancellationToken</span> cancellationToken <span class="token operator">=</span> <span class="token keyword">default</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>Nearly everything a store needs — the type loader, the signaler, the time provider — is supplied through its constructor. What remains here is the scheduler&#39;s identity, which is not settled until the container has built the graph, plus work that has to happen before the scheduler runs and cannot be done during construction: verifying a schema, opening a connection, starting a background scan.</p><p><code>SchedulerIdentity</code> carries <code>SchedulerName</code> and <code>InstanceId</code>, both required. <strong>Record the instance id against the firings this node owns</strong>, so <code>QueryFireInstances</code> can say which node is running what.</p><p>It is called once, after the scheduler is built and before plugins initialize.</p><h2 id="the-contract-that-is-easy-to-get-wrong" tabindex="-1"><a class="header-anchor" href="#the-contract-that-is-easy-to-get-wrong"><span>The contract that is easy to get wrong</span></a></h2><h3 id="the-fire-cycle" tabindex="-1"><a class="header-anchor" href="#the-fire-cycle"><span>The fire cycle</span></a></h3><p>Three members run in a fixed sequence, once per acquisition batch:</p><ol><li><strong><code>AcquireNextTriggers(TriggerAcquisitionRequest request, ct)</code></strong> — reserve triggers for this node. Never return a trigger that would fire later than <code>request.NoLaterThan</code>, and never return more than <code>request.MaxCount</code>.</li><li><strong><code>TriggersFired(triggers, ct)</code></strong> — the scheduler is about to run them. <strong>The returned list must be the same length as the input and index-aligned with it.</strong> The caller reads <code>results[i]</code> against <code>triggers[i]</code>. Return <code>TriggerFiredResult.NotFired</code> for a trigger that should not fire after all and <code>TriggerFiredResult.Failed(exception)</code> for one that could not be processed; both are handled, a ragged list is not.</li><li><strong><code>TriggeredJobComplete(trigger, jobDetail, instruction, ct)</code></strong> — the firing is over. This is what releases a <code>[DisallowConcurrentExecution]</code> job&#39;s siblings, and the scheduler calls it even on paths where the job never ran. <code>ReleaseAcquiredTrigger</code> is only for a trigger that was acquired and never fired.</li></ol><p>Also implement <code>TimeSpan GetAcquireRetryDelay(int failureCount)</code>, called when <code>AcquireNextTriggers</code> fails more than once in succession. Return something between 20 milliseconds and 10 minutes.</p><h3 id="trigger-state" tabindex="-1"><a class="header-anchor" href="#trigger-state"><span>Trigger state</span></a></h3><p>Every store keeps its triggers in one vocabulary — <code>StoredTriggerState</code>, nine members — and resolves to the <code>TriggerState</code> callers see through one function, so two stores cannot report different states for the same situation:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token class-name">TriggerState</span> reported <span class="token operator">=</span> TriggerStateResolver<span class="token punctuation">.</span><span class="token function">Resolve</span><span class="token punctuation">(</span>stored<span class="token punctuation">,</span> isExecuting<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>The precedence is <strong><code>None &gt; Error &gt; Paused &gt; Executing &gt; Blocked &gt; Complete &gt; Normal</code></strong>. Paused and error outrank executing because they are the facts an operator has to act on, and both remain true while a previously started execution finishes. Executing outranks blocked so that the trigger which actually started the running job stays distinguishable from the siblings gated behind it.</p><p>Two more rules to inherit rather than reinvent:</p><ul><li>A stored value this version does not recognise reads as <code>Waiting</code>, and is reported as <code>Normal</code> — schedulable.</li><li>A trigger that does not exist reads as <code>Deleted</code>, which resolves to <code>TriggerState.None</code>.</li></ul><p><code>StoredTriggerStates.ToStoredValue()</code> / <code>FromStoredValue()</code> map to and from the persisted strings, and are public for exactly this.</p><h3 id="queries" tabindex="-1"><a class="header-anchor" href="#queries"><span>Queries</span></a></h3><p>The six paged <code>Query…</code> members are abstract, and three rules keep them consistent with the shipped stores:</p><ul><li><strong>Order by group, then name, ordinal.</strong> Fire instances add fire instance id as a third key, because one trigger can have several firings in flight and group plus name would not order them.</li><li><strong><code>HasMore</code> is exact.</strong> Read one row past <code>Take</code>.</li><li><strong><code>TotalCount</code> only when asked.</strong> <code>Take = 0</code> with <code>IncludeTotalCount = true</code> must skip the row query entirely — that is the counting idiom.</li></ul><p><code>QueryFireInstances</code> answers for the whole cluster if the store keeps firings durably, and for its own process otherwise, which is the whole of an in-memory store&#39;s world. <code>FireInstance.JobKey</code> is <code>null</code> while a firing is only <code>Acquired</code> — the job is not loaded until it starts.</p><h3 id="cluster-nodes" tabindex="-1"><a class="header-anchor" href="#cluster-nodes"><span>Cluster nodes</span></a></h3><p><code>QueryClusterNodes(ct)</code> lists the scheduler nodes the store knows about, as <code>ClusterNode</code>s. It is not paged — a cluster is a handful of nodes, not a data set — and two rules bind it:</p><ul><li><strong>The current node is always in the list, first, and is the only one with <code>IsCurrentNode = true</code>.</strong> It is listed whether or not the store has a record of it yet. The rest follow by instance id, ordinal.</li><li><strong>A store that keeps no membership answers with that one node</strong>, <code>ClusterNodeState.Alive</code>, with <code>LastCheckInUtc</code> and <code>CheckInInterval</code> both <code>null</code>. That is the honest answer for a store that cannot cluster, and it means a caller never has to branch on <code>Clustered</code> before asking.</li></ul><p>A store that <em>does</em> keep membership reports every node it has a record of, including ones that are dead but not yet swept, and decides <code>State</code> with <strong>the same predicate its own failover pass uses</strong> — write that once and call it from both, so the listing can never disagree with the recovery it predicts. <code>Overdue</code> is a missed check-in and nothing more; <code>Failed</code> is the point at which the store takes the node&#39;s work over.</p><h3 id="bulk-members" tabindex="-1"><a class="header-anchor" href="#bulk-members"><span>Bulk members</span></a></h3><p>Many key-set members — <code>PauseJobs(keys)</code>, <code>ResumeTriggers(keys)</code>, <code>DeleteJobs(keys)</code> and so on — have default interface implementations that loop the single-key member. Correct for any store, and one lock or round trip per key. Override the ones your store can do in one pass, and keep the default for the rest.</p><h3 id="two-properties-that-are-answers-not-settings" tabindex="-1"><a class="header-anchor" href="#two-properties-that-are-answers-not-settings"><span>Two properties that are answers, not settings</span></a></h3><p><code>bool Clustered</code> and <code>bool SupportsPersistence</code> are read-only because they describe what the store <em>is</em>. A store that cannot cluster answers <code>false</code> and means it.</p><h2 id="narrowing-what-a-node-picks-up" tabindex="-1"><a class="header-anchor" href="#narrowing-what-a-node-picks-up"><span>Narrowing what a node picks up</span></a></h2><p>Some decorators want <em>less</em> work rather than different work: a node that takes at most five triggers at a time, or one that declines a whole class of job while a maintenance window is open. Both are decisions about the acquisition <strong>request</strong>, and <code>TriggerAcquisitionRequest</code> is a record — so a <code>DelegatingJobStore</code> rewrites it with <code>with</code> and hands it on:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token keyword">public</span> <span class="token keyword">sealed</span> <span class="token keyword">class</span> <span class="token class-name">BudgetedJobStore</span><span class="token punctuation">(</span><span class="token class-name">IJobStore</span> inner<span class="token punctuation">,</span> <span class="token class-name"><span class="token keyword">int</span></span> nodeBudget<span class="token punctuation">)</span> <span class="token punctuation">:</span> <span class="token function">DelegatingJobStore</span><span class="token punctuation">(</span>inner<span class="token punctuation">)</span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    <span class="token keyword">public</span> <span class="token keyword">override</span> <span class="token return-type class-name">ValueTask<span class="token punctuation">&lt;</span>List<span class="token punctuation">&lt;</span>IOperableTrigger<span class="token punctuation">&gt;</span><span class="token punctuation">&gt;</span></span> <span class="token function">AcquireNextTriggers</span><span class="token punctuation">(</span></span>
<span class="line">        <span class="token class-name">TriggerAcquisitionRequest</span> request<span class="token punctuation">,</span></span>
<span class="line">        <span class="token class-name">CancellationToken</span> cancellationToken <span class="token operator">=</span> <span class="token keyword">default</span><span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">{</span></span>
<span class="line">        <span class="token keyword">return</span> <span class="token keyword">base</span><span class="token punctuation">.</span><span class="token function">AcquireNextTriggers</span><span class="token punctuation">(</span></span>
<span class="line">            request <span class="token keyword">with</span> <span class="token punctuation">{</span> MaxCount <span class="token operator">=</span> Math<span class="token punctuation">.</span><span class="token function">Min</span><span class="token punctuation">(</span>request<span class="token punctuation">.</span>MaxCount<span class="token punctuation">,</span> nodeBudget<span class="token punctuation">)</span> <span class="token punctuation">}</span><span class="token punctuation">,</span></span>
<span class="line">            cancellationToken<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">    <span class="token punctuation">}</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><div class="hint-container warning"><p class="hint-container-title">The MaxCount rule</p><p>An override may <strong>lower</strong> <code>MaxCount</code> but must never raise it above what it was given. The choice between lock-free and locked acquisition is made from the request before the store reads it, so a raised count is caught only by post-acquisition validation: the surplus is released and retried. A performance hazard rather than corruption, but a silent one.</p></div><p><code>AcquireNextTriggers</code> is called once per acquisition attempt, so a decorator runs again for every attempt rather than once per batch. Anything time-derived is recomputed, which is what makes the maintenance-window shape below work without restarting anything.</p><h3 id="excluding-job-types-from-acquisition" tabindex="-1"><a class="header-anchor" href="#excluding-job-types-from-acquisition"><span>Excluding job types from acquisition</span></a></h3><p><code>ExcludedJobTypeNames</code> is how a node declines whole classes of work, and <strong>every shipped store honours the request-level property</strong>: the ADO.NET store threads the names into its driver delegate&#39;s criteria so the rows never leave the database, and <code>RAMJobStore</code> skips the candidate. Rewriting the request is therefore not a post-filter — an excluded job type never occupies one of the <code>MaxCount</code> rows.</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token keyword">public</span> <span class="token keyword">sealed</span> <span class="token keyword">class</span> <span class="token class-name">MaintenanceWindowJobStore</span><span class="token punctuation">(</span><span class="token class-name">IJobStore</span> inner<span class="token punctuation">,</span> <span class="token class-name">IMaintenanceWindow</span> window<span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">:</span> <span class="token function">DelegatingJobStore</span><span class="token punctuation">(</span>inner<span class="token punctuation">)</span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    <span class="token comment">// JobType.FullName is the spelling the store persists - &quot;Namespace.TypeName, AssemblyName&quot;.</span></span>
<span class="line">    <span class="token comment">// Type.FullName carries no assembly name and would never match a stored row.</span></span>
<span class="line">    <span class="token keyword">private</span> <span class="token keyword">static</span> <span class="token keyword">readonly</span> <span class="token class-name"><span class="token keyword">string</span></span> reportingJobTypeName <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token constructor-invocation class-name">JobType</span><span class="token punctuation">(</span><span class="token keyword">typeof</span><span class="token punctuation">(</span><span class="token type-expression class-name">ReportingJob</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">.</span>FullName<span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line">    <span class="token keyword">public</span> <span class="token keyword">override</span> <span class="token return-type class-name">ValueTask<span class="token punctuation">&lt;</span>List<span class="token punctuation">&lt;</span>IOperableTrigger<span class="token punctuation">&gt;</span><span class="token punctuation">&gt;</span></span> <span class="token function">AcquireNextTriggers</span><span class="token punctuation">(</span></span>
<span class="line">        <span class="token class-name">TriggerAcquisitionRequest</span> request<span class="token punctuation">,</span></span>
<span class="line">        <span class="token class-name">CancellationToken</span> cancellationToken <span class="token operator">=</span> <span class="token keyword">default</span><span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">{</span></span>
<span class="line">        <span class="token comment">// Asked again on every acquisition, so a window that opens between two of them takes effect on</span></span>
<span class="line">        <span class="token comment">// the next one without restarting anything.</span></span>
<span class="line">        <span class="token keyword">if</span> <span class="token punctuation">(</span><span class="token operator">!</span>window<span class="token punctuation">.</span>IsOpen<span class="token punctuation">)</span></span>
<span class="line">        <span class="token punctuation">{</span></span>
<span class="line">            <span class="token keyword">return</span> <span class="token keyword">base</span><span class="token punctuation">.</span><span class="token function">AcquireNextTriggers</span><span class="token punctuation">(</span>request<span class="token punctuation">,</span> cancellationToken<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">        <span class="token punctuation">}</span></span>
<span class="line"></span>
<span class="line">        <span class="token keyword">return</span> <span class="token keyword">base</span><span class="token punctuation">.</span><span class="token function">AcquireNextTriggers</span><span class="token punctuation">(</span></span>
<span class="line">            request <span class="token keyword">with</span> <span class="token punctuation">{</span> ExcludedJobTypeNames <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token attribute"><span class="token class-name">reportingJobTypeName</span></span><span class="token punctuation">]</span> <span class="token punctuation">}</span><span class="token punctuation">,</span></span>
<span class="line">            cancellationToken<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">    <span class="token punctuation">}</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Two things to get right:</p><ul><li><strong>Name the type the way the store persists it.</strong> That is <code>JobType.FullName</code> — <code>Namespace.TypeName, AssemblyName</code>, the same string <code>TriggerAcquireResult.JobTypeName</code> carries and the same one the ADO schema keeps in <code>JOB_CLASS_NAME</code>. <code>Type.FullName</code> has no assembly name and will never match a stored row.</li><li><strong>Matching is exact.</strong> There is no prefix or wildcard form. The SQL comparison follows the <code>JOB_CLASS_NAME</code> column&#39;s collation, so its case sensitivity is the database&#39;s, not .NET&#39;s; the in-memory store compares ordinally. Rows written by Quartz 2.x or 3.x can carry an older spelling, and the read side never rewrites a stored name, so an exclusion will not match those.</li></ul><p>Entries must be non-blank and there may be at most 1000 of them, both checked when the request is constructed; 1000 is Oracle&#39;s ceiling on an <code>IN</code> list.</p><h2 id="the-ado-net-store-is-not-a-base-class" tabindex="-1"><a class="header-anchor" href="#the-ado-net-store-is-not-a-base-class"><span>The ADO.NET store is not a base class</span></a></h2><p><code>AdoJobStoreBase</code>, <code>LocalTransactionJobStore</code> and <code>ExternalTransactionJobStore</code> are internal. They are still what <code>quartz.jobStore.type</code> names and still what <code>UsePersistentStore</code> builds, so a configuration file needs no change — but they are not something to derive from, and in code the choice between the two is a call rather than a type argument: <code>UsePersistentStore()</code> gives you the local-transaction store, and <code>store.UseAmbientTransactions()</code> inside its callback gives you the other.</p><p>Deriving from the base was never the seam it looked like. Every <code>protected</code> member below its two abstract ones is the connection-taking twin of the public member above it — <code>AddJob(conn, …)</code> beside <code>AddJob(job, …)</code> — because the public one takes the lock and the twin does the work. Overriding one of those changes half of an operation.</p><p>What to reach for instead depends on what the override did:</p>`,57),e(`table`,null,[l[17]||=e(`thead`,null,[e(`tr`,null,[e(`th`,null,`What you were overriding for`),e(`th`,null,`What to do`)])],-1),e(`tbody`,null,[l[14]||=e(`tr`,null,[e(`td`,null,`Narrowing acquisition`),e(`td`,null,[e(`code`,null,`DelegatingJobStore`),r(`, rewriting the request — see `),e(`a`,{href:`#narrowing-what-a-node-picks-up`},`Narrowing what a node picks up`)])],-1),l[15]||=e(`tr`,null,[e(`td`,null,`Logging, metrics, tenant routing, fault injection`),e(`td`,null,[e(`code`,null,`DelegatingJobStore`),r(` — see `),e(`a`,{href:`#decorating-a-store`},`Decorating a store`)])],-1),e(`tr`,null,[l[9]||=e(`td`,null,`A relational database Quartz ships no dialect for`,-1),e(`td`,null,[c(m,{to:`/documentation/quartz-4.x/how-tos/dialect-delegate.html`},{default:i(()=>[...l[8]||=[r(`A Driver Delegate for a New Database`,-1)]]),_:1})])]),e(`tr`,null,[l[13]||=e(`td`,null,`Classifying one more of your driver's failures as retryable`,-1),e(`td`,null,[l[11]||=e(`code`,null,`AdoJobStoreOptions.IsTransient`,-1),l[12]||=r(` — see `,-1),c(m,{to:`/documentation/quartz-4.x/operations.html#what-counts-as-transient`},{default:i(()=>[...l[10]||=[r(`What counts as transient`,-1)]]),_:1})])]),l[16]||=e(`tr`,null,[e(`td`,null,`A different transaction model from either shipped store`),e(`td`,null,[r(`Implement `),e(`code`,null,`IJobStore`),r(`, which is public and stays public. If the two shipped stores nearly fit, `),e(`a`,{href:`https://github.com/quartznet/quartznet/issues`,target:`_blank`,rel:`noopener noreferrer`},`open an issue`),r(` — that is a gap worth hearing about rather than working around`)])],-1)])]),l[42]||=e(`h2`,{id:`rebuilding-jobs-and-triggers`,tabindex:`-1`},[e(`a`,{class:`header-anchor`,href:`#rebuilding-jobs-and-triggers`},[e(`span`,null,`Rebuilding jobs and triggers`)])],-1),l[43]||=e(`p`,null,[r(`A store that reads its data back has to reconstruct `),e(`code`,null,`IJobDetail`),r(` and `),e(`code`,null,`IOperableTrigger`),r(`. The two are not symmetric:`)],-1),e(`ul`,null,[l[28]||=e(`li`,null,[e(`strong`,null,[r(`Jobs go through `),e(`code`,null,`JobBuilder`),r(`.`)]),r(),e(`code`,null,`JobDetailImpl`),r(` is internal, so `),e(`code`,null,`JobBuilder`),r(` is the only supported construction path — which is what the ADO store does too.`)],-1),e(`li`,null,[l[19]||=e(`strong`,null,`Triggers can be constructed directly.`,-1),l[20]||=r(),l[21]||=e(`code`,null,`Quartz.Impl.Triggers.*TriggerImpl`,-1),l[22]||=r(` are public, and `,-1),l[23]||=e(`code`,null,`TriggerBase`,-1),l[24]||=r(` is public and abstract. All five are subclassable — three of them were sealed during 4.x's development and reopened for exactly this. Pair a subclassed trigger with a serializer derived from that trigger's built-in serializer, which is public and unsealed for the same reason; `,-1),l[25]||=e(`code`,null,`BuiltInTriggerSerializerDerivationTest`,-1),l[26]||=r(` guards both halves, in both JSON packages. See `,-1),c(m,{to:`/documentation/quartz-4.x/how-tos/trigger-persistence-delegate.html`},{default:i(()=>[...l[18]||=[r(`Persisting a Custom Trigger Type`,-1)]]),_:1}),l[27]||=r(`.`,-1)])]),l[44]||=t(`<h2 id="testing-one" tabindex="-1"><a class="header-anchor" href="#testing-one"><span>Testing one</span></a></h2><ul><li><strong>Behaviour</strong>: run a real scheduler over your store with <code>UseJobStore&lt;MyStore&gt;()</code> and assert through <code>IScheduler</code>. That is the only way to exercise the fire cycle&#39;s ordering.</li><li><strong>The contract</strong>: the query rules above — ordering, <code>HasMore</code>, the <code>Take = 0</code> count — are all testable against the store directly, with no scheduler.</li><li><strong>Fault handling</strong>: <code>DelegatingJobStore</code> wrapping <em>your</em> store lets a test make one member fail.</li></ul>`,2),e(`p`,null,[l[30]||=r(`See `,-1),c(m,{to:`/documentation/quartz-4.x/tutorial/testing.html`},{default:i(()=>[...l[29]||=[r(`Testing`,-1)]]),_:1}),l[31]||=r(`.`,-1)]),l[45]||=e(`h2`,{id:`see-also`,tabindex:`-1`},[e(`a`,{class:`header-anchor`,href:`#see-also`},[e(`span`,null,`See also`)])],-1),e(`ul`,null,[e(`li`,null,[c(m,{to:`/documentation/quartz-4.x/tutorial/job-stores.html`},{default:i(()=>[...l[32]||=[r(`Job Stores`,-1)]]),_:1}),l[33]||=r(` — the shipped stores and what they guarantee`,-1)]),e(`li`,null,[c(m,{to:`/documentation/quartz-4.x/how-tos/dialect-delegate.html`},{default:i(()=>[...l[34]||=[r(`A Driver Delegate for a New Database`,-1)]]),_:1}),l[35]||=r(` — the right seam for a relational database`,-1)]),e(`li`,null,[c(m,{to:`/documentation/quartz-4.x/tutorial/querying-jobs-and-triggers.html`},{default:i(()=>[...l[36]||=[r(`Querying Jobs and Triggers`,-1)]]),_:1}),l[37]||=r(` — the query contract, from the caller's side`,-1)])])])}var f=s(u,[[`render`,d]]);export{l as _pageData,f as default};