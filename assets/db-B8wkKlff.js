import{a as e,c as t,d as n,l as r,m as i,p as a,s as o,t as s,u as c}from"./app-BsOuDHI-.js";var l=JSON.parse(`{"path":"/documentation/quartz-4.x/db/","title":"Database Schema","lang":"en-US","frontmatter":{"title":"Database Schema"},"git":{"updatedTime":1788441191000,"contributors":[{"name":"Marko Lahma","username":"","email":"marko.lahma@gmail.com","commits":1},{"name":"Claude Fable 5.1","username":"","email":"noreply@anthropic.com","commits":1}],"changelog":[{"hash":"8dee9f767ead84b6e47c023533864d1d3e3178ae","time":1788441191000,"email":"marko.lahma@gmail.com","author":"Marko Lahma","message":"The release post carries the day 4.0.0 shipped","coAuthors":[{"name":"Claude Fable 5.1","email":"noreply@anthropic.com"}]}]},"filePathRelative":"documentation/quartz-4.x/db/index.md"}`),u={name:`index.md`};function d(s,l,u,d,f,p){let m=a(`RouteLink`);return n(),o(`div`,null,[e(`p`,null,[l[1]||=r(`When using an ADO.NET-based job store (the usual being `,-1),l[2]||=e(`code`,null,`LocalTransactionJobStore`,-1),l[3]||=r(`), Quartz requires a set of tables. Creating them can be automatic: `,-1),l[4]||=e(`code`,null,`ProvisionSchema()`,-1),l[5]||=r(` has the store run the DDL for its own database as it starts and create whatever is missing — see `,-1),c(m,{to:`/documentation/quartz-4.x/tutorial/job-stores.html#creating-the-schema`},{default:i(()=>[...l[0]||=[r(`Creating the schema`,-1)]]),_:1}),l[6]||=r(`. It is opt-in rather than the default, because creating tables needs a permission a production database is often right not to grant. `,-1),l[7]||=e(`strong`,null,`Migrating`,-1),l[8]||=r(` an existing schema is still a manual step, and nothing in Quartz does it for you.`,-1)]),l[67]||=t(`<table><thead><tr><th>Table</th><th>Brief Description</th></tr></thead><tbody><tr><td>qrtz_calendars</td><td>Stores non-standard calendars</td></tr><tr><td>qrtz_job_details</td><td>Stores <code>IJobDetail</code> data</td></tr><tr><td>qrtz_locks</td><td>locks used by quartz</td></tr><tr><td>qrtz_scheduler_state</td><td>stores <code>IScheduler</code> data</td></tr><tr><td>qrtz_triggers</td><td>Stores <code>ITrigger</code> data</td></tr><tr><td>qrtz_cron_triggers</td><td>Stores CRON trigger cron expression</td></tr><tr><td>qrtz_fired_triggers</td><td>triggers that are currently running</td></tr><tr><td>qrtz_blob_triggers</td><td>trigger table with a binary blob data storage</td></tr><tr><td>qrtz_simple_triggers</td><td>data for very simple repeat triggers</td></tr><tr><td>qrtz_simprop_triggers</td><td>Reusable table for custom triggers. <code>ICalendarIntervalTrigger</code>, <code>IDailyTimeIntervalTrigger</code>, and <code>IRecurrenceTrigger</code> use this</td></tr><tr><td>qrtz_paused_trigger_grps</td><td><code>IScheduler.PauseTriggerGroups</code> data</td></tr><tr><td>qrtz_paused_job_grps</td><td><code>IScheduler.PauseJobGroups</code> data — one row per paused job group, so a group paused while it holds nothing is still reported as paused</td></tr></tbody></table><p>The scripts to create these tables for various providers can be found <a href="https://github.com/quartznet/quartznet/tree/main/database/tables" target="_blank" rel="noopener noreferrer">here</a>.</p>`,2),e(`p`,null,[l[10]||=r(`Whichever route you take, the ADO.NET driver itself is a package reference of the application's: `,-1),l[11]||=e(`code`,null,`UsePostgres`,-1),l[12]||=r(` needs `,-1),l[13]||=e(`code`,null,`Npgsql`,-1),l[14]||=r(`, `,-1),l[15]||=e(`code`,null,`UseSqlServer`,-1),l[16]||=r(` needs `,-1),l[17]||=e(`code`,null,`Microsoft.Data.SqlClient`,-1),l[18]||=r(`, and a project missing one compiles and then fails as the store initializes with `,-1),l[19]||=e(`code`,null,`Could not load file or assembly`,-1),l[20]||=r(`. The `,-1),c(m,{to:`/documentation/quartz-4.x/tutorial/job-stores.html#configuring-a-persistent-store`},{default:i(()=>[...l[9]||=[r(`method-to-package table`,-1)]]),_:1}),l[21]||=r(` lists all eight.`,-1)]),e(`p`,null,[l[23]||=r(`Upgrading an existing database instead? See `,-1),c(m,{to:`/documentation/database/schema-changes.html`},{default:i(()=>[...l[22]||=[r(`Database Schema Changes`,-1)]]),_:1}),l[24]||=r(` — upgrading from 3.x to 4.x is `,-1),l[25]||=e(`strong`,null,`mandatory`,-1),l[26]||=r(`, because 4.x no longer probes for the optional 3.x columns.`,-1)]),l[68]||=t(`<h2 id="creating-it-and-why-migrating-it-is-different" tabindex="-1"><a class="header-anchor" href="#creating-it-and-why-migrating-it-is-different"><span>Creating it, and why migrating it is different</span></a></h2><p>A store can create a missing schema; nothing in Quartz upgrades one that already exists. The split is not arbitrary, and two neighbouring libraries show what decides it. <a href="https://docs.hangfire.io/en/latest/configuration/using-sql-server.html" target="_blank" rel="noopener noreferrer">Hangfire</a> does both, on by default — <code>SqlServerStorageOptions.PrepareSchemaIfNecessary</code> and its <code>Hangfire.PostgreSql</code> namesake both default to <code>true</code> — and it can, because its schema records its own version: a <code>Schema</code> table with a single <code>Version</code> column that an incremental install script steps forward one release at a time. Even there the two expensive migrations of the 1.8 release are <a href="https://docs.hangfire.io/en/latest/upgrade-guides/upgrading-to-hangfire-1.8.html" target="_blank" rel="noopener noreferrer">held back</a> behind <code>EnableHeavyMigrations</code>, which is off by default, &quot;to prevent uncontrolled upgrades that may lead to extended downtime or deadlocks&quot;. <a href="https://tickerq.net/docs/entity-framework/migrations" target="_blank" rel="noopener noreferrer">TickerQ</a> takes the opposite route: its schema is Entity Framework Core&#39;s, so the application generates the migrations with <code>dotnet ef migrations add</code> and applies them itself, and the library ships neither DDL nor an apply-at-startup switch. Quartz can only do the first of the two because its tables carry no version marker of any kind — a guarded <code>CREATE TABLE</code> skips a table that is already there without looking inside it, and there is nothing to read that would say which columns that table ought to have by now. Adding such a marker would itself be a migration, so provisioning stops at creation and <a href="https://github.com/quartznet/quartznet/tree/main/database/migrations" target="_blank" rel="noopener noreferrer"><code>database/migrations/</code></a>, whose folder names are the version numbers, is what a deployment pipeline runs.</p><h2 id="columns-4-x-requires" tabindex="-1"><a class="header-anchor" href="#columns-4-x-requires"><span>Columns 4.x requires</span></a></h2><p>These four columns are optional in Quartz.NET 3.x — the scheduler probes for them at startup and disables the corresponding feature when they are missing. <strong>4.x removed those probes and assumes all four exist.</strong></p><table><thead><tr><th>Column</th><th>Table(s)</th><th>Added as optional in</th></tr></thead><tbody><tr><td><code>MISFIRE_ORIG_FIRE_TIME</code></td><td><code>QRTZ_TRIGGERS</code></td><td>3.17</td></tr><tr><td><code>EXECUTION_GROUP</code></td><td><code>QRTZ_TRIGGERS</code>, <code>QRTZ_FIRED_TRIGGERS</code></td><td>3.18</td></tr><tr><td><code>PREFERRED_NODE</code></td><td><code>QRTZ_TRIGGERS</code></td><td>3.19</td></tr><tr><td><code>PREFERRED_NODE_AUTO</code></td><td><code>QRTZ_TRIGGERS</code></td><td>3.19</td></tr></tbody></table><p>4.x also needs two <strong>columns</strong> 3.x never had, <code>RETRY_POLICY</code> and <code>RETRY_ATTEMPT</code> on <code>QRTZ_TRIGGERS</code>, and a <strong>table</strong> 3.x never had, <code>QRTZ_PAUSED_JOB_GRPS</code> — which is what lets a job group be paused while it holds nothing and what makes a job group listing report <code>paused</code> truthfully.</p>`,6),e(`p`,null,[l[28]||=r(`Apply `,-1),l[29]||=e(`a`,{href:`https://github.com/quartznet/quartznet/tree/main/database/migrations/4.0`,target:`_blank`,rel:`noopener noreferrer`},[e(`code`,null,`database/migrations/4.0/`)],-1),l[30]||=r(` to add whichever are missing — it covers the table as well as the columns. Every statement is guarded, so it is safe to run on a database that already has some of them. `,-1),c(m,{to:`/documentation/database/schema-changes.html#version-4-0`},{default:i(()=>[...l[27]||=[r(`Database Schema Changes`,-1)]]),_:1}),l[31]||=r(` lists the whole 3.x → 4.x set.`,-1)]),l[69]||=t(`<h2 id="the-qrtz-triggers-table" tabindex="-1"><a class="header-anchor" href="#the-qrtz-triggers-table"><span>The QRTZ_TRIGGERS table</span></a></h2><p>This table stores the <code>ITrigger</code> data that is shared across all trigger types. What is specific to a type lives in one of the sibling tables — <code>QRTZ_CRON_TRIGGERS</code>, <code>QRTZ_SIMPLE_TRIGGERS</code>, <code>QRTZ_SIMPROP_TRIGGERS</code> or <code>QRTZ_BLOB_TRIGGERS</code> — and <code>TRIGGER_TYPE</code> says which.</p>`,2),e(`table`,null,[l[66]||=e(`thead`,null,[e(`tr`,null,[e(`th`,null,`Column`),e(`th`,null,`Holds`)])],-1),e(`tbody`,null,[l[53]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`SCHED_NAME`)]),e(`td`,null,[r(`The `),e(`code`,null,`Scheduler:InstanceName`),r(` this row belongs to. Every table has it: one database can hold several schedulers.`)])],-1),l[54]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`TRIGGER_NAME`),r(`, `),e(`code`,null,`TRIGGER_GROUP`)]),e(`td`,null,[r(`The `),e(`code`,null,`TriggerKey`),r(`. Together with `),e(`code`,null,`SCHED_NAME`),r(` they are the primary key.`)])],-1),l[55]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`JOB_NAME`),r(`, `),e(`code`,null,`JOB_GROUP`)]),e(`td`,null,[r(`The `),e(`code`,null,`JobKey`),r(` of the job this trigger fires.`)])],-1),l[56]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`DESCRIPTION`)]),e(`td`,null,[e(`code`,null,`ITrigger.Description`),r(`.`)])],-1),l[57]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`NEXT_FIRE_TIME`),r(`, `),e(`code`,null,`PREV_FIRE_TIME`)]),e(`td`,null,`Fire times, as UTC ticks. Null when there is none.`)],-1),l[58]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`PRIORITY`)]),e(`td`,null,[e(`code`,null,`ITrigger.Priority`),r(`, which breaks ties between triggers due at the same instant.`)])],-1),l[59]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`TRIGGER_STATE`)]),e(`td`,null,`The stored state — see the table below.`)],-1),l[60]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`TRIGGER_TYPE`)]),e(`td`,null,[e(`code`,null,`CRON`),r(`, `),e(`code`,null,`SIMPLE`),r(`, `),e(`code`,null,`CAL_INT`),r(`, `),e(`code`,null,`DAILY_I`),r(`, `),e(`code`,null,`RECUR`),r(` or `),e(`code`,null,`BLOB`),r(`; which sibling table holds the rest.`)])],-1),l[61]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`START_TIME`),r(`, `),e(`code`,null,`END_TIME`)]),e(`td`,null,`The window the schedule is in force, as UTC ticks.`)],-1),l[62]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`CALENDAR_NAME`)]),e(`td`,null,[r(`The `),e(`code`,null,`QRTZ_CALENDARS`),r(` entry excluding times from this trigger's schedule, if any.`)])],-1),l[63]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`MISFIRE_INSTR`)]),e(`td`,null,`The misfire instruction, as its numeric value.`)],-1),l[64]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`MISFIRE_ORIG_FIRE_TIME`)]),e(`td`,null,`The fire time a misfire handler moved the trigger away from, so a job can see what it missed.`)],-1),e(`tr`,null,[l[35]||=e(`td`,null,[e(`code`,null,`EXECUTION_GROUP`)],-1),e(`td`,null,[l[33]||=r(`The `,-1),c(m,{to:`/documentation/quartz-4.x/tutorial/execution-groups.html`},{default:i(()=>[...l[32]||=[r(`execution group`,-1)]]),_:1}),l[34]||=r(` this trigger's work belongs to.`,-1)])]),e(`tr`,null,[l[38]||=e(`td`,null,[e(`code`,null,`PREFERRED_NODE`),r(`, `),e(`code`,null,`PREFERRED_NODE_AUTO`)],-1),e(`td`,null,[c(m,{to:`/documentation/quartz-4.x/tutorial/node-affinity.html`},{default:i(()=>[...l[36]||=[r(`Node affinity`,-1)]]),_:1}),l[37]||=r(`: which node should acquire this trigger, and whether it claimed the pin itself.`,-1)])]),e(`tr`,null,[l[52]||=e(`td`,null,[e(`code`,null,`RETRY_POLICY`),r(`, `),e(`code`,null,`RETRY_ATTEMPT`)],-1),e(`td`,null,[l[40]||=r(`The trigger's `,-1),c(m,{to:`/documentation/quartz-4.x/how-tos/retrying-failed-jobs.html`},{default:i(()=>[...l[39]||=[r(`retry policy`,-1)]]),_:1}),l[41]||=r(` in its stored string form, and how many retries of the occurrence being executed have already been made. `,-1),l[42]||=e(`code`,null,`RETRY_POLICY`,-1),l[43]||=r(` is `,-1),l[44]||=e(`code`,null,`NULL`,-1),l[45]||=r(` on a trigger that does not retry, which is the default; `,-1),l[46]||=e(`code`,null,`RETRY_ATTEMPT`,-1),l[47]||=r(` is `,-1),l[48]||=e(`code`,null,`0`,-1),l[49]||=r(` on a row this release wrote and `,-1),l[50]||=e(`code`,null,`NULL`,-1),l[51]||=r(` on one an upgrade brought across, and the store reads both as "no retries behind it".`,-1)])]),l[65]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`JOB_DATA`)]),e(`td`,null,[r(`The trigger's own `),e(`code`,null,`JobDataMap`),r(`, serialized.`)])],-1)])]),l[70]||=t(`<h3 id="trigger-states" tabindex="-1"><a class="header-anchor" href="#trigger-states"><span>Trigger states</span></a></h3><p>The string in <code>TRIGGER_STATE</code> is not the enum an application sees. The stored vocabulary is <a href="https://github.com/quartznet/quartznet/blob/main/src/Quartz/Impl/AdoJobStore/StoredTriggerState.cs" target="_blank" rel="noopener noreferrer"><code>StoredTriggerState</code></a>, in <code>Quartz.Extensibility</code>:</p><table><thead><tr><th><code>TRIGGER_STATE</code></th><th>Meaning</th></tr></thead><tbody><tr><td><code>WAITING</code></td><td>Ready to be picked up when its time comes. This is the ordinary resting state.</td></tr><tr><td><code>ACQUIRED</code></td><td>A node has taken this trigger and is about to fire it.</td></tr><tr><td><code>EXECUTING</code></td><td>Its job is running.</td></tr><tr><td><code>COMPLETE</code></td><td>It will not fire again.</td></tr><tr><td><code>BLOCKED</code></td><td>Its job is <code>[DisallowConcurrentExecution]</code> and another firing of it is in progress.</td></tr><tr><td><code>PAUSED</code></td><td>Paused, and will not fire until resumed.</td></tr><tr><td><code>PAUSED_BLOCKED</code></td><td>Both at once: paused, and blocked by a running firing of the same job.</td></tr><tr><td><code>ERROR</code></td><td>The trigger could not be fired — usually because its job type could not be built. <code>IScheduler.ResetTriggerFromErrorState</code> clears it.</td></tr><tr><td><code>DELETED</code></td><td>A transient marker used while a trigger is being removed.</td></tr></tbody></table><p>What <code>IScheduler.GetTriggerState</code> returns is <a href="https://github.com/quartznet/quartznet/blob/main/src/Quartz/TriggerState.cs" target="_blank" rel="noopener noreferrer"><code>TriggerState</code></a>, which is the API&#39;s shorter vocabulary — <code>Normal</code>, <code>Paused</code>, <code>Complete</code>, <code>Error</code>, <code>Blocked</code>, <code>Executing</code> and <code>None</code> for a trigger that does not exist. The stored states map onto it: <code>WAITING</code> and <code>ACQUIRED</code> both read as <code>Normal</code>, <code>PAUSED_BLOCKED</code> reads as <code>Paused</code>, and <code>DELETED</code> reads as <code>None</code>. A trigger whose job is running reads as <code>Executing</code>, unless the row says it is deleted, in error or paused — those are reported as they stand. <code>TriggerStateResolver.Resolve</code> is that mapping, should you need it in code of your own.</p><h3 id="indexes-and-the-acquisition-index-in-particular" tabindex="-1"><a class="header-anchor" href="#indexes-and-the-acquisition-index-in-particular"><span>Indexes, and the acquisition index in particular</span></a></h3><p>Four indexes ship on <code>QRTZ_TRIGGERS</code>, the same four on every dialect. Three are lookups by key — <code>(SCHED_NAME, JOB_NAME, JOB_GROUP)</code>, <code>(SCHED_NAME, TRIGGER_GROUP, TRIGGER_NAME)</code> and <code>(SCHED_NAME, CALENDAR_NAME)</code>. The fourth is the one <strong>both</strong> trigger sweeps read: acquisition, and misfire recovery with the counting peek that precedes it.</p><p>There used to be a fifth on SQL Server, MySQL, Oracle and Firebird — <code>IDX_QRTZ_T_NFT_ST_MISFIRE</code>, over <code>(SCHED_NAME, MISFIRE_INSTR, NEXT_FIRE_TIME, TRIGGER_STATE)</code>, which PostgreSQL and SQLite never had. 4.0 drops it (<a href="https://github.com/quartznet/quartznet/issues/3656" target="_blank" rel="noopener noreferrer">#3656</a>). It led with <code>MISFIRE_INSTR</code>, the column both misfire statements compare with <code>&lt;&gt; -1</code> and no B-tree can seek past, while those statements filter <code>SCHED_NAME</code> and <code>TRIGGER_STATE</code> by equality and <code>NEXT_FIRE_TIME</code> by range — exactly what the fourth index leads with since it was reshaped. Measured plan by plan on all four engines (<a href="https://github.com/quartznet/quartznet/issues/3608" target="_blank" rel="noopener noreferrer">#3608</a>, <a href="https://github.com/quartznet/quartznet/issues/3656" target="_blank" rel="noopener noreferrer">#3656</a>), no optimizer picks it for either misfire statement; MySQL only appeared to, because <code>MySQLDelegate</code> forced it by name, and that hint now points at the acquisition index. It was write cost with no reader. Upgrading from 3.x drops it in <code>schema_30_to_40_indexes_&lt;dialect&gt;.sql</code>, which is a file of its own because it waits for the last 3.x node to shut down — 3.x <em>does</em> sweep from it.</p><p>That fourth one is <code>IDX_QRTZ_T_NFT_ST</code>, the index both sweeps run on, and it is the only index in the schema whose shape differs by dialect:</p><div class="language-sql line-numbers-mode" data-highlighter="prismjs" data-ext="sql"><pre><code class="language-sql"><span class="line"><span class="token comment">-- SQL Server, PostgreSQL, MySQL, Oracle, SQLite</span></span>
<span class="line"><span class="token punctuation">(</span>SCHED_NAME<span class="token punctuation">,</span> TRIGGER_STATE<span class="token punctuation">,</span> NEXT_FIRE_TIME <span class="token keyword">ASC</span><span class="token punctuation">,</span> PRIORITY <span class="token keyword">DESC</span><span class="token punctuation">,</span> MISFIRE_INSTR<span class="token punctuation">)</span></span>
<span class="line"></span>
<span class="line"><span class="token comment">-- Firebird</span></span>
<span class="line"><span class="token punctuation">(</span>SCHED_NAME<span class="token punctuation">,</span> TRIGGER_STATE<span class="token punctuation">,</span> NEXT_FIRE_TIME<span class="token punctuation">)</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Its last two columns are there for a plan rather than for a predicate (<a href="https://github.com/quartznet/quartznet/issues/3510" target="_blank" rel="noopener noreferrer">#3510</a>). <code>SelectNextTriggerToAcquire</code> orders by <code>NEXT_FIRE_TIME ASC, PRIORITY DESC</code> and every dialect splices its row limit into that statement, so the <code>ORDER BY</code> decides which rows come back at all — the highest-priority trigger of a tied set, which is what <code>RAMJobStore</code> does too. An index whose two directions match lets the engine take the first entry instead of reading every candidate and sorting it:</p><table><thead><tr><th>100,000 triggers, 5,000 due, one acquisition</th><th>SQL Server 2022</th><th>MySQL 8.0</th><th>PostgreSQL 15</th><th>Firebird 4</th></tr></thead><tbody><tr><td>p50, three columns</td><td>21.6 ms</td><td>11.8 ms</td><td>0.89 ms</td><td>14.8 ms</td></tr><tr><td>p50, shipped shape</td><td><strong>0.59 ms</strong></td><td><strong>0.69 ms</strong></td><td>0.74 ms</td><td>14.8 ms</td></tr><tr><td>reads, three columns</td><td>20,395</td><td>15,517</td><td>1,526</td><td>10,025</td></tr><tr><td>reads, shipped shape</td><td><strong>8</strong></td><td><strong>96</strong></td><td><strong>24</strong></td><td>10,025</td></tr><tr><td>index size</td><td>+6.2 %</td><td>+13 %</td><td>+0.4 %</td><td>unchanged</td></tr></tbody></table><p><code>MISFIRE_INSTR</code> is the fifth column because the ordered seek starts at the oldest waiting trigger, and the statement&#39;s lower bound on <code>NEXT_FIRE_TIME</code> sits inside an <code>OR</code> with <code>MISFIRE_INSTR</code>, so it cannot narrow that seek. A backlog of misfired triggers below the acquisition window therefore sits in front of every acquisition, and each backlogged row the seek walks costs a lookup into the table to find out it is not wanted. That column makes the whole disjunction index-resident, so the rows are skipped inside the index: against a 5,000-row backlog on SQL Server, 20,401 logical reads become <strong>84</strong>, and on MySQL 15,071 buffer reads become <strong>117</strong>. The statement is unchanged; the predicate never has to become sargable.</p><p><strong>The one cell where this is slower than the three-column index</strong> is a backlog with almost nothing due: several thousand misfired triggers below the window and a handful of candidates above it costs about 3 ms against 1.5 ms. With several thousand <em>due</em> it is 3 ms against 30 ms, and backlogs drain. The trade is deliberate.</p><p>Three things are worth knowing before you diff an index definition:</p><ul><li><strong>Firebird keeps the three-column index.</strong> Its indexes are ascending or descending as a whole, with no per-column direction, so <code>CREATE INDEX</code> rejects the <code>ASC</code> token outright — and the usual workaround, a computed column holding the negated priority, cannot be indexed there either (<em>attempt to index COMPUTED BY column</em>). Acquisition on Firebird still materialises its candidates and sorts them — the 10,025 index record reads in the table above, where every other engine reads under a hundred — and nothing in the shipped schema fixes that. The one lever available is ordering by <code>NEXT_FIRE_TIME</code> alone, which measures 3.2× faster and 345× cheaper there but changes which trigger of a tied set fires first, so if this is costing you, open an issue with numbers rather than patching your schema.</li><li><strong>MySQL before 8.0.1 and MariaDB before 10.8</strong> parse <code>DESC</code> in an index definition and ignore it. That is harmless and it silently buys nothing.</li><li><strong>Oracle</strong> makes a descending key column into a function-based index, so <code>USER_IND_COLUMNS</code> shows a hidden <code>SYS_NC000nn$</code> at that position with <code>DESC</code> as its direction. Expected, not a defect.</li></ul><p><code>PREFERRED_NODE</code>, which node affinity filters and re-pins on, is in no index at all. That was measured on PostgreSQL 15, SQL Server 2022 and MySQL 8.0 against 100,000 triggers (<a href="https://github.com/quartznet/quartznet/issues/3426" target="_blank" rel="noopener noreferrer">#3426</a>); the harness is <a href="https://github.com/quartznet/quartznet/blob/main/src/Quartz.Benchmark/AcquisitionIndexBenchmark.cs" target="_blank" rel="noopener noreferrer"><code>AcquisitionIndexBenchmark</code></a>, which prints the plans again on demand.</p><p><strong>An index on <code>PREFERRED_NODE</code> does nothing for acquisition.</strong> The node-affinity filter is a disjunction — unpinned, <em>or</em> pinned here, <em>or</em> pinned to a node that has stopped checking in — and no B-tree serves an <code>OR</code>. The plan is unchanged on all three engines with the index in place. Nor does adding it to the acquisition index help: it buys nothing over <code>MISFIRE_INSTR</code> alone on SQL Server and MySQL, and it makes PostgreSQL&#39;s backlog case measurably worse — 660 shared buffers against 408 — since a wider entry is more index pages to walk. What it does change is the failover re-pin, the one <code>UPDATE</code> <code>ClusterRecover</code> issues per dead node: from a full scan (PostgreSQL 8.4 ms, SQL Server 4,319 logical reads, MySQL around 88 ms) to a seek of two or three pages. That statement runs once per node failure, so a permanent write cost on the busiest table in the schema is not a trade the shipped schema makes.</p><p><strong>If you add one anyway</strong>, <code>(SCHED_NAME, PREFERRED_NODE, PREFERRED_NODE_AUTO)</code> is the shape the re-pin wants, and a cluster that fails over often enough for a multi-second recovery to hurt is the case for it. Measure your own schema first: none of this is visible below a few thousand triggers, where every plan reads a handful of pages whatever the indexes are.</p>`,18)])}var f=s(u,[[`render`,d]]);export{l as _pageData,f as default};