import{a as e,c as t,d as n,l as r,m as i,p as a,s as o,t as s,u as c}from"./app-emje7bwI.js";var l=JSON.parse(`{"path":"/documentation/quartz-4.x/how-tos/dialect-delegate.html","title":"A Driver Delegate for a New Database","lang":"en-US","frontmatter":{"title":"A Driver Delegate for a New Database"},"git":{"updatedTime":1790451733000,"contributors":[{"name":"Marko Lahma","username":"","email":"marko.lahma@gmail.com","commits":1},{"name":"Claude Opus 5.5","username":"","email":"noreply@anthropic.com","commits":1}],"changelog":[{"hash":"4a29e014ef1e61227140e96264c49cf64cd3183d","time":1790451733000,"email":"marko.lahma@gmail.com","author":"Marko Lahma","message":"A captured log reaches its column whole on Oracle and SQL Server","coAuthors":[{"name":"Claude Opus 5.5","email":"noreply@anthropic.com"}]}]},"filePathRelative":"documentation/quartz-4.x/how-tos/dialect-delegate.md"}`),u={name:`dialect-delegate.md`};function d(s,l,u,d,f,p){let m=a(`RouteLink`);return n(),o(`div`,null,[l[6]||=t(`<h1 id="a-driver-delegate-for-a-new-database" tabindex="-1"><a class="header-anchor" href="#a-driver-delegate-for-a-new-database"><span>A Driver Delegate for a New Database</span></a></h1><p>Quartz ships driver delegates for SQL Server, PostgreSQL, MySQL, Oracle, SQLite and Firebird. Another database, or one of those behind a provider that behaves differently, needs its own.</p><div class="hint-container tip"><p class="hint-container-title">Start here</p><p><strong>Do not implement <code>IDriverDelegate</code></strong>: nothing in the product does, and most of its roughly hundred and ten members are the same SQL everywhere. Subclass <code>StdAdoDelegate</code> and override what differs. The six shipped dialects override <strong>ten distinct members between them</strong>; Firebird and PostgreSQL need two each.</p></div><h2 id="the-seam" tabindex="-1"><a class="header-anchor" href="#the-seam"><span>The seam</span></a></h2><p><code>StdAdoDelegate</code> is <code>public</code>, unsealed, with a public parameterless constructor:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token keyword">public</span> <span class="token keyword">sealed</span> <span class="token keyword">class</span> <span class="token class-name">MyDatabaseDelegate</span> <span class="token punctuation">:</span> <span class="token type-list"><span class="token class-name">StdAdoDelegate</span></span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    <span class="token comment">// override only what differs</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>These ten members are the dialect contract. Treat its other <code>protected virtual</code> members as private implementation steps.</p><table><thead><tr><th>Member</th><th>Override when</th></tr></thead><tbody><tr><td><code>protected virtual string? SchemaResourceName</code></td><td>you want <code>ProvisionSchema()</code> to create your tables — see <a href="#also-needed-a-dbmetadata-and-a-schema">Also needed</a></td></tr><tr><td><code>protected virtual SqlRowLimit GetRowLimit(int count)</code></td><td>your database can limit the rows a statement returns</td></tr><tr><td><code>protected virtual string GetSelectNextTriggerToAcquireSql(TriggerAcquisitionSqlShape shape)</code></td><td>the acquisition statement needs more than a row limit (MySQL&#39;s index hint is the only shipped case)</td></tr><tr><td><code>protected virtual string GetSelectMisfiredTriggersToRecoverSql(int count)</code></td><td>the same, for the misfire scan; <code>count == -1</code> means &quot;no limit&quot;</td></tr><tr><td><code>protected virtual string GetCountMisfiredTriggersInStateSql()</code></td><td>the counting form needs a different shape</td></tr><tr><td><code>protected virtual string ApplyPaging(string sql, bool takeLimited)</code></td><td><code>OFFSET … FETCH NEXT …</code> is not understood</td></tr><tr><td><code>protected virtual void AddPagingParameters(DbCommand cmd, int skip, int take, bool takeLimited)</code></td><td>your paging clause names the two parameters in a different order</td></tr><tr><td><code>public virtual void AddCommandParameter(DbCommand cmd, string paramName, object? paramValue, Enum? dataType = null, int? size = null)</code></td><td>the provider needs types or sizes set explicitly</td></tr><tr><td><code>public virtual object GetDbBooleanValue(bool value)</code></td><td>there is no boolean column type</td></tr><tr><td><code>public virtual bool GetBooleanFromDbValue(object columnValue)</code></td><td>the same, reading back</td></tr></tbody></table><h3 id="row-limiting" tabindex="-1"><a class="header-anchor" href="#row-limiting"><span>Row limiting</span></a></h3><p>There is no ANSI row limit, so <code>StdAdoDelegate</code> applies none. Trigger acquisition and the misfire scan both ask <code>GetRowLimit</code>:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token comment">// … LIMIT n (PostgreSQL, MySQL, SQLite) — or &quot;ROWS&quot; on Firebird</span></span>
<span class="line"><span class="token keyword">protected</span> <span class="token keyword">override</span> <span class="token return-type class-name">SqlRowLimit</span> <span class="token function">GetRowLimit</span><span class="token punctuation">(</span><span class="token class-name"><span class="token keyword">int</span></span> count<span class="token punctuation">)</span></span>
<span class="line">    <span class="token operator">=&gt;</span> SqlRowLimit<span class="token punctuation">.</span><span class="token function">AtStatementEnd</span><span class="token punctuation">(</span><span class="token string">&quot;LIMIT&quot;</span><span class="token punctuation">,</span> count<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line"><span class="token comment">// SELECT TOP n …                              SqlRowLimit.InProjection(&quot;TOP&quot;, count)</span></span>
<span class="line"><span class="token comment">// SELECT * FROM ( … ) WHERE rownum &lt;= n       SqlRowLimit.InEnclosingSelect(&quot;rownum&quot;, count)</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><ul><li><code>SqlRowLimit</code> names the three places a limit can go; the statement is built with it, not spliced.</li><li><code>count</code> is always at least one: &quot;every row&quot; (<code>-1</code>) arrives as <code>SqlRowLimit.Unlimited</code>.</li><li><code>TriggerAcquisitionSqlShape</code> carries what changes the statement text (the row limit, and how many job-type exclusion terms <code>NOT IN</code> needs). It is also the statement&#39;s cache key, so it holds a bucketed exclusion count, not the exact one.</li><li>Override <code>GetSelectNextTriggerToAcquireSql(shape)</code> only for what a row limit cannot express, as MySQL does for <code>FORCE INDEX</code>, still calling <code>base</code> and leaving the limit alone.</li></ul><h3 id="paging" tabindex="-1"><a class="header-anchor" href="#paging"><span>Paging</span></a></h3><p>The default is ANSI, understood by SQL Server 2012+, Oracle 12c+, PostgreSQL and Firebird 3+:</p><div class="language-sql line-numbers-mode" data-highlighter="prismjs" data-ext="sql"><pre><code class="language-sql"><span class="line"> <span class="token keyword">OFFSET</span> <span class="token variable">@pageSkip</span> <span class="token keyword">ROWS</span> <span class="token keyword">FETCH</span> <span class="token keyword">NEXT</span> <span class="token variable">@pageTake</span> <span class="token keyword">ROWS</span> ONLY</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>MySQL and SQLite override <code>ApplyPaging</code> and <code>AddPagingParameters</code>: their clause names the parameters in the other order, and positional providers bind in statement order. Use <code>AdoConstants.ParameterPageSkip</code> and <code>AdoConstants.ParameterPageTake</code>, prefixed with <code>@</code> in SQL and bare when binding:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token keyword">protected</span> <span class="token keyword">override</span> <span class="token return-type class-name"><span class="token keyword">string</span></span> <span class="token function">ApplyPaging</span><span class="token punctuation">(</span><span class="token class-name"><span class="token keyword">string</span></span> sql<span class="token punctuation">,</span> <span class="token class-name"><span class="token keyword">bool</span></span> takeLimited<span class="token punctuation">)</span></span>
<span class="line">    <span class="token operator">=&gt;</span> takeLimited</span>
<span class="line">        <span class="token punctuation">?</span> sql <span class="token operator">+</span> <span class="token string">&quot; LIMIT @&quot;</span> <span class="token operator">+</span> AdoConstants<span class="token punctuation">.</span>ParameterPageTake <span class="token operator">+</span> <span class="token string">&quot; OFFSET @&quot;</span> <span class="token operator">+</span> AdoConstants<span class="token punctuation">.</span>ParameterPageSkip</span>
<span class="line">        <span class="token punctuation">:</span> sql <span class="token operator">+</span> <span class="token string">&quot; LIMIT -1 OFFSET @&quot;</span> <span class="token operator">+</span> AdoConstants<span class="token punctuation">.</span>ParameterPageSkip<span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line"><span class="token keyword">protected</span> <span class="token keyword">override</span> <span class="token return-type class-name"><span class="token keyword">void</span></span> <span class="token function">AddPagingParameters</span><span class="token punctuation">(</span><span class="token class-name">DbCommand</span> cmd<span class="token punctuation">,</span> <span class="token class-name"><span class="token keyword">int</span></span> skip<span class="token punctuation">,</span> <span class="token class-name"><span class="token keyword">int</span></span> take<span class="token punctuation">,</span> <span class="token class-name"><span class="token keyword">bool</span></span> takeLimited<span class="token punctuation">)</span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    <span class="token keyword">if</span> <span class="token punctuation">(</span>takeLimited<span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">{</span></span>
<span class="line">        <span class="token function">AddCommandParameter</span><span class="token punctuation">(</span>cmd<span class="token punctuation">,</span> AdoConstants<span class="token punctuation">.</span>ParameterPageTake<span class="token punctuation">,</span> take<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">    <span class="token punctuation">}</span></span>
<span class="line"></span>
<span class="line">    <span class="token function">AddCommandParameter</span><span class="token punctuation">(</span>cmd<span class="token punctuation">,</span> AdoConstants<span class="token punctuation">.</span>ParameterPageSkip<span class="token punctuation">,</span> skip<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><ul><li><code>takeLimited</code> is <code>false</code> for an unbounded page (<code>Take = int.MaxValue</code>). Without an offset-only form, MySQL uses a <code>LIMIT</code> of the largest <code>BIGINT UNSIGNED</code>, SQLite <code>LIMIT -1</code>.</li><li>The base passes <code>take</code> as <strong>the page size plus one</strong>; the extra row makes <code>PagedResult&lt;T&gt;.HasMore</code> exact.</li></ul><h3 id="booleans" tabindex="-1"><a class="header-anchor" href="#booleans"><span>Booleans</span></a></h3><p>Oracle has no boolean column type, so its delegate maps both ways:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token keyword">public</span> <span class="token keyword">override</span> <span class="token return-type class-name"><span class="token keyword">object</span></span> <span class="token function">GetDbBooleanValue</span><span class="token punctuation">(</span><span class="token class-name"><span class="token keyword">bool</span></span> booleanValue<span class="token punctuation">)</span> <span class="token operator">=&gt;</span> booleanValue <span class="token punctuation">?</span> <span class="token string">&quot;1&quot;</span> <span class="token punctuation">:</span> <span class="token string">&quot;0&quot;</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line"><span class="token keyword">public</span> <span class="token keyword">override</span> <span class="token return-type class-name"><span class="token keyword">bool</span></span> <span class="token function">GetBooleanFromDbValue</span><span class="token punctuation">(</span><span class="token class-name"><span class="token keyword">object</span></span> columnValue<span class="token punctuation">)</span> <span class="token operator">=&gt;</span> Convert<span class="token punctuation">.</span><span class="token function">ToInt32</span><span class="token punctuation">(</span>columnValue<span class="token punctuation">)</span> <span class="token operator">==</span> <span class="token number">1</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>IS_DURABLE</code>, <code>REQUESTS_RECOVERY</code> and similar columns bind through <code>GetDbBooleanValue</code>; the two must agree.</p><h3 id="parameters" tabindex="-1"><a class="header-anchor" href="#parameters"><span>Parameters</span></a></h3><p><code>AddCommandParameter</code> is the last resort. SQL Server&#39;s delegate converts booleans to <code>1</code>/<code>0</code>, sets <code>size = -1</code> for varbinary, and pins strings to <code>size = 4000</code> so the server does not build a query plan per string length.</p><h2 id="what-the-delegate-cannot-reach" tabindex="-1"><a class="header-anchor" href="#what-the-delegate-cannot-reach"><span>What the delegate cannot reach</span></a></h2><div class="hint-container warning"><p class="hint-container-title">Warning</p><p><code>StdAdoConstants</code> (the SQL text) is <strong>internal</strong>. The schema is the contract, public in <code>AdoConstants</code>.</p></div><ul><li>You cannot write <code>StdAdoConstants.SqlSelectNextTriggerToAcquire</code>. Transform <code>base.GetSelectNextTriggerToAcquireSql(shape)</code> (as MySQL&#39;s <code>.Replace(&quot;{0}TRIGGERS t&quot;, …)</code> does), or write the statement whole.</li><li>You can name the schema: <code>AdoConstants.TableTriggers</code>, <code>AdoConstants.ColumnTriggerName</code>, <code>AdoConstants.StateWaiting</code> and the rest.</li><li><code>{0}</code> is the table-prefix placeholder; <code>protected string ReplaceTablePrefix(string query)</code> substitutes it. Statements from the base still contain it; the caller substitutes.</li></ul><div class="hint-container tip"><p class="hint-container-title">Tips</p><p>&quot;Customize one statement&quot; is not supported. The six SQL hooks cover what differs between databases; the other ~76 statements are inlined <code>ReplaceTablePrefix(StdAdoConstants.X)</code> calls. Override the method that issues the statement instead: every <code>IDriverDelegate</code> member on <code>StdAdoDelegate</code> is <code>virtual</code>, including <code>UpdateTriggerStateFromOtherStateWithNextFireTime</code>, which the lock-free acquisition path uses to claim a trigger. More <code>GetXxxSql()</code> hooks can be added compatibly; ask if you need one.</p></div><div class="hint-container warning"><p class="hint-container-title">The value conversions are not all seams</p><p><code>GetDbBooleanValue</code> / <code>GetBooleanFromDbValue</code> are <code>virtual</code>. <code>GetDbDateTimeValue</code>, <code>GetDateTimeFromDbValue</code>, <code>GetDbTimeSpanValue</code> and <code>GetTimeSpanFromDbValue</code> are not: UTC ticks and whole milliseconds are schema contract, and the preferred-node liveness SQL does tick arithmetic on <code>LAST_CHECKIN_TIME</code> and <code>CHECKIN_INTERVAL</code>, so changing them would silently break failover for pinned triggers. A database that must store <code>DATETIME</code> natively implements <code>IDriverDelegate</code> directly and owns its SQL.</p></div><h2 id="initialization" tabindex="-1"><a class="header-anchor" href="#initialization"><span>Initialization</span></a></h2><p><code>StdAdoDelegate.Initialize(DriverDelegateContext context)</code> is <code>public virtual</code>; no shipped dialect overrides it. Override it only to register extra trigger persistence delegates or capture something from the context, and call <code>base.Initialize(context)</code> first.</p><table><thead><tr><th><code>DriverDelegateContext</code> member</th><th></th></tr></thead><tbody><tr><td><code>TablePrefix</code>, <code>SchedulerName</code>, <code>InstanceId</code></td><td>required</td></tr><tr><td><code>DbProvider</code>, <code>TypeLoader</code></td><td>required</td></tr><tr><td><code>ObjectSerializer</code></td><td>nullable</td></tr><tr><td><code>TriggerPersistenceDelegates</code></td><td>the ones registered for this scheduler</td></tr><tr><td><code>TimeProvider</code></td><td>the scheduler&#39;s clock</td></tr><tr><td><code>CommandTimeout</code></td><td>from <code>AdoJobStoreOptions.CommandTimeout</code></td></tr></tbody></table><p>It arrives after construction because a generated <code>InstanceId</code> does not exist when the container builds the delegate. The base exposes <code>protected string SchedulerName { get; }</code> (which scopes nearly every statement) and <code>protected IDbProvider DbProvider { get; }</code>, so you need no copies.</p><h2 id="registering-it" tabindex="-1"><a class="header-anchor" href="#registering-it"><span>Registering it</span></a></h2><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line">builder<span class="token punctuation">.</span>Services<span class="token punctuation">.</span><span class="token function">AddQuartz</span><span class="token punctuation">(</span>q <span class="token operator">=&gt;</span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    q<span class="token punctuation">.</span><span class="token function">UsePersistentStore</span><span class="token punctuation">(</span>s <span class="token operator">=&gt;</span></span>
<span class="line">    <span class="token punctuation">{</span></span>
<span class="line">        s<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">UseDriverDelegate</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>MyDatabaseDelegate<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">        s<span class="token punctuation">.</span><span class="token function">UseGenericDatabase</span><span class="token punctuation">(</span><span class="token string">&quot;MyProvider&quot;</span><span class="token punctuation">,</span> connectionString<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">    <span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"><span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><div class="hint-container warning"><p class="hint-container-title">Order matters</p><p>Registration is <strong>first-wins</strong> (<code>TryAdd</code>). <code>UseSqlServer</code>, <code>UsePostgres</code> and the rest call <code>UseDriverDelegate&lt;…&gt;()</code> internally, so <code>UseDriverDelegate&lt;MyDatabaseDelegate&gt;()</code> must come <strong>before</strong> the database method or it is silently ignored.</p></div><ul><li>The delegate is built with <code>ActivatorUtilities</code>, so <strong>constructor dependencies work</strong> (an <code>ILogger&lt;MyDatabaseDelegate&gt;</code>, anything in the container).</li><li><code>UseDriverDelegate(factory)</code> takes a delegate you build. The factory&#39;s provider resolves this scheduler&#39;s own parts; an <code>IDriverDelegate</code> registered on <code>Services</code> would be invisible to a named scheduler, which resolves under its own key. <code>UseSerializer</code>, <code>UseLockHandler</code>, <code>UseConnectionProvider</code> and <code>UseTriggerPersistenceDelegate</code> take factories the same way.</li><li>The legacy <code>quartz.jobStore.driverDelegateType</code> key still selects a delegate by type name, even when the store is selected in code.</li></ul><h2 id="also-needed-a-dbmetadata-and-a-schema" tabindex="-1"><a class="header-anchor" href="#also-needed-a-dbmetadata-and-a-schema"><span>Also needed: a DbMetadata and a schema</span></a></h2><ol><li><strong>The delegate</strong> — this page.</li><li><strong>A provider registration.</strong> <code>UseGenericDatabase(provider, connectionString, describeMetadata)</code> takes a <code>Func&lt;DbMetadata&gt;</code> describing the ADO.NET provider: connection, command and parameter types, parameter prefix, and <code>DbType</code> spelling. Provider and delegate are independent; the <code>Use…</code> shortcuts set both.</li><li><strong>DDL.</strong> Copy the closest <code>database/tables/tables_&lt;dialect&gt;.sql</code> and adjust the column types.</li></ol><p>Running the DDL by hand satisfies the default <code>SchemaProvisioning.Validate</code>. For <code>ProvisionSchema()</code> to create the tables:</p><ol><li>Embed the script (<code>&lt;EmbeddedResource Include=&quot;MyDatabase.sql&quot; /&gt;</code>).</li><li>Return its manifest resource name, <code>&lt;RootNamespace&gt;.&lt;folder path&gt;.&lt;file name&gt;</code>, from <code>SchemaResourceName</code>.</li><li>Separate statements with a line reading <code>--;;</code> (a semicolon may be inside a stored-procedure body), and write <code>{0}</code> for the table prefix.</li></ol><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token comment">// The schema script this delegate creates its tables from, embedded beside it with</span></span>
<span class="line"><span class="token comment">// &lt;EmbeddedResource Include=&quot;MyDatabase.sql&quot; /&gt; and named the way the compiler names one:</span></span>
<span class="line"><span class="token comment">// &lt;RootNamespace&gt;.&lt;folder path&gt;.&lt;file name&gt;. Statements are separated by a line reading &quot;--;;&quot;.</span></span>
<span class="line"><span class="token keyword">protected</span> <span class="token keyword">override</span> <span class="token return-type class-name"><span class="token keyword">string</span><span class="token punctuation">?</span></span> SchemaResourceName <span class="token operator">=&gt;</span> <span class="token string">&quot;MyCompany.Quartz.MyDatabase.sql&quot;</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The name is looked up in the delegate type&#39;s assembly, then up its base chain, so a subclass of a shipped dialect inherits its script. Unset, <code>ProvisionSchema()</code> fails with a message naming this member.</p><h2 id="see-also" tabindex="-1"><a class="header-anchor" href="#see-also"><span>See also</span></a></h2>`,44),e(`ul`,null,[e(`li`,null,[c(m,{to:`/documentation/quartz-4.x/tutorial/job-stores.html`},{default:i(()=>[...l[0]||=[r(`Job Stores`,-1)]]),_:1}),l[1]||=r(` — how the ADO store is put together`,-1)]),e(`li`,null,[c(m,{to:`/documentation/quartz-4.x/how-tos/custom-job-store.html`},{default:i(()=>[...l[2]||=[r(`A Job Store of Your Own`,-1)]]),_:1}),l[3]||=r(` — the layer above this one`,-1)]),e(`li`,null,[c(m,{to:`/documentation/quartz-4.x/how-tos/trigger-persistence-delegate.html`},{default:i(()=>[...l[4]||=[r(`Persisting a Custom Trigger Type`,-1)]]),_:1}),l[5]||=r(` — the other delegate seam`,-1)])])])}var f=s(u,[[`render`,d]]);export{l as _pageData,f as default};