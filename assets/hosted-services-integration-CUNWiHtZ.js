import{a as e,c as t,d as n,l as r,m as i,p as a,s as o,t as s,u as c}from"./app-emje7bwI.js";var l=JSON.parse(`{"path":"/documentation/quartz-4.x/packages/hosted-services-integration.html","title":"Hosted Services Integration","lang":"en-US","frontmatter":{"title":"Hosted Services Integration"},"git":{"updatedTime":1790451733000,"contributors":[{"name":"Marko Lahma","username":"","email":"marko.lahma@gmail.com","commits":1},{"name":"Claude Opus 5.5","username":"","email":"noreply@anthropic.com","commits":1}],"changelog":[{"hash":"4a29e014ef1e61227140e96264c49cf64cd3183d","time":1790451733000,"email":"marko.lahma@gmail.com","author":"Marko Lahma","message":"A captured log reaches its column whole on Oracle and SQL Server","coAuthors":[{"name":"Claude Opus 5.5","email":"noreply@anthropic.com"}]}]},"filePathRelative":"documentation/quartz-4.x/packages/hosted-services-integration.md"}`),u={name:`hosted-services-integration.md`};function d(s,l,u,d,f,p){let m=a(`RouteLink`);return n(),o(`div`,null,[l[16]||=t(`<p><a href="https://www.nuget.org/packages/Quartz" target="_blank" rel="noopener noreferrer">Quartz</a> runs schedulers as a <a href="https://docs.microsoft.com/en-us/aspnet/core/fundamentals/host/hosted-services" target="_blank" rel="noopener noreferrer">hosted service</a>, started and stopped with the application.</p><h2 id="installation" tabindex="-1"><a class="header-anchor" href="#installation"><span>Installation</span></a></h2><p>The hosted service is in the core package. The <code>worker</code> and <code>web</code> templates already reference the host; a plain <code>console</code> project needs it added:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh"><pre><code class="language-bash"><span class="line">dotnet <span class="token function">add</span> package Quartz</span>
<span class="line">dotnet <span class="token function">add</span> package Microsoft.Extensions.Hosting</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="using" tabindex="-1"><a class="header-anchor" href="#using"><span>Using</span></a></h2>`,5),e(`p`,null,[l[1]||=r(`Call `,-1),l[2]||=e(`code`,null,`AddQuartzHostedService`,-1),l[3]||=r(` on the host application builder or on `,-1),l[4]||=e(`code`,null,`IServiceCollection`,-1),l[5]||=r(`. Configuring the scheduler, jobs and triggers is covered in `,-1),l[6]||=e(`a`,{href:`microsoft-di-integration`},`Microsoft DI Integration`,-1),l[7]||=r(`; several schedulers in one application in `,-1),c(m,{to:`/documentation/quartz-4.x/packages/multiple-schedulers.html`},{default:i(()=>[...l[0]||=[r(`Multiple Schedulers`,-1)]]),_:1}),l[8]||=r(`.`,-1)]),l[17]||=t(`<ul><li>The hosted service starts every scheduler in the container.</li><li>It resolves them when the host starts, so <code>AddQuartz</code> and <code>AddQuartzHostedService</code> can be called in either order.</li><li>Options apply to every scheduler. Override one by name with <code>AddQuartzHostedService(&quot;SchedulerName&quot;, options =&gt; …)</code>.</li></ul><div class="hint-container warning"><p class="hint-container-title">Warning</p><p><code>AddQuartzHostedService()</code> with no scheduler registered throws at startup. Register one with <code>AddQuartz(...)</code>.</p></div><p><strong>Example program utilizing hosted services configuration</strong></p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token class-name">HostApplicationBuilder</span> builder <span class="token operator">=</span> Host<span class="token punctuation">.</span><span class="token function">CreateApplicationBuilder</span><span class="token punctuation">(</span>args<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line"><span class="token comment">// see Quartz documentation about how to configure different configuration aspects</span></span>
<span class="line">builder<span class="token punctuation">.</span><span class="token function">AddQuartz</span><span class="token punctuation">(</span>q <span class="token operator">=&gt;</span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    <span class="token comment">// your configuration here</span></span>
<span class="line"><span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line"><span class="token comment">// Quartz hosting</span></span>
<span class="line">builder<span class="token punctuation">.</span><span class="token function">AddQuartzHostedService</span><span class="token punctuation">(</span>options <span class="token operator">=&gt;</span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    <span class="token comment">// when shutting down we want jobs to complete gracefully</span></span>
<span class="line">    options<span class="token punctuation">.</span>WaitForJobsToComplete <span class="token operator">=</span> <span class="token boolean">true</span><span class="token punctuation">;</span></span>
<span class="line"><span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line"><span class="token keyword">await</span> builder<span class="token punctuation">.</span><span class="token function">Build</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">RunAsync</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="options" tabindex="-1"><a class="header-anchor" href="#options"><span>Options</span></a></h2><p><code>QuartzHostedServiceOptions</code>:</p><table><thead><tr><th>Option</th><th>Default</th><th>Description</th></tr></thead><tbody><tr><td><code>WaitForJobsToComplete</code></td><td><code>false</code></td><td>Shutdown waits for running jobs to finish</td></tr><tr><td><code>AwaitApplicationStarted</code></td><td><code>true</code></td><td>Scheduler starts only after application startup completes</td></tr><tr><td><code>StartDelay</code></td><td>none</td><td>Extra delay before start; counted from startup completion with <code>AwaitApplicationStarted</code></td></tr><tr><td><code>AutoStart</code></td><td><code>true</code></td><td><code>false</code>: built, initialized and bound, but not started; see <a href="#a-scheduler-the-application-starts-itself">below</a></td></tr></tbody></table><p>Without <code>WaitForJobsToComplete</code>, the host stops while jobs are still running. Whether running jobs are also asked to stop is the scheduler&#39;s <code>ShutdownJobInterruption</code> setting.</p><p>To run code around the lifecycle (a warm-up before start, a drain after stop), derive from <code>QuartzHostedService</code> and register the subclass. <code>StartingAsync</code>, <code>StartedAsync</code>, <code>StoppingAsync</code> and <code>StoppedAsync</code> are virtual; <code>Schedulers</code> lists the schedulers it runs.</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line">builder<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">AddQuartzHostedService</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>WarmUpBeforeSchedulingService<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span>options <span class="token operator">=&gt;</span> options<span class="token punctuation">.</span>WaitForJobsToComplete <span class="token operator">=</span> <span class="token boolean">true</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p><code>builder.AddQuartz(...)</code> is <code>builder.Services.AddQuartz(...)</code> that also reads the <code>Quartz</code> configuration section, so <code>appsettings.json</code> settings apply before your callback. For a section with another name, use the <code>IServiceCollection</code> overload:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line">builder<span class="token punctuation">.</span>Services<span class="token punctuation">.</span><span class="token function">AddQuartz</span><span class="token punctuation">(</span>builder<span class="token punctuation">.</span>Configuration<span class="token punctuation">.</span><span class="token function">GetSection</span><span class="token punctuation">(</span><span class="token string">&quot;Scheduling&quot;</span><span class="token punctuation">)</span><span class="token punctuation">,</span> q <span class="token operator">=&gt;</span> <span class="token punctuation">{</span> <span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>A string argument is a scheduler name. <code>builder.AddQuartz(&quot;reporting&quot;, …)</code> registers scheduler <code>reporting</code> and reads <code>Quartz:Schedulers:reporting</code> when the section describes several. <code>builder.AddQuartzSchedulers()</code> registers one per child of that sub-section.</p><h2 id="a-scheduler-the-application-starts-itself" tabindex="-1"><a class="header-anchor" href="#a-scheduler-the-application-starts-itself"><span>A scheduler the application starts itself</span></a></h2><p>Set <code>AutoStart = false</code> when the application must start the scheduler itself: after its own leader election, after a message bus connects, or when a module comes up late.</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line">builder<span class="token punctuation">.</span><span class="token function">AddQuartz</span><span class="token punctuation">(</span><span class="token string">&quot;reporting&quot;</span><span class="token punctuation">,</span> q <span class="token operator">=&gt;</span> <span class="token punctuation">{</span> <span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line"><span class="token comment">// Built, initialized and bound with the host, but left in Created for the application to start</span></span>
<span class="line">builder<span class="token punctuation">.</span><span class="token function">AddQuartzHostedService</span><span class="token punctuation">(</span><span class="token string">&quot;reporting&quot;</span><span class="token punctuation">,</span> options <span class="token operator">=&gt;</span> options<span class="token punctuation">.</span>AutoStart <span class="token operator">=</span> <span class="token boolean">false</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><ul><li>The scheduler is resolved, initialized and bound when the host starts. <code>ISchedulerRegistry</code>, the dashboard and <code>GET /schedulers</code> see it.</li><li>It stays in <code>Created</code> until something calls <code>scheduler.Start()</code>.</li><li><code>AutoStart</code> overrides <code>AwaitApplicationStarted</code> and <code>StartDelay</code>, which then do not apply.</li><li>Shutdown is unchanged: the hosted service shuts down every scheduler it created, started or not. Not registering the hosted service at all would lose that shutdown handling.</li><li>It is per scheduler: the example defers <code>reporting</code> and leaves the others to start with the host.</li></ul><h2 id="health-checks" tabindex="-1"><a class="header-anchor" href="#health-checks"><span>Health checks</span></a></h2><p>The scheduler health check is in the core <code>Quartz</code> package and registers on the standard <code>IHealthChecksBuilder</code>, so a worker with no web stack can use it.</p><table><thead><tr><th>Status</th><th>When</th></tr></thead><tbody><tr><td><em>Healthy</em></td><td>Running and can reach its store</td></tr><tr><td><em>Degraded</em></td><td>In standby, or in <code>Created</code> with <code>AutoStart = false</code></td></tr><tr><td><em>Unhealthy</em></td><td>Anything else</td></tr></tbody></table><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line">builder<span class="token punctuation">.</span>Services<span class="token punctuation">.</span><span class="token function">AddHealthChecks</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">AddQuartz</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p><code>AddQuartz(&quot;reporting&quot;, q =&gt; q.AddQuartzHealthChecks())</code> registers the same check from a named scheduler&#39;s builder, without repeating the name.</p><p>The check reads each scheduler&#39;s own <code>QuartzHostedServiceOptions</code>. A scheduler in <code>Created</code> that did not opt out of <code>AutoStart</code> is <em>unhealthy</em>, including in an application with no hosted service, where nothing will start it.</p><p>Customize the registration in the callback, for example with tags for separate liveness and readiness probes:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line">builder<span class="token punctuation">.</span>Services<span class="token punctuation">.</span><span class="token function">AddHealthChecks</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">AddQuartz</span><span class="token punctuation">(</span>options <span class="token operator">=&gt;</span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    options<span class="token punctuation">.</span>Name <span class="token operator">=</span> <span class="token string">&quot;quartz-scheduler&quot;</span><span class="token punctuation">;</span>   <span class="token comment">// the default, or quartz-scheduler-&lt;name&gt; for a named scheduler</span></span>
<span class="line">    options<span class="token punctuation">.</span>Tags<span class="token punctuation">.</span><span class="token function">AddRange</span><span class="token punctuation">(</span><span class="token punctuation">[</span><span class="token string">&quot;ready&quot;</span><span class="token punctuation">,</span> <span class="token string">&quot;live&quot;</span><span class="token punctuation">]</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">    options<span class="token punctuation">.</span>FailureStatus <span class="token operator">=</span> HealthStatus<span class="token punctuation">.</span>Unhealthy<span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line">    <span class="token comment">// What a scheduler in standby reports. Degraded by default; say Unhealthy where a standby</span></span>
<span class="line">    <span class="token comment">// node must leave the rotation and there is no HTTP probe to remap the status code at.</span></span>
<span class="line">    options<span class="token punctuation">.</span>StandbyStatus <span class="token operator">=</span> HealthStatus<span class="token punctuation">.</span>Unhealthy<span class="token punctuation">;</span></span>
<span class="line"><span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>QuartzHealthCheckOptions</code> go through the options pipeline: the callback, <code>services.Configure&lt;QuartzHealthCheckOptions&gt;(...)</code> and a bound configuration section are equivalent, in any order.</p><table><thead><tr><th>Option</th><th>Meaning</th></tr></thead><tbody><tr><td><code>Name</code></td><td><code>quartz-scheduler</code>, or <code>quartz-scheduler-&lt;name&gt;</code> for a named scheduler</td></tr><tr><td><code>Tags</code></td><td>Tags for filtering into probes</td></tr><tr><td><code>FailureStatus</code></td><td>What the registration reports when the check fails</td></tr><tr><td><code>StandbyStatus</code></td><td>What a scheduler in standby reports; default <em>degraded</em></td></tr><tr><td><code>StaleFiringTolerance</code></td><td>Overdue-trigger detection; default off (<code>null</code>); see <a href="#saying-that-a-scheduler-has-stopped-firing">below</a></td></tr></tbody></table><p><em>Degraded</em> answers HTTP 200, and a worker project has no endpoint to remap it. Set <code>StandbyStatus = HealthStatus.Unhealthy</code> if standby nodes must leave the rotation. It covers standby only: a scheduler in <code>Created</code> because <code>AutoStart</code> is <code>false</code> still reports <em>degraded</em>.</p><p>A named scheduler has its own check. Register it on the health checks builder or inside <code>AddQuartz</code>:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line">builder<span class="token punctuation">.</span>Services<span class="token punctuation">.</span><span class="token function">AddHealthChecks</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">AddQuartz</span><span class="token punctuation">(</span><span class="token string">&quot;reporting&quot;</span><span class="token punctuation">,</span> options <span class="token operator">=&gt;</span> options<span class="token punctuation">.</span>Tags<span class="token punctuation">.</span><span class="token function">Add</span><span class="token punctuation">(</span><span class="token string">&quot;ready&quot;</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line"><span class="token comment">// or, where the scheduler is configured</span></span>
<span class="line">builder<span class="token punctuation">.</span>Services<span class="token punctuation">.</span><span class="token function">AddQuartz</span><span class="token punctuation">(</span><span class="token string">&quot;reporting&quot;</span><span class="token punctuation">,</span> q <span class="token operator">=&gt;</span> q<span class="token punctuation">.</span><span class="token function">AddQuartzHealthChecks</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Configure its options under its name:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line">builder<span class="token punctuation">.</span>Services<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">Configure</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>QuartzHealthCheckOptions<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span><span class="token string">&quot;reporting&quot;</span><span class="token punctuation">,</span> options <span class="token operator">=&gt;</span> options<span class="token punctuation">.</span>Tags<span class="token punctuation">.</span><span class="token function">Add</span><span class="token punctuation">(</span><span class="token string">&quot;ready&quot;</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div>`,32),e(`p`,null,[l[10]||=r(`Serving the report over HTTP (`,-1),l[11]||=e(`code`,null,`MapHealthChecks`,-1),l[12]||=r(`) and how `,-1),l[13]||=e(`em`,null,`degraded`,-1),l[14]||=r(` maps to a status code are in `,-1),c(m,{to:`/documentation/quartz-4.x/packages/aspnet-core-integration.html#health-checks`},{default:i(()=>[...l[9]||=[r(`ASP.NET Core Integration`,-1)]]),_:1}),l[15]||=r(`.`,-1)]),l[18]||=t(`<h3 id="saying-that-a-scheduler-has-stopped-firing" tabindex="-1"><a class="header-anchor" href="#saying-that-a-scheduler-has-stopped-firing"><span>Saying that a scheduler has stopped firing</span></a></h3><p>The checks above test reachability, not progress. A scheduler with a wedged thread, a full thread pool or an unreleased store lock still reports <code>Running</code>, answers store queries and checks in to its cluster, while firing nothing. Set <code>StaleFiringTolerance</code> to detect this:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line">builder<span class="token punctuation">.</span>Services<span class="token punctuation">.</span><span class="token function">AddHealthChecks</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">AddQuartz</span><span class="token punctuation">(</span>options <span class="token operator">=&gt;</span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    <span class="token comment">// Degraded once a trigger is three misfire thresholds overdue, unhealthy at six.</span></span>
<span class="line">    <span class="token comment">// Off (null) by default: what counts as overdue is the application&#39;s to say.</span></span>
<span class="line">    options<span class="token punctuation">.</span>StaleFiringTolerance <span class="token operator">=</span> <span class="token number">3</span><span class="token punctuation">;</span></span>
<span class="line"><span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><ul><li>The check looks for a schedulable trigger whose fire time is overdue by more than <code>StaleFiringTolerance</code> misfire thresholds: <code>AdoJobStoreOptions.MisfireThreshold</code> or <code>InMemoryJobStoreOptions.MisfireThreshold</code>, or one minute for a custom store that exposes neither.</li><li>Overdue by that much: <em>degraded</em>. Overdue by twice that: <em>unhealthy</em>.</li><li>The report data carries <code>overdueTrigger</code>, <code>overdueSince</code> and <code>overdueBy</code>.</li><li>Start with <code>3</code>, as for <code>ClusterCheckinTolerance</code>. A trigger can be one threshold late before it counts as misfired, and one more sweep late before the misfire handler (whose default interval on the database store is the misfire threshold) reaches it, so the value must be more than one.</li><li>Off by default: a scheduler working down a backlog after a maintenance window is behind but healthy.</li><li>Not evaluated for a scheduler in standby; that verdict comes before the store is queried.</li><li>A scheduler with all triggers paused has nothing schedulable, so it never counts as stalled.</li><li>Left unset, the check costs what it did before.</li></ul><h2 id="shutdown-has-a-budget" tabindex="-1"><a class="header-anchor" href="#shutdown-has-a-budget"><span>Shutdown has a budget</span></a></h2><p>The host&#39;s <code>StopAsync</code> token fires after <code>HostOptions.ShutdownTimeout</code> (default thirty seconds) and bounds the wait for running jobs.</p><ul><li>When it fires, schedulers stop <em>waiting</em>. They still shut down job stores, plugins and listeners, and listeners are still notified. A warning naming the scheduler is logged.</li><li>The deadline does not cancel jobs. <code>QuartzSchedulerOptions.ShutdownJobInterruption</code> (default: never) decides whether a shutting-down scheduler asks them to stop; a job that must end on request watches <code>IJobExecutionContext.CancellationToken</code>.</li><li>With <code>WaitForJobsToComplete = true</code> and jobs that outlive the budget, the host stops with those jobs running and their job store updates unfinished. Raise <code>HostOptions.ShutdownTimeout</code> if that matters more than a prompt stop.</li><li>Several schedulers shut down in parallel and share one budget.</li></ul>`,7)])}var f=s(u,[[`render`,d]]);export{l as _pageData,f as default};