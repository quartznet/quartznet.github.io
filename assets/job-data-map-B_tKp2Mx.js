import{a as e,c as t,d as n,l as r,m as i,p as a,s as o,t as s,u as c}from"./app-emje7bwI.js";var l=JSON.parse(`{"path":"/documentation/quartz-4.x/tutorial/job-data-map.html","title":"Job Data","lang":"en-US","frontmatter":{"title":"Job Data"},"git":{"updatedTime":1790451733000,"contributors":[{"name":"Marko Lahma","username":"","email":"marko.lahma@gmail.com","commits":1},{"name":"Claude Opus 5.5","username":"","email":"noreply@anthropic.com","commits":1}],"changelog":[{"hash":"4a29e014ef1e61227140e96264c49cf64cd3183d","time":1790451733000,"email":"marko.lahma@gmail.com","author":"Marko Lahma","message":"A captured log reaches its column whole on Oracle and SQL Server","coAuthors":[{"name":"Claude Opus 5.5","email":"noreply@anthropic.com"}]}]},"filePathRelative":"documentation/quartz-4.x/tutorial/job-data-map.md"}`),u={name:`job-data-map.md`};function d(s,l,u,d,f,p){let m=a(`RouteLink`);return n(),o(`div`,null,[e(`p`,null,[l[1]||=r(`The full reference for the `,-1),l[2]||=e(`code`,null,`JobDataMap`,-1),l[3]||=r(` introduced in `,-1),c(m,{to:`/documentation/quartz-4.x/tutorial/more-about-jobs.html`},{default:i(()=>[...l[0]||=[r(`More About Jobs`,-1)]]),_:1}),l[4]||=r(`: merge precedence, the typed accessors, `,-1),l[5]||=e(`code`,null,`PutAsString`,-1),l[6]||=r(` formats, persistent storage, and what to keep out.`,-1)]),l[32]||=t(`<h2 id="two-maps-and-a-merge" tabindex="-1"><a class="header-anchor" href="#two-maps-and-a-merge"><span>Two maps and a merge</span></a></h2><table><thead><tr><th>Map</th><th>Stored with</th><th>Use</th></tr></thead><tbody><tr><td><code>IJobDetail.JobDataMap</code></td><td>the job</td><td>the same for every trigger that fires it</td></tr><tr><td><code>ITrigger.JobDataMap</code></td><td>the trigger</td><td>several triggers drive one job with different inputs</td></tr></tbody></table><p><code>IJobExecutionContext.MergedJobDataMap</code> is the job&#39;s map with the trigger&#39;s map laid over it: for the same key, <strong>the trigger wins</strong>. It is built lazily, once per firing. Jobs should read it:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token keyword">public</span> <span class="token keyword">sealed</span> <span class="token keyword">class</span> <span class="token class-name">ReportJob</span> <span class="token punctuation">:</span> <span class="token type-list"><span class="token class-name">IJob</span></span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    <span class="token keyword">public</span> <span class="token return-type class-name">ValueTask</span> <span class="token function">Execute</span><span class="token punctuation">(</span><span class="token class-name">IJobExecutionContext</span> context<span class="token punctuation">,</span> <span class="token class-name">CancellationToken</span> cancellationToken <span class="token operator">=</span> <span class="token keyword">default</span><span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">{</span></span>
<span class="line">        <span class="token class-name">JobDataMap</span> data <span class="token operator">=</span> context<span class="token punctuation">.</span>MergedJobDataMap<span class="token punctuation">;</span></span>
<span class="line">        <span class="token class-name"><span class="token keyword">string</span></span> region <span class="token operator">=</span> data<span class="token punctuation">.</span><span class="token function">GetString</span><span class="token punctuation">(</span><span class="token string">&quot;region&quot;</span><span class="token punctuation">)</span><span class="token operator">!</span><span class="token punctuation">;</span></span>
<span class="line">        <span class="token class-name"><span class="token keyword">int</span></span> lookbackDays <span class="token operator">=</span> data<span class="token punctuation">.</span><span class="token function">GetInt</span><span class="token punctuation">(</span><span class="token string">&quot;lookbackDays&quot;</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">        <span class="token comment">// ...</span></span>
<span class="line"></span>
<span class="line">        <span class="token keyword">return</span> <span class="token keyword">default</span><span class="token punctuation">;</span></span>
<span class="line">    <span class="token punctuation">}</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The merged map is a per-firing copy. Values written into it are not saved to the job&#39;s own map. To persist state across fires, use <a href="#persisting-changes-across-fires"><code>[PersistJobDataAfterExecution]</code></a> on the job&#39;s own map.</p><div class="hint-container warning"><p class="hint-container-title">Changed in 4.x</p><p>The scheduler context is <strong>no longer merged into the per-fire map</strong>. In 3.x <code>context.MergedJobDataMap</code> also carried everything in <code>SchedulerContext</code>, so a scheduler-wide key could silently shadow a job&#39;s own key, or be shadowed by it. The merge is now job over trigger and nothing else; read scheduler-wide values from <code>context.Scheduler.Context</code>.</p></div><h2 id="putting-values-in" tabindex="-1"><a class="header-anchor" href="#putting-values-in"><span>Putting values in</span></a></h2><p><code>JobBuilder&lt;TJob&gt;</code> and <code>TriggerBuilder&lt;TJob&gt;</code> have the same three <code>UsingJobData</code> shapes:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token class-name">IJobDetail</span> job <span class="token operator">=</span> JobBuilder<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">Create</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>ReportJob<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span><span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">.</span><span class="token function">WithIdentity</span><span class="token punctuation">(</span><span class="token string">&quot;nightly&quot;</span><span class="token punctuation">,</span> <span class="token string">&quot;reports&quot;</span><span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">.</span><span class="token function">UsingJobData</span><span class="token punctuation">(</span><span class="token string">&quot;region&quot;</span><span class="token punctuation">,</span> <span class="token string">&quot;emea&quot;</span><span class="token punctuation">)</span>                     <span class="token comment">// key and value</span></span>
<span class="line">    <span class="token punctuation">.</span><span class="token function">UsingJobData</span><span class="token punctuation">(</span>j <span class="token operator">=&gt;</span> j<span class="token punctuation">.</span>LookbackDays<span class="token punctuation">,</span> <span class="token number">30</span><span class="token punctuation">)</span>              <span class="token comment">// name the property, not the key</span></span>
<span class="line">    <span class="token punctuation">.</span><span class="token function">UsingJobData</span><span class="token punctuation">(</span>existingMap<span class="token punctuation">)</span>                          <span class="token comment">// merge a whole map in</span></span>
<span class="line">    <span class="token punctuation">.</span><span class="token function">Build</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>UsingJobData(j =&gt; j.LookbackDays, 30)</code> uses the property&#39;s name as the key and its type for the value, so a rename or a type change is a compile error instead of a silent no-op at fire time. It pairs with <a href="#property-injection-the-other-read-side">property injection</a>.</p><p>Data for a single firing does not need a trigger:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token keyword">await</span> scheduler<span class="token punctuation">.</span><span class="token function">TriggerJob</span><span class="token punctuation">(</span>jobKey<span class="token punctuation">,</span> <span class="token keyword">new</span> <span class="token constructor-invocation class-name">JobDataMap</span> <span class="token punctuation">{</span> <span class="token punctuation">[</span><span class="token string">&quot;reason&quot;</span><span class="token punctuation">]</span> <span class="token operator">=</span> <span class="token string">&quot;manual re-run&quot;</span> <span class="token punctuation">}</span><span class="token punctuation">,</span> cancellationToken<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><h2 id="the-read-side-typed-accessors" tabindex="-1"><a class="header-anchor" href="#the-read-side-typed-accessors"><span>The read side: typed accessors</span></a></h2><p><code>JobDataMap</code> implements <code>IDictionary&lt;string, object?&gt;</code>: indexer, <code>TryGetValue</code>, <code>ContainsKey</code>, <code>Remove</code>, <code>Count</code>, <code>Keys</code>, <code>Values</code>, <code>Clear</code>, plus <code>ContainsValue</code> and <code>IsEmpty</code>. It also has 17 typed accessors, as extension members on <code>DataMapExtensions</code>, in two families that read values the same way.</p><h3 id="the-seven-named-types" tabindex="-1"><a class="header-anchor" href="#the-seven-named-types"><span>The seven named types</span></a></h3><table><thead><tr><th>Family</th><th>Members</th><th>Missing or unreadable value</th></tr></thead><tbody><tr><td>throwing readers</td><td><code>GetInt</code>, <code>GetLong</code>, <code>GetFloat</code>, <code>GetDouble</code>, <code>GetBoolean</code>, <code>GetString</code>, <code>GetDateTimeOffset</code></td><td>throws</td></tr><tr><td>try readers</td><td><code>TryGetInt</code>, <code>TryGetLong</code>, <code>TryGetFloat</code>, <code>TryGetDouble</code>, <code>TryGetBoolean</code>, <code>TryGetString</code>, <code>TryGetDateTimeOffset</code></td><td>returns <code>false</code></td></tr></tbody></table><p>Each accepts the value <strong>as its own type or as an invariant-culture string</strong>:</p><ol><li>the stored type is matched first;</li><li>a string is parsed with <code>CultureInfo.InvariantCulture</code>;</li><li>any other stored type falls back to <code>Convert</code> semantics.</li></ol><p>So the same job code works whether the store kept <code>30</code> as an <code>int</code> or as <code>&quot;30&quot;</code>.</p><p><code>GetString</code> returns <code>string?</code> instead of throwing on a missing key; the others throw. Use the <code>TryGet…</code> form when the key is optional. Neither form is faster.</p><h3 id="the-three-generic-readers-for-every-other-type" tabindex="-1"><a class="header-anchor" href="#the-three-generic-readers-for-every-other-type"><span>The three generic readers, for every other type</span></a></h3><p>For a type the seven do not name (a <code>Guid</code>, a <code>TimeSpan</code>, a <code>decimal</code>, a <code>DateOnly</code>, an enum, or a class of your own), use a generic reader:</p><table><thead><tr><th>Accessor</th><th>Entry missing</th><th>Entry unreadable as <code>T</code></th><th>Entry readable as <code>T</code></th></tr></thead><tbody><tr><td><code>TryGet&lt;T&gt;(key, out T value)</code></td><td><code>false</code></td><td><code>false</code></td><td><code>true</code>, value out</td></tr><tr><td><code>Get&lt;T&gt;(key)</code></td><td><code>KeyNotFoundException</code></td><td><code>InvalidCastException</code> naming both types</td><td>the value</td></tr><tr><td><code>GetValueOrDefault&lt;T&gt;(key, defaultValue)</code></td><td><code>defaultValue</code></td><td><code>defaultValue</code></td><td>the value</td></tr></tbody></table><p>&quot;Readable&quot; means: the stored type first, then the invariant string form for every type <code>PutAsString</code> writes, and an enum by name, case-insensitively. <code>Get&lt;Guid&gt;(&quot;batchId&quot;)</code> reads a <code>Guid</code> stored as a string, <code>Get&lt;TimeSpan&gt;(&quot;window&quot;)</code> reads <code>&quot;06:00:00&quot;</code>, and <code>Get&lt;DayOfWeek&gt;(&quot;day&quot;)</code> reads <code>&quot;Monday&quot;</code>. A type with no Quartz string form, such as your own class, is a plain type test.</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token comment">// False when the entry is missing and when it holds something else.</span></span>
<span class="line"><span class="token keyword">if</span> <span class="token punctuation">(</span>data<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">TryGet</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>ReportOptions<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span><span class="token string">&quot;options&quot;</span><span class="token punctuation">,</span> <span class="token keyword">out</span> <span class="token class-name">ReportOptions<span class="token punctuation">?</span></span> options<span class="token punctuation">)</span><span class="token punctuation">)</span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    <span class="token comment">// ...</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span>
<span class="line"><span class="token comment">// Throws KeyNotFoundException for a missing entry, InvalidCastException for a wrong one -</span></span>
<span class="line"><span class="token comment">// the two mistakes told apart, where TryGet answers false to both.</span></span>
<span class="line"><span class="token class-name">ReportOptions</span> required <span class="token operator">=</span> data<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">Get</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>ReportOptions<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span><span class="token string">&quot;options&quot;</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line"><span class="token comment">// Neither throws nor distinguishes: missing and wrong-typed both give the fallback.</span></span>
<span class="line"><span class="token class-name">ReportOptions</span> effective <span class="token operator">=</span> data<span class="token punctuation">.</span><span class="token function">GetValueOrDefault</span><span class="token punctuation">(</span><span class="token string">&quot;options&quot;</span><span class="token punctuation">,</span> <span class="token keyword">new</span> <span class="token constructor-invocation class-name">ReportOptions</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><ul><li>Use <code>Get&lt;T&gt;</code> when the entry is required: it tells a missing entry from a wrong one.</li><li>Do not use <code>GetValueOrDefault&lt;T&gt;</code> where a mistyped key must be noticed: it returns the fallback for both.</li></ul><div class="hint-container tip"><p class="hint-container-title">Tips</p><p><code>SchedulerContext</code> has the same readers. The <code>PutAsString</code> writers belong to <code>JobDataMap</code> only, because they take part in its change tracking.</p></div><div class="hint-container warning"><p class="hint-container-title">Changed in 4.x</p><p>The <code>Get*Value</code> / <code>Get*ValueFromString</code> accessor pairs are gone, and so are the nullable getters (<code>GetNullableInt</code> and friends). One <code>Get…</code>/<code>TryGet…</code> pair per type replaces both. The accessors also moved off <code>StringKeyDirtyFlagMap</code>, which is internal now along with <code>DirtyFlagMap</code>. Call sites are unchanged (<code>map.GetString(…)</code> still compiles), but nothing should name the old types, and the <code>Quartz.Util</code> namespace is gone.</p><p>The less common named accessors became <code>Get&lt;T&gt;</code>, which now does the coercion:</p><table><thead><tr><th>3.x</th><th>4.x</th></tr></thead><tbody><tr><td><code>GetGuid</code></td><td><code>Get&lt;Guid&gt;</code></td></tr><tr><td><code>GetTimeSpan</code></td><td><code>Get&lt;TimeSpan&gt;</code></td></tr><tr><td><code>GetDecimal</code></td><td><code>Get&lt;decimal&gt;</code></td></tr><tr><td><code>GetChar</code></td><td><code>Get&lt;char&gt;</code></td></tr><tr><td><code>GetDateTime</code></td><td><code>Get&lt;DateTime&gt;</code></td></tr><tr><td><code>GetDateOnly</code></td><td><code>Get&lt;DateOnly&gt;</code></td></tr><tr><td><code>GetTimeOnly</code></td><td><code>Get&lt;TimeOnly&gt;</code></td></tr><tr><td><code>GetEnum&lt;T&gt;</code></td><td><code>Get&lt;T&gt;</code></td></tr></tbody></table><p>Each <code>TryGet…</code> changed likewise. Reading is unchanged, including the string forms <code>PutAsString</code> writes.</p></div><h2 id="storing-values-as-strings" tabindex="-1"><a class="header-anchor" href="#storing-values-as-strings"><span>Storing values as strings</span></a></h2><p><code>PutAsString</code> writes a value in a form any store can keep:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token class-name">JobDataMap</span> data <span class="token operator">=</span> <span class="token keyword">new</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">data<span class="token punctuation">.</span><span class="token function">PutAsString</span><span class="token punctuation">(</span><span class="token string">&quot;runAt&quot;</span><span class="token punctuation">,</span> DateTimeOffset<span class="token punctuation">.</span>UtcNow<span class="token punctuation">)</span><span class="token punctuation">;</span>   <span class="token comment">// &quot;O&quot;: 2026-08-22T09:15:00.0000000+00:00</span></span>
<span class="line">data<span class="token punctuation">.</span><span class="token function">PutAsString</span><span class="token punctuation">(</span><span class="token string">&quot;window&quot;</span><span class="token punctuation">,</span> TimeSpan<span class="token punctuation">.</span><span class="token function">FromHours</span><span class="token punctuation">(</span><span class="token number">6</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>  <span class="token comment">// invariant &quot;06:00:00&quot;</span></span>
<span class="line">data<span class="token punctuation">.</span><span class="token function">PutAsString</span><span class="token punctuation">(</span><span class="token string">&quot;batchId&quot;</span><span class="token punctuation">,</span> Guid<span class="token punctuation">.</span><span class="token function">NewGuid</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">data<span class="token punctuation">.</span><span class="token function">PutAsString</span><span class="token punctuation">(</span><span class="token string">&quot;lookbackDays&quot;</span><span class="token punctuation">,</span> <span class="token number">30</span><span class="token punctuation">)</span><span class="token punctuation">;</span>               <span class="token comment">// any IFormattable</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><table><thead><tr><th>Overload</th><th>Written as</th></tr></thead><tbody><tr><td><code>PutAsString(string, DateTime)</code></td><td>round-trip <code>&quot;O&quot;</code>, invariant</td></tr><tr><td><code>PutAsString(string, DateTimeOffset)</code></td><td>round-trip <code>&quot;O&quot;</code>, invariant</td></tr><tr><td><code>PutAsString(string, DateOnly)</code></td><td>round-trip <code>&quot;O&quot;</code>: <code>yyyy-MM-dd</code></td></tr><tr><td><code>PutAsString(string, TimeOnly)</code></td><td>round-trip <code>&quot;O&quot;</code></td></tr><tr><td><code>PutAsString(string, TimeSpan)</code></td><td>invariant default format</td></tr><tr><td><code>PutAsString(string, Guid)</code></td><td>invariant default format</td></tr><tr><td><code>PutAsString(string, bool)</code></td><td>invariant default format</td></tr><tr><td><code>PutAsString(string, char)</code></td><td>invariant default format</td></tr><tr><td><code>PutAsString&lt;T&gt;(string, T) where T : IFormattable</code></td><td>invariant, default format</td></tr></tbody></table><p>Each round-trips through the matching accessor. <code>PutAsString(&quot;runAt&quot;, offset)</code> then <code>GetDateTimeOffset(&quot;runAt&quot;)</code> returns the same instant and offset. <code>Get&lt;DateTime&gt;</code> parses with round-trip semantics, so a <code>DateTime</code> written as <code>&quot;O&quot;</code> keeps its original <code>Kind</code> instead of becoming an unspecified local time. <code>Get&lt;Guid&gt;</code>, <code>Get&lt;TimeSpan&gt;</code>, <code>Get&lt;DateOnly&gt;</code> and <code>Get&lt;TimeOnly&gt;</code> read what <code>PutAsString</code> wrote.</p><h2 id="why-string-safe-storage-matters" tabindex="-1"><a class="header-anchor" href="#why-string-safe-storage-matters"><span>Why string-safe storage matters</span></a></h2><p>A persistent store reads job data back through the serializer or, in string mode, as strings.</p><p><strong>The serializer.</strong> By default a persistent store serializes the whole map.</p><ul><li>Every value must be serializable by the configured serializer.</li><li>A value whose type changes shape must stay readable by the new version. A renamed property on a stored options class makes the job throw on its next fire, possibly months after the deploy. Standard framework types are safe; your own types are a versioning commitment.</li><li>A persistent store accepts the types the accessors cover (<code>string</code>, <code>bool</code>, <code>char</code>, the numeric types, <code>DateTime</code>, <code>DateTimeOffset</code>, <code>TimeSpan</code>, <code>Guid</code>, <code>DateOnly</code>, <code>TimeOnly</code> and enums) plus <code>Dictionary&lt;string, string&gt;</code>. It refuses anything else when the job is stored, instead of writing a blob that fails to load later.</li><li>Both serializers refuse the same set and write it the same way, so you can switch between them.</li><li>A string map&#39;s entries cannot use the name <code>$type</code>. Json.NET writes a value&#39;s type there, and both readers take it as metadata, so a map with an entry under it is refused.</li></ul><p>To store a type of your own, do one of:</p><ul><li>declare it with <code>SystemTextJsonSerializerRegistry.AddTypeInfoResolver</code> on the default serializer (the same registration a trimmed or native-AOT publish needs);</li><li>declare it with <code>NewtonsoftJsonSerializerRegistry.AddJobDataValueType&lt;T&gt;()</code> on the Newtonsoft serializer;</li><li>serialize it yourself and store the string.</li></ul><p>A declared type is read back only by the serializer that wrote it, so the string is the portable choice.</p>`,40),e(`p`,null,[l[8]||=e(`strong`,null,`String mode.`,-1),l[9]||=r(),l[10]||=e(`code`,null,`AdoJobStoreOptions.StoreJobDataAsStrings`,-1),l[11]||=r(` (flat key `,-1),l[12]||=e(`code`,null,`quartz.jobStore.useProperties`,-1),l[13]||=r(`) stores the map as name/value string pairs instead of a serialized blob. Its other store options are in the `,-1),c(m,{to:`/documentation/quartz-4.x/configuration/reference.html`},{default:i(()=>[...l[7]||=[r(`Configuration Reference`,-1)]]),_:1}),l[14]||=r(`.`,-1)]),l[33]||=t(`<div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line">q<span class="token punctuation">.</span><span class="token function">UsePersistentStore</span><span class="token punctuation">(</span>s <span class="token operator">=&gt;</span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    s<span class="token punctuation">.</span><span class="token function">UseSqlServer</span><span class="token punctuation">(</span>connectionString<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">    s<span class="token punctuation">.</span><span class="token function">ConfigureStore</span><span class="token punctuation">(</span>o <span class="token operator">=&gt;</span> o<span class="token punctuation">.</span>StoreJobDataAsStrings <span class="token operator">=</span> <span class="token boolean">true</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"><span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>It removes the versioning problem and makes <code>QRTZ_JOB_DETAILS.JOB_DATA</code> readable in a query tool. The rule: <strong>every value must be a string</strong>. Storing a job with a <code>DateTimeOffset</code> in its map fails under string mode. Write values with <code>PutAsString</code>; the accessors read them the same way in either mode.</p><div class="hint-container tip"><p class="hint-container-title">Tips</p><p>Turn <code>StoreJobDataAsStrings</code> on at the start of a project, not in the middle. Switching it on with data already in the tables leaves rows the store cannot read.</p></div><h2 id="property-injection-the-other-read-side" tabindex="-1"><a class="header-anchor" href="#property-injection-the-other-read-side"><span>Property injection: the other read side</span></a></h2><p>If a job has settable properties named like keys in the merged map, the default job factory sets them before <code>Execute</code> runs:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token keyword">public</span> <span class="token keyword">sealed</span> <span class="token keyword">class</span> <span class="token class-name">ReportJob</span> <span class="token punctuation">:</span> <span class="token type-list"><span class="token class-name">IJob</span></span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    <span class="token keyword">public</span> <span class="token return-type class-name"><span class="token keyword">string</span></span> Region <span class="token punctuation">{</span> <span class="token keyword">get</span><span class="token punctuation">;</span> <span class="token keyword">set</span><span class="token punctuation">;</span> <span class="token punctuation">}</span> <span class="token operator">=</span> <span class="token string">&quot;&quot;</span><span class="token punctuation">;</span></span>
<span class="line">    <span class="token keyword">public</span> <span class="token return-type class-name"><span class="token keyword">int</span></span> LookbackDays <span class="token punctuation">{</span> <span class="token keyword">get</span><span class="token punctuation">;</span> <span class="token keyword">set</span><span class="token punctuation">;</span> <span class="token punctuation">}</span></span>
<span class="line"></span>
<span class="line">    <span class="token keyword">public</span> <span class="token return-type class-name">ValueTask</span> <span class="token function">Execute</span><span class="token punctuation">(</span><span class="token class-name">IJobExecutionContext</span> context<span class="token punctuation">,</span> <span class="token class-name">CancellationToken</span> cancellationToken <span class="token operator">=</span> <span class="token keyword">default</span><span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">{</span></span>
<span class="line">        <span class="token comment">// Region and LookbackDays are already set</span></span>
<span class="line"></span>
<span class="line">        <span class="token keyword">return</span> <span class="token keyword">default</span><span class="token punctuation">;</span></span>
<span class="line">    <span class="token punctuation">}</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><ul><li>Conversion follows the accessors&#39; rules: <code>&quot;30&quot;</code> in the map sets an <code>int LookbackDays</code>.</li><li>A key with no matching property, or a value that cannot be converted, is handled by <code>PropertySettingJobFactory.PropertyMismatchBehavior</code>: <code>Ignore</code>, <code>Warn</code> or <code>Throw</code>. Use <code>Warn</code> in development; otherwise a property silently stays at its default.</li><li><code>UsingJobData(j =&gt; j.LookbackDays, 30)</code> is the matching write side: name the property, get the key.</li></ul><h2 id="a-typed-input-the-third-read-side" tabindex="-1"><a class="header-anchor" href="#a-typed-input-the-third-read-side"><span>A typed input: the third read side</span></a></h2><p>A job whose data is one payload (a message, a command, an event) can declare its type with <code>IJob&lt;TInput&gt;</code>. The payload arrives as a parameter:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token keyword">public</span> <span class="token keyword">sealed</span> <span class="token keyword">record</span> <span class="token class-name">SendEmail</span><span class="token punctuation">(</span><span class="token class-name"><span class="token keyword">string</span></span> To<span class="token punctuation">,</span> <span class="token class-name"><span class="token keyword">string</span></span> Subject<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line"><span class="token keyword">public</span> <span class="token keyword">sealed</span> <span class="token keyword">class</span> <span class="token class-name">SendEmailJob</span> <span class="token punctuation">:</span> <span class="token type-list"><span class="token class-name">IJob<span class="token punctuation">&lt;</span>SendEmail<span class="token punctuation">&gt;</span></span></span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    <span class="token keyword">public</span> <span class="token return-type class-name">ValueTask</span> <span class="token function">Execute</span><span class="token punctuation">(</span><span class="token class-name">IJobExecutionContext</span> context<span class="token punctuation">,</span> <span class="token class-name">SendEmail</span> input<span class="token punctuation">,</span> <span class="token class-name">CancellationToken</span> cancellationToken <span class="token operator">=</span> <span class="token keyword">default</span><span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">{</span></span>
<span class="line">        <span class="token comment">// input.To, input.Subject - no keys, no accessors, no casts</span></span>
<span class="line">        <span class="token keyword">return</span> <span class="token keyword">default</span><span class="token punctuation">;</span></span>
<span class="line">    <span class="token punctuation">}</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span>
<span class="line"><span class="token keyword">public</span> <span class="token keyword">static</span> <span class="token keyword">class</span> <span class="token class-name">TypedInputScheduling</span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    <span class="token keyword">public</span> <span class="token keyword">static</span> <span class="token keyword">async</span> <span class="token return-type class-name">ValueTask</span> <span class="token function">Schedule</span><span class="token punctuation">(</span><span class="token class-name">IScheduler</span> scheduler<span class="token punctuation">,</span> <span class="token class-name">CancellationToken</span> cancellationToken<span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">{</span></span>
<span class="line">        <span class="token keyword">await</span> scheduler<span class="token punctuation">.</span><span class="token function">ScheduleJob</span><span class="token punctuation">(</span></span>
<span class="line">            JobBuilder<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">Create</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>SendEmailJob<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span><span class="token punctuation">)</span></span>
<span class="line">                <span class="token punctuation">.</span><span class="token function">WithIdentity</span><span class="token punctuation">(</span><span class="token string">&quot;welcome&quot;</span><span class="token punctuation">,</span> <span class="token string">&quot;email&quot;</span><span class="token punctuation">)</span></span>
<span class="line">                <span class="token punctuation">.</span><span class="token function">Build</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span></span>
<span class="line">            TriggerBuilder<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">Create</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>SendEmailJob<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span><span class="token punctuation">)</span></span>
<span class="line">                <span class="token punctuation">.</span><span class="token function">WithIdentity</span><span class="token punctuation">(</span><span class="token string">&quot;welcome-3401&quot;</span><span class="token punctuation">,</span> <span class="token string">&quot;email&quot;</span><span class="token punctuation">)</span></span>
<span class="line">                <span class="token punctuation">.</span><span class="token function">StartNow</span><span class="token punctuation">(</span><span class="token punctuation">)</span></span>
<span class="line">                <span class="token punctuation">.</span><span class="token function">UsingInput</span><span class="token punctuation">(</span><span class="token keyword">new</span> <span class="token constructor-invocation class-name">SendEmail</span><span class="token punctuation">(</span><span class="token string">&quot;someone@example.org&quot;</span><span class="token punctuation">,</span> <span class="token string">&quot;Welcome&quot;</span><span class="token punctuation">)</span><span class="token punctuation">)</span></span>
<span class="line">                <span class="token punctuation">.</span><span class="token function">Build</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span></span>
<span class="line">            <span class="token named-parameter punctuation">cancellationToken</span><span class="token punctuation">:</span> cancellationToken<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">    <span class="token punctuation">}</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><ul><li><code>UsingInput</code> is on <code>JobBuilder&lt;TJob&gt;</code>, <code>TriggerBuilder&lt;TJob&gt;</code>, and the <code>AddJob</code> and <code>AddTrigger</code> configurators. It is only offered for a job that declares an input; using it on any other job is a compile error.</li><li>The value is stored in the ordinary <code>JobDataMap</code> under the reserved key <code>SchedulerConstants.JobInput</code> (<code>QRTZ_JOB_INPUT</code>). The scheduler serializes it to a <strong>string</strong> when the job or trigger is stored, so it survives <code>StoreJobDataAsStrings</code>, the serializer&#39;s type check, the blob column and the HTTP API.</li><li>An input on the trigger overrides an input on the job.</li><li>A job that is not an <code>IJob&lt;TInput&gt;</code> can read the payload with <code>context.GetInput&lt;SendEmail&gt;()</code>, which returns <code>null</code> when there is none.</li><li>An <code>IJob&lt;TInput&gt;</code> whose input is missing fails the firing with a <code>SchedulerException</code> naming the key, instead of running on a default payload.</li><li><strong>The input type is inferred from the argument.</strong> A <code>payload</code> held as a base type is stored and read as that base type. Pass the type argument when the static type is not the one you mean: <code>UsingInput&lt;SendEmailJob, SendEmail&gt;(payload)</code>.</li><li><strong>Put a per-firing input on the trigger.</strong> A <code>[PersistJobDataAfterExecution]</code> job re-stores its own map after every firing, so an input on the job is written back each time. That is harmless, since it is already a string.</li></ul>`,11),e(`p`,null,[l[16]||=r(`The scheduler's `,-1),l[17]||=e(`code`,null,`IJobInputSerializer`,-1),l[18]||=r(` writes the payload. It defaults to `,-1),l[19]||=e(`code`,null,`SystemTextJsonJobInputSerializer`,-1),l[20]||=r(`, built from the same registry as the store's serializer; see `,-1),c(m,{to:`/documentation/quartz-4.x/packages/system-text-json.html`},{default:i(()=>[...l[15]||=[r(`JSON Serialization`,-1)]]),_:1}),l[21]||=r(`. A trimmed or native-AOT application declares its payload types with `,-1),l[22]||=e(`code`,null,`SystemTextJsonSerializerRegistry.AddTypeInfoResolver`,-1),l[23]||=r(`, as for a job data value type.`,-1)]),l[34]||=t(`<h2 id="persisting-changes-across-fires" tabindex="-1"><a class="header-anchor" href="#persisting-changes-across-fires"><span>Persisting changes across fires</span></a></h2><p>By default a job&#39;s stored map is written once and read many times. With <code>[PersistJobDataAfterExecution]</code> the job&#39;s own <code>JobDataMap</code> is saved after every execution, so a counter or a watermark survives:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token punctuation">[</span><span class="token attribute"><span class="token class-name">PersistJobDataAfterExecution</span></span><span class="token punctuation">]</span></span>
<span class="line"><span class="token punctuation">[</span><span class="token attribute"><span class="token class-name">DisallowConcurrentExecution</span></span><span class="token punctuation">]</span></span>
<span class="line"><span class="token keyword">public</span> <span class="token keyword">sealed</span> <span class="token keyword">class</span> <span class="token class-name">IncrementalSyncJob</span> <span class="token punctuation">:</span> <span class="token type-list"><span class="token class-name">IJob</span></span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    <span class="token keyword">public</span> <span class="token return-type class-name">ValueTask</span> <span class="token function">Execute</span><span class="token punctuation">(</span><span class="token class-name">IJobExecutionContext</span> context<span class="token punctuation">,</span> <span class="token class-name">CancellationToken</span> cancellationToken <span class="token operator">=</span> <span class="token keyword">default</span><span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">{</span></span>
<span class="line">        <span class="token class-name">JobDataMap</span> data <span class="token operator">=</span> context<span class="token punctuation">.</span>JobDetail<span class="token punctuation">.</span>JobDataMap<span class="token punctuation">;</span></span>
<span class="line">        data<span class="token punctuation">.</span><span class="token function">PutAsString</span><span class="token punctuation">(</span><span class="token string">&quot;lastSyncedAt&quot;</span><span class="token punctuation">,</span> DateTimeOffset<span class="token punctuation">.</span>UtcNow<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">        <span class="token keyword">return</span> <span class="token keyword">default</span><span class="token punctuation">;</span></span>
<span class="line">    <span class="token punctuation">}</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Always add <code>[DisallowConcurrentExecution]</code> too. Without it, two firings read the same map, both write, and one write is lost.</p><p>The map tracks changes and is only written when it changed. To force a write the map did not detect, such as an in-place change to a stored object, set the well-known key:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line">data<span class="token punctuation">[</span>SchedulerConstants<span class="token punctuation">.</span>ForceJobDataMapDirty<span class="token punctuation">]</span> <span class="token operator">=</span> <span class="token string">&quot;true&quot;</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><h2 id="thread-safety" tabindex="-1"><a class="header-anchor" href="#thread-safety"><span>Thread safety</span></a></h2><p><code>JobDataMap</code> is not thread-safe. A job without <code>[DisallowConcurrentExecution]</code> can have several firings running at once, and they share the stored <code>IJobDetail</code>&#39;s map. Reading it concurrently is fine; do not change it from a job that can run concurrently with itself.</p><p>Each firing gets its own <code>MergedJobDataMap</code>, so per-execution data is isolated.</p><h2 id="what-does-not-belong-in-job-data" tabindex="-1"><a class="header-anchor" href="#what-does-not-belong-in-job-data"><span>What does not belong in job data</span></a></h2><p>Job data is durable. On a persistent store it is in <code>QRTZ_JOB_DETAILS.JOB_DATA</code> and <code>QRTZ_TRIGGERS.JOB_DATA</code>, in every backup, in the fired-trigger history, and in the dashboard and HTTP API for anyone who can read a job&#39;s detail. Keep out:</p>`,11),e(`ul`,null,[e(`li`,null,[l[25]||=e(`strong`,null,`Credentials, tokens, connection strings.`,-1),l[26]||=r(` Register the secret with the container instead. The shipped `,-1),l[27]||=e(`code`,null,`SendMailJob`,-1),l[28]||=r(` does this: its options type has no user name or password field. See `,-1),c(m,{to:`/documentation/quartz-4.x/packages/quartz-jobs.html#keep-the-smtp-credential-out-of-job-data`},{default:i(()=>[...l[24]||=[r(`Keep the SMTP credential out of job data`,-1)]]),_:1}),l[29]||=r(`; the pattern applies to any job that needs a secret.`,-1)]),l[30]||=e(`li`,null,[e(`strong`,null,`Large payloads.`),r(` Job data is read on every fire and, under `),e(`code`,null,`[PersistJobDataAfterExecution]`),r(`, written on every fire. Store an identifier and fetch the payload in the job.`)],-1),l[31]||=e(`li`,null,[e(`strong`,null,`Live objects.`),r(` A `),e(`code`,null,`DbConnection`),r(`, an `),e(`code`,null,`HttpClient`),r(` or a logger comes from the container through the job's constructor.`)],-1)]),l[35]||=e(`p`,null,`Job data is for the inputs that distinguish one scheduled instance from another.`,-1)])}var f=s(u,[[`render`,d]]);export{l as _pageData,f as default};