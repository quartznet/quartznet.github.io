import{_ as e,o as t,c as a,e as o}from"./app-D5nNabMk.js";const s={},n=o(`<h1 id="job-stores" tabindex="-1"><a class="header-anchor" href="#job-stores"><span>Job Stores</span></a></h1><p>JobStore&#39;s are responsible for keeping track of all the &quot;work data&quot; that you give to the scheduler: jobs, triggers, calendars, etc. Selecting the appropriate <code>IJobStore</code> implementation for your Quartz scheduler instance is an important step. Luckily, the choice should be a very easy one once you understand the differences between them. You declare which JobStore your scheduler should use (and it&#39;s configuration settings) in the properties file (or object) that you provide to the SchedulerFactory that you use to produce your scheduler instance.</p><div class="custom-container warning"><p class="custom-container-title">WARNING</p><p>Never use a JobStore instance directly in your code. For some reason many people attempt to do this. The JobStore is for behind-the-scenes use of Quartz itself. You have to tell Quartz (through configuration) which JobStore to use, but then you should only work with the Scheduler interface in your code.</p></div><h2 id="ramjobstore" tabindex="-1"><a class="header-anchor" href="#ramjobstore"><span>RAMJobStore</span></a></h2><p><code>RAMJobStore</code> is the simplest JobStore to use, it is also the most performant (in terms of CPU time). <code>RAMJobStore</code> gets its name in the obvious way: it keeps all of its data in RAM. This is why it&#39;s lightning-fast, and also why it&#39;s so simple to configure. The drawback is that when your application ends (or crashes) all of the scheduling information is lost - this means RAMJobStore cannot honor the setting of &quot;non-volatility&quot; on jobs and triggers. For some applications this is acceptable - or even the desired behavior, but for other applications, this may be disastrous.</p><p><strong>Configuring Quartz to use RAMJobStore</strong></p><div class="language-text line-numbers-mode" data-ext="text" data-title="text"><pre class="language-text"><code> // this is actually the default, so you don&#39;t need to explicitly set this
 quartz.jobStore.type = Quartz.Simpl.RAMJobStore, Quartz
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>To use <code>RAMJobStore</code> (and assuming you&#39;re using <code>StdSchedulerFactory</code>) you don&#39;t need to do anything special. Default configuration of Quartz.NET uses <code>RAMJobStore</code> as job store implementation.</p><h2 id="ado-net-job-store-adojobstore" tabindex="-1"><a class="header-anchor" href="#ado-net-job-store-adojobstore"><span>ADO.NET Job Store (AdoJobStore)</span></a></h2><p>AdoJobStore is also aptly named - it keeps all of its data in a database via ADO.NET. Because of this it is a bit more complicated to configure than <code>RAMJobStore</code>, and it also is not as fast. However, the performance draw-back is not terribly bad, especially if you build the database tables with indexes on the primary keys.</p><p>To use AdoJobStore, you must first create a set of database tables for Quartz.NET to use. You can find table-creation SQL scripts in the &quot;database/dbtables&quot; directory of the Quartz.NET distribution. If there is not already a script for your database type, just look at one of the existing ones, and modify it in any way necessary for your DB. One thing to note is that in these scripts, all the the tables start with the prefix <code>QRTZ_</code> (such as the tables <code>QRTZ_TRIGGERS</code>, and <code>QRTZ_JOB_DETAIL</code>). This prefix can actually be anything you&#39;d like, as long as you inform AdoJobStore what the prefix is (in your Quartz.NET properties). Using different prefixes may be useful for creating multiple sets of tables, for multiple scheduler instances, within the same database.</p><p>Currently the only option for the internal implementation of job store is <code>JobStoreTX</code>which creates transactions by itself. This is different from Java version of Quartz where there is also option to choose <code>JobStoreCMT</code> which uses J2EE container managed transactions.</p><p>The last piece of the puzzle is setting up a data source from which AdoJobStore can get connections to your database. Data sources are defined in your Quartz.NET properties. Data source information contains the connection string and ADO.NET delegate information.</p><h3 id="configuring-quartz-to-use-jobstoretx" tabindex="-1"><a class="header-anchor" href="#configuring-quartz-to-use-jobstoretx"><span>Configuring Quartz to use JobStoreTx</span></a></h3><div class="language-text line-numbers-mode" data-ext="text" data-title="text"><pre class="language-text"><code>    quartz.jobStore.type = Quartz.Impl.AdoJobStore.JobStoreTX, Quartz
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Next, you need to select a <code>IDriverDelegate</code> implementation for the JobStore to use. The DriverDelegate is responsible for doing any ADO.NET work that may be needed for your specific database. <code>StdAdoDelegate</code> is a delegate that uses &quot;vanilla&quot; ADO.NET code (and SQL statements) to do its work. If there isn&#39;t another delegate made specifically for your database, try using this delegate - special delegates usually have better performance or workarounds for database specific issues. Other delegates can be found in the <code>Quartz.Impl.AdoJobStore</code> namespace, or in its sub-namespaces.</p><div class="custom-container tip"><p class="custom-container-title">TIP</p><p>Quartz.NET will issue warning if you are using the default StdAdoDelegate as it has poor performance when you have a lot of triggers to select from. Specific delegates have special SQL to limit result set length (SqlServerDelegate uses <code>TOP n</code>, PostgreSQLDelegate <code>LIMIT n</code>, OracleDelegate <code>ROWCOUNT() &lt;= n</code> etc.).</p></div><p>Once you&#39;ve selected your delegate, set its class name as the delegate for AdoJobStore to use.</p><p><strong>Configuring AdoJobStore to use a DriverDelegate</strong></p><div class="language-text line-numbers-mode" data-ext="text" data-title="text"><pre class="language-text"><code>    quartz.jobStore.driverDelegateType = Quartz.Impl.AdoJobStore.StdAdoDelegate, Quartz
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Next, you need to inform the JobStore what table prefix (discussed above) you are using.</p><p><strong>Configuring AdoJobStore with the Table Prefix</strong></p><div class="language-text line-numbers-mode" data-ext="text" data-title="text"><pre class="language-text"><code>    quartz.jobStore.tablePrefix = QRTZ_
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>And finally, you need to set which data source should be used by the JobStore. The named data source must also be defined in your Quartz properties. In this case, we&#39;re specifying that Quartz should use the data source name &quot;myDS&quot; (that is defined elsewhere in the configuration properties).</p><p><strong>Configuring AdoJobStore with the name of the data source to use</strong></p><div class="language-text line-numbers-mode" data-ext="text" data-title="text"><pre class="language-text"><code>    quartz.jobStore.dataSource = myDS
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>One last thing that is needed for the configuration is to set data source connection string information and database provider. Connection string is the standard ADO.NET connection which is driver specific. Database provider is an abstraction of database drivers to create loose coupling between database drivers and Quartz.</p><p><strong>Setting Data Source&#39;s Connection String And Database Provider</strong></p><div class="language-text line-numbers-mode" data-ext="text" data-title="text"><pre class="language-text"><code>     quartz.dataSource.myDS.connectionString = Server=localhost;Database=quartz;Uid=quartznet;Pwd=quartznet
     quartz.dataSource.myDS.provider = MySql
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>Currently following database providers are supported:</p><ul><li><code>SqlServer</code> - SQL Server driver <ul><li>For full framework this is by default System.Data.SqlClient (except in Quartz 3.1)</li><li>From Quartz 3.2 onwards for .NET Core this is by default Microsoft.Data.SqlClient</li></ul></li><li><code>SystemDataSqlClient</code> - Available separately on .NET Core (default for full framework)</li><li><code>MicrosoftDataSqlClient</code> - Available separately on full framework (default for .NET Core)</li><li><code>OracleODP</code> - Oracle&#39;s Oracle Driver</li><li><code>OracleODPManaged</code> - Oracle&#39;s managed driver for Oracle 11</li><li><code>MySql</code> - MySQL Connector/.NET</li><li><code>SQLite</code> - SQLite ADO.NET Provider</li><li><code>SQLite-Microsoft</code> - Microsoft SQLite ADO.NET Provider</li><li><code>Firebird</code> - Firebird ADO.NET Provider</li><li><code>Npgsql</code> - PostgreSQL Npgsql</li></ul><div class="custom-container tip"><p class="custom-container-title">TIP</p><p>There are many community contributed providers, like for NoSQL databases.</p><p>They are not supported by Quartz.NET project though.</p></div><p><strong>You can and should use latest version of driver if newer is available, just create an assembly binding redirect</strong></p><p>If your Scheduler is very busy (i.e. nearly always executing the same number of jobs as the size of the thread pool, then you should probably set the number of connections in the data source to be the about the size of the thread pool + 1. This is commonly configured in the ADO.NET connection string - see your driver implementation for details.</p><p>The <code>quartz.jobStore.useProperties</code> config parameter can be set to &quot;true&quot; (defaults to false) in order to instruct AdoJobStore that all values in JobDataMaps will be strings, and therefore can be stored as name-value pairs, rather than storing more complex objects in their serialized form in the BLOB column. This is much safer in the long term, as you avoid the class versioning issues that there are with serializing your non-String classes into a BLOB.</p><h3 id="configuring-adojobstore-to-use-strings-as-jobdatamap-values" tabindex="-1"><a class="header-anchor" href="#configuring-adojobstore-to-use-strings-as-jobdatamap-values"><span>Configuring AdoJobStore to use strings as JobDataMap values</span></a></h3><div class="custom-container tip"><p class="custom-container-title">TIP</p><p>This is recommended configuration because it greatly decreases the possibility of type serialization issues.</p></div><div class="language-text line-numbers-mode" data-ext="text" data-title="text"><pre class="language-text"><code>    quartz.jobStore.useProperties = true
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><h3 id="choosing-a-serializer" tabindex="-1"><a class="header-anchor" href="#choosing-a-serializer"><span>Choosing a serializer</span></a></h3><p>Quartz.NET supports both binary and JSON serialization. Using binary serialization is discouraged as it will no longer be supported in future versions.</p><ul><li>JSON serialization based on System.Text.Json comes bundled with Quartz</li><li>JSON serialization based on Newtonsoft.Json comes from separate <a href="../packages/json-serialization">Quartz.Serialization.Json</a> NuGet package</li></ul><div class="custom-container tip"><p class="custom-container-title">TIP</p><p>JSON is recommended persistent format to store data in database for greenfield projects. You should also strongly consider setting useProperties to true to restrict key-values to be strings.</p></div><h4 id="using-code" tabindex="-1"><a class="header-anchor" href="#using-code"><span>Using code</span></a></h4><div class="language-csharp line-numbers-mode" data-ext="cs" data-title="cs"><pre class="language-csharp"><code><span class="token class-name"><span class="token keyword">var</span></span> config <span class="token operator">=</span> SchedulerBuilder<span class="token punctuation">.</span><span class="token function">Create</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
config<span class="token punctuation">.</span><span class="token function">UsePersistentStore</span><span class="token punctuation">(</span>store <span class="token operator">=&gt;</span>
<span class="token punctuation">{</span>
    <span class="token comment">// it&#39;s generally recommended to stick with</span>
    <span class="token comment">// string property keys and values when serializing</span>
    store<span class="token punctuation">.</span>UseProperties <span class="token operator">=</span> <span class="token boolean">true</span><span class="token punctuation">;</span>

    <span class="token range operator">..</span><span class="token range operator">..</span>

    store<span class="token punctuation">.</span><span class="token function">UseSystemTextJsonSerializer</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token class-name">ISchedulerFactory</span> schedulerFactory <span class="token operator">=</span> config<span class="token punctuation">.</span><span class="token function">Build</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h4 id="using-properties" tabindex="-1"><a class="header-anchor" href="#using-properties"><span>Using properties</span></a></h4><div class="language-csharp line-numbers-mode" data-ext="cs" data-title="cs"><pre class="language-csharp"><code>    <span class="token comment">// &quot;stj&quot; is an alias for &quot;Quartz.Simpl.SystemTextJsonObjectSerializer, Quartz&quot;</span>
    <span class="token comment">// &quot;newtonsoft&quot; is alias for &quot;Quartz.Simpl.JsonObjectSerializer, Quartz.Serialization.Json&quot;</span>
    quartz<span class="token punctuation">.</span>serializer<span class="token punctuation">.</span>type <span class="token operator">=</span> stj
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div>`,46),i=[n];function r(l,d){return t(),a("div",null,i)}const u=e(s,[["render",r],["__file","job-stores.html.vue"]]),p=JSON.parse('{"path":"/documentation/quartz-4.x/tutorial/job-stores.html","title":"Job Stores","lang":"en-US","frontmatter":{"title":"Job Stores"},"headers":[{"level":2,"title":"RAMJobStore","slug":"ramjobstore","link":"#ramjobstore","children":[]},{"level":2,"title":"ADO.NET Job Store (AdoJobStore)","slug":"ado-net-job-store-adojobstore","link":"#ado-net-job-store-adojobstore","children":[{"level":3,"title":"Configuring Quartz to use JobStoreTx","slug":"configuring-quartz-to-use-jobstoretx","link":"#configuring-quartz-to-use-jobstoretx","children":[]},{"level":3,"title":"Configuring AdoJobStore to use strings as JobDataMap values","slug":"configuring-adojobstore-to-use-strings-as-jobdatamap-values","link":"#configuring-adojobstore-to-use-strings-as-jobdatamap-values","children":[]},{"level":3,"title":"Choosing a serializer","slug":"choosing-a-serializer","link":"#choosing-a-serializer","children":[]}]}],"git":{"updatedTime":1771443994000,"contributors":[{"name":"Copilot","email":"198982749+Copilot@users.noreply.github.com","commits":1}]},"filePathRelative":"documentation/quartz-4.x/tutorial/job-stores.md"}');export{u as comp,p as data};
