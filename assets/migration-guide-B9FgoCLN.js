import{a as e,c as t,d as n,l as r,m as i,p as a,s as o,t as s,u as c}from"./app-BsOuDHI-.js";var l=JSON.parse(`{"path":"/documentation/quartz-4.x/migration-guide.html","title":"Migration Guide","lang":"en-US","frontmatter":{"title":"Migration Guide"},"git":{"updatedTime":1788441191000,"contributors":[{"name":"Marko Lahma","username":"","email":"marko.lahma@gmail.com","commits":1},{"name":"Claude Fable 5.1","username":"","email":"noreply@anthropic.com","commits":1}],"changelog":[{"hash":"8dee9f767ead84b6e47c023533864d1d3e3178ae","time":1788441191000,"email":"marko.lahma@gmail.com","author":"Marko Lahma","message":"The release post carries the day 4.0.0 shipped","coAuthors":[{"name":"Claude Fable 5.1","email":"noreply@anthropic.com"}]}]},"filePathRelative":"documentation/quartz-4.x/migration-guide.md"}`),u={name:`migration-guide.md`},d={class:`hint-container tip`},f={class:`hint-container warning`};function p(s,l,u,p,m,h){let g=a(`RouteLink`);return n(),o(`div`,null,[l[843]||=e(`p`,null,[e(`em`,null,[r(`This document outlines changes needed when upgrading from Quartz.NET 3.x to 4.x. You should also check `),e(`a`,{href:`https://github.com/quartznet/quartznet/releases`,target:`_blank`,rel:`noopener noreferrer`},`the release notes`),r(` for each version.`)])],-1),e(`div`,d,[l[2]||=e(`p`,{class:`hint-container-title`},`Tips`,-1),e(`p`,null,[l[1]||=r(`If you are a new user starting with the latest version, you don't need to follow this guide. Just jump right to `,-1),c(g,{to:`/documentation/quartz-4.x/tutorial/`},{default:i(()=>[...l[0]||=[r(`the tutorial`,-1)]]),_:1})])]),l[844]||=t(`<h2 id="start-here" tabindex="-1"><a class="header-anchor" href="#start-here"><span>Start here</span></a></h2><p>Three questions. Each has a different part of this guide as its answer, and reading the wrong one first is most of what makes an upgrade feel long.</p><p><strong>Is what you are upgrading a running deployment?</strong> Then the order to do things in matters more than any single rename, and two of the seven steps happen <strong>while you are still on 3.x</strong> — they are the two that are painful to discover afterwards. Read <a href="#upgrading-a-running-deployment">Upgrading a running deployment</a> first, and come back for the code.</p><p><strong>Is it an application&#39;s code?</strong> <a href="#the-road-from-3-x-phase-by-phase">The road from 3.x, phase by phase</a> is the reading order: six passes over the API, each with a theme, with the rest of this guide organised by topic underneath. The compiler finds most of the work for you; the passes are for knowing why a name moved and what to reach for instead. Start with <a href="#package-changes">Package Changes</a> — the first error a mixed 3.x/4.x project produces is a package problem rather than a code one, and it is described there.</p>`,4),e(`p`,null,[l[5]||=e(`strong`,null,`Is it neither, because you are starting something new?`,-1),l[6]||=r(` None of this applies. Go to the `,-1),c(g,{to:`/documentation/quartz-4.x/quick-start.html`},{default:i(()=>[...l[3]||=[r(`quick start`,-1)]]),_:1}),l[7]||=r(` and then `,-1),c(g,{to:`/documentation/quartz-4.x/tutorial/`},{default:i(()=>[...l[4]||=[r(`the tutorial`,-1)]]),_:1}),l[8]||=r(`.`,-1)]),l[845]||=t(`<p>If you ran a 4.0 alpha or beta, everything that changed between one pre-release and the next is collected in <a href="#appendix-if-you-ran-a-4-0-pre-release">Appendix: if you ran a 4.0 pre-release</a>. The rest of this guide is written as one statement about 3.x and 4.0, with no build numbers in it.</p><h2 id="the-road-from-3-x-phase-by-phase" tabindex="-1"><a class="header-anchor" href="#the-road-from-3-x-phase-by-phase"><span>The road from 3.x, phase by phase</span></a></h2><p>The 4.0 API is the result of six passes over the public surface, each with its own theme. The guide below is organised by topic rather than by pass, so this is the map: what changed, in the order it is worth working through when migrating.</p><p><strong>1. The extensibility contracts.</strong> The interfaces you implement rather than call. <code>Quartz.Spi</code> is <a href="#quartz-spi-and-quartz-simpl-were-renamed"><code>Quartz.Extensibility</code> and <code>Quartz.Simpl</code> is <code>Quartz.Impl</code></a>; every asynchronous member returns <a href="#tasks-changed-to-valuetask"><code>ValueTask</code></a> and ends with a <code>CancellationToken</code>; <a href="#jobs-take-a-cancellationtoken">jobs take that token as a parameter</a>; the <a href="#the-job-factory-hands-out-a-scope">job factory hands out a scope</a> instead of an instance; the <a href="#the-thread-pool-is-asynchronous">thread pool is asynchronous</a>; and <a href="#trigger-fire-times-are-properties">trigger fire times are properties</a> rather than getter/setter pairs; and <a href="#an-ijobdetail-of-your-own">a job detail of your own</a> is finally implementable, because the one member no implementation could write is gone from <code>IJobDetail</code>. Start here: everything else assumes these signatures.</p><p><strong>2. The vocabulary and the surface.</strong> One word per concept, and nothing public that was never a contract. <a href="#names-that-were-normalized">Names that were normalized</a>, <a href="#the-scheduler-and-the-job-store-speak-the-same-verbs">the scheduler and the job store speak the same verbs</a>, <a href="#matchers-moved-to-quartz">matchers moved to <code>Quartz</code></a>, <a href="#key-t-moved-to-quartz-and-is-immutable"><code>Key&lt;T&gt;</code> moved to <code>Quartz</code> and is immutable</a>, <a href="#schedulermetadata-replaces-schedulermetadata"><code>SchedulerMetadata</code> replaces <code>SchedulerMetaData</code></a>, <a href="#listener-api-changes">the listener API</a>, and <a href="#sealed-and-internalized-types">sealed and internalized types</a>.</p><p><strong>3. Listings, schedules and dates.</strong> <a href="#job-store-listings-became-queries">Job store listings became queries</a> returning <code>PagedResult&lt;T&gt;</code> of headers; <a href="#misfire-instructions-are-enums">misfire instructions are enums</a>; <a href="#intervals-are-said-once-per-builder">intervals are said once per builder</a>; <a href="#timeofday-became-timeonly"><code>TimeOfDay</code> became <code>TimeOnly</code></a> and <a href="#datebuilder-s-static-factories-are-gone"><code>DateBuilder</code>&#39;s static factories are gone</a>; <a href="#executing-is-a-trigger-state"><code>Executing</code> is a trigger state</a>; and <a href="#the-preferred-node-is-a-value">the preferred node is a value</a>.</p><p><strong>4. The ADO.NET job store.</strong> Read <a href="#the-ado-net-store-is-a-store-not-a-base-class">the store is a store, not a base class</a> first: the hierarchy is internal, so several of the sections after it describe a seam that was curated and then closed, and say so. Then <a href="#trigger-states-are-typed-on-the-driver-delegate">trigger states are typed on the driver delegate</a>; <a href="#the-ado-net-job-stores-are-named-for-whose-transaction-they-use">the stores are named for whose transaction they use</a>; <a href="#nine-execute-lock-overloads-became-four-members">nine <code>Execute…Lock</code> overloads became four</a>; <a href="#locks-are-a-schedulerlock-not-a-string">locks are a <code>SchedulerLock</code></a>; <a href="#the-job-store-configuration-is-read-only-and-no-longer-a-public-currency">the job store configuration is read-only</a>; <a href="#the-driver-delegate-speaks-in-records">the driver delegate speaks in records</a>; <a href="#the-optional-columns-are-required-so-the-probes-are-gone">the optional columns are required, so the probes are gone</a> and the <a href="#database-schema-migration">schema migration</a> that goes with that is mandatory; <a href="#ramjobstore-is-sealed"><code>RAMJobStore</code> is sealed</a>; <a href="#a-job-store-of-your-own-can-join-your-transaction">a job store of your own can join your transaction</a>; and the two stores, held to one contract test, now <a href="#the-two-job-stores-answer-the-same-way">answer the same way</a> where they used to disagree.</p><p><strong>5. Configuration and hosting.</strong> The container builds the scheduler: <a href="#stdschedulerfactory-is-gone"><code>StdSchedulerFactory</code> is gone</a>, <a href="#the-standalone-builder-is-the-same-builder">the standalone builder is the same builder</a>, <a href="#no-process-global-scheduler-or-connection-state">there is no process-global scheduler state</a>, <a href="#addjob-registers-the-job-with-the-container"><code>AddJob</code> registers the job with the container</a>, <a href="#one-shape-per-registration-method">one shape per registration method</a>, <a href="#clustering-is-configured-in-one-place">clustering is configured in one place</a>, <a href="#the-hosted-service-starts-every-scheduler">the hosted service starts every scheduler</a>, and <a href="#job-execution-metrics">job execution metrics</a> are published by every scheduler.</p><p><strong>6. Serialization policy and the last edges.</strong><a href="#serializable-survives-only-where-a-database-blob-needs-it"><code>[Serializable]</code> survives only where a database blob needs it</a>; <a href="#the-two-exceptions-moved-out-of-quartz-core">the two exceptions moved out of <code>Quartz.Core</code></a>; <a href="#execution-limits-are-built-once-then-frozen">execution limits are built once, then frozen</a>; <a href="#interruption-has-two-names-not-three">interruption has two names, not three</a>; and the naming and visibility odds and ends gathered in <a href="#other-breaking-changes">Other Breaking Changes</a> at the end of this guide.</p><p>Two differences run through all six and are not called out case by case, because they are almost universal: <code>Task</code> became <code>ValueTask</code> on nearly every member, and the namespaces moved as described in pass 1. If a type you used does not appear in this guide at all, check <a href="#package-changes">Package Changes</a> first — it may have moved packages rather than changed.</p><h2 id="target-framework" tabindex="-1"><a class="header-anchor" href="#target-framework"><span>Target Framework</span></a></h2><p>Quartz.NET 4.x targets <code>net10.0</code>. There is no <code>netstandard2.0</code> build and no support for Full Framework style <code>.config</code> files.</p><p>If you are running on an older .NET version, you will need to upgrade your application to .NET 10 before upgrading to Quartz 4.x.</p><h3 id="if-you-are-a-library-rather-than-an-application" tabindex="-1"><a class="header-anchor" href="#if-you-are-a-library-rather-than-an-application"><span>If you are a library rather than an application</span></a></h3><p>An application can be told to upgrade. An integration package that serves <code>net8.0</code> and <code>net9.0</code> consumers cannot, and has to reference Quartz 3.x for those targets and Quartz 4 for <code>net10.0</code>. That is a conditional <code>PackageReference</code> plus <code>#if NET10_0_OR_GREATER</code> at the call sites that differ, and it works only while the difference stays <em>inside</em> the library — the moment your own public surface would differ per target framework, a separate <code>net10.0</code>-only package or dropping the old targets is the answer, not a deeper <code>#if</code>.</p>`,15),e(`p`,null,[c(g,{to:`/documentation/quartz-4.x/how-tos/embedding-quartz-in-a-library.html#shipping-a-package-that-multi-targets`},{default:i(()=>[...l[9]||=[r(`Embedding Quartz in a Library`,-1)]]),_:1}),l[10]||=r(` has the csproj fragment, the honest limit, and the table of renames a real port hit.`,-1)]),l[846]||=t(`<h2 id="package-changes" tabindex="-1"><a class="header-anchor" href="#package-changes"><span>Package Changes</span></a></h2><h3 id="the-first-error-you-will-see" tabindex="-1"><a class="header-anchor" href="#the-first-error-you-will-see"><span>The first error you will see</span></a></h3><p><code>dotnet add package Quartz</code> in a 3.x application takes the latest version, which is 4.x. The three packages that were merged into it are still in the project file, they reference <code>Quartz</code> with no upper bound, and so the build ends up compiling against <code>Quartz</code> 4.x <strong>and</strong> a 3.x <code>Quartz.Extensions.DependencyInjection</code>, <code>Quartz.Extensions.Hosting</code> or <code>Quartz.Serialization.SystemTextJson</code> beside it. Eight type names exist in both, so the first thing the compiler says is not about any API you have to change:</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text"><pre><code class="language-text"><span class="line">error CS0433: The type &#39;QuartzOptions&#39; exists in both</span>
<span class="line">&#39;Quartz.Extensions.DependencyInjection, Version=3.20.0.0, Culture=neutral, PublicKeyToken=f6b8c98a402cc8a4&#39;</span>
<span class="line">and &#39;Quartz, Version=4.0.0.0, Culture=neutral, PublicKeyToken=f6b8c98a402cc8a4&#39;</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The other seven are <code>ITriggerConfigurator</code>, <code>JobFactoryOptions</code> and <code>SchedulingOptions</code> (from <code>Quartz.Extensions.DependencyInjection</code>), <code>QuartzHostedService</code>, <code>QuartzHostedServiceOptions</code> and <code>QuartzServiceCollectionExtensions</code> (from <code>Quartz.Extensions.Hosting</code>), and <code>JsonSerializationException</code> (from <code>Quartz.Serialization.SystemTextJson</code>). Calls into the duplicated extension classes report <code>CS0121: The call is ambiguous</code> instead, naming both assemblies.</p><p>Neither is a migration step of its own. <strong>Remove the three package references</strong>, as the section below already says; both errors go with them, and what is left is the real work.</p><p><code>Quartz.Extensions.DependencyInjection</code>, <code>Quartz.Extensions.Hosting</code>, and <code>Quartz.Serialization.SystemTextJson</code> have been merged into the main <code>Quartz</code> package. You can remove these package references from your project:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> &lt;PackageReference Include=&quot;Quartz.Extensions.DependencyInjection&quot; Version=&quot;3.*&quot; /&gt;</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"> &lt;PackageReference Include=&quot;Quartz.Extensions.Hosting&quot; Version=&quot;3.*&quot; /&gt;</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"> &lt;PackageReference Include=&quot;Quartz.Serialization.SystemTextJson&quot; Version=&quot;3.*&quot; /&gt;</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> &lt;PackageReference Include=&quot;Quartz&quot; Version=&quot;4.*&quot; /&gt;</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>If you use Newtonsoft.Json serialization, reference <code>Quartz.Serialization.Newtonsoft</code> instead of the old <code>Quartz.Serialization.Json</code>.</p><p>Configuration that names a type from one of the merged assemblies as a string keeps working: a name that fails to resolve is retried against <code>Quartz</code>, with a warning naming both spellings.</p>`,10),e(`p`,null,[l[12]||=e(`code`,null,`Quartz.OpenTracing`,-1),l[13]||=r(` is `,-1),l[14]||=e(`strong`,null,`dropped`,-1),l[15]||=r(` and has no 4.x release. It consumed the `,-1),l[16]||=e(`code`,null,`DiagnosticSource`,-1),l[17]||=r(` events that 4.x replaced with `,-1),l[18]||=e(`code`,null,`System.Diagnostics.Activity`,-1),l[19]||=r(`, and the OpenTracing project itself is archived. Remove the package reference and the `,-1),l[20]||=e(`code`,null,`AddQuartzOpenTracing`,-1),l[21]||=r(` call, and subscribe to Quartz's activity source and meter directly — see `,-1),c(g,{to:`/documentation/quartz-4.x/packages/opentelemetry-integration.html#coming-from-quartz-opentracing`},{default:i(()=>[...l[11]||=[r(`OpenTelemetry Integration`,-1)]]),_:1}),l[22]||=r(`.`,-1)]),l[847]||=t(`<div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> &lt;PackageReference Include=&quot;Quartz.OpenTracing&quot; Version=&quot;3.*&quot; /&gt;</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div>`,1),e(`p`,null,[l[24]||=e(`strong`,null,[e(`code`,null,`OpenTelemetry.Instrumentation.Quartz`),r(` is not the replacement.`)],-1),l[25]||=r(` It reads the same `,-1),l[26]||=e(`code`,null,`DiagnosticSource`,-1),l[27]||=r(` events, so on 4.x it produces no spans at all — silently. Remove that reference too, and add `,-1),l[28]||=e(`code`,null,`AddSource(QuartzInstrumentation.ActivitySourceName)`,-1),l[29]||=r(`; see `,-1),c(g,{to:`/documentation/quartz-4.x/packages/opentelemetry-integration.html#opentelemetry-instrumentation-quartz`},{default:i(()=>[...l[23]||=[r(`OpenTelemetry.Instrumentation.Quartz`,-1)]]),_:1}),l[30]||=r(`.`,-1)]),l[848]||=t(`<div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> &lt;PackageReference Include=&quot;OpenTelemetry.Instrumentation.Quartz&quot; Version=&quot;1.*&quot; /&gt;</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><h3 id="quartz-aspire-is-new" tabindex="-1"><a class="header-anchor" href="#quartz-aspire-is-new"><span><code>Quartz.Aspire</code> is new</span></a></h3><p>Nothing to migrate: it is a new package with no 3.x counterpart, listed here because this is where the package list lives.</p><p><code>Quartz.Aspire</code> is the client integration for <a href="https://aspire.dev/" target="_blank" rel="noopener noreferrer">Aspire</a>. It turns an Aspire connection name into a persistent job store: the driver delegate for the database behind that name, connections taken from a <code>DbDataSource</code> the container already holds, the scheduler&#39;s health check, and Quartz&#39;s two telemetry signals named to the OpenTelemetry pipeline your ServiceDefaults project built.</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line">builder<span class="token punctuation">.</span><span class="token function">AddQuartzPersistentStore</span><span class="token punctuation">(</span><span class="token string">&quot;quartz&quot;</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">builder<span class="token punctuation">.</span><span class="token function">AddQuartz</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div>`,5),e(`p`,null,[l[33]||=r(`It takes `,-1),l[34]||=e(`strong`,null,[r(`no `),e(`code`,null,`Aspire.*`),r(` package dependency`)],-1),l[35]||=r(` — `,-1),l[36]||=e(`code`,null,`IHostApplicationBuilder`,-1),l[37]||=r(` is the whole of its contract — so it works in any generic-host application and is not tied to Aspire's release cadence. There is no hosting integration: the AppHost still declares its database resources itself. See `,-1),c(g,{to:`/documentation/quartz-4.x/packages/aspire.html`},{default:i(()=>[...l[31]||=[r(`Aspire Integration`,-1)]]),_:1}),l[38]||=r(` for every setting it reads from `,-1),l[39]||=e(`code`,null,`Aspire:Quartz`,-1),l[40]||=r(`, and `,-1),c(g,{to:`/documentation/quartz-4.x/how-tos/aspire.html`},{default:i(()=>[...l[32]||=[r(`Running Quartz under Aspire`,-1)]]),_:1}),l[41]||=r(` for what the call expands to.`,-1)]),l[849]||=t(`<p>One of those settings decides something the rest of Quartz leaves off by default, so it is worth reading before the first run:</p><table><thead><tr><th>Setting</th><th>Default</th><th>What it decides</th></tr></thead><tbody><tr><td><code>QuartzAspireSettings.SchemaProvisioning</code></td><td>unset — <code>SchemaProvisioning.CreateIfMissing</code> under <code>Development</code>, nothing said in every other environment</td><td>What the store does about its schema as it starts, in the same three-valued vocabulary <code>AdoJobStoreOptions.SchemaProvisioning</code> uses. Naming a value applies it in every environment; a <code>SchemaProvisioning</code> the application set on the store itself is kept either way. See <a href="#performschemavalidation-became-schemaprovisioning-which-has-a-third-position"><code>PerformSchemaValidation</code> became <code>SchemaProvisioning</code></a></td></tr></tbody></table><h3 id="the-health-check-is-in-quartz-not-quartz-aspnetcore" tabindex="-1"><a class="header-anchor" href="#the-health-check-is-in-quartz-not-quartz-aspnetcore"><span>The health check is in <code>Quartz</code>, not <code>Quartz.AspNetCore</code></span></a></h3><p><code>Quartz.AspNetCore</code> carries <code>&lt;FrameworkReference Include=&quot;Microsoft.AspNetCore.App&quot; /&gt;</code> for the HTTP API, a framework reference reaches the nuspec, and so a worker on a <code>dotnet/runtime</code> image that referenced the package for the health check alone failed to start (<a href="https://github.com/quartznet/quartznet/issues/3532" target="_blank" rel="noopener noreferrer">#3532</a>). The check reads <code>IScheduler.Status</code> and probes the job store, and never wanted ASP.NET Core for either, so it moved into the core package. It registers on the standard <code>IHealthChecksBuilder</code>, as it always did.</p><p>In 3.x the check had no registration of its own: <code>AddQuartzServer</code> from <code>Quartz.AspNetCore</code> registered the hosted service and the health check together, and the only choice on offer was the tag list. In 4.0 each is called by its own name, and neither one is in <code>Quartz.AspNetCore</code>:</p><table><thead><tr><th></th><th>3.x</th><th>4.0</th></tr></thead><tbody><tr><td>The hosted service</td><td><code>services.AddQuartzServer(…)</code></td><td><code>builder.AddQuartzHostedService(…)</code>, in <code>Quartz</code></td></tr><tr><td>The health check</td><td>the same call, implicitly</td><td><code>services.AddHealthChecks().AddQuartz(…)</code>, or <code>q.AddQuartzHealthChecks(…)</code> on the scheduler&#39;s own builder — both in <code>Quartz</code></td></tr><tr><td>Its settings</td><td><code>IEnumerable&lt;string&gt;? healthCheckTags</code></td><td><code>QuartzHealthCheckOptions</code>, in <code>Quartz</code></td></tr></tbody></table><p>See <a href="#addquartzserver-is-addquartzhostedservice"><code>AddQuartzServer</code> is <code>AddQuartzHostedService</code></a> for the split itself. What matters here is the package: if the check was the only reason your project referenced <code>Quartz.AspNetCore</code>, that reference can go.</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> &lt;PackageReference Include=&quot;Quartz.AspNetCore&quot; Version=&quot;3.*&quot; /&gt;</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> &lt;PackageReference Include=&quot;Quartz&quot; Version=&quot;4.*&quot; /&gt;</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div>`,8),e(`p`,null,[l[45]||=r(`Keep `,-1),l[46]||=e(`code`,null,`Quartz.AspNetCore`,-1),l[47]||=r(` if you also serve the `,-1),c(g,{to:`/documentation/quartz-4.x/packages/http-api.html`},{default:i(()=>[...l[42]||=[r(`HTTP API`,-1)]]),_:1}),l[48]||=r(` or the `,-1),c(g,{to:`/documentation/quartz-4.x/packages/dashboard.html`},{default:i(()=>[...l[43]||=[r(`dashboard`,-1)]]),_:1}),l[49]||=r(`; it keeps its framework reference for those. `,-1),l[50]||=e(`code`,null,`MapHealthChecks`,-1),l[51]||=r(`, and the `,-1),l[52]||=e(`code`,null,`HealthCheckOptions.ResultStatusCodes`,-1),l[53]||=r(` mapping that decides what a `,-1),l[54]||=e(`em`,null,`degraded`,-1),l[55]||=r(` scheduler answers over HTTP, are ASP.NET Core's and stay documented on `,-1),c(g,{to:`/documentation/quartz-4.x/packages/aspnet-core-integration.html#health-checks`},{default:i(()=>[...l[44]||=[r(`ASP.NET Core Integration`,-1)]]),_:1}),l[56]||=r(`.`,-1)]),l[850]||=e(`h2`,{id:`documentation-pages-that-moved`,tabindex:`-1`},[e(`a`,{class:`header-anchor`,href:`#documentation-pages-that-moved`},[e(`span`,null,`Documentation pages that moved`)])],-1),l[851]||=e(`p`,null,`The documentation was reorganised alongside the API. The old URLs redirect, so an existing bookmark still arrives somewhere useful, but if you keep links to these pages in a wiki or a runbook, retarget them:`,-1),e(`table`,null,[l[72]||=e(`thead`,null,[e(`tr`,null,[e(`th`,null,`Old URL`),e(`th`,null,`Where it is now`)])],-1),e(`tbody`,null,[e(`tr`,null,[l[59]||=e(`td`,null,[e(`code`,null,`documentation/quartz-4.x/tutorial/crontrigger.html`)],-1),e(`td`,null,[c(g,{to:`/documentation/quartz-4.x/cron-expressions.html`},{default:i(()=>[...l[57]||=[e(`code`,null,`cron-expressions.html`,-1)]]),_:1}),l[58]||=r(` — cron syntax is a reference, not a lesson, so it left the tutorial`,-1)])]),e(`tr`,null,[l[62]||=e(`td`,null,[e(`code`,null,`documentation/quartz-4.x/how-tos/crontrigger.html`)],-1),e(`td`,null,[c(g,{to:`/documentation/quartz-4.x/cron-expressions.html`},{default:i(()=>[...l[60]||=[e(`code`,null,`cron-expressions.html`,-1)]]),_:1}),l[61]||=r(` — a stale second copy of the same material, deleted`,-1)])]),e(`tr`,null,[l[65]||=e(`td`,null,[e(`code`,null,`documentation/quartz-4.x/packages/json-configuration.html`)],-1),e(`td`,null,[c(g,{to:`/documentation/quartz-4.x/configuration/json.html`},{default:i(()=>[...l[63]||=[e(`code`,null,`configuration/json.html`,-1)]]),_:1}),l[64]||=r(` — it configures a scheduler rather than describing a package`,-1)])]),e(`tr`,null,[l[68]||=e(`td`,null,[e(`code`,null,`documentation/quartz-4.x/packages/opentracing-integration.html`)],-1),e(`td`,null,[c(g,{to:`/documentation/quartz-4.x/packages/opentelemetry-integration.html`},{default:i(()=>[...l[66]||=[e(`code`,null,`packages/opentelemetry-integration.html`,-1)]]),_:1}),l[67]||=r(` — the package is gone, as above`,-1)])]),e(`tr`,null,[l[71]||=e(`td`,null,[e(`code`,null,`documentation/quartz-4.x/tutorial/miscellaneous-features.html`)],-1),e(`td`,null,[c(g,{to:`/documentation/quartz-4.x/packages/quartz-plugins.html`},{default:i(()=>[...l[69]||=[e(`code`,null,`packages/quartz-plugins.html`,-1)]]),_:1}),l[70]||=r(` — the grab-bag was split, and plug-ins were the largest part of it`,-1)])])])]),e(`p`,null,[l[74]||=r(`The two anchors that were linked from outside the site — `,-1),l[75]||=e(`code`,null,`#h-hash-for-load-distribution`,-1),l[76]||=r(` and `,-1),l[77]||=e(`code`,null,`#building-cron-expressions-programmatically`,-1),l[78]||=r(` — travelled with the cron material and resolve on `,-1),c(g,{to:`/documentation/quartz-4.x/cron-expressions.html`},{default:i(()=>[...l[73]||=[r(`Cron Expressions`,-1)]]),_:1}),l[79]||=r(`.`,-1)]),l[852]||=e(`h2`,{id:`upgrading-a-running-deployment`,tabindex:`-1`},[e(`a`,{class:`header-anchor`,href:`#upgrading-a-running-deployment`},[e(`span`,null,`Upgrading a running deployment`)])],-1),l[853]||=e(`p`,null,[r(`The map at the top of this guide is a reading order for the `),e(`strong`,null,`code`),r(`: six passes over the API, in the order it is worth working through them. This section is the other question — the order to do things in when what you are upgrading is a `),e(`em`,null,`deployment`),r(`, with a database somebody depends on and nodes that are currently running. Every step links the page that owns it; nothing here is new.`)],-1),l[854]||=e(`p`,null,[r(`Two of the seven happen `),e(`strong`,null,`while you are still on 3.x`),r(`, and they are the two that are painful to discover afterwards.`)],-1),e(`p`,null,[l[81]||=e(`strong`,null,`1. Get off binary serialization, on 3.x.`,-1),l[82]||=r(` .NET removed `,-1),l[83]||=e(`code`,null,`BinaryFormatter`,-1),l[84]||=r(` and 4.x cannot read a blob written by it. The conversion needs `,-1),l[85]||=e(`code`,null,`BinaryObjectSerializer`,-1),l[86]||=r(`, which exists on 3.x and not here, so it is a 3.x task by construction — either let the scheduler rewrite blobs as it runs, or run a program that loads and writes back every serialized asset. `,-1),c(g,{to:`/documentation/quartz-4.x/packages/json-serialization.html#migrating-from-binary-serialization`},{default:i(()=>[...l[80]||=[r(`Migrating from binary serialization`,-1)]]),_:1}),l[87]||=r(` says which blobs rewrite themselves, which never do, and how to find what is left. `,-1),l[88]||=e(`code`,null,`BLOB_TRIGGERS.BLOB_DATA`,-1),l[89]||=r(` is the one that cannot be converted from 4.x at all.`,-1)]),l[855]||=e(`p`,null,[e(`strong`,null,`2. Audit the stored cron expressions, on 3.x.`),r(` 4.x rejects several expressions 3.x stored happily, and it rejects them while `),e(`em`,null,`materialising the row`),r(` — so a surviving expression fails the read of the trigger rather than only its next firing. The SQL to find candidates, and the honest caveat that several of its clauses are over-wide, are in `),e(`a`,{href:`#before-you-upgrade`},`Before you upgrade`),r(`.`)],-1),e(`p`,null,[l[91]||=e(`strong`,null,`3. Decide what the window is.`,-1),l[92]||=r(` Either stop the cluster, or accept that a 3.x node and a 4.0 node will run against one set of tables for as long as the rollout takes. `,-1),c(g,{to:`/documentation/quartz-4.x/operations.html#a-mixed-3-x-and-4-0-window`},{default:i(()=>[...l[90]||=[r(`A mixed 3.x and 4.0 window`,-1)]]),_:1}),l[93]||=r(` is what has been checked about that state and what has not — including the one thing not to do during it, which is let a 4.0 node write a calendar.`,-1)]),e(`p`,null,[l[95]||=e(`strong`,null,`4. Run the mandatory migration script.`,-1),l[96]||=r(),l[97]||=e(`code`,null,`schema_30_to_40_upgrade_<database>.sql`,-1),l[98]||=r(` — one file per database, guarded throughout, safe to run twice, and safe to run with 3.x nodes still firing: `,-1),l[99]||=e(`a`,{href:`#database-schema-migration`},`Database Schema Migration`,-1),l[100]||=r(`. It is mandatory even for a database that took every optional 3.x migration going, because 4.x validates the schema and `,-1),l[101]||=e(`code`,null,`QRTZ_PAUSED_JOB_GRPS`,-1),l[102]||=r(` is new. The index set is the `,-1),l[103]||=e(`em`,null,`other`,-1),l[104]||=r(` file in that folder, and it is step 7. Schema before nodes, always: an old node tolerates a new schema and a new node does not tolerate an old one — `,-1),c(g,{to:`/documentation/quartz-4.x/operations.html#schema-first-then-nodes`},{default:i(()=>[...l[94]||=[r(`Schema first, then nodes`,-1)]]),_:1}),l[105]||=r(`.`,-1)]),e(`p`,null,[l[107]||=e(`strong`,null,`5. Translate the configuration.`,-1),l[108]||=r(` Flat `,-1),l[109]||=e(`code`,null,`quartz.*`,-1),l[110]||=r(` keys still work and mean the same thing, so this can be done gradually — `,-1),l[111]||=e(`a`,{href:`#configuration`},`Configuration`,-1),l[112]||=r(` here, and the whole key table in `,-1),c(g,{to:`/documentation/quartz-4.x/configuration/reference.html#legacy-property-keys`},{default:i(()=>[...l[106]||=[r(`Legacy property keys`,-1)]]),_:1}),l[113]||=r(`. Two things to read before the first start rather than after it: `,-1),l[114]||=e(`a`,{href:`#defaults-that-changed`},`Defaults that changed`,-1),l[115]||=r(`, where an application that never named its scheduler stops seeing its own rows, and `,-1),l[116]||=e(`a`,{href:`#silent-behaviour-changes`},`Silent behaviour changes`,-1),l[117]||=r(`.`,-1)]),e(`p`,null,[l[120]||=e(`strong`,null,`6. Start 4.0.`,-1),l[121]||=r(` It validates that every table it will read exists and names the missing one if not. It reads the job type names 3.x stored — `,-1),l[122]||=e(`code`,null,`SimpleTypeLoader`,-1),l[123]||=r(` maps the renamed types, and a type of your own that moved is an alias you declare, in `,-1),c(g,{to:`/documentation/quartz-4.x/configuration/reference.html#type-loader`},{default:i(()=>[...l[118]||=[r(`Type loader`,-1)]]),_:1}),l[124]||=r(`. Then replace the nodes one at a time — `,-1),c(g,{to:`/documentation/quartz-4.x/operations.html#replacing-the-nodes`},{default:i(()=>[...l[119]||=[r(`Replacing the nodes`,-1)]]),_:1}),l[125]||=r(`.`,-1)]),l[856]||=e(`p`,null,[e(`strong`,null,[r(`7. Run `),e(`code`,null,`schema_30_to_40_indexes_<database>.sql`),r(`, once the last 3.x node is gone.`)]),r(` It is the optional half of the upgrade, in a file of its own precisely so that this step is one command rather than an instruction to run part of a file: `),e(`a`,{href:`#listing-indexes-optional`},`Listing indexes`),r(`, `),e(`a`,{href:`#the-acquisition-index-is-reshaped-optional`},`the reshaped acquisition index`),r(` and `),e(`a`,{href:`#the-misfire-index-is-dropped-optional`},`the dropped misfire index`),r(`. Run it top to bottom. It is deferred because it drops indexes a 3.x node's statements may still be driving scans from — the misfire index is the one 3.x sweeps misfires with — and because it drops and recreates the index `),e(`em`,null,`both`),r(` versions acquire on. On an offline upgrade, run it straight after step 4.`)],-1),e(`p`,null,[c(g,{to:`/documentation/quartz-4.x/production-checklist.html`},{default:i(()=>[...l[126]||=[r(`Before you go live`,-1)]]),_:1}),l[127]||=r(` is the checklist for the state you should be in when this is done.`,-1)]),l[857]||=t(`<h2 id="database-schema-migration" tabindex="-1"><a class="header-anchor" href="#database-schema-migration"><span>Database Schema Migration</span></a></h2><p>Quartz 4.x requires four columns on <code>QRTZ_TRIGGERS</code> (and one on <code>QRTZ_FIRED_TRIGGERS</code>) that were <strong>optional</strong> in 3.x:</p><table><thead><tr><th>Column</th><th>Table(s)</th><th>Optional since</th></tr></thead><tbody><tr><td><code>MISFIRE_ORIG_FIRE_TIME</code></td><td><code>QRTZ_TRIGGERS</code></td><td>3.17</td></tr><tr><td><code>EXECUTION_GROUP</code></td><td><code>QRTZ_TRIGGERS</code>, <code>QRTZ_FIRED_TRIGGERS</code></td><td>3.18</td></tr><tr><td><code>PREFERRED_NODE</code></td><td><code>QRTZ_TRIGGERS</code></td><td>3.19</td></tr><tr><td><code>PREFERRED_NODE_AUTO</code></td><td><code>QRTZ_TRIGGERS</code></td><td>3.19</td></tr></tbody></table><p>3.x probed for each of these at startup and disabled the corresponding feature when it was missing. <strong>4.x removed those probes</strong> and assumes all of them exist, so this migration is mandatory even if you never used misfire reporting, execution groups or node affinity.</p><p>4.x also adds two columns 3.x never had:</p><table><thead><tr><th>Column</th><th>Table</th><th>Holds</th></tr></thead><tbody><tr><td><code>RETRY_POLICY</code></td><td><code>QRTZ_TRIGGERS</code></td><td>A trigger&#39;s retry policy, as its stored string form</td></tr><tr><td><code>RETRY_ATTEMPT</code></td><td><code>QRTZ_TRIGGERS</code></td><td>How many retries of the occurrence being executed have already been made</td></tr></tbody></table><p>Both are nullable with no default, so every row an upgrade brings across reads as &quot;no retry policy&quot; and nothing has to be backfilled. They are in the 4.0 script because 4.x no longer probes for columns: a column added after 4.0 ships would be a required column, and so a mandatory migration in the middle of a major version. The names are on <code>AdoConstants</code> as <code>ColumnRetryPolicy</code> and <code>ColumnRetryAttempt</code>.</p><p>4.x also adds a table 3.x never had:</p><table><thead><tr><th>Table</th><th>Holds</th></tr></thead><tbody><tr><td><code>QRTZ_PAUSED_JOB_GRPS</code></td><td>One row per paused job group — <code>SCHED_NAME</code>, <code>JOB_GROUP</code></td></tr></tbody></table><p>It mirrors <code>QRTZ_PAUSED_TRIGGER_GRPS</code>, and it is what makes <a href="#job-store-listings-became-queries"><code>JobGroup.Paused</code> truthful on the ADO store</a>. A group can be paused while it holds no jobs, so there is no row on <code>QRTZ_JOB_DETAILS</code> to hang a flag on — the trigger side made the same call for the same reason. At startup 4.x validates that every table it will read exists, and <code>QRTZ_PAUSED_JOB_GRPS</code> is one of them, so <strong>this migration is mandatory even for a 3.x database that took every optional migration going</strong>.</p><p>That check is a <code>SELECT 1</code> per table <strong>and</strong> a <code>SELECT &lt;column&gt; … WHERE 1 = 0</code> for each column this migration adds to a table 3.x already had, so a database that skipped it is refused before the scheduler starts, with the column and this script named in the message. What the check cannot see is a column&#39;s <em>shape</em>: a hand-built table whose column is the wrong type or width still starts cleanly and fails later, on the first statement that binds it, with whatever your provider says. Run the script in full rather than section by section, and if a startup succeeded but a firing failed with a raw provider error, the column definitions are the first thing to check.</p><p><code>ProvisionSchema()</code> is not a way around this. Before it creates anything it asks whose schema this is: a table it needs that is already there and short of a column it needs was made by something that is not 4.x, so a 3.x database is refused rather than half-completed. Creating the one table 4.x added and leaving the columns it added missing would produce a scheduler that starts, reports itself provisioned, and then fails every acquisition for ever.</p><p>A <strong>paused job group does not survive the upgrade</strong>, and cannot: 3.x&#39;s ADO store records nothing when it pauses one — its <code>IsJobGroupPaused</code> returns a hard-coded <code>false</code> and <code>PauseJobs</code> only pauses the individual triggers — so there is nothing for the migration to put in <code>QRTZ_PAUSED_JOB_GRPS</code>. The triggers that were paused stay paused, so no job starts running that was not running before; what is lost is the <em>group&#39;s</em> pause, so a trigger added to that job group after the upgrade is born running. Paused <strong>trigger</strong> groups are unaffected: they were already a <code>QRTZ_PAUSED_TRIGGER_GRPS</code> row on 3.x and migrate with the rest of the schema.</p><p>If you rely on a paused job group, <strong>pause it again once you are on 4.x</strong>:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token keyword">await</span> scheduler<span class="token punctuation">.</span><span class="token function">PauseJobGroups</span><span class="token punctuation">(</span>GroupMatcher<span class="token operator">&lt;</span>JobKey<span class="token operator">&gt;</span><span class="token punctuation">.</span><span class="token function">GroupEquals</span><span class="token punctuation">(</span><span class="token string">&quot;nightly&quot;</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>That writes the group&#39;s row to <code>QRTZ_PAUSED_JOB_GRPS</code>, which is what makes <code>JobGroup.Paused</code> and <code>IsJobGroupPaused</code> answer truthfully — and from then on the pause is imposed on the jobs added to that group afterwards, so a trigger stored for one of them is born <code>PAUSED</code>. An equality matcher records a group that holds no job at all, so the call is also how you pause a group before anything is deployed into it. Both stores behave that way on 4.x; see <a href="#the-two-job-stores-answer-the-same-way">The two job stores answer the same way</a>.</p><div class="hint-container warning"><p class="hint-container-title">Warning</p><p>Always run migration scripts in a test environment against a copy of your production database first.</p></div><div class="hint-container warning"><p class="hint-container-title">There is a step before this one</p><p>4.x rejects several cron expressions 3.x stored happily, and it rejects them while materialising the row — so a surviving expression fails the <em>read</em> of the trigger rather than only its next firing. Run the audit in <a href="#before-you-upgrade">Before you upgrade</a> against the live 3.x database before the window opens; the schema migration does not look at the expressions it carries across. <a href="#upgrading-a-running-deployment">Upgrading a running deployment</a> is the whole ordered sequence.</p></div><p><a href="https://github.com/quartznet/quartznet/tree/main/database/migrations/4.0" target="_blank" rel="noopener noreferrer">database/migrations/4.0/</a> holds <strong>two</strong> files per database, and they are run at two different moments:</p><table><thead><tr><th>File</th><th>Status</th><th>When</th></tr></thead><tbody><tr><td><code>schema_30_to_40_upgrade_&lt;database&gt;.sql</code></td><td><strong>Mandatory</strong></td><td>Now. Everything in it is additive and safe to run while 3.x nodes are still firing.</td></tr><tr><td><code>schema_30_to_40_indexes_&lt;database&gt;.sql</code></td><td>Optional, performance only</td><td>Once the last 3.x node has shut down — or straight afterwards, if nothing is running.</td></tr></tbody></table><p><code>&lt;database&gt;</code> is <code>sqlServer</code>, <code>postgres</code>, <code>mysql_innodb</code>, <code>oracle</code>, <code>sqlite</code> or <code>firebird</code>. Every statement in both checks first, so each is safe to run whether or not you already applied the optional 3.x migrations, and safe to run twice. The three sections below describe what the <em>index</em> file does.</p><p>For SQL Server the column additions look like this:</p><div class="language-sql line-numbers-mode" data-highlighter="prismjs" data-ext="sql"><pre><code class="language-sql"><span class="line"><span class="token keyword">IF</span> COL_LENGTH<span class="token punctuation">(</span><span class="token string">&#39;QRTZ_TRIGGERS&#39;</span><span class="token punctuation">,</span><span class="token string">&#39;MISFIRE_ORIG_FIRE_TIME&#39;</span><span class="token punctuation">)</span> <span class="token operator">IS</span> <span class="token boolean">NULL</span></span>
<span class="line"><span class="token keyword">BEGIN</span></span>
<span class="line">  <span class="token keyword">ALTER</span> <span class="token keyword">TABLE</span> <span class="token punctuation">[</span>dbo<span class="token punctuation">]</span><span class="token punctuation">.</span><span class="token punctuation">[</span>QRTZ_TRIGGERS<span class="token punctuation">]</span> <span class="token keyword">ADD</span> <span class="token punctuation">[</span>MISFIRE_ORIG_FIRE_TIME<span class="token punctuation">]</span> <span class="token keyword">bigint</span> <span class="token boolean">NULL</span><span class="token punctuation">;</span></span>
<span class="line"><span class="token keyword">END</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Replace <code>QRTZ_</code> with your configured table prefix if different.</p>`,24),e(`p`,null,[l[129]||=r(`See `,-1),c(g,{to:`/documentation/database/schema-changes.html`},{default:i(()=>[...l[128]||=[r(`Database Schema Changes`,-1)]]),_:1}),l[130]||=r(` for the full version-by-version history, including what each optional 3.x migration does and what skipping it costs.`,-1)]),l[858]||=t(`<h3 id="listing-indexes-optional" tabindex="-1"><a class="header-anchor" href="#listing-indexes-optional"><span>Listing indexes (optional)</span></a></h3><p>The index file aligns the index set with the statements 4.x issues. Two of the additions matter most for the <a href="#job-store-listings-became-queries">job and trigger listings</a>:</p><table><thead><tr><th>Index</th><th>Table and columns</th></tr></thead><tbody><tr><td><code>IDX_QRTZ_J_G_N</code></td><td><code>QRTZ_JOB_DETAILS(SCHED_NAME, JOB_GROUP, JOB_NAME)</code></td></tr><tr><td><code>IDX_QRTZ_T_G_N</code></td><td><code>QRTZ_TRIGGERS(SCHED_NAME, TRIGGER_GROUP, TRIGGER_NAME)</code></td></tr></tbody></table><p>Listings page with <code>ORDER BY JOB_GROUP, JOB_NAME</code> and <code>ORDER BY TRIGGER_GROUP, TRIGGER_NAME</code>, and the primary keys are name-before-group, so no existing index serves those ordered scans. <strong>These are optional</strong> — the queries work without them, but each page becomes a scan plus a sort. Add them if you list jobs or triggers from a large schema. They are in the fresh-install scripts for every dialect already.</p><p>It also drops indexes that are a leftmost prefix of a wider one, or that no 4.x statement can drive a scan from. PostgreSQL gets the largest change: several of its indexes omitted <code>SCHED_NAME</code>, which is the leading column of every predicate Quartz issues, so they could not serve a single-scheduler lookup at all.</p><h3 id="the-acquisition-index-is-reshaped-optional" tabindex="-1"><a class="header-anchor" href="#the-acquisition-index-is-reshaped-optional"><span>The acquisition index is reshaped (optional)</span></a></h3><p>The same file drops and recreates <code>IDX_QRTZ_T_NFT_ST</code>, the index acquisition runs on (<a href="https://github.com/quartznet/quartznet/issues/3510" target="_blank" rel="noopener noreferrer">#3510</a>):</p><table><thead><tr><th></th><th>Table and columns</th></tr></thead><tbody><tr><td>3.x</td><td><code>QRTZ_TRIGGERS(SCHED_NAME, TRIGGER_STATE, NEXT_FIRE_TIME)</code></td></tr><tr><td>4.x</td><td><code>QRTZ_TRIGGERS(SCHED_NAME, TRIGGER_STATE, NEXT_FIRE_TIME ASC, PRIORITY DESC, MISFIRE_INSTR)</code></td></tr><tr><td>4.x, Firebird</td><td><code>QRTZ_TRIGGERS(SCHED_NAME, TRIGGER_STATE, NEXT_FIRE_TIME)</code> — unchanged</td></tr></tbody></table>`,8),e(`p`,null,[l[132]||=r(`Acquisition orders by `,-1),l[133]||=e(`code`,null,`NEXT_FIRE_TIME ASC, PRIORITY DESC`,-1),l[134]||=r(`, so an index carrying both directions lets the engine take the first entry instead of reading every due trigger and sorting it: against 100,000 triggers with 5,000 due, one acquisition on SQL Server goes from 21.6 ms and 20,395 logical reads to 0.6 ms and 8. `,-1),l[135]||=e(`code`,null,`MISFIRE_INSTR`,-1),l[136]||=r(` keeps a backlog of misfired triggers from costing a table lookup per row skipped. The statement, and every acquisition contract around it, is unchanged — this is DDL alone. Firebird's indexes take one direction for the whole index, so it keeps the 3.x shape; see `,-1),c(g,{to:`/documentation/quartz-4.x/db/#indexes-and-the-acquisition-index-in-particular`},{default:i(()=>[...l[131]||=[r(`Database Schema`,-1)]]),_:1}),l[137]||=r(` for the numbers and the MySQL and Oracle footnotes.`,-1)]),l[859]||=t(`<p><strong>If you built a 4.0 preview schema before this landed</strong>, re-run <code>schema_30_to_40_indexes_&lt;database&gt;.sql</code>. The index name did not change, so the script drops it before recreating it; a guarded <code>CREATE INDEX</code> on its own would find the name taken and keep the old three columns.</p><h3 id="the-misfire-index-is-dropped-optional" tabindex="-1"><a class="header-anchor" href="#the-misfire-index-is-dropped-optional"><span>The misfire index is dropped (optional)</span></a></h3><p>The same file drops <code>IDX_QRTZ_T_NFT_ST_MISFIRE</code> (<a href="https://github.com/quartznet/quartznet/issues/3656" target="_blank" rel="noopener noreferrer">#3656</a>):</p><table><thead><tr><th></th><th>Table and columns</th></tr></thead><tbody><tr><td>3.x, on SQL Server, MySQL, Oracle and Firebird</td><td><code>QRTZ_TRIGGERS(SCHED_NAME, MISFIRE_INSTR, NEXT_FIRE_TIME, TRIGGER_STATE)</code></td></tr><tr><td>3.x, on PostgreSQL and SQLite</td><td>never created</td></tr><tr><td>4.x</td><td>dropped on every dialect</td></tr></tbody></table><p>It led with <code>MISFIRE_INSTR</code>, which the misfire sweep and the counting peek that precedes each pass both compare with <code>&lt;&gt; -1</code> — a predicate no B-tree can seek past, so the index could only ever be entered on <code>SCHED_NAME</code>. Reshaping the acquisition index above gave it the two equalities, the range and the residual those two statements actually want, and measured plan by plan on all four engines that had the misfire index, no optimizer picks it for either statement (<a href="https://github.com/quartznet/quartznet/issues/3608" target="_blank" rel="noopener noreferrer">#3608</a>, <a href="https://github.com/quartznet/quartznet/issues/3656" target="_blank" rel="noopener noreferrer">#3656</a>). MySQL alone appeared to, because <code>MySQLDelegate</code> named it in a <code>FORCE INDEX</code> hint; that hint now names the acquisition index. What was left is an index maintained on every trigger write and read by nothing.</p><p><strong>The order inside the file is the precondition.</strong> The reshape above is in the same file and comes first, so a schema still carrying the three-column acquisition index is brought to the 4.x shape before this drop runs, and no schema is ever left with neither index. Run the file top to bottom, and re-run it as often as you like — every statement in it is guarded.</p><p><strong>3.x reads this index</strong>, which is the other half of why the index file waits for the last 3.x node to shut down.</p><p>Full table creation scripts for fresh installations are available in <a href="https://github.com/quartznet/quartznet/tree/main/database/tables" target="_blank" rel="noopener noreferrer">database/tables/</a>.</p><h2 id="defaults-that-changed" tabindex="-1"><a class="header-anchor" href="#defaults-that-changed"><span>Defaults that changed</span></a></h2><p>Almost none of them did. Every option that carried a default on 3.x carries the same one on 4.x — the thread pool&#39;s size, the ADO store&#39;s misfire threshold and misfire batch, the retry intervals, the table prefix, the cluster check-in interval, the hosted service&#39;s two switches. If a value matters to you and you set it, nothing here applies.</p><p>Three effective defaults did change, and they change for one group of users only: those who never configured Quartz at all. <code>StdSchedulerFactory.Initialize()</code> seeded them from an embedded <code>quartz.config</code> that 4.x does not read, so a scheduler built by <code>new StdSchedulerFactory()</code> or <code>GetDefaultScheduler()</code> — and nothing else — was running on them.</p><table><thead><tr><th>Setting</th><th>3.x, unconfigured</th><th>4.x</th></tr></thead><tbody><tr><td><code>quartz.scheduler.instanceName</code></td><td><code>DefaultQuartzScheduler</code></td><td><code>QuartzScheduler</code> (<code>QuartzSchedulerOptions.InstanceName</code>). <strong>Persistent stores key every row on this</strong>, so an upgraded application that never named its scheduler stops seeing its own jobs and triggers until it sets the old name back</td></tr><tr><td><code>quartz.jobStore.misfireThreshold</code></td><td>60 seconds</td><td>5 seconds (<code>InMemoryJobStoreOptions.MisfireThreshold</code>). This was always the in-memory store&#39;s <em>code</em> default on both branches; the embedded file was what raised it</td></tr><tr><td><code>quartz.threadPool.threadCount</code></td><td>10</td><td>10 (<code>ThreadPoolOptions.MaxConcurrency</code>) — unchanged, listed so the table is the whole of it</td></tr></tbody></table><p>Handing the 3.x factory properties always bypassed the file, so <code>new StdSchedulerFactory(properties)</code> and <code>AddQuartz</code> already fell back to the typed defaults. See <a href="#the-quartz-config-file-is-no-longer-read">The <code>quartz.config</code> file is no longer read</a>.</p><p>One default is new rather than changed: <code>QuartzSchedulerOptions.PropagateTraceContext</code> is on, so a trigger scheduled inside an <code>Activity</code> carries two extra entries in its data map. See <a href="#silent-behaviour-changes">Silent behaviour changes</a>.</p><h2 id="silent-behaviour-changes" tabindex="-1"><a class="header-anchor" href="#silent-behaviour-changes"><span>Silent behaviour changes</span></a></h2><p>Most of this guide is about code that stops compiling, which is the easy half: the compiler says where to look. This section is the other half — <strong>calls that still compile and now do something different</strong>. Read it before the first run of the upgraded application rather than after it.</p><h3 id="reading-job-data" tabindex="-1"><a class="header-anchor" href="#reading-job-data"><span>Reading job data</span></a></h3><p>Five of these are in one place, because 3.x had two overlapping accessor sets and 4.x kept the shorter names with the longer set&#39;s semantics. If you called <code>GetIntValue</code>, <code>GetBooleanValue</code> and friends, nothing here moved for you; if you called <code>GetInt</code> and <code>GetBoolean</code>, all of it did.</p><ul><li><strong>The indexer throws on a missing key.</strong> <code>map[&quot;absent&quot;]</code> returned <code>null</code> on 3.x. On 4.x it goes straight to the backing dictionary and throws <code>KeyNotFoundException</code>, on both <code>JobDataMap</code> and <code>SchedulerContext</code>. The &quot;read an optional entry&quot; idiom has to become <code>TryGetValue</code> or a <code>TryGet…</code> accessor.</li><li><strong><code>GetString</code> no longer throws.</strong> 3.x threw <code>KeyNotFoundException</code> for an absent key and <code>InvalidCastException</code> for a value that was not a string. 4.x returns <code>null</code> for both, so a mistyped key and a wrongly-typed value are indistinguishable from &quot;no value&quot;.</li><li><strong>The typed accessors throw a different exception for an absent key.</strong> <code>GetInt</code>, <code>GetDouble</code>, <code>GetBoolean</code> and the rest threw <code>KeyNotFoundException</code> on 3.x and throw <code>InvalidCastException</code> on 4.x. A <code>catch (KeyNotFoundException)</code> around one stops catching.</li><li><strong><code>GetBoolean</code> reads anything that is not <code>&quot;true&quot;</code> as <code>false</code>.</strong> 3.x used <code>Convert.ToBoolean</code>, so a stored <code>&quot;1&quot;</code> or <code>&quot;yes&quot;</code> threw <code>InvalidCastException</code>. 4.x compares against <code>&quot;true&quot;</code> case-insensitively and <em>succeeds</em>, so a flag stored as <code>&quot;1&quot;</code> used to fail loudly and now quietly reads as off. <code>TryGetBoolean</code> reports success for it too.</li><li><strong>Strings are parsed with the invariant culture.</strong> <code>GetInt</code>, <code>GetLong</code>, <code>GetDouble</code>, <code>GetFloat</code> and <code>Get&lt;decimal&gt;</code> used the current culture when the stored value was a string. On a comma-decimal culture <code>&quot;3.14&quot;</code> used to read as <code>314</code> and now reads as <code>3.14</code>. The reverse case is the one to check your stored data for: <code>&quot;3,14&quot;</code> used to read as <code>3.14</code> and now throws <code>InvalidCastException</code> from every numeric accessor — none of them allows a group separator, precisely so that a comma written as a decimal point cannot be read as one and answer <code>314</code>. The new reading is the correct one for data written by an invariant-culture writer, and it is still a different number from the one the job saw yesterday.</li><li><strong><code>Get&lt;DateTime&gt;</code> parses with <code>DateTimeStyles.RoundtripKind</code></strong>, so a stored string ending in <code>Z</code> comes back as <code>Kind=Utc</code> rather than shifted to local time — see <a href="#putasstring-writes-round-trip-formats-now"><code>PutAsString</code> writes round-trip formats now</a>.</li><li><strong>The scheduler context is no longer merged into job properties</strong> — see <a href="#scheduler-context-entries-are-no-longer-injected-into-job-properties">Scheduler context entries are no longer injected into job properties</a>.</li></ul><h3 id="serialization-and-the-store" tabindex="-1"><a class="header-anchor" href="#serialization-and-the-store"><span>Serialization and the store</span></a></h3>`,20),e(`ul`,null,[l[142]||=t(`<li><strong><code>quartz.serializer.type = json</code> means System.Text.Json now.</strong> On 3.x the <code>json</code> alias expanded to the Newtonsoft serializer, with a logged warning that the name was ambiguous. On 4.x <code>json</code> and <code>stj</code> both resolve to <code>SystemTextJsonObjectSerializer</code>, and nothing warns. A configuration file carried over verbatim therefore changes which serializer writes and reads the store&#39;s blobs, which is a different payload shape and a different set of registered trigger and calendar serializers. Spell it <code>newtonsoft</code> if Newtonsoft is what you meant.</li><li><strong>Both serializers refuse a job data value they cannot read back</strong>, at write time rather than at the next read, and both refuse the same set — see <a href="#both-serializers-refuse-a-job-data-value-they-cannot-read-back">Both serializers refuse a job data value they cannot read back</a>.</li><li><strong>A <code>Dictionary&lt;string, string&gt;</code> job data value is written the same way by both serializers</strong>, which is a change to what Newtonsoft writes — see <a href="#a-string-dictionary-is-written-the-same-way-by-both-serializers">A string dictionary is written the same way by both serializers</a>.</li>`,3),e(`li`,null,[l[139]||=e(`strong`,null,`Five calendar types serialize to a new shape.`,-1),l[140]||=r(` 4.x reads both forms and 3.x reads only its own, so this is invisible on a clean cut-over and fatal in a mixed window — see `,-1),c(g,{to:`/documentation/quartz-4.x/operations.html#a-mixed-3-x-and-4-0-window`},{default:i(()=>[...l[138]||=[r(`A mixed 3.x and 4.0 window`,-1)]]),_:1}),l[141]||=r(`.`,-1)]),l[143]||=e(`li`,null,[e(`strong`,null,`The formerly optional columns are required`),r(`, and an unmigrated schema fails at the first trigger load or store with a provider-level missing-column error rather than at startup — see `),e(`a`,{href:`#the-optional-columns-are-required-so-the-probes-are-gone`},`The optional columns are required, so the probes are gone`),r(`.`)],-1),l[144]||=e(`li`,null,[e(`strong`,null,`The two stores now answer the same way`),r(` where they used to disagree, which changes what the ADO store does in six places — see `),e(`a`,{href:`#the-two-job-stores-answer-the-same-way`},`The two job stores answer the same way`),r(`.`)],-1)]),l[860]||=t(`<h3 id="lifecycle-and-telemetry" tabindex="-1"><a class="header-anchor" href="#lifecycle-and-telemetry"><span>Lifecycle and telemetry</span></a></h3><ul><li><strong><code>Standby()</code> after <code>Shutdown()</code> throws</strong> instead of silently pausing something already stopped — see <a href="#the-transitions-are-honest-about-themselves">The transitions are honest about themselves</a>.</li><li><strong>A trigger scheduled inside an <code>Activity</code> carries two extra job-data entries.</strong><code>QuartzSchedulerOptions.PropagateTraceContext</code> is on by default, and the two reserved keys are visible wherever trigger data is: <code>MergedJobDataMap</code>, the dashboard, <code>GET /triggers</code>. Turn it off with <code>q.ConfigureScheduler(options =&gt; options.PropagateTraceContext = false)</code>.</li></ul><h3 id="the-end-time-is-the-last-instant-at-which-a-trigger-may-fire" tabindex="-1"><a class="header-anchor" href="#the-end-time-is-the-last-instant-at-which-a-trigger-may-fire"><span>The end time is the last instant at which a trigger may fire</span></a></h3><p><code>EndTimeUtc</code> means one thing on 4.x, for every trigger type: it is the last instant at which the trigger may fire, so a fire time exactly equal to it is one the trigger fires and the first instant after it is where the schedule stops. That is what Java Quartz&#39;s <code>AbstractTrigger</code> has always documented — &quot;the time after which the trigger will not fire&quot; — and on 3.x the types disagreed about it. Three of them change:</p><table><thead><tr><th>Trigger</th><th>3.x</th><th>4.x</th></tr></thead><tbody><tr><td><code>SimpleTriggerImpl</code></td><td>A fire time equal to <code>EndTimeUtc</code> is dropped</td><td>It fires</td></tr><tr><td><code>CalendarIntervalTriggerImpl</code></td><td>A fire time equal to <code>EndTimeUtc</code> is dropped</td><td>It fires</td></tr><tr><td><code>DailyTimeIntervalTriggerImpl</code></td><td>Fires <em>past</em> <code>EndTimeUtc</code> until the daily window closes, when the end time falls between two fire times; <code>FinalFireTimeUtc</code> reports the close of the daily window even when that is past <code>EndTimeUtc</code></td><td>Firing stops at the end time, and <code>FinalFireTimeUtc</code> is never past it</td></tr><tr><td><code>CronTriggerImpl</code></td><td>A fire time equal to <code>EndTimeUtc</code> fires</td><td>Unchanged</td></tr><tr><td><code>RecurrenceTriggerImpl</code></td><td>New in 4.x</td><td>A fire time equal to <code>EndTimeUtc</code> fires</td></tr></tbody></table><p>A simple or calendar-interval trigger whose <code>EndAt</code> lands exactly on one of its fire times therefore fires one more time than it did on 3.x, and a daily-time-interval trigger whose <code>EndAt</code> lands between two of them fires fewer. Move the end time a second off the boundary to keep the old count.</p><p><code>TriggerFireTimes.ComputeBetween</code> inherits the rule, because it bounds the walk by handing the window&#39;s end to the trigger as its <code>EndTimeUtc</code>: its <code>to</code> is inclusive, for every trigger type, and a fire time landing exactly on it is listed.</p><h3 id="ordering" tabindex="-1"><a class="header-anchor" href="#ordering"><span>Ordering</span></a></h3><ul><li><strong><code>JobKey</code> and <code>TriggerKey</code> compare ordinally</strong>, where 3.x compared with the current culture, so any sorted listing of keys comes out in a different order — see <a href="#key-t-moved-to-quartz-and-is-immutable"><code>Key&lt;T&gt;</code> moved to <code>Quartz</code> and is immutable</a>.</li></ul><h3 id="null-arguments-raise-argumentnullexception" tabindex="-1"><a class="header-anchor" href="#null-arguments-raise-argumentnullexception"><span>Null arguments raise <code>ArgumentNullException</code></span></a></h3><p><strong>Every</strong> <code>IScheduler</code> member does, reading as well as mutating — <code>GetJobDetail</code>, <code>GetTrigger</code>, <code>GetTriggerState</code>, <code>GetTriggersOfJob</code>, <code>GetCalendar</code>, the three <code>Exists</code> overloads and the six <code>Query*</code> members included. beta.1 brought the mutation members under the rule and left the reads as they were, which is two contracts on one interface with nothing to tell a caller which member is which; a read that was handed a null used to fault wherever the null was first dereferenced, which for a store-backed read is inside the store.</p><p><code>CronExpression.ResolveHash</code>&#39;s two overloads are aligned too: they raised the base <code>ArgumentException</code> while the constructor and <code>Parse</code> on the same type raised <code>ArgumentNullException</code>. <code>ArgumentNullException</code> derives from <code>ArgumentException</code>, so an existing <code>catch</code> still catches.</p><p><code>IScheduler</code>&#39;s mutation members raise <code>ArgumentNullException</code>, naming the parameter, where 3.x raised <code>SchedulerException(&quot;JobDetail cannot be null&quot;)</code> from <code>ScheduleJob(null, trigger)</code> and its neighbours. That was Java parity and it was undocumented, so a caller writing <code>catch (ArgumentNullException)</code> caught nothing while one writing <code>catch (SchedulerException)</code> around a scheduling call was swallowing its own bug along with the scheduler&#39;s failures. The new behaviour is what the <code>SchedulerQueryExtensions</code> methods on the same type have always done. <code>RescheduleJob</code> raised <code>ArgumentException</code>, of which <code>ArgumentNullException</code> is a subclass, so a <code>catch</code> there still catches. The <code>&lt;exception&gt;</code> tags on <code>IScheduler</code> say all of it, along with which members refuse what — 3.x had two of them over 65 members and 4.0 has ninety-seven.</p><h3 id="between-the-alphas-and-beta-1" tabindex="-1"><a class="header-anchor" href="#between-the-alphas-and-beta-1"><span>Between the alphas and beta.1</span></a></h3><p>Every correction made between one 4.0 pre-release and the next is now written into the section it belongs to, as one statement about 3.x and 4.0. The build-by-build list — which pre-release introduced each of them, and what an application already running one has to change — is <a href="#appendix-if-you-ran-a-4-0-pre-release">Appendix: if you ran a 4.0 pre-release</a>.</p><h3 id="what-is-on-nobody-s-list-because-it-did-not-change" tabindex="-1"><a class="header-anchor" href="#what-is-on-nobody-s-list-because-it-did-not-change"><span>What is on nobody&#39;s list because it did not change</span></a></h3><p>Worth stating, because the reasonable fear is that everything moved. These were checked against both branches and are identical: every misfire instruction&#39;s numeric value, so a <code>MISFIRE_INSTR</code> column carried over still means what it meant; <code>TriggerState</code>&#39;s existing values, with <code>Executing</code> appended; the misfire computation of every trigger family; the merge order and content of <code>MergedJobDataMap</code>; the rule that <code>[PersistJobDataAfterExecution]</code> writes back only a map that was modified; the lock handler a store picks for itself; <code>StringOperator</code>&#39;s matching; and the effective default of <code>Shutdown(waitForJobsToComplete)</code>.</p><h2 id="configuration" tabindex="-1"><a class="header-anchor" href="#configuration"><span>Configuration</span></a></h2><p>This is the largest change in 4.x. Configuration is now strongly typed options and service registrations rather than a bag of <code>quartz.*</code> strings, and the dependency injection container builds the scheduler.</p><h3 id="flat-keys-still-work" tabindex="-1"><a class="header-anchor" href="#flat-keys-still-work"><span>Flat keys still work</span></a></h3><p>If you configure Quartz from <code>appsettings.json</code> or a <code>NameValueCollection</code> of <code>quartz.*</code> keys, that keeps working. The keys are translated into the typed options, and both spellings of a setting always produce the same result. You do not have to migrate configuration files to move to 4.x.</p><h3 id="a-flat-property-bag-is-checked-for-keys-nobody-reads" tabindex="-1"><a class="header-anchor" href="#a-flat-property-bag-is-checked-for-keys-nobody-reads"><span>A flat property bag is checked for keys nobody reads</span></a></h3><p><code>AddQuartz(services, properties)</code> and <code>AddQuartz(services, name, properties)</code> check the bag the same way <code>QuartzSchedulerBuilder.UseProperties</code> always has: a <code>quartz.*</code> key Quartz does not read throws <code>SchedulerConfigException</code> at registration instead of being silently ignored, and a key 4.0 stopped reading is reported by name with the replacement. That is the commonest shape a 3.x application arrives in, so it is the one the advice is written for — <code>quartz.jobstore.type</code> differs from <code>quartz.jobStore.type</code> by one letter and used to turn a database-backed scheduler into an in-memory one without a word.</p><p>Set <code>quartz.checkConfiguration</code> to <code>false</code> to allow keys of your own. The <code>IConfiguration</code> overloads are still unchecked, because flattening a section invents <code>quartz.*</code> keys whether Quartz reads them or not.</p><h3 id="a-property-key-without-the-quartz-prefix-is-refused" tabindex="-1"><a class="header-anchor" href="#a-property-key-without-the-quartz-prefix-is-refused"><span>A property key without the <code>quartz.</code> prefix is refused</span></a></h3><p>The check above is on the bag handed to <code>AddQuartz</code>. The other way into that bag is <code>services.Configure&lt;QuartzOptions&gt;(…)</code>, and it was not checked at all. <code>QuartzOptions.Properties</code> holds the flat <code>quartz.*</code> keys and <code>QuartzPropertyBridge</code> is its only reader, so a key without that prefix is provably read by nobody — and unlike a misspelling, it has no symptom at all: the scheduler runs as if the application had said nothing. An <code>IValidateOptions&lt;QuartzOptions&gt;</code> now refuses one, at host startup along with the rest of the options validation:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line"> services.Configure&lt;QuartzOptions&gt;(options =&gt;</span>
<span class="line"></span><span class="token prefix unchanged"> </span><span class="token line"> {</span>
<span class="line"></span></span><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line">     options.Properties[&quot;scheduler.instanceName&quot;] = &quot;svc&quot;;</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line">     options.Properties[&quot;quartz.scheduler.instanceName&quot;] = &quot;svc&quot;;</span>
<span class="line"></span></span><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line"> });</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The commonest way to produce such a key is <code>services.Configure&lt;QuartzOptions&gt;(section)</code> against a section written for the 3.x <code>QuartzOptions</code>, which <em>was</em> a dictionary — a section already named <code>Quartz</code> does not supply the prefix, so <code>Quartz:Properties:scheduler.instanceName</code> lands as <code>scheduler.instanceName</code>. Note that the check cannot catch the whole of that mis-bind: a section whose keys match no property on <code>QuartzOptions</code> binds to nothing at all, and an options instance that came out empty is indistinguishable from one nobody configured. What is decidable is the keys that did arrive, and that is what is checked. Bind the section with <code>AddQuartz(configuration)</code> — the call that understands its shape — rather than with <code>Configure&lt;QuartzOptions&gt;</code>.</p><p>Unlike the registration-time check, this one does <strong>not</strong> compare the key against the ones Quartz reads: a configuration section is flattened into this bag with every sub-section turned into a <code>quartz.*</code> key whether Quartz reads it or not, so the stricter check would fail an <code>appsettings.json</code> holding settings for something else. <code>quartz.checkConfiguration = false</code> turns it off too.</p><h3 id="a-property-bag-is-any-dictionary" tabindex="-1"><a class="header-anchor" href="#a-property-bag-is-any-dictionary"><span>A property bag is any dictionary</span></a></h3><p><code>NameValueCollection</code> is a .NET Framework type from the <code>&lt;appSettings&gt;</code> era, and requiring it at the front door made every caller who already had a dictionary build one to get in. The parameter is now <code>IEnumerable&lt;KeyValuePair&lt;string, string?&gt;&gt;</code> — the shape <code>Dictionary&lt;string, string?&gt;</code>, <code>IReadOnlyDictionary&lt;string, string?&gt;</code> and <code>QuartzOptions.Properties</code> all already have, and the one <code>AddInMemoryCollection</code> takes.</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line">services<span class="token punctuation">.</span><span class="token function">AddQuartz</span><span class="token punctuation">(</span><span class="token keyword">new</span> <span class="token constructor-invocation class-name">Dictionary<span class="token punctuation">&lt;</span><span class="token keyword">string</span><span class="token punctuation">,</span> <span class="token keyword">string</span><span class="token punctuation">?</span><span class="token punctuation">&gt;</span></span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    <span class="token punctuation">[</span><span class="token string">&quot;quartz.scheduler.instanceName&quot;</span><span class="token punctuation">]</span> <span class="token operator">=</span> <span class="token string">&quot;core&quot;</span><span class="token punctuation">,</span></span>
<span class="line">    <span class="token punctuation">[</span><span class="token string">&quot;quartz.threadPool.maxConcurrency&quot;</span><span class="token punctuation">]</span> <span class="token operator">=</span> <span class="token string">&quot;10&quot;</span></span>
<span class="line"><span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>A <code>NameValueCollection</code> still goes in unchanged, in one call. It is exactly what a 3.x application holds — <code>StdSchedulerFactory</code> took one — so both <code>UseProperties</code> and <code>AddQuartz</code> keep an overload for it that forwards to the primary shape.</p><table><thead><tr><th>Member</th><th>3.x / earlier 4.0 preview</th><th>4.0</th></tr></thead><tbody><tr><td><code>QuartzSchedulerBuilder.UseProperties</code></td><td><code>UseProperties(NameValueCollection)</code></td><td><code>UseProperties(IEnumerable&lt;KeyValuePair&lt;string, string?&gt;&gt;)</code>, plus the <code>NameValueCollection</code> overload</td></tr><tr><td><code>AddQuartz(services, properties, …)</code></td><td><code>NameValueCollection</code></td><td><code>IEnumerable&lt;KeyValuePair&lt;string, string?&gt;&gt;</code>, plus the <code>NameValueCollection</code> overload</td></tr><tr><td><code>AddQuartz(services, name, properties, …)</code></td><td><code>NameValueCollection</code></td><td><code>IEnumerable&lt;KeyValuePair&lt;string, string?&gt;&gt;</code>, plus the <code>NameValueCollection</code> overload</td></tr><tr><td><code>QuartzOptions.ToNameValueCollection()</code></td><td>returned a <code>NameValueCollection</code></td><td><code>QuartzOptions.ToProperties()</code>, returning a <code>Dictionary&lt;string, string?&gt;</code> that goes straight back into either of the above</td></tr></tbody></table><p><code>KeyValuePair&lt;TKey, TValue&gt;</code> is a struct, so <code>IEnumerable&lt;KeyValuePair&lt;string, string&gt;&gt;</code> is not convertible to <code>IEnumerable&lt;KeyValuePair&lt;string, string?&gt;&gt;</code>. A <code>Dictionary&lt;string, string&gt;</code> whose values are genuinely never null therefore needs one conversion:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> services.AddQuartz(settings);</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> services.AddQuartz(settings.ToDictionary(x =&gt; x.Key, x =&gt; (string?) x.Value));</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p><code>AddQuartz(services, properties)</code> also copies the bag now, as <code>UseProperties</code> always has. The registration phases read it from closures that run later, some only when the container resolves options, so a caller that reused its own collection could reconfigure the scheduler long after <code>AddQuartz</code> returned.</p><h3 id="interrupting-jobs-on-shutdown-is-one-setting-with-four-answers" tabindex="-1"><a class="header-anchor" href="#interrupting-jobs-on-shutdown-is-one-setting-with-four-answers"><span>Interrupting jobs on shutdown is one setting with four answers</span></a></h3><p><code>QuartzSchedulerOptions.InterruptJobsOnShutdown</code> and <code>InterruptJobsOnShutdownWithWait</code> were two booleans, one for a shutdown that waits for running jobs and one for a shutdown that does not. They were never independent — together they answered a single four-way question, and setting one without thinking about the other was the easy mistake, because <code>InterruptJobsOnShutdown = true</code> reads as &quot;yes, interrupt&quot; and means &quot;only when not waiting&quot;.</p><p>They are replaced by <code>ShutdownJobInterruption</code>, an enum with all four answers spelled out:</p><table><thead><tr><th>Value</th><th>Old pair</th></tr></thead><tbody><tr><td><code>Never</code> (default)</td><td>both <code>false</code></td></tr><tr><td><code>WhenNotWaitingForJobs</code></td><td><code>InterruptJobsOnShutdown = true</code></td></tr><tr><td><code>WhenWaitingForJobs</code></td><td><code>InterruptJobsOnShutdownWithWait = true</code></td></tr><tr><td><code>Always</code></td><td>both <code>true</code></td></tr></tbody></table><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line"> q.ConfigureScheduler(options =&gt;</span>
<span class="line"></span><span class="token prefix unchanged"> </span><span class="token line"> {</span>
<span class="line"></span></span><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line">     options.InterruptJobsOnShutdown = true;</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line">     options.InterruptJobsOnShutdownWithWait = true;</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line">     options.ShutdownJobInterruption = ShutdownJobInterruption.Always;</span>
<span class="line"></span></span><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line"> });</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Configuration files need no change. Both flat keys still work and map onto the enum by the table above, and <code>Scheduler:InterruptJobsOnShutdown: true</code> in <code>appsettings.json</code> still means what it meant: the typed binder no longer knows that name, but every section is also flattened onto its <code>quartz.*</code> key, and the property bridge reads it there. <code>Scheduler:ShutdownJobInterruption: &quot;Always&quot;</code> is the new spelling, and binds directly.</p><h3 id="a-setting-stops-wearing-a-verb" tabindex="-1"><a class="header-anchor" href="#a-setting-stops-wearing-a-verb"><span>A setting stops wearing a verb</span></a></h3><p><code>IPersistentStoreBuilder.AcceptEnlistedTransactions()</code> was exactly <code>ConfigureStore(options =&gt; options.AcceptEnlistedTransactions = true)</code> — one assignment, no side effect — while the other nineteen <code>AdoJobStoreOptions</code> settings, <code>UseDbLocks</code> and <code>StoreJobDataAsStrings</code> among them, are set through <code>ConfigureStore</code> like everything else. A reader of the interface reasonably concluded that the settings with verbs were the important ones.</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line"> q.UsePersistentStore(store =&gt;</span>
<span class="line"></span><span class="token prefix unchanged"> </span><span class="token line"> {</span>
<span class="line"></span><span class="token prefix unchanged"> </span><span class="token line">     store.UsePostgres(connectionString);</span>
<span class="line"></span></span><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line">     store.AcceptEnlistedTransactions();</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line">     store.ConfigureStore(options =&gt; options.AcceptEnlistedTransactions = true);</span>
<span class="line"></span></span><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line"> });</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The option, the configuration key <code>quartz.jobStore.acceptEnlistedTransactions</code> and the <code>JobStore:AcceptEnlistedTransactions</code> section entry are all unchanged. <code>UseClustering()</code> keeps its verb: it genuinely does more than one assignment, setting <code>Enabled</code> on <code>ClusteringOptions</code> and <code>UseDbLocks</code> on <code>AdoJobStoreOptions</code>.</p><h3 id="two-names-that-said-the-wrong-thing" tabindex="-1"><a class="header-anchor" href="#two-names-that-said-the-wrong-thing"><span>Two names that said the wrong thing</span></a></h3><table><thead><tr><th>Was</th><th>Is</th><th>Why</th></tr></thead><tbody><tr><td><code>AdoJobStoreOptions.UseProperties</code></td><td><code>AdoJobStoreOptions.StoreJobDataAsStrings</code></td><td><code>UseProperties</code> reads as a verb and collided with <code>QuartzSchedulerBuilder.UseProperties</code> and <code>AddQuartz(properties)</code>, which are about flat <code>quartz.*</code> configuration keys and have nothing to do with how job data is persisted</td></tr><tr><td><code>Matchers.Group&lt;TKey&gt;(@operator, …)</code> / <code>Matchers.Name&lt;TKey&gt;(@operator, …)</code></td><td>the parameter is <code>matchOperator</code></td><td><code>operator</code> is a C# keyword, so naming the argument meant writing <code>@operator:</code> at the call site</td></tr></tbody></table><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line"> q.UsePersistentStore(store =&gt;</span>
<span class="line"></span><span class="token prefix unchanged"> </span><span class="token line"> {</span>
<span class="line"></span><span class="token prefix unchanged"> </span><span class="token line">     store.UseSqlServer(connectionString);</span>
<span class="line"></span></span><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line">     store.Configure(options =&gt; options.UseProperties = true);</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line">     store.ConfigureStore(options =&gt; options.StoreJobDataAsStrings = true);</span>
<span class="line"></span></span><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line"> });</span>
<span class="line"></span></span></span>
<span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> Matchers.Group&lt;JobKey&gt;(@operator: StringOperator.StartsWith, compareTo: &quot;reports&quot;);</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> Matchers.Group&lt;JobKey&gt;(matchOperator: StringOperator.StartsWith, compareTo: &quot;reports&quot;);</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The flat key is unchanged: <code>quartz.jobStore.useProperties</code> still sets <code>StoreJobDataAsStrings</code>. In <code>appsettings.json</code> the section entry follows the option, so <code>JobStore:UseProperties</code> becomes <code>JobStore:StoreJobDataAsStrings</code> — the old spelling still works, because every section is also flattened onto its <code>quartz.*</code> key and read there.</p><h3 id="the-persistent-store-builder-says-which-thing-it-configures" tabindex="-1"><a class="header-anchor" href="#the-persistent-store-builder-says-which-thing-it-configures"><span>The persistent store builder says which thing it configures</span></a></h3><p>Three members of <code>IPersistentStoreBuilder</code> did not follow the conventions the rest of it set.</p><table><thead><tr><th>4.0 preview</th><th>4.0</th></tr></thead><tbody><tr><td><code>store.Configure(options =&gt; …)</code></td><td><code>store.ConfigureStore(options =&gt; …)</code></td></tr><tr><td><code>store.UseDataSourceName(name)</code></td><td><code>store.UseDataSource(name)</code></td></tr><tr><td>—</td><td><code>store.UseDriverDelegate(factory)</code>, new</td></tr></tbody></table><p><code>Configure</code> was a bare verb inside a callback that already has <code>ConfigureScheduler</code> and <code>ConfigureOptions&lt;TOptions&gt;</code> in scope, so it said nothing about which of the things in reach it configured. <code>UseDataSourceName</code> and <code>UseDataSource</code> were two names for one concept, and naming a data source is the string overload of defining one. <code>UseDriverDelegate</code> was the only registration on the builder without a <code>Func&lt;IServiceProvider, T&gt;</code> overload — <code>UseConnectionProvider</code>, <code>UseSerializer</code>, <code>UseLockHandler</code> and <code>UseTriggerPersistenceDelegate</code> all had one — which made a dialect that needs anything from the container the one part of the store that could not be built from it.</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line"> q.UsePersistentStore(store =&gt;</span>
<span class="line"></span><span class="token prefix unchanged"> </span><span class="token line"> {</span>
<span class="line"></span></span><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line">     store.UseDataSourceName(&quot;reporting-db&quot;);</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line">     store.UseDataSource(&quot;reporting-db&quot;);</span>
<span class="line"></span></span><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line">     store.UseSqlServer(connectionString);</span>
<span class="line"></span></span><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line">     store.Configure(options =&gt; options.TablePrefix = &quot;RPT_QRTZ_&quot;);</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line">     store.ConfigureStore(options =&gt; options.TablePrefix = &quot;RPT_QRTZ_&quot;);</span>
<span class="line"></span></span><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line"> });</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>No configuration key moved. <code>quartz.jobStore.*</code>, <code>Quartz:JobStore:*</code> and <code>quartz.jobStore.dataSource</code> all say what they said, so only code that calls these methods has anything to change.</p><h3 id="the-hosted-service-s-extension-point-is-its-four-hooks" tabindex="-1"><a class="header-anchor" href="#the-hosted-service-s-extension-point-is-its-four-hooks"><span>The hosted service&#39;s extension point is its four hooks</span></a></h3><p><code>QuartzHostedService.StartAsync</code> and <code>StopAsync</code> are no longer <code>virtual</code>. They maintain a private list of schedulers and an internal startup task, so an override that did not call base left the schedulers bound to the repository with nothing able to shut them down — a failure that surfaces at process exit, long after the code that caused it.</p><p>The four lifecycle hooks <code>StartingAsync</code>, <code>StartedAsync</code>, <code>StoppingAsync</code> and <code>StoppedAsync</code> stay <code>virtual</code>; they are no-ops that exist for nothing else, and they are what #2386 and #2522 asked for. Work in an overridden <code>StartAsync</code> moves into <code>StartingAsync</code> or <code>StartedAsync</code>, and work in <code>StopAsync</code> into <code>StoppingAsync</code> or <code>StoppedAsync</code>. What a subclass was reaching into <code>StartAsync</code> for is now a member of its own: <code>protected IReadOnlyList&lt;IScheduler&gt; Schedulers</code>, a snapshot of the schedulers the service is running.</p><h3 id="a-shipped-component-is-configured-through-its-options-type-and-only-there" tabindex="-1"><a class="header-anchor" href="#a-shipped-component-is-configured-through-its-options-type-and-only-there"><span>A shipped component is configured through its options type, and only there</span></a></h3><p>Every configurable component Quartz ships published its settings twice: once on an options type bound from configuration and validated, and once as public settable properties on the component itself, assignable on a live instance with nothing saying which wins or when. The component-side setters are <code>internal</code> now, leaving the options type as the one public way:</p><table><thead><tr><th>Component</th><th>Configure through</th></tr></thead><tbody><tr><td><code>RAMJobStore.MisfireThreshold</code></td><td><code>UseInMemoryStore(o =&gt; o.MisfireThreshold = …)</code></td></tr><tr><td>The ADO.NET store&#39;s <code>MisfireThreshold</code></td><td><code>UsePersistentStore(store =&gt; store.ConfigureStore(o =&gt; o.MisfireThreshold = …))</code></td></tr><tr><td><code>TaskSchedulingThreadPool.MaxConcurrency</code>, <code>.Scheduler</code></td><td><code>UseDefaultThreadPool(maxConcurrency: …)</code>; both setters are <code>protected internal</code>, so a pool of your own can still set them</td></tr><tr><td><code>RedisLockHandler.RedisConfiguration</code>, <code>.KeyPrefix</code>, <code>.LockTimeToLive</code>, <code>.LockRetryInterval</code></td><td><code>UseRedisLockHandler(o =&gt; …)</code></td></tr><tr><td>The history plugins&#39; message templates</td><td><code>UseJobHistoryLogging(o =&gt; …)</code>, <code>UseTriggerHistoryLogging(o =&gt; …)</code>, and now <code>UseStructuredJobLogging(o =&gt; …)</code> / <code>UseStructuredTriggerLogging(o =&gt; …)</code></td></tr><tr><td>The scheduling-data plugins&#39; <code>FileNames</code>, <code>ScanInterval</code>, <code>FailOn*</code> (internal outright — see <a href="#the-two-scheduling-data-plugins-have-one-surface">one surface</a>)</td><td><code>UseXmlSchedulingConfiguration(o =&gt; …)</code> / <code>UseJsonSchedulingConfiguration(o =&gt; …)</code></td></tr><tr><td><code>ShutdownHookPlugin.CleanShutdown</code></td><td><code>AddQuartzHostedService(o =&gt; o.WaitForJobsToComplete = …)</code> — the plugin is gone, see <a href="#shutdownhookplugin-is-retired-the-host-already-shuts-the-scheduler-down"><code>ShutdownHookPlugin</code> is retired; the host already shuts the scheduler down</a></td></tr></tbody></table><p>The two shipped row-lock handlers keep their retry knobs, but as <code>init</code>-only properties rather than setters: <code>SelectForUpdateLockHandler</code> has <code>MaxRetry</code> and <code>RetryPeriod</code>, and <code>UpdateRowLockHandler</code> has <code>RetryPeriod</code> — it gained one, having previously hard-coded a second. Set them at construction, or through <code>quartz.jobStore.lockHandler.maxRetry</code> / <code>.retryPeriod</code>, which the binder still writes.</p><p><strong>Flat-key configuration is unaffected.</strong> <code>quartz.plugin.&lt;name&gt;.&lt;property&gt;</code> and <code>quartz.jobStore.lockHandler.&lt;property&gt;</code> write the component directly through reflection, and that binder now binds non-public setters — so every string configuration that worked keeps working, and only the code path that bypassed the options type is closed.</p><p><code>UseStructuredJobLogging</code> and <code>UseStructuredTriggerLogging</code> gain the <code>configure</code> delegate their non-structured siblings always had.</p><p>Two collections stop being assignable, for the reason <code>QuartzOptions.Properties</code> never was — one <code>configure</code> callback must not be able to discard what another added, and the configuration binder needs no setter to bind into a non-null collection:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> x.Files = [&quot;~/quartz_jobs.xml&quot;];</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> x.Files.Add(&quot;~/quartz_jobs.xml&quot;);</span>
<span class="line"></span></span></span>
<span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> options.Tags = [&quot;ready&quot;, &quot;live&quot;];</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> options.Tags.AddRange([&quot;ready&quot;, &quot;live&quot;]);</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>FileSchedulingOptions.Files</code> is a <code>List&lt;string&gt;</code> rather than a <code>string[]</code>, and <code>QuartzHealthCheckOptions.Tags</code> a <code>List&lt;string&gt;</code> rather than an <code>IReadOnlyCollection&lt;string&gt;</code>.</p><h3 id="the-provider-names-have-constants" tabindex="-1"><a class="header-anchor" href="#the-provider-names-have-constants"><span>The provider names have constants</span></a></h3><p><code>DataSourceOptions.Provider</code> is a string naming an ADO.NET driver description, and the eight names Quartz ships one for were discoverable only from a documentation table or by reading a property file. They are constants now — <code>DataSourceOptions.Providers.SqlServer</code>, <code>.Npgsql</code>, <code>.MySql</code>, <code>.MySqlConnector</code>, <code>.Oracle</code>, <code>.Sqlite</code>, <code>.SystemDataSqlite</code>, <code>.Firebird</code> — and the <code>UseSqlServer</code> / <code>UsePostgres</code> / … extensions pass them rather than repeating literals.</p><p>The property stays a <code>string</code>: the set is not closed, since a driver Quartz knows nothing about is describable through <code>UseGenericDatabase</code>. The values are unchanged, so a configuration file naming one of them is unaffected.</p><h3 id="the-standalone-builder-reads-a-configuration-section" tabindex="-1"><a class="header-anchor" href="#the-standalone-builder-reads-a-configuration-section"><span>The standalone builder reads a configuration section</span></a></h3><p><code>QuartzSchedulerBuilder.UseConfiguration(IConfiguration)</code> is new, and is the standalone counterpart of <code>AddQuartz(configuration)</code>: it binds the typed options, reads a <code>Schedule</code> section, and translates the flat <code>quartz.*</code> keys, exactly as the hosted path does.</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> var properties = QuartzConfigurationHelper.ToNameValueCollection(configuration.GetSection(&quot;Quartz&quot;));</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"> var factory = QuartzSchedulerBuilder.Create().UseProperties(properties).Build();</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> var factory = QuartzSchedulerBuilder.Create().UseConfiguration(configuration.GetSection(&quot;Quartz&quot;)).Build();</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>QuartzConfigurationHelper</code> — whose one public method existed for that flatten-then-configure sample — is internal. <code>UseProperties(NameValueCollection)</code> is unchanged and is still the way in for a bag you built yourself, from a properties file or from environment variables.</p><h3 id="two-scheduler-thread-settings-were-dead-and-are-gone" tabindex="-1"><a class="header-anchor" href="#two-scheduler-thread-settings-were-dead-and-are-gone"><span>Two scheduler thread settings were dead and are gone</span></a></h3><p>The scheduling loop is a long-running <code>Task</code>, not a <code>Thread</code>, so neither of these ever did anything in 4.0 — one named a thread that does not exist, the other tried to stop a thread that does not exist from keeping the process alive.</p><table><thead><tr><th>Removed</th><th>Use instead</th></tr></thead><tbody><tr><td><code>QuartzSchedulerOptions.ThreadName</code> / <code>quartz.scheduler.threadName</code></td><td>nothing</td></tr><tr><td><code>QuartzSchedulerOptions.MakeSchedulerThreadDaemon</code> / <code>quartz.scheduler.makeSchedulerThreadDaemon</code></td><td>nothing for the scheduler; <code>AdoJobStoreOptions.UseBackgroundThreads</code> for the store&#39;s threads</td></tr></tbody></table><p>Both keys are in the removed-key table, so a properties bag that still carries one is told why rather than told it is unknown.</p><p>The setting that does still matter is the job store&#39;s, and it is renamed to say what .NET calls it:</p><table><thead><tr><th>Before</th><th>After</th></tr></thead><tbody><tr><td><code>AdoJobStoreOptions.MakeThreadsDaemons</code></td><td><code>AdoJobStoreOptions.UseBackgroundThreads</code></td></tr></tbody></table><p>The flat key <code>quartz.jobStore.makeThreadsDaemons</code> is unchanged and still sets it. It governs the misfire handler and the cluster manager, which are the only real threads Quartz creates — so it is now the whole answer to &quot;do Quartz&#39;s threads hold my console application open&quot;.</p><h3 id="performschemavalidation-became-schemaprovisioning-which-has-a-third-position" tabindex="-1"><a class="header-anchor" href="#performschemavalidation-became-schemaprovisioning-which-has-a-third-position"><span><code>PerformSchemaValidation</code> became <code>SchemaProvisioning</code>, which has a third position</span></a></h3><p>A persistent store can now create its own schema (<a href="https://github.com/quartznet/quartznet/issues/3531" target="_blank" rel="noopener noreferrer">#3531</a>), which the old flag had nowhere to say. The check and the creation are one decision — a store that may create its schema obviously validates it too — so they are one setting with three positions rather than two flags that can contradict each other.</p><table><thead><tr><th>Before</th><th>After</th></tr></thead><tbody><tr><td><code>AdoJobStoreOptions.PerformSchemaValidation = true</code></td><td><code>AdoJobStoreOptions.SchemaProvisioning = SchemaProvisioning.Validate</code> (the default)</td></tr><tr><td><code>AdoJobStoreOptions.PerformSchemaValidation = false</code></td><td><code>AdoJobStoreOptions.SchemaProvisioning = SchemaProvisioning.None</code></td></tr><tr><td>—</td><td><code>AdoJobStoreOptions.SchemaProvisioning = SchemaProvisioning.CreateIfMissing</code>, or <code>store.ProvisionSchema()</code></td></tr></tbody></table><p>The flat key <code>quartz.jobStore.performSchemaValidation</code> still works and still means what it meant: <code>true</code> bridges to <code>Validate</code> and <code>false</code> to <code>None</code>. <code>quartz.jobStore.schemaProvisioning</code> is the new key and takes a member name — <code>None</code>, <code>Validate</code> or <code>CreateIfMissing</code>, case-insensitively. A file carrying both is decided by the new key, since it is the only one of the two that can express the third position.</p><p><code>CreateIfMissing</code> only ever creates: no object is altered and none is dropped, so it is safe to leave on against a schema that already exists and cannot turn a mis-typed <code>TablePrefix</code> into data loss — though it will happily create a second, empty schema under the mis-typed one. It is equally <strong>not</strong> an upgrade: a schema that has every table but is missing a column a later release added is left exactly as it is. <code>database/migrations/</code> is still what moves a schema forward, and the 3.x → 4.0 upgrade is still mandatory.</p><p>It is not the default, because creating tables needs DDL permission that a production database is usually right not to grant. When the account has none, the failure names the fresh-install script for your database and says to run it and drop back to <code>Validate</code>.</p><p><code>IDriverDelegate</code> gained the member behind it:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token return-type class-name">ValueTask</span> <span class="token function">CreateSchema</span><span class="token punctuation">(</span><span class="token class-name">ConnectionAndTransactionHolder</span> conn<span class="token punctuation">,</span> <span class="token class-name">CancellationToken</span> cancellationToken <span class="token operator">=</span> <span class="token keyword">default</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>A delegate deriving from <code>StdAdoDelegate</code> inherits an implementation that runs an embedded script, and names the script by overriding <code>protected virtual string? SchemaResourceName</code>. The six shipped delegates each name their own, and the lookup walks the base chain nearest first — so a delegate that derives from one of them to change a statement or two inherits that dialect&#39;s script and needs no override, while one that overrides the name still wins. A delegate that names no script anywhere in its chain throws when asked to provision, saying so, rather than reporting success and creating nothing.</p><h3 id="code-first-configuration-is-typed" tabindex="-1"><a class="header-anchor" href="#code-first-configuration-is-typed"><span>Code-first configuration is typed</span></a></h3><p>Settings that used to be write-only properties on the configurator are now options:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line"> services.AddQuartz(q =&gt;</span>
<span class="line"></span><span class="token prefix unchanged"> </span><span class="token line"> {</span>
<span class="line"></span></span><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line">     q.ConfigureScheduler(options =&gt; options.InstanceName = &quot;core&quot;);</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line">     q.ConfigureScheduler(options =&gt; options.InstanceId = &quot;node-1&quot;);</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line">     q.MaxBatchSize = 5;</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line">     q.InterruptJobsOnShutdown = true;</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line">     q.ConfigureScheduler(options =&gt;</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">     {</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">         options.InstanceName = &quot;core&quot;;</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">         options.InstanceId = &quot;node-1&quot;;</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">         options.MaxBatchSize = 5;</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">         options.ShutdownJobInterruption = ShutdownJobInterruption.WhenNotWaitingForJobs;</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">     });</span>
<span class="line"></span></span><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line"> });</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The option names are the same words as the configuration keys, so <code>Quartz:Scheduler:MaxBatchSize</code> and <code>options.MaxBatchSize</code> are the same setting said two ways.</p><h3 id="data-sources-no-longer-need-a-name" tabindex="-1"><a class="header-anchor" href="#data-sources-no-longer-need-a-name"><span>Data sources no longer need a name</span></a></h3><p>A scheduler has one job store and therefore one database, so there is no name to invent:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line"> q.UsePersistentStore(store =&gt;</span>
<span class="line"></span><span class="token prefix unchanged"> </span><span class="token line"> {</span>
<span class="line"></span></span><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line">     store.UseProperties = true;</span>
<span class="line"></span></span><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line">     store.UseClustering();</span>
<span class="line"></span></span><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line">     store.UseSqlServer(&quot;sql-server-01&quot;, connectionString);</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line">     store.UseSqlServer(connectionString);</span>
<span class="line"></span></span><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line">     store.UseSystemTextJsonSerializer();</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line">     store.ConfigureStore(options =&gt; options.StoreJobDataAsStrings = true);</span>
<span class="line"></span></span><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line"> });</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Schedulers that need different databases are registered under different names, and each gets its own services.</p><h3 id="the-quartz-config-file-is-no-longer-read" tabindex="-1"><a class="header-anchor" href="#the-quartz-config-file-is-no-longer-read"><span>The quartz.config file is no longer read</span></a></h3><p>Nothing is loaded from disk any more. A <code>quartz.config</code> file next to your application — or named by the <code>quartz.config</code> environment variable — is ignored, and so is the copy of it Quartz used to ship as an embedded resource. A scheduler is configured by the properties you hand to <code>QuartzSchedulerBuilder.UseProperties</code>, by an <code>IConfiguration</code> section passed to <code>AddQuartz</code>, or in code through the container.</p><p>The three settings the embedded file supplied are not defaults any more either. They were seeded by <code>StdSchedulerFactory.Initialize()</code>, which is the only entry point that ever read the file, and which is gone with the factory — see <a href="#stdschedulerfactory-is-gone"><code>StdSchedulerFactory</code> is gone</a>:</p><table><thead><tr><th>Setting</th><th>Old embedded value</th><th>Default now</th></tr></thead><tbody><tr><td><code>quartz.scheduler.instanceName</code></td><td><code>DefaultQuartzScheduler</code></td><td><code>QuartzScheduler</code> (<code>QuartzSchedulerOptions.InstanceName</code>)</td></tr><tr><td><code>quartz.threadPool.threadCount</code></td><td>10</td><td>10 (<code>ThreadPoolOptions.MaxConcurrency</code>)</td></tr><tr><td><code>quartz.jobStore.misfireThreshold</code></td><td>60000</td><td>5 seconds (<code>InMemoryJobStoreOptions.MisfireThreshold</code>)</td></tr></tbody></table><p>Note these were never the defaults for <code>AddQuartz</code> or for <code>new StdSchedulerFactory(properties)</code> in the first place: handing the factory properties always bypassed the file, so those paths fell back — and still fall back — to the typed option defaults. Set them explicitly if you want the other values.</p><p>The one thing the file was still needed for was describing an ADO.NET driver Quartz ships no metadata for. That now has a code-first form, and the <code>quartz.dbprovider.*</code> keys themselves still work — they just arrive through <code>IConfiguration</code> or a <code>NameValueCollection</code> like every other key:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line">q<span class="token punctuation">.</span><span class="token function">UsePersistentStore</span><span class="token punctuation">(</span>store <span class="token operator">=&gt;</span> store<span class="token punctuation">.</span><span class="token function">UseGenericDatabase</span><span class="token punctuation">(</span><span class="token string">&quot;MyDatabase&quot;</span><span class="token punctuation">,</span> connectionString<span class="token punctuation">,</span> <span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">=&gt;</span> <span class="token keyword">new</span> <span class="token constructor-invocation class-name">DbMetadata</span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    ProductName <span class="token operator">=</span> <span class="token string">&quot;My Database&quot;</span><span class="token punctuation">,</span></span>
<span class="line">    ConnectionType <span class="token operator">=</span> <span class="token keyword">typeof</span><span class="token punctuation">(</span><span class="token type-expression class-name">MyConnection</span><span class="token punctuation">)</span><span class="token punctuation">,</span></span>
<span class="line">    CommandType <span class="token operator">=</span> <span class="token keyword">typeof</span><span class="token punctuation">(</span><span class="token type-expression class-name">MyCommand</span><span class="token punctuation">)</span><span class="token punctuation">,</span></span>
<span class="line">    ParameterType <span class="token operator">=</span> <span class="token keyword">typeof</span><span class="token punctuation">(</span><span class="token type-expression class-name">MyParameter</span><span class="token punctuation">)</span><span class="token punctuation">,</span></span>
<span class="line">    ParameterDbType <span class="token operator">=</span> <span class="token keyword">typeof</span><span class="token punctuation">(</span><span class="token type-expression class-name">MyDbType</span><span class="token punctuation">)</span><span class="token punctuation">,</span></span>
<span class="line">    ParameterDbTypePropertyName <span class="token operator">=</span> <span class="token keyword">nameof</span><span class="token punctuation">(</span>MyParameter<span class="token punctuation">.</span>MyDbType<span class="token punctuation">)</span><span class="token punctuation">,</span></span>
<span class="line">    ParameterNamePrefix <span class="token operator">=</span> <span class="token string">&quot;@&quot;</span><span class="token punctuation">,</span></span>
<span class="line">    DbBinaryTypeName <span class="token operator">=</span> <span class="token string">&quot;VarBinary&quot;</span><span class="token punctuation">,</span></span>
<span class="line"><span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div>`,107),e(`p`,null,[l[146]||=r(`See `,-1),c(g,{to:`/documentation/quartz-4.x/configuration/reference.html#describing-a-driver-quartz-does-not-know`},{default:i(()=>[...l[145]||=[r(`the configuration reference`,-1)]]),_:1}),l[147]||=r(` for the full description. `,-1),l[148]||=e(`code`,null,`DbProvider.RegisterDbMetadata`,-1),l[149]||=r(` is gone with the process-wide lookup it wrote into; use the callback above, once per driver.`,-1)]),l[861]||=t(`<h3 id="quartzoptions-is-no-longer-a-dictionary" tabindex="-1"><a class="header-anchor" href="#quartzoptions-is-no-longer-a-dictionary"><span><code>QuartzOptions</code> is no longer a dictionary</span></a></h3><p><code>QuartzOptions</code> used to derive from <code>Dictionary&lt;string, string?&gt;</code>, and to hold a scheduler&#39;s jobs and triggers as well as its flat keys. It was the pivot the whole configuration model turned on; now that settings are typed options, the only things left in it were the legacy keys and the one thing that was never configuration at all. The keys moved to a <code>Properties</code> dictionary, and jobs and triggers became a per-scheduler registration like every other part of a scheduler.</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line"> services.Configure&lt;QuartzOptions&gt;(options =&gt;</span>
<span class="line"></span><span class="token prefix unchanged"> </span><span class="token line"> {</span>
<span class="line"></span></span><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line">     options[&quot;quartz.plugin.jobHistory.type&quot;] = &quot;Quartz.Plugins.History.LoggingJobHistoryPlugin, Quartz.Plugins&quot;;</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line">     options.Properties[&quot;quartz.plugin.jobHistory.type&quot;] = &quot;Quartz.Plugins.History.LoggingJobHistoryPlugin, Quartz.Plugins&quot;;</span>
<span class="line"></span></span><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line"> });</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>A plugin whose type you know is better added as one, which also gives it constructor injection:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line">services<span class="token punctuation">.</span><span class="token function">AddQuartz</span><span class="token punctuation">(</span>q <span class="token operator">=&gt;</span> q<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">AddPlugin</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>LoggingJobHistoryPlugin<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>Because the keys are a property rather than the object itself, a section of flat keys no longer binds onto <code>QuartzOptions</code> directly — it would bind <code>Quartz:Properties:*</code>. Pass the section to <code>AddQuartz</code>, which reads the keys where they have always been and binds the typed options from the same section:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> services.Configure&lt;QuartzOptions&gt;(configuration.GetSection(&quot;Quartz&quot;));</span>
<span class="line"></span></span><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line"> services.AddQuartz(configuration.GetSection(&quot;Quartz&quot;), q =&gt; { /* ... */ });</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>Jobs and triggers are added through the builder. The overloads taking an <code>IServiceProvider</code> cover the case the options callback used to be needed for:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> services.AddOptions&lt;QuartzOptions&gt;()</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line">     .Configure&lt;IOptions&lt;SampleOptions&gt;&gt;((options, sample) =&gt;</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line">     {</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line">         options.AddJob&lt;ExampleJob&gt;(j =&gt; j.WithIdentity(&quot;job&quot;, &quot;group&quot;));</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line">         options.AddTrigger(t =&gt; t</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line">             .ForJob(&quot;job&quot;, &quot;group&quot;)</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line">             .WithCronSchedule(sample.Value.CronSchedule));</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line">     });</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> services.AddQuartz(q =&gt;</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line"> {</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">     q.AddJob&lt;ExampleJob&gt;(j =&gt; j.WithIdentity(&quot;job&quot;, &quot;group&quot;));</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">     q.AddTrigger((provider, t) =&gt; t</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">         .ForJob(&quot;job&quot;, &quot;group&quot;)</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">         .WithCronSchedule(provider.GetRequiredService&lt;IOptions&lt;SampleOptions&gt;&gt;().Value.CronSchedule));</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line"> });</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>QuartzOptions.SchedulerName</code> used to read and write <code>schedName</code> — an ADO.NET column key that nothing reads — so a scheduler name set through it was accepted and then silently ignored. It is gone along with <code>SchedulerId</code> and <code>MisfireThreshold</code>; see <a href="#quartzoptions-lost-its-three-typed-settings"><code>QuartzOptions</code> lost its three typed settings</a>.</p><h3 id="removed" tabindex="-1"><a class="header-anchor" href="#removed"><span>Removed</span></a></h3><table><thead><tr><th>Removed</th><th>Use instead</th></tr></thead><tbody><tr><td><code>QuartzOptions : Dictionary&lt;string, string?&gt;</code></td><td><code>QuartzOptions.Properties</code></td></tr><tr><td><code>QuartzOptions.JobDetails</code>, <code>.Triggers</code>, <code>.AddJob</code>, <code>.AddTrigger</code></td><td><code>AddQuartz(q =&gt; q.AddJob(…))</code> / <code>q.AddTrigger(…)</code></td></tr><tr><td><code>StdSchedulerFactory</code> and its 47 constants</td><td><code>QuartzSchedulerBuilder.UseProperties(properties)</code> — see <a href="#stdschedulerfactory-is-gone"><code>StdSchedulerFactory</code> is gone</a></td></tr><tr><td><code>StdSchedulerFactory.PropertySchedulerName</code></td><td>nothing; it named an ADO.NET column, not a setting</td></tr><tr><td><code>SchedulerBuilder</code></td><td><code>QuartzSchedulerBuilder</code> for standalone use, <code>AddQuartz</code> under a host</td></tr><tr><td><code>DirectSchedulerFactory</code></td><td><code>QuartzSchedulerBuilder</code>, with <code>UseThreadPool(IThreadPool)</code> / <code>UseJobStore(IJobStore)</code> for pre-built parts</td></tr><tr><td><code>IPropertyConfigurer</code>, <code>IPropertySetter</code>, <code>IPropertyConfigurationRoot</code>, <code>PropertiesHolder</code>, <code>PropertiesSetter</code></td><td>typed options</td></tr><tr><td><code>SchedulerBuilder.UseZeroSizeThreadPool()</code></td><td><code>UseThreadPool&lt;ZeroSizeThreadPool&gt;()</code> — the pool type is still public, only the shorthand is gone</td></tr><tr><td><code>SchedulerBuilder.UseDedicatedThreadPool()</code></td><td><code>UseDefaultThreadPool(…)</code>; <code>DedicatedThreadPool</code> is internal, because a dedicated-thread <code>TaskScheduler</code> is no longer how a thread pool is written — see <a href="#the-thread-pool-is-asynchronous">The thread pool is asynchronous</a></td></tr><tr><td><code>SchedulerPluginConfigurationExtensions.UsePlugin&lt;T&gt;(name)</code></td><td><code>AddPlugin&lt;T&gt;(name)</code> on the builder — see <a href="#plugins-are-registered-like-listeners">Plugins are registered like listeners</a></td></tr><tr><td><code>SchedulerPluginConfigurationExtensions.TryRegisterSingleton&lt;TService, TImplementation&gt;()</code></td><td><code>builder.Services.TryAddSingleton&lt;TService, TImplementation&gt;()</code>; the builder no longer wraps the container&#39;s own verbs</td></tr><tr><td><code>AddQuartz(Action&lt;configurator, IServiceProvider&gt;)</code></td><td>see below</td></tr><tr><td><code>quartz.config</code> file discovery, <code>StdSchedulerFactory.PropertiesFile</code></td><td><code>IConfiguration</code>, or properties passed to <code>QuartzSchedulerBuilder.UseProperties</code></td></tr><tr><td><code>DbProvider.RegisterDbMetadata</code></td><td>the metadata factory on <code>UseGenericDatabase</code></td></tr><tr><td><code>quartz.scheduler.proxy*</code>, <code>quartz.scheduler.exporter*</code></td><td>nothing; remoting is not supported on modern .NET</td></tr><tr><td><code>quartz.jobListener.&lt;name&gt;.*</code>, <code>quartz.triggerListener.&lt;name&gt;.*</code></td><td><code>AddJobListener&lt;T&gt;(matchers)</code> / <code>AddTriggerListener&lt;T&gt;(matchers)</code>; the keys are rejected rather than ignored — see <a href="#the-listener-property-keys-are-retired">The listener property keys are retired</a></td></tr><tr><td><code>QuartzOptions.SchedulerName</code>, <code>.SchedulerId</code>, <code>.MisfireThreshold</code></td><td>the typed options — see <a href="#quartzoptions-lost-its-three-typed-settings"><code>QuartzOptions</code> lost its three typed settings</a></td></tr><tr><td><code>IPersistentStoreBuilder.UseDataSourceConnectionProvider()</code></td><td><code>DataSourceOptions.UseRegisteredDataSource</code></td></tr><tr><td><code>AdoJobStoreOptions.Clustered</code>, <code>.ClusterCheckinInterval</code>, <code>.ClusterCheckinMisfireThreshold</code></td><td><code>ClusteringOptions</code> — see <a href="#clustering-is-configured-in-one-place">Clustering is configured in one place</a></td></tr><tr><td><code>SchedulerRepository.Instance</code></td><td><code>ISchedulerRepository</code> resolved from the container</td></tr><tr><td><code>DBConnectionManager.Instance</code></td><td>nothing; register a provider with <code>UseConnectionProvider</code> and resolve <code>IDbProvider</code> from the container — see <a href="#the-connection-manager-is-gone">The connection manager is gone</a></td></tr><tr><td><code>StdSchedulerFactory.GetDbConnectionManager()</code>, <code>.GetSchedulerRepository()</code></td><td><code>IDbProvider</code>, keyed by scheduler name / <code>ISchedulerRepository</code>, both resolved from the container</td></tr></tbody></table><h3 id="deferred-configuration" tabindex="-1"><a class="header-anchor" href="#deferred-configuration"><span>Deferred configuration</span></a></h3><p>The <code>AddQuartz</code> overloads taking an <code>IServiceProvider</code> are gone. They existed to reach services while configuring, which the options pattern already does:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> services.AddQuartz((q, provider) =&gt;</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"> {</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line">     var connectionString = provider.GetRequiredService&lt;IConfiguration&gt;().GetConnectionString(&quot;Scheduler&quot;);</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line">     q.UsePersistentStore(store =&gt; store.UseSqlServer(&quot;default&quot;, connectionString));</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"> });</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> var connectionString = builder.Configuration.GetConnectionString(&quot;Scheduler&quot;);</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line"> services.AddQuartz(q =&gt; q.UsePersistentStore(store =&gt; store.UseSqlServer(connectionString)));</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>For an option that genuinely depends on a service, use the options pattern directly:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line">services<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">AddOptions</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>AdoJobStoreOptions<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span><span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">.</span><span class="token generic-method"><span class="token function">Configure</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>IMyService<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span><span class="token punctuation">(</span>options<span class="token punctuation">,</span> service<span class="token punctuation">)</span> <span class="token operator">=&gt;</span> options<span class="token punctuation">.</span>TablePrefix <span class="token operator">=</span> service<span class="token punctuation">.</span>TablePrefix<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>Listeners and plugins do not need this at all: they are registered services, so the container injects their dependencies.</p><h3 id="spi-changes" tabindex="-1"><a class="header-anchor" href="#spi-changes"><span>SPI changes</span></a></h3><p>If you implement <code>IJobStore</code> or <code>ISchedulerPlugin</code> yourself, they take their collaborators through constructors now instead of being handed them afterwards.</p><p><code>IJobStore</code> loses <code>InstanceId</code>, <code>InstanceName</code>, <code>ThreadPoolSize</code> and <code>TimeProvider</code>, and <code>Initialize</code> takes the scheduler&#39;s identity in place of its collaborators:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> ValueTask Initialize(ITypeLoadHelper loadHelper, ISchedulerSignaler signaler, CancellationToken cancellationToken = default);</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> ValueTask Initialize(SchedulerIdentity identity, CancellationToken cancellationToken = default);</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>Take what you need — <code>ISchedulerSignaler</code>, <code>ITypeLoader</code>, <code>TimeProvider</code>, <code>IOptions&lt;QuartzSchedulerOptions&gt;</code> — through your constructor. The identity is the one thing a constructor cannot carry, for the reason <a href="#if-you-implement-ijobstore">If you implement <code>IJobStore</code></a> gives. What remains in <code>Initialize</code> is work that has to happen before the scheduler runs and cannot be done while constructing, such as verifying a database schema.</p><p>Options arrive as the scheduler&#39;s own, whichever of the three interfaces you ask for. <code>IOptionsMonitor&lt;QuartzSchedulerOptions&gt;</code> and <code>IOptionsSnapshot&lt;QuartzSchedulerOptions&gt;</code> work as well as <code>IOptions&lt;&gt;</code>: <code>CurrentValue</code> and <code>Value</code> are the options of the scheduler your component belongs to, <code>Get(name)</code> answers for the name you pass, and <code>OnChange</code> reports your scheduler&#39;s changes and not another&#39;s.</p><p>Plugin configuration extension methods now extend <code>IQuartzBuilder</code> and register the plugin as a service, rather than deriving from <code>PropertiesSetter</code> to write string keys.</p><h3 id="no-process-global-scheduler-or-connection-state" tabindex="-1"><a class="header-anchor" href="#no-process-global-scheduler-or-connection-state"><span>No process-global scheduler or connection state</span></a></h3><p><code>SchedulerRepository.Instance</code> and <code>DBConnectionManager.Instance</code> are gone. The repository is an ordinary container registration now, which means <strong>a scheduler is only visible in the repository belonging to the container that built it</strong>:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> var scheduler = SchedulerRepository.Instance.Lookup(&quot;reporting&quot;);</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> var scheduler = serviceProvider.GetRequiredService&lt;ISchedulerRepository&gt;().Lookup(&quot;reporting&quot;);</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>The connection manager did not become a container registration — it <em>was</em> one, holding a copy of what the container already knew, and it is removed outright. Registering a connection provider is a store configuration call, and reading one back is container resolution:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> DBConnectionManager.Instance.AddConnectionProvider(&quot;default&quot;, myProvider);</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> services.AddQuartz(q =&gt; q.UsePersistentStore(store =&gt; store.UseConnectionProvider(_ =&gt; myProvider)));</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>See <a href="#the-connection-manager-is-gone">The connection manager is gone</a> for the full picture.</p><p>The observable consequence is that schedulers built different ways no longer find each other. Given a scheduler registered with <code>AddQuartz</code> and another built by a <code>QuartzSchedulerBuilder</code> in the same process:</p><ul><li><code>ISchedulerFactory.GetAllSchedulers()</code> on either one lists only its own schedulers.</li><li><code>ISchedulerFactory.LookupScheduler(name)</code> returns <code>null</code> for the other one&#39;s name.</li><li><code>ISchedulerRepository.Lookup(name)</code> likewise sees only its own container&#39;s schedulers.</li></ul><p>If you were relying on that reach — typically to find a scheduler from code that had no reference to the factory that created it — register the scheduler with <code>AddQuartz</code> and inject <code>IScheduler</code>, <code>ISchedulerFactory</code> or <code>ISchedulerRepository</code> instead. If you genuinely need one repository across several entry points, register your own instance before calling <code>AddQuartz</code>; every Quartz registration is <code>TryAdd</code>, so yours wins:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token class-name"><span class="token keyword">var</span></span> repository <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token constructor-invocation class-name">SchedulerRepository</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">services<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">AddSingleton</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>ISchedulerRepository<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span>repository<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">services<span class="token punctuation">.</span><span class="token function">AddQuartz</span><span class="token punctuation">(</span><span class="token comment">/* ... */</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>A <code>QuartzSchedulerBuilder</code> owns the container it creates, so its repository holds only the schedulers it built. <code>StdSchedulerFactory.GetSchedulerRepository()</code> and <code>GetDbConnectionManager()</code> went with the class; resolve <code>ISchedulerRepository</code> from the container instead, and a scheduler&#39;s <code>IDbProvider</code> from the same container under the scheduler&#39;s name as the service key.</p><h2 id="stdschedulerfactory-is-gone" tabindex="-1"><a class="header-anchor" href="#stdschedulerfactory-is-gone"><span><code>StdSchedulerFactory</code> is gone</span></a></h2><p>The properties-based factory has been removed. It was the last construction path that was not the container: it parsed <code>quartz.*</code> strings, loaded types by name, set properties by reflection, and built a scheduler of its own. Since 4.0 it did none of that itself — it built a service collection, handed the properties to the same translation layer <code>AddQuartz</code> uses, and resolved the scheduler from the container like everything else. What was left was a second front door onto one hallway, plus 47 public constants whose only purpose was to spell keys that a configuration file spells anyway.</p><p>Flat <code>quartz.*</code> keys are <strong>not</strong> going away. What changed is which type you hand them to:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> ISchedulerFactory factory = new StdSchedulerFactory(properties);</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"> IScheduler scheduler = await factory.GetScheduler();</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> IScheduler scheduler = await QuartzSchedulerBuilder.Create()</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">     .UseProperties(properties)</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">     .BuildScheduler();</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>UseProperties</code> feeds the same translator, so every key means exactly what it always did, including the <code>quartz.checkConfiguration</code> check that rejects a misspelled key. Configuration written in code wins over the properties whichever order the two are applied in: property-derived options are applied before anything the builder was told, and implementations the properties name are registered after — options being last-wins and registrations first-wins, the same rule <code>AddQuartz</code> follows.</p><p>Two behaviors did not survive, because they lived in <code>Initialize()</code> rather than in the properties:</p><ul><li><strong>The <code>quartz.*</code> environment-variable overlay.</strong> <code>new StdSchedulerFactory()</code> with no arguments read every <code>quartz.*</code> environment variable. Nothing does that now. Use <code>IConfiguration</code> with <code>AddEnvironmentVariables()</code>, which is the ordinary way to say it, and pass the section to <code>AddQuartz</code> or flatten it yourself into the <code>NameValueCollection</code> you hand to <code>UseProperties</code>.</li><li><strong>The embedded <code>quartz.config</code> defaults.</strong> A factory constructed with no properties used to start from <code>instanceName = DefaultQuartzScheduler</code>, <code>threadCount = 10</code> and <code>misfireThreshold = 60000</code>. A builder given no properties starts from the typed option defaults instead — <code>InstanceName</code> <code>QuartzScheduler</code>, <code>MaxConcurrency</code> 10, and an in-memory <code>MisfireThreshold</code> of 5 seconds. Set them explicitly if you were relying on the old values. Note that this was never the behavior of <code>new StdSchedulerFactory(properties)</code> either, which always bypassed the file.</li></ul><p><code>IsSupportedConfigurationKey</code> is gone with the class, so a configuration carrying keys of your own can no longer be allowed by subclassing the factory. Set <code>quartz.checkConfiguration</code> to <code>false</code> instead.</p><p><code>GetDefaultScheduler()</code> is gone too. It returned a process-wide scheduler from a process-wide factory, which stopped being a coherent idea when the repository became the container&#39;s — see <a href="#no-process-global-scheduler-or-connection-state">No process-global scheduler or connection state</a>. Build one scheduler where the application starts and hold it, or register it with <code>AddQuartz</code> and inject <code>IScheduler</code>.</p><h3 id="every-removed-constant" tabindex="-1"><a class="header-anchor" href="#every-removed-constant"><span>Every removed constant</span></a></h3><p>The strings are unchanged. This table is here so a <code>Ctrl+F</code> for the constant you used finds the key to write instead, and the typed option that is usually the better answer.</p><table><thead><tr><th>Removed constant</th><th>Key</th><th>Typed equivalent</th></tr></thead><tbody><tr><td><code>PropertySchedulerInstanceName</code></td><td><code>quartz.scheduler.instanceName</code></td><td><code>QuartzSchedulerOptions.InstanceName</code></td></tr><tr><td><code>PropertySchedulerInstanceId</code></td><td><code>quartz.scheduler.instanceId</code></td><td><code>QuartzSchedulerOptions.InstanceId</code></td></tr><tr><td><code>PropertySchedulerInstanceIdGeneratorPrefix</code></td><td><code>quartz.scheduler.instanceIdGenerator</code></td><td>constructor injection into your <code>IInstanceIdGenerator</code></td></tr><tr><td><code>PropertySchedulerInstanceIdGeneratorType</code></td><td><code>quartz.scheduler.instanceIdGenerator.type</code></td><td>register <code>IInstanceIdGenerator</code> in the container</td></tr><tr><td><code>PropertySchedulerThreadName</code></td><td><code>quartz.scheduler.threadName</code></td><td>nothing; the key is rejected rather than ignored. The scheduling loop is a <code>Task</code>, not a <code>Thread</code>, so there is no thread of its own to name</td></tr><tr><td><code>PropertySchedulerBatchTimeWindow</code></td><td><code>quartz.scheduler.batchTriggerAcquisitionFireAheadTimeWindow</code></td><td><code>QuartzSchedulerOptions.BatchTriggerAcquisitionFireAheadTimeWindow</code></td></tr><tr><td><code>PropertySchedulerMaxBatchSize</code></td><td><code>quartz.scheduler.batchTriggerAcquisitionMaxCount</code></td><td><code>QuartzSchedulerOptions.MaxBatchSize</code></td></tr><tr><td><code>PropertySchedulerExporterPrefix</code></td><td><code>quartz.scheduler.exporter</code></td><td>nothing; remoting is not supported on modern .NET</td></tr><tr><td><code>PropertySchedulerExporterType</code></td><td><code>quartz.scheduler.exporter.type</code></td><td>nothing; see above</td></tr><tr><td><code>PropertySchedulerProxy</code></td><td><code>quartz.scheduler.proxy</code></td><td><code>Quartz.HttpClient</code> for talking to a remote scheduler</td></tr><tr><td><code>PropertySchedulerProxyType</code></td><td><code>quartz.scheduler.proxy.type</code></td><td><code>Quartz.HttpClient</code>; the key is now rejected rather than ignored</td></tr><tr><td><code>PropertySchedulerIdleWaitTime</code></td><td><code>quartz.scheduler.idleWaitTime</code></td><td><code>QuartzSchedulerOptions.IdleWaitTime</code></td></tr><tr><td><code>PropertySchedulerMakeSchedulerThreadDaemon</code></td><td><code>quartz.scheduler.makeSchedulerThreadDaemon</code></td><td>nothing; the key is rejected rather than ignored. The scheduling loop is a <code>Task</code>, not a <code>Thread</code>, so it never held a process open. For the store&#39;s misfire and cluster threads use <code>quartz.jobStore.makeThreadsDaemons</code> / <code>AdoJobStoreOptions.UseBackgroundThreads</code></td></tr><tr><td><code>PropertySchedulerTypeLoadHelperType</code></td><td><code>quartz.scheduler.typeLoadHelper.type</code></td><td><code>UseTypeLoader&lt;T&gt;()</code>, or <code>UseSimpleTypeLoader()</code></td></tr><tr><td><code>PropertySchedulerJobFactoryPrefix</code></td><td><code>quartz.scheduler.jobFactory</code></td><td>constructor injection into your <code>IJobFactory</code></td></tr><tr><td><code>PropertySchedulerJobFactoryType</code></td><td><code>quartz.scheduler.jobFactory.type</code></td><td><code>UseJobFactory&lt;T&gt;()</code></td></tr><tr><td><code>PropertySchedulerInterruptJobsOnShutdown</code></td><td><code>quartz.scheduler.interruptJobsOnShutdown</code></td><td><code>QuartzSchedulerOptions.ShutdownJobInterruption</code></td></tr><tr><td><code>PropertySchedulerInterruptJobsOnShutdownWithWait</code></td><td><code>quartz.scheduler.interruptJobsOnShutdownWithWait</code></td><td><code>QuartzSchedulerOptions.ShutdownJobInterruption</code></td></tr><tr><td><code>PropertySchedulerContextPrefix</code></td><td><code>quartz.context.key</code></td><td><code>QuartzSchedulerOptions.Context</code></td></tr><tr><td><code>PropertyThreadPoolPrefix</code></td><td><code>quartz.threadPool</code></td><td><code>ThreadPoolOptions</code></td></tr><tr><td><code>PropertyThreadPoolType</code></td><td><code>quartz.threadPool.type</code></td><td><code>UseThreadPool&lt;T&gt;()</code>, or <code>UseThreadPool(instance)</code></td></tr><tr><td><code>PropertyTimeProviderType</code></td><td><code>quartz.timeProvider.type</code></td><td><code>UseTimeProvider(TimeProvider)</code></td></tr><tr><td><code>PropertyJobStorePrefix</code></td><td><code>quartz.jobStore</code></td><td><code>AdoJobStoreOptions</code> / <code>InMemoryJobStoreOptions</code></td></tr><tr><td><code>PropertyJobStoreType</code></td><td><code>quartz.jobStore.type</code></td><td><code>UseInMemoryStore()</code>, <code>UsePersistentStore()</code> — with <code>UseAmbientTransactions()</code> inside it for the store 3.x called <code>JobStoreCMT</code> — <code>UsePersistentStore&lt;T&gt;()</code> for a persistent store of your own, or <code>UseJobStore(instance)</code></td></tr><tr><td><code>PropertyJobStoreDbRetryInterval</code></td><td><code>quartz.jobStore.dbRetryInterval</code></td><td><code>AdoJobStoreOptions.DbRetryInterval</code></td></tr><tr><td><code>PropertyJobStoreLockHandlerPrefix</code></td><td><code>quartz.jobStore.lockHandler</code></td><td>constructor injection into your <code>ILockHandler</code></td></tr><tr><td><code>PropertyJobStoreLockHandlerType</code></td><td><code>quartz.jobStore.lockHandler.type</code></td><td><code>UseLockHandler&lt;T&gt;()</code></td></tr><tr><td><code>PropertyTablePrefix</code></td><td><code>tablePrefix</code> (under <code>quartz.jobStore</code>)</td><td><code>AdoJobStoreOptions.TablePrefix</code></td></tr><tr><td><code>PropertyDataSourcePrefix</code></td><td><code>quartz.dataSource</code></td><td><code>DataSourceOptions</code>, bound from <code>Quartz:DataSource:&lt;name&gt;</code></td></tr><tr><td><code>PropertyDataSourceProvider</code></td><td><code>provider</code> (under a data source)</td><td><code>DataSourceOptions.Provider</code></td></tr><tr><td><code>PropertyDataSourceConnectionString</code></td><td><code>connectionString</code> (under a data source)</td><td><code>DataSourceOptions.ConnectionString</code></td></tr><tr><td><code>PropertyDataSourceConnectionStringName</code></td><td><code>connectionStringName</code> (under a data source)</td><td><code>DataSourceOptions.ConnectionStringName</code></td></tr><tr><td><code>PropertyDbProvider</code></td><td><code>quartz.dbprovider</code></td><td>the metadata factory on <code>UseGenericDatabase</code></td></tr><tr><td><code>PropertyDbProviderType</code></td><td><code>connectionProvider.type</code> (under a data source)</td><td><code>UseConnectionProvider&lt;T&gt;()</code>, or <code>DataSourceOptions.UseRegisteredDataSource</code> — the key itself is still read</td></tr><tr><td><code>PropertyExecutionLimitPrefix</code></td><td><code>quartz.executionLimit</code></td><td><code>UseExecutionLimits(limits =&gt; …)</code></td></tr><tr><td><code>PropertyPluginPrefix</code></td><td><code>quartz.plugin</code></td><td><code>AddPlugin&lt;T&gt;()</code></td></tr><tr><td><code>PropertyPluginType</code></td><td><code>type</code> (under a plugin)</td><td><code>AddPlugin&lt;T&gt;()</code></td></tr><tr><td><code>PropertyJobListenerPrefix</code></td><td><code>quartz.jobListener</code></td><td><code>AddJobListener&lt;T&gt;(matchers)</code>; the key is rejected rather than ignored — see <a href="#the-listener-property-keys-are-retired">The listener property keys are retired</a></td></tr><tr><td><code>PropertyTriggerListenerPrefix</code></td><td><code>quartz.triggerListener</code></td><td><code>AddTriggerListener&lt;T&gt;(matchers)</code>; the key is rejected rather than ignored — see above</td></tr><tr><td><code>PropertyListenerType</code></td><td><code>type</code> (under a listener)</td><td>the two <code>Add*Listener&lt;T&gt;</code> methods above</td></tr><tr><td><code>PropertyCheckConfiguration</code></td><td><code>quartz.checkConfiguration</code></td><td>still read, by <code>QuartzSchedulerBuilder.UseProperties</code></td></tr><tr><td><code>PropertyObjectSerializer</code></td><td><code>quartz.serializer</code></td><td><code>UseSerializer&lt;T&gt;()</code>, <code>UseSystemTextJsonSerializer()</code></td></tr><tr><td><code>PropertyThreadExecutor</code></td><td><code>quartz.threadExecutor</code></td><td>nothing; there is no thread executor since the thread pool became asynchronous</td></tr><tr><td><code>PropertyThreadExecutorType</code></td><td><code>quartz.threadExecutor.type</code></td><td>nothing; see above</td></tr><tr><td><code>DefaultInstanceId</code></td><td><code>NON_CLUSTERED</code></td><td><code>QuartzSchedulerOptions.DefaultInstanceId</code>, which is the same string</td></tr><tr><td><code>AutoGenerateInstanceId</code></td><td><code>AUTO</code></td><td><code>QuartzSchedulerOptions.GenerateInstanceId</code></td></tr><tr><td><code>SystemPropertyAsInstanceId</code></td><td><code>SYS_PROP</code></td><td>keep setting <code>quartz.scheduler.instanceId</code> to <code>SYS_PROP</code>: it still selects the generator that reads the id from the <code>quartz.scheduler.instanceId</code> environment variable. That generator is internal and cannot be named directly, so to vary the behaviour, register an <code>IInstanceIdGenerator</code> of your own in the container as described above</td></tr></tbody></table><p>Three more constants existed in 3.x and had already gone before this release, listed here because a 3.x configuration is the one most likely to still name them:</p><table><thead><tr><th>Removed constant</th><th>Value</th><th>Replacement</th></tr></thead><tbody><tr><td><code>ConfigurationSectionName</code></td><td><code>quartz</code></td><td>the <code>&lt;quartz&gt;</code> Full Framework configuration section is not read; use <code>IConfiguration</code></td></tr><tr><td><code>PropertiesFile</code></td><td><code>quartz.config</code></td><td>nothing is read from disk — see &quot;The quartz.config file is no longer read&quot; above</td></tr><tr><td><code>PropertySchedulerName</code></td><td><code>schedName</code></td><td>nothing; it named an ADO.NET column rather than a setting</td></tr></tbody></table><h3 id="every-removed-member" tabindex="-1"><a class="header-anchor" href="#every-removed-member"><span>Every removed member</span></a></h3><p>The factory was also a set of override points, and a 3.x application that subclassed it was usually reaching for one of them. Each had a reason that the container now covers directly.</p><table><thead><tr><th>Removed member</th><th>Replacement</th></tr></thead><tbody><tr><td><code>StdSchedulerFactory()</code></td><td><code>QuartzSchedulerBuilder.Create()</code></td></tr><tr><td><code>StdSchedulerFactory(NameValueCollection)</code></td><td><code>QuartzSchedulerBuilder.Create().UseProperties(properties)</code></td></tr><tr><td><code>Initialize(NameValueCollection)</code></td><td><code>UseProperties(properties)</code></td></tr><tr><td><code>Initialize()</code></td><td>nothing; there is no file and no environment overlay left to read</td></tr><tr><td><code>GetScheduler()</code>, <code>LookupScheduler(name)</code>, <code>GetAllSchedulers()</code></td><td>unchanged apart from the by-name rename — they are <code>ISchedulerFactory</code>, which <code>Build()</code> returns</td></tr><tr><td><code>static GetDefaultScheduler()</code></td><td>build a scheduler where the application starts and hold it, or inject <code>IScheduler</code></td></tr><tr><td><code>Dispose()</code>, <code>Dispose(bool)</code></td><td>the factory <code>Build()</code> returns owns its container and implements <code>IDisposable</code> and <code>IAsyncDisposable</code>; cast to dispose it</td></tr><tr><td><code>GetSchedulerRepository()</code></td><td><code>ISchedulerRepository</code>, resolved from the container</td></tr><tr><td><code>GetDBConnectionManager()</code> (3.x)</td><td>nothing; the container is the provider registry — see <a href="#the-connection-manager-is-gone">The connection manager is gone</a></td></tr><tr><td><code>GetNamedConnectionString(string)</code> (3.x)</td><td><code>DataSourceOptions.ConnectionStringName</code>, resolved from <code>IConfiguration</code>&#39;s connection strings</td></tr><tr><td><code>Instantiate(QuartzSchedulerResources, QuartzScheduler)</code> (3.x)</td><td>nothing; both types are internal and the container builds the graph</td></tr><tr><td><code>InstantiateType&lt;T&gt;(Type?)</code> (3.x)</td><td>register the implementation in the container — this was the seam a container had to patch, and it is the container now</td></tr><tr><td><code>IsSupportedConfigurationKey(string)</code></td><td>set <code>quartz.checkConfiguration</code> to <code>false</code> to allow keys of your own</td></tr><tr><td><code>LoadType(string?)</code></td><td><code>ITypeLoader</code>, selected with <code>UseTypeLoader&lt;T&gt;()</code></td></tr><tr><td><code>ValidateConfiguration()</code> (3.x)</td><td><code>quartz.checkConfiguration</code> for the keys, and <code>IValidateOptions&lt;T&gt;</code> for the typed options</td></tr></tbody></table><h2 id="the-builder-surface-says-each-thing-once" tabindex="-1"><a class="header-anchor" href="#the-builder-surface-says-each-thing-once"><span>The builder surface says each thing once</span></a></h2><p>Three members were spellings of a member beside them, and the audit that found them ruled on the rest of their families at the same time — the reasoning lives on the members, and <code>AGENTS.md</code> lists the sets that were counted and left alone.</p><table><thead><tr><th>Removed</th><th>Say it with</th></tr></thead><tbody><tr><td><code>IScheduler.ScheduleJob(IJobDetail, ITrigger, CancellationToken)</code> and <code>ScheduleJob(ITrigger, CancellationToken)</code></td><td>the surviving overloads, which took <code>ScheduleJobOptions options</code> without a default only because these two made one ambiguous. <code>options</code> now defaults, so <code>ScheduleJob(job, trigger)</code> still compiles — but a <strong>token passed positionally</strong> no longer does, and is a compile error rather than a silent change: write <code>ScheduleJob(job, trigger, cancellationToken: token)</code></td></tr><tr><td><code>IServiceCollection.AddQuartzHealthChecks(Action&lt;QuartzHealthCheckOptions&gt;?)</code></td><td><code>services.AddHealthChecks().AddQuartz(configure)</code>, which is what it forwarded to. <code>IQuartzBuilder.AddQuartzHealthChecks()</code> stays: a scheduler&#39;s builder knows which scheduler the check is for, so it is the one receiver that carries information the ratified form would make you repeat</td></tr><tr><td><code>JobBuilder&lt;TJob&gt;.OfType(string typeName)</code></td><td><code>OfType((JobType) typeName)</code>. The <code>string</code> → <code>JobType</code> conversion is <strong>explicit</strong> on purpose — a name is taken unvalidated and fails at the first use of <code>JobType.Type</code> — and a method taking the string directly hid the leap the cast exists to show</td></tr></tbody></table><p><code>ScheduleJob</code>&#39;s store path is now chosen by <code>ScheduleJobOptions.Replace</code> rather than by which overload was called: not replacing goes through <code>IJobStore.ScheduleJob</code> exactly as before, so that member keeps its caller and a store&#39;s span and metric names for the ordinary call are unchanged, while replacing still goes through <code>IJobStore.ScheduleJobs</code> because over-writing a job and its trigger has to be one operation under one lock. An explicit <code>new ScheduleJobOptions()</code> therefore takes the plain path now, which is the same store outcome and the better span name.</p><p><code>QuartzAspireSettings.ProvisionSchema</code> became <code>SchemaProvisioning</code>, of type <code>SchemaProvisioning?</code> — see the <a href="#quartz-aspire-is-new"><code>Quartz.Aspire</code> is new</a> settings table.</p><h2 id="the-standalone-builder-is-the-same-builder" tabindex="-1"><a class="header-anchor" href="#the-standalone-builder-is-the-same-builder"><span>The standalone builder is the same builder</span></a></h2><p><code>QuartzSchedulerBuilder.Create</code> takes the callback <code>AddQuartz</code> takes, and hands it the same <code>IQuartzBuilder</code>. There is one configuration API with two front doors: <code>AddQuartz</code> for an application that has a container, <code>QuartzSchedulerBuilder</code> for one that does not, and the callback in between reads identically either way.</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> var scheduler = await QuartzSchedulerBuilder.Create()</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line">     .Configure(q =&gt;</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line">     {</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line">         q.UsePersistentStore(store =&gt; store.UseSqlServer(connectionString));</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line">         q.AddJob&lt;ReportJob&gt;(j =&gt; j.WithIdentity(&quot;report&quot;));</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line">     })</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line">     .UseDefaultThreadPool(maxConcurrency: 20)</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line">     .BuildScheduler();</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> IScheduler scheduler = await QuartzSchedulerBuilder</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">     .Create(q =&gt; q</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">         .UseDefaultThreadPool(maxConcurrency: 20)</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">         .UsePersistentStore(store =&gt; store.UseSqlServer(connectionString))</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">         .AddJob&lt;ReportJob&gt;(j =&gt; j.WithIdentity(&quot;report&quot;).StoreDurably()))</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">     .BuildScheduler();</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Everything on <code>IQuartzBuilder</code> — jobs, triggers, calendars, listeners, plugins, execution limits — and every extension a package contributes is available to the callback, which is the point. The builder itself declares five members and no more: <code>Create(configure)</code>, <code>Build()</code>, <code>BuildScheduler()</code>, <code>UseConfiguration(IConfiguration)</code> and the two <code>UseProperties</code> overloads. It re-declares nothing, so nothing can fall behind what <code>IQuartzBuilder</code> grows.</p><p>The callback is optional, exactly as <code>AddQuartz()</code>&#39;s is, so a scheduler described entirely by configuration or by flat keys is still <code>Create().UseConfiguration(section)</code> or <code>Create().UseProperties(properties)</code>.</p><p><code>Services</code> and <code>SchedulerName</code> are <code>IQuartzBuilder</code>&#39;s and are read off the callback&#39;s argument, as <code>q.Services</code> and <code>q.SchedulerName</code>. If you ran a 4.0 pre-release that let a chain be written directly on <code>QuartzSchedulerBuilder</code>, see <a href="#appendix-if-you-ran-a-4-0-pre-release">Appendix: if you ran a 4.0 pre-release</a>.</p><h3 id="build-returns-something-you-can-dispose" tabindex="-1"><a class="header-anchor" href="#build-returns-something-you-can-dispose"><span><code>Build()</code> returns something you can dispose</span></a></h3><p><code>Build()</code> used to return <code>ISchedulerFactory</code> while handing back an object that owned a container, so every caller that wanted to shut it down had to cast to a type the type system never mentioned. It now returns <code>StandaloneSchedulerFactory</code>, which <em>is</em> an <code>ISchedulerFactory</code> and is also <code>IAsyncDisposable</code> and <code>IDisposable</code>:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> ISchedulerFactory factory = builder.Build();</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"> using IDisposable container = (IDisposable) factory;</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> await using StandaloneSchedulerFactory factory = builder.Build();</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Prefer <code>await using</code>: disposal shuts the scheduler down, which is asynchronous work that <code>Dispose()</code> can only block on. A caller that never disposes behaves as it always did — the scheduler runs until the process ends.</p><p>Two members moved the other way, from the standalone builder onto <code>IQuartzBuilder</code>, so that a scheduler registered with <code>AddQuartz</code> can also be given a part that was built rather than configured:</p><table><thead><tr><th>Member</th><th>Meaning</th></tr></thead><tbody><tr><td><code>UseThreadPool(IThreadPool)</code></td><td>uses a pool the caller constructed</td></tr><tr><td><code>UseJobStore(IJobStore)</code></td><td>uses a store the caller constructed</td></tr></tbody></table><table><thead><tr><th>Removed from <code>QuartzSchedulerBuilder</code></th><th>Use instead</th></tr></thead><tbody><tr><td><code>Configure(Action&lt;IQuartzBuilder&gt;)</code></td><td><code>Create(Action&lt;IQuartzBuilder&gt;)</code>, which is the same callback moved to where the builder is created</td></tr><tr><td><code>ConfigureScheduler</code>, <code>UseDefaultThreadPool</code> ×2, <code>UseJobFactory(IJobFactory)</code>, <code>UseInMemoryStore</code></td><td>the identical <code>IQuartzBuilder</code> members, inside the <code>Create</code> callback</td></tr></tbody></table><h2 id="ischeduler-is-iasyncdisposable" tabindex="-1"><a class="header-anchor" href="#ischeduler-is-iasyncdisposable"><span><code>IScheduler</code> is <code>IAsyncDisposable</code></span></a></h2><p><code>IScheduler</code> now implements <code>IAsyncDisposable</code>, so a scheduler can be scoped with <code>await using</code> instead of a <code>try</code>/<code>finally</code> that remembers to shut it down:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line"> IScheduler scheduler = await factory.GetScheduler();</span>
<span class="line"></span></span><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> try</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"> {</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line">     await scheduler.Start();</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line">     …</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"> }</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"> finally</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"> {</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line">     await scheduler.Shutdown(waitForJobsToComplete: false);</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"> }</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> await using (scheduler)</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line"> {</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">     await scheduler.Start();</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">     …</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line"> }</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Disposing releases what <strong>that instance</strong> owns, which is not the same thing for every scheduler:</p><table><thead><tr><th>Instance</th><th>Disposing it</th></tr></thead><tbody><tr><td>A local scheduler</td><td><code>Shutdown(waitForJobsToComplete: false)</code>. It owns the execution it drives.</td></tr><tr><td>The <code>IScheduler</code> a container injects</td><td>disposes the scheduler it built, and does nothing at all if it never built one.</td></tr><tr><td>A <code>DelegatingScheduler</code></td><td>forwards to the scheduler it wraps.</td></tr><tr><td><code>HttpScheduler</code></td><td>releases its own resources and <strong>never</strong> shuts the remote scheduler down. A client going away is not an instruction to stop scheduling for everybody else — call <code>Shutdown</code> for that.</td></tr></tbody></table><p>Disposal is idempotent: disposing a scheduler that is already shut down does nothing. It is deliberately the non-waiting shutdown — <code>await using</code> means &quot;stop this when the block ends&quot;, not &quot;drain gracefully&quot;. Call <code>Shutdown(waitForJobsToComplete: true)</code> yourself when running jobs should be allowed to finish; disposing afterwards is then a no-op.</p><p><code>IScheduler</code> only <em>inherits</em> <code>IAsyncDisposable</code>; it does not redeclare <code>DisposeAsync</code>. An earlier 4.0 preview did, with <code>new ValueTask DisposeAsync()</code>, which forced every implementation of the interface to carry a member the base interface had already asked for. An <code>IScheduler</code> of your own implements <code>IAsyncDisposable.DisposeAsync</code> and nothing else changes — the method that satisfied the redeclared one satisfies this too.</p><h3 id="a-container-holding-a-scheduler-is-disposed-asynchronously" tabindex="-1"><a class="header-anchor" href="#a-container-holding-a-scheduler-is-disposed-asynchronously"><span>A container holding a scheduler is disposed asynchronously</span></a></h3><p>This is the one change that can surface as a runtime error. <code>ServiceProvider.Dispose()</code> throws when a singleton it created implements only <code>IAsyncDisposable</code>, so a container that resolved an <code>IScheduler</code> has to be disposed with <code>await using</code>:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> using var provider = services.BuildServiceProvider();</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> await using var provider = services.BuildServiceProvider();</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>Applications hosted by <code>IHost</code> need no change — the host already disposes its container asynchronously, and <code>AddQuartzHostedService</code> shuts the schedulers down before that anyway. This is about a container built by hand, which is mostly tests.</p><h2 id="a-scheduler-s-lifecycle-is-one-value" tabindex="-1"><a class="header-anchor" href="#a-scheduler-s-lifecycle-is-one-value"><span>A scheduler&#39;s lifecycle is one value</span></a></h2><p><code>IScheduler.IsStarted</code>, <code>IScheduler.InStandbyMode</code> and <code>IScheduler.IsShutdown</code> are gone. One <code>SchedulerStatus Status</code> replaces all three:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token keyword">public</span> <span class="token keyword">enum</span> <span class="token class-name">SchedulerStatus</span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    Unknown<span class="token punctuation">,</span>        <span class="token comment">// a scheduler that could not be asked - a remote one that did not answer</span></span>
<span class="line">    Created<span class="token punctuation">,</span>        <span class="token comment">// built, never started</span></span>
<span class="line">    Running<span class="token punctuation">,</span>        <span class="token comment">// firing triggers</span></span>
<span class="line">    Standby<span class="token punctuation">,</span>        <span class="token comment">// started once, stood down, can be started again</span></span>
<span class="line">    ShuttingDown<span class="token punctuation">,</span>   <span class="token comment">// Shutdown() is running</span></span>
<span class="line">    Shutdown        <span class="token comment">// down, and not restartable</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The three booleans mapped onto it exactly, so every call site has a mechanical replacement:</p><table><thead><tr><th>3.x</th><th>4.x</th></tr></thead><tbody><tr><td><code>scheduler.IsStarted</code></td><td><code>scheduler.Status is not SchedulerStatus.Created</code></td></tr><tr><td><code>scheduler.InStandbyMode</code></td><td><code>scheduler.Status is SchedulerStatus.Created or SchedulerStatus.Standby or SchedulerStatus.ShuttingDown</code></td></tr><tr><td><code>scheduler.IsShutdown</code></td><td><code>scheduler.Status is SchedulerStatus.Shutdown</code></td></tr></tbody></table><p>Those rows are the <em>faithful</em> translations, and two of them are worth reading twice. <code>IsStarted</code> stayed <code>true</code> after a shutdown — it meant &quot;<code>Start</code> has been called at some point&quot;, not &quot;is running now&quot; — so code that used it as &quot;is running&quot; was already wrong and should say <code>Status is SchedulerStatus.Running</code>. And <code>InStandbyMode</code> was <code>true</code> for a scheduler that had never been started at all, which is why <code>Created</code> exists: both fire nothing, but only one of them has ever run and has a <code>RunningSince</code>.</p><p><code>SchedulerMetadata</code> follows, for the same reason:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> if (metadata.Shutdown) { … }</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"> else if (metadata.InStandbyMode) { … }</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"> else if (metadata.Started) { … }</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> switch (metadata.Status) { … }</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><table><thead><tr><th>3.x</th><th>4.x</th></tr></thead><tbody><tr><td><code>SchedulerMetadata.Started</code>, <code>.InStandbyMode</code>, <code>.Shutdown</code></td><td><code>required SchedulerStatus Status</code></td></tr></tbody></table><p>The two producers derive it the same way now. They did not before: <code>StdScheduler.GetMetadata</code> set <code>Started</code> from &quot;has ever been started&quot; while <code>HttpScheduler.GetMetadata</code> set it from &quot;is running now&quot;, so the same scheduler described itself differently depending on which side of an HTTP call you asked from. <code>RunningSince</code> is unchanged.</p><h3 id="the-transitions-are-honest-about-themselves" tabindex="-1"><a class="header-anchor" href="#the-transitions-are-honest-about-themselves"><span>The transitions are honest about themselves</span></a></h3><p>Reducing the state to one value made four behaviours that were only true of some of the booleans have to become true of the value. A transition that does not happen is not announced:</p><table><thead><tr><th>Call</th><th>3.x</th><th>4.x</th></tr></thead><tbody><tr><td><code>Start()</code> on a scheduler that is already running</td><td>re-emits <code>SchedulerStarting</code> and <code>SchedulerStarted</code>, and tells the job store the scheduler resumed</td><td>does nothing at all</td></tr><tr><td><code>Standby()</code> on a scheduler that is not running</td><td>emits <code>SchedulerInStandbyMode</code> and tells the job store it paused, for a scheduler that was already firing nothing</td><td>does nothing at all; a never-started scheduler stays <code>Created</code></td></tr><tr><td><code>Standby()</code> on a scheduler that has shut down</td><td>silently pauses a scheduler that is already down</td><td>throws <code>SchedulerException</code></td></tr><tr><td><code>Shutdown()</code></td><td>calls <code>Standby()</code> on the way down, so listeners hear <code>SchedulerInStandbyMode</code> before <code>SchedulerShuttingDown</code></td><td>goes <code>Running</code>/<code>Standby</code>/<code>Created</code> → <code>ShuttingDown</code> → <code>Shutdown</code>, and no listener is told about a standby that never happened</td></tr></tbody></table><p>The last one is a listener-visible change, inherited from Java, and it is the one to check for. A listener that counted <code>SchedulerInStandbyMode</code> to detect &quot;the scheduler stopped firing&quot; should be counting <code>SchedulerShuttingDown</code> as well — it is raised by every shutdown, as it always was. The full sequence over a start, a standby and a shutdown is now exactly:</p><p><code>SchedulerStarting</code> → <code>SchedulerStarted</code> → <code>SchedulerInStandbyMode</code> → <code>SchedulerShuttingDown</code> → <code>SchedulerShutdown</code></p><p><code>Status</code> becomes <code>Shutdown</code> at the <em>end</em> of the shutdown, once the plugins and the job store are down, so a scheduler that is draining its running jobs reads <code>ShuttingDown</code> for as long as it takes — and it reads <code>Shutdown</code> even when a plugin or job store threw on the way down, because the shutdown is claimed once and cannot be run again to finish the job.</p><p>A scheduler that has begun shutting down refuses work exactly as a shut-down one does, and <code>ISchedulerFactory.GetScheduler()</code> refuses to hand it back for the same reason. It is still <em>listed</em> by <code>ISchedulerRepository</code> and by the dashboard while it drains, which is what <code>ShuttingDown</code> is for.</p><h3 id="the-health-check-reports-the-state-it-found" tabindex="-1"><a class="header-anchor" href="#the-health-check-reports-the-state-it-found"><span>The health check reports the state it found</span></a></h3><p><code>AddQuartzHealthChecks()</code> read <code>IsStarted</code> alone, which made a scheduler in standby <strong>healthy</strong> (it had been started once) and a shut-down one fall through to the store probe, where the failure was reported as a connectivity problem. It reports the state it actually found instead:</p><table><thead><tr><th><code>Status</code></th><th>Health</th></tr></thead><tbody><tr><td><code>Running</code></td><td>the store probe decides: <code>Healthy</code>, or <code>Unhealthy</code> when the store cannot be reached</td></tr><tr><td><code>Standby</code></td><td><code>Degraded</code> — deliberate and reversible, so neither healthy nor a reason to take a node out of rotation</td></tr><tr><td><code>Created</code>, with <code>AutoStart</code> left on</td><td><code>Unhealthy</code> — the host was going to start it and has not</td></tr><tr><td><code>Created</code>, with <code>AutoStart</code> set to <code>false</code></td><td><code>Degraded</code> — the application starts this one itself, so sitting in <code>Created</code> is the configuration working</td></tr><tr><td><code>ShuttingDown</code>, <code>Shutdown</code>, <code>Unknown</code></td><td><code>Unhealthy</code>, with a message naming the scheduler and the state</td></tr></tbody></table><p>The check also no longer throws when it is registered for a default scheduler in a container that has only named ones: it reports <code>Unhealthy</code> and says to call <code>AddQuartzHealthChecks()</code> on the scheduler&#39;s own builder.</p><p><strong>A clustered scheduler is also asked when this node last checked in.</strong> The check-in loop runs on its own timer, so a node whose cluster manager has wedged still fires, still answers a store query and still reports <code>Running</code> — while its peers, to whom it looks dead, recover its triggers. A last check-in older than <code>QuartzHealthCheckOptions.ClusterCheckinTolerance</code> times that node&#39;s own configured check-in interval now reports <code>Degraded</code>, naming the node and how late it is. The tolerance is a <code>double</code> defaulting to <code>3</code>; set it to <code>null</code> or <code>0</code> to make no such query. <strong>Nothing is read on an unclustered scheduler</strong>, so an in-memory or single-node store is unaffected.</p><h2 id="clustering-is-configured-in-one-place" tabindex="-1"><a class="header-anchor" href="#clustering-is-configured-in-one-place"><span>Clustering is configured in one place</span></a></h2><p><code>AdoJobStoreOptions</code> no longer carries <code>Clustered</code>, <code>ClusterCheckinInterval</code> and <code>ClusterCheckinMisfireThreshold</code>. Those three said the same thing as <code>UseClustering(…)</code> and <code>ClusteringOptions</code>, so a scheduler had two places to be clustered from and they could disagree. <code>ClusteringOptions</code> is now the one place, and whether a store is clustered is something it <em>reports</em> rather than something you can also set on it.</p><table><thead><tr><th>Removed</th><th>Use instead</th></tr></thead><tbody><tr><td><code>AdoJobStoreOptions.Clustered</code></td><td><code>ClusteringOptions.Enabled</code></td></tr><tr><td><code>AdoJobStoreOptions.ClusterCheckinInterval</code></td><td><code>ClusteringOptions.CheckinInterval</code></td></tr><tr><td><code>AdoJobStoreOptions.ClusterCheckinMisfireThreshold</code></td><td><code>ClusteringOptions.CheckinMisfireThreshold</code></td></tr></tbody></table><p><code>ClusteringOptions</code>&#39; two intervals are no longer nullable — <code>UseClustering()</code> with no arguments turns clustering on without touching them, because it configures the options object rather than assigning a copy over it.</p><p>Code-first configuration is unchanged:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line">store<span class="token punctuation">.</span><span class="token function">UseClustering</span><span class="token punctuation">(</span>cluster <span class="token operator">=&gt;</span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    cluster<span class="token punctuation">.</span>CheckinInterval <span class="token operator">=</span> TimeSpan<span class="token punctuation">.</span><span class="token function">FromSeconds</span><span class="token punctuation">(</span><span class="token number">10</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">    cluster<span class="token punctuation">.</span>CheckinMisfireThreshold <span class="token operator">=</span> TimeSpan<span class="token punctuation">.</span><span class="token function">FromSeconds</span><span class="token punctuation">(</span><span class="token number">20</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"><span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The flat keys are unchanged as well — <code>quartz.jobStore.clustered</code>, <code>quartz.jobStore.clusterCheckinInterval</code> and <code>quartz.jobStore.clusterCheckinMisfireThreshold</code> all still work, and so does the <code>JobStore:Clustered</code> spelling in <code>appsettings.json</code>, because <code>AddQuartz</code> reads every section as flat keys too. What is new is the hierarchical spelling that matches the options type:</p><div class="language-json line-numbers-mode" data-highlighter="prismjs" data-ext="json"><pre><code class="language-json"><span class="line"><span class="token punctuation">{</span></span>
<span class="line">  <span class="token property">&quot;Quartz&quot;</span><span class="token operator">:</span> <span class="token punctuation">{</span></span>
<span class="line">    <span class="token property">&quot;JobStore&quot;</span><span class="token operator">:</span> <span class="token punctuation">{</span></span>
<span class="line">      <span class="token property">&quot;Clustering&quot;</span><span class="token operator">:</span> <span class="token punctuation">{</span></span>
<span class="line">        <span class="token property">&quot;Enabled&quot;</span><span class="token operator">:</span> <span class="token boolean">true</span><span class="token punctuation">,</span></span>
<span class="line">        <span class="token property">&quot;CheckinInterval&quot;</span><span class="token operator">:</span> <span class="token string">&quot;00:00:10&quot;</span><span class="token punctuation">,</span></span>
<span class="line">        <span class="token property">&quot;CheckinMisfireThreshold&quot;</span><span class="token operator">:</span> <span class="token string">&quot;00:00:20&quot;</span></span>
<span class="line">      <span class="token punctuation">}</span></span>
<span class="line">    <span class="token punctuation">}</span></span>
<span class="line">  <span class="token punctuation">}</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>AdoJobStoreOptions</code> validation lost the rule that <code>UseDbLocks</code> must be on when <code>Clustered</code> is: it can no longer see both settings, and it never needed to, since every path that enables clustering enables database locking with it and a store with an explicit lock handler of its own — the Redis one, say — was never wrong to leave <code>UseDbLocks</code> off.</p><h2 id="the-sqlite-extension-methods-swapped-names" tabindex="-1"><a class="header-anchor" href="#the-sqlite-extension-methods-swapped-names"><span>The SQLite extension methods swapped names</span></a></h2><div class="hint-container warning"><p class="hint-container-title">Warning</p><p><code>UseSqlite</code> did not exist in 3.x and now means <strong>Microsoft.Data.Sqlite</strong>. The method that used to be called <code>UseSQLite</code> — the legacy <strong>System.Data.SQLite</strong> driver — is now <code>UseSystemDataSqlite</code>. Changing <code>UseSQLite</code> to <code>UseSqlite</code> compiles and runs, and silently swaps your ADO.NET provider. Read the table before doing a case-insensitive find and replace.</p></div><table><thead><tr><th>3.x / 4.0 preview</th><th>4.0</th><th>ADO.NET driver</th><th>Provider name</th></tr></thead><tbody><tr><td><code>UseSQLite</code></td><td><code>UseSystemDataSqlite</code></td><td>System.Data.SQLite</td><td><code>SQLite</code></td></tr><tr><td><code>UseMicrosoftSQLite</code></td><td><code>UseSqlite</code></td><td>Microsoft.Data.Sqlite</td><td><code>SQLite-Microsoft</code></td></tr></tbody></table><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> store.UseSQLite(connectionString);          // System.Data.SQLite</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> store.UseSystemDataSqlite(connectionString);</span>
<span class="line"></span></span></span>
<span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> store.UseMicrosoftSQLite(connectionString); // Microsoft.Data.Sqlite</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> store.UseSqlite(connectionString);</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The short name goes to the driver you should reach for, which is the same rule <code>UseMySql</code> and <code>UseMySqlConnector</code> already followed, and the same spelling Entity Framework Core uses for the same choice. The provider names themselves — what a <code>quartz.dataSource.&lt;name&gt;.provider</code> key says — are unchanged, so nothing in a configuration file has to move.</p><h2 id="a-data-source-is-defined-referred-to-or-handed-over" tabindex="-1"><a class="header-anchor" href="#a-data-source-is-defined-referred-to-or-handed-over"><span>A data source is defined, referred to, or handed over</span></a></h2><p>There were five ways to say where a job store&#39;s connections come from, and two of them said the same thing. <code>UseDataSourceConnectionProvider()</code> is gone; it existed only to set <code>DataSourceOptions.UseRegisteredDataSource</code>, and being a method it also had to be called in the right order to take effect.</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line"> q.UsePersistentStore(store =&gt;</span>
<span class="line"></span><span class="token prefix unchanged"> </span><span class="token line"> {</span>
<span class="line"></span></span><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line">     store.UsePostgres(db =&gt; db.Provider = &quot;Npgsql&quot;);</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line">     store.UseDataSourceConnectionProvider();</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line">     store.UsePostgres(db =&gt; db.UseRegisteredDataSource = true);</span>
<span class="line"></span></span><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line"> });</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>What is left says four different things:</p><table><thead><tr><th>Member</th><th>Role</th></tr></thead><tbody><tr><td><code>UseDataSource(configure)</code></td><td><strong>defines</strong> a data source — which driver, and how to reach the database. The database methods such as <code>UseSqlServer</code> are shorthands for it</td></tr><tr><td><code>UseDataSource(name)</code></td><td><strong>refers to</strong> a data source by name, picking up settings registered elsewhere, such as a <code>Quartz:DataSource:&lt;name&gt;</code> section</td></tr><tr><td><code>DataSourceOptions.UseRegisteredDataSource</code></td><td>takes connections from the container&#39;s unkeyed <code>DbDataSource</code>, instead of from a connection string</td></tr><tr><td><code>DataSourceOptions.DataSourceServiceKey</code> / <code>.DataSourceFactory</code></td><td>the same, for a <code>DbDataSource</code> registered under a key of its own or built by the caller. Set from code — neither a service key nor a delegate is something a configuration binder can produce</td></tr><tr><td><code>UseConnectionProvider&lt;T&gt;()</code> / <code>UseConnectionProvider(factory)</code></td><td><strong>replaces</strong> the connection provider outright, for connections Quartz cannot describe. The code spelling of <code>quartz.dataSource.&lt;name&gt;.connectionProvider.type</code></td></tr></tbody></table><p>Where connections come from is a property of the data source, so the first three are said in <code>DataSourceOptions</code> alongside <code>ConnectionString</code> and <code>ConnectionStringName</code>, and <code>UseRegisteredDataSource</code> wins over both. <code>UseConnectionProvider</code> is the one that is not a data source setting, because it does not describe a database — it hands over the object that reaches one. It is therefore also the one method here that replaces rather than defers: it beats whatever <code>UseSqlServer</code> and its siblings registered, whichever order the two were called in, and names this store&#39;s data source so it needs no <code>UseDataSource</code> call of its own.</p><h3 id="adddatasourceprovider-went-with-it" tabindex="-1"><a class="header-anchor" href="#adddatasourceprovider-went-with-it"><span><code>AddDataSourceProvider()</code> went with it</span></a></h3><p>The two always travelled together. <code>AddDataSourceProvider()</code>, on the configurator, registered <code>DataSourceDbProvider</code> in the container as the <code>IDbProvider</code> to use; <code>UseDataSourceConnectionProvider()</code>, on the store, then named it as that data source&#39;s connection provider. Two calls, in two different places, that had to agree with each other to do anything. Both are gone, and <code>UseRegisteredDataSource</code> does the whole job — the data source builds its own <code>DataSourceDbProvider</code> around the <code>DbDataSource</code> it resolves from the container:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line"> services.AddQuartz(q =&gt;</span>
<span class="line"></span><span class="token prefix unchanged"> </span><span class="token line"> {</span>
<span class="line"></span></span><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line">     q.AddDataSourceProvider();</span>
<span class="line"></span></span><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line">     q.UsePersistentStore(store =&gt;</span>
<span class="line"></span><span class="token prefix unchanged"> </span><span class="token line">     {</span>
<span class="line"></span></span><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line">         store.UsePostgres(db =&gt; db.Provider = &quot;Npgsql&quot;);</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line">         store.UseDataSourceConnectionProvider();</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line">         store.UsePostgres(db =&gt; db.UseRegisteredDataSource = true);</span>
<span class="line"></span></span><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line">     });</span>
<span class="line"></span><span class="token prefix unchanged"> </span><span class="token line"> });</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Registering the <code>DbDataSource</code> itself is still yours to do — <code>services.AddNpgsqlDataSource(…)</code>, or whatever your provider offers. What is no longer yours to do is wiring it up to Quartz.</p><p><code>UseRegisteredDataSource</code> asks for the container&#39;s one unkeyed <code>DbDataSource</code>, which was the only shape 3.x&#39;s <code>AddDataSourceProvider()</code> could express either. A container holding several — a scheduler per tenant — says which one it means with <code>DataSourceServiceKey</code>, and a data source built rather than registered goes in <code>DataSourceFactory</code>:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line">services<span class="token punctuation">.</span><span class="token function">AddNpgsqlDataSource</span><span class="token punctuation">(</span>tenantA<span class="token punctuation">,</span> <span class="token named-parameter punctuation">serviceKey</span><span class="token punctuation">:</span> <span class="token string">&quot;tenant-a&quot;</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">services<span class="token punctuation">.</span><span class="token function">AddQuartz</span><span class="token punctuation">(</span><span class="token string">&quot;tenant-a&quot;</span><span class="token punctuation">,</span> q <span class="token operator">=&gt;</span> q<span class="token punctuation">.</span><span class="token function">UsePersistentStore</span><span class="token punctuation">(</span>store <span class="token operator">=&gt;</span></span>
<span class="line">    store<span class="token punctuation">.</span><span class="token function">UsePostgres</span><span class="token punctuation">(</span>db <span class="token operator">=&gt;</span> db<span class="token punctuation">.</span>DataSourceServiceKey <span class="token operator">=</span> <span class="token string">&quot;tenant-a&quot;</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Both are settable from code only — a service key is any object and a factory is a delegate, so a configuration binder can produce neither. Both imply <code>UseRegisteredDataSource</code>, so neither needs a connection string.</p><p>Commands on this path are now made by the connection rather than built from the driver description. A <code>DbDataSource</code> configures the connections it hands out — <code>NpgsqlDataSource</code> attaches its type mappers, its logging and its composite type registrations — and a command reaches those through the connection it belongs to, which a command constructed by reflection and given a connection afterwards does not. The connection-string path is unchanged.</p><h2 id="quartzoptions-lost-its-three-typed-settings" tabindex="-1"><a class="header-anchor" href="#quartzoptions-lost-its-three-typed-settings"><span><code>QuartzOptions</code> lost its three typed settings</span></a></h2><p><code>QuartzOptions</code> is the flat <code>quartz.*</code> property bag. Three of its members were typed settings that existed nowhere else in it, each reading and writing a key that has a typed option of its own, so they were a third spelling of a setting that already had two.</p><table><thead><tr><th>Removed</th><th>Use instead</th></tr></thead><tbody><tr><td><code>QuartzOptions.SchedulerName</code></td><td><code>QuartzSchedulerOptions.InstanceName</code>, or <code>Properties[&quot;quartz.scheduler.instanceName&quot;]</code></td></tr><tr><td><code>QuartzOptions.SchedulerId</code></td><td><code>QuartzSchedulerOptions.InstanceId</code>, or <code>Properties[&quot;quartz.scheduler.instanceId&quot;]</code></td></tr><tr><td><code>QuartzOptions.MisfireThreshold</code></td><td><code>InMemoryJobStoreOptions.MisfireThreshold</code> / <code>AdoJobStoreOptions.MisfireThreshold</code></td></tr></tbody></table><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> services.Configure&lt;QuartzOptions&gt;(options =&gt; options.SchedulerName = &quot;core&quot;);</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> services.AddQuartz(q =&gt; q.ConfigureScheduler(options =&gt; options.InstanceName = &quot;core&quot;));</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p><code>MisfireThreshold</code> also had a round-trip problem of its own: it stored a <code>TimeSpan</code> as a string of whole milliseconds, so a value with sub-millisecond precision did not read back as it was written.</p><p><code>Properties</code>, <code>ToProperties()</code> — which is what <code>ToNameValueCollection()</code> became, see &quot;A property bag is any dictionary&quot; — and <code>Scheduling</code> stay. <code>Scheduling</code> is the exception that proves the rule — its three directives say how a configured schedule is applied to a scheduler rather than how a component is configured, and they have no options type of their own to bind onto, so this is where they live.</p><p><code>Scheduling</code> is get-only, like <code>Properties</code>. Options callbacks run in order over one instance, so assigning a fresh <code>SchedulingOptions</code> threw away whatever <code>Quartz:Scheduling</code> — or an earlier callback — had already set, with nothing to show for it. Set the properties instead; the configuration binder needs no setter to bind into a non-null complex property.</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> services.Configure&lt;QuartzOptions&gt;(options =&gt; options.Scheduling = new SchedulingOptions { IgnoreDuplicates = true });</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> services.Configure&lt;QuartzOptions&gt;(options =&gt; options.Scheduling.IgnoreDuplicates = true);</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="addjob-registers-the-job-with-the-container" tabindex="-1"><a class="header-anchor" href="#addjob-registers-the-job-with-the-container"><span><code>AddJob</code> registers the job with the container</span></a></h2><p><code>AddJob&lt;T&gt;()</code>, <code>AddJob(type, …)</code> and <code>ScheduleJob&lt;T&gt;()</code> now register the job type as a <strong>scoped</strong> service, with <code>TryAdd</code> semantics.</p><p>Before, they described the job to the scheduler and nothing else. The job factory resolved the job from the container and fell back to <code>ActivatorUtilities</code> when it found no registration, so a job whose constructor the container could not satisfy was never noticed at startup: <code>ValidateOnBuild</code> — which the host turns on by default in the Development environment — had never heard of the type. The failure arrived at fire time instead, as a <code>JobInstantiationException</code>, by which point the trigger had fired, the job had not run, and every trigger of that job had been moved to <code>TriggerState.Error</code> (discussion <a href="https://github.com/quartznet/quartznet/discussions/3211" target="_blank" rel="noopener noreferrer">#3211</a>).</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line">services<span class="token punctuation">.</span><span class="token function">AddQuartz</span><span class="token punctuation">(</span>q <span class="token operator">=&gt;</span> q<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">AddJob</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>SendReportsJob<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span>j <span class="token operator">=&gt;</span> j<span class="token punctuation">.</span><span class="token function">WithIdentity</span><span class="token punctuation">(</span><span class="token string">&quot;send-reports&quot;</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line"><span class="token comment">// now throws when the container is built:</span></span>
<span class="line"><span class="token comment">// Unable to resolve service for type &#39;IReportStore&#39; while attempting to activate &#39;SendReportsJob&#39;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Two things follow from this.</p><p><strong>A job you register yourself keeps your registration.</strong> The lifetime, factory or implementation type you chose wins, because Quartz&#39;s registration is a <code>TryAdd</code>. Registering the same job with <code>AddJob</code> twice is harmless for the same reason. If you were registering your jobs explicitly to get startup validation, that line is now redundant, but it is not wrong:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> services.AddScoped&lt;SendReportsJob&gt;();   // no longer needed for validation</span>
<span class="line"></span></span><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line"> services.AddQuartz(q =&gt; q.AddJob&lt;SendReportsJob&gt;(j =&gt; j.WithIdentity(&quot;send-reports&quot;)));</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p><strong>Scoped is the lifetime the job factory uses.</strong> It opens a dependency injection scope per fire, resolves the job from it, and disposes the scope when the job returns — so a job may take scoped dependencies such as a database context. If a job of yours has to be a singleton, register it as one yourself; it must then be thread-safe and must not capture scoped dependencies.</p><p>A job type that is an interface or an abstract class is not registered, since the container could not construct it. Jobs named by an XML or JSON schedule are not registered either — nothing describes them to the container — so those still fail at fire time if their dependencies are missing.</p><p><strong>A registered job may not take a scheduler&#39;s own parts by constructor.</strong> <code>IScheduler</code>, <code>ISchedulerFactory</code>, <code>IJobStore</code>, <code>IThreadPool</code> and <code>IOptions&lt;QuartzSchedulerOptions&gt;</code> belong to one scheduler. A job that injected one of them used to be activated through its scheduler&#39;s view of the container and was handed that scheduler&#39;s; a registered job is now resolved from the container like any other service, which resolves them unkeyed — the default scheduler&#39;s, or nothing at all in a container holding only named schedulers. Rather than leave a job silently holding another scheduler&#39;s collaborators, the constructor is refused when the host starts, with a message naming the job and the parameter:</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text"><pre><code class="language-text"><span class="line">Job type ArchiveJob is registered on scheduler &#39;acme&#39;, and its constructor takes ISchedulerFactory</span>
<span class="line">schedulerFactory — a part that belongs to one scheduler. …</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>Take the scheduler from <code>IJobExecutionContext.Scheduler</code>, which is the scheduler actually running the job; take <code>IJobExecutionContextAccessor</code> in code the context is not handed to; or, where the job really has to be <em>constructed</em> with something of its scheduler&#39;s, register it with <code>AddJobType&lt;T&gt;(factory)</code> — below — and resolve that part by key inside the factory, which is not examined. A job type the container does not hold is unaffected: it is still activated by the job factory and still receives its own scheduler&#39;s parts.</p><h3 id="addjobtype-gives-one-scheduler-its-own-build-of-a-job-type" tabindex="-1"><a class="header-anchor" href="#addjobtype-gives-one-scheduler-its-own-build-of-a-job-type"><span><code>AddJobType</code> gives one scheduler its own build of a job type</span></a></h3><p>New in 4.0, and only interesting when a container holds more than one scheduler. <code>AddJob&lt;T&gt;</code>&#39;s registration is unkeyed and <code>TryAdd</code>, so under a scheduler per tenant the first registration is what every scheduler gets — different implementations or different lifetimes for one job type were not expressible. <code>AddJobType</code> says the registration belongs to <em>this</em> scheduler:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line">services<span class="token punctuation">.</span><span class="token function">AddQuartz</span><span class="token punctuation">(</span><span class="token string">&quot;acme&quot;</span><span class="token punctuation">,</span> q <span class="token operator">=&gt;</span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    q<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">AddJobType</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>ReportJob<span class="token punctuation">,</span> AcmeReportJob<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>                        <span class="token comment">// a different implementation</span></span>
<span class="line">    q<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">AddJobType</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>AuditJob<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span>ServiceLifetime<span class="token punctuation">.</span>Singleton<span class="token punctuation">)</span><span class="token punctuation">;</span>               <span class="token comment">// a different lifetime</span></span>
<span class="line">    q<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">AddJobType</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>ExportJob<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span>sp <span class="token operator">=&gt;</span> <span class="token keyword">new</span> <span class="token constructor-invocation class-name">ExportJob</span><span class="token punctuation">(</span>sp<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">GetRequiredKeyedService</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>IExportSink<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span><span class="token string">&quot;acme&quot;</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">    q<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">AddJob</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>ReportJob<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span>j <span class="token operator">=&gt;</span> j<span class="token punctuation">.</span><span class="token function">WithIdentity</span><span class="token punctuation">(</span><span class="token string">&quot;report&quot;</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"><span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The registration is made under the scheduler&#39;s service key, or unkeyed for the default scheduler — whose registrations <em>are</em> the unkeyed ones, so an empty key would not be the same thing. The job factory looks there first and falls back to the container&#39;s unkeyed registration, so a scheduler that was given nothing of its own resolves exactly as before, and the default scheduler resolves in one lookup as it always has. Nothing about a single-scheduler application changes.</p><p>The lifetime defaults to <code>ServiceLifetime.Scoped</code> — the lifetime the job factory is built around — and is named through an overload rather than an optional parameter. That is not a style choice: a default value that is an enum from an assembly which only ships in a shared framework is a metadata constant whose type coverlet&#39;s Cecil resolver cannot resolve, and it silently drops the <em>entire</em> containing assembly from the coverage report.</p><h2 id="naming-a-job-type-by-string-says-so-under-trimming" tabindex="-1"><a class="header-anchor" href="#naming-a-job-type-by-string-says-so-under-trimming"><span>Naming a job type by string says so under trimming</span></a></h2><p><code>Quartz</code> is marked trimmable in 4.0, and the job-type APIs say what they need. Nothing here is a source or binary break — the attributes are annotations — but an application published with <code>PublishTrimmed</code> now gets told things 3.x never mentioned.</p><p><strong>The typed spelling carries the requirement.</strong> <code>JobBuilder.Create&lt;T&gt;()</code>, <code>JobBuilder&lt;TJob&gt;.OfType&lt;T&gt;()</code> and <code>OfType(Type)</code>, <code>AddJob&lt;T&gt;()</code>, <code>AddJob(Type, …)</code>, <code>AddJobType&lt;T&gt;()</code>, <code>AddJobType&lt;TJob, TImpl&gt;()</code>, <code>AddTrigger&lt;TJob&gt;()</code>, <code>ScheduleJob&lt;T&gt;()</code>, <code>TriggerBuilder.Create&lt;TJob&gt;()</code>, <code>new JobType(Type)</code> and the implicit <code>Type</code> → <code>JobType</code> conversion all declare <code>[DynamicallyAccessedMembers(PublicConstructors | PublicProperties | Interfaces)]</code> on the job type — what a job factory constructs, what a <code>JobDataMap</code> binds onto, and where <code>[DisallowConcurrentExecution]</code> may be inherited from. The generic <code>JobBuilder&lt;TJob&gt;</code>, <code>TriggerBuilder&lt;TJob&gt;</code>, <code>IJobConfigurator&lt;TJob&gt;</code> and <code>ITriggerConfigurator&lt;TJob&gt;</code> declare the same on their type parameter. <code>PublicMethods</code>, which some of these declared in earlier 4.0 previews, is gone: a kept property keeps its accessors, and <code>IJob.Execute</code> is reached through the interface.</p><p>If you pass a <code>Type</code> variable rather than a <code>typeof</code> or a generic argument, the compiler now asks the same of <em>your</em> code, which is the annotation doing its job:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> static IJobDetail Build(Type jobType) =&gt; JobBuilder.Create().OfType(jobType).Build();</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> static IJobDetail Build(</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">     [DynamicallyAccessedMembers(</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">         DynamicallyAccessedMemberTypes.PublicConstructors</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">         | DynamicallyAccessedMemberTypes.PublicProperties</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">         | DynamicallyAccessedMemberTypes.Interfaces)] Type jobType)</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">     =&gt; JobBuilder.Create().OfType(jobType).Build();</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div>`,162),e(`p`,null,[l[151]||=t(`<strong>The name-taking spelling is <code>[RequiresUnreferencedCode]</code>.</strong> The <code>JobType(string)</code> constructor and the explicit <code>string</code> → <code>JobType</code> cast report <code>IL2026</code> in a trimmed build, because a type resolved from a string cannot be proven reachable. So does the <code>job_scheduling_data</code> XML loader. Prefer the typed spelling, or root the type yourself with a <a href="https://learn.microsoft.com/dotnet/core/deploying/trimming/trimming-options#root-descriptors" target="_blank" rel="noopener noreferrer">trimmer root descriptor</a>. See `,14),c(g,{to:`/documentation/quartz-4.x/how-tos/trimming-and-native-aot.html`},{default:i(()=>[...l[150]||=[r(`Publishing Trimmed and Native AOT`,-1)]]),_:1}),l[152]||=r(` for what a trimmed application has to do.`,-1)]),l[862]||=t(`<p><code>DbMetadata.ConnectionType</code>, <code>.CommandType</code> and <code>.ParameterType</code> carry annotations too, saying what <code>DbProvider</code> does with each: constructs connections and commands, and reads the properties the metadata names. A <code>UseGenericDatabase</code> callback that sets them from <code>typeof(...)</code> already satisfies that.</p><p><strong>One behaviour change came with it.</strong> An ADO.NET store&#39;s trigger acquisition decided <code>[DisallowConcurrentExecution]</code> with a check that did not look at interfaces, while <code>IJobDetail.ConcurrentExecutionDisallowed</code> did. A job that inherited the attribute from an interface was therefore serialized when it fired but not when it was acquired, so a batch could hold two of its triggers and the second was released again. Both now ask the same question.</p><h3 id="a-named-type-is-checked-against-ijob-before-it-is-constructed" tabindex="-1"><a class="header-anchor" href="#a-named-type-is-checked-against-ijob-before-it-is-constructed"><span>A named type is checked against <code>IJob</code> before it is constructed</span></a></h3><p>A job type given as a <code>Type</code> — <code>AddJob(Type)</code>, <code>new JobType(Type)</code> — was always checked for <code>IJob</code>. A job type given as a <em>name</em> was not, and the first thing that established it was a job at all was the cast that followed construction: the container was asked for the type (and <code>GetService</code> constructs whatever is registered for it), then <code>ActivatorUtilities</code>, so the type&#39;s static constructor, module initializer and instance constructor had all run — with the scheduler scope&#39;s services injected — before anything refused.</p><p>The check now runs first, in the job factory, in the activator cache and in <code>TypeActivator</code>. A firing of a job whose type is not an <code>IJob</code> fails with a <code>JobInstantiationException</code> naming the type instead of an <code>InvalidCastException</code>, and nothing of that type runs. If you catch <code>InvalidCastException</code> around a firing for this reason, catch <code>SchedulerException</code> instead.</p><h2 id="a-job-type-rename-is-declared-as-a-map" tabindex="-1"><a class="header-anchor" href="#a-job-type-rename-is-declared-as-a-map"><span>A job type rename is declared as a map</span></a></h2><p>A persistent job store keeps the job&#39;s type as text, in <code>QRTZ_JOB_DETAILS.JOB_CLASS_NAME</code>. Renaming the class, moving its namespace or moving it to another assembly therefore breaks every row that names it, and on 3.x the supported answer was an <code>ITypeLoadHelper</code> of your own: a class to write, register and keep, for a one-line fact.</p><p>4.0 lets the fact be stated as a fact. <code>TypeLoaderOptions.Aliases</code> maps the name as it was stored to the name the type has now, and <code>UseTypeLoader(configure)</code> is the typed way to add one:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line">services<span class="token punctuation">.</span><span class="token function">AddQuartz</span><span class="token punctuation">(</span>q <span class="token operator">=&gt;</span> q<span class="token punctuation">.</span><span class="token function">UseTypeLoader</span><span class="token punctuation">(</span>loader <span class="token operator">=&gt;</span></span>
<span class="line">    loader<span class="token punctuation">.</span><span class="token function">Map</span><span class="token punctuation">(</span><span class="token string">&quot;Acme.Jobs.NightlyReport, Acme.Jobs&quot;</span><span class="token punctuation">,</span> <span class="token keyword">typeof</span><span class="token punctuation">(</span><span class="token type-expression class-name">NightlyRollupJob</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>The same map binds from configuration, so the rename can ship in <code>appsettings.json</code> with the deployment that performs it:</p><div class="language-json line-numbers-mode" data-highlighter="prismjs" data-ext="json"><pre><code class="language-json"><span class="line"><span class="token punctuation">{</span></span>
<span class="line">  <span class="token property">&quot;Quartz&quot;</span><span class="token operator">:</span> <span class="token punctuation">{</span></span>
<span class="line">    <span class="token property">&quot;TypeLoader&quot;</span><span class="token operator">:</span> <span class="token punctuation">{</span></span>
<span class="line">      <span class="token property">&quot;Aliases&quot;</span><span class="token operator">:</span> <span class="token punctuation">{</span></span>
<span class="line">        <span class="token property">&quot;Acme.Jobs.NightlyReport, Acme.Jobs&quot;</span><span class="token operator">:</span> <span class="token string">&quot;Acme.Jobs.NightlyRollupJob, Acme.Jobs&quot;</span></span>
<span class="line">      <span class="token punctuation">}</span></span>
<span class="line">    <span class="token punctuation">}</span></span>
<span class="line">  <span class="token punctuation">}</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The map is read by the loader Quartz ships, which is the one place a type name is resolved at run time — so it applies to <code>JOB_CLASS_NAME</code>, to jobs named in XML or JSON scheduling data, and to the <code>quartz.plugin.&lt;name&gt;.type</code> key. The flat keys naming a scheduler&#39;s own components are read while the service collection is still being built, before any options exist, and are not aliased. Keys are compared the way that loader already compares its own table of Quartz&#39;s 3.x → 4.0 renames: ordinal, matching either the whole name or the part of it before the comma that starts the assembly.</p>`,12),e(`p`,null,[l[154]||=r(`Two things are deliberate. An alias whose target names no loadable type `,-1),l[155]||=e(`strong`,null,`fails options validation at startup`,-1),l[156]||=r(`, naming both halves of the entry, rather than surfacing later as a `,-1),l[157]||=e(`code`,null,`TypeLoadException`,-1),l[158]||=r(` that names the dead name and nothing about the mapping meant to save it. And nothing is ever written back: a job read under an aliased name still has the old spelling in its row, which is what makes an alias safe during a rolling deployment — the nodes still running the old build keep writing the old name while the new ones read it. Retiring an alias is still the SQL `,-1),l[159]||=e(`code`,null,`UPDATE`,-1),l[160]||=r(` in `,-1),c(g,{to:`/documentation/troubleshooting.html#job-deserialization-failures-after-refactoring`},{default:i(()=>[...l[153]||=[r(`Job deserialization failures after refactoring`,-1)]]),_:1}),l[161]||=r(`, run once nothing is hitting it any more.`,-1)]),l[863]||=t(`<p><code>UseTypeLoader&lt;T&gt;()</code> is unchanged and still replaces the loader outright, for a scheme rather than a list — a name resolved out of a plugin&#39;s <code>AssemblyLoadContext</code>, say. Replacing the loader takes the map with it, since the map is the shipped loader&#39;s.</p><h2 id="the-job-scope-is-prepared-without-writing-a-job-factory" tabindex="-1"><a class="header-anchor" href="#the-job-scope-is-prepared-without-writing-a-job-factory"><span>The job scope is prepared without writing a job factory</span></a></h2><p><code>ConfigureScope</code> — the hook that prepares the dependency injection scope a job is built in, and the place an <code>AsyncLocal</code> is set for the job&#39;s dependencies to read — was reachable only by deriving from <code>MicrosoftDependencyInjectionJobFactory</code>, overriding a protected method, and registering the derived factory. That is a class and a registration to set one ambient value:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> public sealed class TenantJobFactory : MicrosoftDependencyInjectionJobFactory</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"> {</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line">     public TenantJobFactory(IServiceProvider serviceProvider) : base(serviceProvider) { }</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"></span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line">     protected override void ConfigureScope(IServiceScope scope, TriggerFiredBundle bundle, IScheduler scheduler)</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line">         =&gt; Tenant.Current.Value = bundle.JobDetail.JobDataMap.GetString(&quot;tenant&quot;);</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"> }</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"> services.AddQuartz(q =&gt; q.UseJobFactory&lt;TenantJobFactory&gt;());</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> services.AddQuartz(q =&gt; q.ConfigureJobScope(</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">     (scope, bundle, scheduler) =&gt; Tenant.Current.Value = bundle.JobDetail.JobDataMap.GetString(&quot;tenant&quot;)));</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The contract is the one the virtual method always had: it runs before the job is resolved, and it is synchronous so that an <code>AsyncLocal</code> set in it survives into <code>Execute</code>. Callbacks combine rather than replace, and the same delegate is <code>JobFactoryOptions.ConfigureScope</code>, which is per-scheduler like every other component&#39;s options. Overriding the protected method still works and still takes the delegate&#39;s place if the override does not call base.</p><h3 id="the-firing-can-be-read-without-being-handed-it" tabindex="-1"><a class="header-anchor" href="#the-firing-can-be-read-without-being-handed-it"><span>The firing can be read without being handed it</span></a></h3><p>New in 4.0: <code>IJobExecutionContextAccessor</code>, registered by <code>AddQuartz</code> as a singleton, is the firing the calling code is part of. It is for code that is not the job and cannot be handed an <code>IJobExecutionContext</code> — a scoped service, a logging enricher, a repository three calls below <code>Execute</code>. 3.x had no such thing, so an application that needed one wrote its own <code>AsyncLocal</code> and set it from a job factory:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> public static class Tenant { public static readonly AsyncLocal&lt;string?&gt; Current = new(); }</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"> services.AddQuartz(q =&gt; q.ConfigureJobScope(</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line">     (scope, bundle, scheduler) =&gt; Tenant.Current.Value = bundle.Trigger.Key.Group));</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> public sealed class TenantConnectionFactory(IJobExecutionContextAccessor accessor)</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line"> {</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">     public string ConnectionString =&gt; connectionStrings[accessor.Current!.Trigger.Key.Group];</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line"> }</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>Current</code> is set from the moment the execution context exists — before the trigger and job listeners are notified — until the job has been returned to the job factory, and is <code>null</code> at every other time, including on the scheduling thread. It travels with the <code>ExecutionContext</code>, so it survives <code>await</code> and is captured by <code>Task.Run</code>, and it can never be another firing&#39;s. Work a job leaves running past the end of its execution reads <code>null</code> rather than the finished context, since by then the job&#39;s scope has been disposed. There is no setter.</p><p>It does <strong>not</strong> replace <code>ConfigureJobScope</code>. The execution context takes the job instance, so it does not exist while the job is being constructed: anything that needs the tenant <em>at construction time</em> still gets it from the hook.</p><h2 id="a-job-can-take-a-typed-input" tabindex="-1"><a class="header-anchor" href="#a-job-can-take-a-typed-input"><span>A job can take a typed input</span></a></h2><p>New in 4.0, and purely additive: <code>IJob&lt;TInput&gt;</code> lets a job declare the type of the payload it is scheduled with, instead of digging it out of a stringly <code>JobDataMap</code>. 3.x had no such thing, so every framework that embedded Quartz wrote the same adapter job and payload-carrying context by hand.</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> public sealed class SendEmailJob : IJob</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"> {</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line">     public ValueTask Execute(IJobExecutionContext context, CancellationToken cancellationToken = default)</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line">     {</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line">         SendEmail input = JsonSerializer.Deserialize&lt;SendEmail&gt;(context.MergedJobDataMap.GetString(&quot;payload&quot;)!)!;</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line">         // ...</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line">     }</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"> }</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"> trigger.UsingJobData(&quot;payload&quot;, JsonSerializer.Serialize(new SendEmail(to, subject)));</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> public sealed class SendEmailJob : IJob&lt;SendEmail&gt;</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line"> {</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">     public ValueTask Execute(IJobExecutionContext context, SendEmail input, CancellationToken cancellationToken = default)</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">     {</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">         // ...</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">     }</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line"> }</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line"> trigger.UsingInput(new SendEmail(to, subject));</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The whole of the dispatch is <code>IJob&lt;TInput&gt;</code>&#39;s default implementation of <code>IJob.Execute</code>, so the job factory, <code>JobRunShell</code> and the listeners still see an <code>IJob</code> and nothing closes a generic method over a runtime type — which is what keeps this working in a trimmed or native-AOT publish.</p><p><strong>The API added.</strong> In <code>Quartz</code>: <code>IJob&lt;TInput&gt;</code>; <code>SchedulerConstants.JobInput</code> (<code>&quot;QRTZ_JOB_INPUT&quot;</code>); <code>JobExecutionContextInputExtensions.GetInput&lt;TInput&gt;(this IJobExecutionContext)</code> and <code>TryGetInput&lt;TInput&gt;(this IJobExecutionContext, out TInput?)</code>; and <code>JobInputBuilderExtensions.UsingInput&lt;TJob, TInput&gt;</code> on <code>JobBuilder&lt;TJob&gt;</code>, <code>TriggerBuilder&lt;TJob&gt;</code>, <code>IJobConfigurator&lt;TJob&gt;</code> and <code>ITriggerConfigurator&lt;TJob&gt;</code>. In <code>Quartz.Extensibility</code>: <code>IJobInputSerializer</code>. In <code>Quartz.Impl</code>: <code>SystemTextJsonJobInputSerializer</code>, and a fourth optional <code>IJobInputSerializer? inputSerializer = null</code> parameter on the <code>JobExecutionContextImpl</code> constructor — the one existing signature that moved, and source-compatible because it is optional.</p><p><strong>Where the payload lives.</strong> The value goes into the ordinary <code>JobDataMap</code> under <code>SchedulerConstants.JobInput</code>, and the scheduler serializes it to a <strong>string</strong> as the job or trigger is stored — so it survives <code>StoreJobDataAsStrings</code>, the System.Text.Json write gate, the Newtonsoft serializer, the binary blob column and the HTTP wire without any of them knowing about it. A trigger&#39;s input overrides a job&#39;s, which is the <code>MergedJobDataMap</code> rule everything else follows.</p><p><code>UsingInput</code> is offered only on a builder for a job that declares an input; it is an extension method rather than a builder member because a method cannot constrain its own type&#39;s type parameter (CS0699). <code>GetInput&lt;TInput&gt;()</code> is an extension on <code>IJobExecutionContext</code> rather than a member for the same kind of reason: a member would break every hand-written context in the wild.</p><p><strong>Two things to know.</strong> The input type is inferred from the argument&#39;s static type, so pass the type argument explicitly — <code>UsingInput&lt;SendEmailJob, SendEmail&gt;(payload)</code> — when the payload is held as a base type. And a <code>[PersistJobDataAfterExecution]</code> job whose <em>detail</em> carries the input writes it back after every firing; that is harmless, since it is already the string it will be read as, but an input that differs per firing belongs on the trigger.</p><p><strong>Converting a job over a store that predates the input.</strong> An <code>IJob&lt;TInput&gt;</code> fails a firing that carries no input, by name, rather than running it with a default payload — right for a fresh application, and a trap for one whose store already holds triggers written by its 3.x self, whose payload is spread over flat <code>JobDataMap</code> keys with nothing under <code>QRTZ_JOB_INPUT</code>. A job that has to serve both shapes while those triggers drain stays an <code>IJob</code> and reads them apart with <code>TryGetInput</code>:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token keyword">public</span> <span class="token return-type class-name">ValueTask</span> <span class="token function">Execute</span><span class="token punctuation">(</span><span class="token class-name">IJobExecutionContext</span> context<span class="token punctuation">,</span> <span class="token class-name">CancellationToken</span> cancellationToken <span class="token operator">=</span> <span class="token keyword">default</span><span class="token punctuation">)</span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    <span class="token class-name">SendEmail</span> input <span class="token operator">=</span> context<span class="token punctuation">.</span><span class="token function">TryGetInput</span><span class="token punctuation">(</span><span class="token keyword">out</span> <span class="token class-name">SendEmail<span class="token punctuation">?</span></span> typed<span class="token punctuation">)</span> <span class="token operator">&amp;&amp;</span> typed <span class="token keyword">is</span> <span class="token keyword">not</span> <span class="token keyword">null</span></span>
<span class="line">        <span class="token punctuation">?</span> typed</span>
<span class="line">        <span class="token punctuation">:</span> <span class="token keyword">new</span> <span class="token constructor-invocation class-name">SendEmail</span><span class="token punctuation">(</span>context<span class="token punctuation">.</span>MergedJobDataMap<span class="token punctuation">.</span><span class="token function">GetString</span><span class="token punctuation">(</span><span class="token string">&quot;to&quot;</span><span class="token punctuation">)</span><span class="token operator">!</span><span class="token punctuation">,</span> context<span class="token punctuation">.</span>MergedJobDataMap<span class="token punctuation">.</span><span class="token function">GetString</span><span class="token punctuation">(</span><span class="token string">&quot;subject&quot;</span><span class="token punctuation">)</span><span class="token operator">!</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line">    <span class="token comment">// ...</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>TryGetInput</code> answers <code>false</code> only when the key is absent; a value that is present and unreadable still throws, because corruption is not compatibility. <code>GetInput&lt;TInput&gt;()</code> cannot tell the two apart — a payload that read back as <code>null</code> and no payload at all are the same answer there.</p>`,21),e(`p`,null,[l[163]||=e(`strong`,null,`Serialization.`,-1),l[164]||=r(),l[165]||=e(`code`,null,`IJobInputSerializer`,-1),l[166]||=r(` is registered per scheduler, keyed by name like every other component, and defaults to `,-1),l[167]||=e(`code`,null,`SystemTextJsonJobInputSerializer`,-1),l[168]||=r(` built from the same `,-1),l[169]||=e(`code`,null,`SystemTextJsonSerializerRegistry`,-1),l[170]||=r(` as the store's `,-1),l[171]||=e(`code`,null,`IObjectSerializer`,-1),l[172]||=r(`. An application published trimmed or natively declares its payload types with `,-1),l[173]||=e(`code`,null,`SystemTextJsonSerializerRegistry.AddTypeInfoResolver`,-1),l[174]||=r(`, exactly as it declares a job data value type — there is no second registration to learn. See `,-1),c(g,{to:`/documentation/quartz-4.x/tutorial/job-data-map.html#a-typed-input-the-third-read-side`},{default:i(()=>[...l[162]||=[r(`Job Data`,-1)]]),_:1}),l[175]||=r(`.`,-1)]),l[864]||=t(`<h2 id="scheduling-a-trigger-can-replace-one-atomically" tabindex="-1"><a class="header-anchor" href="#scheduling-a-trigger-can-replace-one-atomically"><span>Scheduling a trigger can replace one atomically</span></a></h2><p>New in 4.0, and purely additive: <code>IScheduler.ScheduleJob</code> has two more overloads, which take a <code>ScheduleJobOptions</code> and can therefore replace what is already stored.</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token return-type class-name">ValueTask<span class="token punctuation">&lt;</span>DateTimeOffset<span class="token punctuation">&gt;</span></span> <span class="token function">ScheduleJob</span><span class="token punctuation">(</span><span class="token class-name">ITrigger</span> trigger<span class="token punctuation">,</span> <span class="token class-name">ScheduleJobOptions</span> options<span class="token punctuation">,</span> <span class="token class-name">CancellationToken</span> cancellationToken <span class="token operator">=</span> <span class="token keyword">default</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"><span class="token return-type class-name">ValueTask<span class="token punctuation">&lt;</span>DateTimeOffset<span class="token punctuation">&gt;</span></span> <span class="token function">ScheduleJob</span><span class="token punctuation">(</span><span class="token class-name">IJobDetail</span> jobDetail<span class="token punctuation">,</span> <span class="token class-name">ITrigger</span> trigger<span class="token punctuation">,</span> <span class="token class-name">ScheduleJobOptions</span> options<span class="token punctuation">,</span> <span class="token class-name">CancellationToken</span> cancellationToken <span class="token operator">=</span> <span class="token keyword">default</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>On 3.x — and on the two existing overloads, which are unchanged — rescheduling under a key you may or may not already hold meant three round trips and a window:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> if (await scheduler.CheckExists(trigger.Key, cancellationToken))</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"> {</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line">     await scheduler.UnscheduleJob(trigger.Key, cancellationToken);</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"> }</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"> await scheduler.ScheduleJob(trigger, cancellationToken);</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> await scheduler.ScheduleJob(trigger, new ScheduleJobOptions { Replace = true }, cancellationToken);</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The new form is one store operation under the store&#39;s own lock — <code>SchedulerLock.TriggerAccess</code> on an ADO store, the single lock on the in-memory one — so two nodes doing it at once cannot interleave into a schedule with the trigger deleted and not replaced. The job-and-trigger overload goes through the store&#39;s <code>ScheduleJobs</code>, so the job and its trigger are written together rather than under two locks.</p><p><code>options</code> deliberately has <strong>no default</strong> on either overload. Giving it one would make <code>scheduler.ScheduleJob(trigger)</code> and <code>scheduler.ScheduleJob(job, trigger)</code> ambiguous between the pair.</p><p><strong>A replaced trigger keeps its previous fire time.</strong> The ADO store has done this since #1834; the in-memory store cloned the incoming trigger and lost it, so the same code reported &quot;never fired&quot; on one store and a real time on the other. The in-memory store now carries the value over, and <code>JobStoreContractTest</code> pins it on every store — along with the rule that a trigger replaced into a paused group is still born paused, so an upsert is not a way to escape a pause. A caller who <em>means</em> to reset the history sets <code>PreviousFireTimeUtc</code> on the incoming trigger, which is honoured.</p><p><strong>Over HTTP</strong>, <code>POST /triggers/schedule</code> takes a <code>replace</code> flag, <code>HttpScheduler</code> implements both new overloads, and the flag reaches the scheduler unchanged. A request that omits it behaves exactly as before.</p><h2 id="a-scheduler-says-which-clock-it-keeps" tabindex="-1"><a class="header-anchor" href="#a-scheduler-says-which-clock-it-keeps"><span>A scheduler says which clock it keeps</span></a></h2><p>New in 4.0: <code>IScheduler.TimeProvider</code>, a <strong>default interface member</strong> answering <code>TimeProvider.System</code>. Nothing has to implement it — a scheduler written outside this repository keeps compiling and reports what its triggers would have used anyway — and the schedulers Quartz ships answer better:</p><table><thead><tr><th>Scheduler</th><th>Answers</th></tr></thead><tbody><tr><td>the local scheduler</td><td>the <code>TimeProvider</code> it was configured with</td></tr><tr><td><code>DelegatingScheduler</code></td><td>whatever it decorates</td></tr><tr><td>the deferred scheduler <code>AddQuartz</code> registers</td><td>the built scheduler&#39;s, or the system clock while there is no scheduler yet</td></tr><tr><td><code>HttpScheduler</code></td><td>the system clock — the clock that decides when a trigger fires is the remote scheduler&#39;s, and a <code>TimeProvider</code> cannot be fetched over a wire</td></tr></tbody></table><p>3.x had no way to ask. Code that builds a trigger <em>for</em> a scheduler had to reach for <code>DateTimeOffset.UtcNow</code>, so an application or a test running the scheduler on a clock of its own computed &quot;in ten minutes&quot; against a different clock from the one the scheduling loop compares the answer to:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> ITrigger trigger = TriggerBuilder.Create()</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line">     .StartAt(DateTimeOffset.UtcNow.AddMinutes(10))</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line">     .Build();</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> ITrigger trigger = TriggerBuilder.Create(scheduler.TimeProvider)</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">     .StartAt(scheduler.TimeProvider.GetUtcNow().AddMinutes(10))</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">     .Build();</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>It is also what a job rescheduling itself from inside <code>Execute</code> reads, as <code>context.Scheduler.TimeProvider</code>, and it is what the one-call <code>ScheduleJob&lt;TJob, TInput&gt;</code> overloads below use for their <code>TimeSpan</code> form and for the trigger they build.</p><h2 id="a-typed-job-can-be-scheduled-in-one-call" tabindex="-1"><a class="header-anchor" href="#a-typed-job-can-be-scheduled-in-one-call"><span>A typed job can be scheduled in one call</span></a></h2><p>Also new, on top of <a href="#a-job-can-take-a-typed-input">typed inputs</a>: two extension methods on <code>IScheduler</code> that schedule one firing of a job with a payload and a time, and nothing else to build.</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token return-type class-name">ValueTask<span class="token punctuation">&lt;</span>ScheduledOneOffJob<span class="token punctuation">&gt;</span></span> <span class="token generic-method"><span class="token function">ScheduleJob</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>TJob<span class="token punctuation">,</span> TInput<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span><span class="token keyword">this</span> <span class="token class-name">IScheduler</span> scheduler<span class="token punctuation">,</span> <span class="token class-name">TInput</span> input<span class="token punctuation">,</span> <span class="token class-name">DateTimeOffset</span> at<span class="token punctuation">,</span></span>
<span class="line">    <span class="token class-name">OneOffJobOptions</span> options <span class="token operator">=</span> <span class="token keyword">default</span><span class="token punctuation">,</span> <span class="token class-name">CancellationToken</span> cancellationToken <span class="token operator">=</span> <span class="token keyword">default</span><span class="token punctuation">)</span> <span class="token keyword">where</span> <span class="token class-name">TJob</span> <span class="token punctuation">:</span> <span class="token type-list"><span class="token class-name">IJob<span class="token punctuation">&lt;</span>TInput<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line"><span class="token return-type class-name">ValueTask<span class="token punctuation">&lt;</span>ScheduledOneOffJob<span class="token punctuation">&gt;</span></span> <span class="token generic-method"><span class="token function">ScheduleJob</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>TJob<span class="token punctuation">,</span> TInput<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span><span class="token keyword">this</span> <span class="token class-name">IScheduler</span> scheduler<span class="token punctuation">,</span> <span class="token class-name">TInput</span> input<span class="token punctuation">,</span> <span class="token class-name">TimeSpan</span> delay<span class="token punctuation">,</span></span>
<span class="line">    <span class="token class-name">OneOffJobOptions</span> options <span class="token operator">=</span> <span class="token keyword">default</span><span class="token punctuation">,</span> <span class="token class-name">CancellationToken</span> cancellationToken <span class="token operator">=</span> <span class="token keyword">default</span><span class="token punctuation">)</span> <span class="token keyword">where</span> <span class="token class-name">TJob</span> <span class="token punctuation">:</span> <span class="token type-list"><span class="token class-name">IJob<span class="token punctuation">&lt;</span>TInput<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>They are the imperative twin of <code>IQuartzBuilder.ScheduleJob&lt;TJob&gt;</code>, which only ever existed for the firings an application declares at start-up. 3.x had nothing of the kind.</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token class-name">ScheduledOneOffJob</span> firing <span class="token operator">=</span> <span class="token keyword">await</span> scheduler<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">ScheduleJob</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>SendInvoiceJob<span class="token punctuation">,</span> SendInvoice<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span></span>
<span class="line">    invoice<span class="token punctuation">,</span> TimeSpan<span class="token punctuation">.</span><span class="token function">FromDays</span><span class="token punctuation">(</span><span class="token number">7</span><span class="token punctuation">)</span><span class="token punctuation">,</span></span>
<span class="line">    OneOffJobOptions<span class="token punctuation">.</span><span class="token function">Replacing</span><span class="token punctuation">(</span><span class="token interpolation-string"><span class="token string">$&quot;invoice-</span><span class="token interpolation"><span class="token punctuation">{</span><span class="token expression language-csharp">invoice<span class="token punctuation">.</span>CustomerId</span><span class="token punctuation">}</span></span><span class="token string">&quot;</span></span><span class="token punctuation">)</span> <span class="token keyword">with</span> <span class="token punctuation">{</span> Group <span class="token operator">=</span> invoice<span class="token punctuation">.</span>CustomerId <span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line">logger<span class="token punctuation">.</span><span class="token function">LogInformation</span><span class="token punctuation">(</span><span class="token string">&quot;Reminder {Trigger} scheduled for {At}&quot;</span><span class="token punctuation">,</span> firing<span class="token punctuation">.</span>TriggerKey<span class="token punctuation">,</span> firing<span class="token punctuation">.</span>FirstFireTimeUtc<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line"><span class="token keyword">await</span> scheduler<span class="token punctuation">.</span><span class="token function">UnscheduleJob</span><span class="token punctuation">(</span>firing<span class="token punctuation">.</span>TriggerKey<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><strong>What the call answers with</strong> is <code>ScheduledOneOffJob</code>, a two-member <code>readonly record struct</code>: the <code>TriggerKey</code> of the firing — the handle to cancel it with, or to replace it by scheduling the same name again — and the <code>FirstFireTimeUtc</code> the store computed, which is what the <code>ScheduleJob(trigger, options)</code> overload it wraps returns and what a caller logging &quot;scheduled for X&quot; would otherwise have to infer from the time it asked for. It stays two members: everything else about the firing is a property of the trigger the key names.</p><p><strong>One durable job per job type, many triggers.</strong> The job is stored once under <code>SchedulerConstants.ScheduledJobKey&lt;TJob&gt;()</code>, which is <code>(typeof(TJob).Name, SchedulerConstants.ScheduledJobGroup)</code> — a new reserved constant spelled <code>&quot;QRTZ_SCHEDULED&quot;</code> — and every call adds a trigger to it. That is the shape a message bus&#39;s Quartz integration converges on: a scheduled message is a trigger, so there is no job churn to pay for. The job is ensured with <code>AddJob</code> and <code>Replace</code>, which is idempotent and cluster-safe, and remembered per scheduler instance; a store that reports the job missing gets it put back and the firing retried once, so a cluster restore or an operator&#39;s delete does not turn into a permanent failure. The key is public because an integration with a second scheduling path — a recurring trigger it builds itself for the same job — has to point that trigger at the same job rather than add one of its own to a group it is told to stay out of.</p><p><strong><code>OneOffJobOptions</code> is named for what the call creates</strong>, not for the call: one off, one firing, one trigger. It carries what would otherwise be <code>TriggerBuilder</code> calls — <code>Name</code>, <code>Group</code>, <code>Description</code>, <code>Priority</code>, <code>ExecutionGroup</code>, <code>MisfireInstruction</code> — plus <code>Replace</code>, which it passes on to the store, and <code>RequestRecovery</code>, the one member that describes the ensured <em>job</em>: set it and that job is marked <code>RequestsRecovery</code>, so a firing interrupted by a hard shutdown is re-executed when the scheduler comes back. Because the job is ensured once per scheduler instance, the first call&#39;s value wins for the lifetime of the process — which is why it is a named boolean rather than a configuration delegate that would look as though it varied per call. Every member is optional, and <code>default</code> is &quot;a one-shot trigger with a generated name, in this job type&#39;s own group&quot;. An overload that schedules a <em>recurring</em> job would have something else to say — a schedule, an end time, a calendar — and will get an options type of its own rather than nullable members here that mean nothing to a single firing. It is a different thing from <code>ScheduleJobOptions</code>, which describes a <em>store</em> operation and says only whether it may over-write.</p><p><code>OneOffJobOptions.Replacing(name)</code> is its preset, and it is the one in the family that takes an argument. <code>AddJobOptions.Replacing</code> and its siblings are properties because <code>Replace = true</code> is the whole of what they say; here <code>Replace</code> alone would do nothing, because a firing with no name of its own is given a generated one and a generated name has nothing to replace. The name is what makes replacing mean something, so it is a parameter rather than something to remember to set afterwards. <code>AddJobOptions</code> gained the preset for its <em>second</em> flag at the same time — <code>AddJobOptions.ReplacingAndStoringNonDurable</code>, the twin of <code>AddCalendarOptions.ReplacingAndUpdatingTriggers</code>.</p><p><code>Name</code> and <code>Group</code> are two loose strings rather than a <code>TriggerKey</code>, and that is deliberate: the two halves have different defaults — a generated name, a group named after the job type — and each is worth setting without the other. &quot;Call it <code>order-42</code>, wherever such firings go&quot; and &quot;put it in this saga&#39;s group, any name will do&quot; are both ordinary, and a key can express neither. The key exists once the trigger does, and the call answers with it.</p><p><code>SchedulerConstants.ScheduledJobKey&lt;TJob&gt;()</code> is where the durable job&#39;s key is spelled. In the 4.0 previews it was <code>SchedulerJobExtensions.ScheduledJobKey&lt;TJob&gt;()</code> — a static that took no receiver, sitting in a class of extension methods, so every call site had to name the class anyway. It moved to sit beside <code>SchedulerConstants.ScheduledJobGroup</code>, which is the other half of the same convention.</p><p>The trigger group is the correlation axis: everything scheduled for one saga, one tenant or one conversation can share a group and be listed, paused or unscheduled together. Cancelling is <code>UnscheduleJob(key)</code>; the durable job stays behind, one row per job type whatever the traffic. Mind the default when adopting these overloads inside something that already has a trigger-key contract: <code>Group</code> defaults to the job type&#39;s name, so an integration whose callers cancel with <code>new TriggerKey(id)</code> — the <em>default</em> group — must say <code>Group = TriggerKey.DefaultGroup</code> or its cancellation silently stops matching.</p>`,27),e(`p`,null,[l[177]||=r(`See `,-1),c(g,{to:`/documentation/quartz-4.x/how-tos/one-off-job.html#a-payload-and-a-time-in-one-call`},{default:i(()=>[...l[176]||=[r(`One-Off Job`,-1)]]),_:1}),l[178]||=r(`.`,-1)]),l[865]||=t(`<h2 id="a-scheduled-job-links-back-to-the-trace-that-scheduled-it" tabindex="-1"><a class="header-anchor" href="#a-scheduled-job-links-back-to-the-trace-that-scheduled-it"><span>A scheduled job links back to the trace that scheduled it</span></a></h2><p>New in 4.0, and on by default: when a scheduling call is made inside an <code>Activity</code>, the scheduler records that activity&#39;s W3C trace context on the trigger, and the firing&#39;s <code>Quartz.Job.Execute</code> span carries an <code>ActivityLink</code> back to it. Nothing has to be configured, and no code changes.</p><p>3.x propagated nothing, so every integrator that cared reached into the trigger&#39;s <code>JobDataMap</code> and invented a key of its own:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> trigger.UsingJobData(&quot;traceparent&quot;, Activity.Current?.Id);   // and a job that read it back by hand</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> // nothing — the scheduler writes it, and the execute span links to it</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p><strong>A link, not a parent.</strong> A job scheduled for next Tuesday runs a week after the request that asked for it, quite possibly on another node. Making the firing a <em>child</em> of the scheduling span would produce a trace that stays open for a week, which no backend can render and no operator can read. The firing is its own trace root and the link is how you walk back — the shape OpenTelemetry gives an asynchronous producer and the consumer that eventually picks the work up. The span name is unchanged.</p><p><strong>The API added.</strong> In <code>Quartz</code>: <code>SchedulerConstants.TraceParent</code> (<code>&quot;QRTZ_TRACEPARENT&quot;</code>), <code>SchedulerConstants.TraceState</code> (<code>&quot;QRTZ_TRACESTATE&quot;</code>), and <code>QuartzSchedulerOptions.PropagateTraceContext</code>, which defaults to <code>true</code>. There is no flat <code>quartz.*</code> key for it: nothing on 3.x did this, so there is no configuration to migrate — and <code>LegacyPropertyKeys</code> correctly rejects an invented one.</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line">q<span class="token punctuation">.</span><span class="token function">ConfigureScheduler</span><span class="token punctuation">(</span>options <span class="token operator">=&gt;</span> options<span class="token punctuation">.</span>PropagateTraceContext <span class="token operator">=</span> <span class="token boolean">false</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p><strong>Where it is written.</strong> On the <strong>trigger&#39;s</strong> data map, by the scheduler, at every entry point that stores a trigger — both <code>ScheduleJob</code> overload pairs, <code>ScheduleJobs</code>, both <code>TriggerJob</code> forms and <code>RescheduleJob</code>. A reschedule counts because it is the call that decided when the firing happens, so the link follows the move: a replacement trigger rebuilt from the original carries the original&#39;s data map, reserved keys included, and the new context overwrites the old one rather than being kept beside it.</p><p><code>UpdateTriggerDetails</code> deliberately writes <strong>no</strong> trace context. It changes metadata on a trigger that stays scheduled and keeps its fire times, so the trace that asked for the firing is still the one that scheduled it — re-pointing the link at whoever later edited a description would make it say something false. (It does normalize a typed input in the map it is given, because that is about what a store can hold rather than about what scheduled anything.)</p><p>Two consequences worth knowing:</p><ul><li><strong>Two more entries per trigger row.</strong> They are ordinary string job data, so they travel through <code>StoreJobDataAsStrings</code>, the System.Text.Json write gate, the Newtonsoft serializer, the binary blob column and the HTTP wire without any of them knowing about it — and they are visible in <code>MergedJobDataMap</code>, in the dashboard and in <code>GET /triggers</code>, like every other <code>QRTZ_*</code> reserved key. Turn the option off if that is not wanted.</li><li><strong>The trigger&#39;s map, never the job&#39;s.</strong> <code>[PersistJobDataAfterExecution]</code> writes back only the job&#39;s map, so the two never meet and a persisted job cannot carry a <code>traceparent</code> forward into its next firing.</li></ul><p><strong>A stale context is removed rather than kept.</strong> The scheduler stores the trigger object it was handed rather than a copy of it, so a trigger scheduled once inside a request and again outside one has the key <em>removed</em> on the second call. A stale link reads exactly like a true one, which makes leaving it the worst of the three answers.</p><p><strong>Over HTTP it is free.</strong> <code>POST /jobs/{group}/{name}/trigger</code> and the scheduling endpoints run inside ASP.NET Core&#39;s own server span, so the request&#39;s trace reaches the trigger without <code>Quartz.AspNetCore</code> mentioning tracing at all.</p>`,13),e(`p`,null,[l[180]||=r(`See `,-1),c(g,{to:`/documentation/quartz-4.x/packages/opentelemetry-integration.html#linking-a-firing-to-what-scheduled-it`},{default:i(()=>[...l[179]||=[r(`Observability`,-1)]]),_:1}),l[181]||=r(`.`,-1)]),l[866]||=t(`<h2 id="a-component-of-your-own-is-chosen-the-same-way-a-shipped-one-is" tabindex="-1"><a class="header-anchor" href="#a-component-of-your-own-is-chosen-the-same-way-a-shipped-one-is"><span>A component of your own is chosen the same way a shipped one is</span></a></h2><p>Three seams that had no code-first spelling at all, and one that only worked through a type-name string:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line">q<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">UseJobStore</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>MyJobStore<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>                                   <span class="token comment">// built by the container</span></span>
<span class="line">q<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">UseJobStore</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>MyJobStore<span class="token punctuation">,</span> MyJobStoreOptions<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span>o <span class="token operator">=&gt;</span> o<span class="token punctuation">.</span>X <span class="token operator">=</span> <span class="token number">1</span><span class="token punctuation">)</span><span class="token punctuation">;</span>     <span class="token comment">// with options of its own</span></span>
<span class="line">q<span class="token punctuation">.</span><span class="token function">UseJobStore</span><span class="token punctuation">(</span>provider <span class="token operator">=&gt;</span> <span class="token keyword">new</span> <span class="token constructor-invocation class-name">MyJobStore</span><span class="token punctuation">(</span>provider<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">GetRequiredService</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>ISchedulerSignaler<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line">q<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">UseInstanceIdGenerator</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>HostNameInstanceIdGenerator<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>UseJobStore&lt;T&gt;</code> is the seam for a store that keeps scheduling data somewhere Quartz has never heard of; <code>UseInMemoryStore</code> and <code>UsePersistentStore</code> remain the way to select the stores Quartz ships, since they configure them as well as choose them — and since <a href="#the-ado-net-store-is-a-store-not-a-base-class">those stores are internal</a>, a shipped store is chosen by the call rather than by a type argument. <code>UsePersistentStore&lt;T&gt;(configure)</code> takes a persistent store of your own, and <code>UseJobStore(IJobStore)</code>, which takes a store you built yourself, is unchanged.</p><p><code>UseInstanceIdGenerator&lt;T&gt;()</code> replaces the pair of keys <code>quartz.scheduler.instanceId = AUTO</code> and <code>quartz.scheduler.instanceIdGenerator.type</code>, and it says both: choosing a generator sets <code>GenerateInstanceId</code>, because a generator that was chosen and then never called is configuration that says nothing. Only a clustered scheduler generates an id — the generator is not called otherwise — which is unchanged.</p><p>The <code>&lt;T, TOptions&gt;</code> shapes are sugar over <code>ConfigureOptions&lt;TOptions&gt;</code>: the options are declared as that scheduler&#39;s, so a component that takes <code>IOptions&lt;TOptions&gt;</code> under <code>AddQuartz(&quot;reporting&quot;, …)</code> is handed what was configured for <code>reporting</code> rather than the unnamed instance.</p><h2 id="listener-matchers-are-a-collection" tabindex="-1"><a class="header-anchor" href="#listener-matchers-are-a-collection"><span>Listener matchers are a collection</span></a></h2><p>The builder&#39;s nine listener overloads took <code>params IMatcher&lt;T&gt;[]</code>; they take <code>params IReadOnlyCollection&lt;IMatcher&lt;T&gt;&gt;</code>, which is what <code>IListenerManager.AddJobListener</code> and <code>AddTriggerListener</code> already took. Existing call sites are unaffected — loose arguments still bind, and an array is still a <code>IReadOnlyCollection&lt;T&gt;</code> — and a caller that holds a <code>List&lt;IMatcher&lt;JobKey&gt;&gt;</code> no longer has to call <code>ToArray()</code> on the way in.</p><h2 id="addquartzhttpapi-is-registered-on-the-service-collection" tabindex="-1"><a class="header-anchor" href="#addquartzhttpapi-is-registered-on-the-service-collection"><span><code>AddQuartzHttpApi</code> is registered on the service collection</span></a></h2><p>The HTTP API serves every scheduler in the container through one set of endpoints, so it was never a scheduler&#39;s own setting; it just had nowhere else to be written:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> services.AddQuartz(q =&gt; q.AddQuartzHttpApi());</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> services.AddQuartzHttpApi();</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>The <code>IQuartzBuilder</code> form is gone rather than kept as a synonym: written inside <code>AddQuartz(name, …)</code> it read as that scheduler&#39;s API while configuring everybody&#39;s, and two of them with different <code>ApiPath</code>s were last-writer-wins with nothing to say so. <code>QuartzHttpApiOptions</code> stays singular for the same reason — <code>ApiPath</code> is a property of the process.</p><h2 id="the-route-is-named-where-the-endpoints-are-mapped" tabindex="-1"><a class="header-anchor" href="#the-route-is-named-where-the-endpoints-are-mapped"><span>The route is named where the endpoints are mapped</span></a></h2><p><code>MapQuartzHttpApi</code> and <code>MapQuartzDashboard</code> take a route pattern, which is how the rest of ASP.NET Core reads — <code>MapHealthChecks(&quot;/health&quot;)</code> — and it puts the path with the application&#39;s other routes instead of in a registration callback somewhere else:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> services.AddQuartzHttpApi(options =&gt; options.ApiPath = &quot;/ops/api&quot;);</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"> services.AddQuartzDashboard(options =&gt; options.DashboardPath = &quot;/ops/quartz&quot;);</span>
<span class="line"></span></span><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line"> …</span>
<span class="line"></span></span><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> endpoints.MapQuartzHttpApi();</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"> endpoints.MapQuartzDashboard();</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> endpoints.MapQuartzHttpApi(&quot;/ops/api&quot;);</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line"> endpoints.MapQuartzDashboard(&quot;/ops/quartz&quot;);</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Nothing is removed. <code>QuartzHttpApiOptions.ApiPath</code> and <code>QuartzDashboardOptions.DashboardPath</code> are still there and still the shape to use when the path comes from configuration, and the parameterless overloads read them. When both are given the pattern at the map site wins, being the more specific of the two. A pattern given there is held to the same rule as the option it overrides, and a bad one is an <code>ArgumentException</code> naming the parameter rather than an options-validation failure — by the time the endpoints are mapped, the validators have already run.</p><p>There is no <code>MapQuartzDashboard(existingComponents, pattern)</code>: the dashboard page routes are fixed at <code>/quartz</code> when integrating with an application&#39;s own Blazor root, which is the same reason a custom <code>DashboardPath</code> is rejected in that mode.</p><h2 id="the-http-api-and-the-dashboard-will-not-serve-anonymously-by-accident" tabindex="-1"><a class="header-anchor" href="#the-http-api-and-the-dashboard-will-not-serve-anonymously-by-accident"><span>The HTTP API and the dashboard will not serve anonymously by accident</span></a></h2><p>Both surfaces can schedule a job whose type is a string the request carries, resolved later with <code>Type.GetType</code> — so with <code>Quartz.Jobs</code> on the probing path an endpoint anybody can reach is remote code execution rather than an information leak. 4.0 therefore refuses to start rather than serve one by omission.</p><p>A hosted lifecycle service enumerates the endpoints Quartz mapped, before the server binds its listener, and throws unless every one of them carries authorization metadata or an explicit <code>AllowAnonymous()</code>. A non-null <code>AuthorizationOptions.FallbackPolicy</code> satisfies it too, since that covers every endpoint that states nothing, and authorization on a <code>MapGroup</code> above the mapping counts. Registering the services without mapping anything is left alone. The message names all three fixes:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line">app<span class="token punctuation">.</span><span class="token function">MapQuartzHttpApi</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">RequireAuthorization</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>                 <span class="token comment">// the whole surface</span></span>
<span class="line"><span class="token comment">// or QuartzHttpApiOptions.SchedulerAuthorizationPolicy        // each scheduler on its own</span></span>
<span class="line"><span class="token comment">// or QuartzDashboardOptions.AuthorizationPolicy</span></span>
<span class="line">app<span class="token punctuation">.</span><span class="token function">MapQuartzDashboard</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">AllowAnonymous</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>                     <span class="token comment">// deliberately open</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div>`,21),e(`p`,null,[l[183]||=e(`code`,null,`AllowAnonymous()`,-1),l[184]||=r(` is a supported answer; what is not supported is saying nothing. Quartz authorizes and something else authenticates, so the application still registers the scheme — see `,-1),c(g,{to:`/documentation/quartz-4.x/packages/http-api.html#production-hardening`},{default:i(()=>[...l[182]||=[r(`Production hardening`,-1)]]),_:1}),l[185]||=r(`.`,-1)]),l[867]||=t(`<h3 id="one-builder-covers-the-dashboard-s-pages-and-its-hub" tabindex="-1"><a class="header-anchor" href="#one-builder-covers-the-dashboard-s-pages-and-its-hub"><span>One builder covers the dashboard&#39;s pages and its hub</span></a></h3><p><code>MapQuartzDashboard()</code> returns an <code>IEndpointConventionBuilder</code>, where 3.x returned a <code>RazorComponentsEndpointConventionBuilder</code>. The dashboard is two sets of endpoints — the Blazor pages and the SignalR hub that streams the same data — and the old return type was the pages&#39; builder alone, so <code>MapQuartzDashboard().RequireAuthorization()</code> protected the pages and left the hub open. In the overload taking an existing components builder it reached the host application&#39;s <em>own</em> pages as well. What comes back now is the dashboard&#39;s pages and its hub, and nothing else, so one <code>RequireAuthorization()</code> or <code>AllowAnonymous()</code> is the whole answer.</p><p>Code that called a member specific to <code>RazorComponentsEndpointConventionBuilder</code> on the result — a render-mode call, say — configures that on its own <code>MapRazorComponents</code> and passes that builder to the <code>existingComponents</code> overload instead.</p><h3 id="a-paged-request-is-bounded-by-maxpagesize" tabindex="-1"><a class="header-anchor" href="#a-paged-request-is-bounded-by-maxpagesize"><span>A paged request is bounded by <code>MaxPageSize</code></span></a></h3><p><code>QuartzHttpApiOptions.MaxPageSize</code> is <code>1000</code>, and a <code>take</code> naming a <strong>number</strong> above it is a <code>400</code> naming the cap — <code>?take=2147483647</code> included. <code>?take=all</code> names no number, so it is <em>bounded</em> by the cap rather than refused by it, and reports <code>hasMore</code>.</p><p>That asymmetry is what keeps the 3.x-compatible listings working: <code>GetJobKeys</code> and its neighbours ask for everything whether the answer is three rows or three million, and <code>HttpScheduler</code> turns a truncated answer to one of those into an exception rather than a short list. Set <code>MaxPageSize</code> to <code>0</code> to bound nothing.</p><h3 id="a-500-says-nothing-about-the-server" tabindex="-1"><a class="header-anchor" href="#a-500-says-nothing-about-the-server"><span>A <code>500</code> says nothing about the server</span></a></h3><p>The <code>detail</code> of a <code>500</code> is one fixed sentence — <em>&quot;The scheduler failed to handle the request. The failure is recorded in the server&#39;s log.&quot;</em> — rather than the exception&#39;s message, which for a driver fault names the server, the database, the login or the constraint. The exception&#39;s <em>type</em> was never returned on that path; its message is now withheld too, and both keep being logged. <code>QuartzHttpApiOptions.IncludeStackTraceInProblemDetails</code> — the switch that already says &quot;I am debugging this&quot; — puts the real message back. A client matching on a <code>500</code>&#39;s <code>detail</code> now matches on a constant.</p><h3 id="a-job-s-two-attribute-flags-are-nullable-on-the-wire" tabindex="-1"><a class="header-anchor" href="#a-job-s-two-attribute-flags-are-nullable-on-the-wire"><span>A job&#39;s two attribute flags are nullable on the wire</span></a></h3><p><code>concurrentExecutionDisallowed</code> and <code>persistJobDataAfterExecution</code> are <code>bool?</code> rather than <code>bool</code> in every job body the API reads or writes, and in <code>Quartz.Dashboard.Services.JobDetailDto</code>. Present means the sender stated the flag; absent means &quot;whatever <code>[DisallowConcurrentExecution]</code> and <code>[PersistJobDataAfterExecution]</code> on the type say&quot;, which is what <code>JobBuilder</code> has always done with a flag nobody set.</p><p>The old shape had no way to say &quot;not stated&quot;, and JSON&#39;s default for a missing <code>bool</code> is <code>false</code>, so a well-formed <code>POST …/jobs</code> that simply did not mention concurrency stored a job its author had declared unsafe to run concurrently as safe to. A client that sent both fields is unaffected; one that omitted them now gets the attribute honoured.</p><p>The other half is on the way out: a job whose type the answering process cannot resolve reports both flags as <code>null</code> instead of failing the whole read. <code>GET …/jobs/{group}/{name}</code> used to answer <code>500</code> for such a job — permanently, for every caller — because the projection read an effective value and the lazy getter threw. That is the ordinary heterogeneous-cluster case as much as it is a bad type name.</p><p>Reading them off an <code>IJobDetail</code> in code is unchanged: <code>IJobDetail.ConcurrentExecutionDisallowed</code> is still <code>bool</code>, and now answers <code>false</code> for a type that cannot be resolved rather than throwing <code>InvalidOperationException</code>.</p><h3 id="a-job-type-name-is-never-resolved-by-the-contract-types" tabindex="-1"><a class="header-anchor" href="#a-job-type-name-is-never-resolved-by-the-contract-types"><span>A job type name is never resolved by the contract types</span></a></h3><p><code>JobDetailDto.AsIJobDetail()</code> and <code>JobDetailDto.Create()</code> no longer resolve a job type name, in either direction, and <code>JobBuilder.Build()</code> no longer deduces the two attribute flags — <code>JobDetailImpl</code> does it lazily, when something asks and the type is at hand.</p><p>Two things follow. <code>Quartz.HttpClient</code> can schedule and add jobs whose type only the server has, which <code>packages/http-client.md</code> always promised and beta.1 could not do (<code>InvalidOperationException: Job type … cannot be resolved</code>); and a hostile or compromised server can no longer choose an assembly simple name that a client&#39;s runtime goes looking for. If you were relying on <code>Build()</code> to snapshot the attributes at build time — for instance by building a detail, unloading the assembly and reading the flags — read them before the assembly goes.</p><p>The persistent store does not resolve one outside firing either. A process that cannot load its job classes — the <code>UseThreadPool&lt;ZeroSizeThreadPool&gt;()</code> administration node, a web application that edits schedules without referencing the worker that runs them — reads, pauses, resumes, reschedules, updates, triggers, adds triggers to and deletes jobs on the stored name alone, and both attribute flags come from <code>QRTZ_JOB_DETAILS.IS_NONCONCURRENT</code> and <code>IS_UPDATE_DATA</code> rather than from the type. <code>RescheduleJob</code> is the call that used to fail — <code>JobPersistenceException: Couldn&#39;t replace trigger: Could not load type &#39;Acme.Jobs.MessageCleanupJob, Acme.Jobs&#39;</code> — because it resolved the class in order to answer &quot;may this job run concurrently?&quot;, and the row now answers that. Such a process therefore needs no <code>ITypeLoader</code> of its own, and the placeholder type 3.x required is no longer the way to get there; a placeholder was always a hazard, because whether it carried <code>[DisallowConcurrentExecution]</code> decided whether a replacement trigger was stored <code>WAITING</code> or <code>BLOCKED</code> for a job it was standing in for. Only the firing path resolves a job&#39;s class, and that runs where the job runs.</p><p><code>IScheduler.Interrupt</code> is the one administration operation this does not reach: it is documented as not cluster aware, cancels the tokens of the firings running in the scheduler it is called on, and an administration node runs none. That is a property of the operation rather than of the missing assembly.</p><h2 id="one-shape-per-registration-method" tabindex="-1"><a class="header-anchor" href="#one-shape-per-registration-method"><span>One shape per registration method</span></a></h2><p>The <code>AddJob</code> / <code>AddTrigger</code> / <code>AddCalendar</code> grid had overloads that said the same thing twice, and optional parameters that made the no-argument calls ambiguous. Each method now has one pair of shapes: one taking a configurator, one taking a configurator and the <code>IServiceProvider</code>.</p><table><thead><tr><th>Removed</th><th>Use instead</th></tr></thead><tbody><tr><td><code>AddJob&lt;T&gt;(JobKey?, …)</code>, <code>AddJob(Type, JobKey?, …)</code></td><td><code>WithIdentity(jobKey)</code> inside the configurator</td></tr><tr><td><code>AddJob&lt;T&gt;()</code>, <code>AddJob&lt;T&gt;(JobKey)</code> with no configurator</td><td><code>AddJob&lt;T&gt;(j =&gt; j.WithIdentity(…))</code></td></tr></tbody></table><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line"> var jobKey = new JobKey(&quot;awesome job&quot;, &quot;awesome group&quot;);</span>
<span class="line"></span></span><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> q.AddJob&lt;ExampleJob&gt;(jobKey, j =&gt; j.WithDescription(&quot;my awesome job&quot;));</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> q.AddJob&lt;ExampleJob&gt;(j =&gt; j.WithIdentity(jobKey).WithDescription(&quot;my awesome job&quot;));</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The job type on <code>AddTrigger&lt;TJob&gt;</code> is what lets the trigger&#39;s job data name the job&#39;s properties. A trigger that only points at its job with <code>ForJob</code> has nothing to name, and <code>AddTrigger</code> with no type argument is that call — the 3.x spelling, unchanged.</p><p>The interface these methods extend is <code>IQuartzBuilder</code>, which is what 3.x called <code>IServiceCollectionQuartzConfigurator</code>. The <code>AddQuartz</code> overloads that handed the callback an <code>IServiceProvider</code> alongside it are gone with it: a container built while the container is still being described could only be a second, throwaway one. Ask for the service provider where it is actually used — each registration method has a shape that is given one, at the point where the container really exists:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> services.AddQuartz((q, serviceProvider) =&gt;</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"> {</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line">     var schedule = serviceProvider.GetRequiredService&lt;IOptions&lt;SampleOptions&gt;&gt;().Value.CronSchedule;</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line">     q.AddTrigger(t =&gt; t.WithIdentity(&quot;custom&quot;).ForJob(jobKey).WithCronSchedule(schedule));</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"> });</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> services.AddQuartz(q =&gt;</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line"> {</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">     q.AddTrigger((serviceProvider, t) =&gt; t</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">         .WithIdentity(&quot;custom&quot;)</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">         .ForJob(jobKey)</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">         .WithCronSchedule(serviceProvider.GetRequiredService&lt;IOptions&lt;SampleOptions&gt;&gt;().Value.CronSchedule));</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line"> });</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>AddCalendar</code> takes the same <code>AddCalendarOptions</code> record that <code>IScheduler.AddCalendar</code> does, instead of two adjacent bools whose order was impossible to remember, and its first parameter is <code>name</code> rather than <code>calendarName</code>:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> q.AddCalendar&lt;HolidayCalendar&gt;(&quot;holidays&quot;, replace: true, updateTriggers: true,</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line">     cal =&gt; cal.AddExcludedDay(new DateOnly(2025, 12, 25)));</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> q.AddCalendar&lt;HolidayCalendar&gt;(&quot;holidays&quot;, new AddCalendarOptions { Replace = true, UpdateTriggers = true },</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">     cal =&gt; cal.AddExcludedDay(new DateOnly(2025, 12, 25)));</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Both the options and the configurator are optional in the type-based overload, so <code>q.AddCalendar&lt;HolidayCalendar&gt;(&quot;holidays&quot;)</code> is a valid registration of an empty calendar.</p><h3 id="a-calendar-can-be-built-from-the-container" tabindex="-1"><a class="header-anchor" href="#a-calendar-can-be-built-from-the-container"><span>A calendar can be built from the container</span></a></h3><p>Both generic <code>AddCalendar&lt;T&gt;</code> overloads are constrained <code>where T : ICalendar, new()</code> and construct the calendar themselves, so a calendar that needs a dependency — a holiday list read from a database, a clock, an options snapshot — could not be registered at all. There is a factory overload for that:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line">q<span class="token punctuation">.</span><span class="token function">AddCalendar</span><span class="token punctuation">(</span><span class="token string">&quot;holidays&quot;</span><span class="token punctuation">,</span> serviceProvider <span class="token operator">=&gt;</span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    <span class="token class-name">AnnualCalendar</span> calendar <span class="token operator">=</span> <span class="token keyword">new</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span> TimeZone <span class="token operator">=</span> TimeZoneInfo<span class="token punctuation">.</span>Utc <span class="token punctuation">}</span><span class="token punctuation">;</span></span>
<span class="line">    <span class="token keyword">foreach</span> <span class="token punctuation">(</span><span class="token class-name">MonthDay</span> day <span class="token keyword">in</span> serviceProvider<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">GetRequiredService</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>IHolidayList<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span>Days<span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">{</span></span>
<span class="line">        calendar<span class="token punctuation">.</span><span class="token function">AddExcludedDay</span><span class="token punctuation">(</span>day<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">    <span class="token punctuation">}</span></span>
<span class="line"></span>
<span class="line">    <span class="token keyword">return</span> calendar<span class="token punctuation">;</span></span>
<span class="line"><span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The factory is handed the scheduler-scoped <code>IServiceProvider</code>, so a calendar registered for a named scheduler is given that scheduler&#39;s parts rather than the default scheduler&#39;s, and it runs once, when the scheduler&#39;s content is resolved. <code>AddCalendarOptions</code> is the optional third parameter as it is on the other overloads. It is an <code>IQuartzBuilder</code> extension, so it reads the same inside <code>QuartzSchedulerBuilder.Create(q =&gt; …)</code>.</p><h2 id="plugins-are-registered-like-listeners" tabindex="-1"><a class="header-anchor" href="#plugins-are-registered-like-listeners"><span>Plugins are registered like listeners</span></a></h2><p><code>AddPlugin</code> had four shapes, one of which took the plugin&#39;s name first and the rest of which could not take a name at all. It now has the same three shapes as the listener registrations, each with an optional trailing name:</p><table><thead><tr><th>Shape</th><th>Meaning</th></tr></thead><tbody><tr><td><code>AddPlugin&lt;T&gt;(string? name = null)</code></td><td>the container constructs the plugin</td></tr><tr><td><code>AddPlugin&lt;T&gt;(Func&lt;IServiceProvider, T&gt; factory, string? name = null)</code></td><td>you construct it</td></tr><tr><td><code>AddPlugin&lt;T, TOptions&gt;(Action&lt;TOptions&gt;? configure = null, string? name = null)</code></td><td>it is given options of its own</td></tr></tbody></table><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> q.AddPlugin(&quot;xml&quot;, provider =&gt; new XmlSchedulingDataProcessorPlugin());</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> q.AddPlugin(provider =&gt; new XmlSchedulingDataProcessorPlugin(), &quot;xml&quot;);</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>The name is how the scheduler refers to the plugin and the name a <code>quartz.plugin.&lt;name&gt;.*</code> key configures it under — some plugins derive persisted job and trigger keys from it — so it is part of the deployment&#39;s identity rather than a label. Left unset, the plugin&#39;s type name is used, exactly as before.</p><h3 id="a-plugin-implements-only-what-it-has-to-say" tabindex="-1"><a class="header-anchor" href="#a-plugin-implements-only-what-it-has-to-say"><span>A plugin implements only what it has to say</span></a></h3><p><code>ISchedulerPlugin.Start</code> and <code>ISchedulerPlugin.Shutdown</code> have default implementations that do nothing. Most plugins do all their work in <code>Initialize</code> — attaching a listener, registering a resolver — and had to write two empty members to say so:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line"> public sealed class MyPlugin : ISchedulerPlugin</span>
<span class="line"></span><span class="token prefix unchanged"> </span><span class="token line"> {</span>
<span class="line"></span><span class="token prefix unchanged"> </span><span class="token line">     public ValueTask Initialize(string name, IScheduler scheduler, CancellationToken ct = default) { … }</span>
<span class="line"></span></span><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"></span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line">     public ValueTask Start(CancellationToken ct = default) =&gt; default;</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"></span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line">     public ValueTask Shutdown(CancellationToken ct = default) =&gt; default;</span>
<span class="line"></span></span><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line"> }</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Nothing about the lifecycle changes: the scheduler still calls both, through the interface, at the same two moments. An existing plugin that declares them keeps working and needs no change. The plugins shipped here dropped every such member they had, which is the only visible effect in the public API — a member that did nothing is no longer on the concrete type. Call the plugin through <code>ISchedulerPlugin</code> if you were invoking one of those directly.</p><h3 id="the-two-scheduling-data-plugins-have-one-surface" tabindex="-1"><a class="header-anchor" href="#the-two-scheduling-data-plugins-have-one-surface"><span>The two scheduling-data plugins have one surface</span></a></h3><p><code>XmlSchedulingDataProcessorPlugin</code> and <code>JsonSchedulingDataProcessorPlugin</code> read one schedule out of two file formats, and had drifted into two different types: one published <code>ProcessFile</code> and no <code>Shutdown</code>, the other a do-nothing <code>Shutdown</code> and no <code>ProcessFile</code>, and both published the settings that configure them as get-only properties. Their surface is now the same, and it is the two constructors plus what <code>ISchedulerPlugin</code> and <code>IFileScanListener</code> ask for:</p><table><thead><tr><th>Member</th><th>Was</th><th>Now</th></tr></thead><tbody><tr><td><code>XmlSchedulingDataProcessorPlugin.ProcessFile(string, CancellationToken)</code></td><td>public on the XML plugin, private on the JSON one</td><td>private on both. <code>IFileScanListener.FileUpdated(fileName)</code> says a file changed, and it is what <code>FileScanJob</code> calls — reading a file on demand is that call, made yourself</td></tr><tr><td><code>JsonSchedulingDataProcessorPlugin.Shutdown(CancellationToken)</code></td><td>public on the JSON plugin, absent on the XML one</td><td>absent on both: <code>ISchedulerPlugin.Shutdown</code> does nothing by default, and neither plugin has anything to release</td></tr><tr><td><code>FileNames</code>, <code>ScanInterval</code>, <code>FailOnFileNotFound</code>, <code>FailOnSchedulingError</code></td><td>public get-only on both, with internal setters</td><td>internal, like every other shipped component&#39;s settings. <code>FileSchedulingOptions</code> is what says them, and <code>quartz.plugin.&lt;name&gt;.fileNames</code> and friends still write them</td></tr><tr><td><code>Name</code>, <code>Scheduler</code></td><td>public get-only on both</td><td>internal. They are what <code>Initialize</code> was handed, and the scheduler that handed them over is the one asking</td></tr></tbody></table><p>Nothing here was writable from outside, so nothing that <em>configured</em> a plugin stops compiling. Code that <em>read</em> one of these properties is what changes, and the answer is the options rather than the plugin: <code>IOptionsFactory&lt;FileSchedulingOptions&gt;.Create(schedulerName)</code> is the same value from the place that decided it.</p><p>The <code>FileNames</code>/<code>Files</code> split stays, on purpose: <code>FileSchedulingOptions.Files</code> is a <code>List&lt;string&gt;</code> because that is what code and a configuration binder say naturally, and the plugin keeps a delimited <code>FileNames</code> string because <code>quartz.plugin.&lt;name&gt;.fileNames</code> is one. The join happens in <code>UseXmlSchedulingConfiguration</code> / <code>UseJsonSchedulingConfiguration</code>, and both plugins now agree about it.</p><p>Both also take a <code>params string[]</code> overload — <code>q.UseJsonSchedulingConfiguration(&quot;~/quartz_jobs.json&quot;)</code> — for the case every sample showed, which is one file and nothing else to say about it. It adds to <code>Files</code> rather than replacing it, so it composes with the callback form and with itself; rescanning and the two <code>FailOn*</code> settings still need the callback.</p><h3 id="the-container-is-not-in-the-scheduler-context" tabindex="-1"><a class="header-anchor" href="#the-container-is-not-in-the-scheduler-context"><span>The container is not in the scheduler context</span></a></h3><p>3.x&#39;s <code>ServiceCollectionSchedulerFactory</code> put the <code>IServiceProvider</code> into <code>scheduler.Context[&quot;Quartz.ServiceProvider&quot;]</code>, and a plugin that needed the container read it back out of there. 4.0 writes no such entry. Plugins and listeners are constructed by the container, so they take what they need by constructor — and a job takes its dependencies the same way:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> private IServiceProvider? services;</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"></span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"> public ValueTask Initialize(string name, IScheduler scheduler, CancellationToken cancellationToken = default)</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"> {</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line">     services = (IServiceProvider) scheduler.Context[&quot;Quartz.ServiceProvider&quot;]!;</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> private readonly IMyService service;</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line"></span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line"> public MyPlugin(IMyService service) =&gt; this.service = service;</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The entry was Quartz&#39;s plumbing in a map that belongs to the application, and the HTTP API&#39;s <code>GET …/schedulers/{name}/context</code> answered <code>500</code> for every container-built scheduler because of it (<a href="https://github.com/quartznet/quartznet/issues/3408" target="_blank" rel="noopener noreferrer">#3408</a>). That endpoint renders every value as text now, so a context entry of any type reads back.</p><h2 id="cross-cutting-concerns-run-as-middleware" tabindex="-1"><a class="header-anchor" href="#cross-cutting-concerns-run-as-middleware"><span>Cross-cutting concerns run as middleware</span></a></h2><p>A log scope, a tenant context, a metric or a translation of what a library throws has to <em>surround</em> the call to the job, and on 3.x nothing could. <code>IJobListener</code> is notification-only — it is told a job is about to run and told what it did, with the execution happening between the two notifications rather than inside them — so the only place left was a job that wrapped another job. That adapter is what ABP, Elsa and Brighter each ship, and asking for the seam has been open since 2021 (<a href="https://github.com/quartznet/quartznet/discussions/988" target="_blank" rel="noopener noreferrer">#988</a>).</p><p>4.0 adds it:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token keyword">public</span> <span class="token keyword">delegate</span> <span class="token return-type class-name">ValueTask</span> <span class="token function">JobExecutionDelegate</span><span class="token punctuation">(</span><span class="token class-name">IJobExecutionContext</span> context<span class="token punctuation">,</span> <span class="token class-name">CancellationToken</span> cancellationToken<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line"><span class="token keyword">public</span> <span class="token keyword">interface</span> <span class="token class-name">IJobExecutionMiddleware</span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    <span class="token return-type class-name">ValueTask</span> <span class="token function">Invoke</span><span class="token punctuation">(</span><span class="token class-name">IJobExecutionContext</span> context<span class="token punctuation">,</span> <span class="token class-name">JobExecutionDelegate</span> next<span class="token punctuation">,</span> <span class="token class-name">CancellationToken</span> cancellationToken <span class="token operator">=</span> <span class="token keyword">default</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>registered on the scheduler&#39;s builder in the three shapes listeners already have:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line">q<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">AddJobMiddleware</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>LogScopeMiddleware<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">q<span class="token punctuation">.</span><span class="token function">AddJobMiddleware</span><span class="token punctuation">(</span>provider <span class="token operator">=&gt;</span> <span class="token keyword">new</span> <span class="token constructor-invocation class-name">MeteredMiddleware</span><span class="token punctuation">(</span>provider<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">GetRequiredService</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>IMeterFactory<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">q<span class="token punctuation">.</span><span class="token function">AddJobMiddleware</span><span class="token punctuation">(</span><span class="token keyword">new</span> <span class="token constructor-invocation class-name">TenantScopeMiddleware</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Middleware is keyed per scheduler, so a named scheduler&#39;s is its own; it runs in registration order, outermost first; and the chain is composed once when the scheduler is built, so one instance serves every firing and per-firing state belongs in an <code>AsyncLocal&lt;T&gt;</code> or in the job&#39;s scope rather than in a field. A scheduler with no middleware has no pipeline at all, so nothing changes for an application that adds none.</p><p>It runs inside the execution span and the duration measurement — what a middleware costs is part of what the firing cost — and outside the run shell&#39;s exception handling, so a <code>JobExecutionException</code> a middleware throws is honoured exactly like one the job raised, <code>RefireImmediately</code> and all.</p>`,59),e(`p`,null,[l[187]||=e(`strong`,null,`Nothing about listeners changed.`,-1),l[188]||=r(` They stay notification-only, `,-1),l[189]||=e(`code`,null,`VetoJobExecution`,-1),l[190]||=r(` stays the way to refuse a fire, and matchers stay a listener's way of choosing which jobs it hears about. The tutorial's `,-1),c(g,{to:`/documentation/quartz-4.x/tutorial/job-execution-middleware.html`},{default:i(()=>[...l[186]||=[r(`Job Execution Middleware`,-1)]]),_:1}),l[191]||=r(` lesson has the whole of it, including which of the two a given concern belongs in.`,-1)]),l[868]||=t(`<h3 id="jobinterruptmonitorplugin-is-retired-a-job-timeout-is-middleware" tabindex="-1"><a class="header-anchor" href="#jobinterruptmonitorplugin-is-retired-a-job-timeout-is-middleware"><span><code>JobInterruptMonitorPlugin</code> is retired; a job timeout is middleware</span></a></h3><p>The plugin&#39;s opt-in surface was two <code>JobDataMap</code> strings the compiler never saw — <code>&quot;AutoInterruptable&quot;</code> to enable it at all, <code>&quot;MaxRunTime&quot;</code> for a number of milliseconds whose unit was documented only by the parse — and behind them a monitor task scheduled per firing. The middleware seam is where that belongs, so <code>JobInterruptMonitorPlugin</code>, <code>UseJobAutoInterrupt</code>, <code>JobAutoInterruptOptions</code> and both keys are gone. The replacement is in the core <code>Quartz</code> package and is typed end to end:</p><table><thead><tr><th>3.x / earlier 4.0 preview</th><th>4.0</th></tr></thead><tbody><tr><td><code>q.UseJobAutoInterrupt(o =&gt; o.DefaultMaxRunTime = TimeSpan.FromMinutes(5))</code></td><td><code>q.AddJobTimeout(TimeSpan.FromMinutes(5))</code></td></tr><tr><td><code>q.UseJobAutoInterrupt()</code> and no per-job data</td><td><code>q.AddJobTimeout()</code>, which bounds only the jobs that declare a budget</td></tr><tr><td><code>.UsingJobData(JobInterruptMonitorPlugin.JobDataMapKeyAutoInterruptable, true)</code></td><td>nothing — every job the scheduler runs is bounded by the default</td></tr><tr><td><code>.UsingJobData(JobInterruptMonitorPlugin.JobDataMapKeyMaxRunTime, &quot;5000&quot;)</code></td><td><code>[JobTimeout(&quot;00:00:05&quot;)]</code> on the job class</td></tr><tr><td>no way to exempt one job from the default</td><td><code>[JobTimeout(&quot;00:00:00&quot;)]</code> on the job class</td></tr><tr><td><code>quartz.plugin.jobAutoInterrupt.type = Quartz.Plugin.Interrupt.JobInterruptMonitorPlugin, Quartz.Plugins</code></td><td>remove the keys; the type no longer exists, so a configuration still naming it fails to load</td></tr><tr><td><code>JobInterruptMonitorPlugin.JobDataMapKeyAutoInterruptable</code> / <code>.JobDataMapKeyMaxRunTime</code></td><td>removed</td></tr></tbody></table><p>The budget travels with the job type rather than with its stored data, which is why it moved to an attribute: <code>[JobTimeout]</code> is read the same way <code>[DisallowConcurrentExecution]</code> is, is inherited from a base class or an interface, and needs no schema column, no wire format and no migration of existing job data. Delete the two keys from any job or trigger data map that carries them — they mean nothing now.</p><p><strong>One behaviour is deliberately different, and it is the point of the change.</strong> The plugin interrupted the firing and stopped there, which the run shell classifies as a <em>completed</em> execution: no <code>JobExecutionException</code> reached a job listener, and a trigger&#39;s retry policy was never consulted. The middleware interrupts through the same public <code>IScheduler.InterruptFireInstance</code> path and then raises a <code>JobExecutionException</code> naming the budget, so a timeout is an ordinary failure — visible to listeners, counted as an error, and retried by the trigger&#39;s <code>RetryPolicy</code> if it has one. A job that swallows its cancellation and returns normally is reported as timed out too.</p><p>What has <em>not</em> changed is that a job which ignores its <code>CancellationToken</code> cannot be stopped. That was the plugin&#39;s limit as well; <code>CA2016</code> is the mitigation.</p><h3 id="shutdownhookplugin-is-retired-the-host-already-shuts-the-scheduler-down" tabindex="-1"><a class="header-anchor" href="#shutdownhookplugin-is-retired-the-host-already-shuts-the-scheduler-down"><span><code>ShutdownHookPlugin</code> is retired; the host already shuts the scheduler down</span></a></h3><p><code>ShutdownHookPlugin</code> subscribed to <code>AppDomain.CurrentDomain.ProcessExit</code> with an <code>async void</code> handler, so nothing awaited the shutdown it started: the process was free to exit part-way through, which is the opposite of the clean shutdown the plugin&#39;s name promised. <code>ShutdownHookPlugin</code>, <code>UseShutdownHook</code> and <code>ShutdownHookOptions</code> are gone.</p><table><thead><tr><th>3.x / earlier 4.0 preview</th><th>4.0</th></tr></thead><tbody><tr><td><code>q.UseShutdownHook()</code> under a host</td><td><code>services.AddQuartzHostedService()</code> — already the recommended registration, and already what stops the scheduler</td></tr><tr><td><code>q.UseShutdownHook(o =&gt; o.CleanShutdown = true)</code></td><td><code>services.AddQuartzHostedService(o =&gt; o.WaitForJobsToComplete = true)</code> — <code>CleanShutdown</code> under its real name</td></tr><tr><td><code>q.UseShutdownHook(o =&gt; o.CleanShutdown = false)</code></td><td><code>services.AddQuartzHostedService()</code>; <code>WaitForJobsToComplete</code> defaults to <code>false</code></td></tr><tr><td><code>q.UseShutdownHook()</code> with no host</td><td><code>await scheduler.Shutdown(waitForJobsToComplete: true)</code> on the application&#39;s own exit path</td></tr><tr><td><code>quartz.plugin.shutdownHook.type = Quartz.Plugin.Management.ShutdownHookPlugin, Quartz.Plugins</code></td><td>remove the keys; the type no longer exists, so a configuration still naming it fails to load</td></tr><tr><td><code>ShutdownHookPlugin.CleanShutdown</code></td><td><code>QuartzHostedServiceOptions.WaitForJobsToComplete</code></td></tr></tbody></table><p><code>QuartzHostedService</code> is an <code>IHostedLifecycleService</code>, so the host stops every registered scheduler as part of its own shutdown and <em>awaits</em> it, inside <code>HostOptions.ShutdownTimeout</code> rather than in a handler the runtime abandons. It also shuts several schedulers down concurrently, which <code>ProcessExit</code> never did.</p><p>An application with no host has an exit path of its own — the end of <code>Main</code>, a <code>Ctrl+C</code> handler, the disposal of a scope — and that path can await the shutdown:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token keyword">await</span> <span class="token keyword">using</span> <span class="token class-name">StandaloneSchedulerFactory</span> schedulerFactory <span class="token operator">=</span> QuartzSchedulerBuilder<span class="token punctuation">.</span><span class="token function">Create</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">Build</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"><span class="token class-name">IScheduler</span> scheduler <span class="token operator">=</span> <span class="token keyword">await</span> schedulerFactory<span class="token punctuation">.</span><span class="token function">GetScheduler</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"><span class="token keyword">await</span> scheduler<span class="token punctuation">.</span><span class="token function">Start</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line"><span class="token comment">// ... the application runs ...</span></span>
<span class="line"></span>
<span class="line"><span class="token keyword">await</span> scheduler<span class="token punctuation">.</span><span class="token function">Shutdown</span><span class="token punctuation">(</span><span class="token named-parameter punctuation">waitForJobsToComplete</span><span class="token punctuation">:</span> <span class="token boolean">true</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>3.x&#39;s <code>Quartz.Server</code> host already shut down this way and never used the plugin.</p><h2 id="registered-schedulers-can-be-listed-without-being-started" tabindex="-1"><a class="header-anchor" href="#registered-schedulers-can-be-listed-without-being-started"><span>Registered schedulers can be listed without being started</span></a></h2><p><code>ISchedulerFactory.GetAllSchedulers()</code> — <code>GetAllSchedulers()</code> on 3.x too — lists the schedulers <em>something has already created</em>. It reads <code>ISchedulerRepository</code>, and a repository holds instances, so a scheduler nobody has asked for is not in it. Under a scheduler-per-tenant registration that meant there was no way to ask a container which tenants it knows about short of building every one of them, which is the opposite of what an operator wanted when they asked.</p><p><code>ISchedulerRegistry</code> answers from the registrations instead, and is registered by <code>AddQuartz</code>, so any container with Quartz in it has one:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token keyword">foreach</span> <span class="token punctuation">(</span><span class="token class-name">SchedulerRegistration</span> registration <span class="token keyword">in</span> <span class="token keyword">await</span> registry<span class="token punctuation">.</span><span class="token function">QuerySchedulers</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    Console<span class="token punctuation">.</span><span class="token function">WriteLine</span><span class="token punctuation">(</span><span class="token interpolation-string"><span class="token string">$&quot;</span><span class="token interpolation"><span class="token punctuation">{</span><span class="token expression language-csharp">registration<span class="token punctuation">.</span>Name</span><span class="token punctuation">}</span></span><span class="token string">: </span><span class="token interpolation"><span class="token punctuation">{</span><span class="token expression language-csharp">registration<span class="token punctuation">.</span>Status<span class="token punctuation">?.</span><span class="token function">ToString</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">??</span> <span class="token string">&quot;not created&quot;</span></span><span class="token punctuation">}</span></span><span class="token string">&quot;</span></span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token keyword">public</span> <span class="token keyword">sealed</span> <span class="token keyword">record</span> <span class="token class-name">SchedulerRegistration</span><span class="token punctuation">(</span><span class="token class-name"><span class="token keyword">string</span></span> Name<span class="token punctuation">,</span> <span class="token class-name">SchedulerOrigin</span> Origin<span class="token punctuation">,</span> <span class="token class-name">SchedulerStatus<span class="token punctuation">?</span></span> Status<span class="token punctuation">)</span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    <span class="token keyword">public</span> <span class="token return-type class-name"><span class="token keyword">bool</span></span> IsCreated <span class="token punctuation">{</span> <span class="token keyword">get</span><span class="token punctuation">;</span> <span class="token punctuation">}</span>   <span class="token comment">// Status is not null</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span>
<span class="line"><span class="token keyword">public</span> <span class="token keyword">enum</span> <span class="token class-name">SchedulerOrigin</span> <span class="token punctuation">{</span> Container<span class="token punctuation">,</span> Runtime <span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><ul><li><strong><code>Status</code> is <code>null</code> exactly when nothing has been built under that name.</strong> Asking does not build it — that is the whole point.</li><li><strong><code>Origin.Container</code></strong> is a scheduler <code>AddQuartz()</code> or <code>AddQuartz(name, …)</code> registered. <strong><code>Origin.Runtime</code></strong> is one that is in the repository without a registration behind it: a <code>QuartzSchedulerBuilder</code> scheduler bound by hand, or a remote scheduler from <code>AddQuartzHttpClient</code>. Nothing in the container owns a runtime scheduler&#39;s lifetime.</li><li>The default scheduler is listed under its configured <code>InstanceName</code>, which is the one name that is not the name it was registered under — it has no service key at all.</li></ul><p>Nothing is removed: <code>GetAllSchedulers()</code> still means what it always meant, and it is still the call to make when you want the live schedulers themselves rather than an inventory.</p><h3 id="get-schedulers-answers-from-the-registry" tabindex="-1"><a class="header-anchor" href="#get-schedulers-answers-from-the-registry"><span><code>GET /schedulers</code> answers from the registry</span></a></h3><p>The HTTP API&#39;s scheduler listing reads <code>ISchedulerRegistry</code> too, so it carries the registrations rather than only what has been built, and each entry says which it is:</p><div class="language-json line-numbers-mode" data-highlighter="prismjs" data-ext="json"><pre><code class="language-json"><span class="line"><span class="token punctuation">{</span> <span class="token property">&quot;name&quot;</span><span class="token operator">:</span> <span class="token string">&quot;acme&quot;</span><span class="token punctuation">,</span> <span class="token property">&quot;schedulerInstanceId&quot;</span><span class="token operator">:</span> <span class="token null keyword">null</span><span class="token punctuation">,</span> <span class="token property">&quot;status&quot;</span><span class="token operator">:</span> <span class="token null keyword">null</span><span class="token punctuation">,</span> <span class="token property">&quot;origin&quot;</span><span class="token operator">:</span> <span class="token string">&quot;Container&quot;</span> <span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><table><thead><tr><th></th><th>3.x and the earlier 4.0 previews</th><th>4.0</th></tr></thead><tbody><tr><td>Source</td><td><code>ISchedulerRepository.LookupAll()</code></td><td><code>ISchedulerRegistry.QuerySchedulers()</code></td></tr><tr><td><code>status</code></td><td>always a name</td><td><code>null</code> when nothing has built the scheduler</td></tr><tr><td><code>schedulerInstanceId</code></td><td>always present</td><td><code>null</code> when nothing has built the scheduler</td></tr><tr><td><code>origin</code></td><td>—</td><td><code>Container</code> or <code>Runtime</code></td></tr></tbody></table><p>A reader that assumed <code>status</code> and <code>schedulerInstanceId</code> are always present has to handle <code>null</code>, and one that took the listing to mean &quot;the schedulers that are running&quot; should filter on <code>status</code>. Nothing else moved: a scheduler&#39;s own routes still resolve through the repository, so <code>GET /schedulers/{name}</code> answers <code>404</code> for a registration nothing has built.</p><p><code>Quartz.Dashboard</code>&#39;s own projection changed with it — <code>SchedulerHeaderDto.Status</code> and <code>SchedulerInstanceId</code> are nullable, it gained <code>Origin</code> and <code>IsCreated</code>, and <code>SchedulerDetailDto</code> grew the <code>SchedulerMetadata</code> the dashboard used to drop: <code>Clustered</code>, <code>Persistent</code>, <code>JobStoreTypeName</code>, <code>ThreadPoolTypeName</code>, <code>ThreadPoolSize</code>, <code>RunningSince</code>, <code>JobsExecuted</code> and <code>Version</code>. Both types are public because <code>IQuartzApiClient</code> is replaceable; an application that implements that interface itself has to fill the new members.</p><p><code>ISchedulerRegistry</code> is deliberately the <em>narrow</em> half of the API <a href="https://github.com/quartznet/quartznet/issues/3338" target="_blank" rel="noopener noreferrer">#3338</a> sketches for runtime tenant lifecycle. Adding and removing schedulers at runtime is a 4.1 concern; when it lands, its manager interface extends this one rather than replacing it.</p><h2 id="a-shared-database-says-so-when-two-schedulers-disagree-about-the-table-prefix" tabindex="-1"><a class="header-anchor" href="#a-shared-database-says-so-when-two-schedulers-disagree-about-the-table-prefix"><span>A shared database says so when two schedulers disagree about the table prefix</span></a></h2><p>Two schedulers sharing a database are told apart by <code>SCHED_NAME</code> and share one table prefix. Pointing one of them at a different prefix by accident used to be silent in the worst possible way: the scheduler connects, schema validation passes against the tables it <em>was</em> pointed at, it starts, it reports healthy, and it never sees its tenant&#39;s data. <code>SchemaProvisioning.CreateIfMissing</code> makes that easier to reach rather than harder, since it will create the mis-typed table set rather than needing one to be there.</p><p>Creating a scheduler now records its database and table prefix, and a scheduler that shares a database with one already created but disagrees about the prefix is reported at <code>Warning</code>, naming both schedulers and both prefixes. It is a warning rather than an error because separate table sets in one database are legal and occasionally deliberate; the message says which two registrations to look at and leaves the decision where it belongs.</p><p>The check sees what one container can see, which is its own schedulers. Two processes, or two containers in one process, sharing a database cannot be compared this way, and a database reached through a provider that reports no connection string and no <code>DbDataSource</code> is not guessed about. Nothing about the check is configurable, and nothing fails because of it.</p><h2 id="several-schedulers-are-registered-explicitly" tabindex="-1"><a class="header-anchor" href="#several-schedulers-are-registered-explicitly"><span>Several schedulers are registered explicitly</span></a></h2><p><code>AddQuartz(IConfiguration)</code> used to look for a <code>Schedulers</code> sub-section and, if it found one, register one named scheduler per child instead of the single scheduler it otherwise registers. One call did two different things depending on the shape of a file. The fan-out has a name of its own now:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> services.AddQuartz(builder.Configuration.GetSection(&quot;Quartz&quot;));   // with a Schedulers section</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> services.AddQuartzSchedulers(builder.Configuration.GetSection(&quot;Quartz&quot;));</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p><code>AddQuartz(configuration)</code> throws a <code>SchedulerConfigException</code> naming <code>AddQuartzSchedulers</code> when it is handed a section with a <code>Schedulers</code> sub-section, and <code>AddQuartzSchedulers</code> throws when handed one without. <code>AddQuartz(name, configuration)</code>, which registers one of the schedulers a <code>Schedulers</code> section describes, is unchanged.</p><p>The six phases that decide which of a scheduler&#39;s descriptions wins — configuration is last-wins, registration is first-wins — are now documented on <code>AddQuartz</code> itself rather than in comments inside it.</p><h2 id="every-scheduler-in-the-container-can-be-configured-at-once" tabindex="-1"><a class="header-anchor" href="#every-scheduler-in-the-container-can-be-configured-at-once"><span>Every scheduler in the container can be configured at once</span></a></h2><p><code>ConfigureAllQuartzSchedulers(Action&lt;IQuartzBuilder&gt;)</code> is new. It is the options pattern&#39;s <code>ConfigureAll</code>, for schedulers: the delegate is applied to every scheduler <code>AddQuartz()</code>, <code>AddQuartz(name, …)</code> or <code>AddQuartzSchedulers(…)</code> registers in the container.</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line">services<span class="token punctuation">.</span><span class="token function">AddQuartz</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">services<span class="token punctuation">.</span><span class="token function">AddQuartz</span><span class="token punctuation">(</span><span class="token string">&quot;acme&quot;</span><span class="token punctuation">,</span> q <span class="token operator">=&gt;</span> q<span class="token punctuation">.</span><span class="token function">UseInMemoryStore</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">services<span class="token punctuation">.</span><span class="token function">AddQuartz</span><span class="token punctuation">(</span><span class="token string">&quot;initech&quot;</span><span class="token punctuation">,</span> q <span class="token operator">=&gt;</span> q<span class="token punctuation">.</span><span class="token function">UseInMemoryStore</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line"><span class="token comment">// Both named schedulers and the default one get their own instance of each.</span></span>
<span class="line">services<span class="token punctuation">.</span><span class="token function">ConfigureAllQuartzSchedulers</span><span class="token punctuation">(</span>q <span class="token operator">=&gt;</span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    q<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">AddPlugin</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>AuditPlugin<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span><span class="token string">&quot;audit&quot;</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">    q<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">AddJobListener</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>TenantMetricsListener<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"><span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><strong>The order of the calls does not matter.</strong> Schedulers already registered are configured where this is called; schedulers registered afterwards are configured by their own <code>AddQuartz</code>. That is the point: a library that adds something to every scheduler cannot know whether the application registers its schedulers before or after calling it.</p><p>The delegate is handed a builder <em>per scheduler</em>, so everything it registers lands under that scheduler&#39;s own service key — exactly as if it had been written inside that scheduler&#39;s <code>AddQuartz(name, q =&gt; …)</code> callback. A plugin or listener added this way is therefore <strong>one instance per scheduler</strong>, each initialized with the name of the scheduler it belongs to, rather than one instance shared between them.</p><p>It runs after each scheduler&#39;s own configuration callback, which is what makes the order immaterial. The usual precedence follows: registration is first-wins, so a job store or thread pool a scheduler chose for itself is not replaced by one chosen here; options are last-wins, so a value set here overrides the same option set on one scheduler, exactly as <code>ConfigureAll&lt;TOptions&gt;</code> overrides an earlier named <code>Configure</code>.</p><p>Remote schedulers registered with <code>AddQuartzHttpClient</code> are not built by a builder and are skipped. Calling it when no scheduler is registered at all is not an error.</p><p><code>AddQuartzDashboard()</code> is its first caller: it installs its live-events and history plugins this way, so a scheduler registered with <code>AddQuartz(&quot;acme&quot;, …)</code> finally has a populated live view and execution history rather than two silently empty pages.</p><h2 id="ischeduler-is-a-service-keyed-by-the-scheduler-s-name" tabindex="-1"><a class="header-anchor" href="#ischeduler-is-a-service-keyed-by-the-scheduler-s-name"><span><code>IScheduler</code> is a service, keyed by the scheduler&#39;s name</span></a></h2><p><code>AddQuartz()</code> now registers <code>IScheduler</code> as well as <code>ISchedulerFactory</code>. The default scheduler is registered unkeyed and a named scheduler under its own name, so a scheduler is injected the way any other service is:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token keyword">public</span> <span class="token keyword">sealed</span> <span class="token keyword">class</span> <span class="token class-name">ReportRunner</span><span class="token punctuation">(</span></span>
<span class="line">    <span class="token class-name">IScheduler</span> scheduler<span class="token punctuation">,</span>                                       <span class="token comment">// the default scheduler</span></span>
<span class="line">    <span class="token punctuation">[</span><span class="token function">FromKeyedServices</span><span class="token punctuation">(</span><span class="token string">&quot;reporting&quot;</span><span class="token punctuation">)</span><span class="token punctuation">]</span> <span class="token class-name">IScheduler</span> reporting<span class="token punctuation">)</span>      <span class="token comment">// AddQuartz(&quot;reporting&quot;, …)</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token class-name"><span class="token keyword">var</span></span> scheduler <span class="token operator">=</span> provider<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">GetRequiredService</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>IScheduler<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"><span class="token class-name"><span class="token keyword">var</span></span> reporting <span class="token operator">=</span> provider<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">GetRequiredKeyedService</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>IScheduler<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span><span class="token string">&quot;reporting&quot;</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>Resolving <code>ISchedulerFactory</code> and awaiting <code>GetScheduler()</code> still works and is unchanged; it is no longer the only way.</p><p>What is registered is a handle rather than the scheduler itself, because building a scheduler is asynchronous and a container constructs synchronously. Every asynchronous member awaits the scheduler being built, so they are always safe. The synchronous ones — <code>SchedulerInstanceId</code>, <code>Status</code>, <code>Context</code> and <code>ListenerManager</code> — can only answer once the scheduler exists, and throw <code>InvalidOperationException</code> if reading one would have to build it. Under <code>AddQuartzHostedService()</code> that cannot happen: every scheduler in the container is built while the host starts, which is all those members need — starting them is a separate step, and by default waits until the application has started. <code>SchedulerName</code> is answered from the registration and never builds anything.</p><h2 id="a-remote-scheduler-is-registered-by-name-not-by-a-marker-interface" tabindex="-1"><a class="header-anchor" href="#a-remote-scheduler-is-registered-by-name-not-by-a-marker-interface"><span>A remote scheduler is registered by name, not by a marker interface</span></a></h2><p><code>AddQuartzHttpClient&lt;TScheduler&gt;(…)</code> is removed, along with the runtime type generation behind it. A second remote scheduler needed a marker interface only because the container had no other way to tell two <code>IScheduler</code> registrations apart; the service key says it directly:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> services.AddQuartzHttpClient&lt;IMyScheduler&gt;(&quot;MyScheduler&quot;, &quot;QuartzHttpClient&quot;);</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"> services.AddQuartzHttpClient&lt;IMySecondScheduler&gt;(&quot;MySecondScheduler&quot;, &quot;QuartzHttpClient&quot;);</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> services.AddQuartzHttpClient(&quot;MyScheduler&quot;, &quot;QuartzHttpClient&quot;);</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line"> services.AddQuartzHttpClient(&quot;MySecondScheduler&quot;, &quot;QuartzHttpClient&quot;);</span>
<span class="line"></span></span></span>
<span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> var mine = provider.GetRequiredService&lt;IMyScheduler&gt;();</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> var mine = provider.GetRequiredKeyedService&lt;IScheduler&gt;(&quot;MyScheduler&quot;);</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The marker interfaces themselves can be deleted, and the first remote scheduler registered is still the unkeyed <code>IScheduler</code> for a container that holds only one.</p><p>In a container that also holds a local scheduler, <strong><code>AddQuartz()</code> goes first</strong>:</p><table><thead><tr><th>Order</th><th>What happens</th></tr></thead><tbody><tr><td><code>AddQuartz()</code> then <code>AddQuartzHttpClient(…)</code></td><td>The local default scheduler owns <code>GetRequiredService&lt;IScheduler&gt;()</code>; the remote one is reached by name. This is the arrangement to write.</td></tr><tr><td><code>AddQuartzHttpClient(…)</code> then <code>AddQuartz()</code></td><td><code>AddQuartz()</code> throws <code>InvalidOperationException</code> at registration, naming <code>AddQuartzHttpClient</code>. Registration is first-wins, so this used to make &quot;the scheduler&quot; the remote one with no error at all — a program that thought it held its own scheduler was scheduling jobs in somebody else&#39;s process.</td></tr><tr><td><code>AddQuartzHttpClient(…)</code> then <code>AddQuartz(&quot;Local&quot;, …)</code></td><td>Fine either way round. A named scheduler is keyed by its name and never wanted the unkeyed slot.</td></tr></tbody></table><h3 id="a-client-is-named-or-built-never-handed-over" tabindex="-1"><a class="header-anchor" href="#a-client-is-named-or-built-never-handed-over"><span>A client is named or built, never handed over</span></a></h3><p><code>HttpClientOptions.HttpClient</code> and the <code>AddQuartzHttpClient(schedulerName, HttpClient, …)</code> overload are removed. An options object is bound from configuration, cached and shared; a live <code>HttpClient</code> sitting in one is a disposable resource with no owner, unreachable from <code>appsettings.json</code>, and it goes around <code>IHttpClientFactory</code> — which is what keeps a long-lived client from pinning stale DNS.</p><p>There are two shapes, both of which say who made the client:</p><table><thead><tr><th>Shape</th><th>How</th></tr></thead><tbody><tr><td>A named <code>IHttpClientFactory</code> client — prefer this</td><td><code>AddQuartzHttpClient(name, &quot;QuartzHttpClient&quot;)</code>, or <code>options.HttpClientName</code></td></tr><tr><td>A factory of your own</td><td><code>AddQuartzHttpClient(name, provider =&gt; …)</code>, or <code>options.CreateHttpClient</code></td></tr></tbody></table><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> var client = new HttpClient { BaseAddress = new Uri(&quot;http://localhost:5000/quartz-api/&quot;) };</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"> services.AddQuartzHttpClient(&quot;MyScheduler&quot;, client);</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> services.AddHttpClient(&quot;QuartzHttpClient&quot;, c =&gt; c.BaseAddress = new Uri(&quot;http://localhost:5000/quartz-api/&quot;));</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line"> services.AddQuartzHttpClient(&quot;MyScheduler&quot;, &quot;QuartzHttpClient&quot;);</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>To keep building the client yourself, wrap it in a factory — it runs once, when the scheduler is first resolved, and is handed the container:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> services.AddQuartzHttpClient(&quot;MyScheduler&quot;, client);</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> services.AddQuartzHttpClient(&quot;MyScheduler&quot;, _ =&gt; client);</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>Either way the client belongs to whoever made it. The scheduler never disposes it.</p><p>This removes <code>Quartz.HttpClient</code>&#39;s only use of <code>System.Reflection.Emit</code>, so the package no longer emits a type at runtime for a scheduler interface — one fewer thing standing between it and ahead-of-time compilation.</p><p>Remote schedulers are also bound into <code>ISchedulerRepository</code> when the host starts, rather than the first time one is injected. A dashboard or an HTTP API listing the container&#39;s schedulers now shows them without something else having had to use them first. They are bound under their own name; reading a remote scheduler&#39;s instance id costs a request, and a name identifies one registration on its own. A container with no host is unaffected — nothing runs the binder, and the scheduler is built when it is first used, exactly as before.</p><h2 id="quartz-can-be-added-to-the-host-application-builder" tabindex="-1"><a class="header-anchor" href="#quartz-can-be-added-to-the-host-application-builder"><span>Quartz can be added to the host application builder</span></a></h2><p><code>AddQuartz</code> and <code>AddQuartzHostedService</code> have <code>IHostApplicationBuilder</code> overloads, which is how an application built by <code>Host.CreateApplicationBuilder</code> or <code>WebApplication.CreateBuilder</code> adds everything else. A builder has both halves of what the configuration overload needs, so the section is found rather than handed over:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> services.AddQuartz(builder.Configuration.GetSection(&quot;Quartz&quot;), q =&gt; { });</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"> services.AddQuartzHostedService();</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> builder.AddQuartz(q =&gt; { });</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line"> builder.AddQuartzHostedService();</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The section it reads is <code>Quartz</code>, which is the name every sample and documentation page uses. An application whose Quartz configuration lives elsewhere passes the section explicitly, through the <code>IServiceCollection</code> overloads — nothing was removed, and they mean exactly what they always did.</p><p>A string is a scheduler&#39;s name here as it is everywhere else in Quartz: <code>builder.AddQuartz(&quot;reporting&quot;)</code> registers a scheduler called <code>reporting</code> and reads its settings from <code>Quartz:Schedulers:reporting</code>, and <code>builder.AddQuartzSchedulers()</code> registers one scheduler per child of that sub-section.</p><h2 id="the-hosted-service-starts-every-scheduler" tabindex="-1"><a class="header-anchor" href="#the-hosted-service-starts-every-scheduler"><span>The hosted service starts every scheduler</span></a></h2><p><code>AddQuartzHostedService()</code> registered the hosted service only if an unkeyed <code>ISchedulerFactory</code> was already in the service collection. Calling it before <code>AddQuartz()</code> therefore registered nothing for the default scheduler and said nothing about it — the application started, and no job ever ran.</p><p>The hosted service is now registered unconditionally and resolves its schedulers when the host starts, so the two calls can be made in either order. It starts every scheduler in the container, the default one and each named one, which is what the pair of services it replaces did between them.</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> services.AddQuartz(q =&gt; …);            // had to come first</span>
<span class="line"></span></span><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line"> services.AddQuartzHostedService(options =&gt; options.WaitForJobsToComplete = true);</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> services.AddQuartz(q =&gt; …);            // either order now</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>A container with no scheduler in it at all is a <code>SchedulerConfigException</code> at startup: the hosted service was asked for, so something was meant to run.</p><p><code>QuartzHostedServiceOptions</code> are now named options, keyed by scheduler name. Options passed to <code>AddQuartzHostedService(configure)</code> apply to every scheduler, which is what one shared options object meant before; a scheduler that has to differ is configured by name, and its settings are applied after the shared ones whichever order the calls are made in:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line">services<span class="token punctuation">.</span><span class="token function">AddQuartzHostedService</span><span class="token punctuation">(</span>options <span class="token operator">=&gt;</span> options<span class="token punctuation">.</span>WaitForJobsToComplete <span class="token operator">=</span> <span class="token boolean">true</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">services<span class="token punctuation">.</span><span class="token function">AddQuartzHostedService</span><span class="token punctuation">(</span><span class="token string">&quot;Reporting&quot;</span><span class="token punctuation">,</span> options <span class="token operator">=&gt;</span> options<span class="token punctuation">.</span>StartDelay <span class="token operator">=</span> TimeSpan<span class="token punctuation">.</span><span class="token function">FromMinutes</span><span class="token punctuation">(</span><span class="token number">2</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p><code>QuartzHostedService</code>&#39;s constructor changed to match: it takes the <code>IServiceProvider</code> it resolves the schedulers from and an <code>IOptionsMonitor&lt;QuartzHostedServiceOptions&gt;</code> instead of one factory and one options object. Subclasses registered with <code>AddQuartzHostedService&lt;T&gt;()</code> need their constructors updated; the <code>Starting</code>/<code>Started</code>/<code>Stopping</code>/<code>Stopped</code> overrides are unchanged. The internal <code>NamedSchedulerHostedService</code> is gone, its work having moved into the one service.</p><h3 id="a-hosted-scheduler-can-be-started-by-the-application" tabindex="-1"><a class="header-anchor" href="#a-hosted-scheduler-can-be-started-by-the-application"><span>A hosted scheduler can be started by the application</span></a></h3><p>New in 4.x, with nothing to migrate. <code>QuartzHostedServiceOptions.AutoStart</code> defaults to <code>true</code>, which is what the hosted service has always done; setting it to <code>false</code> has the scheduler resolved, initialized and bound with the host but left in <code>SchedulerStatus.Created</code> for the application to start:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line">services<span class="token punctuation">.</span><span class="token function">AddQuartzHostedService</span><span class="token punctuation">(</span><span class="token string">&quot;Reporting&quot;</span><span class="token punctuation">,</span> options <span class="token operator">=&gt;</span> options<span class="token punctuation">.</span>AutoStart <span class="token operator">=</span> <span class="token boolean">false</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>On 3.x the only way not to start a scheduler was not to register the hosted service, which lost the shutdown handling with it. The scheduler is bound, so <code>ISchedulerRegistry</code>, the dashboard and <code>GET /schedulers</code> all report it, and it is still shut down when the host stops — opting out of the start is not opting out of the stop. <code>AutoStart</code> wins over <code>AwaitApplicationStarted</code> and <code>StartDelay</code>, both of which say <em>when</em> the hosted service starts a scheduler that it does not start at all.</p><p>The health check follows: a scheduler in <code>Created</code> whose <code>AutoStart</code> is <code>false</code> is reported <strong>degraded</strong> rather than <strong>unhealthy</strong>, on the same argument as standby — it is doing what it was told, and failing the probe would take a correctly configured node out of rotation. A <code>Created</code> scheduler that nothing opted out of is unhealthy as before.</p><p>A library embedding Quartz in someone else&#39;s application is what this is for, and the how-to page on embedding Quartz in a library covers the whole pattern.</p><h2 id="the-asp-net-core-methods-say-quartz-once" tabindex="-1"><a class="header-anchor" href="#the-asp-net-core-methods-say-quartz-once"><span>The ASP.NET Core methods say Quartz once</span></a></h2><table><thead><tr><th>Before</th><th>After</th></tr></thead><tbody><tr><td><code>IQuartzBuilder.AddHttpApi(…)</code></td><td><code>IServiceCollection.AddQuartzHttpApi(…)</code></td></tr><tr><td><code>IEndpointRouteBuilder.MapQuartzApi()</code></td><td><code>MapQuartzHttpApi()</code></td></tr></tbody></table><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> services.AddQuartz(q =&gt; q.AddHttpApi());</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> services.AddQuartzHttpApi();</span>
<span class="line"></span></span></span>
<span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> app.MapQuartzApi().RequireAuthorization();</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> app.MapQuartzHttpApi().RequireAuthorization();</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The health check is no longer one of them — it ships in the core <code>Quartz</code> package (see <a href="#the-health-check-is-in-quartz-not-quartz-aspnetcore">The health check is in <code>Quartz</code>, not <code>Quartz.AspNetCore</code></a>), and it composes with an application&#39;s other checks rather than needing a call of its own:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line">services<span class="token punctuation">.</span><span class="token function">AddHealthChecks</span><span class="token punctuation">(</span><span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">.</span><span class="token function">AddSqlServer</span><span class="token punctuation">(</span>connectionString<span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">.</span><span class="token function">AddQuartz</span><span class="token punctuation">(</span><span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">.</span><span class="token function">AddQuartz</span><span class="token punctuation">(</span><span class="token string">&quot;Reporting&quot;</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>services.AddQuartzHealthChecks()</code> is gone; the health-checks builder is the receiver, and <code>services.AddHealthChecks().AddQuartz()</code> is the same call with the composition made visible. A named scheduler can also ask for its own check from inside <code>AddQuartz</code>, and it reports on <em>its</em> scheduler rather than the default one; either way it is named <code>quartz-scheduler-&lt;scheduler name&gt;</code> unless you say otherwise:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line">services<span class="token punctuation">.</span><span class="token function">AddQuartz</span><span class="token punctuation">(</span><span class="token string">&quot;Reporting&quot;</span><span class="token punctuation">,</span> q <span class="token operator">=&gt;</span> q<span class="token punctuation">.</span><span class="token function">AddQuartzHealthChecks</span><span class="token punctuation">(</span>options <span class="token operator">=&gt;</span> options<span class="token punctuation">.</span>Tags<span class="token punctuation">.</span><span class="token function">Add</span><span class="token punctuation">(</span><span class="token string">&quot;ready&quot;</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p><code>QuartzHealthCheckOptions</code> goes through the options pipeline. It used to be constructed and read inside the registration call, so <code>services.Configure&lt;QuartzHealthCheckOptions&gt;(...)</code> and a configuration section bound to the type silently did nothing; both now apply, whichever order they are written in. The options belong to the scheduler being checked, so a named scheduler&#39;s are configured under its name:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line">services<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">Configure</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>QuartzHealthCheckOptions<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span><span class="token string">&quot;Reporting&quot;</span><span class="token punctuation">,</span> options <span class="token operator">=&gt;</span> options<span class="token punctuation">.</span>Tags<span class="token punctuation">.</span><span class="token function">Add</span><span class="token punctuation">(</span><span class="token string">&quot;ready&quot;</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p><code>QuartzHealthCheckOptions.Name</code> is nullable, and left unset the check is named after the scheduler it reports on. Assigning a name still overrides that.</p><h2 id="addquartzserver-is-addquartzhostedservice" tabindex="-1"><a class="header-anchor" href="#addquartzserver-is-addquartzhostedservice"><span><code>AddQuartzServer</code> is <code>AddQuartzHostedService</code></span></a></h2><p><code>Quartz.AspNetCore.AddQuartzServer</code> did two unrelated things behind one name: it registered the hosted service that starts the scheduler, and — on frameworks that had them — a health check. Both are separately available, and neither was ever specific to ASP.NET Core, so the combined method is gone and each half is called by its own name:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line"> services.AddQuartz(q =&gt; { /* ... */ });</span>
<span class="line"></span></span></span>
<span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> services.AddQuartzServer(options =&gt; options.WaitForJobsToComplete = true);</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> services.AddQuartzHostedService(options =&gt; options.WaitForJobsToComplete = true);</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line"> services.AddHealthChecks().AddQuartz();</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Both halves live in the core <code>Quartz</code> package, so an application replacing <code>AddQuartzServer</code> needs no <code>Quartz.AspNetCore</code> reference at all — see <a href="#the-hosted-service-starts-every-scheduler">The hosted service starts every scheduler</a> for what the hosted service now starts, <a href="#the-health-check-is-in-quartz-not-quartz-aspnetcore">The health check is in <code>Quartz</code>, not <code>Quartz.AspNetCore</code></a> for where the check went, and <a href="#the-asp-net-core-methods-say-quartz-once">The ASP.NET Core methods say Quartz once</a> for what is left in that package.</p><p>The health-check overload that took <code>IEnumerable&lt;string&gt; healthCheckTags</code> is gone with it; tags are <code>QuartzHealthCheckOptions.Tags</code>, which is added to rather than assigned:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> services.AddQuartzServer(configure, healthCheckTags: [&quot;ready&quot;, &quot;live&quot;]);</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> services.AddHealthChecks().AddQuartz(options =&gt; options.Tags.AddRange([&quot;ready&quot;, &quot;live&quot;]));</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p><code>QuartzHealthCheckOptions.StandbyStatus</code> is the one setting that changes a <strong>verdict</strong> rather than the registration: a scheduler in standby reports <code>Degraded</code>, and setting this reports something else. Degraded is the right default — standby is deliberate and reversible — but ASP.NET Core maps it to HTTP 200, and a worker project has no endpoint on which to remap it, so a deployment whose standby nodes must leave the rotation had nothing to say. It covers standby alone: a scheduler still in <code>Created</code> because <code>AutoStart</code> is <code>false</code> keeps reporting degraded. <code>FailureStatus</code> is unrelated and unchanged — it is what the <em>registration</em> reports when the check says it failed.</p><h2 id="the-openapi-calendar-schema-names-the-properties-the-payload-actually-uses" tabindex="-1"><a class="header-anchor" href="#the-openapi-calendar-schema-names-the-properties-the-payload-actually-uses"><span>The OpenAPI calendar schema names the properties the payload actually uses</span></a></h2><p>The HTTP API&#39;s endpoints handle <code>ICalendar</code>, which OpenAPI cannot describe, so the published document has always been shaped by a stand-in type. Nothing compiled against that stand-in, so two of its property names were simply wrong, and a client generated from the document did not round-trip a calendar at all:</p><table><thead><tr><th>Schema said</th><th>Server sends</th></tr></thead><tbody><tr><td><code>calendarType</code></td><td><code>type</code></td></tr><tr><td><code>calendarBase</code></td><td><code>baseCalendar</code></td></tr></tbody></table><p>Nothing about the wire format changed — the server has always written <code>type</code> and <code>baseCalendar</code>. What changes is generated clients: regenerate against 4.x and the two properties on your calendar model are renamed, which is a compile error at each use rather than a silent one. Hand-written clients were already having to send <code>type</code> and <code>baseCalendar</code> to be understood, so they need no change.</p><p>The rest of the schema was already right: <code>description</code>, <code>timeZoneId</code>, <code>excludedDays</code>, <code>excludedDates</code>, <code>cronExpressionString</code>, <code>rangeStart</code>, <code>rangeEnd</code> and <code>invertTimeRange</code> are what a calendar of each built-in type serializes to. A test in <code>Quartz.Tests.AspNetCore</code> now compares the stand-in against those names in both directions, so the schema cannot quietly drift again.</p><h2 id="remoting-a-scheduler-is-not-a-quartz-concern" tabindex="-1"><a class="header-anchor" href="#remoting-a-scheduler-is-not-a-quartz-concern"><span>Remoting a scheduler is not a Quartz concern</span></a></h2><p><code>ISchedulerProxyFactory</code> and <code>HttpSchedulerProxyFactory</code> are removed, and the <code>quartz.scheduler.proxy*</code> and <code>quartz.scheduler.exporter*</code> keys are now rejected rather than silently accepted and ignored.</p><p>Nothing read them. <code>ISchedulerProxyFactory</code> had no caller inside Quartz once .NET Remoting went away — no builder member reached it, no configuration key selected it — while the key validator still whitelisted <code>quartz.scheduler.proxy</code>, so a configuration file could carry a proxy setting that changed nothing. A configuration that still carries one now gets a <code>SchedulerConfigException</code> saying what to use instead:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> quartz.scheduler.proxy = true</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"> quartz.scheduler.proxy.type = Quartz.Impl.HttpSchedulerProxyFactory, Quartz.HttpClient</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token comment">// talk to a remote scheduler over HTTP, from Quartz.HttpClient</span></span>
<span class="line">services<span class="token punctuation">.</span><span class="token function">AddQuartzHttpClient</span><span class="token punctuation">(</span><span class="token string">&quot;Quartz ASP.NET Core Sample Scheduler&quot;</span><span class="token punctuation">,</span> <span class="token string">&quot;QuartzHttpClient&quot;</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line"><span class="token comment">// or serve one over HTTP: AddQuartzHttpApi + MapQuartzHttpApi, from Quartz.AspNetCore</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="tasks-changed-to-valuetask" tabindex="-1"><a class="header-anchor" href="#tasks-changed-to-valuetask"><span>Tasks Changed to ValueTask</span></a></h2><p>In a majority of interfaces that previously returned or took a <code>Task</code> or <code>Task&lt;T&gt;</code> parameter, these have been changed to <code>ValueTask</code> or <code>ValueTask&lt;T&gt;</code>.</p><p>In most cases, all you will need to do is adjust the signature from <code>Task</code> to <code>ValueTask</code>.</p><p>For example, to migrate jobs:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token comment">// 3.x</span></span>
<span class="line"><span class="token keyword">public</span> <span class="token keyword">async</span> <span class="token return-type class-name">Task</span> <span class="token function">Execute</span><span class="token punctuation">(</span><span class="token class-name">IJobExecutionContext</span> context<span class="token punctuation">)</span></span>
<span class="line"></span>
<span class="line"><span class="token comment">// 4.x</span></span>
<span class="line"><span class="token keyword">public</span> <span class="token keyword">async</span> <span class="token return-type class-name">ValueTask</span> <span class="token function">Execute</span><span class="token punctuation">(</span><span class="token class-name">IJobExecutionContext</span> context<span class="token punctuation">,</span> <span class="token class-name">CancellationToken</span> cancellationToken <span class="token operator">=</span> <span class="token keyword">default</span><span class="token punctuation">)</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>Execute</code> also gained a <code>CancellationToken</code>. See <a href="#jobs-take-a-cancellationtoken">Jobs take a CancellationToken</a> below for why, and for what to do with it.</p><p>Listeners are the one place where a missed <code>Task</code> → <code>ValueTask</code> does not fail the build. Every member of <code>IJobListener</code>, <code>ITriggerListener</code> and <code>ISchedulerListener</code> has a default implementation, so a 3.x signature still compiles and simply stops implementing anything — the default runs, and the method is never called. Quartz refuses a listener in that shape when it is registered, naming the member; see <a href="#the-compiler-will-not-point-at-the-callbacks-you-have-to-change-but-the-registration-will">The compiler will not point at the callbacks you have to change, but the registration will</a>.</p><div class="hint-container warning"><p class="hint-container-title">Warning</p><p>The following operations should never be performed on a <code>ValueTask&lt;TResult&gt;</code> instance:</p><ul><li>Awaiting the instance multiple times.</li><li>Calling <code>AsTask</code> multiple times.</li><li>Using <code>.Result</code> or <code>.GetAwaiter().GetResult()</code> when the operation hasn&#39;t yet completed, or using them multiple times.</li><li>Using more than one of these techniques to consume the instance.</li></ul><p>If you need <code>Task</code> semantics (e.g., to await multiple times), call <code>.AsTask()</code> on the <code>ValueTask</code> once and work with the resulting <code>Task</code>.</p></div><p>For more information on <code>ValueTask</code> please see <a href="https://learn.microsoft.com/en-us/dotnet/api/system.threading.tasks.valuetask-1" target="_blank" rel="noopener noreferrer">Microsoft docs</a>.</p><h2 id="systemtime-replaced-with-timeprovider" tabindex="-1"><a class="header-anchor" href="#systemtime-replaced-with-timeprovider"><span>SystemTime Replaced with TimeProvider</span></a></h2><p><code>SystemTime</code> has been removed. To provide a custom time source (e.g., for testing), inject a <code>TimeProvider</code> via configuration:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token comment">// 3.x</span></span>
<span class="line">SystemTime<span class="token punctuation">.</span>UtcNow <span class="token operator">=</span> <span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">=&gt;</span> <span class="token keyword">new</span> <span class="token constructor-invocation class-name">DateTimeOffset</span><span class="token punctuation">(</span><span class="token number">2025</span><span class="token punctuation">,</span> <span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">0</span><span class="token punctuation">,</span> <span class="token number">0</span><span class="token punctuation">,</span> <span class="token number">0</span><span class="token punctuation">,</span> TimeSpan<span class="token punctuation">.</span>Zero<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line"><span class="token comment">// 4.x — use TimeProvider</span></span>
<span class="line"><span class="token class-name">IScheduler</span> scheduler <span class="token operator">=</span> <span class="token keyword">await</span> QuartzSchedulerBuilder</span>
<span class="line">    <span class="token punctuation">.</span><span class="token function">Create</span><span class="token punctuation">(</span>q <span class="token operator">=&gt;</span> q<span class="token punctuation">.</span><span class="token function">UseTimeProvider</span><span class="token punctuation">(</span><span class="token keyword">new</span> <span class="token constructor-invocation class-name">FakeTimeProvider</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">.</span><span class="token function">BuildScheduler</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Under a host, the same call goes on the <code>AddQuartz</code> builder:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line">services<span class="token punctuation">.</span><span class="token function">AddQuartz</span><span class="token punctuation">(</span>q <span class="token operator">=&gt;</span> q<span class="token punctuation">.</span><span class="token function">UseTimeProvider</span><span class="token punctuation">(</span><span class="token keyword">new</span> <span class="token constructor-invocation class-name">FakeTimeProvider</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><h3 id="the-clock-is-a-construction-parameter-which-is-an-api-shape-change" tabindex="-1"><a class="header-anchor" href="#the-clock-is-a-construction-parameter-which-is-an-api-shape-change"><span>The clock is a construction parameter, which is an API-shape change</span></a></h3>`,127),e(`p`,null,[l[193]||=e(`code`,null,`SystemTime.UtcNow`,-1),l[194]||=r(` was a public mutable field: anything could assign it, from anywhere, at any time, and every scheduler in the process saw the result. `,-1),l[195]||=e(`code`,null,`TimeProvider`,-1),l[196]||=r(` is handed to a scheduler when it is built. That is the point — but it means anything of yours that `,-1),l[197]||=e(`em`,null,`wraps`,-1),l[198]||=r(` scheduler construction has to grow a way to be told which clock to use, because there is no longer a hook to reach in afterwards. Builders are the same story: a trigger built by `,-1),l[199]||=e(`code`,null,`TriggerBuilder.Create()`,-1),l[200]||=r(` with no argument reads `,-1),l[201]||=e(`code`,null,`TimeProvider.System`,-1),l[202]||=r(`, so a component that builds triggers outside the container needs the clock passed to it. See `,-1),c(g,{to:`/documentation/quartz-4.x/tutorial/time-and-timeprovider.html#the-trap-triggers-built-outside-the-container`},{default:i(()=>[...l[192]||=[r(`The trap: triggers built outside the container`,-1)]]),_:1}),l[203]||=r(`.`,-1)]),l[869]||=t(`<h3 id="replacing-a-systemtime-offset-not-a-freeze" tabindex="-1"><a class="header-anchor" href="#replacing-a-systemtime-offset-not-a-freeze"><span>Replacing a <code>SystemTime</code> <em>offset</em>, not a freeze</span></a></h3><p><code>FakeTimeProvider</code> — from <code>Microsoft.Extensions.TimeProvider.Testing</code> — is the right answer when a test owns the whole clock: it starts stopped and only moves when you advance it. It is the wrong answer when something else in the process still computes from the real clock, because the two then disagree by however long the test has been running, and it cannot be moved backwards at all.</p><p>3.x code that assigned an <em>offset</em> — <code>SystemTime.UtcNow = () =&gt; DateTimeOffset.UtcNow + skew</code> — wants a clock that still runs, and that is about thirty lines:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token doc-comment comment">/// <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>summary</span><span class="token punctuation">&gt;</span></span></span></span>
<span class="line"><span class="token doc-comment comment">/// A clock that runs at the system&#39;s speed, shifted by an offset that can be moved at will —</span></span>
<span class="line"><span class="token doc-comment comment">/// forwards or backwards — without stopping.</span></span>
<span class="line"><span class="token doc-comment comment">/// <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>summary</span><span class="token punctuation">&gt;</span></span></span></span>
<span class="line"><span class="token keyword">public</span> <span class="token keyword">sealed</span> <span class="token keyword">class</span> <span class="token class-name">OffsetTimeProvider</span><span class="token punctuation">(</span><span class="token class-name">TimeProvider</span> inner<span class="token punctuation">)</span> <span class="token punctuation">:</span> TimeProvider</span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    <span class="token keyword">private</span> <span class="token class-name"><span class="token keyword">long</span></span> offsetTicks<span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line">    <span class="token keyword">public</span> <span class="token return-type class-name">TimeSpan</span> Offset</span>
<span class="line">    <span class="token punctuation">{</span></span>
<span class="line">        <span class="token keyword">get</span> <span class="token operator">=&gt;</span> TimeSpan<span class="token punctuation">.</span><span class="token function">FromTicks</span><span class="token punctuation">(</span>Interlocked<span class="token punctuation">.</span><span class="token function">Read</span><span class="token punctuation">(</span><span class="token keyword">ref</span> offsetTicks<span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">        <span class="token keyword">set</span> <span class="token operator">=&gt;</span> Interlocked<span class="token punctuation">.</span><span class="token function">Exchange</span><span class="token punctuation">(</span><span class="token keyword">ref</span> offsetTicks<span class="token punctuation">,</span> <span class="token keyword">value</span><span class="token punctuation">.</span>Ticks<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">    <span class="token punctuation">}</span></span>
<span class="line"></span>
<span class="line">    <span class="token keyword">public</span> <span class="token keyword">override</span> <span class="token return-type class-name">DateTimeOffset</span> <span class="token function">GetUtcNow</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">=&gt;</span> inner<span class="token punctuation">.</span><span class="token function">GetUtcNow</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">+</span> Offset<span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line">    <span class="token keyword">public</span> <span class="token keyword">override</span> <span class="token return-type class-name">TimeZoneInfo</span> LocalTimeZone <span class="token operator">=&gt;</span> inner<span class="token punctuation">.</span>LocalTimeZone<span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line">    <span class="token keyword">public</span> <span class="token keyword">override</span> <span class="token return-type class-name"><span class="token keyword">long</span></span> <span class="token function">GetTimestamp</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">=&gt;</span> inner<span class="token punctuation">.</span><span class="token function">GetTimestamp</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line">    <span class="token keyword">public</span> <span class="token keyword">override</span> <span class="token return-type class-name"><span class="token keyword">long</span></span> TimestampFrequency <span class="token operator">=&gt;</span> inner<span class="token punctuation">.</span>TimestampFrequency<span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line">    <span class="token comment">// Left to the real clock deliberately: a timer that only fires when something advances the</span></span>
<span class="line">    <span class="token comment">// offset would deadlock every wait inside the scheduler.</span></span>
<span class="line">    <span class="token keyword">public</span> <span class="token keyword">override</span> <span class="token return-type class-name">ITimer</span> <span class="token function">CreateTimer</span><span class="token punctuation">(</span><span class="token class-name">TimerCallback</span> callback<span class="token punctuation">,</span> <span class="token class-name"><span class="token keyword">object</span><span class="token punctuation">?</span></span> state<span class="token punctuation">,</span> <span class="token class-name">TimeSpan</span> dueTime<span class="token punctuation">,</span> <span class="token class-name">TimeSpan</span> period<span class="token punctuation">)</span></span>
<span class="line">        <span class="token operator">=&gt;</span> inner<span class="token punctuation">.</span><span class="token function">CreateTimer</span><span class="token punctuation">(</span>callback<span class="token punctuation">,</span> state<span class="token punctuation">,</span> dueTime<span class="token punctuation">,</span> period<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token class-name">OffsetTimeProvider</span> clock <span class="token operator">=</span> <span class="token keyword">new</span><span class="token punctuation">(</span>TimeProvider<span class="token punctuation">.</span>System<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">services<span class="token punctuation">.</span><span class="token function">AddQuartz</span><span class="token punctuation">(</span>q <span class="token operator">=&gt;</span> q<span class="token punctuation">.</span><span class="token function">UseTimeProvider</span><span class="token punctuation">(</span>clock<span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line"><span class="token comment">// ... and later, from the test or the diagnostic endpoint that owns it:</span></span>
<span class="line">clock<span class="token punctuation">.</span>Offset <span class="token operator">=</span> TimeSpan<span class="token punctuation">.</span><span class="token function">FromHours</span><span class="token punctuation">(</span><span class="token number">26</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div>`,5),e(`p`,null,[l[205]||=e(`code`,null,`CreateTimer`,-1),l[206]||=r(` deliberately delegates to the real clock. A timer that only fires when something advances the offset would stall every wait inside the scheduler — which is exactly what `,-1),l[207]||=e(`code`,null,`FakeTimeProvider`,-1),l[208]||=r(` does, and why advancing it is not enough on its own. When you `,-1),l[209]||=e(`em`,null,`do`,-1),l[210]||=r(` want a frozen clock, advance it and then wait for the scheduler to react rather than sleeping; see `,-1),c(g,{to:`/documentation/quartz-4.x/tutorial/testing.html#controlling-time`},{default:i(()=>[...l[204]||=[r(`Controlling time`,-1)]]),_:1}),l[211]||=r(`.`,-1)]),l[870]||=t(`<h3 id="a-clock-belongs-to-one-scheduler" tabindex="-1"><a class="header-anchor" href="#a-clock-belongs-to-one-scheduler"><span>A clock belongs to one scheduler</span></a></h3><p><code>UseTimeProvider</code> used to replace the container&#39;s <code>TimeProvider</code> registration, so calling it on one named scheduler re-timed every scheduler in the container. It now registers at the scheduler&#39;s own slot, and one scheduler can be driven by a fake clock while the rest keep real time:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line">services<span class="token punctuation">.</span><span class="token function">AddQuartz</span><span class="token punctuation">(</span><span class="token string">&quot;reporting&quot;</span><span class="token punctuation">,</span> q <span class="token operator">=&gt;</span> q<span class="token punctuation">.</span><span class="token function">UseTimeProvider</span><span class="token punctuation">(</span><span class="token keyword">new</span> <span class="token constructor-invocation class-name">FakeTimeProvider</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">services<span class="token punctuation">.</span><span class="token function">AddQuartz</span><span class="token punctuation">(</span><span class="token string">&quot;billing&quot;</span><span class="token punctuation">,</span> q <span class="token operator">=&gt;</span> …<span class="token punctuation">)</span><span class="token punctuation">;</span>   <span class="token comment">// still on the system clock</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>A scheduler that was not given a clock of its own asks the container, so an application-wide <code>TimeProvider</code> registration is inherited by every scheduler exactly as it was. In full, most specific first:</p><table><thead><tr><th>Where the clock comes from</th><th>Beats</th></tr></thead><tbody><tr><td><code>UseTimeProvider(...)</code> on that scheduler&#39;s builder</td><td>everything below</td></tr><tr><td>a <code>TimeProvider</code> registered in the container</td><td>the key and the default</td></tr><tr><td><code>quartz.timeProvider.type</code></td><td>the default</td></tr><tr><td><code>TimeProvider.System</code></td><td>—</td></tr></tbody></table><p>The third row is the fix to a precedence inversion: <code>quartz.timeProvider.type</code> was applied by replacing the registration <em>after</em> the configuration callback had run, so a leftover key in a configuration file silently overrode the clock the application had chosen in code. It is now tried rather than replaced, like every other implementation a flat key names — code beats strings in both directions, opposite orders notwithstanding.</p><p>Triggers built by <code>q.AddTrigger(...)</code> and <code>q.ScheduleJob(...)</code> are built with their scheduler&#39;s clock, so a trigger given no start time starts at the time that scheduler thinks it is.</p><h3 id="the-scanning-jobs-take-a-clock-and-speak-datetimeoffset" tabindex="-1"><a class="header-anchor" href="#the-scanning-jobs-take-a-clock-and-speak-datetimeoffset"><span>The scanning jobs take a clock and speak <code>DateTimeOffset</code></span></a></h3><p><code>DirectoryScanJob</code> and <code>FileScanJob</code> compare a file&#39;s last write time against &quot;now&quot; to decide whether a file has settled enough to report. That &quot;now&quot; came from the system clock directly, which no test could move. Both take a <code>TimeProvider</code> now:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token keyword">public</span> <span class="token function">DirectoryScanJob</span><span class="token punctuation">(</span><span class="token class-name">TimeProvider<span class="token punctuation">?</span></span> timeProvider <span class="token operator">=</span> <span class="token keyword">null</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"><span class="token keyword">public</span> <span class="token function">DirectoryScanJob</span><span class="token punctuation">(</span><span class="token class-name">IServiceProvider</span> serviceProvider<span class="token punctuation">,</span> <span class="token class-name">TimeProvider<span class="token punctuation">?</span></span> timeProvider <span class="token operator">=</span> <span class="token keyword">null</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"><span class="token keyword">public</span> <span class="token function">FileScanJob</span><span class="token punctuation">(</span><span class="token class-name">TimeProvider<span class="token punctuation">?</span></span> timeProvider <span class="token operator">=</span> <span class="token keyword">null</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The job factory hands them the <code>TimeProvider</code> in the container — including a scheduler&#39;s own, per the table above. The parameter is optional so that <code>new DirectoryScanJob()</code> still compiles, and <code>null</code> means <code>TimeProvider.System</code>.</p><p>The times they work in are <code>DateTimeOffset</code> rather than local <code>DateTime</code>, which is the same instant said unambiguously, and is what the rest of the API has spoken since 3.0:</p><table><thead><tr><th>3.x / earlier 4.0 preview</th><th>4.0</th></tr></thead><tbody><tr><td><code>protected virtual DateTime FileScanJob.GetLastModifiedDate(string)</code>, returning <code>DateTime.MinValue</code> for a missing file</td><td><code>protected virtual DateTimeOffset? GetLastModifiedTime(string fileName)</code>, returning <code>null</code> for a missing file</td></tr><tr><td><code>protected void DirectoryScanJob.GetUpdatedOrNewFiles(string, DateTime, DateTime, IReadOnlyCollection&lt;FileInfo&gt;, out List&lt;FileInfo&gt;, out List&lt;FileInfo&gt;, out List&lt;FileInfo&gt;, string, bool)</code></td><td>private, along with the <code>DirectoryScanResult</code> it returns</td></tr></tbody></table><p><code>GetUpdatedOrNewFiles</code> was <code>protected</code> but never <code>virtual</code>, and <code>DirectoryScanJob.Execute</code> is not virtual either, so no subclass could take part in the scan or do anything with a result it computed. <code>IDirectoryScanListener</code> is the seam, and it is handed the files themselves.</p><p>The <code>LAST_MODIFIED_TIME</code> entry these jobs keep in their own job data is written as a <code>DateTimeOffset</code> now. A <code>DateTime</code> written by an earlier version is still read, as the instant it denoted, so an upgraded scheduler does not re-report every file it has already seen.</p><h3 id="the-shipped-jobs-are-configured-by-name" tabindex="-1"><a class="header-anchor" href="#the-shipped-jobs-are-configured-by-name"><span>The shipped jobs are configured by name</span></a></h3><p>Every job in <code>Quartz.Jobs</code> was configured by <code>const string</code> keys read out of the merged <code>JobDataMap</code>, which is a configuration model with no compiler in it: the key can be misspelled, the value can be of the wrong type, and a setting the job honours can simply be missing from the documentation — as <code>SEARCH_PATTERN</code> and <code>INCLUDE_SUB_DIRECTORIES</code> were, <code>internal const</code>s that <code>DirectoryScanJob</code> read on every fire.</p><p>Each job now has an options record that maps onto its keys in one place, and an extension that writes it:</p><table><thead><tr><th>Job</th><th>Options</th><th>Extension</th></tr></thead><tbody><tr><td><code>DirectoryScanJob</code></td><td><code>DirectoryScanOptions</code></td><td><code>UsingDirectoryScanOptions(…)</code></td></tr><tr><td><code>FileScanJob</code></td><td><code>FileScanOptions</code></td><td><code>UsingFileScanOptions(…)</code></td></tr><tr><td><code>NativeJob</code></td><td><code>NativeJobOptions</code></td><td><code>UsingNativeJobOptions(…)</code></td></tr><tr><td><code>SendMailJob</code></td><td><code>SendMailOptions</code></td><td><code>UsingSendMailOptions(…)</code></td></tr></tbody></table><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line"> IJobDetail job = JobBuilder.Create&lt;DirectoryScanJob&gt;()</span>
<span class="line"></span><span class="token prefix unchanged"> </span><span class="token line">     .WithIdentity(&quot;inboxScan&quot;)</span>
<span class="line"></span></span><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line">     .UsingJobData(DirectoryScanJob.DirectoryNames, &quot;/var/spool/inbox&quot;)</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line">     .UsingJobData(DirectoryScanJob.DirectoryScanListenerName, nameof(InboxListener))</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line">     .UsingJobData(&quot;SEARCH_PATTERN&quot;, &quot;*.csv&quot;)</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line">     .UsingJobData(DirectoryScanJob.MinimumUpdateAge, 30000L)</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line">     .UsingDirectoryScanOptions(new DirectoryScanOptions</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">     {</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">         Directories = [&quot;/var/spool/inbox&quot;],</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">         ScanListenerName = nameof(InboxListener),</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">         SearchPattern = &quot;*.csv&quot;,</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">         MinimumUpdateAge = TimeSpan.FromSeconds(30),</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">     })</span>
<span class="line"></span></span><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line">     .Build();</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Nothing about what is stored changes. The extensions write the same keys, <code>FromJobData</code> reads them back, and a job scheduled by 3.x reads identically — including a value a job store in <code>StoreJobDataAsStrings</code> mode left behind as a string. Configuring key by key still works, and <code>SEARCH_PATTERN</code> and <code>INCLUDE_SUB_DIRECTORIES</code> are <code>public const</code>s now, so the literal is no longer the only way to reach them. <code>MinimumUpdateAge</code> is a <code>TimeSpan</code> in the options and a millisecond count in the map, as it has always been.</p><p>The extensions are generic in the configurator, so both configuration surfaces keep their type: a <code>JobBuilder&lt;TJob&gt;</code> chain still ends in <code>Build()</code>, and the <code>IJobConfigurator&lt;TJob&gt;</code> that <code>AddJob</code> hands you still chains its own members.</p><h3 id="directoryscanjob-finds-its-listener-among-what-you-registered" tabindex="-1"><a class="header-anchor" href="#directoryscanjob-finds-its-listener-among-what-you-registered"><span><code>DirectoryScanJob</code> finds its listener among what you registered</span></a></h3><p>The <code>DIRECTORY_SCAN_LISTENER_NAME</code> key used to be resolved by sweeping <strong>every loaded assembly</strong> with <code>GetTypes()</code> on the first miss, matching the simple type name from any of them, and remembering the answer — misses included — in a process-lifetime dictionary keyed on that name. The name is job data, so a job data map could grow an unbounded cache and pay for a full reflection sweep per distinct value, and a same-named type from an assembly you did not mean would win.</p><p>Three bounded lookups replace it, in order:</p><table><thead><tr><th>How you register the listener</th><th>What to put in <code>ScanListenerName</code></th></tr></thead><tbody><tr><td><code>AddKeyedSingleton&lt;IDirectoryScanListener&gt;(&quot;inbox&quot;, …)</code></td><td><code>&quot;inbox&quot;</code></td></tr><tr><td><code>AddSingleton&lt;IDirectoryScanListener, InboxListener&gt;()</code></td><td><code>nameof(InboxListener)</code></td></tr><tr><td><code>scheduler.Context[&quot;inbox&quot;] = listener</code></td><td><code>&quot;inbox&quot;</code></td></tr></tbody></table><p><strong><code>AddSingleton&lt;InboxListener&gt;()</code> alone no longer resolves</strong> — register it under the interface, or key it. The failure names the listener and all three places the job looked.</p><h3 id="directoryscanjob-stores-its-file-list-as-something-a-job-store-can-write" tabindex="-1"><a class="header-anchor" href="#directoryscanjob-stores-its-file-list-as-something-a-job-store-can-write"><span><code>DirectoryScanJob</code> stores its file list as something a job store can write</span></a></h3><p>The job is <code>[PersistJobDataAfterExecution]</code> and kept its previous scan under <code>CURRENT_FILE_LIST</code> as a <code>List&lt;FileInfo&gt;</code> — a value neither shipped serializer can read back, and one both refuse outright. So the job&#39;s first firing against a persistent store failed to persist, and <code>GET …/jobs/{group}/{name}</code> refused the map. It is a <code>Dictionary&lt;string, string&gt;</code> of full path to last-write ticks now, which is one of the shapes both admit. A <code>List&lt;FileInfo&gt;</code> left in a running scheduler&#39;s in-memory map is still read.</p><h3 id="the-smtp-password-does-not-belong-in-job-data" tabindex="-1"><a class="header-anchor" href="#the-smtp-password-does-not-belong-in-job-data"><span>The SMTP password does not belong in job data</span></a></h3><p><code>SendMailJob</code> read its credential from the <code>smtp_username</code> and <code>smtp_password</code> job data entries. Job data is durable: a persistent job store writes it to <code>QRTZ_JOB_DETAILS</code>, every node in the cluster reads it, the dashboard shows it, and a support-bundle export of that table carries it. <code>SendMailJob</code> takes the credential from the container instead:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token class-name">CredentialCache</span> credentials <span class="token operator">=</span> <span class="token keyword">new</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">credentials<span class="token punctuation">.</span><span class="token function">Add</span><span class="token punctuation">(</span><span class="token string">&quot;smtp.example.com&quot;</span><span class="token punctuation">,</span> <span class="token number">587</span><span class="token punctuation">,</span> <span class="token string">&quot;Basic&quot;</span><span class="token punctuation">,</span> <span class="token keyword">new</span> <span class="token constructor-invocation class-name">NetworkCredential</span><span class="token punctuation">(</span><span class="token string">&quot;mailer&quot;</span><span class="token punctuation">,</span> smtpPassword<span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">services<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">AddSingleton</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>ICredentialsByHost<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span>credentials<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><strong>A <code>CredentialCache</code> bound to the server, rather than a bare <code>NetworkCredential</code>, and that is a rule rather than a suggestion.</strong> <code>smtp_host</code> is job data, so the host to authenticate to is chosen by whoever scheduled the job; a <code>NetworkCredential</code> answers <code>ICredentialsByHost.GetCredential</code> with itself for <em>every</em> host, so the two together hand the registered login to whatever host that job data names. The job now asks the registered credential for the host it is about to connect to, and:</p><table><thead><tr><th>What you register</th><th>A job pointed at a host it covers</th><th>A job pointed at any other host</th></tr></thead><tbody><tr><td><code>CredentialCache</code> with an entry for the host</td><td>authenticates with that entry</td><td>sends unauthenticated</td></tr><tr><td>bare <code>NetworkCredential</code></td><td>—</td><td><strong>refuses to send</strong>, naming the host and <code>CredentialCache</code></td></tr></tbody></table><p><code>SendMailOptions</code> deliberately has no user name or password on it. The two job data keys are still read when nothing is registered, so a job scheduled by an earlier version keeps sending — that path is unaffected, since whoever wrote the user name wrote the host beside it — and the job logs a warning saying where the credential now lives. A credential from the container wins over one in job data.</p><p><code>SendMailOptions.EnableSsl</code> (<code>smtp_enable_ssl</code>) is new, and <code>false</code> by default, which is <code>SmtpClient</code>&#39;s own default. Turn it on for anything that authenticates: SMTP <code>AUTH LOGIN</code> is base64, not encryption.</p><h3 id="nativejob-no-longer-redirects-a-stream-nobody-reads" tabindex="-1"><a class="header-anchor" href="#nativejob-no-longer-redirects-a-stream-nobody-reads"><span><code>NativeJob</code> no longer redirects a stream nobody reads</span></a></h3><p><code>NativeJob</code> redirected both of the child&#39;s streams whatever <code>ConsumeStreams</code> said, while the consumer threads were started only when it was on. With the defaults — <code>ConsumeStreams</code> off, <code>WaitForProcess</code> on — a process that wrote more than a pipe buffer holds blocked on its own write, and the synchronous <code>WaitForExit</code> held a Quartz worker thread for as long as that lasted, which was for ever. Nothing is redirected unless something is reading it, and the wait is <code>WaitForExitAsync(cancellationToken)</code>, so a shutdown reaches the job.</p><p><code>NativeJob.Execute</code> is <code>async</code> now; nothing about the override points changed.</p><table><thead><tr><th>3.x</th><th>4.x</th></tr></thead><tbody><tr><td><code>SendMailJob()</code></td><td><code>SendMailJob(ICredentialsByHost? credentials = null)</code>, which the job factory fills from the container</td></tr><tr><td><code>MailInfo.SmtpUserName</code> / <code>MailInfo.SmtpPassword</code></td><td><code>MailInfo.Credentials</code>, a <code>NetworkCredential?</code> resolved for <code>MailInfo.SmtpHost</code>. An override of <code>Send</code> routing mail through another transport gets whichever credential applied</td></tr><tr><td><code>protected virtual MailMessage BuildMessageFromParameters(JobDataMap data)</code></td><td><code>protected virtual MailMessage BuildMessage(SendMailOptions options)</code> — the same override point, reading a value instead of a bag</td></tr><tr><td><code>protected virtual string GetRequiredParameter(JobDataMap, string)</code>, <code>GetOptionalParameter(JobDataMap, string)</code></td><td>removed; <code>SendMailOptions.FromJobData</code> is the one reader, and it reports the same missing key</td></tr></tbody></table><h2 id="logging" tabindex="-1"><a class="header-anchor" href="#logging"><span>Logging</span></a></h2><p>LibLog has been replaced with <code>Microsoft.Extensions.Logging.Abstractions</code>. Reconfigure logging using an <code>ILoggerFactory</code>. Example with a simple console logger:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token class-name"><span class="token keyword">var</span></span> loggerFactory <span class="token operator">=</span> LoggerFactory<span class="token punctuation">.</span><span class="token function">Create</span><span class="token punctuation">(</span>builder <span class="token operator">=&gt;</span></span>
<span class="line">    <span class="token punctuation">{</span></span>
<span class="line">        builder</span>
<span class="line">            <span class="token punctuation">.</span><span class="token function">SetMinimumLevel</span><span class="token punctuation">(</span>LogLevel<span class="token punctuation">.</span>Debug<span class="token punctuation">)</span></span>
<span class="line">            <span class="token punctuation">.</span><span class="token function">AddSimpleConsole</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">    <span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">LogProvider<span class="token punctuation">.</span><span class="token function">SetLogProvider</span><span class="token punctuation">(</span>loggerFactory<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The console tour&#39;s <a href="https://github.com/quartznet/quartznet/blob/main/src/Quartz.Examples/Logging.cs" target="_blank" rel="noopener noreferrer"><code>Logging.cs</code></a> sets up <a href="https://serilog.net/" target="_blank" rel="noopener noreferrer">Serilog</a>, NLog and Microsoft.Logging behind Quartz, all three in one file.</p><p>The example above is the standalone shape. <strong>Under a host there is nothing to do</strong>: <code>AddQuartz</code> registers the scheduler&#39;s parts in your container, and they are injected the host&#39;s <code>ILoggerFactory</code> like every other service. A <code>LogProvider.SetLogProvider</code> call is not needed to see the scheduler, its loop, its job store, its cluster manager or its thread pool — only for the handful of types nothing can inject, which the next section lists.</p><p>Further information on configuring Microsoft.Logging can be found <a href="https://docs.microsoft.com/en-us/dotnet/core/extensions/logging" target="_blank" rel="noopener noreferrer">at Microsoft docs</a>.</p><h3 id="every-message-carries-an-event-id" tabindex="-1"><a class="header-anchor" href="#every-message-carries-an-event-id"><span>Every message carries an event id</span></a></h3><p>Every shipped package logs through source-generated <code>[LoggerMessage]</code> methods rather than <code>logger.LogInformation(…)</code> and its siblings. Two things follow. Nothing is formatted or boxed when the level is off, including on the scheduling loop. And every message has a stable event id, so an operator can filter or alert on the event rather than on its text.</p><p>Ids are allocated in ranges by area, and are stable from 4.0 onwards:</p><table><thead><tr><th>Range</th><th>Area</th></tr></thead><tbody><tr><td>1000–1999</td><td>Scheduler core — the scheduler, its firing loop, the job run shell, the signaler, the error listener</td></tr><tr><td>2000–2999</td><td><code>RAMJobStore</code></td></tr><tr><td>3000–3499</td><td>ADO.NET store, its connections and its driver delegate</td></tr><tr><td>3500–3599</td><td>Clustering — check-in, failed-instance detection and recovery</td></tr><tr><td>3600–3699</td><td>Misfire handling</td></tr><tr><td>3700–3799</td><td>Lock handlers</td></tr><tr><td>4000–4999</td><td>Configuration, dependency injection and hosting, including the thread pools and job factories</td></tr><tr><td>5000–5999</td><td>Serialization, type loading, triggers, calendars, XML scheduling data and the utilities</td></tr><tr><td>6000–6199</td><td>The history plugins — <code>LoggingJobHistoryPlugin</code> and <code>LoggingTriggerHistoryPlugin</code></td></tr><tr><td>6200–6299</td><td>The XML scheduling data plugin</td></tr><tr><td>6300–6399</td><td>The JSON scheduling data plugin and its processor</td></tr><tr><td>7000–7399</td><td><code>Quartz.Jobs</code> — the directory scan job (7000–7099), the file scan job (7100–7199), the native job (7200–7299) and the send mail job (7300–7399)</td></tr><tr><td>8000–8099</td><td><code>Quartz.Extensions.Redis</code> — the Redis lock handler</td></tr><tr><td>9000–9099</td><td><code>Quartz.AspNetCore</code> — the HTTP API</td></tr></tbody></table><h3 id="each-scheduling-path-logs-under-its-own-category" tabindex="-1"><a class="header-anchor" href="#each-scheduling-path-logs-under-its-own-category"><span>Each scheduling path logs under its own category</span></a></h3><p>Jobs and triggers can be declared three ways — in a scheduling file, in a JSON scheduling file, or with <code>AddJob</code>/<code>AddTrigger</code> inside <code>AddQuartz</code> — and one class does the work for all three. It used to log for all three as well: <code>ContainerConfigurationProcessor</code> and <code>JsonSchedulingDataProcessor</code> both handed <code>XmlSchedulingDataProcessor</code> an <code>ILogger&lt;XmlSchedulingDataProcessor&gt;</code>, so <code>Adding 2 jobs, 2 triggers</code> arrived under <code>Quartz.Xml.XmlSchedulingDataProcessor</code> whichever path put them there.</p><p>Each path now logs under itself:</p><table><thead><tr><th>What declared them</th><th>Category</th></tr></thead><tbody><tr><td>a scheduling file</td><td><code>Quartz.Xml.XmlSchedulingDataProcessor</code></td></tr><tr><td><code>AddQuartz(q =&gt; q.AddJob…)</code></td><td><code>Quartz.Configuration.ContainerConfigurationProcessor</code></td></tr><tr><td>a JSON scheduling file</td><td><code>Quartz.Plugins.Json.JsonSchedulingDataProcessor</code></td></tr></tbody></table><p>The <strong>event ids are unchanged</strong>, because it is the same event from a different source — so a filter or an alert keyed on the id keeps matching all three, and one keyed on the category can now tell them apart. A logging filter that silenced <code>Quartz.Xml</code> to quiet the container path no longer does, which is the point.</p><p>6400–6599 was the interrupt monitor and the shutdown hook. Both plugins are retired, so no 4.0 assembly raises an event in that range — a job&#39;s timeout is middleware in the core package and logs under the scheduler&#39;s own range, as <code>1090</code>–<code>1092</code>. See <a href="#jobinterruptmonitorplugin-is-retired-a-job-timeout-is-middleware"><code>JobInterruptMonitorPlugin</code> is retired; a job timeout is middleware</a>.</p><p>Levels and message templates are otherwise <strong>unchanged from 3.x</strong>, so a log query that matched a message before still matches it. The exceptions:</p><ul><li><code>JobStoreSupport.LogWarnIfNonZero</code> wrote the cluster recovery counts at Information when the count was non-zero and at Debug when it was zero, whatever its name said. Those six messages are Warning events now, raised only when the count is non-zero. If you filtered them at Information, filter them at Warning.</li><li>Four messages carried a typo, corrected while their ids were new: <code>Removed {Count} &#39;complete&#39; triggers.</code> lost a double space, <code>complete triggers(s)</code> became <code>complete trigger(s)</code>, <code>Found {TriggerGroupDeleteCount}delete trigger group commands.</code> gained the missing space, and the message about a trigger that already existed — spelled with double spaces in one place and single spaces in the other — is one event with the single-space spelling. A query matching those on text needs updating; one matching on event id does not, which is rather the point.</li><li><code>NativeJob</code> relayed each line of the spawned process&#39;s output through one template, <code>{Type}&gt;{Line}</code>, with <code>Type</code> holding <code>stdout</code> or <code>stderr</code>. It is now one event per stream — <code>stdout&gt;{Line}</code> at Information (7201) and <code>stderr&gt;{Line}</code> at Warning (7202) — so the text a sink renders is unchanged and which stream a line came from is an event id rather than a property. A structured sink that indexed the <code>Type</code> property no longer receives one; filter on the id instead. The levels are the ones the two streams already logged at.</li></ul><p><code>LoggingJobHistoryPlugin</code> and <code>LoggingTriggerHistoryPlugin</code> are a special case, because the message they log is a template <em>you</em> configure — <code>JobSuccessMessage</code> and its siblings, with <code>{0}</code>-style placeholders — and a template only known at run time cannot be a compile-time one. They format it exactly as before and pass the result through an event whose own template is <code>{Message}</code>: the text a sink renders is unchanged, and each occurrence has an id of its own (6000–6003 for the job plugin, 6010–6012 for the trigger plugin). <code>StructuredLoggingJobHistoryPlugin</code> and <code>StructuredLoggingTriggerHistoryPlugin</code> keep plain <code>ILogger</code> calls and have no event ids, because their configured templates carry <em>named</em> placeholders that a structured sink resolves into properties for itself — which is the whole reason those two plugins exist, and which no fixed template can preserve.</p><h2 id="the-ambient-logger-factory-stays-ambient" tabindex="-1"><a class="header-anchor" href="#the-ambient-logger-factory-stays-ambient"><span>The ambient logger factory stays ambient</span></a></h2><p><code>LogProvider.SetLogProvider(ILoggerFactory)</code> is the one piece of mutable process-wide state left in Quartz, and it is deliberate rather than overlooked.</p><p>Everything the scheduler is made of is built by a container and is injected an <code>ILogger</code> the ordinary way: <code>QuartzScheduler</code> and its scheduling loop, the signaler and the error listener, the ADO.NET job store together with its cluster manager, its misfire handler, its units of work, its driver delegate and its lock handler, and the components <code>Use*&lt;T&gt;()</code> chooses — the thread pool, the job factory, the type loader, the instance id generator. <strong>Under <code>AddQuartz</code>, none of that needs this slot set.</strong></p><p>What is left over cannot be injected anything, and this is the whole of it:</p><table><thead><tr><th>Type</th><th>Why it cannot be injected</th></tr></thead><tbody><tr><td><code>JobChainingJobListener</code></td><td>Constructed by the caller and handed over already built</td></tr><tr><td><code>CronTriggerImpl</code></td><td>A trigger, which may have been deserialized out of a job store rather than constructed at all</td></tr><tr><td><code>TimeZones</code>, <code>MisfireInstructionNames</code>, <code>FileUtil</code>, <code>QuartzEnvironment</code></td><td>Static helpers, reached from parsing and deserialization with no scheduler in scope</td></tr><tr><td>The jobs in <code>Quartz.Jobs</code>, and anything else a caller constructs directly</td><td>Same reason as the listeners</td></tr></tbody></table><p>A type cannot be handed a logger by a container it never meets, so those sites read the ambient factory instead of going unlogged. Setting it is how you see <em>them</em> — not how you see the scheduler.</p><p>A standalone <code>QuartzSchedulerBuilder</code> is the one place where setting it still configures everything. The builder creates a container of its own, which has no logging providers in it unless you register some on <code>builder.Services</code>, so that container&#39;s <code>ILoggerFactory</code> forwards to this slot. Either way of saying it works; registering a provider takes precedence.</p><p>Nor is the slot seeded from the container, which would be the obvious convenience. It outlives any one container: a process that builds a host, disposes it and builds another — every integration test suite, and every application that reloads configuration — would be left holding a disposed <code>ILoggerFactory</code>, and the next logger created anywhere in Quartz would throw <code>ObjectDisposedException</code> from somewhere unrelated to logging. Whoever sets the factory owns its lifetime, and only the application can make that call. The same applies to a hand-written <code>LogProvider.SetLogProvider(host.Services.GetRequiredService&lt;ILoggerFactory&gt;())</code>: it is correct as long as the host outlives the schedulers, and under a host it is now only worth writing for the types in the table above.</p><p><code>TimeZones.AddResolver</code> is ambient for the same reason. <code>FindById</code> is reached from parsing a <code>CronExpression</code> and from deserializing a trigger out of a job store blob, neither of which has a scheduler in scope — which is why installing <code>Quartz.Plugins.TimeZoneConverter</code> in one scheduler changes id resolution for the whole process, and why each registration is undone by disposing it rather than by anyone owning the slot — see <a href="#timezoneutil-became-quartz-timezones"><code>TimeZoneUtil</code> became <code>Quartz.TimeZones</code></a>.</p><h2 id="job-execution-metrics" tabindex="-1"><a class="header-anchor" href="#job-execution-metrics"><span>Job execution metrics</span></a></h2><div class="hint-container caution"><p class="hint-container-title">EVERY NAME CHANGED</p><p>Every instrument and every attribute Quartz publishes was renamed in 4.0, and two of the four instruments were removed. <strong>Dashboards, alerts and recording rules built on the 3.x names all break.</strong> The <a href="#old-and-new-telemetry-names">old → new table</a> below is the complete mapping; nothing in it is emitted under both names, because two names for one series doubles the cardinality and settles nothing.</p></div><p>Three things were wrong at once. The instruments were named <code>scheduling.quartz.*</code>, which is neither the package&#39;s name nor OpenTelemetry&#39;s convention; the attributes were unprefixed (<code>job.name</code>, <code>trigger.group</code>), so any other instrumented library in the process could claim the same names; and the duration was recorded in milliseconds, into a histogram whose default bucket boundaries assume seconds. Every scheduler also now publishes them at all — configuring the meter used to be wired to <code>StdSchedulerFactory</code>, so a scheduler registered with <code>AddQuartz</code> emitted nothing.</p><table><thead><tr><th>Instrument</th><th>Type</th><th>Unit</th><th>Tags</th></tr></thead><tbody><tr><td><code>quartz.job.execution.duration</code></td><td><code>Histogram&lt;double&gt;</code></td><td><code>s</code></td><td><code>quartz.scheduler.name</code>, <code>quartz.scheduler.id</code>, <code>quartz.trigger.group</code>, <code>quartz.trigger.name</code>, <code>quartz.job.group</code>, <code>quartz.job.name</code>, <code>quartz.execution.group</code> when the trigger names one, <strong>+ <code>error.type</code></strong> when the execution failed</td></tr><tr><td><code>quartz.job.execution.active</code></td><td><code>UpDownCounter&lt;long&gt;</code></td><td><code>{job}</code></td><td>the same identity attributes</td></tr></tbody></table>`,72),e(`p`,null,[l[213]||=r(`Five more instruments cover misfires, acquisition, cluster check-in and recovery, and store round trips; the `,-1),c(g,{to:`/documentation/quartz-4.x/packages/opentelemetry-integration.html#metrics`},{default:i(()=>[...l[212]||=[r(`observability page`,-1)]]),_:1}),l[214]||=r(` has the whole set.`,-1)]),l[871]||=t(`<h3 id="old-and-new-telemetry-names" tabindex="-1"><a class="header-anchor" href="#old-and-new-telemetry-names"><span>Old and new telemetry names</span></a></h3><table><thead><tr><th>3.x</th><th>4.x</th><th>Notes</th></tr></thead><tbody><tr><td><em>(none)</em></td><td><code>quartz.trigger.misfire</code></td><td>New in 4.0. A <code>Counter&lt;long&gt;</code> of firings that were owed and did not happen on time</td></tr><tr><td><em>(none)</em></td><td><code>quartz.trigger.acquisition.duration</code></td><td>New in 4.0. A <code>Histogram&lt;double&gt;</code> of what the scheduling loop waits on its store for</td></tr><tr><td><em>(none)</em></td><td><code>quartz.trigger.acquired</code></td><td>New in 4.0. A <code>Counter&lt;long&gt;</code> of what those rounds came back with</td></tr><tr><td><em>(none)</em></td><td><code>quartz.cluster.checkin.duration</code></td><td>New in 4.0. A <code>Histogram&lt;double&gt;</code> of how long a cluster check-in takes, per attempt</td></tr><tr><td><em>(none)</em></td><td><code>quartz.cluster.recovery.trigger</code></td><td>New in 4.0. A <code>Counter&lt;long&gt;</code> of fired-trigger rows recovered from a failed node</td></tr><tr><td><em>(none)</em></td><td><code>quartz.jobstore.operation.duration</code></td><td>New in 4.0. A <code>Histogram&lt;double&gt;</code> of every round trip to the store, tagged with which operation</td></tr><tr><td><code>scheduling.quartz.execute</code></td><td><em>removed</em></td><td>The histogram&#39;s own <strong>count</strong> is the number of executions: <code>sum(rate(quartz_job_execution_duration_count[5m]))</code> in Prometheus, or whatever your backend calls a histogram&#39;s count</td></tr><tr><td><code>scheduling.quartz.execute.errors</code></td><td><em>removed</em></td><td>The <strong><code>error.type</code>-tagged subset</strong> of the same count is the number of failures — and it now says <em>what</em> failed, which the counter never did</td></tr><tr><td><code>scheduling.quartz.execute.active</code></td><td><code>quartz.job.execution.active</code></td><td>Also an <code>UpDownCounter&lt;long&gt;</code> now (was <code>Counter&lt;long&gt;</code>), unit <code>{job}</code> (was <code>ea</code>)</td></tr><tr><td><code>scheduling.quartz.execute.duration</code></td><td><code>quartz.job.execution.duration</code></td><td><strong>Unit <code>s</code>, not <code>ms</code></strong> — a chart with a hard-coded millisecond axis reads 1000× low until it is changed</td></tr><tr><td><code>job.name</code></td><td><code>quartz.job.name</code></td><td></td></tr><tr><td><code>job.group</code></td><td><code>quartz.job.group</code></td><td></td></tr><tr><td><code>job.type</code></td><td><code>quartz.job.type</code></td><td>Span attribute</td></tr><tr><td><code>trigger.name</code></td><td><code>quartz.trigger.name</code></td><td></td></tr><tr><td><code>trigger.group</code></td><td><code>quartz.trigger.group</code></td><td></td></tr><tr><td><code>scheduler.name</code></td><td><code>quartz.scheduler.name</code></td><td></td></tr><tr><td><code>scheduler.id</code></td><td><code>quartz.scheduler.id</code></td><td>Span attribute</td></tr><tr><td><code>fire.instance.id</code></td><td><code>quartz.fire.instance.id</code></td><td>Span attribute</td></tr><tr><td><code>jobstore.trigger.count</code></td><td><code>quartz.jobstore.trigger.count</code></td><td>Job store span attribute</td></tr><tr><td><code>jobstore.batch.size</code></td><td><code>quartz.jobstore.batch.size</code></td><td>Job store span attribute</td></tr><tr><td><em>(none)</em></td><td><code>quartz.execution.group</code></td><td>New in 4.0. On the execution span and both execution instruments, and only when the trigger names a group</td></tr><tr><td><em>(none)</em></td><td><code>quartz.jobstore.operation</code></td><td>New in 4.0. Which store operation a <code>quartz.jobstore.operation.duration</code> measurement is about</td></tr><tr><td><em>(none)</em></td><td><code>quartz.cluster.recovered.instance.id</code></td><td>New in 4.0. The failed node a <code>quartz.cluster.recovery.trigger</code> measurement is about — not the node reporting it, which is <code>quartz.scheduler.id</code></td></tr><tr><td><code>scheduling.quartz.exception_type</code></td><td><code>error.type</code></td><td>The one attribute that is <em>not</em> namespaced, and its value changed too — see below</td></tr><tr><td><code>Quartz.Job.Vetoed</code></td><td><code>Quartz.Job.Veto</code></td><td>Span name for a vetoed fire. <code>Quartz.Job.Execute</code> is unchanged, and so is every <code>Quartz.JobStore.*</code> name a 3.x span carried — though every store emits those now, not only the ADO.NET one, and there are 33 of them since the group forms of pause and resume <a href="#pausing-by-matcher-is-a-group-operation-and-is-named-for-one">got names of their own</a></td></tr></tbody></table><p>Renaming a series is not something a backend can do for you: a Prometheus recording rule bridging the old name to the new one, or a dashboard variable, is the usual way to keep history readable across the upgrade.</p><p><strong>The constants did not move.</strong> <code>Quartz.Diagnostics.ActivityTags.JobName</code> is still <code>ActivityTags.JobName</code>; its <em>value</em> changed. Code that reads a tag name through the constants compiles and runs unchanged — it is the queries written against the strings that need rewriting.</p><h3 id="the-two-strings-you-subscribe-with-are-constants-now" tabindex="-1"><a class="header-anchor" href="#the-two-strings-you-subscribe-with-are-constants-now"><span>The two strings you subscribe with are constants now</span></a></h3><p><code>Quartz.Diagnostics.QuartzInstrumentation</code> publishes the <code>ActivitySource</code> and <code>Meter</code> names, which used to be internal — so wiring Quartz into OpenTelemetry began by typing <code>&quot;Quartz&quot;</code> twice from memory:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line">services<span class="token punctuation">.</span><span class="token function">AddOpenTelemetry</span><span class="token punctuation">(</span><span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">.</span><span class="token function">WithTracing</span><span class="token punctuation">(</span>tracing <span class="token operator">=&gt;</span> tracing<span class="token punctuation">.</span><span class="token function">AddSource</span><span class="token punctuation">(</span>QuartzInstrumentation<span class="token punctuation">.</span>ActivitySourceName<span class="token punctuation">)</span><span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">.</span><span class="token function">WithMetrics</span><span class="token punctuation">(</span>metrics <span class="token operator">=&gt;</span> metrics<span class="token punctuation">.</span><span class="token function">AddMeter</span><span class="token punctuation">(</span>QuartzInstrumentation<span class="token punctuation">.</span>MeterName<span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Both are still <code>&quot;Quartz&quot;</code>, so an existing <code>AddSource(&quot;Quartz&quot;)</code> keeps working.</p><p>The nine <strong>instrument</strong> names are constants too, on the nested <code>QuartzInstrumentation.Instruments</code>: <code>JobExecutionActive</code>, <code>JobExecutionDuration</code>, <code>TriggerMisfire</code>, <code>TriggerRetry</code>, <code>TriggerAcquisitionDuration</code>, <code>TriggerAcquired</code>, <code>ClusterCheckinDuration</code>, <code>ClusterRecoveryTrigger</code> and <code>JobStoreOperationDuration</code>. They were string literals inside the meter, so a dashboard or a metrics view could only copy them out of a documentation page. The values are unchanged and the meter builds its instruments from the constants, with a test holding the two sets equal in both directions.</p><h3 id="two-instruments-not-four" tabindex="-1"><a class="header-anchor" href="#two-instruments-not-four"><span>Two instruments, not four</span></a></h3><p><code>scheduling.quartz.execute</code> and <code>scheduling.quartz.execute.errors</code> are gone. A histogram already carries its own count, so the first was a second instrument write per fire for a number every exporter derives from the duration series; and with <code>error.type</code> on the histogram, the failures are that count&#39;s <code>error.type</code>-tagged subset, which is what the second reported — less precisely, since a counter cannot say what failed.</p><p><strong>The duration is in seconds.</strong> OpenTelemetry records durations in seconds, and a histogram&#39;s default bucket boundaries are chosen for them: recording milliseconds put every execution longer than ten seconds into the top bucket, alongside every other duration series in the application, so &quot;how long do jobs take&quot; was unanswerable from the buckets. <code>quartz.job.execution.active</code> is <code>{job}</code> rather than <code>ea</code>, which is not a UCUM unit at all.</p><p><strong>Every measurement is tagged with <code>quartz.scheduler.name</code>.</strong> A process can run several schedulers — named registrations, or a host and a test harness side by side — and their measurements used to arrive as one undifferentiated series. Tagging them apart is a cardinality change: a backend stores one series per scheduler where it stored one in total, and a query that aggregated across everything now needs to say so (<code>sum without (quartz_scheduler_name)</code> in Prometheus, or the equivalent grouping in whatever reads these). One extra series per scheduler is the whole of the increase; the tag&#39;s values are the scheduler names an application configured, which is a fixed, small set.</p><p><strong>And with <code>quartz.scheduler.id</code>.</strong> A cluster is several schedulers sharing one name, so the name alone answers &quot;which application&quot; and never &quot;which node&quot;. Every measurement carries both, so a slow node, a node that stopped firing, or a node whose check-ins are taking longer than the rest can be seen without a trace. It is the same cardinality trade: one series per node where there was one per scheduler, and the values are the instance ids in the cluster.</p><p>The instruments themselves are also created through the container&#39;s <code>IMeterFactory</code> when it has one, which every application on the generic host does. The meter used to be a process-wide static, so two containers in one process published to the same instruments; they now publish to their own, which is what makes <code>MetricCollector</code> — the <code>Microsoft.Extensions.Diagnostics.Testing</code> reader that collects one factory&#39;s instruments — able to see them at all. The meter&#39;s name and what an exporter subscribes to are unchanged, and an application that never calls <code>AddMetrics()</code> still gets a meter, created directly as before. Quartz does not register an <code>IMeterFactory</code> of its own: doing so would put Quartz&#39;s factory in the way of the application&#39;s wherever <code>AddMetrics()</code> happened to be called after <code>AddQuartz</code>.</p><p><strong><code>quartz.job.execution.active</code> is an up-down counter.</strong> The number of jobs running goes down as often as it goes up, and Quartz has always measured the decrement — but a <code>Counter</code> is monotonic by OpenTelemetry&#39;s definition, so an exporter aggregating one is entitled to drop or mis-render a negative measurement, leaving a &quot;jobs currently running&quot; chart that only ever climbs. The meaning is unchanged; the instrument type an exporter sees is not, so a dashboard or an alert built on the old series has to be rebuilt on a non-monotonic one — a <code>Sum</code> with <code>IsMonotonic = false</code> in the OpenTelemetry SDK, which Prometheus renders as a gauge rather than a counter.</p><p><strong><code>scheduling.quartz.exception_type</code> is <code>error.type</code>, and it names the exception the job threw.</strong> Two things were wrong with it. The tag was added to a copy of the tag list and thrown away, so the errors counter carried the four identity tags and nothing else: an exporter could see that executions failed, but never what failed. And the type it named was the <code>JobExecutionException</code> that the job run shell wraps anything a job throws in — the same answer for very nearly every failure there is.</p><p>The tag now arrives, and it reports the exception an application would recognise. A job that throws <code>InvalidOperationException</code> is reported as <code>System.InvalidOperationException</code>, not as the <code>JobExecutionException</code> -&gt; <code>JobExecutionProcessException</code> -&gt; cause chain the run shell built around it. A job that raises a <code>JobExecutionException</code> itself has no such wrapper underneath and is reported as <code>Quartz.JobExecutionException</code>, which is what it chose to say. The value is a fully-qualified type name and nothing else: a message would be unbounded, and an attribute an exporter aggregates on has to have bounded cardinality.</p><p>The name is OpenTelemetry&#39;s. <a href="https://opentelemetry.io/docs/specs/semconv/registry/attributes/error/" target="_blank" rel="noopener noreferrer"><code>error.type</code></a> is the semantic convention&#39;s attribute for what an operation failed with, so these series line up with every other instrumented failure in the same dashboard; the Quartz-specific spelling is gone rather than emitted alongside it, since two names for one attribute doubles the series and settles nothing. <strong>A query or an alert matching on <code>scheduling.quartz.exception_type</code> has to be rewritten</strong>, and one that expected the value <code>JobExecutionException</code> now sees the type the job threw.</p><p>The tag is on <code>quartz.job.execution.duration</code>, so a failed run&#39;s duration can be told apart from a successful one&#39;s — and, with the errors counter gone, so that the failures can be counted at all. It is deliberately <em>not</em> on <code>quartz.job.execution.active</code>, whose increment and decrement have to carry identical attributes or the series never comes back to zero.</p><p>A vetoed fire is not an execution and appears in neither instrument: the job never ran, so there is no duration to record. Vetoes are visible in traces, as a <code>Quartz.Job.Veto</code> span.</p><p>The execution&#39;s span carries the same <code>error.type</code> with the same value, so one attribute finds a failure in a trace and in a metric alike. The span also still records the exception as an event, with the whole wrapper chain, because that is where the stack traces are.</p><h2 id="json-serialization" tabindex="-1"><a class="header-anchor" href="#json-serialization"><span>JSON Serialization</span></a></h2><p>To configure JSON serialization to be used in job store, instead of the old <code>UseJsonSerializer</code> you should now use either <code>UseSystemTextJsonSerializer</code> or <code>UseNewtonsoftJsonSerializer</code>:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token comment">// 3.x</span></span>
<span class="line">q<span class="token punctuation">.</span><span class="token function">UseJsonSerializer</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line"><span class="token comment">// 4.x — System.Text.Json (included in main package)</span></span>
<span class="line">q<span class="token punctuation">.</span><span class="token function">UseSystemTextJsonSerializer</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line"><span class="token comment">// 4.x — Newtonsoft.Json (requires Quartz.Serialization.Newtonsoft package)</span></span>
<span class="line">q<span class="token punctuation">.</span><span class="token function">UseNewtonsoftJsonSerializer</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Remove the old <code>Quartz.Serialization.Json</code> package reference.</p><h3 id="deserialization-failures-surface-as-quartz-jsonserializationexception" tabindex="-1"><a class="header-anchor" href="#deserialization-failures-surface-as-quartz-jsonserializationexception"><span>Deserialization failures surface as <code>Quartz.JsonSerializationException</code></span></a></h3><table><thead><tr><th>3.x</th><th>4.x</th></tr></thead><tbody><tr><td><code>IObjectSerializer.DeSerialize</code> could let <code>Newtonsoft.Json.JsonSerializationException</code> and <code>Newtonsoft.Json.JsonReaderException</code> escape</td><td>Every deserialization failure arrives as <code>Quartz.JsonSerializationException</code> — a <code>SchedulerException</code> — from <strong>both</strong> serializers, with the payload in the message and the underlying parse failure as <code>InnerException</code></td></tr></tbody></table><p><code>Quartz.JsonSerializationException</code> shadows Newtonsoft&#39;s type of the same name inside the <code>Quartz.Serialization.Newtonsoft</code> package, because every file in it sits under the <code>Quartz</code> namespace and the enclosing namespace wins over a <code>using</code>. The wrapping <code>catch</code> therefore never matched Newtonsoft&#39;s parse failures and they escaped raw. Code that catches <code>Newtonsoft.Json.JsonSerializationException</code> around <code>IObjectSerializer.Deserialize</code> — or around any job store read that goes through it — has to catch <code>Quartz.JsonSerializationException</code> instead:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token comment">// 3.x</span></span>
<span class="line"><span class="token keyword">catch</span> <span class="token punctuation">(</span><span class="token class-name">Newtonsoft<span class="token punctuation">.</span>Json<span class="token punctuation">.</span>JsonSerializationException</span> e<span class="token punctuation">)</span></span>
<span class="line"></span>
<span class="line"><span class="token comment">// 4.x — the same failure, whichever serializer is configured</span></span>
<span class="line"><span class="token keyword">catch</span> <span class="token punctuation">(</span><span class="token class-name">Quartz<span class="token punctuation">.</span>JsonSerializationException</span> e<span class="token punctuation">)</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="both-serializers-refuse-a-job-data-value-they-cannot-read-back" tabindex="-1"><a class="header-anchor" href="#both-serializers-refuse-a-job-data-value-they-cannot-read-back"><span>Both serializers refuse a job data value they cannot read back</span></a></h3><p>The System.Text.Json read side is closed on purpose — a stored job data value comes back as a string, a bool, an int, a long, a double, null or a <code>Dictionary&lt;string, string&gt;</code>, and there is no polymorphic <code>object</code> deserialization, which is what keeps a trimmed publish honest. The write side was not closed to match, so a <code>JobDataMap</code> holding a <code>List&lt;string&gt;</code> or a <code>Dictionary&lt;string, object&gt;</code> serialized without complaint and threw <code>JsonSerializationException</code> on read. The blob was in the database by then, and the failure belonged to whoever next ran the job.</p><p>Writing now refuses such a value, naming the entry and the type:</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text"><pre><code class="language-text"><span class="line">Job data entry &#39;recipients&#39; holds a System.Collections.Generic.List\`1[[System.String, …]], which Quartz&#39;s</span>
<span class="line">JSON format cannot read back. A job data value has to be one of the types JobDataMap declares an accessor</span>
<span class="line">for (string, bool, char, int, long, float, double, decimal, DateTime, DateTimeOffset, TimeSpan, Guid,</span>
<span class="line">DateOnly, TimeOnly or an enum), a Dictionary&lt;string, string&gt;, or a type the application declares through</span>
<span class="line">SystemTextJsonSerializerRegistry.AddTypeInfoResolver. Anything with structure of its own has to be</span>
<span class="line">serialized by the job and stored as a string.</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The set is the one <code>DataMapExtensions</code> declares an accessor for, plus <code>Dictionary&lt;string, string&gt;</code>, plus enums by rule. A type of your own is declared through <code>AddTypeInfoResolver</code> — the same registration a trimmed or native-AOT publish already needs — and is then written like any other, coming back as the string map the store format gives for any object.</p><p>The check lives in the job data map converter, which the HTTP API shares with the store serializer, so it applies there too: a job whose map holds a <code>List&lt;string&gt;</code> — in a <code>RAMJobStore</code> as much as a persistent one — now fails to serialize into a <code>GET</code> response with the message above, where before the server sent a payload <code>Quartz.HttpClient</code> threw on reading. Both ends use the same closed reader, so a value the server cannot read back is one the client cannot either, and <code>AddTypeInfoResolver</code> is the answer on both.</p><p><strong>Newtonsoft refuses the same set.</strong> It had the same asymmetry, and one type of its own: a <code>TimeZoneInfo</code> in a job data map was written as <code>{&quot;$type&quot;:&quot;System.TimeZoneInfo, …&quot;,&quot;Id&quot;:&quot;Tokyo Standard Time&quot;, …}</code> and then read back by nothing — the contract resolver keeps Json.NET off <code>ISerializable</code>, and every public member of a zone is read-only, so there was nothing to rebuild it from. It now refuses against the very same declaration System.Text.Json refuses against — not a copy of that list, that list — so the two cannot come to accept different things, and a value written by one serializer is a value the other&#39;s reader has an answer for. The message is the same but for the way out it names:</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text"><pre><code class="language-text"><span class="line">Job data entry &#39;zone&#39; holds a System.TimeZoneInfo, which Quartz&#39;s JSON format cannot read back. … or a</span>
<span class="line">type the application declares through NewtonsoftJsonSerializerRegistry.AddJobDataValueType. …</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p><code>AddJobDataValueType&lt;T&gt;()</code> is this package&#39;s counterpart to <code>AddTypeInfoResolver</code>. Json.NET needs no metadata handed to it, so the call registers nothing but your word that the type reads back:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line">store<span class="token punctuation">.</span><span class="token function">UseNewtonsoftJsonSerializer</span><span class="token punctuation">(</span>json <span class="token operator">=&gt;</span> json<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">AddJobDataValueType</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>ReportOptions<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>Three things used to round-trip through Newtonsoft and now need that declaration, because the other serializer never accepted them and a value only one reader can load is a one-way door: a class of your own, a <code>JobKey</code> or <code>TriggerKey</code> held as a job data value, and a collection. A <code>TimeZoneInfo</code> and a nested <code>JobDataMap</code> are past declaring — Json.NET could not read either back — so store a zone&#39;s <code>Id</code>, and serialize a nested structure in the job and store the result as a string. <code>StoreJobDataAsStrings</code> was never affected by any of this.</p><h3 id="a-string-dictionary-is-written-the-same-way-by-both-serializers" tabindex="-1"><a class="header-anchor" href="#a-string-dictionary-is-written-the-same-way-by-both-serializers"><span>A string dictionary is written the same way by both serializers</span></a></h3><p>A <code>Dictionary&lt;string, string&gt;</code> is the one job data value with structure that both formats carry, and until 4.0 they did not write it the same way. Json.NET names the type of any value an <code>object</code>-typed slot cannot, so the Newtonsoft serializer wrote the map with the type beside it:</p><div class="language-json line-numbers-mode" data-highlighter="prismjs" data-ext="json"><pre><code class="language-json"><span class="line"><span class="token punctuation">{</span><span class="token property">&quot;headers&quot;</span><span class="token operator">:</span><span class="token punctuation">{</span><span class="token property">&quot;$type&quot;</span><span class="token operator">:</span><span class="token string">&quot;System.Collections.Generic.Dictionary\`2[[System.String, System.Private.CoreLib],[System.String, System.Private.CoreLib]], System.Private.CoreLib&quot;</span><span class="token punctuation">,</span><span class="token property">&quot;tenant&quot;</span><span class="token operator">:</span><span class="token string">&quot;acme&quot;</span><span class="token punctuation">}</span><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>System.Text.Json wrote the plain object, and its reader takes every property of an object as an entry of the map — so a value written by a Newtonsoft-configured scheduler and read by a System.Text.Json-configured one came back carrying an assembly-qualified type name under a <code>$type</code> key, in the same key space as the application&#39;s own data, with nothing said about it.</p><p>4.0&#39;s Newtonsoft serializer writes the plain object, which is byte for byte what System.Text.Json writes:</p><div class="language-json line-numbers-mode" data-highlighter="prismjs" data-ext="json"><pre><code class="language-json"><span class="line"><span class="token punctuation">{</span><span class="token property">&quot;headers&quot;</span><span class="token operator">:</span><span class="token punctuation">{</span><span class="token property">&quot;tenant&quot;</span><span class="token operator">:</span><span class="token string">&quot;acme&quot;</span><span class="token punctuation">}</span><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p><strong>This is a change to the bytes Newtonsoft writes, and both readers read both forms.</strong> A blob already in the database keeps loading: Json.NET still builds the map out of the type it named, and the System.Text.Json reader now treats a <code>$type</code> property as the metadata it always was rather than handing it back as an entry. Nothing has to be rewritten, and there is no migration to run.</p>`,48),e(`p`,null,[l[216]||=r(`The one direction that does not hold is backwards: 3.x's Newtonsoft reader has no answer for an object with no type beside it, and hands back a Json.NET `,-1),l[217]||=e(`code`,null,`JObject`,-1),l[218]||=r(` instead of the dictionary. That matters only while a 3.x node is reading what a 4.0 node wrote — see `,-1),c(g,{to:`/documentation/quartz-4.x/operations.html#a-mixed-3-x-and-4-0-window`},{default:i(()=>[...l[215]||=[r(`A mixed 3.x and 4.0 window`,-1)]]),_:1}),l[219]||=r(`.`,-1)]),l[872]||=t(`<p><strong><code>$type</code> is now a reserved name inside a stored string map.</strong> Both serializers refuse a map that stores an entry under it, the same way and at the same moment as any other value they cannot read back:</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text"><pre><code class="language-text"><span class="line">Job data entry &#39;headers&#39; holds a string map with an entry named &#39;$type&#39;. &#39;$type&#39; is the name Json.NET</span>
<span class="line">writes a value&#39;s type under, so Quartz&#39;s JSON format reads it as metadata rather than as an entry and no</span>
<span class="line">reader can hand it back. Store that entry under a name of its own.</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Nothing is lost by that refusal, because such a map never survived a round trip: Json.NET wrote the name twice and then failed to read its own blob, and a map System.Text.Json wrote was one Json.NET could not load at all. The refusal is what turns a blob in the column that nobody can read into a failure the application that wrote it hears about.</p><h3 id="custom-trigger-and-calendar-serializers-are-no-longer-static" tabindex="-1"><a class="header-anchor" href="#custom-trigger-and-calendar-serializers-are-no-longer-static"><span>Custom trigger and calendar serializers are no longer static</span></a></h3><p>The static registration methods have been removed, because they wrote into process-global dictionaries: two schedulers in one process could not have different custom serializers, and registration order silently decided which one won.</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token comment">// 3.x</span></span>
<span class="line">SystemTextJsonObjectSerializer<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">AddCalendarSerializer</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>CustomCalendar<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span><span class="token keyword">new</span> <span class="token constructor-invocation class-name">CustomCalendarSerializer</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">NewtonsoftJsonObjectSerializer<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">AddTriggerSerializer</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>CustomTrigger<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span><span class="token keyword">new</span> <span class="token constructor-invocation class-name">CustomTriggerSerializer</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line"><span class="token comment">// 4.x — register through the store builder; what the callback registers belongs to that scheduler alone</span></span>
<span class="line">q<span class="token punctuation">.</span><span class="token function">UsePersistentStore</span><span class="token punctuation">(</span>store <span class="token operator">=&gt;</span> store<span class="token punctuation">.</span><span class="token function">UseSystemTextJsonSerializer</span><span class="token punctuation">(</span>json <span class="token operator">=&gt;</span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    json<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">AddCalendarSerializer</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>CustomCalendar<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span><span class="token keyword">new</span> <span class="token constructor-invocation class-name">CustomCalendarSerializer</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">    json<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">AddTriggerSerializer</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>CustomTrigger<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span><span class="token keyword">new</span> <span class="token constructor-invocation class-name">CustomTriggerSerializer</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"><span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>If you construct a serializer yourself, pass it a registry instead:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token class-name"><span class="token keyword">var</span></span> registry <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token constructor-invocation class-name">SystemTextJsonSerializerRegistry</span><span class="token punctuation">(</span><span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">.</span><span class="token generic-method"><span class="token function">AddCalendarSerializer</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>CustomCalendar<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span><span class="token keyword">new</span> <span class="token constructor-invocation class-name">CustomCalendarSerializer</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line"><span class="token class-name"><span class="token keyword">var</span></span> serializer <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token constructor-invocation class-name">SystemTextJsonObjectSerializer</span><span class="token punctuation">(</span>registry<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The registries start out knowing every built-in trigger and calendar type, so a custom registration adds to that set. The parameterless serializer constructors still exist and use the built-ins only.</p><p>Each package has exactly one registration type: the callback parameter <em>is</em> the registry — the same <code>SystemTextJsonSerializerRegistry</code> / <code>NewtonsoftJsonSerializerRegistry</code> the serializer constructors take — rather than an <code>…SerializerOptions</code> wrapper that forwarded to it. A lambda body like the one above compiles unchanged; only an explicitly typed lambda parameter or a variable of the removed <code>SystemTextJsonSerializerOptions</code> / <code>NewtonsoftJsonSerializerOptions</code> types needs retyping. The one member that was not a registration, Newtonsoft&#39;s <code>RegisterTriggerConverters</code>, is a parameter of the extension method:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> store.UseNewtonsoftJsonSerializer(json =&gt;</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"> {</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line">     json.RegisterTriggerConverters = true;</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line">     json.AddTriggerSerializer&lt;CustomTrigger&gt;(new CustomTriggerSerializer());</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"> });</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> store.UseNewtonsoftJsonSerializer(</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">     json =&gt; json.AddTriggerSerializer&lt;CustomTrigger&gt;(new CustomTriggerSerializer()),</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">     registerTriggerConverters: true);</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>One consequence worth checking: the HTTP API, the dashboard and <code>Quartz.HttpClient</code> also serialize triggers, and none of them belongs to a single scheduler, so they no longer inherit a scheduler&#39;s custom serializers for free. They read a container-wide registry — register it as a singleton to make a custom serializer visible there:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line">services<span class="token punctuation">.</span><span class="token function">AddSingleton</span><span class="token punctuation">(</span><span class="token keyword">new</span> <span class="token constructor-invocation class-name">SystemTextJsonSerializerRegistry</span><span class="token punctuation">(</span><span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">.</span><span class="token generic-method"><span class="token function">AddTriggerSerializer</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>CustomTrigger<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span><span class="token keyword">new</span> <span class="token constructor-invocation class-name">CustomTriggerSerializer</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>See <a href="packages/system-text-json">Serialization (System.Text.Json)</a> for the full picture.</p><h3 id="a-serialized-trigger-carries-its-preferred-node" tabindex="-1"><a class="header-anchor" href="#a-serialized-trigger-carries-its-preferred-node"><span>A serialized trigger carries its preferred node</span></a></h3>`,15),e(`p`,null,[l[221]||=r(`Both JSON trigger serializers write a trigger's `,-1),c(g,{to:`/documentation/quartz-4.x/tutorial/node-affinity.html`},{default:i(()=>[...l[220]||=[r(`node affinity`,-1)]]),_:1}),l[222]||=r(` pin, as the same pair the `,-1),l[223]||=e(`code`,null,`QRTZ_TRIGGERS`,-1),l[224]||=r(` row stores:`,-1)]),l[873]||=t(`<div class="language-json line-numbers-mode" data-highlighter="prismjs" data-ext="json"><pre><code class="language-json"><span class="line"><span class="token property">&quot;PreferredNode&quot;</span><span class="token operator">:</span> <span class="token string">&quot;node-1&quot;</span><span class="token punctuation">,</span></span>
<span class="line"><span class="token property">&quot;PreferredNodeAuto&quot;</span><span class="token operator">:</span> <span class="token boolean">false</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p><code>PreferredNode</code> is the node name — <code>null</code> when the trigger is unpinned, <code>*</code> for an auto pin no node has claimed yet — and <code>PreferredNodeAuto</code> says whether the node holding the pin claimed it automatically. It takes both halves: a claimed auto pin and one the caller named look identical from the node name alone, yet only the automatic one is released when its node stops checking in.</p><p>Payloads written before the fields existed, 3.x&#39;s included, have neither and read back as <code>PreferredNode.None</code> — an unpinned trigger, which is what they always were.</p><p>This matters wherever a whole trigger travels as JSON. The HTTP API, <code>Quartz.HttpClient</code> and the dashboard dropped the pin in both directions, so scheduling or rescheduling a pinned trigger over HTTP quietly unpinned it, and the same held for a custom <code>IJobStore</code> that persists serialized triggers. The ADO.NET job store was never affected: it keeps the pin in the <code>PREFERRED_NODE</code> and <code>PREFERRED_NODE_AUTO</code> columns and reapplies them to every trigger it reads, blob triggers included.</p><h3 id="a-newtonsoft-serialized-trigger-keeps-its-time-zone" tabindex="-1"><a class="header-anchor" href="#a-newtonsoft-serialized-trigger-keeps-its-time-zone"><span>A Newtonsoft-serialized trigger keeps its time zone</span></a></h3><p><code>UseNewtonsoftJsonSerializer</code> leaves <code>registerTriggerConverters</code> off by default, and with it off a trigger is written as a plain object graph. A <code>TimeZoneInfo</code> written that way came out as its whole public surface — <code>Id</code>, <code>DisplayName</code>, <code>BaseUtcOffset</code> and the rest, every one of them read-only — so reading the object back set nothing, and the trigger&#39;s getter fell through to <code>TimeZoneInfo.Local</code>. A trigger stored under <code>Tokyo Standard Time</code> fired on whichever zone the reading machine was in, and nothing said so.</p><p>A <code>TimeZoneInfo</code> converter is now attached to every property <em>typed</em> as one, so a zone on a trigger travels as its id — the same spelling the trigger and calendar serializers have always used:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> &quot;TimeZone&quot;: { &quot;Id&quot;: &quot;Tokyo Standard Time&quot;, &quot;DisplayName&quot;: &quot;(UTC+09:00) Osaka, Sapporo, Tokyo&quot;, … }</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> &quot;TimeZone&quot;: &quot;Tokyo Standard Time&quot;</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>Both forms are read, so blobs already written in the object form still load and pick their zone out of <code>Id</code>. <code>CalendarIntervalTriggerImpl</code>, <code>DailyTimeIntervalTriggerImpl</code> and <code>RecurrenceTriggerImpl</code> were affected; <code>CronTriggerImpl</code> was not, because its zone rides on the <code>CronExpression</code>, which has always had a converter of its own.</p><p>The converter is attached per property rather than registered on the serializer, so it changes typed members only. A <code>TimeZoneInfo</code> stored as a <em>value</em> in a <code>JobDataMap</code> sits in an <code>object</code>-typed slot and is written exactly as before, <code>$type</code> and all — that payload has never been readable back (a <code>TimeZoneInfo</code> has no constructor Json.NET can call), and this change neither introduces that nor repairs it. Store the zone&#39;s id if a job needs one in its data.</p><p>The ADO.NET job store persists every trigger type Quartz ships through a persistence delegate rather than as a blob, so this reaches a scheduler only through a trigger type the store has to serialize — a custom one — or through code that hands a trigger to <code>IObjectSerializer</code> itself. The private <code>timeZoneInfoId</code> helpers on those trigger implementations, which had been meant for exactly this and never worked because Json.NET&#39;s default contract does not serialize private members, are gone.</p><h3 id="newtonsoft-types-moved-out-of-the-core-namespaces" tabindex="-1"><a class="header-anchor" href="#newtonsoft-types-moved-out-of-the-core-namespaces"><span>Newtonsoft types moved out of the core namespaces</span></a></h3><p>The <code>Quartz.Serialization.Newtonsoft</code> package used to put types in namespaces that read as if they came from the core package, and one of its types collided outright: both packages had a <code>Quartz.JsonConfigurationExtensions</code>, which made <code>UseNewtonsoftJsonSerializer</code> ambiguous when both were referenced.</p><table><thead><tr><th>3.x</th><th>4.x</th></tr></thead><tbody><tr><td><code>Quartz.JsonConfigurationExtensions</code> (Newtonsoft)</td><td><code>Quartz.NewtonsoftJsonConfigurationExtensions</code></td></tr><tr><td><code>Quartz.Serialization.Json.Triggers.ITriggerSerializer</code>, <code>TriggerSerializer&lt;T&gt;</code>, the built-in trigger serializers</td><td><code>Quartz.Serialization.Newtonsoft.Triggers.*</code> — this is the namespace a ported file actually names, and in 4.x the <em>core</em> package owns the <code>Quartz.Serialization.SystemTextJson.Triggers</code> spelling nearest to it</td></tr><tr><td><code>Quartz.ICalendarSerializer</code>, <code>Quartz.CalendarSerializer&lt;T&gt;</code></td><td><code>Quartz.Serialization.Newtonsoft.Calendars.*</code> — the same namespace shape as the System.Text.Json package&#39;s <code>Quartz.Serialization.SystemTextJson.Calendars</code></td></tr><tr><td><code>Quartz.Converters.NameValueCollectionConverter</code></td><td>internal — the serializer registers it itself, and the System.Text.Json package&#39;s converter of the same name has always been internal</td></tr></tbody></table><p><code>UseNewtonsoftJsonSerializer</code> itself is unchanged — only the <code>using</code> on a file that names one of these types. <code>AddCalendarSerializer&lt;TCalendar&gt;</code> is now constrained to <code>ICalendar</code>, matching the trigger side; a call that passed something else was never going to work at runtime.</p><p>Its <em>argument</em> is constrained too, in both packages: it takes <code>CalendarSerializer&lt;TCalendar&gt;</code> rather than <code>ICalendarSerializer</code>, so the calendar type and the serializer that reads it have to agree.</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> // compiled fine, then threw InvalidCastException on the first calendar that round-tripped</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"> json.AddCalendarSerializer&lt;HolidayCalendar&gt;(new AnnualCalendarSerializer());</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> json.AddCalendarSerializer(new HolidayCalendarSerializer());   // TCalendar is inferred</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Existing calls that pair correctly — including ones that spell the type argument out — compile unchanged. The type argument is now inferable from the serializer, so it can be dropped.</p><p><code>AddTriggerSerializer&lt;TTrigger&gt;</code> deliberately keeps <code>ITriggerSerializer</code>. The documented way to serialize a trigger that derives from a built-in one is a serializer that derives from the built-in serializer — <code>class ReportTriggerSerializer : CronTriggerSerializer</code> for a <code>ReportTrigger : CronTriggerImpl</code>. That serializer is a <code>TriggerSerializer&lt;CronTriggerImpl&gt;</code>, not a <code>TriggerSerializer&lt;ReportTrigger&gt;</code>, and <code>TriggerSerializer&lt;T&gt;</code> is an invariant class, so tightening the argument the same way would reject exactly the pattern the package recommends. Calendars have no equivalent pattern.</p><p><code>ICalendarSerializer</code> also gained <code>CalendarTypeName</code>, closing the last contract gap between the two packages. It is a default interface member returning empty, so an existing implementation compiles unchanged and keeps its 3.x behavior: matched by the calendar&#39;s assembly-qualified type name. When a serializer provides a name, the registry indexes it under both — the assembly-qualified key always stays registered, because that is what <code>CALENDARS.CALENDAR</code> payloads written by 3.x carry. Calendar lookups are case-insensitive now, matching the trigger side and the System.Text.Json package.</p><h3 id="the-openapi-trigger-schema-describes-the-whole-payload" tabindex="-1"><a class="header-anchor" href="#the-openapi-trigger-schema-describes-the-whole-payload"><span>The OpenAPI trigger schema describes the whole payload</span></a></h3><p>The HTTP API&#39;s endpoints handle <code>ITrigger</code>, which OpenAPI cannot describe, so the published document has always been shaped by a stand-in type. Nothing compiled against that stand-in, so it fell behind the payload it was meant to describe, and five properties the server has been sending were missing from the schema:</p><table><thead><tr><th>Property</th><th>Present on</th></tr></thead><tbody><tr><td><code>nextFireTimeUtc</code></td><td>every trigger</td></tr><tr><td><code>previousFireTimeUtc</code></td><td>every trigger</td></tr><tr><td><code>executionGroup</code></td><td>every trigger</td></tr><tr><td><code>timesTriggered</code></td><td>every trigger type except <code>CronTrigger</code></td></tr><tr><td><code>recurrenceRule</code></td><td><code>RecurrenceTrigger</code></td></tr></tbody></table><p><code>RecurrenceTrigger</code> itself was also missing from the list of trigger types the <code>triggerType</code> discriminator can carry.</p><p>Nothing about the wire format changed — those fields were always on it. What changes is generated clients: a client regenerated against 4.x gains the five properties, and code that had been reading them through an escape hatch (a raw <code>JsonElement</code>, an extra partial-class member) can read them from the generated model instead. The two fire times are computed by the scheduler, so a value sent with a trigger being scheduled is overwritten; <code>executionGroup</code>, <code>timesTriggered</code> and <code>recurrenceRule</code> are read from what you send.</p><p>A test in <code>Quartz.Tests.AspNetCore</code> now compares the stand-in against the property names a trigger of each built-in type actually serializes to, in both directions, so the schema cannot quietly fall behind again.</p><h2 id="sealed-and-internalized-types" tabindex="-1"><a class="header-anchor" href="#sealed-and-internalized-types"><span>Sealed and Internalized Types</span></a></h2><p>Many types have been sealed and/or internalized to minimize the API surface that needs to be maintained. If you were extending a type that is now sealed or internal, file an issue to request it be reopened.</p><p>The ones most likely to be visible in existing code:</p><p><strong><code>QuartzScheduler</code> and <code>QuartzSchedulerResources</code> are internal.</strong> <code>QuartzScheduler</code> is the implementation behind <code>IScheduler</code> and was only reachable through <code>StdScheduler</code>&#39;s constructor, which is internal too now that the container builds the scheduler. Resolve <code>IScheduler</code> or <code>ISchedulerFactory</code>; the settings that used to live on <code>QuartzSchedulerResources</code> are <code>QuartzSchedulerOptions</code>.</p><p><strong><code>StdAdoConstants</code> and <code>IAdoUtil</code> are internal, and constants are no longer inherited.</strong> <code>AdoConstants</code> stays public — table, column and state names are a real contract for delegate authors — but it is a <code>static class</code> now, and <code>AdoJobStoreBase</code>, <code>StdAdoDelegate</code> and <code>DbLockHandler</code> no longer derive from it or from <code>StdAdoConstants</code>:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line"> public class MyDelegate : StdAdoDelegate</span>
<span class="line"></span><span class="token prefix unchanged"> </span><span class="token line"> {</span>
<span class="line"></span></span><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line">     private string CountRows() =&gt; $&quot;SELECT COUNT(*) FROM {TablePrefixSubst}{TableTriggers}&quot;;</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line">     private string CountRows() =&gt; $&quot;SELECT COUNT(*) FROM {{0}}{AdoConstants.TableTriggers}&quot;;</span>
<span class="line"></span></span><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line"> }</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The <code>Sql*</code> statement templates on <code>StdAdoConstants</code> are not visible at all any more — the exact text of a statement is not a contract. Build the statement your dialect needs, or override the <code>GetSelect*Sql</code> hooks, which are unchanged.</p><p><code>AdoConstants</code> names <strong>every</strong> table the store reads or writes, which in 3.x it did not: <code>TableSimplePropertiesTriggers</code> (<code>&quot;SIMPROP_TRIGGERS&quot;</code>) was declared <code>protected</code> on <code>SimplePropertiesTriggerPersistenceDelegateSupport</code> and nowhere else. The startup schema validation walks the names on <code>AdoConstants</code>, so it never asked whether <code>QRTZ_SIMPROP_TRIGGERS</code> was there, and a database missing only that table started and failed later — on the first calendar-interval, daily-time-interval or recurrence trigger written to it. It is <code>public const</code> on <code>AdoConstants</code> in 4.x and validated with the other eleven. The <code>protected</code> spelling stays on <code>SimplePropertiesTriggerPersistenceDelegateBase</code> for a delegate that derives from it, so a subclass compiles unchanged.</p><p><code>DbLockHandler.AdoUtil</code> is <code>private protected</code> for the same reason, so a lock handler written outside Quartz no longer sees it. What such a handler needs from it — preparing a statement and binding a parameter — is <code>protected</code> on <code>DbLockHandler</code> itself:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token keyword">protected</span> <span class="token return-type class-name">DbCommand</span> <span class="token function">PrepareCommand</span><span class="token punctuation">(</span><span class="token class-name">ConnectionAndTransactionHolder</span> conn<span class="token punctuation">,</span> <span class="token class-name"><span class="token keyword">string</span></span> commandText<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"><span class="token keyword">protected</span> <span class="token return-type class-name"><span class="token keyword">void</span></span> <span class="token function">AddCommandParameter</span><span class="token punctuation">(</span><span class="token class-name">DbCommand</span> command<span class="token punctuation">,</span> <span class="token class-name"><span class="token keyword">string</span></span> paramName<span class="token punctuation">,</span> <span class="token class-name"><span class="token keyword">object</span><span class="token punctuation">?</span></span> paramValue<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p><code>ExecuteSql</code> is the one method a <code>DbLockHandler</code> subclass exists to implement, and it could not be implemented without these; both shipped row-lock handlers now issue their statements through them, so <code>SelectForUpdateLockHandler</code> and <code>UpdateRowLockHandler</code> are literal example code for one of your own. There is no overload taking a provider-specific data type or a size, because a lock statement binds a scheduler name and a lock name and both are strings. A handler that does not lock in a database implements <code>ILockHandler</code> directly instead.</p><p><strong>Three trigger persistence delegates became public</strong>, so a custom delegate list can name all five built-ins: <code>CronTriggerPersistenceDelegate</code>, <code>SimpleTriggerPersistenceDelegate</code> and <code>DailyTimeIntervalTriggerPersistenceDelegate</code> join <code>CalendarIntervalTriggerPersistenceDelegate</code> and <code>RecurrenceTriggerPersistenceDelegate</code>. All five are <code>sealed</code>; write your own against <code>SimplePropertiesTriggerPersistenceDelegateBase</code> or <code>ITriggerPersistenceDelegate</code>.</p><p><strong><code>SchedulerConstants</code> is a static class</strong> rather than a struct, and every public options type is <code>sealed</code> — <code>QuartzOptions</code>, <code>SchedulingOptions</code>, <code>QuartzHostedServiceOptions</code>, <code>QuartzHttpApiOptions</code> and <code>HttpClientOptions</code> among them. None ever had a virtual member, and Quartz constructs each of them itself inside a <code>configure</code> callback, so a derived type was never going to be seen. <strong><code>MisfireInstruction</code> is internal</strong> — see <a href="#the-enums-are-the-vocabulary">the enums are the vocabulary</a>.</p><p><strong><code>HttpScheduler</code> is <code>sealed</code>.</strong> It is a wire client — every member turns a call into an HTTP request — so deriving from it and overriding half of them produces a scheduler that is partly remote and partly not. Wrap it in an <code>IScheduler</code> of your own if you need to intercept calls.</p><p><strong>Quartz.Dashboard&#39;s Blazor components are not API.</strong> They are <code>public</code> because the Razor compiler makes them so, but they are UI and are excluded from the dashboard&#39;s public-API baseline. Build against <code>QuartzDashboardOptions</code>, <code>AddQuartzDashboard</code> and the model types.</p><p><strong>The <code>Quartz.Xml.JobSchedulingData20</code> namespace is gone.</strong> It held the fourteen classes that <code>xsd.exe</code> generated from <code>job_scheduling_data_2_0.xsd</code> — <code>QuartzXmlConfiguration20</code>, <code>abstractTriggerType</code>, <code>calendarIntervalTriggerType</code>, <code>cronTriggerType</code>, <code>entryType</code>, <code>jobdatamapType</code>, <code>jobdetailType</code>, <code>jobschedulingdataSchedule</code>, <code>preprocessingcommandsType</code>, <code>preprocessingcommandsTypeDeletejob</code>, <code>preprocessingcommandsTypeDeletetrigger</code>, <code>processingdirectivesType</code>, <code>simpleTriggerType</code> and <code>triggerType</code>. They were public only because <code>XmlSerializer</code> cannot serialize internal types; nothing in Quartz took or returned one, and their names never followed .NET conventions because a code generator picked them. The processor reads the document itself now, so the model is internal — and so, from this release, is the processor.</p><p><strong>The XML format has not changed</strong> — the schema, the file, and every element and attribute in it are exactly as they were, and <code>job_scheduling_data_2_0.xsd</code> still validates the document before it is read. Only two failures report differently:</p><table><thead><tr><th>Input</th><th>3.x / earlier 4.0 preview</th><th>4.0</th></tr></thead><tbody><tr><td>A file that is not well-formed XML</td><td><code>InvalidOperationException</code>, &quot;There is an error in XML document (3, 13)&quot;, wrapping an <code>XmlException</code></td><td>the <code>XmlException</code> itself, naming the line, the position and the unclosed elements</td></tr><tr><td>A file whose elements are not in the <code>http://quartznet.sourceforge.net/JobSchedulingData</code> namespace</td><td><code>InvalidOperationException</code>, &quot;&lt;job-scheduling-data xmlns=&#39;&#39;&gt; was not expected&quot;</td><td><code>SchedulerConfigException</code> naming the namespace that was expected</td></tr></tbody></table><p>A schema violation still throws <code>SchedulingDataValidationException</code> carrying every error found, and <code>XmlSchedulingDataProcessorPlugin</code> still wraps whatever surfaces in a <code>SchedulerException</code>, so a plugin-based setup sees no change at all.</p><p><strong>Nor will it change.</strong> The schema is frozen at what it already says: <code>simple</code>, <code>cron</code> and <code>calendar-interval</code> triggers, and none of the trigger kinds or trigger settings written since. A daily time interval trigger, a recurrence rule, a <a href="#a-trigger-can-carry-a-retry-policy">retry policy</a> and an execution group are expressible in the JSON format and are not expressible in XML, now or later — two file formats that both grow is two parsers and two schemas for one feature, and the XML one is the one that has to keep loading files written twenty years ago. It does: <strong>XML scheduling is not deprecated and is not going away in 4.x</strong>, and a schedule that only needs the three trigger kinds above never has to move. Write a new schedule as JSON, and move an XML one when it needs something the schema cannot spell. <code>UseJsonSchedulingConfiguration</code> takes the same <code>FileSchedulingOptions</code> as its XML twin, so the registration is one word different.</p><h3 id="the-shipped-plugins-are-sealed" tabindex="-1"><a class="header-anchor" href="#the-shipped-plugins-are-sealed"><span>The shipped plugins are sealed</span></a></h3><p><code>LoggingJobHistoryPlugin</code>, <code>LoggingTriggerHistoryPlugin</code>, <code>StructuredLoggingJobHistoryPlugin</code>, <code>StructuredLoggingTriggerHistoryPlugin</code>, <code>XmlSchedulingDataProcessorPlugin</code> and <code>JsonSchedulingDataProcessorPlugin</code> are <code>sealed</code>, and so is <code>NoOpJob</code>. A plugin is an <code>ISchedulerPlugin</code> — four members, all of which a plugin of your own implements directly — so deriving from a shipped one only ever inherited a <code>Name</code> property and a scheduler reference. Write the plugin against the interface; every shipped plugin is example code for one.</p><p>Two consequences are worth calling out:</p><ul><li><code>XmlSchedulingDataProcessorPlugin.TypeLoader</code> was <code>protected</code> and is now private. It was there for a subclass to read; the constructor that takes an <code>ITypeLoader</code> is how the type loader gets in.</li><li><code>LoggingJobHistoryPlugin</code> and <code>LoggingTriggerHistoryPlugin</code> no longer have the <code>IsInfoEnabled</code> / <code>IsWarnEnabled</code> / <code>WriteInfo</code> / <code>WriteWarning</code> protected hooks. They predate <code>Microsoft.Extensions.Logging</code> and duplicated <code>ILogger.IsEnabled</code>: pass an <code>ILogger</code> to the constructor and route it wherever you want the history to land, which is what the constructor taking <code>(ILogger&lt;T&gt;, TimeProvider)</code> is for.</li></ul><h3 id="the-shipped-implementations-are-sealed" tabindex="-1"><a class="header-anchor" href="#the-shipped-implementations-are-sealed"><span>The shipped implementations are sealed</span></a></h3><p>Where the extension point is an interface or an abstract base, the concrete class beside it is closed. Each of these was a <code>public class</code> with <code>virtual</code> members in 3.x:</p><table><thead><tr><th>Sealed in 4.x</th><th>Derive from this instead</th></tr></thead><tbody><tr><td><code>AnnualCalendar</code>, <code>CronCalendar</code>, <code>DailyCalendar</code>, <code>HolidayCalendar</code>, <code>MonthlyCalendar</code>, <code>WeeklyCalendar</code></td><td><code>BaseCalendar</code>, which stays open, or <code>ICalendar</code> directly. Every shipped calendar is a short <code>BaseCalendar</code> — the <code>virtual</code> <code>IsDayExcluded</code> / <code>SetDayExcluded</code> / <code>AreAllDaysExcluded</code> overrides they offered only made sense for that one calendar&#39;s day set anyway</td></tr><tr><td><code>JobExecutionContextImpl</code>, and its nine <code>virtual</code> members with it</td><td><code>IJobExecutionContext</code>. A test double implements the interface; a decorator wraps one</td></tr><tr><td><code>DefaultThreadPool</code>, <code>ZeroSizeThreadPool</code></td><td><code>IThreadPool</code>, or <code>TaskSchedulingThreadPool</code>, which stays open — see <a href="#the-thread-pool-is-asynchronous">The thread pool is asynchronous</a></td></tr><tr><td><code>JobChainingJobListener</code></td><td>The three listener interfaces. Every notification member is a default interface member now, so implementing one directly costs a <code>Name</code> at most — see <a href="#the-three-support-base-classes-are-gone">The three <code>*Support</code> base classes are gone</a></td></tr></tbody></table><p><strong>The five <code>*TriggerImpl</code> types are deliberately not in that list.</strong> A trigger of your own derives from <code>TriggerBase</code> or from any of the five, <code>HasAdditionalProperties</code> is <code>virtual</code> on <code>TriggerBase</code>, and deriving from a built-in trigger pairs with deriving from that trigger&#39;s built-in JSON serializer — which is why those serializers are public and unsealed in both packages, so a subclass can call <code>base.SerializeFields</code> / <code>base.DeserializeFields</code> and keep the built-in fields&#39; stored shape. <code>CalendarIntervalTriggerImpl</code>, <code>DailyTimeIntervalTriggerImpl</code> and <code>RecurrenceTriggerImpl</code> were sealed during 4.x&#39;s development and were reopened for exactly that pairing — see <a href="#other-breaking-changes">Other Breaking Changes</a>.</p><p><code>JobChainingJobListener</code> also changed base. It derived from <code>Quartz.Listener.JobListenerSupport</code>, which is gone, and implements <code>IJobListener</code> directly, so its <code>Name</code> and <code>JobWasExecuted</code> are no longer <code>override</code>s. A chain you customized by deriving from it becomes an <code>IJobListener</code> that holds one and forwards.</p><p><code>RAMJobStore</code> is sealed too, and has its own section because it comes with a replacement seam — see <a href="#ramjobstore-is-sealed"><code>RAMJobStore</code> is sealed</a>.</p><h2 id="triggerbase-property-removals" tabindex="-1"><a class="header-anchor" href="#triggerbase-property-removals"><span>TriggerBase Property Removals</span></a></h2>`,57),e(`p`,null,[l[226]||=t(`<code>AbstractTrigger</code> — the abstract base every trigger implementation derives from — is <strong><code>TriggerBase</code></strong>, the .NET spelling of the same idea. It is abstract, so it is never a configuration string and never a JSON <code>$type</code> value; a custom trigger updates its base list and recompiles. (One consequence for the <em>binary</em> escape hatch: a <code>BinaryFormatter</code> payload names a private base-class field as <code>AbstractTrigger+field</code>, so reading a 3.x <code>BLOB_TRIGGERS</code> payload with the compatibility package on 4.x does not survive the rename — do that part of a binary migration on 3.x, which is the recommended path anyway; see `,14),c(g,{to:`/documentation/quartz-4.x/packages/json-serialization.html#migrating-from-binary-serialization`},{default:i(()=>[...l[225]||=[r(`Migrating from binary serialization`,-1)]]),_:1}),l[227]||=r(`.)`,-1)]),l[874]||=t(`<p>The following properties have been removed from <code>TriggerBase</code> as they are redundant with the <code>Key</code> and <code>JobKey</code> properties:</p><table><thead><tr><th>Removed property</th><th>Replacement</th></tr></thead><tbody><tr><td><code>Name</code></td><td><code>Key.Name</code></td></tr><tr><td><code>Group</code></td><td><code>Key.Group</code></td></tr><tr><td><code>JobName</code></td><td><code>JobKey.Name</code></td></tr><tr><td><code>JobGroup</code></td><td><code>JobKey.Group</code></td></tr><tr><td><code>FullName</code></td><td><code>Key.ToString()</code></td></tr><tr><td><code>FullJobName</code></td><td><code>JobKey.ToString()</code></td></tr></tbody></table><p><code>HasMillisecondPrecision</code> left <code>ITrigger</code> and is <code>protected abstract</code> on <code>TriggerBase</code>. It is how a trigger describes its own schedule to the base class — which rounds the start time down to the second when it is false — and nothing outside the trigger acted on it. A custom trigger changes <code>public override</code> to <code>protected override</code>; to test the behaviour, assert on <code>StartTimeUtc.Millisecond</code> instead of on the flag.</p><p><code>TriggerBase.FireInstanceId</code> and <code>IOperableTrigger.FireInstanceId</code> are <code>string?</code>. A store writes the fire instance id as it hands a trigger over, so a trigger straight out of <code>TriggerBuilder</code> — or one read back that has never fired — has always answered <code>null</code> there; 4.0 says so in the type instead of declaring a non-nullable <code>string</code> initialised to <code>null!</code>. A store or a trigger implementation of your own may need a <code>!</code> or a null check where the compiler now asks for one, which is the point of the annotation. <code>IJobExecutionContext.FireInstanceId</code> is unchanged and non-nullable — a context exists only for a firing.</p><h2 id="a-blank-calendar-name-is-no-calendar-name" tabindex="-1"><a class="header-anchor" href="#a-blank-calendar-name-is-no-calendar-name"><span>A blank calendar name is no calendar name</span></a></h2><p><code>TriggerBase.CalendarName</code> stores an empty or whitespace-only name as <code>null</code>, and so do <code>TriggerBuilder.WithCalendarName</code> and <code>TriggerDetailsUpdate.WithCalendarName</code>, which assign through it. The name is not trimmed — a calendar is looked up by the exact name it was registered under — only blanks collapse.</p><p>This closes a trap rather than changing a working behaviour. Every job store reads a non-null calendar name as &quot;this trigger observes a calendar&quot;, looks it up, and drops the fire when it is not found. A trigger holding <code>&quot;&quot;</code> therefore never fired again, and said so only through a single <code>Couldn&#39;t find calendar with name &#39;&#39;</code> line from the ADO delegate — nothing at all under <code>RAMJobStore</code>. Oracle hid the problem entirely, since <code>&#39;&#39;</code> is <code>NULL</code> there.</p><p>If a database already holds <code>CALENDAR_NAME = &#39;&#39;</code> rows — the dashboard&#39;s reschedule wrote them before <a href="https://github.com/quartznet/quartznet/issues/3294" target="_blank" rel="noopener noreferrer">#3294</a> was fixed — <strong>no migration script is needed</strong>. Such a row rehydrates as &quot;no calendar&quot;, so the trigger starts firing again on the next acquisition, and the column is written back as <code>NULL</code> the next time the trigger is persisted.</p><p>The normalization and the missing-calendar warning both job stores now log are not 4.x-only: they ship on 3.x as well, so upgrading is not what fixes them. One part of #3294 <em>is</em> specific to 4.x:</p><ul><li><code>IScheduler.RescheduleJob</code> throws <code>SchedulerException</code> when the new trigger names a calendar that does not exist, the way <code>ScheduleJob</code> always has. On 3.x it stores the trigger and leaves it permanently unfireable, and that was left alone there rather than turning a long-standing silent success into a throw on a released branch. If you reschedule onto a calendar you intend to add afterwards, add the calendar first.</li></ul><h2 id="the-two-job-stores-answer-the-same-way" tabindex="-1"><a class="header-anchor" href="#the-two-job-stores-answer-the-same-way"><span>The two job stores answer the same way</span></a></h2><p><code>RAMJobStore</code> and the ADO.NET job store implement one interface, and on 3.x they quietly disagreed about a handful of answers — each had its own tests, written against whatever the store in front of the author happened to do. <code>JobStoreContractTest</code> runs one set of assertions against both stores, and closing the disagreements it found changed behaviour a 3.x application could have depended on. Each of these is a 3.x behaviour, not a 4.x regression:</p><ul><li><p><strong>A prefix <code>PauseTriggerGroups</code> pauses every group it matches.</strong> <code>RAMJobStore</code> recorded the <em>matcher&#39;s text</em> rather than the group it matched, so <code>PauseTriggerGroups(GroupMatcher&lt;TriggerKey&gt;.GroupStartsWith(&quot;report&quot;))</code> paused the triggers of only the first matching group, returned that one group, and left a phantom entry named <code>report</code> in the paused-group set — which then paused anything later added to a group literally called <code>report</code>. It now records each matched group and pauses all of their triggers, which is what the ADO store always did. <code>ResumeTriggerGroups</code> with a non-equality matcher forgets those groups again; it previously only ever cleared an exact-name pause.</p><p>The semantics both stores now share: a pause remembers the <strong>groups the matcher matched</strong>, not the pattern. A trigger added afterwards to a matched group is born paused; a trigger added to a group that <em>would</em> have matched but did not exist at pause time is not. To pause a group before it exists, pause it by exact name — <code>GroupEquals</code> deliberately records a group that holds nothing yet.</p></li><li><p><strong>A paused job group binds what is added to it on the ADO store too.</strong> <code>PauseJobGroups</code> promises a pause that is &quot;imposed on jobs added to the group afterwards&quot;, and <code>RAMJobStore</code> has always kept it; the ADO store recorded the pause and then consulted only the <em>trigger&#39;s</em> group when it stored one, so a job added to a paused group ran while <code>IsJobGroupPaused</code> and every listing said the group was paused. Storing a trigger now consults the job&#39;s group as well, and one for a job in a paused group is born <code>PAUSED</code> — or <code>PAUSED_BLOCKED</code>, where a trigger-group pause would have said so. One refinement is deliberately not ported: <code>RAMJobStore</code> remembers a job resumed <em>individually</em> inside a still-paused group and leaves it running, where the ADO store imposes the group&#39;s pause on that job&#39;s triggers again the next time they are stored.</p></li><li><p><strong>Pausing no longer discards an error.</strong> <code>RAMJobStore</code> moved a trigger to <code>Paused</code> from every state but <code>Complete</code>, so pausing a trigger — or the group or job it belongs to — silently overwrote <code>TriggerState.Error</code>: the failure disappeared from listings and <code>ResetTriggerFromErrorState</code> found nothing left to reset. Only <code>Normal</code> (waiting), acquired and blocked triggers are pausable now, matching what the ADO store writes <code>PAUSED</code> over. A trigger in error keeps its state, and <code>PauseTrigger</code> returns false for it, because nothing moved.</p><p>Resetting such a trigger while its group is paused lands it in <code>Paused</code> rather than <code>Normal</code> on both stores, which is what the ADO store always did — the group is still paused, so the trigger comes out of error into the pause rather than past it. If you have code that pauses a group to suppress a failing trigger, pause it and then reset it: the pause no longer does both.</p></li><li><p><strong><code>ResumeAll</code> on the ADO store now resumes groups that hold no triggers.</strong> It walked the groups it found in <code>QRTZ_TRIGGERS</code> and cleared only those, plus its own all-groups marker. A group paused while empty — <code>PauseTriggerGroups(GroupEquals(&quot;nightly&quot;))</code> before anything is scheduled into <code>nightly</code>, which is a supported thing to do — is in no trigger row, so its <code>QRTZ_PAUSED_TRIGGER_GRPS</code> row survived the resume and went on pausing everything scheduled into that group afterwards, with no way to see why short of reading the table. <code>ResumeAll</code> clears the whole table for the scheduler now, which is what <code>RAMJobStore</code> always did. The 3.x row can be removed by hand with <code>DELETE FROM QRTZ_PAUSED_TRIGGER_GRPS WHERE SCHED_NAME = &#39;…&#39;</code> if a database carries one.</p></li><li><p><strong>The all-groups-paused marker is no longer listed as a group.</strong> The ADO store records <code>PauseAll</code> as a row named <code>_$_ALL_GROUPS_PAUSED_$_</code> in the same table it lists paused groups from, so <code>GetPausedTriggerGroups()</code> — and <code>QueryTriggerGroups(new TriggerGroupQuery { Paused = true })</code> — handed back a group name no trigger can ever belong to. Code that displayed the list showed it to operators, and code that looped it called <code>ResumeTriggerGroups</code> on a group that does not exist. The listing and its count filter the marker out now; the marker itself is unchanged, so nothing about the schema or about how a pause is recorded moves. <code>RAMJobStore</code> never had such a row.</p></li><li><p><strong>A duplicate says so on both stores.</strong> <code>AddCalendar</code> over an existing name without <code>Replace = true</code>, and <code>AddJob</code> over an existing key without <code>replace: true</code>, raise <code>ObjectAlreadyExistsException</code> on the ADO store as they always did on <code>RAMJobStore</code>. Both calls passed through a blanket <code>catch</code> that re-wrapped it as a plain <code>JobPersistenceException</code> with the real exception in <code>InnerException</code>, so <code>catch (ObjectAlreadyExistsException)</code> worked against one store and not the other. <code>ObjectAlreadyExistsException</code> derives from <code>JobPersistenceException</code>, so code catching the base type is unaffected.</p></li><li><p><strong>The threshold instant itself is a misfire on the ADO store too.</strong> A trigger is late once its fire time is <em>at or before</em> <code>now - MisfireThreshold</code>, and that is now one comparison wherever the question is asked. The ADO store&#39;s periodic sweep asked for <code>NEXT_FIRE_TIME &lt; @nextFireTime</code> while everything else — <code>RAMJobStore</code>, and the ADO store&#39;s own single-trigger path that a resumed or unblocked trigger goes through — used <code>&lt;=</code>, so the ADO store disagreed with the in-memory one and with itself about one tick. The acquisition statement moved with it, from <code>NEXT_FIRE_TIME &gt;= @noEarlierThan</code> to <code>&gt;</code>, so that a waiting trigger belongs to acquisition or to the misfire handler and never to both. It is a change to the SQL only — no schema migration — and the practical effect is that a trigger due at exactly that instant misfires rather than being fired late without its policy. 3.x behaves the old way.</p></li><li><p><strong>An unblocked trigger&#39;s misfire policy is applied as it is unblocked, in memory too.</strong> When a <code>[DisallowConcurrentExecution]</code> job finishes, the triggers of that job that sat <code>Blocked</code> behind it go back to <code>Waiting</code> — and a trigger that passed its fire time while it waited has its misfire policy applied there and then, so its state and <code>NextFireTimeUtc</code> are settled by the time <code>TriggeredJobComplete</code> returns. The ADO store has always done this; <code>RAMJobStore</code> returned the trigger to the acquisition set and left the policy to the next acquisition, so a <code>GetTrigger</code> in between reported the fire time the trigger had already missed, and a trigger past its end time read as waiting on one store and finished on the other. As on the ADO store, an unblocked trigger whose policy leaves it with nothing to fire is removed rather than kept in <code>Complete</code>, so <code>GetTrigger</code> answers <code>null</code> for it. 3.x behaves the old way.</p></li><li><p><strong>A persistent store refuses a repeat interval it cannot hold exactly.</strong> Duration columns keep whole milliseconds, and <code>StdAdoDelegate.GetDbTimeSpanValue</code> cast <code>TotalMilliseconds</code> to <code>long</code>, so a <code>SimpleTrigger</code> whose repeat interval was shorter than a millisecond was stored as <code>0</code> — and a trigger read back with a zero repeat interval throws <code>DivideByZeroException</code> out of <code>GetFireTimeAfter</code> on its next firing, which the store logs and swallows, leaving the row in <code>ACQUIRED</code> for ever. A job that stopped running and said nothing. Scheduling such a trigger into an ADO store now raises <code>ArgumentException</code> naming the trigger, the column and the rule; round the interval to a whole number of milliseconds. This is the one place the two stores are <em>meant</em> to differ: <code>RAMJobStore</code> keeps taking it, because it has no column to round the value to. See <a href="https://github.com/quartznet/quartznet/issues/3673" target="_blank" rel="noopener noreferrer">#3673</a>.</p></li></ul><h2 id="jobkey-and-triggerkey-null-validation" tabindex="-1"><a class="header-anchor" href="#jobkey-and-triggerkey-null-validation"><span>JobKey and TriggerKey Null Validation</span></a></h2><p><code>JobKey</code> and <code>TriggerKey</code> now throw <code>ArgumentNullException</code> when you specify <code>null</code> for <code>name</code> or <code>group</code>. Triggers can no longer be constructed with a null group name. If your code was relying on null group names, switch to an explicit group name.</p><h2 id="jobdatamap-and-schedulercontext-stand-alone" tabindex="-1"><a class="header-anchor" href="#jobdatamap-and-schedulercontext-stand-alone"><span>JobDataMap and SchedulerContext stand alone</span></a></h2><p>On 3.x, <code>JobDataMap</code> and <code>SchedulerContext</code> both derived from <code>Quartz.Util.StringKeyDirtyFlagMap : DirtyFlagMap&lt;string, object&gt;</code> — three public types across two namespaces for one concept, and the base chain published two members that could silently destroy data. The chain is internal now: <code>JobDataMap</code> and <code>SchedulerContext</code> are sealed, self-contained dictionaries implementing <code>IDictionary&lt;string, object?&gt;</code> and <code>IReadOnlyDictionary&lt;string, object?&gt;</code>.</p><p>Almost nothing changes at a call site:</p><ul><li>The typed read accessors — <code>GetInt</code>, <code>GetString</code>, <code>Get&lt;T&gt;</code> and the rest — are extension members in the <code>Quartz</code> namespace (declared in <code>DataMapExtensions</code>), on both <code>JobDataMap</code> and <code>SchedulerContext</code>. <code>map.GetInt(&quot;retries&quot;)</code> compiles unchanged. They are deliberately declared for the two concrete types, not for <code>IReadOnlyDictionary&lt;string, object?&gt;</code>, so they do not appear on every string-keyed dictionary in scope. A variable typed as the removed base class is the one thing that no longer compiles — type it <code>JobDataMap</code> (or <code>IDictionary&lt;string, object?&gt;</code> when only the dictionary surface matters).</li><li><code>Dirty</code> and <code>ClearDirtyFlag()</code> are internal. Calling <code>ClearDirtyFlag()</code> from a job made <code>[PersistJobDataAfterExecution]</code> silently skip re-storing the data — nothing else on the type could destroy data like that. To force a data blob rewrite, put a <code>SchedulerConstants.ForceJobDataMapDirty</code> entry in the source dictionary when constructing the map, which remains the supported mechanism (the binary-to-JSON migration recipe uses it).</li><li>The <code>Get(TKey key)</code> method 3.x had is gone with the base chain. Use the indexer or <code>TryGetValue</code>.</li><li><code>JobDataMap.Equals</code>/<code>GetHashCode</code> compare content now. The inherited comparison looked at key sets only, so two maps with the same keys but different values counted as equal — and assigning such a map as a nested value did not mark the outer map dirty, silently skipping the job store rewrite. Equal maps also hash equally now. <code>SchedulerContext</code> compares by reference.</li><li><code>SchedulerContext</code> is backed by a <code>ConcurrentDictionary&lt;string, object?&gt;</code> and is safe to read and write concurrently — plugins write to it while jobs read it, and enumerating it during a write no longer races.</li></ul><p>The binary-serialized shape of <code>JobDataMap</code> is unchanged: the type keeps <code>[Serializable]</code>, its serialization constructor and the <code>version</code>/<code>dirty</code>/<code>map</code> entries, so <code>JOB_DATA</code> and <code>BLOB_TRIGGERS</code> blobs written by 3.x still load — see <a href="#serializable-survives-only-where-a-database-blob-needs-it"><code>[Serializable]</code> survives only where a database blob needs it</a>.</p><p><code>IsReadOnly</code> is an explicit interface implementation and cannot be accessed directly on the maps. <code>IsFixedSize</code>, <code>SyncRoot</code> and <code>IsSynchronized</code> are gone with the non-generic interfaces — see <a href="#jobdatamap-dropped-the-non-generic-collection-interfaces"><code>JobDataMap</code> dropped the non-generic collection interfaces</a>.</p><h2 id="jobdatamap-dropped-the-non-generic-collection-interfaces" tabindex="-1"><a class="header-anchor" href="#jobdatamap-dropped-the-non-generic-collection-interfaces"><span><code>JobDataMap</code> dropped the non-generic collection interfaces</span></a></h2><p><code>JobDataMap</code> and <code>SchedulerContext</code> (on 3.x, their <code>DirtyFlagMap&lt;TKey, TValue&gt;</code> base) no longer implement <code>System.Collections.IDictionary</code> or <code>System.Collections.ICollection</code>. Those duplicated the generic interfaces with untyped members that cast at runtime — <code>Add(object, object)</code> and the <code>object</code> indexer threw <code>InvalidCastException</code> for a key of the wrong type instead of <code>ArgumentException</code> (#1417), and <code>SyncRoot</code> handed out a lock object the map never took.</p><table><thead><tr><th>3.x</th><th>4.x</th></tr></thead><tbody><tr><td><code>((IDictionary) map).Add(key, value)</code></td><td><code>map.Add(key, value)</code></td></tr><tr><td><code>((IDictionary) map)[key]</code></td><td><code>map[key]</code></td></tr><tr><td><code>((IDictionary) map).Contains(key)</code></td><td><code>map.ContainsKey(key)</code></td></tr><tr><td><code>((IDictionary) map).Remove(key)</code></td><td><code>map.Remove(key)</code></td></tr><tr><td><code>map.CopyTo(array, index)</code> (<code>Array</code>)</td><td><code>map.CopyTo(KeyValuePair&lt;string, object?&gt;[], index)</code></td></tr><tr><td><code>new JobDataMap(someIDictionary)</code></td><td><code>new JobDataMap(someIDictionaryOfStringToObject)</code></td></tr></tbody></table><p><code>ISerializable</code> is untouched, so persisted maps still load. The generic <code>JobDataMap(IDictionary&lt;string, object?&gt;)</code> constructor also took over what the removed non-generic one did with a <code>QRTZ_FORCE_JOB_DATAMAP_DIRTY</code> entry: the entry is not copied, and the new map is left flagged dirty.</p><p>The accessor set gained a reader for <code>decimal</code>, so a <code>decimal</code> in a job data map can now be read back the way every other primitive can. It is spelled <code>Get&lt;decimal&gt;</code> / <code>TryGet&lt;decimal&gt;</code> — see <a href="#one-accessor-per-type-for-the-types-job-data-is-made-of">One accessor per type</a>.</p><h2 id="jobdatamap-s-typed-accessors-are-extension-members" tabindex="-1"><a class="header-anchor" href="#jobdatamap-s-typed-accessors-are-extension-members"><span><code>JobDataMap</code>&#39;s typed accessors are extension members</span></a></h2><p><code>JobDataMap</code> declared sixty typed accessors of its own — <code>GetIntValue</code>, <code>TryGetIntValue</code>, <code>GetIntValueFromString</code>, <code>TryGetIntValueFromString</code>, and the same four for <code>bool</code>, <code>char</code>, <code>double</code>, <code>float</code>, <code>long</code>, <code>Guid</code>, <code>TimeSpan</code>, <code>DateTime</code> and <code>DateTimeOffset</code> — while the <code>StringKeyDirtyFlagMap</code> it derived from declared a second, shorter set doing the same job. Two names for one lookup, differing only in a suffix. The <code>…Value</code> set is gone; the shorter set survives as extension members in the <code>Quartz</code> namespace (declared in <code>DataMapExtensions</code>, for both <code>JobDataMap</code> and <code>SchedulerContext</code>), so the call sites read the same:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> int retries = context.JobDetail.JobDataMap.GetIntValue(&quot;retries&quot;);</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> int retries = context.JobDetail.JobDataMap.GetInt(&quot;retries&quot;);</span>
<span class="line"></span></span></span>
<span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> if (map.TryGetTimeSpanValue(&quot;timeout&quot;, out TimeSpan timeout)) { }</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> if (map.TryGet(&quot;timeout&quot;, out TimeSpan timeout)) { }</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><table><thead><tr><th>3.x <code>JobDataMap</code></th><th>4.x extension members</th></tr></thead><tbody><tr><td><code>GetBooleanValue</code>, <code>GetBooleanValueFromString</code></td><td><code>GetBoolean</code></td></tr><tr><td><code>GetCharFromString</code></td><td><code>Get&lt;char&gt;</code></td></tr><tr><td><code>GetDateTimeValue</code>, <code>GetDateTimeValueFromString</code></td><td><code>Get&lt;DateTime&gt;</code></td></tr><tr><td><code>GetDateTimeOffsetValue</code>, <code>GetDateTimeOffsetValueFromString</code></td><td><code>GetDateTimeOffset</code></td></tr><tr><td><code>GetDoubleValue</code>, <code>GetDoubleValueFromString</code></td><td><code>GetDouble</code></td></tr><tr><td><code>GetFloatValue</code>, <code>GetFloatValueFromString</code></td><td><code>GetFloat</code></td></tr><tr><td><code>GetGuidValue</code>, <code>GetGuidValueFromString</code></td><td><code>Get&lt;Guid&gt;</code></td></tr><tr><td><code>GetIntValue</code>, <code>GetIntValueFromString</code></td><td><code>GetInt</code></td></tr><tr><td><code>GetLongValue</code>, <code>GetLongValueFromString</code></td><td><code>GetLong</code></td></tr><tr><td><code>GetTimeSpanValue</code>, <code>GetTimeSpanValueFromString</code></td><td><code>Get&lt;TimeSpan&gt;</code></td></tr><tr><td>every <code>TryGet…Value</code> / <code>TryGet…ValueFromString</code></td><td>the matching <code>TryGet…</code>, or <code>TryGet&lt;T&gt;</code></td></tr><tr><td><code>GetNullableGuidValue</code></td><td><code>TryGet&lt;Guid&gt;</code>, or read the entry and test it yourself</td></tr><tr><td>(none)</td><td><code>GetString</code>, <code>TryGetString</code>, <code>Get&lt;decimal&gt;</code>, <code>TryGet&lt;decimal&gt;</code></td></tr><tr><td>your own <code>TryGetValue&lt;T&gt;</code> extension</td><td><strong>keep it.</strong> 3.x&#39;s <code>JobDataMap</code> had no <code>TryGetValue&lt;T&gt;</code> either, so such a call was always the caller&#39;s own extension on <code>IDictionary&lt;string, object&gt;</code> — and <code>JobDataMap</code> still implements that interface, so it still binds. Do not substitute <code>TryGet&lt;T&gt;</code>, which is a different thing: see below</td></tr></tbody></table><p>The <code>…FromString</code> half collapses because the retained accessors already convert: a value written as a string — which is what <code>StoreJobDataAsStrings</code> forces, and what <code>PutAsString</code> writes — is parsed on the way out, so one accessor covers both. <code>GetNullableGuidValue</code> is the only one without a direct replacement; it returned <code>null</code> both for &quot;absent&quot; and for &quot;present but not a <code>Guid</code>&quot;, which <code>TryGet&lt;Guid&gt;</code> distinguishes.</p><p><code>PutAsString</code>&#39;s eleven overloads are one generic <code>PutAsString&lt;T&gt;(string key, T value) where T : IConvertible</code>, plus the ones the constraint cannot express (<code>DateTime</code>, <code>DateTimeOffset</code>, <code>DateOnly</code>, <code>TimeOnly</code>, <code>Guid</code>, <code>TimeSpan</code>). Call sites are unchanged, with two exceptions described below.</p><h3 id="one-accessor-per-type-for-the-types-job-data-is-made-of" tabindex="-1"><a class="header-anchor" href="#one-accessor-per-type-for-the-types-job-data-is-made-of"><span>One accessor per type, for the types job data is made of</span></a></h3><p>A named accessor per readable type is a set that only ever grows — 4.0&#39;s previews reached 33 of them per receiver, <code>GetDateOnly</code> and <code>GetTimeOnly</code> and <code>GetEnum&lt;TEnum&gt;</code> included — and the generic <code>Get&lt;T&gt;</code>/<code>TryGet&lt;T&gt;</code> beside them did not subsume any of it, because they were a pure type test: a value written through <code>PutAsString</code> is a <code>string</code>, so <code>Get&lt;Guid&gt;</code> failed where <code>GetGuid</code> parsed.</p><p>4.0 settles it the other way round. <strong><code>Get&lt;T&gt;</code>, <code>TryGet&lt;T&gt;</code> and <code>GetValueOrDefault&lt;T&gt;</code> coerce</strong>, with exactly the rule the named accessors use — the stored type first, then the invariant string form for every type <code>PutAsString</code> writes one of, an enum by name (case-insensitively), and <code>Convert</code> for an exotic stored type. A type Quartz has no string form for, such as an options class of your own, is the plain type test it always was. So the named accessors are down to the set the tutorials teach, and the exotic tail is spelled generically:</p><table><thead><tr><th>Named accessor</th><th>4.0</th></tr></thead><tbody><tr><td><code>GetGuid</code> / <code>TryGetGuid</code></td><td><code>Get&lt;Guid&gt;</code> / <code>TryGet&lt;Guid&gt;</code></td></tr><tr><td><code>GetTimeSpan</code> / <code>TryGetTimeSpan</code></td><td><code>Get&lt;TimeSpan&gt;</code> / <code>TryGet&lt;TimeSpan&gt;</code></td></tr><tr><td><code>GetDecimal</code> / <code>TryGetDecimal</code></td><td><code>Get&lt;decimal&gt;</code> / <code>TryGet&lt;decimal&gt;</code></td></tr><tr><td><code>GetChar</code> / <code>TryGetChar</code></td><td><code>Get&lt;char&gt;</code> / <code>TryGet&lt;char&gt;</code></td></tr><tr><td><code>GetDateTime</code> / <code>TryGetDateTime</code></td><td><code>Get&lt;DateTime&gt;</code> / <code>TryGet&lt;DateTime&gt;</code></td></tr><tr><td><code>GetDateOnly</code> / <code>TryGetDateOnly</code></td><td><code>Get&lt;DateOnly&gt;</code> / <code>TryGet&lt;DateOnly&gt;</code></td></tr><tr><td><code>GetTimeOnly</code> / <code>TryGetTimeOnly</code></td><td><code>Get&lt;TimeOnly&gt;</code> / <code>TryGet&lt;TimeOnly&gt;</code></td></tr><tr><td><code>GetEnum&lt;TEnum&gt;</code> / <code>TryGetEnum&lt;TEnum&gt;</code></td><td><code>Get&lt;TEnum&gt;</code> / <code>TryGet&lt;TEnum&gt;</code></td></tr></tbody></table><p>What stays named, on both <code>JobDataMap</code> and <code>SchedulerContext</code>: <code>GetInt</code>, <code>GetLong</code>, <code>GetFloat</code>, <code>GetDouble</code>, <code>GetBoolean</code>, <code>GetString</code> and <code>GetDateTimeOffset</code>, each with its <code>TryGet…</code> twin, plus the three generic readers. That is 17 members per receiver where there were 33, and nothing can be read that could not be read before — including under <code>StoreJobDataAsStrings = true</code>, which is the setting that made the old <code>Get&lt;T&gt;</code> a trap.</p><p><code>Get&lt;T&gt;</code> throws a <code>KeyNotFoundException</code> naming the key when there is no entry, and an <code>InvalidCastException</code> naming the key, the stored type and the requested one when the entry cannot be read as a <code>T</code>; <code>TryGet&lt;T&gt;</code> answers <code>false</code> for either; <code>GetValueOrDefault&lt;T&gt;(key, defaultValue)</code> answers the fallback for either.</p><div class="hint-container warning"><p class="hint-container-title"><code>GetValueOrDefault&lt;T&gt;</code> and <code>TryGet&lt;T&gt;</code> changed answer for a string</p><p>They coerce now. <code>map.GetValueOrDefault(&quot;retries&quot;, -1)</code> on an entry holding <code>&quot;42&quot;</code> answered <code>-1</code> in the previews and answers <code>42</code> in 4.0, and <code>TryGet&lt;int&gt;</code> on the same entry answers <code>true</code> rather than <code>false</code>. That is the point of the change — the value <em>is</em> 42, and the store is what turned it into a string — but code that used the old behaviour to detect &quot;stored as a string&quot; needs <code>map.TryGet(&quot;key&quot;, out string _)</code> instead.</p></div><h3 id="putasstring-writes-round-trip-formats-now" tabindex="-1"><a class="header-anchor" href="#putasstring-writes-round-trip-formats-now"><span><code>PutAsString</code> writes round-trip formats now</span></a></h3><p><code>PutAsString(key, dateTimeOffset)</code> used to write the invariant general form — no fractional seconds — and a <code>DateTime</code> argument bound to the <code>IConvertible</code> overload, which erased sub-second precision <em>and</em> <code>DateTimeKind</code>. Both now write the round-trip (&quot;O&quot;) format: what you read back is what you stored, to the tick, Kind and offset included. Reading is unaffected for existing data — the accessors parse both the old general form and &quot;O&quot; — but two edges are observable:</p><ul><li><strong>Overload rebinding</strong>: <code>map.PutAsString(key, someDateTime)</code> used to bind to the generic <code>IConvertible</code> overload and store <code>&quot;01/02/2026 15:04:05&quot;</code>; it now binds to the dedicated <code>DateTime</code> overload and stores <code>&quot;2026-01-02T15:04:05.0000000&quot;</code>. Anything <em>outside</em> Quartz that reads <code>JOB_DATA</code> strings and expects the old shape sees the new one after the value is next written.</li><li><strong><code>Get&lt;DateTime&gt;</code> parses with <code>DateTimeStyles.RoundtripKind</code></strong> — its own behavioral change, independent of what was written: a stored string ending in <code>Z</code> used to come back shifted to the reader&#39;s local time with <code>Kind=Local</code>; it now comes back with the UTC clock reading and <code>Kind=Utc</code>. That is the correct reading, but a job computing e.g. <code>DateTime.Now - map.Get&lt;DateTime&gt;(key)</code> on such a value shifts by the local UTC offset. Such <code>Z</code> strings exist in real stores — the System.Text.Json serializer writes a boxed UTC <code>DateTime</code> as <code>&quot;…Z&quot;</code> and hands it back as a raw string.</li></ul><h3 id="putasstring-t-is-constrained-to-iformattable" tabindex="-1"><a class="header-anchor" href="#putasstring-t-is-constrained-to-iformattable"><span><code>PutAsString&lt;T&gt;</code> is constrained to <code>IFormattable</code></span></a></h3><p>The generic overload asked for <code>IConvertible</code>, the legacy conversion interface, and then only ever called its formatting member. It asks for <code>IFormattable</code> now, which is what it actually uses. The practical effect is a wider set of types, not a narrower one: <code>Int128</code>, <code>Half</code>, <code>BigInteger</code>, <code>Complex</code> and any formattable type of your own were never <code>IConvertible</code> and could not be written this way.</p><p><code>bool</code> and <code>char</code> are <code>IConvertible</code> but not <code>IFormattable</code> — neither has anything to format for a culture — so they gained dedicated overloads and keep writing exactly what they wrote before (<code>&quot;True&quot;</code> / <code>&quot;False&quot;</code>, and the single character). The six round-trip overloads are unchanged.</p><p><code>string</code> is the one type that loses the call: <code>map.PutAsString(key, someString)</code> no longer compiles. Write <code>map[key] = someString</code>, which is what it did.</p><h3 id="putasstring-string-guid-is-gone" tabindex="-1"><a class="header-anchor" href="#putasstring-string-guid-is-gone"><span><code>PutAsString(string, Guid?)</code> is gone</span></a></h3><p>Passing <code>null</code> stored a present-but-null entry that nothing could read back — <code>TryGet&lt;Guid&gt;</code> returned <code>false</code>, <code>Get&lt;Guid&gt;</code> threw, and under <code>StoreJobDataAsStrings = true</code> the null was coerced to an empty string with the same outcome. An unreadable entry is worse than a missing key, so the overload is gone rather than fixed: call <code>PutAsString(key, value.Value)</code> when there is a value, and decide explicitly — usually <code>map.Remove(key)</code> — when there is not.</p><h2 id="listener-api-changes" tabindex="-1"><a class="header-anchor" href="#listener-api-changes"><span>Listener API Changes</span></a></h2><p>The three kinds of listener are managed identically now: a listener is registered under a name, registering the same name again replaces it, and it is removed by that name.</p><table><thead><tr><th>3.x</th><th>4.x</th></tr></thead><tbody><tr><td><code>AddJobListener(l, params IMatcher&lt;JobKey&gt;[])</code> and <code>AddJobListener(l, IReadOnlyCollection&lt;…&gt;)</code></td><td>one <code>AddJobListener(l, params IReadOnlyCollection&lt;IMatcher&lt;JobKey&gt;&gt;)</code></td></tr><tr><td><code>AddTriggerListener(l, params IMatcher&lt;TriggerKey&gt;[])</code> and the collection overload</td><td>one <code>AddTriggerListener(l, params IReadOnlyCollection&lt;IMatcher&lt;TriggerKey&gt;&gt;)</code></td></tr><tr><td><code>GetJobListeners()</code>, <code>GetTriggerListeners()</code>, <code>GetSchedulerListeners()</code> → arrays</td><td>→ <code>IReadOnlyList&lt;T&gt;</code></td></tr><tr><td><code>AddJobListenerMatcher</code>, <code>RemoveJobListenerMatcher</code>, <code>SetJobListenerMatchers</code>, <code>GetJobListenerMatchers</code></td><td>gone — see <a href="#matchers-are-given-at-registration">Matchers are given at registration</a></td></tr><tr><td><code>AddTriggerListenerMatcher</code>, <code>RemoveTriggerListenerMatcher</code>, <code>SetTriggerListenerMatchers</code>, <code>GetTriggerListenerMatchers</code></td><td>gone — same</td></tr><tr><td><code>GetJobListener(name)</code>, <code>GetTriggerListener(name)</code> throw <code>KeyNotFoundException</code></td><td>return null</td></tr><tr><td><code>RemoveSchedulerListener(ISchedulerListener)</code></td><td><code>RemoveSchedulerListener(string name)</code></td></tr><tr><td>—</td><td><code>GetSchedulerListener(string name)</code> → <code>ISchedulerListener?</code></td></tr></tbody></table><p>C# 13 params collections turn the two <code>Add*Listener</code> overloads into a single member that accepts both call shapes, so existing calls compile unchanged:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token comment">// both still work</span></span>
<span class="line">scheduler<span class="token punctuation">.</span>ListenerManager<span class="token punctuation">.</span><span class="token function">AddJobListener</span><span class="token punctuation">(</span>myJobListener<span class="token punctuation">,</span> matcherA<span class="token punctuation">,</span> matcherB<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">scheduler<span class="token punctuation">.</span>ListenerManager<span class="token punctuation">.</span><span class="token function">AddJobListener</span><span class="token punctuation">(</span>myJobListener<span class="token punctuation">,</span> listOfMatchers<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Asking for a listener that is not registered returns null instead of throwing, so checking is no longer an exception:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> try { var listener = listenerManager.GetJobListener(name); } catch (KeyNotFoundException) { }</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> IJobListener? listener = listenerManager.GetJobListener(name);</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="matchers-are-given-at-registration" tabindex="-1"><a class="header-anchor" href="#matchers-are-given-at-registration"><span>Matchers are given at registration</span></a></h3><p>Eight members let an application edit a registered listener&#39;s matcher set at run time, one matcher at a time, by listener name: <code>AddJobListenerMatcher</code>, <code>RemoveJobListenerMatcher</code>, <code>SetJobListenerMatchers</code>, <code>GetJobListenerMatchers</code>, and their trigger twins. They are gone. Matchers are part of registration, which is when anyone knows them, and <code>Add*Listener(listener, params matchers)</code> is where they are given — including through the container, as <code>AddJobListener&lt;T&gt;(matchers)</code>.</p><p>A listener whose matching has to change is registered again under the same name. That replaces the listener and its matchers together, so the two can never be out of step — which is what the remove-then-re-add dance these members existed to avoid was actually about:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> listenerManager.SetJobListenerMatchers(&quot;audit&quot;, [GroupMatcher&lt;JobKey&gt;.GroupEquals(&quot;reports&quot;)]);</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> listenerManager.AddJobListener(audit, GroupMatcher&lt;JobKey&gt;.GroupEquals(&quot;reports&quot;));</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> listenerManager.AddJobListenerMatcher(&quot;audit&quot;, GroupMatcher&lt;JobKey&gt;.GroupEquals(&quot;ingest&quot;));</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> // name every matcher the listener is to have, since the registration replaces the whole set</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line"> listenerManager.AddJobListener(audit,</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">     GroupMatcher&lt;JobKey&gt;.GroupEquals(&quot;reports&quot;),</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">     GroupMatcher&lt;JobKey&gt;.GroupEquals(&quot;ingest&quot;));</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Reading a listener&#39;s matchers back had one real caller — the notification path&#39;s own matching — and the scheduler now reads them from the listener&#39;s registration rather than by looking the name up. There is no replacement for <code>GetJobListenerMatchers</code> on <code>IListenerManager</code>: hold on to the matchers you registered with if you need them.</p><h3 id="the-listener-property-keys-are-retired" tabindex="-1"><a class="header-anchor" href="#the-listener-property-keys-are-retired"><span>The listener property keys are retired</span></a></h3><p><code>quartz.jobListener.&lt;name&gt;.type</code> and <code>quartz.triggerListener.&lt;name&gt;.type</code> named a listener class for Quartz to find by reflection and attach. They are <strong>rejected</strong> now, with a message naming the registration that replaced them, rather than accepted and ignored — a key that quietly stopped attaching a listener is a pipeline with a hole in it and no symptom.</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> [&quot;quartz.jobListener.audit.type&quot;] = &quot;Acme.AuditListener, Acme&quot;,</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"> [&quot;quartz.triggerListener.audit.type&quot;] = &quot;Acme.AuditTriggerListener, Acme&quot;,</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line">services<span class="token punctuation">.</span><span class="token function">AddQuartz</span><span class="token punctuation">(</span>q <span class="token operator">=&gt;</span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    q<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">AddJobListener</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>AuditListener<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span>GroupMatcher<span class="token operator">&lt;</span>JobKey<span class="token operator">&gt;</span><span class="token punctuation">.</span><span class="token function">GroupEquals</span><span class="token punctuation">(</span><span class="token string">&quot;reports&quot;</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">    q<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">AddTriggerListener</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>AuditTriggerListener<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"><span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The registration is strictly the stronger of the two. The keys had no way to say which jobs or triggers a listener heard, so a listener named by configuration heard <em>everything</em> — matchers are given at registration, and configuration is not a registration. They had no scheduler-listener twin either, so <code>ISchedulerListener</code> was never reachable this way at all. And they resolved a type from a string, which no trimmed or native-AOT publish can follow.</p><p>Two details are worth knowing while porting:</p><ul><li><strong>The name comes from the listener now.</strong> The key&#39;s <code>&lt;name&gt;</code> was written onto the listener&#39;s <code>Name</code> property after it was built, which is how the listener manager identified it. A registration carries no name of its own, so the listener says what it is called — <code>Name =&gt; &quot;audit&quot;</code>, or the <code>GetType().Name</code> default. Two listeners of one kind that must answer to different names are two instances registered with <code>AddJobListener(listener, matchers)</code>.</li><li><strong>The other <code>quartz.jobListener.&lt;name&gt;.&lt;property&gt;</code> keys go too.</strong> They were applied to the listener by reflection after it was built. A listener built by the container takes what it needs through its constructor, and options of its own through <code>IOptions&lt;T&gt;</code> like anything else.</li></ul><p><code>PropertyListenerFactory</code>, which built these listeners, is gone with them; it was internal, so nothing named it.</p><h3 id="the-broadcast-listeners-are-gone" tabindex="-1"><a class="header-anchor" href="#the-broadcast-listeners-are-gone"><span>The broadcast listeners are gone</span></a></h3><p><code>BroadcastJobListener</code>, <code>BroadcastTriggerListener</code> and <code>BroadcastSchedulerListener</code> fanned one notification out to several listeners — which is what the listener manager does for every registered listener already. Register the listeners individually:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> scheduler.ListenerManager.AddJobListener(new BroadcastJobListener(&quot;audit&quot;, [first, second]));</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> scheduler.ListenerManager.AddJobListener(first);</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line"> scheduler.ListenerManager.AddJobListener(second);</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The one thing a broadcast did that individual registration does not is group several listeners under a single name, so that one <code>RemoveJobListener</code> detached all of them; remove them by their own names instead. A broadcast was also a public type that had to track every default-implemented member of its interface forever, and it swallowed what a listener behind it threw, which the manager does not.</p><h3 id="listeners-are-told-which-scheduler-is-calling" tabindex="-1"><a class="header-anchor" href="#listeners-are-told-which-scheduler-is-calling"><span>Listeners are told which scheduler is calling</span></a></h3><p>The listener callbacks that run inside a firing have always been able to say who was calling: <code>IJobExecutionContext.Scheduler</code>. The rest could not. A listener registered with two schedulers in one host — which <code>AddQuartz(&quot;acme&quot;, …)</code> and <code>AddQuartz(&quot;initech&quot;, …)</code> make an ordinary thing to do — was told that <em>a</em> trigger had been paused, or that <em>a</em> scheduler had failed, with no way to ask which one (#3063).</p><p>There is one rule now, and it holds for all three listener interfaces: <strong>a listener reaches the scheduler it serves through its execution context, or as an argument of the callback when there is no execution.</strong> That makes <code>IScheduler</code> a parameter of every one of <code>ISchedulerListener</code>&#39;s twenty-three members, and of <code>ITriggerListener.TriggerMisfired</code> — the one trigger callback with no context to carry it, because a misfire is noticed rather than executed.</p><p>Where that parameter goes differs between the two interfaces, and the difference is settled rather than unfinished. <code>ISchedulerListener</code> takes the scheduler <strong>first</strong>, because its notifications are about the scheduler: they have no other subject to lead with, and the scheduler is a scheduler listener&#39;s only channel to the thing it is listening to. <code>ITriggerListener</code> leads with the <strong>trigger</strong> on all four of its members, and <code>TriggerMisfired</code> takes the scheduler second, where its siblings take an <code>IJobExecutionContext</code> — see <a href="#triggermisfired-takes-the-trigger-first"><code>TriggerMisfired</code> takes the trigger first</a>.</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> public ValueTask SchedulerStarted(CancellationToken cancellationToken = default)</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> public ValueTask SchedulerStarted(IScheduler scheduler, CancellationToken cancellationToken = default)</span>
<span class="line"></span></span><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line"> {</span>
<span class="line"></span></span><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line">     logger.LogInformation(&quot;Scheduler started&quot;);</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line">     logger.LogInformation(&quot;Scheduler {SchedulerName} started&quot;, scheduler.SchedulerName);</span>
<span class="line"></span></span><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line">     return default;</span>
<span class="line"></span><span class="token prefix unchanged"> </span><span class="token line"> }</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>It is the <code>IScheduler</code> rather than a name or an identity record, because a listener that knows <em>which</em> scheduler usually wants to <em>act</em> on it — pause the trigger it was just told about, read <code>Status</code>. Identity is <code>SchedulerName</code> and <code>SchedulerInstanceId</code> on the same object.</p><p>The instance handed over is the one <code>ISchedulerFactory.GetScheduler()</code> returns, and the very one <code>IJobExecutionContext.Scheduler</code> carries, so those two compare by reference. An <code>IScheduler</code> injected from the container does not: that is a proxy that resolves the scheduler on first use, so compare a <code>SchedulerName</code> there rather than the objects.</p><h4 id="schedulererror-says-what-the-error-was-about" tabindex="-1"><a class="header-anchor" href="#schedulererror-says-what-the-error-was-about"><span><code>SchedulerError</code> says what the error was about</span></a></h4><p><code>SchedulerError</code> took a message and an exception, which said what happened but never what it happened to. Most of the places that raise it know: a job that threw is reported from its execution context, a job that could not be built from the bundle that was fired. Those facts now travel in a <code>SchedulerErrorContext</code>:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> ValueTask SchedulerError(string msg, SchedulerException cause, CancellationToken cancellationToken = default);</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> ValueTask SchedulerError(IScheduler scheduler, SchedulerErrorContext errorContext, CancellationToken cancellationToken = default);</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token keyword">public</span> <span class="token keyword">sealed</span> <span class="token keyword">record</span> <span class="token class-name">SchedulerErrorContext</span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    <span class="token keyword">public</span> required <span class="token return-type class-name"><span class="token keyword">string</span></span> Message <span class="token punctuation">{</span> <span class="token keyword">get</span><span class="token punctuation">;</span> <span class="token keyword">init</span><span class="token punctuation">;</span> <span class="token punctuation">}</span></span>
<span class="line">    <span class="token keyword">public</span> required <span class="token return-type class-name">SchedulerException</span> Exception <span class="token punctuation">{</span> <span class="token keyword">get</span><span class="token punctuation">;</span> <span class="token keyword">init</span><span class="token punctuation">;</span> <span class="token punctuation">}</span></span>
<span class="line">    <span class="token keyword">public</span> <span class="token return-type class-name">TriggerKey<span class="token punctuation">?</span></span> TriggerKey <span class="token punctuation">{</span> <span class="token keyword">get</span><span class="token punctuation">;</span> <span class="token keyword">init</span><span class="token punctuation">;</span> <span class="token punctuation">}</span></span>
<span class="line">    <span class="token keyword">public</span> <span class="token return-type class-name">JobKey<span class="token punctuation">?</span></span> JobKey <span class="token punctuation">{</span> <span class="token keyword">get</span><span class="token punctuation">;</span> <span class="token keyword">init</span><span class="token punctuation">;</span> <span class="token punctuation">}</span></span>
<span class="line">    <span class="token keyword">public</span> <span class="token return-type class-name"><span class="token keyword">string</span><span class="token punctuation">?</span></span> FireInstanceId <span class="token punctuation">{</span> <span class="token keyword">get</span><span class="token punctuation">;</span> <span class="token keyword">init</span><span class="token punctuation">;</span> <span class="token punctuation">}</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>Message</code> and <code>Exception</code> are the two old parameters, so a listener that only logged needs one rename:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> public ValueTask SchedulerError(string message, SchedulerException exception, CancellationToken cancellationToken = default)</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> public ValueTask SchedulerError(IScheduler scheduler, SchedulerErrorContext errorContext, CancellationToken cancellationToken = default)</span>
<span class="line"></span></span><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line"> {</span>
<span class="line"></span></span><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line">     logger.LogError(exception, &quot;{Message}&quot;, message);</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line">     logger.LogError(errorContext.Exception, &quot;{Message}&quot;, errorContext.Message);</span>
<span class="line"></span></span><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line">     return default;</span>
<span class="line"></span><span class="token prefix unchanged"> </span><span class="token line"> }</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The three keys are nullable because some errors genuinely have no subject: a scan for the next trigger to fire that never reached a trigger, a job store retrying a failed connection, a schedule file that names many jobs. Read a null as &quot;the scheduler could not say&quot;, not as &quot;this concerns no trigger&quot;. Where the scheduler does know — every failure inside a firing, and a misfire notification that a listener broke — all three are filled in, which is what discussion #3211 asked for: the trigger behind a <code>SchedulerError</code>.</p><p><code>ISchedulerSignaler.NotifySchedulerListenersError</code> takes the record for the same reason, so a job store of your own can report the trigger it failed for:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> ValueTask NotifySchedulerListenersError(string message, SchedulerException exception, CancellationToken cancellationToken = default);</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> ValueTask NotifySchedulerListenersError(SchedulerErrorContext errorContext, CancellationToken cancellationToken = default);</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><h4 id="the-compiler-will-not-point-at-the-callbacks-you-have-to-change-but-the-registration-will" tabindex="-1"><a class="header-anchor" href="#the-compiler-will-not-point-at-the-callbacks-you-have-to-change-but-the-registration-will"><span>The compiler will not point at the callbacks you have to change, but the registration will</span></a></h4><p>Every member of these interfaces has a default implementation, which is what lets a listener implement only the notifications it cares about — and it is also why a listener that keeps an old signature still compiles. It simply stops being an implementation of anything: the default runs instead, and the method becomes dead code that is never called.</p><p>So the build does not fail. Registering the listener does. Quartz reads the shape of a listener as it is registered and refuses one that carries a public method with a notification&#39;s name but not its signature, naming both signatures in a <code>SchedulerConfigException</code> (#3398):</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text"><pre><code class="language-text"><span class="line">MyListener declares &#39;ValueTask SchedulerError(String, SchedulerException, CancellationToken)&#39;, which does</span>
<span class="line">not implement ISchedulerListener.SchedulerError. The interface member is</span>
<span class="line">&#39;ValueTask SchedulerError(IScheduler, SchedulerErrorContext, CancellationToken)&#39;: the names match but the</span>
<span class="line">signatures do not, and every member of ISchedulerListener has a default implementation, so this compiles</span>
<span class="line">and the default runs instead. The scheduler never calls SchedulerError. Listener callbacks take</span>
<span class="line">IScheduler scheduler first since 4.0.0-alpha.2. Correct the signature, or rename the method if it is not</span>
<span class="line">meant to be that notification. See</span>
<span class="line">https://www.quartz-scheduler.net/documentation/quartz-4.x/migration-guide.html#listeners-are-told-which-scheduler-is-calling.</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Where that lands depends on how the listener was registered. <code>q.AddJobListener&lt;MyListener&gt;()</code> and the instance overload throw from the <code>AddQuartz</code> call itself, since the listener&#39;s type is known while the configuration is still being written. Everything else — a factory overload declared as the interface, a listener registered as a plain service, and <code>scheduler.ListenerManager.AddJobListener(…)</code> — throws when the listener is attached to a scheduler, which for a hosted application is host start.</p><p>A method that deliberately overloads a notification&#39;s name for an unrelated purpose is refused as well. Rename it: the alternative is letting the far commoner stale signature through in silence. A listener that implements the interface explicitly is never examined, because an explicit implementation is not a public method of the class.</p><p>The table below is the whole list of what moved. The compile-time hints are worth knowing too: a callback that no longer overrides anything and does not touch instance state trips CA1822 (&quot;can be marked as static&quot;), and an <code>&lt;inheritdoc /&gt;</code> on it trips MA0196. Both fired on Quartz&#39;s own listeners while this change was made.</p><h4 id="every-signature-that-changed" tabindex="-1"><a class="header-anchor" href="#every-signature-that-changed"><span>Every signature that changed</span></a></h4><table><thead><tr><th>3.x</th><th>4.x</th></tr></thead><tbody><tr><td><code>ISchedulerListener.JobScheduled(ITrigger, ct)</code></td><td><code>JobScheduled(IScheduler, ITrigger, ct)</code></td></tr><tr><td><code>ISchedulerListener.JobUnscheduled(TriggerKey, ct)</code></td><td><code>JobUnscheduled(IScheduler, TriggerKey, ct)</code></td></tr><tr><td><code>ISchedulerListener.TriggerFinalized(ITrigger, ct)</code></td><td><code>TriggerFinalized(IScheduler, ITrigger, ct)</code></td></tr><tr><td><code>ISchedulerListener.TriggerPaused(TriggerKey, ct)</code></td><td><code>TriggerPaused(IScheduler, TriggerKey, ct)</code></td></tr><tr><td><code>ISchedulerListener.TriggersPaused(string?, ct)</code></td><td><code>TriggersPaused(IScheduler, string?, ct)</code></td></tr><tr><td><code>ISchedulerListener.TriggerResumed(TriggerKey, ct)</code></td><td><code>TriggerResumed(IScheduler, TriggerKey, ct)</code></td></tr><tr><td><code>ISchedulerListener.TriggersResumed(string?, ct)</code></td><td><code>TriggersResumed(IScheduler, string?, ct)</code></td></tr><tr><td><code>ISchedulerListener.TriggerInError(TriggerKey, ct)</code></td><td><code>TriggerInError(IScheduler, TriggerKey, ct)</code></td></tr><tr><td><code>ISchedulerListener.TriggersInError(JobKey, ct)</code></td><td><code>TriggersInError(IScheduler, JobKey, ct)</code></td></tr><tr><td><code>ISchedulerListener.JobAdded(IJobDetail, ct)</code></td><td><code>JobAdded(IScheduler, IJobDetail, ct)</code></td></tr><tr><td><code>ISchedulerListener.JobDeleted(JobKey, ct)</code></td><td><code>JobDeleted(IScheduler, JobKey, ct)</code></td></tr><tr><td><code>ISchedulerListener.JobPaused(JobKey, ct)</code></td><td><code>JobPaused(IScheduler, JobKey, ct)</code></td></tr><tr><td><code>ISchedulerListener.JobsPaused(string?, ct)</code></td><td><code>JobsPaused(IScheduler, string?, ct)</code></td></tr><tr><td><code>ISchedulerListener.JobResumed(JobKey, ct)</code></td><td><code>JobResumed(IScheduler, JobKey, ct)</code></td></tr><tr><td><code>ISchedulerListener.JobsResumed(string?, ct)</code></td><td><code>JobsResumed(IScheduler, string?, ct)</code></td></tr><tr><td><code>ISchedulerListener.JobInterrupted(JobKey, ct)</code></td><td><code>JobInterrupted(IScheduler, JobKey, ct)</code></td></tr><tr><td><code>ISchedulerListener.SchedulerError(string, SchedulerException, ct)</code></td><td><code>SchedulerError(IScheduler, SchedulerErrorContext, ct)</code></td></tr><tr><td><code>ISchedulerListener.SchedulerStarting(ct)</code></td><td><code>SchedulerStarting(IScheduler, ct)</code></td></tr><tr><td><code>ISchedulerListener.SchedulerStarted(ct)</code></td><td><code>SchedulerStarted(IScheduler, ct)</code></td></tr><tr><td><code>ISchedulerListener.SchedulerInStandbyMode(ct)</code></td><td><code>SchedulerInStandbyMode(IScheduler, ct)</code></td></tr><tr><td><code>ISchedulerListener.SchedulerShuttingDown(ct)</code></td><td><code>SchedulerShuttingDown(IScheduler, ct)</code></td></tr><tr><td><code>ISchedulerListener.SchedulerShutdown(ct)</code></td><td><code>SchedulerShutdown(IScheduler, ct)</code></td></tr><tr><td><code>ISchedulerListener.SchedulingDataCleared(ct)</code></td><td><code>SchedulingDataCleared(IScheduler, ct)</code></td></tr><tr><td><code>ITriggerListener.TriggerMisfired(ITrigger, ct)</code></td><td><code>TriggerMisfired(ITrigger, IScheduler, ct)</code></td></tr></tbody></table><p><code>IJobListener</code>&#39;s members and <code>ITriggerListener</code>&#39;s other three are unchanged: they all receive an <code>IJobExecutionContext</code>, which is how they reach the scheduler already.</p><h4 id="triggermisfired-takes-the-trigger-first" tabindex="-1"><a class="header-anchor" href="#triggermisfired-takes-the-trigger-first"><span><code>TriggerMisfired</code> takes the trigger first</span></a></h4><p><code>ITriggerListener</code>&#39;s members lead with the trigger they are about, and <code>TriggerMisfired</code> is no exception: the scheduler it gains follows the trigger, in the position <code>TriggerFired</code>, <code>VetoJobExecution</code> and <code>TriggerComplete</code> give the <code>IJobExecutionContext</code>.</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> public ValueTask TriggerMisfired(ITrigger trigger, CancellationToken cancellationToken = default)</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> public ValueTask TriggerMisfired(ITrigger trigger, IScheduler scheduler, CancellationToken cancellationToken = default)</span>
<span class="line"></span></span><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line"> {</span>
<span class="line"></span><span class="token prefix unchanged"> </span><span class="token line">     logger.LogWarning(&quot;{SchedulerName} missed {TriggerKey}&quot;, scheduler.SchedulerName, trigger.Key);</span>
<span class="line"></span><span class="token prefix unchanged"> </span><span class="token line">     return default;</span>
<span class="line"></span><span class="token prefix unchanged"> </span><span class="token line"> }</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>A listener that writes the two the other way round still compiles — and quietly stops implementing the interface, with the do-nothing default running in its place. Quartz refuses a listener in that shape as it is registered, and says the parameters are the notification&#39;s own, in the wrong order. That is worth knowing if you ran a 4.0 pre-release, where the scheduler was briefly the first parameter; see <a href="#appendix-if-you-ran-a-4-0-pre-release">Appendix: if you ran a 4.0 pre-release</a>.</p><h3 id="the-three-support-base-classes-are-gone" tabindex="-1"><a class="header-anchor" href="#the-three-support-base-classes-are-gone"><span>The three <code>*Support</code> base classes are gone</span></a></h3><p><code>JobListenerSupport</code>, <code>TriggerListenerSupport</code> and <code>SchedulerListenerSupport</code> existed so that a listener could implement only the notifications it cared about. Every member of <code>IJobListener</code>, <code>ITriggerListener</code> and <code>ISchedulerListener</code> is now a default interface member — the notifications do nothing, and <code>Name</code> returns <code>GetType().Name</code> — so the base classes had nothing left to add, and they cost you your one base class.</p><p>Implement the interface instead, and drop <code>override</code>:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> public sealed class MyListener : JobListenerSupport</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> public sealed class MyListener : IJobListener</span>
<span class="line"></span></span><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line"> {</span>
<span class="line"></span></span><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line">     public override string Name =&gt; &quot;MyListener&quot;;</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line">     public override ValueTask JobWasExecuted(IJobExecutionContext context, JobExecutionException? jobException, CancellationToken cancellationToken = default)</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line">     public ValueTask JobWasExecuted(IJobExecutionContext context, JobExecutionException? jobException, CancellationToken cancellationToken = default)</span>
<span class="line"></span></span><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line">     {</span>
<span class="line"></span><span class="token prefix unchanged"> </span><span class="token line">         ...</span>
<span class="line"></span><span class="token prefix unchanged"> </span><span class="token line">     }</span>
<span class="line"></span><span class="token prefix unchanged"> </span><span class="token line"> }</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>Name</code> can go: it defaults to the type&#39;s name. Keep it only when several instances of one type are registered with the same scheduler, since the later registration would otherwise replace the earlier one.</p><p>A listener that derived from one of these base classes fails to compile, which is the outcome you want. A 3.x listener that implemented the interface directly has the silent version of the same problem: its <code>Task</code>-returning members compile against 4.x and implement nothing. Quartz refuses such a listener when it is registered and names the member — see <a href="#the-compiler-will-not-point-at-the-callbacks-you-have-to-change-but-the-registration-will">The compiler will not point at the callbacks you have to change, but the registration will</a>.</p><p>One consequence is easy to miss: <strong>a default interface member is not a class member</strong>. Code that reads <code>Name</code> off the concrete type no longer compiles unless the listener declares <code>Name</code> itself, so read it through the interface:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> var listener = new MyListener();</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"> scheduler.ListenerManager.RemoveSchedulerListener(listener.Name);</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> ISchedulerListener listener = new MyListener();</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line"> scheduler.ListenerManager.RemoveSchedulerListener(listener.Name);</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="scheduler-listeners-are-identified-by-name" tabindex="-1"><a class="header-anchor" href="#scheduler-listeners-are-identified-by-name"><span>Scheduler listeners are identified by name</span></a></h3><p><code>ISchedulerListener</code> has a <code>Name</code>, a default interface member returning <code>GetType().Name</code>. Registering two scheduler listeners whose <code>Name</code> matches replaces the first, exactly as it has always worked for job and trigger listeners. Override <code>Name</code> if you register several instances of one type with the same scheduler:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> scheduler.ListenerManager.RemoveSchedulerListener(mySchedulerListener);</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> scheduler.ListenerManager.RemoveSchedulerListener(mySchedulerListener.Name);</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>A test double does not run a default interface member, so a faked <code>ISchedulerListener</code> needs its <code>Name</code> configured before <code>AddSchedulerListener</code> will accept it:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token class-name">ISchedulerListener</span> listener <span class="token operator">=</span> A<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">Fake</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>ISchedulerListener<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">A<span class="token punctuation">.</span><span class="token function">CallTo</span><span class="token punctuation">(</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">=&gt;</span> listener<span class="token punctuation">.</span>Name<span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">Returns</span><span class="token punctuation">(</span><span class="token string">&quot;myListener&quot;</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="jobspaused-and-jobsresumed-take-a-nullable-group" tabindex="-1"><a class="header-anchor" href="#jobspaused-and-jobsresumed-take-a-nullable-group"><span><code>JobsPaused</code> and <code>JobsResumed</code> take a nullable group</span></a></h3><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> ValueTask JobsPaused(string jobGroup, CancellationToken cancellationToken = default);</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"> ValueTask JobsResumed(string jobGroup, CancellationToken cancellationToken = default);</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> ValueTask JobsPaused(IScheduler scheduler, string? jobGroup, CancellationToken cancellationToken = default);</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line"> ValueTask JobsResumed(IScheduler scheduler, string? jobGroup, CancellationToken cancellationToken = default);</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Null means every group, matching <code>TriggersPaused</code> and <code>TriggersResumed</code>, which were already nullable. The scheduler&#39;s own pause-all path still raises the job events once per group; the signature simply stops claiming a group name is guaranteed where the trigger twins admit it is not. Implementations with a non-nullable parameter produce a nullability warning (an error under <code>TreatWarningsAsErrors</code>) until the <code>?</code> is added.</p><p><code>JobScheduled(ITrigger)</code> and <code>JobUnscheduled(TriggerKey)</code> remain asymmetric on purpose: once a trigger is unscheduled there is no trigger left to hand out.</p><h3 id="two-members-were-renamed" tabindex="-1"><a class="header-anchor" href="#two-members-were-renamed"><span>Two members were renamed</span></a></h3><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> ValueTask SchedulerShuttingdown(CancellationToken cancellationToken = default);</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> ValueTask SchedulerShuttingDown(CancellationToken cancellationToken = default);</span>
<span class="line"></span></span></span>
<span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> ValueTask SchedulerError(string msg, SchedulerException cause, CancellationToken cancellationToken = default);</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> ValueTask SchedulerError(IScheduler scheduler, SchedulerErrorContext errorContext, CancellationToken cancellationToken = default);</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>SchedulerError</code>&#39;s two parameters became one record on the way — see <a href="#listeners-are-told-which-scheduler-is-calling">Listeners are told which scheduler is calling</a>.</p><h3 id="jobinterrupted-says-which-firing-was-interrupted" tabindex="-1"><a class="header-anchor" href="#jobinterrupted-says-which-firing-was-interrupted"><span><code>JobInterrupted</code> says which firing was interrupted</span></a></h3><p><code>ISchedulerListener</code> gained an overload, as a default interface member:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token return-type class-name">ValueTask</span> <span class="token function">JobInterrupted</span><span class="token punctuation">(</span><span class="token class-name">IScheduler</span> scheduler<span class="token punctuation">,</span> <span class="token class-name">JobKey</span> jobKey<span class="token punctuation">,</span> <span class="token class-name"><span class="token keyword">string</span></span> fireInstanceId<span class="token punctuation">,</span> <span class="token class-name">CancellationToken</span> cancellationToken <span class="token operator">=</span> <span class="token keyword">default</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>A job without <code>[DisallowConcurrentExecution]</code> can have several firings in flight at once and the key names all of them, so a listener told only the key could not say which execution was cancelled, could not correlate the interruption with that firing&#39;s own <code>JobToBeExecuted</code>/<code>JobWasExecuted</code>, and could not tell an <code>Interrupt(jobKey)</code> that cancelled four firings from one that cancelled one.</p><p>The scheduler raises the new overload. <code>InterruptFireInstance</code> raises it once; <strong><code>Interrupt(jobKey)</code> raises it once per firing it cancelled</strong>, where it used to raise one notification however many it stopped. The default body calls the key-only overload, so a listener that implements that one keeps working — and now hears once per interrupted firing. Implement one or the other, not both.</p><h3 id="instantiation-failures-name-the-trigger" tabindex="-1"><a class="header-anchor" href="#instantiation-failures-name-the-trigger"><span>Instantiation failures name the trigger</span></a></h3><p>When <code>IJobFactory</code> cannot produce a job — a constructor dependency the container cannot resolve is the usual reason — the trigger has already fired, but there is no <code>IJobExecutionContext</code> yet, so no <code>ITriggerListener</code> or <code>IJobListener</code> callback can be raised. <code>SchedulerError</code> is the only notification, and it used to carry the job key as interpolated message text and the trigger nowhere at all.</p><p>It now receives a <code>JobInstantiationException</code>, and the <code>SchedulerErrorContext</code> around it names the same firing without any unwrapping:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token keyword">public</span> <span class="token return-type class-name">ValueTask</span> <span class="token function">SchedulerError</span><span class="token punctuation">(</span><span class="token class-name">IScheduler</span> scheduler<span class="token punctuation">,</span> <span class="token class-name">SchedulerErrorContext</span> errorContext<span class="token punctuation">,</span> <span class="token class-name">CancellationToken</span> cancellationToken <span class="token operator">=</span> <span class="token keyword">default</span><span class="token punctuation">)</span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    <span class="token keyword">if</span> <span class="token punctuation">(</span>errorContext<span class="token punctuation">.</span>Exception <span class="token keyword">is</span> <span class="token class-name">JobInstantiationException</span> failure<span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">{</span></span>
<span class="line">        logger<span class="token punctuation">.</span><span class="token function">LogError</span><span class="token punctuation">(</span>failure<span class="token punctuation">,</span> <span class="token string">&quot;Job {Job} could not be built for trigger {Trigger}, fire {FireInstanceId}&quot;</span><span class="token punctuation">,</span></span>
<span class="line">            errorContext<span class="token punctuation">.</span>JobKey<span class="token punctuation">,</span> errorContext<span class="token punctuation">.</span>TriggerKey<span class="token punctuation">,</span> errorContext<span class="token punctuation">.</span>FireInstanceId<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">    <span class="token punctuation">}</span></span>
<span class="line"></span>
<span class="line">    <span class="token keyword">return</span> <span class="token keyword">default</span><span class="token punctuation">;</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Additive — <code>SchedulerError</code> already took a <code>SchedulerException</code> — and it mirrors what <code>JobExecutionProcessException</code> carries for execution-time failures. Both factory paths raise it: the container&#39;s, where <code>ActivatorUtilities</code> throws, and <code>SimpleJobFactory</code>&#39;s, where the original failure arrives as the <code>InnerException</code>.</p><p>The two messages reporting this also had their closing quote moved to where it belongs, from after the inner exception&#39;s message to after the type name: <code>Problem instantiating type &#39;MyNamespace.MyJob&#39;: message</code>. Code matching on that text should read the exception instead.</p>`,134),e(`p`,null,[l[229]||=r(`To prevent the failure rather than observe it, see `,-1),c(g,{to:`/documentation/quartz-4.x/packages/microsoft-di-integration.html#failing-fast-when-job-dependencies-cannot-be-resolved`},{default:i(()=>[...l[228]||=[r(`failing fast when job dependencies cannot be resolved`,-1)]]),_:1}),l[230]||=r(`.`,-1)]),l[875]||=t(`<h3 id="triggers-entering-the-error-state-are-reported" tabindex="-1"><a class="header-anchor" href="#triggers-entering-the-error-state-are-reported"><span>Triggers entering the error state are reported</span></a></h3><p>A trigger could be parked in <code>TriggerState.Error</code> with nothing observing it. The stores logged a line and moved on, so a trigger simply stopped working and the only way to find out was to poll <code>GetTriggerState</code> or read the log — and two of the ADO store&#39;s transitions, a job type that will not load while acquiring and a job that cannot be read back in <code>TriggersFired</code>, did not reach the scheduler at all.</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token return-type class-name">ValueTask</span> <span class="token function">TriggerInError</span><span class="token punctuation">(</span><span class="token class-name">IScheduler</span> scheduler<span class="token punctuation">,</span> <span class="token class-name">TriggerKey</span> triggerKey<span class="token punctuation">,</span> <span class="token class-name">CancellationToken</span> cancellationToken <span class="token operator">=</span> <span class="token keyword">default</span><span class="token punctuation">)</span> <span class="token operator">=&gt;</span> <span class="token keyword">default</span><span class="token punctuation">;</span></span>
<span class="line"><span class="token return-type class-name">ValueTask</span> <span class="token function">TriggersInError</span><span class="token punctuation">(</span><span class="token class-name">IScheduler</span> scheduler<span class="token punctuation">,</span> <span class="token class-name">JobKey</span> jobKey<span class="token punctuation">,</span> <span class="token class-name">CancellationToken</span> cancellationToken <span class="token operator">=</span> <span class="token keyword">default</span><span class="token punctuation">)</span> <span class="token operator">=&gt;</span> <span class="token keyword">default</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>Following the singular/plural pair <code>ISchedulerListener</code> already has for pause and resume. Both are default-implemented, as are the matching <code>ISchedulerSignaler.NotifySchedulerListenersTriggerInError</code> and <code>NotifySchedulerListenersTriggersInError</code>, so an existing listener or signaler still compiles and behaves exactly as it does today — no notification.</p><p>The plural is keyed by <code>JobKey</code> rather than by the individual triggers because <code>SetAllJobTriggersError</code> is one bulk statement in the persistent store, and enumerating the affected keys would mean an extra query on a failure path. Ask <code>GetTriggersOfJob</code> where the keys themselves matter.</p><p>Neither carries a cause. The stores raise these and receive only a <code>SchedulerInstruction</code>; <code>SchedulerError</code> says <em>why</em> and these say <em>what changed</em>, and where both apply they arrive together. Recover a trigger with <code>IScheduler.ResetTriggerFromErrorState</code>.</p><p><code>JobChainingJobListener</code> is <code>sealed</code> — see <a href="#the-shipped-implementations-are-sealed">The shipped implementations are sealed</a>.</p><p>An <code>IJobStore</code> that implements <code>IJobListener</code> no longer automatically receives all events. Register it explicitly as a job listener using <code>ListenerManager</code>:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line">scheduler<span class="token punctuation">.</span>ListenerManager<span class="token punctuation">.</span><span class="token function">AddJobListener</span><span class="token punctuation">(</span>myJobStoreListener<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><h2 id="scheduler-configuration-validation" tabindex="-1"><a class="header-anchor" href="#scheduler-configuration-validation"><span>Scheduler Configuration Validation</span></a></h2>`,10),e(`ul`,null,[l[237]||=e(`li`,null,[e(`code`,null,`IdleWaitTime`),r(` values less than or equal to zero are no longer silently replaced with a 30-second default — they now throw.`)],-1),l[238]||=e(`li`,null,[r(`Negative values for `),e(`code`,null,`IdleWaitTime`),r(` or `),e(`code`,null,`BatchTimeWindow`),r(` are rejected.`)],-1),l[239]||=e(`li`,null,[e(`code`,null,`MaxBatchSize`),r(` values less than or equal to zero are rejected.`)],-1),e(`li`,null,[l[232]||=e(`code`,null,`MaxBatchSize`,-1),l[233]||=r(` may not exceed `,-1),l[234]||=e(`code`,null,`ThreadPoolOptions.MaxConcurrency`,-1),l[235]||=r(`. The two are configured through different builder methods and different configuration sections, so the pair is only ever wrong by accident — and triggers acquired beyond the number of threads available to run them are held by this node, unfireable by any other, until the pool drains. See `,-1),c(g,{to:`/documentation/quartz-4.x/tutorial/advanced-enterprise-features.html#batching-trigger-acquisition`},{default:i(()=>[...l[231]||=[r(`Batching trigger acquisition`,-1)]]),_:1}),l[236]||=r(`.`,-1)])]),l[876]||=t(`<h3 id="a-wait-no-timer-will-take-is-refused-where-it-was-configured" tabindex="-1"><a class="header-anchor" href="#a-wait-no-timer-will-take-is-refused-where-it-was-configured"><span>A wait no timer will take is refused where it was configured</span></a></h3><p><code>Task.Delay</code> tops out at <code>uint.MaxValue - 1</code> milliseconds — a little under 50 days — and refuses anything longer with an <code>ArgumentOutOfRangeException</code> naming a parameter called <code>delay</code>. Every duration Quartz waits out that way is configurable and none of them was checked against it, so a value past the ceiling started fine and was reported later, from wherever the wait happened to be observed: <code>MisfireHandlerFrequency</code>&#39;s surfaced out of the ADO store&#39;s <code>Shutdown</code>, saying nothing about the setting or the limit (<a href="https://github.com/quartznet/quartznet/issues/3577" target="_blank" rel="noopener noreferrer">#3577</a>).</p><p>Six settings are now refused at startup, and the failure names the option, the ceiling and the value:</p><table><thead><tr><th>Setting</th><th>Which wait it becomes</th></tr></thead><tbody><tr><td><code>AdoJobStoreOptions.MisfireHandlerFrequency</code></td><td>the pause between misfire passes</td></tr><tr><td><code>AdoJobStoreOptions.MisfireThreshold</code></td><td>the same, when no frequency is set — that is when the threshold <em>is</em> the frequency</td></tr><tr><td><code>AdoJobStoreOptions.DbRetryInterval</code></td><td>the pause before retrying a failed connection</td></tr><tr><td><code>AdoJobStoreOptions.TransientRetryInterval</code></td><td>the pause before retrying a transient failure</td></tr><tr><td><code>ClusteringOptions.CheckinInterval</code></td><td>the pause between cluster check-ins</td></tr><tr><td><code>QuartzHostedServiceOptions.StartDelay</code></td><td>what the hosted service waits before starting the scheduler</td></tr></tbody></table><p>Two more are checked where they are written rather than through <code>ValidateOnStart</code>: a lock handler&#39;s <code>RetryPeriod</code> has no options type at all, so the setter refuses it, and <code>IScheduler.StartDelayed</code> checks its argument — it waits on a task nobody observes, so a delay the timer refused used to fault that task, be collected in silence, and leave a scheduler that never started.</p><p><code>IdleWaitTime</code> deliberately has no ceiling: it is spent on a <code>SemaphoreSlim</code> so that a scheduling change can cut it short, and a semaphore takes a timeout of any length. It is bounded below and not above.</p><p>Nothing about this is a break for a configuration anyone meant — a wait of fifty days is not a setting, it is a typo or an overflowed unit conversion — but it is a startup failure where the same value used to start.</p><h3 id="validation-happens-at-startup-through-one-mechanism" tabindex="-1"><a class="header-anchor" href="#validation-happens-at-startup-through-one-mechanism"><span>Validation happens at startup, through one mechanism</span></a></h3><p>Options validation used to fire when the options were first resolved — which is inside a scheduler factory, so the stack trace pointed at Quartz internals rather than at the registration line — and only if something resolved them at all. Every Quartz options type is now registered with <code>ValidateOnStart</code>, so a bad value fails <code>Host.Build()</code> with every failure listed.</p><p>Which types are checked follows what the scheduler actually reads. <code>QuartzSchedulerOptions</code> and <code>ThreadPoolOptions</code> always; <code>InMemoryJobStoreOptions</code> when <code>UseInMemoryStore</code> was called; <code>AdoJobStoreOptions</code> and <code>ClusteringOptions</code> when <code>UsePersistentStore</code> was; <code>DataSourceOptions</code> when a data source was configured. Validating the ADO options for an in-memory scheduler would have turned an unset <code>DataSource</code> into a startup failure for a configuration nobody wrote.</p><p>The two satellite packages that validated differently now use <code>IValidateOptions&lt;T&gt;</code> like everything else, so one exception type — <code>OptionsValidationException</code> — reports every configuration mistake:</p><table><thead><tr><th>Before</th><th>After</th></tr></thead><tbody><tr><td><code>AddOptions&lt;QuartzHttpApiOptions&gt;().Validate(lambda, message)</code></td><td><code>QuartzHttpApiOptionsValidator</code>, with <code>ValidateOnStart()</code></td></tr><tr><td><code>HttpClientOptions.AssertValid()</code> throwing <code>InvalidOperationException</code></td><td><code>HttpClientOptionsValidator</code>, throwing <code>OptionsValidationException</code> from <code>AddQuartzHttpClient</code></td></tr></tbody></table><p><code>ValidateOnStart</code> is inert in the container <code>QuartzSchedulerBuilder</code> builds, because nothing there plays the host&#39;s part of resolving <code>IStartupValidator</code>. <code>Build()</code> passes <code>ValidateOnBuild</code>, which checks the object graph rather than the values, so a bad value there still surfaces when the component reading it is built.</p><h3 id="useclustering-c-c-enabled-false-is-now-an-error" tabindex="-1"><a class="header-anchor" href="#useclustering-c-c-enabled-false-is-now-an-error"><span><code>UseClustering(c =&gt; c.Enabled = false)</code> is now an error</span></a></h3><p>It used to be undefined: the builder turned clustering on, the callback turned it off, and the store was left with database locking on, no cluster manager and no check-in row — with nothing said. It fails validation now. A scheduler that should not cluster does not call <code>UseClustering</code>.</p><h3 id="ignoreduplicates-on-its-own-turns-overwriting-off" tabindex="-1"><a class="header-anchor" href="#ignoreduplicates-on-its-own-turns-overwriting-off"><span><code>IgnoreDuplicates</code> on its own turns overwriting off</span></a></h3><p><code>Scheduling.IgnoreDuplicates</code> and <code>Scheduling.OverwriteExistingData</code> are two answers to one question — what to do with a declared job or trigger whose key is already stored — and <code>OverwriteExistingData</code> wins. It defaults to <code>true</code>, so on 3.x setting <code>IgnoreDuplicates</code> and nothing else did nothing at all: an application asking to <em>pass over</em> a duplicate got <em>replace it</em>, which is the opposite, in silence and at every start.</p><p><strong>Setting <code>IgnoreDuplicates</code> now turns overwriting off</strong>, because the default of <code>true</code> is a default rather than a statement — the option records whether its setter ran, and configuration binding and a <code>Configure&lt;QuartzOptions&gt;</code> callback both go through it. So the natural one-liner does what it says:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line">services<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">Configure</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>QuartzOptions<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span>options <span class="token operator">=&gt;</span> options<span class="token punctuation">.</span>Scheduling<span class="token punctuation">.</span>IgnoreDuplicates <span class="token operator">=</span> <span class="token boolean">true</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>Writing <em>both</em> down is still refused at startup, because that is an application asking for opposite things, and the failure names both settings. The three positions are therefore:</p><table><thead><tr><th>What you write</th><th>What happens to a duplicate key</th></tr></thead><tbody><tr><td>neither</td><td>replaced</td></tr><tr><td><code>IgnoreDuplicates = true</code></td><td>passed over</td></tr><tr><td><code>OverwriteExistingData = false</code></td><td>reported as an error</td></tr></tbody></table><p>The same rule reaches the scheduling files: a document whose processing directives carry <code>ignore-duplicates</code> and say nothing about <code>overwrite-existing-data</code> gets overwriting turned off, in both the XML and the JSON plugin.</p><div class="hint-container warning"><p class="hint-container-title">beta.1 refused the one-liner</p><p>On beta.1 <code>options.Scheduling.IgnoreDuplicates = true</code> alone was a fatal <code>OptionsValidationException</code> at startup. If you worked around it by adding <code>OverwriteExistingData = false</code>, that spelling keeps working and means the same thing; if you worked around it by adding <code>OverwriteExistingData = true</code>, remove that line — it is now the pair that is refused, and it was never what you wanted.</p></div><h2 id="cron-parser-enhancements" tabindex="-1"><a class="header-anchor" href="#cron-parser-enhancements"><span>Cron Parser Enhancements</span></a></h2><p>The cron expression parser now supports additional syntax:</p>`,25),e(`ul`,null,[l[244]||=t(`<li><code>L</code> and <code>LW</code> combinations in day-of-month expressions (e.g., <code>LW</code> for last weekday of the month)</li><li><code>LW-&lt;OFFSET&gt;</code> for offset from the last weekday (e.g., <code>LW-2</code> for two days before the last weekday). If the calculated day crosses a month boundary, it resets to the 1st.</li><li>Day-of-month and day-of-week can be specified together. A field written <code>*</code> or <code>?</code> <strong>restricts nothing</strong>, so the other field decides: <code>0 15 10 1 * *</code> is the 1st of the month, and <code>0 15 10 * * MON</code> is every Monday. When <strong>both</strong> fields name days, the expression fires on the union of the two — <code>0 15 10 1,2,3 * MON,FRI</code> is the 1st, 2nd and 3rd <strong>and</strong> every Monday and Friday. This is the Unix <code>crontab(5)</code> rule. It is deliberately <strong>not</strong> Cronos&#39;s, which ANDs even when both fields are restricted; the union is what Quartz has always documented. <code>?</code> and <code>*</code> are now full synonyms in a day field, so <code>? * ?</code> is legal and means every day.</li>`,3),e(`li`,null,[l[241]||=e(`code`,null,`H`,-1),l[242]||=r(` (hash) tokens for `,-1),c(g,{to:`/documentation/quartz-4.x/cron-expressions.html#h-hash-for-load-distribution`},{default:i(()=>[...l[240]||=[r(`load distribution`,-1)]]),_:1}),l[243]||=r(` across triggers`,-1)])]),l[877]||=t(`<p>Parse errors name the fix instead of only the constraint. A 5-field Unix/crontab expression — the shape every online cron generator emits — is not Quartz&#39;s own form (Quartz cron puts seconds first), so reading one <em>as Quartz</em> is still an error; but the error now shows the corrected 6-field expression to use, names <code>CronFormat.Unix</code> as the way to read it as written instead, and the day-of-week range error explains that Quartz numbers days 1-7 starting at Sunday where Unix cron uses 0-6, recommending names (<code>SUN</code>, <code>MON</code>, …) as the unambiguous spelling. When the 5-field expression&#39;s day-of-week is a bare number the error goes one step further and shows the renumbered expression too: prepending a seconds field to <code>30 4 * * 1</code> keeps the <code>1</code>, and <code>1</code> is Monday in crontab but Sunday in Quartz, so the message names <code>&quot;0 30 4 ? * MON&quot;</code> as the schedule that was meant.</p><p>A day name in the <strong>month</strong> field gets the same advice, because that is what a pasted crontab line looks like from the parser&#39;s side: <code>0 12 * * MON</code> never reaches the field count, since crontab&#39;s fifth field is day-of-week and Quartz&#39;s fifth is month, so <code>MON</code> is rejected as a month name first.</p><h3 id="a-cron-expression-can-be-written-in-the-unix-five-field-form" tabindex="-1"><a class="header-anchor" href="#a-cron-expression-can-be-written-in-the-unix-five-field-form"><span>A cron expression can be written in the Unix five-field form</span></a></h3><p><code>CronFormat.Quartz</code> is the default and nothing changes without asking for it. <code>CronFormat.Unix</code> reads the five-field crontab form, and there are exactly three doors:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line">CronExpression<span class="token punctuation">.</span><span class="token function">Parse</span><span class="token punctuation">(</span><span class="token string">&quot;30 4 * * 1&quot;</span><span class="token punctuation">,</span> CronFormat<span class="token punctuation">.</span>Unix<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">CronExpression<span class="token punctuation">.</span><span class="token function">TryParse</span><span class="token punctuation">(</span>candidate<span class="token punctuation">,</span> CronFormat<span class="token punctuation">.</span>Unix<span class="token punctuation">,</span> <span class="token keyword">out</span> <span class="token class-name">CronExpression<span class="token punctuation">?</span></span> expression<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">CronScheduleBuilder<span class="token punctuation">.</span><span class="token function">Create</span><span class="token punctuation">(</span><span class="token string">&quot;30 4 * * 1&quot;</span><span class="token punctuation">,</span> CronFormat<span class="token punctuation">.</span>Unix<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>WithCronSchedule</code> has no format overload; compose one — <code>WithCronSchedule(CronExpression.Parse(s, CronFormat.Unix))</code>. A time zone composes the same way: <code>CronExpression.Parse(s, CronFormat.Unix).WithTimeZone(tz)</code>.</p><p>The format decides two things and nothing else. <strong>The field layout</strong>: five fields, minutes first, with no seconds and no year. <strong>The day-of-week numbering</strong>: 0-7, with both 0 and 7 meaning Sunday, so <code>1-5</code> is Monday to Friday as it is in crontab. Everything else is one grammar — <code>L</code>, <code>W</code> and <code>#</code> all work inside the five-field layout, and <code>L</code> alone in day-of-week still means Saturday, because it is not a number and so has nothing to renumber. When both day fields name days the expression fires on the union, which is <code>crontab(5)</code>&#39;s rule and the same one Quartz applies to its own expressions.</p><p><strong><code>H</code> composes with the format too.</strong> <code>CronScheduleBuilder.Create(s, CronFormat.Unix)</code> reads it: it rewrites the expression to the Quartz form first and then defers the hash to the trigger&#39;s identity, so <code>Create(&quot;H 4 * * 1&quot;, CronFormat.Unix)</code> on a trigger identified as <code>nightly</code> comes out as <code>0 13 4 ? * MON</code>. <code>CronExpression.ParseWithHash(s, CronFormat, hashKey)</code>, <code>ParseWithHash(s, CronFormat, hashSeed)</code> and <code>TryParseWithHash(s, CronFormat, hashKey, out …)</code> run the same rewrite first and are the way to do it without a trigger. Because the rewrite runs first, an <code>H</code> in a five-field day-of-week is hashed over Quartz&#39;s 1-7 rather than crontab&#39;s 0-7, so it never lands on a day the renumbering would have moved. The format-aware <code>Parse</code> and <code>TryParse</code> take no hash key, so there is nothing for <code>H</code> to hash against; <code>ResolveHash</code> still takes a hash key but no format, because it answers with a string rather than an expression and so stays a Quartz-form operation.</p><p><strong>The expression is normalised to the canonical Quartz form, and the original is not recoverable.</strong><code>CronExpression.Parse(&quot;30 4 * * 1&quot;, CronFormat.Unix).CronExpressionString</code> is <code>&quot;0 30 4 ? * MON&quot;</code>, and that is what <code>QRTZ_CRON_TRIGGERS.CRON_EXPRESSION</code>, the dashboard and the HTTP API all show. That is deliberate: a stored string that parses only under a flag the store does not persist would be a trap, so there is no <code>CRON_FORMAT</code> column and there will not be one. It is the same trade as the uppercasing that has always turned <code>mon-fri</code> into <code>MON-FRI</code>.</p><table><thead><tr><th>crontab</th><th>stored, and shown, as</th></tr></thead><tbody><tr><td><code>30 4 * * 1</code></td><td><code>0 30 4 ? * MON</code></td></tr><tr><td><code>0 12 1 * *</code></td><td><code>0 0 12 1 * ?</code></td></tr><tr><td><code>* * * * *</code></td><td><code>0 * * * * ?</code></td></tr><tr><td><code>0 0 13 * 5</code></td><td><code>0 0 0 13 * FRI</code> — the 13th <strong>and</strong> every Friday, by the union rule</td></tr><tr><td><code>0 0 * * 0-6</code></td><td><code>0 0 0 * * ?</code></td></tr><tr><td><code>0 0 * * 5-1</code></td><td><code>0 0 0 ? * FRI-MON</code></td></tr><tr><td><code>0 0 * * 1/2</code></td><td><code>0 0 0 ? * 2/2</code> — numeric, because <code>MON/2</code> is <a href="#the-parser-refuses-what-it-used-to-ignore">rejected</a></td></tr></tbody></table><p>Because the format is an argument to the parse, <strong>the XML schema, the HTTP API and the dashboard do not take one</strong>: a five-field expression is something you write in C#, not something you store. The <code>@</code> macros below have no such limit.</p><h3 id="daily-and-the-rest-of-the-macros-work-everywhere" tabindex="-1"><a class="header-anchor" href="#daily-and-the-rest-of-the-macros-work-everywhere"><span><code>@daily</code> and the rest of the macros work everywhere</span></a></h3><p>The <code>@</code> macros need no format, because they name the same schedule in every cron there is. They are expanded by the <code>CronExpression</code> constructor, which is what every entry point goes through, so they are read wherever an expression string is — <code>&lt;cron-expression&gt;@daily&lt;/cron-expression&gt;</code> in an XML scheduling file, the dashboard&#39;s expression box and the HTTP API included.</p><table><thead><tr><th>macro</th><th>expands to</th></tr></thead><tbody><tr><td><code>@yearly</code>, <code>@annually</code></td><td><code>0 0 0 1 1 ?</code></td></tr><tr><td><code>@monthly</code></td><td><code>0 0 0 1 * ?</code></td></tr><tr><td><code>@weekly</code></td><td><code>0 0 0 ? * SUN</code></td></tr><tr><td><code>@daily</code>, <code>@midnight</code></td><td><code>0 0 0 * * ?</code></td></tr><tr><td><code>@hourly</code></td><td><code>0 0 * * * ?</code></td></tr></tbody></table><p>This is Vixie&#39;s set and only Vixie&#39;s set: there is no <code>@every_minute</code> or <code>@every_second</code>, and no jittered variant, because <code>0 * * * * ?</code> is already short and <code>H</code> spreads load deterministically. <code>@reboot</code> is rejected by name — a scheduler has no reboot to fire on — and any other <code>@name</code> is rejected with the supported list. The expansion is what gets stored, so a trigger written with <code>@daily</code> shows <code>0 0 0 * * ?</code>.</p><h3 id="a-bad-argument-rather-than-a-bad-expression" tabindex="-1"><a class="header-anchor" href="#a-bad-argument-rather-than-a-bad-expression"><span>A bad argument, rather than a bad expression</span></a></h3><p>Two entry points answer differently now, and neither is about the grammar.</p><p><code>CronExpression.TryParse(s, format, out)</code> returns <code>false</code> for a <code>CronFormat</code> it does not know instead of raising <code>ArgumentOutOfRangeException</code>: the one method whose whole promise is that it will not throw does not throw. <code>Parse(s, format)</code> still raises it, and documents it.</p><p><code>new CronExpression(null)</code> and <code>CronScheduleBuilder.Create(null)</code> raise <code>ArgumentNullException</code>, where 3.x raised <code>ArgumentException(&quot;cronExpression cannot be null&quot;)</code> — <code>ArgumentNullException</code> is what <code>CronExpression.Parse</code> next door has always raised. It is an <code>ArgumentException</code>, so a <code>catch</code> still catches; the message is the framework&#39;s rather than Quartz&#39;s.</p><h3 id="the-parser-refuses-what-it-used-to-ignore" tabindex="-1"><a class="header-anchor" href="#the-parser-refuses-what-it-used-to-ignore"><span>The parser refuses what it used to ignore</span></a></h3><p>A handful of expressions parsed, and then meant something other than what they said. Each is now a <code>FormatException</code> whose message names the expression that says what the author meant.</p><table><thead><tr><th>Expression</th><th>3.x</th><th>4.0</th></tr></thead><tbody><tr><td><code>1-5W</code></td><td>the <code>W</code> was dropped; meant <code>1-5</code></td><td><code>FormatException</code> — write <code>1W,2W,3W,4W,5W</code> or drop the <code>W</code></td></tr><tr><td><code>? * L-3</code>, <code>? * LW</code></td><td>the suffix was dropped; meant Saturday</td><td><code>FormatException</code> — those forms belong to day-of-month. A bare <code>L</code> in day-of-week is still Saturday</td></tr><tr><td><code>MON,FRI#3</code></td><td>fired on the third <strong>Monday</strong>; the Friday was ignored</td><td><code>FormatException</code> — <code>#</code> cannot appear beside other days</td></tr><tr><td><code>5C</code>, <code>1C</code></td><td>parsed, meant <code>5</code> / <code>1</code>; <code>C</code> was never implemented</td><td><code>FormatException</code> — use <code>ModifiedByCalendar</code></td></tr><tr><td><code>*/0</code>, <code>5/0</code>, <code>0-10/0</code></td><td>a step of 0 degenerated to no step</td><td><code>FormatException</code></td></tr><tr><td><code>0-10/120</code></td><td>the step was never range-checked</td><td><code>FormatException</code> — <code>Increment &gt; 59 : 120</code>, as <code>0/120</code> already did</td></tr><tr><td><code>MON/2</code></td><td>every second week — a fortnight with <strong>no stable phase</strong></td><td><code>FormatException</code> — <code>MON,WED,FRI</code> for a step through the week, or <code>RecurrenceScheduleBuilder.Create(&quot;FREQ=WEEKLY;INTERVAL=2;BYDAY=MO&quot;)</code> for every second Monday</td></tr></tbody></table><p><code>MON/2</code> is the one that changes a schedule rather than only a spelling, so it is worth the paragraph. A textual day-of-week followed by <code>/</code> meant &quot;every N weeks&quot;, and the numeric <code>2/2</code> beside it meant an ordinary step — two readings of the same grammar. The fortnight also had no stable phase: it counted whole weeks from wherever the search started, so a misfire, a restart, a failover or a dashboard query recomputed it from a different day and moved it. It is rejected rather than quietly re-read as a step, because re-reading it would turn a fortnightly job into a thrice-weekly one — 26 fires a year become 156 — with nothing logged. Every second Monday is <code>RecurrenceScheduleBuilder.Create(&quot;FREQ=WEEKLY;INTERVAL=2;BYDAY=MO&quot;)</code>, which anchors the interval on the trigger&#39;s start time and so keeps its phase.</p><h3 id="before-you-upgrade" tabindex="-1"><a class="header-anchor" href="#before-you-upgrade"><span>Before you upgrade</span></a></h3><p>Every row in that table is a string 3.x stored happily. The exception is thrown from <code>CronTriggerImpl.CronExpressionString</code>&#39;s setter, which the ADO.NET job store calls while materialising a row, so a surviving expression fails the <em>read</em> rather than only the trigger. Audit the stored expressions first:</p><div class="language-sql line-numbers-mode" data-highlighter="prismjs" data-ext="sql"><pre><code class="language-sql"><span class="line"><span class="token comment">-- run against every scheduler&#39;s tables before upgrading; substitute your table prefix</span></span>
<span class="line"><span class="token keyword">SELECT</span> SCHED_NAME<span class="token punctuation">,</span> TRIGGER_NAME<span class="token punctuation">,</span> TRIGGER_GROUP<span class="token punctuation">,</span> CRON_EXPRESSION</span>
<span class="line"><span class="token keyword">FROM</span>   QRTZ_CRON_TRIGGERS</span>
<span class="line"><span class="token keyword">WHERE</span>  CRON_EXPRESSION <span class="token operator">LIKE</span> <span class="token string">&#39;%C%&#39;</span>    <span class="token comment">-- &#39;C&#39; (calendar), never implemented</span></span>
<span class="line">   <span class="token operator">OR</span>  CRON_EXPRESSION <span class="token operator">LIKE</span> <span class="token string">&#39;%/0%&#39;</span>   <span class="token comment">-- step of zero</span></span>
<span class="line">   <span class="token operator">OR</span>  CRON_EXPRESSION <span class="token operator">LIKE</span> <span class="token string">&#39;%#%,%&#39;</span>  <span class="token comment">-- &#39;#&#39; beside other days</span></span>
<span class="line">   <span class="token operator">OR</span>  CRON_EXPRESSION <span class="token operator">LIKE</span> <span class="token string">&#39;%,%#%&#39;</span></span>
<span class="line">   <span class="token operator">OR</span>  CRON_EXPRESSION <span class="token operator">LIKE</span> <span class="token string">&#39;%/%&#39;</span>    <span class="token comment">-- then eyeball for a textual day-of-week step</span></span>
<span class="line">   <span class="token operator">OR</span>  CRON_EXPRESSION <span class="token operator">LIKE</span> <span class="token string">&#39;%W%&#39;</span><span class="token punctuation">;</span>   <span class="token comment">-- then eyeball for &#39;W&#39; after a range</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Several of those clauses are deliberately over-wide and need reading by eye: most <code>/</code> is an ordinary step, most <code>W</code> is a legitimate <code>15W</code> or <code>LW</code>, and <code>%C%</code> also matches the month names <code>DEC</code> and <code>OCT</code>, because expressions are stored upper-cased. <code>CronExpression.TryParse</code> against the 4.0 assembly is the authoritative check once you have a candidate list.</p><p>Two sources are <strong>not</strong> reachable from this query. <code>CronCalendar</code> expressions live inside the serialized blob in <code>QRTZ_CALENDARS</code> and surface only at deserialization, and expressions built in code or held in configuration files never reach the database at all.</p><h3 id="if-your-expressions-came-from-another-cron-library" tabindex="-1"><a class="header-anchor" href="#if-your-expressions-came-from-another-cron-library"><span>If your expressions came from another cron library</span></a></h3><p>That audit is about Quartz expressions 3.x stored happily, and it will not catch this one. 4.0 also <em>loosened</em> the day fields: 3.x required <code>?</code> in exactly one of day-of-month and day-of-week, and 4.0 reads <code>*</code> and <code>?</code> there as synonyms that restrict nothing, taking the union when both name days — crontab&#39;s rule, and what <code>cron-expressions.md</code> has always described.</p><p>The consequence is for expressions that came from a six-field .NET dialect derived from crontab, Cronos being the common one. On 3.x such an expression <strong>failed to store</strong>: the operator got a job with no trigger, which is bad but visible. On 4.0 it stores and fires, and two shapes fire on a different day than the library they were written for.</p><table><thead><tr><th>Expression</th><th>Quartz 4.0</th><th>Cronos</th><th></th></tr></thead><tbody><tr><td><code>0 0 2 * * MON</code></td><td>2026-08-24 02:00</td><td>2026-08-24 02:00</td><td>same</td></tr><tr><td><code>0 0 7 * * *</code></td><td>2026-08-21 07:00</td><td>2026-08-21 07:00</td><td>same</td></tr><tr><td><code>0 30 6 * * MON,TUE,WED,THU,FRI</code></td><td>2026-08-21 06:30</td><td>2026-08-21 06:30</td><td>same</td></tr><tr><td><code>0 0 2 * * 1</code></td><td>2026-08-23 (<strong>Sunday</strong>)</td><td>2026-08-24 (<strong>Monday</strong>)</td><td><strong>differ</strong></td></tr><tr><td><code>0 0 2 5 * MON</code></td><td>2026-08-24 (the union)</td><td>2026-10-05 (the intersection)</td><td><strong>differ</strong></td></tr></tbody></table>`,32),e(`p`,null,[l[247]||=t(`Searching from <code>2026-08-21T00:00:00Z</code>. The last two rows are the only ones that matter: Quartz numbers the days of the week 1-7 from Sunday and those dialects number them 0-6, so the same digit is a different day — in every numeric form, <code>1</code> and <code>1-5</code> and <code>*/2</code> and the <code>#n</code> and <code>L</code> forms with them, so <code>6#3</code> is the third Saturday here and the third Friday there; and Quartz fires on the union of the two day fields where Cronos fires on their intersection. Writing the day as a <strong>name</strong> — <code>MON</code> — settles the first, and <code>?</code> in one day field settles the second. Five-field expressions are not affected: <code>CronFormat.Unix</code> renumbers them, and `,23),c(g,{to:`/documentation/quartz-4.x/cron-expressions.html#the-unix-five-field-form`},{default:i(()=>[...l[245]||=[r(`The Unix five-field form`,-1)]]),_:1}),l[248]||=r(` is where that is described, along with `,-1),c(g,{to:`/documentation/quartz-4.x/cron-expressions.html#an-expression-copied-from-spring`},{default:i(()=>[...l[246]||=[r(`the same trap for a Spring expression`,-1)]]),_:1}),l[249]||=r(`.`,-1)]),l[878]||=t(`<p>Nothing can warn on a valid expression, so the audit is a second query. It is C# rather than SQL because what it does is split a field and look at the characters in it, and six database dialects spell that six ways:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token comment">// Both shapes below are valid Quartz expressions, so nothing rejects them and nothing logs.</span></span>
<span class="line"><span class="token comment">// What this lists is the expressions worth reading again if they were carried over from a</span></span>
<span class="line"><span class="token comment">// crontab-derived library such as Cronos or NCrontab.</span></span>
<span class="line"><span class="token keyword">await</span> <span class="token keyword">using</span> <span class="token class-name">DbCommand</span> command <span class="token operator">=</span> connection<span class="token punctuation">.</span><span class="token function">CreateCommand</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line"><span class="token comment">// A schema shared by several schedulers holds all of their rows in these tables, told apart</span></span>
<span class="line"><span class="token comment">// by SCHED_NAME — the scheduler&#39;s InstanceName — alone. The audit therefore filters on it and</span></span>
<span class="line"><span class="token comment">// lists it, because a line naming only the trigger&#39;s group and name would not say whose</span></span>
<span class="line"><span class="token comment">// trigger it is. Run it for every scheduler before upgrading, and substitute your own table</span></span>
<span class="line"><span class="token comment">// prefix for QRTZ_.</span></span>
<span class="line">command<span class="token punctuation">.</span>CommandText <span class="token operator">=</span></span>
<span class="line">    <span class="token string">&quot;&quot;</span>&quot;</span>
<span class="line">    <span class="token class-name">SELECT</span> SCHED_NAME<span class="token punctuation">,</span> TRIGGER_NAME<span class="token punctuation">,</span> TRIGGER_GROUP<span class="token punctuation">,</span> CRON_EXPRESSION</span>
<span class="line">    FROM   QRTZ_CRON_TRIGGERS</span>
<span class="line">    <span class="token class-name">WHERE</span>  SCHED_NAME <span class="token operator">=</span> @schedulerName</span>
<span class="line">    <span class="token string">&quot;&quot;</span>&quot;<span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line"><span class="token comment">// The name is bound, never written into the text. &#39;@&#39; is the marker this sample spells; a</span></span>
<span class="line"><span class="token comment">// provider that spells it otherwise — &#39;:&#39; on Oracle — needs that one character changed, which</span></span>
<span class="line"><span class="token comment">// is what DbMetadata.ParameterNamePrefix carries for Quartz&#39;s own statements.</span></span>
<span class="line"><span class="token class-name">DbParameter</span> schedulerNameParameter <span class="token operator">=</span> command<span class="token punctuation">.</span><span class="token function">CreateParameter</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">schedulerNameParameter<span class="token punctuation">.</span>ParameterName <span class="token operator">=</span> <span class="token string">&quot;@schedulerName&quot;</span><span class="token punctuation">;</span></span>
<span class="line">schedulerNameParameter<span class="token punctuation">.</span>Value <span class="token operator">=</span> schedulerName<span class="token punctuation">;</span></span>
<span class="line">command<span class="token punctuation">.</span>Parameters<span class="token punctuation">.</span><span class="token function">Add</span><span class="token punctuation">(</span>schedulerNameParameter<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line"><span class="token keyword">await</span> <span class="token keyword">using</span> <span class="token class-name">DbDataReader</span> reader <span class="token operator">=</span> <span class="token keyword">await</span> command<span class="token punctuation">.</span><span class="token function">ExecuteReaderAsync</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line"><span class="token keyword">while</span> <span class="token punctuation">(</span><span class="token keyword">await</span> reader<span class="token punctuation">.</span><span class="token function">ReadAsync</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    <span class="token class-name"><span class="token keyword">string</span></span> scheduler <span class="token operator">=</span> reader<span class="token punctuation">.</span><span class="token function">GetString</span><span class="token punctuation">(</span><span class="token number">0</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">    <span class="token class-name"><span class="token keyword">string</span></span> name <span class="token operator">=</span> reader<span class="token punctuation">.</span><span class="token function">GetString</span><span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">    <span class="token class-name"><span class="token keyword">string</span></span> <span class="token keyword">group</span> <span class="token operator">=</span> reader<span class="token punctuation">.</span><span class="token function">GetString</span><span class="token punctuation">(</span><span class="token number">2</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">    <span class="token class-name"><span class="token keyword">string</span></span> expression <span class="token operator">=</span> reader<span class="token punctuation">.</span><span class="token function">GetString</span><span class="token punctuation">(</span><span class="token number">3</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line">    <span class="token comment">// A stored expression is always the canonical six-field Quartz form: seconds, minutes,</span></span>
<span class="line">    <span class="token comment">// hours, day-of-month, month, day-of-week, and optionally a year.</span></span>
<span class="line">    <span class="token class-name"><span class="token keyword">string</span><span class="token punctuation">[</span><span class="token punctuation">]</span></span> fields <span class="token operator">=</span> expression<span class="token punctuation">.</span><span class="token function">Split</span><span class="token punctuation">(</span><span class="token punctuation">(</span><span class="token keyword">char</span><span class="token punctuation">[</span><span class="token punctuation">]</span><span class="token punctuation">?</span><span class="token punctuation">)</span> <span class="token keyword">null</span><span class="token punctuation">,</span> StringSplitOptions<span class="token punctuation">.</span>RemoveEmptyEntries<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">    <span class="token keyword">if</span> <span class="token punctuation">(</span>fields<span class="token punctuation">.</span>Length <span class="token operator">&lt;</span> <span class="token number">6</span><span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">{</span></span>
<span class="line">        <span class="token keyword">continue</span><span class="token punctuation">;</span></span>
<span class="line">    <span class="token punctuation">}</span></span>
<span class="line"></span>
<span class="line">    <span class="token class-name"><span class="token keyword">string</span></span> dayOfMonth <span class="token operator">=</span> fields<span class="token punctuation">[</span><span class="token number">3</span><span class="token punctuation">]</span><span class="token punctuation">;</span></span>
<span class="line">    <span class="token class-name"><span class="token keyword">string</span></span> dayOfWeek <span class="token operator">=</span> fields<span class="token punctuation">[</span><span class="token number">5</span><span class="token punctuation">]</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line">    <span class="token comment">// Quartz numbers the days 1-7 from Sunday; the other dialects number them 0-6 (Unix: 0-7),</span></span>
<span class="line">    <span class="token comment">// also from Sunday. So a day written as a number names a different day in each, in every</span></span>
<span class="line">    <span class="token comment">// form that carries one: &#39;1&#39;, &#39;1-5&#39;, &#39;*/2&#39;, and the &#39;6#3&#39; and &#39;6L&#39; forms with it.</span></span>
<span class="line">    <span class="token keyword">if</span> <span class="token punctuation">(</span><span class="token function">IsNumericDay</span><span class="token punctuation">(</span>dayOfWeek<span class="token punctuation">)</span><span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">{</span></span>
<span class="line">        Console<span class="token punctuation">.</span><span class="token function">WriteLine</span><span class="token punctuation">(</span><span class="token interpolation-string"><span class="token string">$&quot;</span><span class="token interpolation"><span class="token punctuation">{</span><span class="token expression language-csharp">scheduler</span><span class="token punctuation">}</span></span><span class="token string">: </span><span class="token interpolation"><span class="token punctuation">{</span><span class="token expression language-csharp"><span class="token keyword">group</span></span><span class="token punctuation">}</span></span><span class="token string">.</span><span class="token interpolation"><span class="token punctuation">{</span><span class="token expression language-csharp">name</span><span class="token punctuation">}</span></span><span class="token string">: numeric day-of-week &#39;</span><span class="token interpolation"><span class="token punctuation">{</span><span class="token expression language-csharp">dayOfWeek</span><span class="token punctuation">}</span></span><span class="token string">&#39; — write it as a name&quot;</span></span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">    <span class="token punctuation">}</span></span>
<span class="line"></span>
<span class="line">    <span class="token comment">// Quartz fires on the union of the two day fields, as crontab does; Cronos intersects them.</span></span>
<span class="line">    <span class="token keyword">if</span> <span class="token punctuation">(</span><span class="token operator">!</span><span class="token function">IsUnrestricted</span><span class="token punctuation">(</span>dayOfMonth<span class="token punctuation">)</span> <span class="token operator">&amp;&amp;</span> <span class="token operator">!</span><span class="token function">IsUnrestricted</span><span class="token punctuation">(</span>dayOfWeek<span class="token punctuation">)</span><span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">{</span></span>
<span class="line">        Console<span class="token punctuation">.</span><span class="token function">WriteLine</span><span class="token punctuation">(</span><span class="token interpolation-string"><span class="token string">$&quot;</span><span class="token interpolation"><span class="token punctuation">{</span><span class="token expression language-csharp">scheduler</span><span class="token punctuation">}</span></span><span class="token string">: </span><span class="token interpolation"><span class="token punctuation">{</span><span class="token expression language-csharp"><span class="token keyword">group</span></span><span class="token punctuation">}</span></span><span class="token string">.</span><span class="token interpolation"><span class="token punctuation">{</span><span class="token expression language-csharp">name</span><span class="token punctuation">}</span></span><span class="token string">: both day fields restricted — this fires on their union&quot;</span></span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">    <span class="token punctuation">}</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span>
<span class="line"><span class="token comment">// A day named by number rather than by name, whatever decorates it. A letter anywhere says the</span></span>
<span class="line"><span class="token comment">// days were written as names and there is nothing to renumber — except &#39;L&#39;, which is a</span></span>
<span class="line"><span class="token comment">// position rather than a name and shifts with the digit in front of it.</span></span>
<span class="line"><span class="token keyword">static</span> <span class="token return-type class-name"><span class="token keyword">bool</span></span> <span class="token function">IsNumericDay</span><span class="token punctuation">(</span><span class="token class-name"><span class="token keyword">string</span></span> field<span class="token punctuation">)</span></span>
<span class="line">    <span class="token operator">=&gt;</span> field<span class="token punctuation">.</span><span class="token function">Any</span><span class="token punctuation">(</span><span class="token keyword">char</span><span class="token punctuation">.</span>IsDigit<span class="token punctuation">)</span> <span class="token operator">&amp;&amp;</span> <span class="token operator">!</span>field<span class="token punctuation">.</span><span class="token function">Any</span><span class="token punctuation">(</span>c <span class="token operator">=&gt;</span> <span class="token keyword">char</span><span class="token punctuation">.</span><span class="token function">IsLetter</span><span class="token punctuation">(</span>c<span class="token punctuation">)</span> <span class="token operator">&amp;&amp;</span> c <span class="token operator">!=</span> <span class="token char">&#39;L&#39;</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line"><span class="token comment">// &#39;*&#39; and &#39;?&#39; both mean &quot;this field restricts nothing&quot;; either one leaves the other in charge.</span></span>
<span class="line"><span class="token keyword">static</span> <span class="token return-type class-name"><span class="token keyword">bool</span></span> <span class="token function">IsUnrestricted</span><span class="token punctuation">(</span><span class="token class-name"><span class="token keyword">string</span></span> field<span class="token punctuation">)</span> <span class="token operator">=&gt;</span> field <span class="token keyword">is</span> <span class="token string">&quot;*&quot;</span> <span class="token keyword">or</span> <span class="token string">&quot;?&quot;</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="daylight-saving-time" tabindex="-1"><a class="header-anchor" href="#daylight-saving-time"><span>Daylight saving time</span></a></h2><p>Three schedules fire at different times than they did on 3.x. None needs a code change, but all three change <em>when</em> existing triggers run, so review any schedule that crosses a transition.</p><p><strong>Interval cron expressions fire through both halves of a fall-back hour.</strong> A cron expression whose second, minute or hour field uses a wildcard, a step or a range — <code>0 * * * * ?</code>, <code>0 0/30 * * * ?</code> — now fires through <strong>both</strong> occurrences of the wall-clock window that repeats when the clock goes back. Previously the repeated window fired only once, so an &quot;every minute&quot; schedule silently skipped an hour of real time. Over a fall-back hour, fixed-time expressions such as <code>0 30 2 * * ?</code> — including comma lists like <code>0 0,30 2 * * ?</code> — are unchanged: they still fire once per day, at the first occurrence of an ambiguous wall-clock time.</p><p><strong>A cron time that does not exist fires when the gap ends, not shifted by the transition delta.</strong> A daily <code>0 30 2 * * ?</code> over a 02:00–03:00 spring-forward gap now fires at <strong>03:00</strong>, the instant the clocks moved, where 3.x fired at 03:30. In a zone whose daylight delta is not a whole hour — Australia/Lord_Howe — the fire reads 02:30 rather than 02:45. Every wall clock the gap swallowed names that one instant, so an expression matching several of them still fires once: <code>0 0,30 2 * * ?</code> fires once, as it did before. What <em>can</em> change a fire count is a match between the gap&#39;s end and where the delta shift used to land, which the search now reaches instead of resuming past it. An hourly <code>0 30 * * * ?</code> is the common shape: it fires at 03:00 for the occurrence the gap swallowed and again at 03:30 for the next hour&#39;s, where the delta shift moved the first onto 03:30 and the two collided into a single fire. <code>IsSatisfiedBy</code> now agrees with the fire time, which it did not before: a spring-forward gap takes no real time, so its start and its end are the same instant, and that makes the gap&#39;s end the one in-gap answer a search starting a second earlier can reproduce. A trigger therefore no longer fires at an instant its own expression rejects. For the same reason a <code>CronCalendar</code> written over the skipped hour now excludes that instant, where it used to exclude nothing at all on the transition day. Fall-back behaviour is untouched.</p><p><strong><code>CalendarIntervalTrigger</code> with <code>PreserveHourOfDayAcrossDaylightSavings</code> steps in local wall-clock time.</strong> Fire times are now always exactly on schedule and strictly increasing; the previous implementation could return times the schedule never specified when its daylight-saving adjustments failed to make progress. In time zones whose daylight delta is not a whole hour — Australia/Lord_Howe — the scheduled local time no longer drifts by the sub-hour part of the delta across a transition, so the flag preserves the full time of day as its documentation always promised.</p><h2 id="new-features" tabindex="-1"><a class="header-anchor" href="#new-features"><span>New Features</span></a></h2><p>Everything 4.x can do that a 3.x application probably is not doing yet. A few of these were backported to a late 3.x release after they were written here, so if you are upgrading from the newest 3.x you may already have them.</p>`,9),e(`ul`,null,[e(`li`,null,[e(`strong`,null,[c(g,{to:`/documentation/quartz-4.x/tutorial/recurrencetrigger.html`},{default:i(()=>[...l[250]||=[r(`RecurrenceTrigger (RRULE)`,-1)]]),_:1})]),l[251]||=r(` — schedule jobs using RFC 5545 recurrence rules for complex patterns like "every 2nd Monday of the month" or "last weekday of March each year"`,-1)]),l[280]||=e(`li`,null,[e(`strong`,null,`H (hash) token in cron expressions`),r(` — deterministic load distribution across triggers using the trigger identity as seed`)],-1),e(`li`,null,[l[253]||=e(`strong`,null,`HTTP API`,-1),l[254]||=r(` — optional REST API for managing the scheduler remotely (see `,-1),c(g,{to:`/documentation/quartz-4.x/packages/http-api.html`},{default:i(()=>[...l[252]||=[r(`HTTP API`,-1)]]),_:1}),l[255]||=r(`)`,-1)]),l[281]||=t(`<li><strong><code>Quartz.HttpClient</code></strong> — the other end of that API: <code>HttpScheduler</code> is an <code>IScheduler</code> that speaks to a remote scheduler over HTTP, which is what replaces .NET Remoting (see <a href="#remoting-a-scheduler-is-not-a-quartz-concern">Remoting a scheduler is not a Quartz concern</a>)</li><li><strong>Paged, projected job store queries</strong> — list and count jobs, triggers, groups and calendars a page at a time, with the metadata a listing needs already in the row (see <a href="#job-store-listings-became-queries">Job store listings became queries</a>)</li><li><strong>Bulk fetch by key</strong> — <code>GetJobDetails(keys)</code> and <code>GetTriggers(keys)</code> turn a page of keys into one round trip, over ADO.NET and over HTTP alike (see <a href="#job-store-listings-became-queries">Job store listings became queries</a>)</li><li><strong>Fire instances are a listing</strong> — <code>QueryFireInstances</code> answers what is running as a paged, projected query that a persistent store answers for the whole cluster, where <code>GetCurrentlyExecutingJobs</code> could only speak for this process (see <a href="#what-is-running-is-a-listing-not-a-list-of-contexts">What is running is a listing, not a list of contexts</a>)</li><li><strong>The nodes of a cluster can be listed</strong> — <code>QueryClusterNodes</code> reports every node the store knows about, with the same <code>Alive</code>/<code>Overdue</code>/<code>Failed</code> verdict the failover sweep decides recovery by. On 3.x nothing in the product read <code>QRTZ_SCHEDULER_STATE</code>, so &quot;which of my four nodes is alive&quot; was a question answered by hand-written SQL (see <a href="#the-nodes-of-a-cluster-are-a-listing">The nodes of a cluster are a listing</a>)</li><li><strong>Job data by property name</strong> — bind job data to the job property it is meant for instead of spelling its key (see <a href="#job-data-can-name-the-property">Job data can name the property</a>)</li>`,6),e(`li`,null,[l[257]||=e(`strong`,null,`A trigger can carry a retry policy`,-1),l[258]||=r(` — `,-1),l[259]||=e(`code`,null,`WithRetryPolicy(RetryPolicy.Exponential(3, TimeSpan.FromSeconds(30)))`,-1),l[260]||=r(` re-fires a trigger whose job threw, on a fixed, exponential or explicitly tabulated schedule of waits, persisted and survivable across a restart or a failover. 3.x had nothing: a failed job either asked for `,-1),l[261]||=e(`code`,null,`RefireImmediately`,-1),l[262]||=r(` — an in-process loop with no delay and no ceiling — or scheduled a fresh trigger for itself from inside `,-1),l[263]||=e(`code`,null,`Execute`,-1),l[264]||=r(` (see `,-1),l[265]||=e(`a`,{href:`#a-trigger-can-carry-a-retry-policy`},`A trigger can carry a retry policy`,-1),l[266]||=r(` and `,-1),c(g,{to:`/documentation/quartz-4.x/how-tos/retrying-failed-jobs.html`},{default:i(()=>[...l[256]||=[r(`Retrying Failed Jobs`,-1)]]),_:1}),l[267]||=r(`)`,-1)]),l[282]||=t(`<li><strong><code>TriggerState.Executing</code></strong> — tell whether a trigger&#39;s job is running, across the whole cluster (see <a href="#executing-is-a-trigger-state">Executing is a trigger state</a>)</li><li><strong><code>JobInstantiationException</code></strong> — a job that could not be built names the trigger, the job and the fire instance instead of only interpolating the job key into a message (see <a href="#instantiation-failures-name-the-trigger">Instantiation failures name the trigger</a>)</li><li><strong><code>ISchedulerListener.TriggerInError</code> / <code>TriggersInError</code></strong> — observe a trigger being moved to <code>TriggerState.Error</code>, including two ADO store transitions that reached nothing at all before (see <a href="#triggers-entering-the-error-state-are-reported">Triggers entering the error state are reported</a>)</li><li><strong>Every listener callback names its scheduler</strong> — one listener can serve several schedulers in one host and still say which of them paused a trigger or failed, and <code>SchedulerError</code> carries the trigger, job and firing it was raised for. A listener still carrying a 3.x signature — or one from a 4.0 pre-release — is refused when it is registered, with a message naming the member, rather than being attached and never called (see <a href="#listeners-are-told-which-scheduler-is-calling">Listeners are told which scheduler is calling</a>)</li>`,4),e(`li`,null,[l[269]||=t(`<strong>Joining a transaction the application owns</strong> — the ADO job store can take part in a transaction you started, so saving your own data and scheduling the job that acts on it commit together or not at all. Turn it on with <code>store.ConfigureStore(o =&gt; o.AcceptEnlistedTransactions = true)</code>, <code>JobStore:AcceptEnlistedTransactions</code>, or <code>quartz.jobStore.acceptEnlistedTransactions</code>, then hand the store a connection for the duration of a scope with <code>IScheduler.EnlistTransaction</code> / <code>EnlistConnection</code>. Handing over a connection is the only way to take part: a connection the job store opens for itself is deliberately kept out of any ambient <code>TransactionScope</code>, since a second connection in that transaction would require promoting it to a distributed one. See `,14),c(g,{to:`/documentation/quartz-4.x/tutorial/job-stores.html#joining-an-existing-transaction`},{default:i(()=>[...l[268]||=[r(`Joining an existing transaction`,-1)]]),_:1})]),l[283]||=t(`<li><strong>Job execution middleware</strong> — <code>IJobExecutionMiddleware</code> wraps every firing a scheduler performs, which is where a log scope, a tenant context, a metric or a translation of a library&#39;s exceptions belongs. A listener could never do it: it is notified before and after the execution rather than around it. Registered with <code>AddJobMiddleware&lt;T&gt;()</code> and its factory and instance overloads (see <a href="#cross-cutting-concerns-run-as-middleware">Cross-cutting concerns run as middleware</a>)</li><li><strong>Builder methods for the classic history plugins</strong> — <code>UseJobHistoryLogging()</code> and <code>UseTriggerHistoryLogging()</code>. Only the structured-logging variants had one, so the classic history plugins could previously be reached only through <code>quartz.plugin.*</code> property keys</li><li><strong>A plugin implements only what it has to say</strong> — <code>ISchedulerPlugin.Start</code> and <code>Shutdown</code> have default implementations, so a plugin that does its work in <code>Initialize</code> writes one member rather than three (see <a href="#a-plugin-implements-only-what-it-has-to-say">A plugin implements only what it has to say</a>)</li><li><strong>A calendar can have dependencies</strong> — <code>AddCalendar(name, serviceProvider =&gt; …)</code> builds the calendar from the container, which the <code>where T : ICalendar, new()</code> generic overloads cannot (see <a href="#a-calendar-can-be-built-from-the-container">A calendar can be built from the container</a>)</li><li><strong>A renamed job type is declared, not coded around</strong> — <code>q.UseTypeLoader(loader =&gt; loader.Map(&quot;Acme.Jobs.NightlyReport, Acme.Jobs&quot;, typeof(NightlyRollupJob)))</code>, or the same map under <code>Quartz:TypeLoader:Aliases</code> in configuration, keeps every row still carrying the old <code>JOB_CLASS_NAME</code> firing. On 3.x the only answer was an <code>ITypeLoadHelper</code> of your own (see <a href="#a-job-type-rename-is-declared-as-a-map">A job type rename is declared as a map</a>)</li><li><strong>A <code>quartz.*</code> key that lost its prefix is refused</strong> — <code>QuartzOptions.Properties</code> is read only through the <code>quartz.</code> prefix, so a key without one produced no symptom at all; it now fails options validation at startup (see <a href="#a-property-key-without-the-quartz-prefix-is-refused">A property key without the <code>quartz.</code> prefix is refused</a>)</li><li><strong>An <code>IJobDetail</code> of your own</strong> — the interface no longer declares a member only Quartz can implement, so an application can supply its own job detail type and have <code>RAMJobStore</code> hand it back rather than quietly swapping it for Quartz&#39;s (see <a href="#an-ijobdetail-of-your-own">An <code>IJobDetail</code> of your own</a>)</li><li><strong>A one-word question needs no query object</strong> — <code>QueryTriggersInError()</code> is the trigger query with the error-state filter already set, <code>ResetTriggersFromErrorState(GroupMatcher&lt;TriggerKey&gt;)</code> resets the failed triggers of a group, <code>Exists(string calendarName)</code> answers whether a calendar is there without deserializing it, and <code>ISchedulerFactory.GetRequiredScheduler(name)</code> throws where <code>LookupScheduler</code> returns null (see <a href="#reading-has-two-altitudes-on-purpose">Reading has two altitudes on purpose</a>)</li><li><strong>Pause, resume and reset a set of keys in one call</strong> — <code>PauseTriggers</code>, <code>ResumeTriggers</code>, <code>PauseJobs</code>, <code>ResumeJobs</code> and <code>ResetTriggersFromErrorState</code> take a key collection, do the whole set in one lock and one transaction, and answer with the keys they applied to (see <a href="#a-set-of-keys-pauses-resumes-or-resets-in-one-call">A set of keys pauses, resumes or resets in one call</a>)</li><li><strong><code>TriggerDetailsUpdate.WithExecutionGroup</code></strong> — move a stored trigger into an execution group, or out of every group, without rescheduling it. <code>QRTZ_TRIGGERS.EXECUTION_GROUP</code> was already written by the generic trigger update, and <code>RAMJobStore</code> applies it in place the same way it applies a preferred node, so both stores behave alike</li>`,10),e(`li`,null,[l[271]||=e(`strong`,null,`A chain can fan out`,-1),l[272]||=r(` — `,-1),l[273]||=e(`code`,null,`JobChainingJobListener`,-1),l[274]||=r(` takes more than one follow-up for a job, either by calling `,-1),l[275]||=e(`code`,null,`AddJobChainLink`,-1),l[276]||=r(` again with the same first job or with the new `,-1),l[277]||=e(`code`,null,`AddJobChainLinks(firstJob, followUpJobs)`,-1),l[278]||=r(`. Each follow-up is triggered as its own firing, so they run concurrently rather than one after another, and one that cannot be triggered is logged without costing its siblings theirs. On 3.x the second link threw, so a chain could only be sequential (see `,-1),c(g,{to:`/documentation/faq.html#how-do-i-chain-job-execution-or-how-do-i-create-a-workflow`},{default:i(()=>[...l[270]||=[r(`How do I chain Job execution?`,-1)]]),_:1}),l[279]||=r(`)`,-1)])]),l[879]||=t(`<h2 id="job-data-can-name-the-property" tabindex="-1"><a class="header-anchor" href="#job-data-can-name-the-property"><span>Job data can name the property</span></a></h2><p><code>UsingJobData</code> has an overload that takes the job property rather than its key:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line"> q.AddJob&lt;ExampleJob&gt;(jobKey, j =&gt; j</span>
<span class="line"></span></span><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line">     .UsingJobData(nameof(ExampleJob.InjectedString), &quot;Hello&quot;)</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line">     .UsingJobData(nameof(ExampleJob.InjectedBool), true)</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line">     .UsingJobData(x =&gt; x.InjectedString, &quot;Hello&quot;)</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">     .UsingJobData(x =&gt; x.InjectedBool, true)</span>
<span class="line"></span></span><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line"> );</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The key is the property&#39;s name and the value has to be of the property&#39;s type, so a value that would have been silently coerced — an <code>int</code> written to a <code>string</code> property, say — no longer compiles, and neither does a property of an unrelated job.</p><p>Everything the compiler cannot rule out is rejected where the job data is written, by asking the job factory&#39;s own lookup whether the key this property&#39;s name becomes leads back to this same property:</p><ul><li>a property with no public setter, or a nested path;</li><li>one reached by casting the lambda parameter to another job;</li><li>one the factory cannot find — a name starting with a lowercase letter (keys are looked up upper-cased), or a property that implements an interface explicitly, which is not public on the job class;</li><li>one whose name resolves to a <em>different</em> property of another type, which is what a <code>new</code> member that hides a base property does;</li><li>a value that will not convert to the property&#39;s type, or that would lose information doing so — a <code>double</code> rounded into an <code>int</code>, or saturated into a <code>float</code>;</li><li><code>null</code> for a property that cannot hold one. Type inference widens <code>TValue</code> to the nullable form, so <code>int? retries = …; UsingJobData(j =&gt; j.RetryCount, retries)</code> compiles and is rejected here rather than quietly becoming <code>0</code> when the job runs.</li></ul><p>Enums are stored by name. Nothing is instantiated: the expression is read, never run.</p><p>Existing string-keyed <code>UsingJobData</code> calls are unaffected.</p><h3 id="the-builders-carry-the-job-type" tabindex="-1"><a class="header-anchor" href="#the-builders-carry-the-job-type"><span>The builders carry the job type</span></a></h3><p>Inferring <code>x</code> needs the builder to know the job type, so the builders and the configurator interfaces are generic in it. <code>JobBuilder</code> and <code>TriggerBuilder</code> are now static classes holding the <code>Create</code> methods, and the builder itself is <code>JobBuilder&lt;TJob&gt;</code> / <code>TriggerBuilder&lt;TJob&gt;</code>:</p><table><thead><tr><th>4.0 preview</th><th>4.0</th></tr></thead><tbody><tr><td><code>JobBuilder</code></td><td><code>JobBuilder&lt;TJob&gt;</code>, from <code>JobBuilder.Create&lt;TJob&gt;()</code> — <code>JobBuilder.Create()</code> gives <code>JobBuilder&lt;IJob&gt;</code></td></tr><tr><td><code>TriggerBuilder</code></td><td><code>TriggerBuilder&lt;TJob&gt;</code>, from <code>TriggerBuilder.Create&lt;TJob&gt;()</code> — <code>TriggerBuilder.Create()</code> gives <code>TriggerBuilder&lt;IJob&gt;</code></td></tr><tr><td><code>IJobConfigurator</code></td><td><code>IJobConfigurator&lt;TJob&gt;</code></td></tr><tr><td><code>ITriggerConfigurator</code></td><td><code>ITriggerConfigurator&lt;TJob&gt;</code> — the 4.x <code>ITriggerConfigurator</code> is a new, much smaller base holding only <code>WithSchedule</code>, see <a href="#one-family-of-withxschedule-extensions">One family of <code>WithXSchedule</code> extensions</a></td></tr><tr><td><code>IJobDetail.GetJobBuilder()</code></td><td>an extension method returning <code>JobBuilder&lt;IJob&gt;</code> — see <a href="#an-ijobdetail-of-your-own">An <code>IJobDetail</code> of your own</a></td></tr><tr><td><code>ITrigger.GetTriggerBuilder()</code></td><td>returns <code>TriggerBuilder&lt;IJob&gt;</code></td></tr></tbody></table><p>Chained code is unaffected — <code>JobBuilder.Create&lt;MyJob&gt;().WithIdentity(&quot;x&quot;).Build()</code> reads the same, and so do the <code>AddJob&lt;T&gt;</code> / <code>ScheduleJob&lt;T&gt;</code> lambdas, whose parameter type is now inferred as the generic configurator. What breaks is naming the builder as a type:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> TriggerBuilder builder = trigger.GetTriggerBuilder();</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> var builder = trigger.GetTriggerBuilder();</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>The configurator interfaces are <strong>invariant</strong> in <code>TJob</code>, so a configuration delegate shared across job types no longer type-checks. <code>TJob</code> appears both as an input (<code>Expression&lt;Func&lt;TJob, TValue&gt;&gt;</code>) and in the returned interface, so no variance annotation can recover it — make the helper generic instead:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> Action&lt;ITriggerConfigurator&gt; common = t =&gt; t.StartNow().WithSimpleSchedule();</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"> q.ScheduleJob&lt;JobA&gt;(common);</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"> q.ScheduleJob&lt;JobB&gt;(common);</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> static void Common&lt;TJob&gt;(ITriggerConfigurator&lt;TJob&gt; t) where TJob : IJob =&gt; t.StartNow().WithSimpleSchedule();</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line"> q.ScheduleJob&lt;JobA&gt;(Common);</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line"> q.ScheduleJob&lt;JobB&gt;(Common);</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>AddTrigger</code> gained an <code>AddTrigger&lt;TJob&gt;</code> overload, because a trigger added on its own has no job type to infer from otherwise. The internal <code>TriggerConfigurator</code> is gone: <code>TriggerBuilder&lt;TJob&gt;</code> implements <code>ITriggerConfigurator&lt;TJob&gt;</code> itself, which also gets <code>WithExecutionGroup</code> and <code>WithPreferredNode</code> onto the DI configurator for the first time.</p><p>That deletion also changed which clock a DI-registered trigger is born with. <code>TriggerConfigurator</code> always built with <code>TimeProvider.System</code>; <code>AddTrigger</code> and <code>ScheduleJob</code> now use the container&#39;s registered <code>TimeProvider</code>. If you register a non-system one — a <code>FakeTimeProvider</code> in a test, say — a trigger without an explicit <code>StartAt</code> now starts at that provider&#39;s now rather than at wall clock, which is almost certainly what you wanted, but it does change when such triggers first fire.</p><p>Three runtime checks come with the type parameter, all only on a builder whose <code>TJob</code> is not <code>IJob</code>:</p><ul><li><code>JobBuilder.Create&lt;TJob&gt;().OfType(type)</code> and <code>OfType&lt;T&gt;()</code> throw <code>ArgumentException</code> <strong>at the <code>OfType</code> call</strong> when the type is not a <code>TJob</code>. If you resolve a job type at runtime — from configuration, or a decorator type — build it with <code>JobBuilder.Create().OfType(type)</code> rather than the generic overload.</li><li><code>JobBuilder.Create&lt;TJob&gt;().OfType((JobType) typeName)</code> throws <code>InvalidOperationException</code> on <code>Build()</code> instead, because a type named by string is only known once it resolves.</li><li><code>TriggerBuilder.Create&lt;TJob&gt;().ForJob(jobDetail)</code> throws <code>ArgumentException</code> when the detail is not for a <code>TJob</code>. <code>ForJob(JobKey)</code> carries no type, and a detail whose type name does not resolve in this process cannot be checked either — both are accepted.</li></ul><h2 id="an-ijobdetail-of-your-own" tabindex="-1"><a class="header-anchor" href="#an-ijobdetail-of-your-own"><span>An <code>IJobDetail</code> of your own</span></a></h2><p><code>IJobDetail</code> declared <code>GetJobBuilder()</code>, which nobody outside Quartz could implement. <code>JobBuilder&lt;TJob&gt;</code> is sealed with an internal constructor and builds Quartz&#39;s own detail, so an implementation of the interface had to return a builder that produces somebody else&#39;s type (<a href="https://github.com/quartznet/quartznet/issues/1143" target="_blank" rel="noopener noreferrer">#1143</a>). That was not only awkward to write around: <code>RAMJobStore</code> called the member to re-store the job data of a <code>[PersistJobDataAfterExecution]</code> job, so the first completion of such a job silently replaced the caller&#39;s detail with Quartz&#39;s.</p><p>The unimplementable member is gone from the interface, and the one a job store actually needs replaces it:</p><table><thead><tr><th>4.0 preview</th><th>4.0</th></tr></thead><tbody><tr><td><code>IJobDetail.GetJobBuilder()</code></td><td><code>JobDetailExtensions.GetJobBuilder(this IJobDetail)</code>, in the <code>Quartz</code> namespace</td></tr><tr><td>—</td><td><code>IJobDetail.WithJobData(JobDataMap)</code> — a copy of the detail carrying the given data</td></tr></tbody></table><p><strong>Calling code changes nothing.</strong> <code>detail.GetJobBuilder()</code> still compiles, still resolves without a new <code>using</code>, and still returns <code>JobBuilder&lt;IJob&gt;</code>; it is now filled in from the detail&#39;s public state rather than by the detail itself. It carries the detail&#39;s <code>JobType</code> across as it is, so a detail read from a job store whose stored type name names nothing in this process rebuilds, and keeps the stored spelling, rather than throwing.</p><p><strong>An implementation writes <code>WithJobData</code> instead of <code>GetJobBuilder</code>.</strong> It returns a copy of the detail carrying the given map, leaving the instance it was called on alone; the map is taken as given, not copied.</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> public JobBuilder&lt;IJob&gt; GetJobBuilder() =&gt; /* nothing you can write */;</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> public IJobDetail WithJobData(JobDataMap jobDataMap) =&gt; new MyJobDetail(Key, JobType, …, jobDataMap);</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>How far your own detail travels depends on what holds it:</p><ul><li><code>RAMJobStore</code> keeps the instances it is given and hands back <code>Clone()</code>s of them, so a detail of your own round-trips as itself — including across the re-store of a <code>[PersistJobDataAfterExecution]</code> job, which is what <code>WithJobData</code> is for.</li><li>Anything that keeps a detail as data does not. The ADO.NET job store writes the columns of <code>QRTZ_JOB_DETAILS</code> and rebuilds every detail it reads through <code>JobBuilder</code>, so what comes back is Quartz&#39;s own implementation; <code>HttpScheduler</code> rebuilds one the same way from its wire payload. Whatever your type carries beyond the interface&#39;s members is gone by then, so anything that has to survive a persistent store belongs in the <code>JobDataMap</code>.</li></ul><h2 id="a-trigger-can-carry-a-retry-policy" tabindex="-1"><a class="header-anchor" href="#a-trigger-can-carry-a-retry-policy"><span>A trigger can carry a retry policy</span></a></h2><p>3.x had no retry policy. A job that failed either asked for <code>RefireImmediately</code> — an in-process loop with no delay and no ceiling — or scheduled a fresh trigger for itself from inside <code>Execute</code>. 4.x adds <code>Quartz.RetryPolicy</code>, a value describing how many times and how far apart a failed job&#39;s trigger is re-fired.</p><table><thead><tr><th>Member</th><th>What it is</th></tr></thead><tbody><tr><td><code>RetryPolicy.Fixed(maxAttempts, delay)</code></td><td>The same wait before every retry</td></tr><tr><td><code>RetryPolicy.Exponential(maxAttempts, initialDelay, factor = 2, maxDelay = null)</code></td><td>A wait multiplied by <code>factor</code> each time, optionally clamped</td></tr><tr><td><code>RetryPolicy.Explicit(params delays)</code></td><td>A table of waits; <code>MaxAttempts</code> is its length and the last entry repeats</td></tr><tr><td><code>RetryPolicy.DelayFor(attempt)</code></td><td>The wait before the given retry, counting from one</td></tr><tr><td><code>RetryPolicy.ToStoredString()</code> / <code>Parse</code> / <code>TryParse</code></td><td>The single-string form the <code>RETRY_POLICY</code> column, the JSON payload and a serialized trigger are to carry</td></tr></tbody></table><p>There is no public constructor, so a policy that could not be honoured — no attempts, a negative wait, a backoff that shrinks — cannot be built. A policy carries which of the three factories made it, so <code>Exponential(3, delay, factor: 1)</code> waits exactly as <code>Fixed(3, delay)</code> does but is a different value and stores as <code>exp;3;…;1</code>: the shape says what the trigger&#39;s author chose, and turning the rate up is a change to make on one and not on the other. Equality is written out by hand rather than left to a record, because the delay table is a collection and reference equality on it would make two policies built from the same numbers different values; the backoff factor is compared bit for bit, which is what round-tripping it through the <code>&quot;R&quot;</code> format guarantees.</p><p><code>MaxAttempts</code> counts retries <em>after</em> the first failure, not fires: <code>Fixed(2, …)</code> runs a persistently failing job three times in total.</p><h3 id="where-a-policy-is-set-and-read" tabindex="-1"><a class="header-anchor" href="#where-a-policy-is-set-and-read"><span>Where a policy is set and read</span></a></h3><table><thead><tr><th>Member</th><th>What it is</th></tr></thead><tbody><tr><td><code>ITrigger.RetryPolicy</code></td><td>The trigger&#39;s policy, or <code>null</code> — the default — when it does not retry</td></tr><tr><td><code>ITrigger.RetryAttempt</code></td><td>How many times the occurrence being executed has already been retried; <code>0</code> on a regular fire</td></tr><tr><td><code>IMutableTrigger.RetryPolicy</code> / <code>RetryAttempt</code></td><td>The setters, for a job store restoring a trigger from its row</td></tr><tr><td><code>TriggerBuilder&lt;TJob&gt;.WithRetryPolicy(RetryPolicy?)</code></td><td>Give a trigger a policy while building it</td></tr><tr><td><code>ITriggerConfigurator&lt;TJob&gt;.WithRetryPolicy(RetryPolicy?)</code></td><td>The same, in the DI configuration API</td></tr><tr><td><code>TriggerDetailsUpdate.WithRetryPolicy(RetryPolicy?)</code></td><td>Change a stored trigger&#39;s policy without rescheduling it</td></tr><tr><td><code>TriggerHeader.RetryPolicy</code> (string) / <code>RetryAttempt</code></td><td>What a trigger listing reports</td></tr></tbody></table><p>A builder-built trigger fully defines its retry policy, so a definition that says nothing clears a stored one when it replaces an existing trigger — the same rule <code>WithExecutionGroup</code> and <code>WithPreferredNode</code> follow. <code>TriggerDetailsUpdate</code> deliberately has no way to set the <em>attempt</em>: the policy is configuration, while the attempt belongs to the occurrence in flight, and setting it would either grant a running job extra attempts or take away ones it has already spent.</p><p><code>TriggerHeader</code> carries the policy as its stored string rather than as a <code>RetryPolicy</code>, because a listing reports the column: a row whose policy a newer node wrote in a shape this one cannot read still has to list. That makes <code>TriggerHeader</code>&#39;s constructor two parameters longer, which is a breaking change for anything constructing one by hand.</p><h3 id="where-a-policy-is-stored" tabindex="-1"><a class="header-anchor" href="#where-a-policy-is-stored"><span>Where a policy is stored</span></a></h3><p>The policy travels as its stored string everywhere: the <code>RETRY_POLICY</code> column, the <code>retryPolicy</code> property of a serialized trigger, and the <code>retryPolicy</code> field of a JSON scheduling file or configuration section. A payload written before 4.0 has neither property, and reads back as no policy and no attempt.</p><div class="language-json line-numbers-mode" data-highlighter="prismjs" data-ext="json"><pre><code class="language-json"><span class="line"><span class="token punctuation">{</span></span>
<span class="line">  <span class="token property">&quot;Name&quot;</span><span class="token operator">:</span> <span class="token string">&quot;nightly-import&quot;</span><span class="token punctuation">,</span></span>
<span class="line">  <span class="token property">&quot;JobName&quot;</span><span class="token operator">:</span> <span class="token string">&quot;import&quot;</span><span class="token punctuation">,</span></span>
<span class="line">  <span class="token property">&quot;RetryPolicy&quot;</span><span class="token operator">:</span> <span class="token string">&quot;exp;3;00:00:30;2;00:10:00&quot;</span><span class="token punctuation">,</span></span>
<span class="line">  <span class="token property">&quot;Cron&quot;</span><span class="token operator">:</span> <span class="token punctuation">{</span> <span class="token property">&quot;Expression&quot;</span><span class="token operator">:</span> <span class="token string">&quot;0 0 2 * * ?&quot;</span> <span class="token punctuation">}</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>A string that is not a policy is refused where the file is read, rather than scheduling a trigger that would never retry.</p><h3 id="what-happens-when-a-job-fails" tabindex="-1"><a class="header-anchor" href="#what-happens-when-a-job-fails"><span>What happens when a job fails</span></a></h3><p>A job fails, for retry purposes, when <code>Execute</code> throws anything other than a <code>JobExecutionException</code> that asks for something itself. On failure with attempts left the scheduler sets the trigger&#39;s next fire time to <code>now + policy.DelayFor(attempt + 1)</code>, increments the attempt and hands the job store a new instruction, <code>SchedulerInstruction.RetryTrigger</code>. Everything else about the trigger is untouched.</p><table><thead><tr><th>Member</th><th>What it is</th></tr></thead><tbody><tr><td><code>SchedulerInstruction.RetryTrigger</code></td><td>New enum member, appended — no existing value moved. <code>ITriggerListener.TriggerComplete</code> sees it; there is no new listener member</td></tr><tr><td><code>IJobExecutionContext.RetryAttempt</code></td><td><code>0</code> on a regular fire, <em>n</em> on the <em>n</em>-th retry</td></tr><tr><td><code>TriggerBase.RetryFired(ICalendar?)</code></td><td><code>public virtual</code>, and only on <code>TriggerBase</code> — <code>IOperableTrigger</code> is unchanged</td></tr><tr><td><code>IDriverDelegate.UpdateTriggerForRetry</code> / <code>ClearTriggerRetryAttempt</code></td><td><strong>Breaking for anything implementing <code>IDriverDelegate</code></strong>; <code>StdAdoDelegate</code> implements both <code>virtual</code></td></tr></tbody></table><p>Four rules are worth knowing:</p><ul><li><strong>A retry never displaces the next scheduled occurrence.</strong> If <code>retryAt + 1s &gt;= NextFireTimeUtc</code> the retry is dropped and the occurrence supersedes it, attempt reset. The one-second margin exists because <code>CalendarIntervalTriggerImpl.GetFireTimeAfter</code> and <code>DailyTimeIntervalTriggerImpl.GetFireTimeAfter</code> add a second before searching. A retry is also never scheduled past the trigger&#39;s <code>EndTimeUtc</code>, nor past the end of the calendar: an exponential policy&#39;s waits eventually exceed the room left in a <code>DateTimeOffset</code>, and a retry with nowhere to land is declined rather than answering a failed job with an overflow.</li><li><strong>A retry fire is not a <code>Triggered()</code> call.</strong> <code>RetryFired</code> advances past the retry instant with the same calendar-skip loop, but touches no counter and leaves <code>PreviousFireTimeUtc</code> alone — so a retry burns no repeat count and no RRULE <code>COUNT</code> slot, and reports the <em>original</em> occurrence as its <code>ScheduledFireTimeUtc</code>.</li><li><strong>Exhausted attempts go back to the ordinary schedule, not to <code>TriggerState.Error</code>.</strong> One bad hour must not kill a cron trigger. A misfired retry gets the trigger&#39;s own misfire instruction and the attempt is cleared; there is no retry-specific misfire policy.</li><li><strong><code>RefireImmediately</code> is not a zero-delay retry.</strong> It re-runs the job on the same thread inside the same firing, with no delay, no ceiling, nothing persisted and no slot released, and it does not touch <code>RetryAttempt</code>. An explicit <code>RefireImmediately</code> or <code>Unschedule*</code> wins over the trigger&#39;s policy. An <code>OperationCanceledException</code> from the scheduler&#39;s own token never retries — shutdown and interrupt are operator decisions, and a node vanishing mid-execution is what <code>RequestsRecovery</code> is for.</li></ul><p>One meter, <code>quartz.trigger.retry</code>, counts each retry the scheduler schedules, tagged like <code>quartz.trigger.misfire</code>. Log event <code>1056</code> reports the trigger, the attempt and the retry instant at <code>Information</code>.</p><h3 id="if-you-implement-idriverdelegate-the-retry-members" tabindex="-1"><a class="header-anchor" href="#if-you-implement-idriverdelegate-the-retry-members"><span>If you implement <code>IDriverDelegate</code>: the retry members</span></a></h3><p>Two new members, neither with a default implementation — the same way <code>UpdateTriggerPreferredNodeConditional</code> and the listing members were added. A delegate deriving from <code>StdAdoDelegate</code> inherits both as <code>virtual</code> and needs no change; one implementing the interface from scratch has to supply them.</p><table><thead><tr><th>Member</th><th>Purpose</th></tr></thead><tbody><tr><td><code>UpdateTriggerForRetry(conn, trigger, newState, ct)</code></td><td>Write the retry a completing trigger scheduled: <code>NEXT_FIRE_TIME</code>, <code>RETRY_ATTEMPT</code> and <code>TRIGGER_STATE</code> in one narrow statement</td></tr><tr><td><code>ClearTriggerRetryAttempt(conn, triggerKey, ct)</code></td><td>Put <code>RETRY_ATTEMPT</code> back to zero and touch nothing else</td></tr></tbody></table><p>The pair is deliberately narrow rather than folded into <code>UpdateTrigger</code>: a retry moves one instant, and the generic trigger update would write back a preferred node and a job data map that this path has no business touching. Clearing is issued only when the completing trigger actually carried a non-zero attempt, so an ordinary completion still costs no extra round trip.</p><p><code>UpdateMisfiredTrigger</code> and <code>UpdateMisfiredTriggers</code> did not change shape, but the statements behind them now also write <code>RETRY_ATTEMPT</code>: misfire handling recomputes a trigger from its schedule, so the occurrence that was waiting to be retried is gone and the attempt goes with it. A delegate that spells those statements itself needs the same column.</p><h2 id="trimming-annotations" tabindex="-1"><a class="header-anchor" href="#trimming-annotations"><span>Trimming annotations</span></a></h2><p><code>ScheduleJob&lt;T&gt;</code>, <code>AddTrigger&lt;TJob&gt;</code> and <code>TriggerBuilder.Create&lt;TJob&gt;()</code> gained <code>[DynamicallyAccessedMembers]</code> on their type parameter, matching <code>AddJob&lt;T&gt;</code> and <code>JobBuilder.Create&lt;T&gt;()</code> — they build job details or bind job data now, so the job type&#39;s members have to survive trimming. If you wrap them in your own generic method and build with the trim analyzer on, the forwarding type parameter needs the same annotation:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> public static void Register&lt;TJob&gt;(IQuartzBuilder q) where TJob : IJob</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> public static void Register&lt;[DynamicallyAccessedMembers(DynamicallyAccessedMemberTypes.PublicConstructors | DynamicallyAccessedMemberTypes.PublicMethods)] TJob&gt;(IQuartzBuilder q) where TJob : IJob</span>
<span class="line"></span></span><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line">     =&gt; q.ScheduleJob&lt;TJob&gt;(t =&gt; t.StartNow());</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>Quartz</code> is also marked <code>IsTrimmable</code> now, which changes what a trimmed publish tells you about it. Under <code>TrimMode=full</code> the assembly is member-trimmed either way; what the mark changes is the reporting. With <code>TrimmerSingleWarn</code> on, which is the default, an assembly&#39;s trim-analysis warnings collapse into one <code>IL2104: Assembly &#39;Quartz&#39; produced trim warnings</code>, and <code>IsTrimmable</code> exempts exactly its <code>IL2026</code>s from that collapse. So on 4.0 you see every <code>[RequiresUnreferencedCode]</code> call site individually, and <code>&lt;TrimmerSingleWarn&gt;false&lt;/TrimmerSingleWarn&gt;</code> shows the rest — around fifty in all, at the reflective call sites listed in <code>src/Quartz/TrimAnalysisBaseline.cs</code>.</p>`,56),e(`p`,null,[l[285]||=t(`That is not a regression: the same reflection was there before and the same code could break under trimming; you can now see which parts. None of those warnings is suppressed in the shipped assembly, deliberately — suppressing them would hide a real risk from you. Configuration by flat <code>quartz.*</code> keys, jobs named as strings (<code>job_scheduling_data</code> XML, a persisted <code>JOB_CLASS_NAME</code>), and <code>JobDataMap</code> values bound onto job properties are the paths that need reflection; an application that configures in code, references its job types statically and keeps job data to primitives exercises far less of it. A <strong>persistent job store</strong> publishes trimmed, and publishes native AOT, when it is told how to reach its driver without naming one — <code>UseSqlServer(SqlClientFactory.Instance, connectionString)</code>, or a registered <code>DbDataSource</code>: the default System.Text.Json serializer carries a source-generated contract for every blob a store writes, and a custom trigger or calendar type is answered by the registry it was registered with. The one thing left open is a job-data value of a type of your own, which the registry is handed metadata for through <code>SystemTextJsonSerializerRegistry.AddTypeInfoResolver</code> — see `,17),c(g,{to:`/documentation/quartz-4.x/how-tos/trimming-and-native-aot.html`},{default:i(()=>[...l[284]||=[r(`Publishing Trimmed and Native AOT`,-1)]]),_:1}),l[286]||=r(` for the shape of that.`,-1)]),l[880]||=t(`<p><code>Quartz</code> also declares <strong><code>IsAotCompatible</code></strong> on 4.0, which 3.x does not. What it claims is narrow and checkable: nothing Quartz does needs code generated at run time, so a native AOT publish reports no <code>IL3050</code> against it at all. The last pair belonged to configuration binding, and binding the <code>Quartz</code> section is source-generated now — the compiler writes a binder for each options type — so configuring from <code>appsettings.json</code> is as ahead-of-time-safe as configuring in code, with nothing asked of your application. The <code>IL2xxx</code> above are unaffected by the claim and still reported; <code>Quartz.Trimming.Canary</code> is published by ILCompiler and <strong>run</strong> on Windows, Linux and macOS on every pull request, scheduling and firing over a real SQLite store and binding a whole scheduler out of an <code>IConfiguration</code>. The rest of the track is <a href="https://github.com/quartznet/quartznet/issues/3341" target="_blank" rel="noopener noreferrer">#3341</a>.</p>`,1),e(`p`,null,[l[288]||=t(`<strong>Every other shipped package now answers the question too</strong>, which on 3.x none of them does. On 3.x an application that adds any plugin to a trimmed publish gets one <code>IL2104</code> per unmarked assembly and no detail; on 4.0, <code>Quartz.Jobs</code>, <code>Quartz.Plugins</code>, <code>Quartz.HttpClient</code>, <code>Quartz.AspNetCore</code>, <code>Quartz.Extensions.Redis</code> and <code>Quartz.Plugins.TimeZoneConverter</code> are marked trimmable and report their individual call sites — between none and two each, all of them a job type spelled as a string, and all of them an <code>IL2026</code>, which is the code <code>IsTrimmable</code> exempts from the collapse. No public member of any of them gained <code>[RequiresUnreferencedCode]</code> or <code>[DynamicallyAccessedMembers]</code>, so nothing you call changes shape. <code>Quartz.Serialization.Newtonsoft</code> and <code>Quartz.Dashboard</code> say the opposite deliberately, in their csproj and in their nuget.org readme: Json.NET&#39;s contract is reflection, and Blazor Server binds components and their parameters by name. The table is in `,28),c(g,{to:`/documentation/quartz-4.x/how-tos/trimming-and-native-aot.html#which-packages-say-whether-they-can-be-trimmed`},{default:i(()=>[...l[287]||=[r(`Which packages say whether they can be trimmed`,-1)]]),_:1}),l[289]||=r(`.`,-1)]),l[881]||=t(`<h2 id="executing-is-a-trigger-state" tabindex="-1"><a class="header-anchor" href="#executing-is-a-trigger-state"><span>Executing is a trigger state</span></a></h2><p>There was no way to ask whether a trigger&#39;s job is running right now. 3.x&#39;s <code>IScheduler.GetCurrentlyExecutingJobs</code> only ever saw the node it was called on, so a process that schedules and observes triggers but does not execute them — a dashboard, an admin UI, a separate control application — could not answer the question at all. (That member is gone in 4.0; see <a href="#what-is-running-is-a-listing-not-a-list-of-contexts">what is running is a listing</a>.)</p><p><code>TriggerState</code> now has an <code>Executing</code> member, reported by both <code>IScheduler.GetTriggerState</code> and trigger listings. With a persistent job store it is visible from every node, because it is established from the fired-triggers table rather than from process-local state.</p><h3 id="what-changed-in-what-you-get-back" tabindex="-1"><a class="header-anchor" href="#what-changed-in-what-you-get-back"><span>What changed in what you get back</span></a></h3><p>A trigger with an execution in flight previously reported <code>Normal</code>, <code>Complete</code>, or <code>Blocked</code> depending on its schedule; it now reports <code>Executing</code>. States are resolved in this order:</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text"><pre><code class="language-text"><span class="line">None &gt; Error &gt; Paused &gt; Executing &gt; Blocked &gt; Complete &gt; Normal</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>So a trigger that is paused, or in the error state, still reports that even while its job runs — those are the facts an operator has to act on. Two consequences worth calling out:</p><ul><li><code>Blocked</code> now means a <strong>different</strong> trigger of the same <code>[DisallowConcurrentExecution]</code> job is running, so this one cannot fire. The trigger that is actually running reports <code>Executing</code>. Previously both reported <code>Blocked</code> and nothing could tell them apart.</li><li>A trigger with no fire times left whose final execution is still running reports <code>Executing</code> rather than <code>Complete</code>. In 3.x this case reported <code>Blocked</code>, which was a stand-in for a state that did not exist yet.</li></ul><p>Note that executing is not exclusive with being scheduled: a trigger whose job allows concurrent execution can be running several jobs and still be due to fire again. It reports <code>Executing</code> until the last of them finishes.</p><h3 id="what-to-check-in-your-own-code" tabindex="-1"><a class="header-anchor" href="#what-to-check-in-your-own-code"><span>What to check in your own code</span></a></h3><ul><li><strong>Health checks and guards of the form <code>if (state == TriggerState.Normal)</code></strong> will now see <code>Executing</code> for a trigger that is simply busy. Treat <code>Executing</code> as healthy.</li><li><strong>Watchdogs of the form <code>if (state != TriggerState.Normal) await ResumeTrigger(key)</code></strong> deserve a closer look. <code>ResumeTrigger</code> applies the trigger&#39;s misfire policy to any trigger whose next fire time has passed, regardless of the state it is currently in — and for a long-running job on a short interval, the next fire time is in the past for the whole execution. Such a watchdog can therefore alter the schedule of a perfectly healthy trigger. This is not new in 4.x, but <code>Executing</code> routes more triggers into it, so gate the watchdog on the states you actually mean to repair (<code>Error</code>, <code>Paused</code>).</li><li><strong>Alerting that treats <code>Blocked</code> as &quot;a job is running&quot;</strong> should move to <code>Executing</code>.</li></ul><h3 id="filtering-a-listing-by-state" tabindex="-1"><a class="header-anchor" href="#filtering-a-listing-by-state"><span>Filtering a listing by state</span></a></h3><p><code>TriggerQuery.State</code> accepts <code>Executing</code> like any other state. Because the filter and the reported state are derived together, a listing filtered by <code>Normal</code> no longer returns triggers it would then report as <code>Executing</code>.</p><h3 id="a-note-on-stale-executions" tabindex="-1"><a class="header-anchor" href="#a-note-on-stale-executions"><span>A note on stale executions</span></a></h3><p>Executing is established from the fired-triggers table, which is the only durable record that a job is running. If a node dies mid-execution, its rows stay behind until another node&#39;s cluster recovery clears them, so during that window a trigger reports <code>Executing</code> although nothing is running — and, because the filter and the reported state agree by construction, it is also absent from a listing filtered by <code>Normal</code>. The same window already existed for <code>Blocked</code>; it is now visible for more states.</p><p>How long that lasts depends on the job. For an ordinary job the rows are cleared as soon as another node detects the failure, roughly <code>clusterCheckinInterval</code> + <code>clusterCheckinMisfireThreshold</code>. For a <code>[DisallowConcurrentExecution]</code> job, cluster recovery deliberately <em>preserves</em> the executing rows on first detection — the node may still be alive and running the job, and reviving it elsewhere would break the concurrency guarantee — and only cleans them up on a later pass, once the elapsed time exceeds <code>2 × clusterCheckinInterval + clusterCheckinMisfireThreshold</code>. So with a 15-second interval and a 60-second threshold, expect up to about 90 seconds plus one more check-in cycle before such a trigger stops reporting <code>Executing</code>.</p><h3 id="if-you-implement-idriverdelegate-the-executing-state" tabindex="-1"><a class="header-anchor" href="#if-you-implement-idriverdelegate-the-executing-state"><span>If you implement <code>IDriverDelegate</code>: the executing state</span></a></h3><p><code>IsTriggerCurrentlyExecuting</code> was replaced by <code>SelectTriggerStateWithExecuting</code>, which returns the stored state and whether an execution is in flight from a single statement, so reporting a trigger&#39;s state stays one round trip. Subclasses of <code>StdAdoDelegate</code> get it for free. There is no schema change.</p><p>Note that <code>GetTriggerState</code> now calls this method instead of <code>SelectTriggerState</code>. If you override <code>SelectTriggerState</code> to handle a vendor quirk or a legacy state value, override <code>SelectTriggerStateWithExecuting</code> as well — the compiler cannot tell you, because the old method is still on the interface and still used elsewhere.</p><h2 id="what-is-running-is-a-listing-not-a-list-of-contexts" tabindex="-1"><a class="header-anchor" href="#what-is-running-is-a-listing-not-a-list-of-contexts"><span>What is running is a listing, not a list of contexts</span></a></h2><p><code>IScheduler.GetCurrentlyExecutingJobs()</code> is <strong>removed</strong>. It read a list of live <code>IJobExecutionContext</code>s that this process happened to be holding, so it could only ever answer for one node — the same gap that <code>TriggerState.Executing</code> above works around from the other side.</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> List&lt;IJobExecutionContext&gt; running = await scheduler.GetCurrentlyExecutingJobs();</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> PagedResult&lt;FireInstance&gt; running = await scheduler.QueryFireInstances();</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>With a persistent job store the answer now covers the whole cluster, because a firing is a row rather than a field. <code>FireInstance</code> is what a store can say about one firing from anywhere:</p><table><thead><tr><th>Member</th><th>Meaning</th></tr></thead><tbody><tr><td><code>FireInstanceId</code></td><td>identifies this firing; what <code>InterruptFireInstance</code> takes</td></tr><tr><td><code>TriggerKey</code></td><td>the trigger that fired</td></tr><tr><td><code>JobKey</code></td><td>the job — <code>null</code> while the firing is only <code>Acquired</code></td></tr><tr><td><code>SchedulerInstanceId</code></td><td>the node that reserved or is running it</td></tr><tr><td><code>State</code></td><td><code>FireInstanceState.Acquired</code> or <code>FireInstanceState.Executing</code></td></tr><tr><td><code>FireTimeUtc</code></td><td>when the owning node recorded the firing</td></tr><tr><td><code>ScheduledFireTimeUtc</code></td><td>the fire time the schedule called for</td></tr><tr><td><code>ExecutionGroup</code></td><td>the execution group the trigger carried when it fired</td></tr></tbody></table><p><code>FireInstanceQuery</code> derives from <code>PagedQuery</code> and filters by trigger group and name matchers, job key, scheduler instance id, and state. <strong><code>State</code> defaults to <code>Executing</code></strong>, so a query that says nothing lists what is running, which is what the old member meant; set it to <code>null</code> to include reservations. Results are ordered by trigger group, then trigger name, then fire instance id — one trigger can have several firings at once, so the fire instance id is what makes a page deterministic.</p><h3 id="what-a-fire-instance-deliberately-does-not-carry" tabindex="-1"><a class="header-anchor" href="#what-a-fire-instance-deliberately-does-not-carry"><span>What a fire instance deliberately does not carry</span></a></h3><p>The job instance, the merged job data map, <code>Result</code>, <code>JobRunTime</code> and the cancellation handle exist only inside the process running the job. If you were reaching for those — a progress endpoint, a &quot;cancel this one&quot; button implemented in-process — keep the contexts yourself. That is about thirty lines:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token keyword">public</span> <span class="token keyword">sealed</span> <span class="token keyword">class</span> <span class="token class-name">RunningJobs</span> <span class="token punctuation">:</span> <span class="token type-list"><span class="token class-name">IJobListener</span></span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    <span class="token keyword">private</span> <span class="token keyword">readonly</span> <span class="token class-name">ConcurrentDictionary<span class="token punctuation">&lt;</span><span class="token keyword">string</span><span class="token punctuation">,</span> IJobExecutionContext<span class="token punctuation">&gt;</span></span> running <span class="token operator">=</span> <span class="token keyword">new</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line">    <span class="token keyword">public</span> <span class="token return-type class-name"><span class="token keyword">string</span></span> Name <span class="token operator">=&gt;</span> <span class="token keyword">nameof</span><span class="token punctuation">(</span>RunningJobs<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line">    <span class="token keyword">public</span> <span class="token return-type class-name">IReadOnlyCollection<span class="token punctuation">&lt;</span>IJobExecutionContext<span class="token punctuation">&gt;</span></span> Current <span class="token operator">=&gt;</span> running<span class="token punctuation">.</span>Values<span class="token punctuation">.</span><span class="token function">ToArray</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line">    <span class="token keyword">public</span> <span class="token return-type class-name">ValueTask</span> <span class="token function">JobToBeExecuted</span><span class="token punctuation">(</span><span class="token class-name">IJobExecutionContext</span> context<span class="token punctuation">,</span> <span class="token class-name">CancellationToken</span> cancellationToken <span class="token operator">=</span> <span class="token keyword">default</span><span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">{</span></span>
<span class="line">        running<span class="token punctuation">[</span>context<span class="token punctuation">.</span>FireInstanceId<span class="token punctuation">]</span> <span class="token operator">=</span> context<span class="token punctuation">;</span></span>
<span class="line">        <span class="token keyword">return</span> <span class="token keyword">default</span><span class="token punctuation">;</span></span>
<span class="line">    <span class="token punctuation">}</span></span>
<span class="line"></span>
<span class="line">    <span class="token keyword">public</span> <span class="token return-type class-name">ValueTask</span> <span class="token function">JobWasExecuted</span><span class="token punctuation">(</span><span class="token class-name">IJobExecutionContext</span> context<span class="token punctuation">,</span> <span class="token class-name">JobExecutionException<span class="token punctuation">?</span></span> jobException<span class="token punctuation">,</span> <span class="token class-name">CancellationToken</span> cancellationToken <span class="token operator">=</span> <span class="token keyword">default</span><span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">{</span></span>
<span class="line">        running<span class="token punctuation">.</span><span class="token function">TryRemove</span><span class="token punctuation">(</span>context<span class="token punctuation">.</span>FireInstanceId<span class="token punctuation">,</span> <span class="token keyword">out</span> _<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">        <span class="token keyword">return</span> <span class="token keyword">default</span><span class="token punctuation">;</span></span>
<span class="line">    <span class="token punctuation">}</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span>
<span class="line"><span class="token comment">// registered like any other listener</span></span>
<span class="line">scheduler<span class="token punctuation">.</span>ListenerManager<span class="token punctuation">.</span><span class="token function">AddJobListener</span><span class="token punctuation">(</span>runningJobs<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Note the key: <code>FireInstanceId</code> is what ties a context you are holding to a row <code>QueryFireInstances</code> returned, so the two can be used together — list across the cluster, then reach for the live context when the firing turns out to be yours.</p><h3 id="three-things-to-know-about-the-numbers" tabindex="-1"><a class="header-anchor" href="#three-things-to-know-about-the-numbers"><span>Three things to know about the numbers</span></a></h3><ul><li><strong>A vetoed firing does not linger.</strong> Applying an <code>ITriggerListener</code> veto completes the firing, which removes it. It can be listed for the instant between the store recording the firing and the veto being decided, and never after. <code>GetCurrentlyExecutingJobs</code> never showed one at all, because it only recorded a job that had started.</li><li><strong>Elapsed time is <code>yourClock.UtcNow - FireTimeUtc</code></strong>, and <code>FireTimeUtc</code> was written by the firing node&#39;s clock. In a cluster with skewed clocks the subtraction carries the skew and can come out negative; clamp it at zero, as the dashboard does.</li><li><strong><code>ScheduledFireTimeUtc</code> is the schedule after a misfire, not before it.</strong> It is what the owning node recorded, so for a misfired trigger it is the rescheduled time rather than the one that was missed — it can differ from <code>IJobExecutionContext.ScheduledFireTimeUtc</code>, and <code>FireTimeUtc - ScheduledFireTimeUtc</code> is not misfire lateness.</li></ul><h3 id="schedulermetadata-gained-a-node-local-count" tabindex="-1"><a class="header-anchor" href="#schedulermetadata-gained-a-node-local-count"><span><code>SchedulerMetadata</code> gained a node-local count</span></a></h3><p><code>SchedulerMetadata.LocalExecutingJobs</code> is the number of executions running in <em>this</em> process. It is named for its scope on purpose: it and a cluster-wide <code>QueryFireInstances</code> count answer different questions and will differ on a cluster.</p><h3 id="the-http-endpoint-changed-shape-breaking" tabindex="-1"><a class="header-anchor" href="#the-http-endpoint-changed-shape-breaking"><span>The HTTP endpoint changed shape (breaking)</span></a></h3><p><code>GET …/jobs/currently-executing</code> is replaced by <code>GET …/jobs/fire-instances</code>, which is paged like the other listings. The body was a bare array of contexts; it is now the usual <code>{ items, hasMore, totalCount }</code> envelope.</p><table><thead><tr><th>Old field</th><th>New field</th></tr></thead><tbody><tr><td><code>jobDetail.name</code> / <code>jobDetail.group</code></td><td><code>jobName</code> / <code>jobGroup</code> — and they are <code>null</code> for a reserved firing</td></tr><tr><td><code>jobDetail.*</code> (type, durability, recovery, concurrency flags, job data map)</td><td>gone; fetch the job detail by key if you need it</td></tr><tr><td><code>trigger</code> (the whole serialized trigger)</td><td><code>triggerName</code> / <code>triggerGroup</code></td></tr><tr><td><code>trigger.executionGroup</code></td><td><code>executionGroup</code></td></tr><tr><td><code>calendar</code></td><td>gone</td></tr><tr><td><code>recovering</code></td><td>gone</td></tr><tr><td><code>fireTime</code></td><td><code>fireTimeUtc</code></td></tr><tr><td><code>scheduledFireTime</code></td><td><code>scheduledFireTimeUtc</code></td></tr><tr><td><code>previousFireTime</code>, <code>nextFireTime</code></td><td>gone; they describe the trigger, not the firing</td></tr><tr><td>—</td><td><code>fireInstanceId</code> (new — the old DTO never carried it, although clients read for it)</td></tr><tr><td>—</td><td><code>schedulerInstanceId</code> (new)</td></tr><tr><td>—</td><td><code>state</code> (new; <code>&quot;Acquired&quot;</code> or <code>&quot;Executing&quot;</code>, a name like every other enum on the wire)</td></tr></tbody></table><p>Query parameters are <code>skip</code>, <code>take</code>, <code>includeTotalCount</code>, the <code>group*</code>/<code>name*</code> matcher forms the other listings use, <code>jobName</code>+<code>jobGroup</code>, <code>schedulerInstanceId</code>, and <code>state</code>. A request naming no <code>state</code> gets the query record&#39;s default of <code>Executing</code>; <code>state=Any</code> asks for every state.</p><p>The scheduler body&#39;s <code>statistics</code> object gained <code>localExecutingJobs</code>, mirroring the metadata member.</p><h3 id="if-you-implement-ijobstore" tabindex="-1"><a class="header-anchor" href="#if-you-implement-ijobstore"><span>If you implement <code>IJobStore</code></span></a></h3><p>Four members. <code>QueryFireInstances(FireInstanceQuery, CancellationToken)</code> is the listing, abstract like the rest of the query family; <code>QueryClusterNodes(CancellationToken)</code> is the node listing described in <a href="#the-nodes-of-a-cluster-are-a-listing">The nodes of a cluster are a listing</a>; <code>Exists(string calendarName, CancellationToken)</code> is what replaced <code>CalendarExists</code> and is meant to be answered without materializing the calendar. And <code>Initialize</code> takes a <code>SchedulerIdentity</code>, which is the whole of what it takes — see <a href="#spi-changes">SPI changes</a> for what its 3.x parameters became:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token return-type class-name">ValueTask</span> <span class="token function">Initialize</span><span class="token punctuation">(</span><span class="token class-name">SchedulerIdentity</span> identity<span class="token punctuation">,</span> <span class="token class-name">CancellationToken</span> cancellationToken <span class="token operator">=</span> <span class="token keyword">default</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>The identity — the scheduler&#39;s name and this node&#39;s instance id — could not be a constructor argument, because with <code>GenerateInstanceId</code> the id is produced by an <code>IInstanceIdGenerator</code> that runs after the container has built the object graph. Initialization is the first moment it is settled, which is the same reasoning as <code>LockHandlerContext</code> on <code>ILockHandler.Initialize</code>. A store records it against the firings it owns, so that a listing can say which node is running what.</p><p>The ADO.NET store previously learned a generated id through a special case in the scheduler factory, which is gone: every store is told, the same way, at the same moment.</p><h3 id="if-you-implement-idriverdelegate-fire-instances" tabindex="-1"><a class="header-anchor" href="#if-you-implement-idriverdelegate-fire-instances"><span>If you implement <code>IDriverDelegate</code>: fire instances</span></a></h3><p><code>SelectFireInstances(conn, FireInstanceQuery, CancellationToken)</code> is new. Subclasses of <code>StdAdoDelegate</code> get it for free. It is a separate member rather than paging on <code>SelectFiredTriggerRecords</code>, because every caller of that one is a recovery pass that has to see all the rows — handing one a page would leave the rest unrecovered.</p><p><code>QRTZ_FIRED_TRIGGERS.EXECUTION_GROUP</code> is now live: written by the fired-trigger insert and update, read back into <code>FireInstance.ExecutionGroup</code>. <strong>No schema change</strong> — the column has shipped on every dialect since 3.18 — but rows written by an earlier 4.0 preview hold <code>NULL</code>.</p><h2 id="the-nodes-of-a-cluster-are-a-listing" tabindex="-1"><a class="header-anchor" href="#the-nodes-of-a-cluster-are-a-listing"><span>The nodes of a cluster are a listing</span></a></h2><p>Nothing in 3.x read <code>QRTZ_SCHEDULER_STATE</code>. The table was written on every check-in and swept by the failover pass, and that was the whole of it: an operator asking which of four nodes was alive ran SQL by hand, because <code>SchedulerMetadata.JobStoreClustered</code> — a <code>bool</code> saying clustering is <em>on</em> — was the only cluster fact the API exposed.</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token class-name">List<span class="token punctuation">&lt;</span>ClusterNode<span class="token punctuation">&gt;</span></span> nodes <span class="token operator">=</span> <span class="token keyword">await</span> scheduler<span class="token punctuation">.</span><span class="token function">QueryClusterNodes</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p><code>ClusterNode</code> is <code>InstanceId</code>, <code>LastCheckInUtc</code>, <code>CheckInInterval</code>, a <code>ClusterNodeState</code> of <code>Alive</code>, <code>Overdue</code> or <code>Failed</code>, and <code>IsCurrentNode</code>. The node answering is first, always present — even before its first check-in has written a row — and the only one whose <code>IsCurrentNode</code> is <code>true</code>; the rest follow by instance id. The state is decided by the same predicate the recovery sweep applies, so the listing and the failover it predicts cannot disagree, and the two times are <code>null</code> rather than zero when the store keeps no check-in history. <strong><code>null</code> is not the epoch</strong>: a reader that falls back to <code>DateTimeOffset.MinValue</code> will draw a node that has been dead since year one.</p>`,50),e(`p`,null,[l[291]||=r(`A scheduler that is not clustered answers with the one node it is, `,-1),l[292]||=e(`code`,null,`Alive`,-1),l[293]||=r(` and with no times, rather than with an empty list, so a caller need not branch on whether clustering is on. See `,-1),c(g,{to:`/documentation/quartz-4.x/tutorial/advanced-enterprise-features.html#seeing-the-cluster`},{default:i(()=>[...l[290]||=[r(`Seeing the cluster`,-1)]]),_:1}),l[294]||=r(`.`,-1)]),l[882]||=e(`p`,null,[e(`code`,null,`SchedulerStateRecord`),r(` is unchanged and stays the ADO.NET store's own row shape, the way `),e(`code`,null,`FiredTriggerRecord`),r(` sits beside `),e(`code`,null,`FireInstance`),r(`; `),e(`code`,null,`ClusterNode`),r(` is the store-neutral projection.`)],-1),e(`table`,null,[l[305]||=e(`thead`,null,[e(`tr`,null,[e(`th`,null,`Where`),e(`th`,null,`What is new`)])],-1),e(`tbody`,null,[l[302]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`IScheduler`)]),e(`td`,null,[e(`code`,null,`ValueTask<List<ClusterNode>> QueryClusterNodes(CancellationToken cancellationToken = default)`),r(` — implement it if you have an `),e(`code`,null,`IScheduler`),r(` of your own; `),e(`code`,null,`DelegatingScheduler`),r(` forwards it for you`)])],-1),l[303]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`IJobStore`)]),e(`td`,null,[e(`code`,null,`ValueTask<List<ClusterNode>> QueryClusterNodes(CancellationToken cancellationToken = default)`),r(` — a plain member, so a store of your own must implement it. A store with no cluster state answers with one node: itself, `),e(`code`,null,`Alive`),r(`, both times `),e(`code`,null,`null`)])],-1),e(`tr`,null,[l[301]||=e(`td`,null,`HTTP API`,-1),e(`td`,null,[l[296]||=e(`code`,null,`GET {ApiPath}/schedulers/{name}/nodes`,-1),l[297]||=r(`, unpaged, and `,-1),l[298]||=e(`code`,null,`HttpScheduler.QueryClusterNodes`,-1),l[299]||=r(` reads it (see `,-1),c(g,{to:`/documentation/quartz-4.x/packages/http-api.html#cluster-nodes`},{default:i(()=>[...l[295]||=[r(`Cluster nodes`,-1)]]),_:1}),l[300]||=r(`)`,-1)])]),l[304]||=e(`tr`,null,[e(`td`,null,`Dashboard`),e(`td`,null,[e(`code`,null,`IQuartzApiClient.QueryClusterNodes(name, CancellationToken)`),r(`, and a `),e(`strong`,null,`Cluster`),r(` page at `),e(`code`,null,`/quartz/cluster`)])],-1)])]),l[883]||=e(`p`,null,[r(`Neither member touches the schema, and neither writes anything: `),e(`code`,null,`QueryClusterNodes`),r(` is a read of the rows the check-in loop already keeps.`)],-1),l[884]||=e(`h2`,{id:`an-unset-execution-group-can-be-the-trigger-s-group`,tabindex:`-1`},[e(`a`,{class:`header-anchor`,href:`#an-unset-execution-group-can-be-the-trigger-s-group`},[e(`span`,null,`An unset execution group can be the trigger's group`)])],-1),e(`p`,null,[l[307]||=e(`code`,null,`ExecutionLimitsBuilder.UseTriggerGroupWhenUnset()`,-1),l[308]||=r(` limits a trigger that carries no execution group as though it belonged to a group named after its own `,-1),l[309]||=e(`code`,null,`TriggerKey.Group`,-1),l[310]||=r(`. It is opt-in and additive; nothing changes unless you call it. See `,-1),c(g,{to:`/documentation/quartz-4.x/tutorial/execution-groups.html#letting-the-trigger-group-stand-in`},{default:i(()=>[...l[306]||=[r(`Execution Groups`,-1)]]),_:1}),l[311]||=r(`.`,-1)]),l[885]||=t(`<p>One signature moved with it: <code>ExecutionSlots.TryTake</code> takes the trigger group as a second argument, so a job store of your own cannot silently opt out of the derivation by not passing it. Whether the second argument is used at all is <code>ExecutionLimits.UsesTriggerGroupWhenUnset</code>, which is what the builder method sets.</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> if (!slots.TryTake(candidate.ExecutionGroup))</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> if (!slots.TryTake(candidate.ExecutionGroup, candidate.Key.Group))</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="an-execution-limit-can-be-cluster-wide" tabindex="-1"><a class="header-anchor" href="#an-execution-limit-can-be-cluster-wide"><span>An execution limit can be cluster-wide</span></a></h2><p>An execution limit now says what it is counted against. <code>ExecutionLimitScope.Node</code>, the default and the only behaviour 3.x and earlier 4.0 previews had, is what <em>this</em> node may run — so an N-node cluster runs up to N times the number. <code>ExecutionLimitScope.Cluster</code> is what every node sharing the job store may run between them, which is what a per-tenant quota actually means:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line">q<span class="token punctuation">.</span><span class="token function">UseExecutionLimits</span><span class="token punctuation">(</span>limits <span class="token operator">=&gt;</span> limits</span>
<span class="line">    <span class="token punctuation">.</span><span class="token function">ForGroup</span><span class="token punctuation">(</span><span class="token string">&quot;high-cpu&quot;</span><span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">)</span>                                  <span class="token comment">// per node, as before</span></span>
<span class="line">    <span class="token punctuation">.</span><span class="token function">ForGroup</span><span class="token punctuation">(</span><span class="token string">&quot;tenant-acme&quot;</span><span class="token punctuation">,</span> <span class="token number">8</span><span class="token punctuation">,</span> ExecutionLimitScope<span class="token punctuation">.</span>Cluster<span class="token punctuation">)</span>  <span class="token comment">// per cluster</span></span>
<span class="line">    <span class="token punctuation">.</span><span class="token function">ForOtherGroups</span><span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Nothing changes for a configuration that does not ask for it, and both scopes can appear in one set of limits. <code>quartz.clusterExecutionLimit.&lt;group&gt;</code> is the property spelling, taking the same group keys and values as <code>quartz.executionLimit.&lt;group&gt;</code>.</p><p><strong>No schema change.</strong> The count is an aggregate over <code>QRTZ_FIRED_TRIGGERS.EXECUTION_GROUP</code>, which has shipped on every dialect since 3.18 and has been written since the fired-trigger insert started carrying it. A row there already has a reservation&#39;s lifetime — inserted on acquisition, updated when the trigger fires, deleted on completion or by cluster recovery — so there is no new table, no new column, and no migration on either branch.</p>`,7),e(`p`,null,[l[313]||=r(`Read `,-1),c(g,{to:`/documentation/quartz-4.x/tutorial/execution-groups.html#clustering-considerations`},{default:i(()=>[...l[312]||=[r(`Execution Groups`,-1)]]),_:1}),l[314]||=r(` before relying on it: the ceiling is `,-1),l[315]||=e(`strong`,null,`approximate with a bounded overshoot`,-1),l[316]||=r(` unless `,-1),l[317]||=e(`code`,null,`AcquireTriggersWithinLock`,-1),l[318]||=r(` is on, it `,-1),l[319]||=e(`strong`,null,`fails closed`,-1),l[320]||=r(` (a node that cannot reach the store fires nothing rather than firing unmetered), and work held at a ceiling for longer than `,-1),l[321]||=e(`code`,null,`MisfireThreshold`,-1),l[322]||=r(` goes to misfire handling.`,-1)]),l[886]||=t(`<h3 id="what-moved" tabindex="-1"><a class="header-anchor" href="#what-moved"><span>What moved</span></a></h3><table><thead><tr><th>Before</th><th>4.0</th></tr></thead><tbody><tr><td><code>ExecutionGroupLimit(ExecutionGroupScope Scope, int? MaxConcurrent)</code></td><td><code>ExecutionGroupLimit(ExecutionGroupScope Group, int? MaxConcurrent, ExecutionLimitScope Scope = Node)</code></td></tr><tr><td><code>ExecutionLimits.TryGetLimit(ExecutionGroupScope scope, out int?)</code></td><td><code>TryGetLimit(ExecutionGroupScope group, out int?)</code> — parameter renamed; the number is still all it returns</td></tr><tr><td><code>ExecutionLimits.CreateSlots()</code></td><td><code>CreateSlots(IReadOnlyCollection&lt;ExecutionGroupInFlight&gt;? clusterInFlight = null)</code></td></tr><tr><td>—</td><td><code>ExecutionLimits.HasClusterScopedLimits</code></td></tr><tr><td>—</td><td><code>ExecutionGroupInFlight(string? ExecutionGroup, string TriggerGroup, int Count)</code></td></tr><tr><td>—</td><td><code>TriggerAcquisitionCriteria.ClusterInFlight</code></td></tr><tr><td>—</td><td><code>IDriverDelegate.SelectExecutionGroupsInFlight(conn, cancellationToken)</code></td></tr><tr><td><code>ExecutionLimitsResponse</code> / <code>SetExecutionLimitsRequest</code> carrying <code>Dictionary&lt;string, int?&gt;</code></td><td>carrying <code>Dictionary&lt;string, ExecutionLimitDto&gt;</code>, where the DTO is <code>(int? MaxConcurrent, ExecutionLimitScope Scope)</code></td></tr><tr><td><code>ExecutionLimitsDto(Dictionary&lt;string, int?&gt; Limits)</code> (dashboard)</td><td><code>ExecutionLimitsDto(Dictionary&lt;string, DashboardExecutionLimit&gt; Limits, bool UsesTriggerGroupWhenUnset = false, bool CanReport = true)</code> — see <a href="#the-dashboard-s-client-speaks-one-currency">The dashboard&#39;s client speaks one currency</a></td></tr></tbody></table><p>If you implement <code>IDriverDelegate</code> from scratch rather than deriving from <code>StdAdoDelegate</code>, the new member is a compile break — deliberately, because returning &quot;nothing in flight&quot; from a stub would fail the ceiling open. <code>StdAdoDelegate</code> implements it for every dialect with one statement and no overrides.</p><p>If you implement <code>IJobStore</code>, nothing is required: a store that reports <code>Clustered == false</code> has one node, so a cluster-scoped limit and a node-scoped one are the same number, and the limits it is handed already say what that number is. A store that <em>is</em> clustered honours the scope by passing its own in-flight counts to <code>CreateSlots</code>, and must <strong>not</strong> expect the scheduler thread to have subtracted this node&#39;s running work from a cluster-scoped limit — those firings are already reservations in its own ledger, and subtracting them twice halves the quota.</p><h2 id="batched-misfire-recovery" tabindex="-1"><a class="header-anchor" href="#batched-misfire-recovery"><span>Batched Misfire Recovery</span></a></h2><p>Misfire recovery now handles a whole batch of misfired triggers with a handful of statements instead of a few per trigger. Nothing needs configuring — the read side is a single set-based query, and the write side uses ADO.NET batching automatically on providers that support it (<code>DbConnection.CanCreateBatch</code>), falling back to individual statements on those that do not.</p><p>This matters if you implement <code>IDriverDelegate</code> yourself, which now has two more members:</p><table><thead><tr><th>Member</th><th>Purpose</th></tr></thead><tbody><tr><td><code>SelectMisfiredTriggersToRecover</code></td><td>Reads a whole misfire batch as populated triggers in one round-trip</td></tr><tr><td><code>UpdateMisfiredTriggers</code></td><td>Applies a batch of misfire updates, batching the statements where supported</td></tr></tbody></table><p>If you subclass <code>StdAdoDelegate</code> you get both for free. A driver delegate for a database with its own row-limiting syntax should also override <code>GetSelectMisfiredTriggersToRecoverSql</code>.</p><p>One behavioral note: <code>ITriggerListener.TriggerMisfired</code> is now raised for every trigger in a batch before any of that batch&#39;s database updates are written, where previously the notification and the update were interleaved per trigger. Everything still happens inside the same transaction and under the same lock, so what other nodes observe is unchanged.</p><h2 id="batched-trigger-fire" tabindex="-1"><a class="header-anchor" href="#batched-trigger-fire"><span>Batched trigger fire</span></a></h2><p>Firing one trigger used to take between six and nine round trips inside the <code>TRIGGER_ACCESS</code> lock, and completing the job that fired took a read per trigger of that job on top. Every one of those is time every other node in the cluster spends waiting for the lock. The fire path now reads the trigger&#39;s row once and writes everything in one batch, and completion asks one question instead of one per trigger.</p><p>Nothing needs configuring. Batching is used where <code>DbConnection.CanCreateBatch</code> says the provider supports it, and the same statements go out one command at a time where it does not — a provider that cannot batch issues exactly the statements it always did, in the same order.</p><p>This matters if you implement <code>IDriverDelegate</code> yourself, which has three more members:</p><table><thead><tr><th>Member</th><th>Purpose</th></tr></thead><tbody><tr><td><code>ApplyTriggerFired</code></td><td>Every write one fire makes — the fired-trigger row, the misfire original fire time, the sibling states of a serial job, the trigger&#39;s row and its schedule — described by <code>TriggerFiredUpdate</code> and issued as one batch</td></tr><tr><td><code>UpdateTriggerStatesForJobFromOtherState(conn, jobKey, IReadOnlyList&lt;TriggerStateTransition&gt;, ct)</code></td><td>Several conditional state changes for one job in one round trip, beside the single-transition overload that is still there</td></tr><tr><td><code>SelectTriggerKeysForJob(conn, jobKey, StoredTriggerState, ct)</code></td><td>The keys of a job&#39;s triggers in one state, beside the unfiltered overload</td></tr></tbody></table><p><code>ITriggerPersistenceDelegate</code> gains <code>TryDescribeUpdateExtendedTriggerProperties</code>, which appends the statement <code>UpdateExtendedTriggerProperties</code> would have issued to a <code>List&lt;SqlStatement&gt;</code> rather than issuing it, so that a trigger&#39;s schedule travels in the same round trip as its row. It is a default interface member returning <code>false</code>, so a persistence delegate written before this keeps being given a round trip of its own and behaves exactly as it did. <code>SqlStatement</code> and <code>SqlStatementParameter</code> are public for this reason.</p><p>Two contracts moved to make it possible:</p><table><thead><tr><th>Was</th><th>Is</th></tr></thead><tbody><tr><td><code>StoredTriggerHeader(Key, JobKey, State, NextFireTimeUtc)</code></td><td><code>StoredTriggerHeader(Key, JobKey, State, NextFireTimeUtc, TriggerType)</code> — the discriminator comes off the same row, and reading it there is what removes the separate <code>SELECT TRIGGER_TYPE</code></td></tr><tr><td><code>SqlSelectTriggerHeader</code> projects four columns</td><td>It projects <code>TRIGGER_TYPE</code> as well</td></tr></tbody></table><p>Behavioral notes:</p><ul><li>The fired-trigger row is written from the scheduled fire time the store hands <code>ApplyTriggerFired</code>, not from the trigger&#39;s own next fire time. It used to be read off the trigger, which meant the row had to be written before <code>Triggered()</code> advanced it — an ordering constraint a batch cannot honour, and one that was never written down anywhere.</li><li>A batch that fails is replayed statement by statement so the exception names the statement that failed — unless the failure was transient, in which case it surfaces as itself. A replay against a connection that has just dropped, or a transaction the server has already doomed, produces a different and unrecognisable failure, and the store&#39;s retry only recognises a transient failure from the exception it is handed. This applies to the batched misfire writes above as well.</li><li>The exception a failed fire raises now names the fire rather than the individual statement: <code>Couldn&#39;t record the fire of trigger &#39;…&#39; for &#39;…&#39; job</code>, where it used to be one of <code>Couldn&#39;t update fired trigger</code>, <code>Couldn&#39;t update states of blocked triggers</code> or <code>Couldn&#39;t store trigger &#39;…&#39;</code>.</li><li><strong><code>IDriverDelegate.UpdateFiredTrigger</code> is gone.</strong> It was the fire path&#39;s only caller, and <code>ApplyTriggerFired</code> writes that row as one command of its batch instead. A delegate that overrode it to change what a fire records has to override <code>ApplyTriggerFired</code> — which is why the member was removed rather than left in place: an override that is never invoked fails silently, and a customisation discovered to have stopped working in production is worse than one that fails to compile at upgrade time. The statement itself is unchanged; the internal <code>StdAdoConstants.SqlUpdateFiredTrigger</code> still spells it.</li><li>The fire path also no longer calls <code>TriggerExists</code> or <code>UpdateTrigger</code>, both of which stay because other paths still use them. <strong>A delegate that overrode either of those to change what a fire stores has to move that override to <code>ApplyTriggerFired</code> as well</strong> — they still compile and still work, they are simply no longer on this path.</li><li>Completion no longer loads a job&#39;s triggers to ask the database for each one&#39;s state. It asks for the keys in the state it cares about, loads those in one read, and applies their misfire policies as one batched write, which is what misfire recovery already did. A trigger that runs out of fire times while blocked is still stored <code>COMPLETE</code>, still finalized to the scheduler listeners, and still deleted.</li></ul><h2 id="batched-cluster-recovery" tabindex="-1"><a class="header-anchor" href="#batched-cluster-recovery"><span>Batched cluster recovery</span></a></h2><p>Recovering a failed node used to issue up to ten statements per fired-trigger row it left behind, each its own round trip, all of them inside the <code>TRIGGER_ACCESS</code> lock every other node is waiting for. The statements are the same; they now travel a set at a time.</p><p>Nothing needs configuring, and the same <code>DbBatch</code> support decides it as everywhere else: providers that cannot batch issue exactly the statements they always did, one command at a time, in the same order.</p><p>This matters if you implement <code>IDriverDelegate</code> yourself, which has three more members:</p><table><thead><tr><th>Member</th><th>Purpose</th></tr></thead><tbody><tr><td><code>UpdateTriggerStatesForJobsFromOtherState(conn, IReadOnlyCollection&lt;JobKey&gt;, newState, oldState, ct)</code></td><td>The single-job overload&#39;s statement, once per job key, in one batch — unblocking the siblings of every interrupted execution</td></tr><tr><td><code>DeleteFiredTriggers(conn, IReadOnlyCollection&lt;string&gt; entryIds, ct)</code></td><td>The single-entry delete, once per id, in one batch — clearing a dead node&#39;s rows while holding some of them back</td></tr><tr><td><code>SelectTriggerKeysInState(conn, IReadOnlyCollection&lt;TriggerKey&gt;, state, ct)</code></td><td>One read saying which of the given triggers are in a state, where <code>SelectTriggerState</code> per key was a round trip per key — deciding which of a dead node&#39;s triggers had run to COMPLETE</td></tr></tbody></table><p>The first two return <code>ValueTask</code> rather than a row count: a batch does not report one per command in any portable way, and the store counts the rows it asked about rather than the rows that moved. Subclassing <code>StdAdoDelegate</code> gets all three for free.</p><p>Releasing what a dead node had reserved is the fourth such step, and it uses <code>UpdateTriggerStatesFromOtherStates(conn, IReadOnlyCollection&lt;TriggerKey&gt;, newState, oldStates, ct)</code> — described under <a href="#batched-trigger-acquisition-and-the-sets-pause-and-resume-work-on">Batched trigger acquisition, and the sets pause and resume work on</a>, because pause and resume want the same shape. It is one <code>UPDATE</code> with a key-set predicate rather than a command per key, so it <em>does</em> report a row count; a caller wanting one old state passes a set of one.</p><p>Behavioural notes:</p>`,28),e(`ul`,null,[l[331]||=e(`li`,null,[e(`code`,null,`ClusterRecover`),r(` is a sequence of named steps — release, unblock, reschedule, delete the fired rows, delete the triggers those rows were the last of, give up the failed node's registration — rather than one pass that did all of it per row. The steps are grouped by what they do instead of issued row by row, which is only safe because they run in one transaction under one lock and touch disjoint rows. What another node can observe is unchanged.`)],-1),l[332]||=e(`li`,null,`Scheduling a replacement firing is still row by row: each one reads whether its job still exists and what its trigger's data map holds, and both answers decide whether anything is written at all.`,-1),e(`li`,null,[l[324]||=e(`strong`,null,[r(`A node that finds its own `),e(`code`,null,`QRTZ_SCHEDULER_STATE`),r(` row gone now handles it.`)],-1),l[325]||=r(` On 3.x it logged `,-1),l[326]||=e(`code`,null,`This scheduler instance (…) is still active but was recovered by another instance in the cluster`,-1),l[327]||=r(` and carried on. It now writes its row back, names the peer that recovered it where the remaining rows say which one that must have been, counts the event on `,-1),l[328]||=e(`code`,null,`quartz.cluster.recovery.trigger`,-1),l[329]||=r(` under its own instance id, and does not recover its own fired triggers — the peer already did, and doing it again would replay a firing that is already being replayed. See `,-1),c(g,{to:`/documentation/quartz-4.x/operations.html#when-the-node-that-was-taken-over-is-still-running`},{default:i(()=>[...l[323]||=[r(`When the node that was taken over is still running`,-1)]]),_:1}),l[330]||=r(`.`,-1)])]),l[887]||=t(`<h2 id="batched-trigger-acquisition-and-the-sets-pause-and-resume-work-on" tabindex="-1"><a class="header-anchor" href="#batched-trigger-acquisition-and-the-sets-pause-and-resume-work-on"><span>Batched trigger acquisition, and the sets pause and resume work on</span></a></h2><p>Acquiring a trigger used to cost three statements per candidate on top of the read that found it: the acquisition <code>SELECT</code> named the batch, and then each candidate was read back on its own, compare-and-swapped into the acquired state, and written to the fired-triggers table. Pausing or resuming a set of triggers — or a matcher&#39;s worth of jobs — walked the set and called the single-key member, which is two or three statements per key. All of it runs inside the <code>TRIGGER_ACCESS</code> lock, so all of it is time the other nodes of a cluster spend waiting.</p><p>A round now reads its candidates in one statement and writes their fired-trigger rows in one batch; the compare-and-swap stays per candidate, because its result is what decides whether that candidate was acquired at all. Pause and resume read the stored states of a whole set once and then issue one statement per transition the set turns out to need. Nothing needs configuring, and nothing about the rows written or the answers returned has changed.</p><p>This matters if you implement <code>IDriverDelegate</code> yourself. Every member below is a <strong>default interface member</strong> whose default is the per-key loop the store used to make, so a delegate written before this keeps compiling and keeps behaving exactly as it did — it simply keeps paying the round trips. <code>StdAdoDelegate</code> overrides all of them, so a delegate deriving from it gets the batched forms for free.</p><table><thead><tr><th>Member</th><th>Purpose</th></tr></thead><tbody><tr><td><code>InsertFiredTriggers(conn, IReadOnlyList&lt;IOperableTrigger&gt;, state, jobDetail, ct)</code></td><td>An acquisition round&#39;s fired-trigger rows as one batch</td></tr><tr><td><code>SelectStoredTriggerHeaders(conn, IReadOnlyCollection&lt;TriggerKey&gt;, ct)</code></td><td>The plural of <code>SelectTriggerHeader</code>; what pause and resume decide a whole set&#39;s transitions from. Not the listing <code>SelectTriggerHeaders</code>, which pages over <code>TriggerHeader</code></td></tr><tr><td><code>UpdateTriggerStatesFromOtherStates(conn, IReadOnlyCollection&lt;TriggerKey&gt;, newState, oldStates, ct)</code></td><td>One conditional transition for a whole key set — one <code>UPDATE</code> with a key-set predicate — beside the store-wide overload that is still there. Cluster recovery releases a dead node&#39;s reservations through it as well, with a set of one old state</td></tr><tr><td><code>SelectTriggerKeysForJobs(conn, IReadOnlyCollection&lt;JobKey&gt;, ct)</code></td><td>Every trigger key of a set of jobs in one read, for pausing and resuming a job matcher</td></tr><tr><td><code>SelectPausedJobGroups(conn, IReadOnlyCollection&lt;string&gt;, ct)</code></td><td>Which of a set of job groups already have a paused row — the set form of <code>IsJobGroupPaused</code></td></tr><tr><td><code>InsertPausedJobGroups(conn, IReadOnlyCollection&lt;string&gt;, ct)</code></td><td>The missing paused-job-group rows, together</td></tr></tbody></table><p>One new property comes with them:</p><table><thead><tr><th>Member</th><th>Purpose</th></tr></thead><tbody><tr><td><code>FiltersAcquisitionJobTypeExclusions</code></td><td>Whether <code>SelectTriggersToAcquire</code> already keeps <code>TriggerAcquisitionCriteria.ExcludedJobTypeNames</code> out of the rows it returns. It defaults to <code>false</code>, and <code>StdAdoDelegate</code> answers <code>true</code></td></tr></tbody></table><p><code>ExcludedJobTypeNames</code> is the one acquisition criterion a delegate cannot safely ignore: ignoring it means the node <em>runs</em> job types the deployment excluded. <code>AdoJobStoreBase</code> therefore keeps a backstop, which now drops an excluded candidate on the job type name the acquisition read already returned — before the candidate costs a read and a type resolution. A delegate that answers <code>true</code> is taken at its word and the backstop is skipped entirely, so the shipped dialects pay nothing per candidate for a promise their own <code>NOT IN</code> clause already keeps. <strong>If you subclass <code>StdAdoDelegate</code> and override <code>GetSelectNextTriggerToAcquireSql</code> with a statement that does not splice in the exclusion terms, override <code>FiltersAcquisitionJobTypeExclusions</code> back to <code>false</code></strong>, or the exclusions stop being enforced anywhere.</p><p>Behavioral notes:</p><ul><li><code>StdAdoDelegate.SelectTriggersForJob</code> is now the job&#39;s trigger keys followed by one <code>SelectTriggers</code> for the set, where it used to read each trigger back on its own. The triggers and their order are the same.</li><li><code>StdAdoDelegate.SelectTriggers</code> and <code>InsertFiredTriggers</code> answer a one-element set through <code>SelectTrigger</code> and <code>InsertFiredTrigger</code>. One key is one statement either way, and the set forms pay for a key-set predicate and a <code>DbBatch</code> to say the same thing — which matters because the scheduler&#39;s default acquisition batch size is one. An override of either singular member therefore also takes effect for a set of one.</li><li>Pausing or resuming a job no longer materializes that job&#39;s triggers to reach their keys. The schedule in a trigger&#39;s type table was read and never looked at.</li><li><code>PauseAll</code> and <code>ResumeAll</code> pass the any-group matcher once rather than naming each trigger group in turn. The statements are the same ones; there is one set of them instead of one set per group.</li><li>A resume still applies each overdue trigger&#39;s misfire policy with that trigger&#39;s own write, because recovering a misfire recomputes one trigger&#39;s schedule and cannot be expressed as a set operation.</li></ul><h2 id="job-store-listings-became-queries" tabindex="-1"><a class="header-anchor" href="#job-store-listings-became-queries"><span>Job store listings became queries</span></a></h2><p>Listing jobs or triggers meant reading every key and then spending one round trip per key on anything more than the key — the job&#39;s type, the trigger&#39;s state or next fire time. Nothing could ask for a page, and nothing could ask for a count without materializing what it counted.</p><p>Those members are replaced by query members that take a query record and return one page of projected results. <strong>Existing code keeps compiling</strong>: every removed <code>IScheduler</code> member comes back as an extension method in <code>SchedulerQueryExtensions</code>, with the same name and signature.</p><h3 id="what-replaced-what" tabindex="-1"><a class="header-anchor" href="#what-replaced-what"><span>What replaced what</span></a></h3><p><code>IScheduler</code> — the left column still works, now as an extension method:</p><table><thead><tr><th>Removed from <code>IScheduler</code></th><th>Query member</th></tr></thead><tbody><tr><td><code>GetJobKeys(matcher)</code></td><td><code>QueryJobs(new JobQuery { Group = matcher })</code></td></tr><tr><td><code>GetTriggerKeys(matcher)</code></td><td><code>QueryTriggers(new TriggerQuery { Group = matcher })</code></td></tr><tr><td><code>GetJobGroupNames()</code></td><td><code>QueryJobGroups(new JobGroupQuery())</code></td></tr><tr><td><code>GetTriggerGroupNames()</code></td><td><code>QueryTriggerGroups(new TriggerGroupQuery())</code></td></tr><tr><td><code>GetPausedTriggerGroups()</code></td><td><code>QueryTriggerGroups(new TriggerGroupQuery { Paused = true })</code></td></tr><tr><td><code>GetCalendarNames()</code></td><td><code>QueryCalendarNames(new CalendarQuery())</code></td></tr><tr><td><code>IsJobGroupPaused(group)</code></td><td><code>QueryJobGroups(new JobGroupQuery { Name = group, Paused = true, Take = 1 })</code></td></tr><tr><td><code>IsTriggerGroupPaused(group)</code></td><td><code>QueryTriggerGroups(new TriggerGroupQuery { Name = group, Paused = true, Take = 1 })</code></td></tr></tbody></table><p><code>IJobStore</code> loses the same members plus the counting and existence ones, and has no extension methods to soften it — if you implement a job store, you implement the query members:</p><table><thead><tr><th>Removed from <code>IJobStore</code></th><th>Use instead</th></tr></thead><tbody><tr><td><code>GetJobKeys</code>, <code>GetTriggerKeys</code></td><td><code>QueryJobs</code>, <code>QueryTriggers</code></td></tr><tr><td><code>GetJobGroupNames</code>, <code>GetTriggerGroupNames</code>, <code>GetPausedTriggerGroups</code></td><td><code>QueryJobGroups</code>, <code>QueryTriggerGroups</code></td></tr><tr><td><code>GetCalendarNames</code></td><td><code>QueryCalendarNames</code></td></tr><tr><td><code>IsJobGroupPaused</code>, <code>IsTriggerGroupPaused</code></td><td>the matching <code>Query*Groups</code> with <code>Name</code> and <code>Paused = true</code></td></tr><tr><td><code>GetNumberOfJobs</code>, <code>GetNumberOfTriggers</code>, <code>GetNumberOfCalendars</code></td><td>the matching query with <code>Take = 0, IncludeTotalCount = true</code></td></tr><tr><td><code>CalendarExists(name)</code></td><td><code>Exists(string calendarName)</code>, which is the same question under the name the job and trigger overloads already use — see <a href="#reading-has-two-altitudes-on-purpose">Reading has two altitudes on purpose</a></td></tr></tbody></table><p>Two members are new on both interfaces: <strong><code>GetJobDetails(jobKeys)</code></strong> and <strong><code>GetTriggers(triggerKeys)</code></strong> retrieve many by key in one round trip. Keys that do not exist are simply absent, duplicates fold away, and results come back in the order the keys were asked for.</p><p>On <code>IJobStore</code> the bulk job fetch is spelled <strong><code>GetJobs(jobKeys)</code></strong>, completing the store&#39;s own pairs: <code>GetJob</code>/<code>GetJobs</code> beside <code>GetTrigger</code>/<code>GetTriggers</code>. The scheduler&#39;s noun is <code>JobDetail</code> — it hands users <code>IJobDetail</code>, so <code>IScheduler</code> keeps <code>GetJobDetail</code>/<code>GetJobDetails</code> — while the store speaks in storage terms. Singular/plural pairs are consistent within each interface; the two interfaces deliberately differ.</p><h3 id="paging-and-projection" tabindex="-1"><a class="header-anchor" href="#paging-and-projection"><span>Paging and projection</span></a></h3><p>Every query derives from <code>PagedQuery</code>, which carries <code>Skip</code>, <code>Take</code> and <code>IncludeTotalCount</code>. The result is a <code>PagedResult&lt;T&gt;</code> with <code>Items</code>, <code>HasMore</code> and a nullable <code>TotalCount</code>. <code>HasMore</code> is exact and costs nothing: stores read one item past <code>Take</code> rather than running a second query.</p><p><code>Take</code> defaults to <strong>250</strong> (<code>PagedQuery.DefaultTake</code>) — a query that sets nothing returns the first bounded page, and <code>HasMore</code> says whether anything was left out. Earlier 4.0 previews defaulted to <code>int.MaxValue</code>. The bound is the one value in one place: the HTTP endpoints apply the same default when a request names no <code>take</code>, the <code>HttpScheduler</code> always puts <code>take</code> on the wire (earlier previews omitted the parameter for <code>int.MaxValue</code>, which after this change would have silently handed the decision to the server), and the compat extension methods below pin <code>Take = PagedQuery.All</code> so their 3.x semantics — return everything — cannot silently narrow. Asking for everything is an explicit opt-in:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token class-name">PagedResult<span class="token punctuation">&lt;</span>JobHeader<span class="token punctuation">&gt;</span></span> everything <span class="token operator">=</span> <span class="token keyword">await</span> scheduler<span class="token punctuation">.</span><span class="token function">QueryJobs</span><span class="token punctuation">(</span><span class="token keyword">new</span> <span class="token constructor-invocation class-name">JobQuery</span> <span class="token punctuation">{</span> Take <span class="token operator">=</span> PagedQuery<span class="token punctuation">.</span>All <span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p><strong><code>PagedQuery.All</code></strong> is <code>int.MaxValue</code> under a name, and <code>?take=all</code> is what it is called on the wire. Both are additive: <code>Take = int.MaxValue</code> and <code>?take=2147483647</code> still say the same thing and go on working. They exist because &quot;everything&quot; was a magic number in a place where a magic number reads as an overflow guard, and because <code>?take=2147483647</code> in a request line is indistinguishable from a mistake.</p><p>The count idiom — <code>Take = 0, IncludeTotalCount = true</code> — is recognized by the stores, which then run only the count and skip the page select entirely.</p><p>Because the query types are records, walk a result by <code>with</code>-ing the next <code>Skip</code>:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token comment">// Before</span></span>
<span class="line"><span class="token class-name">IReadOnlyCollection<span class="token punctuation">&lt;</span>JobKey<span class="token punctuation">&gt;</span></span> keys <span class="token operator">=</span> <span class="token keyword">await</span> scheduler<span class="token punctuation">.</span><span class="token function">GetJobKeys</span><span class="token punctuation">(</span>GroupMatcher<span class="token operator">&lt;</span>JobKey<span class="token operator">&gt;</span><span class="token punctuation">.</span><span class="token function">AnyGroup</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"><span class="token keyword">foreach</span> <span class="token punctuation">(</span><span class="token class-name">JobKey</span> key <span class="token keyword">in</span> keys<span class="token punctuation">)</span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    <span class="token class-name">IJobDetail<span class="token punctuation">?</span></span> detail <span class="token operator">=</span> <span class="token keyword">await</span> scheduler<span class="token punctuation">.</span><span class="token function">GetJobDetail</span><span class="token punctuation">(</span>key<span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// one round trip each</span></span>
<span class="line">    Console<span class="token punctuation">.</span><span class="token function">WriteLine</span><span class="token punctuation">(</span><span class="token interpolation-string"><span class="token string">$&quot;</span><span class="token interpolation"><span class="token punctuation">{</span><span class="token expression language-csharp">key</span><span class="token punctuation">}</span></span><span class="token string"> -&gt; </span><span class="token interpolation"><span class="token punctuation">{</span><span class="token expression language-csharp">detail<span class="token punctuation">?.</span>JobType<span class="token punctuation">.</span>FullName</span><span class="token punctuation">}</span></span><span class="token string">&quot;</span></span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token comment">// After — one round trip per page, and the type name is already there</span></span>
<span class="line"><span class="token class-name">JobQuery</span> query <span class="token operator">=</span> <span class="token keyword">new</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span> Group <span class="token operator">=</span> GroupMatcher<span class="token operator">&lt;</span>JobKey<span class="token operator">&gt;</span><span class="token punctuation">.</span><span class="token function">AnyGroup</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span> Take <span class="token operator">=</span> <span class="token number">100</span> <span class="token punctuation">}</span><span class="token punctuation">;</span></span>
<span class="line"><span class="token keyword">while</span> <span class="token punctuation">(</span><span class="token boolean">true</span><span class="token punctuation">)</span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    <span class="token class-name">PagedResult<span class="token punctuation">&lt;</span>JobHeader<span class="token punctuation">&gt;</span></span> page <span class="token operator">=</span> <span class="token keyword">await</span> scheduler<span class="token punctuation">.</span><span class="token function">QueryJobs</span><span class="token punctuation">(</span>query<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">    <span class="token keyword">foreach</span> <span class="token punctuation">(</span><span class="token class-name">JobHeader</span> job <span class="token keyword">in</span> page<span class="token punctuation">.</span>Items<span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">{</span></span>
<span class="line">        Console<span class="token punctuation">.</span><span class="token function">WriteLine</span><span class="token punctuation">(</span><span class="token interpolation-string"><span class="token string">$&quot;</span><span class="token interpolation"><span class="token punctuation">{</span><span class="token expression language-csharp">job<span class="token punctuation">.</span>Key</span><span class="token punctuation">}</span></span><span class="token string"> -&gt; </span><span class="token interpolation"><span class="token punctuation">{</span><span class="token expression language-csharp">job<span class="token punctuation">.</span>JobTypeName</span><span class="token punctuation">}</span></span><span class="token string">&quot;</span></span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">    <span class="token punctuation">}</span></span>
<span class="line"></span>
<span class="line">    <span class="token keyword">if</span> <span class="token punctuation">(</span><span class="token operator">!</span>page<span class="token punctuation">.</span>HasMore<span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">{</span></span>
<span class="line">        <span class="token keyword">break</span><span class="token punctuation">;</span></span>
<span class="line">    <span class="token punctuation">}</span></span>
<span class="line"></span>
<span class="line">    query <span class="token operator">=</span> query <span class="token keyword">with</span> <span class="token punctuation">{</span> Skip <span class="token operator">=</span> query<span class="token punctuation">.</span>Skip <span class="token operator">+</span> page<span class="token punctuation">.</span>Items<span class="token punctuation">.</span>Count <span class="token punctuation">}</span><span class="token punctuation">;</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>JobHeader</code> is a job&#39;s metadata <em>without</em> its <code>JobDataMap</code>, so listing never loads or deserializes job data. <code>TriggerHeader</code> carries the state, fire times, priority, calendar name and execution group that previously cost an extra round trip per trigger each. When you do need the whole thing, follow a page with one bulk fetch:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token class-name">List<span class="token punctuation">&lt;</span>IJobDetail<span class="token punctuation">&gt;</span></span> details <span class="token operator">=</span> <span class="token keyword">await</span> scheduler<span class="token punctuation">.</span><span class="token function">GetJobDetails</span><span class="token punctuation">(</span>page<span class="token punctuation">.</span>Items<span class="token punctuation">.</span><span class="token function">Select</span><span class="token punctuation">(</span>x <span class="token operator">=&gt;</span> x<span class="token punctuation">.</span>Key<span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">ToList</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>Counting is a query that reads no rows:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token comment">// Before</span></span>
<span class="line"><span class="token class-name"><span class="token keyword">int</span></span> total <span class="token operator">=</span> <span class="token keyword">await</span> jobStore<span class="token punctuation">.</span><span class="token function">GetNumberOfTriggers</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line"><span class="token comment">// After</span></span>
<span class="line"><span class="token class-name">PagedResult<span class="token punctuation">&lt;</span>TriggerHeader<span class="token punctuation">&gt;</span></span> count <span class="token operator">=</span> <span class="token keyword">await</span> scheduler<span class="token punctuation">.</span><span class="token function">QueryTriggers</span><span class="token punctuation">(</span></span>
<span class="line">    <span class="token keyword">new</span> <span class="token constructor-invocation class-name">TriggerQuery</span> <span class="token punctuation">{</span> Take <span class="token operator">=</span> <span class="token number">0</span><span class="token punctuation">,</span> IncludeTotalCount <span class="token operator">=</span> <span class="token boolean">true</span> <span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"><span class="token class-name"><span class="token keyword">int</span></span> total <span class="token operator">=</span> count<span class="token punctuation">.</span>TotalCount<span class="token operator">!</span><span class="token punctuation">.</span>Value<span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Unlike the old counting members, this counts what a filter selects rather than a whole table — how many triggers are in the error state, for instance:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token class-name">PagedResult<span class="token punctuation">&lt;</span>TriggerHeader<span class="token punctuation">&gt;</span></span> failed <span class="token operator">=</span> <span class="token keyword">await</span> scheduler<span class="token punctuation">.</span><span class="token function">QueryTriggers</span><span class="token punctuation">(</span></span>
<span class="line">    <span class="token keyword">new</span> <span class="token constructor-invocation class-name">TriggerQuery</span> <span class="token punctuation">{</span> State <span class="token operator">=</span> TriggerState<span class="token punctuation">.</span>Error<span class="token punctuation">,</span> Take <span class="token operator">=</span> <span class="token number">0</span><span class="token punctuation">,</span> IncludeTotalCount <span class="token operator">=</span> <span class="token boolean">true</span> <span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">Console<span class="token punctuation">.</span><span class="token function">WriteLine</span><span class="token punctuation">(</span><span class="token interpolation-string"><span class="token string">$&quot;</span><span class="token interpolation"><span class="token punctuation">{</span><span class="token expression language-csharp">failed<span class="token punctuation">.</span>TotalCount</span><span class="token punctuation">}</span></span><span class="token string"> triggers need attention&quot;</span></span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>TriggerQuery</code> also filters on <code>Job</code>, <code>CalendarName</code> and <code>Group</code>; the filters combine with AND, and a null <code>Group</code> matches every group.</p><h3 id="behavior-worth-knowing" tabindex="-1"><a class="header-anchor" href="#behavior-worth-knowing"><span>Behavior worth knowing</span></a></h3><ul><li><strong>Ordering is group first, then name</strong>, and every page uses the same ordering, so paging is consistent. <code>RAMJobStore</code> compares ordinal. The ADO job store sorts in the database, so both the group order and the order within a group follow the <strong>server&#39;s collation</strong> — for most collations that differs from ordinal only in case and accent handling, but it is the database&#39;s decision, not Quartz&#39;s. Sort the page yourself if you need one specific culture&#39;s ordering.</li><li><strong>A null matcher now throws.</strong> <code>scheduler.GetJobKeys(null)</code> and <code>GetTriggerKeys(null)</code> raise <code>ArgumentNullException</code> instead of silently narrowing the listing to the <code>DEFAULT</code> group.</li><li><strong>The extension methods enumerate everything.</strong> They preserve the old semantics — and the old cost. Use the query member with <code>Skip</code>/<code>Take</code> wherever the result can be large.</li><li><strong><code>JobGroup.Paused</code> is real on both stores now.</strong> On 3.x the ADO store had nowhere to record a paused job group, so <code>IsJobGroupPaused</code> answered <code>false</code> for every group and the pause was lost on restart. 4.x stores the group names in <code>QRTZ_PAUSED_JOB_GRPS</code>, so a paused job group survives a restart, reaches the other nodes of a cluster, and is listed by <code>QueryJobGroups(new JobGroupQuery { Paused = true })</code>. <strong>That table is new, which is what makes the 4.0 migration mandatory even for a database that took every optional 3.x migration</strong> — see <a href="#database-schema-migration">Database Schema Migration</a>.</li><li><strong>A group can be paused while it holds no jobs</strong>, and <code>Paused = true</code> reports it. The unfiltered listing does not: it enumerates the groups jobs are in, and an empty group is not one of them. Trigger groups have always behaved this way; job groups now match. <code>PauseJobGroups(GroupMatcher&lt;JobKey&gt;.GroupEquals(g))</code> therefore answers <code>[g]</code> on the ADO store where 3.x answered <code>[]</code> for a group with no jobs.</li><li><strong>The recorded pause imposes itself on jobs added to the group afterwards</strong>, on both stores. Pausing a job group pauses the triggers of the jobs in it at that moment <em>and</em> records the group, and a trigger stored later for a job in a recorded group is born <code>PAUSED</code> — which is what <code>PauseJobGroups</code> has always promised and what only <code>RAMJobStore</code> used to do. See <a href="#the-two-job-stores-answer-the-same-way">The two job stores answer the same way</a> for the one refinement that is still in-memory only.</li><li><strong>Two indexes were added</strong> to support the ordered scans — see <a href="#database-schema-migration">Database Schema Migration</a>.</li></ul><h3 id="if-you-implement-idriverdelegate-the-listing-members" tabindex="-1"><a class="header-anchor" href="#if-you-implement-idriverdelegate-the-listing-members"><span>If you implement <code>IDriverDelegate</code>: the listing members</span></a></h3><p>Beyond the two batched-misfire members above, the query work adds, removes and consolidates a fair amount. New members to implement:</p><table><thead><tr><th>Member</th><th>Purpose</th></tr></thead><tbody><tr><td><code>SelectJobHeaders</code>, <code>SelectTriggerHeaders</code></td><td>One page of projected job/trigger listing rows</td></tr><tr><td><code>SelectJobGroups(conn, JobGroupQuery, ct)</code>, <code>SelectTriggerGroups(conn, TriggerGroupQuery, ct)</code></td><td>One page of groups, with pause state</td></tr><tr><td><code>SelectCalendarNames</code></td><td>One page of calendar names</td></tr><tr><td><code>SelectJobDetails</code>, <code>SelectTriggers</code></td><td>Bulk fetch by key set</td></tr><tr><td><code>InsertPausedJobGroup</code>, <code>DeletePausedJobGroup</code>, <code>IsJobGroupPaused</code></td><td>Read and write <code>QRTZ_PAUSED_JOB_GRPS</code>, mirroring the three <code>…PausedTriggerGroup</code> members</td></tr></tbody></table><p>Deleted, having had no caller: <code>SelectMisfiredTriggers</code>, both <code>HasMisfiredTriggersInState</code> overloads, <code>SelectMisfiredTriggersInGroupInState</code>, <code>IsExistingTriggerGroup</code>, <code>SelectJobExecutionCount</code>, <code>SelectTriggerForFireTime</code>, <code>SelectNumJobs</code>, <code>SelectNumTriggers</code>, <code>SelectNumCalendars</code>, <code>SelectCalendars</code>, <code>SelectPausedTriggerGroups</code>, <code>SelectJobGroups(conn, ct)</code> and <code>DeleteAllPausedTriggerGroups</code>. The <code>GetSelectNextMisfiredTriggersInStateToAcquireSql</code> hook went with them, so a dialect delegate that overrode it should delete that override.</p><p>Renamed to say what they return — a custom delegate author reads these names as the spec, and three of them said <code>Names</code> or whole entities where they return keys, while <code>SelectTriggerGroups</code> was one name overloaded across two unrelated result shapes:</p><table><thead><tr><th>3.x</th><th>4.x</th></tr></thead><tbody><tr><td><code>SelectTriggerNamesForJob</code> → <code>List&lt;TriggerKey&gt;</code></td><td><code>SelectTriggerKeysForJob</code></td></tr><tr><td><code>SelectJobsInGroup</code> → <code>List&lt;JobKey&gt;</code></td><td><code>SelectJobKeysInGroup</code></td></tr><tr><td><code>SelectTriggersInGroup</code> → <code>List&lt;TriggerKey&gt;</code></td><td><code>SelectTriggerKeysInGroup</code></td></tr><tr><td><code>SelectTriggerGroups(conn, GroupMatcher, ct)</code> → <code>List&lt;string&gt;</code></td><td><code>SelectTriggerGroupNames</code> — the paged <code>SelectTriggerGroups(conn, TriggerGroupQuery, ct)</code> now owns the name alone</td></tr></tbody></table><p>Consolidated into records rather than overload families:</p><table><thead><tr><th>3.x</th><th>4.x</th></tr></thead><tbody><tr><td><code>SelectFiredTriggerRecords</code>, <code>SelectFiredTriggerRecordsByJob</code>, <code>SelectInstancesFiredTriggerRecords</code></td><td><code>SelectFiredTriggerRecords(conn, FiredTriggerQuery, ct)</code></td></tr><tr><td>four <code>DeleteFiredTriggers</code> overloads</td><td><code>DeleteFiredTriggers(conn, FiredTriggerQuery, ct)</code></td></tr><tr><td>two <code>SelectTriggerToAcquire</code> overloads</td><td><code>SelectTriggersToAcquire(conn, TriggerAcquisitionCriteria, ct)</code></td></tr><tr><td>two <code>SelectJobForTrigger</code> overloads</td><td>one, with a required <code>bool loadJobType</code></td></tr><tr><td><code>DeletePausedTriggerGroup(conn, string, ct)</code></td><td>the <code>GroupMatcher&lt;TriggerKey&gt;</code> overload</td></tr></tbody></table><p><code>FiredTriggerQuery</code> carries an optional <code>Trigger</code>, <code>Job</code> and <code>InstanceId</code> combined with AND — all null selects or deletes every fired trigger. <code>TriggerAcquisitionCriteria</code> carries <code>NoLaterThan</code>, <code>NoEarlierThan</code>, <code>MaxCount</code>, <code>ExecutionLimits</code> and <code>LiveNodeCutoff</code>, and is the extension point for future acquisition filtering: another way of narrowing what a node picks up is another optional property, not another overload. <code>LiveNodeCutoff</code> is a <code>required DateTimeOffset</code> like its two time siblings — it was briefly a raw <code>UtcTicks</code> <code>long</code> that defaulted to zero, which silently meant &quot;every node counts as dead&quot;; the tick conversion lives in the parameter binder now.</p><p>Subclassing <code>StdAdoDelegate</code> gets you all of it. A database whose row-limiting syntax is not the ANSI <code>OFFSET … FETCH NEXT</code> should override the paging seam — <strong><code>ApplyPaging(sql, takeLimited)</code></strong> appends the clause and <strong><code>AddPagingParameters(cmd, skip, take, takeLimited)</code></strong> binds it (<code>skip</code> and <code>take</code> are <code>int</code>, matching the query objects they serve). Override both together when your clause names the two parameters in the other order, because providers that bind positionally take parameters in the order the statement mentions them. <code>MySQLDelegate</code> and <code>SQLiteDelegate</code> do exactly this for <code>LIMIT … OFFSET</code>. The two parameter names the pair has to agree on are <code>AdoConstants.ParameterPageSkip</code> and <code>AdoConstants.ParameterPageTake</code> — new in 4.0, and public because a delegate outside Quartz has to spell them.</p><p>A dialect that reads more than <code>%</code> and <code>_</code> as a <code>LIKE</code> wildcard says so in <strong><code>AdditionalLikeWildcards</code></strong>, which is <code>&quot;&quot;</code> on <code>StdAdoDelegate</code> and <code>&quot;[&quot;</code> on <code>SqlServerDelegate</code>: T-SQL reads <code>[</code> as the start of a character class, so <code>?nameContains=[a-z]</code> matched by class on SQL Server and Sybase and literally everywhere else. It cannot be escaped for everyone — the standard says an escape character must be followed by a wildcard or itself, and PostgreSQL enforces that. <code>EscapeSqlLikeWildcards</code> gained an overload taking the extra characters; the one-argument form still escapes the standard three.</p><p>The value-conversion pairs on <code>IDbAccessor</code> are no longer all overridable on <code>StdAdoDelegate</code>: the boolean pair (<code>GetDbBooleanValue</code> / <code>GetBooleanFromDbValue</code>) stays virtual, because Oracle genuinely stores booleans differently, but the date/time and time-span conversions are frozen. UTC ticks and whole milliseconds are part of the schema contract — the preferred-node liveness SQL does raw tick arithmetic against <code>LAST_CHECKIN_TIME</code> and <code>CHECKIN_INTERVAL</code>, so a delegate that changed the storage format would silently break cluster failover for pinned triggers (3.x only logged a warning when it detected such an override; 4.0 removes the half-open door). A delegate for a database that stores <code>DATETIME</code> natively must implement <code>IDriverDelegate</code> directly and own its SQL.</p><p>Every <em>statement-issuing</em> member is a seam, though, and one of them was not. <strong><code>UpdateTriggerStateFromOtherStateWithNextFireTime</code> is <code>virtual</code></strong> — it was the only <code>IDriverDelegate</code> member <code>StdAdoDelegate</code> implemented without it, which was an oversight rather than a decision. It is the lock-free acquisition path&#39;s claim on a trigger, so it is exactly the statement a dialect is likeliest to want to reshape, and the store reaches the delegate through the interface, which means a subclass that could only hide it with <code>new</code> would never have run. Purely additive: nothing that compiled before compiles differently.</p>`,51),e(`p`,null,[l[334]||=e(`code`,null,`StdAdoDelegate.SchemaResourceName`,-1),l[335]||=r(` is not new, but the how-to now documents it: overriding that `,-1),l[336]||=e(`code`,null,`protected virtual string?`,-1),l[337]||=r(` with the manifest resource name of an embedded script is the whole of what a delegate has to do to support `,-1),l[338]||=e(`code`,null,`SchemaProvisioning.CreateIfMissing`,-1),l[339]||=r(`. All six shipped dialects override it. See `,-1),c(g,{to:`/documentation/quartz-4.x/how-tos/dialect-delegate.html`},{default:i(()=>[...l[333]||=[r(`A Driver Delegate for a New Database`,-1)]]),_:1}),l[340]||=r(`.`,-1)]),l[888]||=t(`<p><code>StdAdoDelegate</code> also exposes <strong><code>protected string SchedulerName { get; }</code></strong>, beside the <code>protected IDbProvider DbProvider { get; }</code> it already had. Nearly every statement it sends is scoped by SCHED_NAME, so a delegate writing a statement of its own needs the same value; it used to have to override <code>Initialize</code> and keep a second copy of it from <code>DriverDelegateContext</code>. Purely additive — a delegate that already captures its own copy goes on working.</p><p>Finally, <code>ITriggerPersistenceDelegate</code> gained a batch <code>LoadExtendedTriggerProperties</code> taking several trigger keys. It is a <strong>default interface method</strong> that loops the single-key overload, so a third-party trigger persistence delegate needs no change; override it only to turn a batch into one round trip.</p><h2 id="reading-has-two-altitudes-on-purpose" tabindex="-1"><a class="header-anchor" href="#reading-has-two-altitudes-on-purpose"><span>Reading has two altitudes on purpose</span></a></h2><p><code>IScheduler</code>&#39;s <code>Query*</code> members and <code>SchedulerQueryExtensions</code>&#39; <code>Get*</code> conveniences look like the same API twice. They are not, and neither is going anywhere. A <code>Query*</code> member takes a record — a filter, a page, an optional total count — and answers with headers rich enough to render a listing. A <code>Get*</code> convenience answers the question that needs none of that, in one line, with the bare keys or names the caller asked for. The query members cannot be as short; the conveniences cannot page. Keeping both is the ruling, so that neither is re-audited as duplication.</p><p>What that ruling does <em>not</em> justify is a shorthand that saves only the <code>new</code>. Four of the five that existed did exactly that, and they are gone:</p><table><thead><tr><th>4.0 preview</th><th>4.0</th></tr></thead><tbody><tr><td><code>QueryJobs()</code></td><td><code>QueryJobs(new JobQuery())</code></td></tr><tr><td><code>QueryTriggers()</code></td><td><code>QueryTriggers(new TriggerQuery())</code></td></tr><tr><td><code>QueryFireInstances()</code></td><td><code>QueryFireInstances(new FireInstanceQuery())</code></td></tr><tr><td><code>QueryFireInstancesOfJob(jobKey)</code></td><td><code>QueryFireInstances(new FireInstanceQuery { Job = jobKey })</code></td></tr><tr><td><code>QueryTriggersInError()</code></td><td>unchanged — it is a <strong>preset</strong>, not a synonym</td></tr></tbody></table><p>The line is whether the shorthand knows something. <code>QueryTriggersInError()</code> knows the filter, and &quot;which triggers are broken?&quot; is a question an operator asks; <code>QueryJobs()</code> was a call with an argument left out, and the argument is the point of the query API. The preset pages exactly as the member does: <code>PagedQuery.DefaultTake</code> items, with <code>HasMore</code> reporting the rest.</p><h3 id="resetting-the-triggers-that-failed-is-one-call-each-way" tabindex="-1"><a class="header-anchor" href="#resetting-the-triggers-that-failed-is-one-call-each-way"><span>Resetting the triggers that failed is one call each way</span></a></h3><p><code>ResetTriggerFromErrorState(TriggerKey)</code> and <code>ResetTriggersFromErrorState(IReadOnlyCollection&lt;TriggerKey&gt;)</code> were already there, and <code>TriggerQuery.State</code> could already select the failed ones. What was missing was a way to say &quot;the ones in this group&quot;:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token comment">// Before</span></span>
<span class="line"><span class="token class-name">PagedResult<span class="token punctuation">&lt;</span>TriggerHeader<span class="token punctuation">&gt;</span></span> failed <span class="token operator">=</span> <span class="token keyword">await</span> scheduler<span class="token punctuation">.</span><span class="token function">QueryTriggers</span><span class="token punctuation">(</span><span class="token keyword">new</span> <span class="token constructor-invocation class-name">TriggerQuery</span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    Group <span class="token operator">=</span> GroupMatcher<span class="token operator">&lt;</span>TriggerKey<span class="token operator">&gt;</span><span class="token punctuation">.</span><span class="token function">GroupEquals</span><span class="token punctuation">(</span><span class="token string">&quot;imports&quot;</span><span class="token punctuation">)</span><span class="token punctuation">,</span></span>
<span class="line">    State <span class="token operator">=</span> TriggerState<span class="token punctuation">.</span>Error<span class="token punctuation">,</span></span>
<span class="line">    Take <span class="token operator">=</span> <span class="token keyword">int</span><span class="token punctuation">.</span>MaxValue</span>
<span class="line"><span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"><span class="token keyword">await</span> scheduler<span class="token punctuation">.</span><span class="token function">ResetTriggersFromErrorState</span><span class="token punctuation">(</span>failed<span class="token punctuation">.</span>Items<span class="token punctuation">.</span><span class="token function">Select</span><span class="token punctuation">(</span>x <span class="token operator">=&gt;</span> x<span class="token punctuation">.</span>Key<span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">ToList</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line"><span class="token comment">// After</span></span>
<span class="line"><span class="token keyword">await</span> scheduler<span class="token punctuation">.</span><span class="token function">ResetTriggersFromErrorState</span><span class="token punctuation">(</span>GroupMatcher<span class="token operator">&lt;</span>TriggerKey<span class="token operator">&gt;</span><span class="token punctuation">.</span><span class="token function">GroupEquals</span><span class="token punctuation">(</span><span class="token string">&quot;imports&quot;</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>It is that pair of calls, not a new store operation: two round trips, not one transaction, and a trigger that enters the error state between them is left for the next call. What resetting a trigger <em>does</em> is unchanged. Passing <code>null</code> throws rather than quietly meaning every group.</p><p>It is a member of <code>IScheduler</code> rather than an extension — see <a href="#resetting-a-group-from-the-error-state-is-an-ischeduler-member">Resetting a group from the error state is an <code>IScheduler</code> member</a>, which is also where the one call site that has to change is described.</p><h3 id="exists-has-a-calendar-overload" tabindex="-1"><a class="header-anchor" href="#exists-has-a-calendar-overload"><span><code>Exists</code> has a calendar overload</span></a></h3><p><code>IScheduler.Exists</code> and <code>IJobStore.Exists</code> took a <code>JobKey</code> or a <code>TriggerKey</code>. Asking the same question about a calendar meant loading it:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> bool exists = await scheduler.GetCalendar(&quot;holidays&quot;) is not null;</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> bool exists = await scheduler.Exists(&quot;holidays&quot;);</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>The difference is not spelling. <code>GetCalendar</code> reads the stored blob and deserializes it — an <code>AnnualCalendar</code> holding a decade of excluded days, materialized to be thrown away. The overload asks the store for the name: <code>RAMJobStore</code> looks it up without cloning, and the ADO.NET store runs the existence statement that selects a constant rather than the calendar column. <strong>No new <code>IDriverDelegate</code> member</strong> — <code>CalendarExists</code> has been on that interface all along, used by <code>AddCalendar</code> and by the trigger update that validates a calendar reference; it simply had nothing above it.</p><p>Over HTTP it is <code>GET {ApiPath}/schedulers/{name}/calendars/{calendarName}/exists</code>, answering <code>{ &quot;exists&quot;: … }</code> like the job and trigger routes it mirrors.</p><h3 id="a-scheduler-can-be-required-rather-than-looked-up" tabindex="-1"><a class="header-anchor" href="#a-scheduler-can-be-required-rather-than-looked-up"><span>A scheduler can be required rather than looked up</span></a></h3><p><code>ISchedulerFactory.LookupScheduler</code> answers <code>null</code> for a name this container does not know, so every caller that treats absence as a bug wrote the same throw:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> IScheduler scheduler = await factory.LookupScheduler(&quot;reporting&quot;)</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line">     ?? throw new InvalidOperationException(&quot;No scheduler named &#39;reporting&#39;&quot;);</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> IScheduler scheduler = await factory.GetRequiredScheduler(&quot;reporting&quot;);</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>GetRequiredScheduler</code> throws <code>SchedulerNotFoundException</code>, which is a <code>SchedulerException</code> carrying the <code>SchedulerName</code> that was asked for. It is an extension method rather than a member of <code>ISchedulerFactory</code>, for two reasons: it is composition over <code>LookupScheduler</code> that no factory could implement better, and an extension also resolves on a concrete receiver such as <code>StandaloneSchedulerFactory</code>, where a default interface method would need a cast. <code>LookupScheduler</code> is unchanged and stays the right call when a missing scheduler is an answer rather than a failure.</p><h2 id="shapes-that-were-examined-and-kept" tabindex="-1"><a class="header-anchor" href="#shapes-that-were-examined-and-kept"><span>Shapes that were examined and kept</span></a></h2><p>The read-API pass that produced the sections above also looked at five spots that read as inconsistencies and are not. Nothing changed in any of them; they are written down so that the next reader does not re-open them.</p><ul><li><strong><code>ITrigger.GetTriggerBuilder()</code> is an interface member while <code>IJobDetail.GetJobBuilder()</code> is an extension.</strong> A detail&#39;s builder can be filled in from the detail&#39;s public state alone, so an extension writes it once for every implementation. A trigger&#39;s cannot: the builder has to be created against the trigger&#39;s own <code>TimeProvider</code>, so the rebuilt trigger computes its fire times from the same reading of &quot;now&quot; — and a trigger&#39;s clock is not on <code>ITrigger</code>, because nothing else has any business reading it.</li><li><strong><code>TriggerDetailsUpdate</code> spells the misfire instruction six ways.</strong> Five typed <code>WithMisfireInstruction</code> overloads, one per schedule family, and <code>WithMisfireInstructionCode(int)</code>. Each typed one is load-bearing: the same number means a different policy in each family, so naming the family is what lets the store reject an update aimed at a trigger of another one. The untyped one is the only way to set a code on a trigger <em>outside</em> the five built-in families — a custom <code>ITrigger</code> is in none of them, so every typed overload would be rejected for it.</li><li><strong><code>SchedulerContext</code> gets the read accessors and not <code>PutAsString</code>.</strong> Writing to a <code>JobDataMap</code> is not just storing a value: the map records that it was changed, which is what persists it after a <code>[PersistJobDataAfterExecution]</code> job runs, and its equality is part of deciding whether anything moved. An extension cannot participate in that, and a context has nothing to participate in — it is process state that no store writes back.</li><li><strong><code>FireInstanceQuery</code> prefixes its trigger filters and the other queries do not.</strong> Every query names a filter for what it selects on: <code>Group</code> and <code>Name</code> are the result&#39;s own identity, and a filter on something the result refers to carries that thing&#39;s name (<code>Job</code>, <code>CalendarName</code>, <code>SchedulerInstanceId</code>). A firing is identified by a fire instance id rather than by a key, so its trigger is a reference like any other — hence <code>TriggerGroup</code> and <code>TriggerName</code>.</li><li><strong><code>Matchers</code> and the per-type factories both build matchers.</strong> The split is described under <a href="#matchers-is-the-entry-point-combinators-are-extensions"><code>Matchers</code> is the entry point</a>: a factory on the type names its comparison, a root on <code>Matchers</code> takes the comparison as a value.</li></ul><h2 id="trigger-states-are-typed-on-the-driver-delegate" tabindex="-1"><a class="header-anchor" href="#trigger-states-are-typed-on-the-driver-delegate"><span>Trigger states are typed on the driver delegate</span></a></h2><p>Eighteen members of <code>IDriverDelegate</code> took a trigger state as a <code>string</code> whose only legal values were the <code>AdoConstants.State*</code> constants. A typo, a stale spelling, or a transposed <code>newState</code>/<code>oldState</code> pair compiled and then quietly matched no row. <code>StoredTriggerState</code> is now that type — and it lives in <strong><code>Quartz.Extensibility</code></strong>, not in the ADO namespace, because it is every store&#39;s vocabulary: the in-memory store keeps its triggers in the same enum (its private <code>InternalTriggerState</code> twin is gone), and a custom <code>IJobStore</code> uses it too. The precedence that turns a stored state plus &quot;is it executing&quot; into the <code>TriggerState</code> callers see is public beside it:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token class-name">TriggerState</span> reported <span class="token operator">=</span> TriggerStateResolver<span class="token punctuation">.</span><span class="token function">Resolve</span><span class="token punctuation">(</span>stored<span class="token punctuation">,</span> isExecuting<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p><code>Resolve</code> applies <code>None &gt; Error &gt; Paused &gt; Executing &gt; Blocked &gt; Complete &gt; Normal</code>. Every built-in store resolves through it, so a custom store that does the same cannot report a different state than the ADO store would for the same situation — which used to require re-deriving the precedence from an internal class&#39;s XML comment.</p>`,28),e(`p`,null,[l[342]||=e(`strong`,null,`Nothing changes in the database.`,-1),l[343]||=r(` The columns still hold the same strings; the conversion happens at the delegate boundary, and `,-1),l[344]||=e(`code`,null,`AdoConstants.State*`,-1),l[345]||=r(` stays public because the strings are the schema contract (the string mapping, `,-1),l[346]||=e(`code`,null,`StoredTriggerStates`,-1),l[347]||=r(`, stays in `,-1),l[348]||=e(`code`,null,`Quartz.Impl.AdoJobStore`,-1),l[349]||=r(` with them). A 4.0 scheduler reads and writes rows a 3.x one wrote, so the two can share a cluster for the length of a rolling upgrade — under conditions that are about calendars and execution limits rather than trigger states, and that `,-1),c(g,{to:`/documentation/quartz-4.x/operations.html#a-mixed-3-x-and-4-0-window`},{default:i(()=>[...l[341]||=[r(`Operating a Cluster`,-1)]]),_:1}),l[350]||=r(` sets out.`,-1)]),l[889]||=t(`<table><thead><tr><th><code>AdoConstants</code> constant</th><th>Stored value</th><th><code>StoredTriggerState</code> member</th></tr></thead><tbody><tr><td><code>StateWaiting</code></td><td><code>WAITING</code></td><td><code>StoredTriggerState.Waiting</code></td></tr><tr><td><code>StateAcquired</code></td><td><code>ACQUIRED</code></td><td><code>StoredTriggerState.Acquired</code></td></tr><tr><td><code>StateExecuting</code></td><td><code>EXECUTING</code></td><td><code>StoredTriggerState.Executing</code></td></tr><tr><td><code>StateComplete</code></td><td><code>COMPLETE</code></td><td><code>StoredTriggerState.Complete</code></td></tr><tr><td><code>StateBlocked</code></td><td><code>BLOCKED</code></td><td><code>StoredTriggerState.Blocked</code></td></tr><tr><td><code>StateError</code></td><td><code>ERROR</code></td><td><code>StoredTriggerState.Error</code></td></tr><tr><td><code>StatePaused</code></td><td><code>PAUSED</code></td><td><code>StoredTriggerState.Paused</code></td></tr><tr><td><code>StatePausedBlocked</code></td><td><code>PAUSED_BLOCKED</code></td><td><code>StoredTriggerState.PausedBlocked</code></td></tr><tr><td><code>StateDeleted</code></td><td><code>DELETED</code></td><td><code>StoredTriggerState.Deleted</code></td></tr></tbody></table><p>Both directions are public, because a custom delegate binds the string into its own statements, and both are plain static methods — one mapping, one call shape:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token class-name"><span class="token keyword">string</span></span> stored <span class="token operator">=</span> StoredTriggerStates<span class="token punctuation">.</span><span class="token function">ToStoredValue</span><span class="token punctuation">(</span>StoredTriggerState<span class="token punctuation">.</span>PausedBlocked<span class="token punctuation">)</span><span class="token punctuation">;</span>   <span class="token comment">// &quot;PAUSED_BLOCKED&quot;</span></span>
<span class="line"><span class="token class-name">StoredTriggerState</span> state <span class="token operator">=</span> StoredTriggerStates<span class="token punctuation">.</span><span class="token function">FromStoredValue</span><span class="token punctuation">(</span>stored<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p><code>FromStoredValue</code> is deliberately lenient in exactly the way the store always was: a value this version does not recognise — left by a third-party delegate, a migration, or a hand-repaired row — reads as <code>Waiting</code> (schedulable, reported as a normal trigger), and a <code>null</code> column value reads as <code>Deleted</code>, the sentinel a missing trigger already reported. <code>SelectTriggerState</code> returns <code>StoredTriggerState</code> for the same reason its result flows straight into <code>AddTrigger</code> and <code>UpdateTrigger</code>.</p><p>One behavior follows from that, and only for a row whose stored state no Quartz version writes: the mutation side now agrees with the listing side about it. A listing has always reported such a trigger as <code>Normal</code>, while <code>PauseTrigger</code> matched neither <code>WAITING</code> nor <code>ACQUIRED</code> and silently did nothing; now it pauses it. Storing such a trigger back writes <code>WAITING</code> rather than preserving the unrecognised value.</p><p><code>MisfiredTriggerUpdate.NewState</code> and <code>TriggerExecutionState.State</code> (and its constructor) carry the enum too. On <code>AdoJobStoreBase</code>, the protected members that pass a state through follow: <code>AddTrigger</code>, <code>UpdateMisfiredTrigger</code> and <code>CheckBlockedState</code>, the last of which now returns <code>ValueTask&lt;StoredTriggerState&gt;</code>.</p><h3 id="the-fromotherstates-members-take-a-collection" tabindex="-1"><a class="header-anchor" href="#the-fromotherstates-members-take-a-collection"><span>The <code>…FromOtherStates</code> members take a collection</span></a></h3><p>Three members hard-coded two or three old states. A caller that wanted one repeated a value; one that wanted four could not say so. The predicate is now generated for the length of the set, with duplicates folded away.</p><table><thead><tr><th>3.x / earlier 4.0 preview</th><th>4.0</th></tr></thead><tbody><tr><td><code>UpdateTriggerStatesFromOtherStates(conn, newState, oldState1, oldState2, ct)</code></td><td><code>(conn, StoredTriggerState newState, IReadOnlyCollection&lt;StoredTriggerState&gt; oldStates, ct)</code></td></tr><tr><td><code>UpdateTriggerStateFromOtherStates(conn, key, newState, oldState1, oldState2, oldState3, ct)</code></td><td><code>(conn, key, StoredTriggerState newState, IReadOnlyCollection&lt;StoredTriggerState&gt; oldStates, ct)</code></td></tr><tr><td><code>UpdateTriggerGroupStateFromOtherStates(conn, matcher, newState, oldState1, oldState2, oldState3, ct)</code></td><td><code>(conn, matcher, StoredTriggerState newState, IReadOnlyCollection&lt;StoredTriggerState&gt; oldStates, ct)</code></td></tr></tbody></table><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> await Delegate.UpdateTriggerStatesFromOtherStates(conn, AdoConstants.StateWaiting,</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line">     AdoConstants.StateAcquired, AdoConstants.StateBlocked, cancellationToken);</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> await Delegate.UpdateTriggerStatesFromOtherStates(conn, StoredTriggerState.Waiting,</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">     [StoredTriggerState.Acquired, StoredTriggerState.Blocked], cancellationToken);</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>An empty set throws <code>ArgumentException</code> rather than building a statement that matches nothing. The parameter is a plain collection rather than a <code>params</code> one because every async member of this codebase ends with its cancellation token, and a <code>params</code> parameter has to come last.</p><h3 id="one-term-for-a-scheduler-instance" tabindex="-1"><a class="header-anchor" href="#one-term-for-a-scheduler-instance"><span>One term for a scheduler instance</span></a></h3><p>The value these members carry has always been the scheduler <em>instance id</em> (<code>quartz.scheduler.instanceId</code>), not the scheduler name — <code>SchedulerStateRecord.SchedulerInstanceId</code> already said so, and the interface said <code>instanceId</code> while the implementation said <code>instanceName</code>.</p><table><thead><tr><th>3.x / earlier 4.0 preview</th><th>4.0</th></tr></thead><tbody><tr><td><code>FiredTriggerQuery.InstanceName</code></td><td><code>FiredTriggerQuery.InstanceId</code></td></tr><tr><td><code>InsertSchedulerState(conn, string instanceName, …)</code></td><td><code>instanceId</code></td></tr><tr><td><code>UpdateSchedulerState(conn, string instanceName, …)</code></td><td><code>instanceId</code></td></tr><tr><td><code>DeleteSchedulerState(conn, string instanceName, …)</code></td><td><code>instanceId</code></td></tr><tr><td><code>SelectSchedulerStateRecords(conn, string? instanceName, …)</code></td><td><code>instanceId</code></td></tr></tbody></table><p><strong>Column names are unchanged</strong>: <code>SCHED_NAME</code> still holds the scheduler name and <code>INSTANCE_NAME</code> the instance id. <code>TriggerPersistenceDelegateContext.SchedulerName</code> and <code>DriverDelegateContext.SchedulerName</code> keep that spelling — those two really are the scheduler name, and the second one was <code>DelegateInitializationArgs.InstanceName</code> — see <a href="#the-initialization-seams-are-context-records">The initialization seams are context records</a>.</p><h3 id="three-parameter-shapes-were-fixed" tabindex="-1"><a class="header-anchor" href="#three-parameter-shapes-were-fixed"><span>Three parameter shapes were fixed</span></a></h3><ul><li><p><strong><code>IsJobCurrentlyExecuting(conn, JobKey jobKey, ct)</code></strong> — it took <code>(string jobName, string jobGroup)</code>, the last member of the interface splitting a key into two transposable strings.</p></li><li><p><strong><code>SelectJobForTrigger(conn, key, loadHelper, bool loadJobType, ct)</code></strong> — <code>loadJobType</code> defaulted to <code>true</code> while sitting in front of the cancellation token, so a call passing a token positionally bound it to the wrong parameter. Both values are genuinely used, so the parameter stays; pass <code>loadJobType: true</code> for the previous default.</p></li><li><p><strong><code>UpdateTriggerPreferredNodeConditional(conn, key, PreferredNodeTransition transition, ct)</code></strong> — the compare-and-swap took <code>(node, auto, expectedNode, expectedAuto)</code>, four loose values in two pairs that are trivial to transpose and impossible for the compiler to check. The record names both sides and carries <a href="#the-preferred-node-is-a-value"><code>PreferredNode</code></a> values rather than the raw column pair:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token keyword">await</span> Delegate<span class="token punctuation">.</span><span class="token function">UpdateTriggerPreferredNodeConditional</span><span class="token punctuation">(</span>conn<span class="token punctuation">,</span> trigger<span class="token punctuation">.</span>Key<span class="token punctuation">,</span> <span class="token keyword">new</span> <span class="token constructor-invocation class-name">PreferredNodeTransition</span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    Expected <span class="token operator">=</span> PreferredNode<span class="token punctuation">.</span>Auto<span class="token punctuation">,</span></span>
<span class="line">    New <span class="token operator">=</span> PreferredNode<span class="token punctuation">.</span><span class="token function">For</span><span class="token punctuation">(</span>InstanceId<span class="token punctuation">)</span></span>
<span class="line"><span class="token punctuation">}</span><span class="token punctuation">,</span> cancellationToken<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></li></ul><h3 id="the-matcher-based-selects-stay-deliberately" tabindex="-1"><a class="header-anchor" href="#the-matcher-based-selects-stay-deliberately"><span>The matcher-based selects stay, deliberately</span></a></h3><p><code>SelectTriggerGroups(matcher)</code>, <code>SelectJobsInGroup</code>, <code>SelectTriggersInGroup</code> and <code>SelectTriggerNamesForJob</code> look like leftovers next to the paged <code>SelectJobHeaders</code> / <code>SelectTriggerHeaders</code> / <code>SelectTriggerGroups(query)</code> members, and they are not. They are not listings: they serve the pause/resume and removal mutation paths, which have to move every matching row under one lock and therefore must not be paged. Their doc comments now say so, and they are not going away.</p><h2 id="the-ado-net-store-is-a-store-not-a-base-class" tabindex="-1"><a class="header-anchor" href="#the-ado-net-store-is-a-store-not-a-base-class"><span>The ADO.NET store is a store, not a base class</span></a></h2><p><code>AdoJobStoreBase</code>, <code>LocalTransactionJobStore</code>, <code>ExternalTransactionJobStore</code> and <code>AdoJobStoreDependencies</code> are internal. They still do exactly what they did — <code>quartz.jobStore.type = Quartz.Impl.AdoJobStore.LocalTransactionJobStore, Quartz</code> resolves and constructs the same store, and so do the <code>JobStoreTX</code> and <code>JobStoreCMT</code> spellings the bridge translates — but the hierarchy is no longer something to derive from. That takes 162 members and roughly a hundred <code>protected</code> hooks out of the contract, which was the largest single thing in it and the least used.</p><p>The reason is that deriving from it was never the seam it looked like. Every <code>protected</code> member below the two abstract ones is the connection-taking twin of the public member above it — <code>AddJob(conn, …)</code> beside <code>AddJob(job, …)</code> — because the public one takes the lock and the twin does the work. Overriding one of those changes half of an operation.</p>`,22),e(`p`,null,[l[352]||=t(`<strong>The seam for a relational database Quartz does not ship a dialect for is, and always was, the driver delegate.</strong> <code>IDriverDelegate</code>, <code>StdAdoDelegate</code> and the six shipped dialects are unchanged and stay public, as do <code>ITriggerPersistenceDelegate</code>, <code>ILockHandler</code>, <code>IDbProvider</code>, <code>AdoConstants</code>, <code>ConnectionAndTransactionHolder</code> and every record they name — <code>TriggerAcquisitionCriteria</code>, <code>TriggerAcquireResult</code>, <code>MisfiredTriggerBatch</code>, <code>SchedulerStateRecord</code> and the rest are delegate contract rather than store internals, and were never candidates for this. See `,24),c(g,{to:`/documentation/quartz-4.x/how-tos/dialect-delegate.html`},{default:i(()=>[...l[351]||=[r(`A Driver Delegate for a New Database`,-1)]]),_:1}),l[353]||=r(`.`,-1)]),l[890]||=e(`p`,null,[e(`strong`,null,[r(`If you derived from `),e(`code`,null,`JobStoreSupport`),r(`, `),e(`code`,null,`JobStoreTX`),r(` or `),e(`code`,null,`JobStoreCMT`),r(` on 3.x`)]),r(`, there are four answers depending on what the override did:`)],-1),e(`table`,null,[l[366]||=e(`thead`,null,[e(`tr`,null,[e(`th`,null,`What the override did`),e(`th`,null,`What to do now`)])],-1),e(`tbody`,null,[l[364]||=e(`tr`,null,[e(`td`,null,[r(`Narrowed acquisition — `),e(`code`,null,`CreateAcquisitionCriteria`),r(`, `),e(`code`,null,`MaxCount`),r(`, `),e(`code`,null,`ExcludedJobTypeNames`)]),e(`td`,null,[r(`Derive from `),e(`code`,null,`DelegatingJobStore`),r(` and rewrite the request: `),e(`code`,null,`base.AcquireNextTriggers(request with { MaxCount = …, ExcludedJobTypeNames = … })`),r(`. `),e(`code`,null,`TriggerAcquisitionRequest`),r(` is a record and the store copies both fields straight into its delegate criteria, so the exclusion is still applied in SQL rather than after the read`)])],-1),e(`tr`,null,[l[359]||=e(`td`,null,`Added logging, metrics, tenant routing, fault injection`,-1),e(`td`,null,[l[355]||=e(`code`,null,`DelegatingJobStore`,-1),l[356]||=r(`, which is what it was always for — every member is `,-1),l[357]||=e(`code`,null,`virtual`,-1),l[358]||=r(`. See `,-1),c(g,{to:`/documentation/quartz-4.x/how-tos/custom-job-store.html`},{default:i(()=>[...l[354]||=[r(`A Job Store of Your Own`,-1)]]),_:1})])]),e(`tr`,null,[l[363]||=e(`td`,null,`Classified one more of a driver's failures as retryable`,-1),e(`td`,null,[l[361]||=e(`code`,null,`AdoJobStoreOptions.IsTransient`,-1),l[362]||=r(`, a predicate consulted before the built-in list. See `,-1),c(g,{to:`/documentation/quartz-4.x/operations.html#what-counts-as-transient`},{default:i(()=>[...l[360]||=[r(`What counts as transient`,-1)]]),_:1})])]),l[365]||=e(`tr`,null,[e(`td`,null,[r(`Changed `),e(`em`,null,`how a transaction is managed`)]),e(`td`,null,[r(`Implement `),e(`code`,null,`IJobStore`),r(`, which is public and stays public. If your transaction model is one the two shipped stores nearly fit, `),e(`a`,{href:`https://github.com/quartznet/quartznet/issues`,target:`_blank`,rel:`noopener noreferrer`},`open an issue`),r(` — that is a gap worth hearing about rather than working around`)])],-1)])]),l[891]||=t(`<p><code>RecoverMisfiredJobsResult</code> went with them; it was the return type of a <code>protected</code> method and nothing else.</p><h2 id="the-ado-net-job-stores-are-named-for-whose-transaction-they-use" tabindex="-1"><a class="header-anchor" href="#the-ado-net-job-stores-are-named-for-whose-transaction-they-use"><span>The ADO.NET job stores are named for whose transaction they use</span></a></h2><p><code>JobStoreTX</code> and <code>JobStoreCMT</code> were named after a Java EE distinction — &quot;TX&quot; versus &quot;container-managed transactions&quot; — that says nothing in .NET, and neither name says which one commits. They now say it:</p><table><thead><tr><th>3.x / earlier 4.0 preview</th><th>4.0</th></tr></thead><tbody><tr><td><code>Quartz.Impl.AdoJobStore.JobStoreTX</code></td><td><code>Quartz.Impl.AdoJobStore.LocalTransactionJobStore</code></td></tr><tr><td><code>Quartz.Impl.AdoJobStore.JobStoreCMT</code></td><td><code>Quartz.Impl.AdoJobStore.ExternalTransactionJobStore</code></td></tr></tbody></table><p><code>LocalTransactionJobStore</code> begins the transaction each operation runs in and commits or rolls it back itself; it stays the default and the one nearly everybody wants. <code>ExternalTransactionJobStore</code> runs inside a transaction somebody else owns and neither commits nor rolls back.</p><p>Their abstract base follows the same cleanup: <code>JobStoreSupport</code> is <strong><code>AdoJobStoreBase</code></strong>, so <code>LocalTransactionJobStore : AdoJobStoreBase</code> says what it is instead of carrying the Java/Spring <code>*Support</code> idiom the listener bases already shed. For the same reason <code>SimplePropertiesTriggerPersistenceDelegateSupport</code> — the base class for a custom trigger type&#39;s persistence delegate — is <strong><code>SimplePropertiesTriggerPersistenceDelegateBase</code></strong>. Both are abstract types: they are never spelled in configuration strings and never stored as <code>$type</code> values, so there is no fallback to need — a subclass updates the name in its base list and recompiles.</p><p><strong>Configuration naming either as a string keeps working.</strong> <code>quartz.jobStore.type</code> is the one type name almost every persistent configuration spells out, so both old names resolve through the same fallback as the <a href="#quartz-spi-and-quartz-simpl-were-renamed">renamed namespaces</a>, with a warning telling you what to write instead:</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text"><pre><code class="language-text"><span class="line"># both of these resolve, the first with a warning</span>
<span class="line">quartz.jobStore.type = Quartz.Impl.AdoJobStore.JobStoreTX, Quartz</span>
<span class="line">quartz.jobStore.type = Quartz.Impl.AdoJobStore.LocalTransactionJobStore, Quartz</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><strong>In code, the choice is a call rather than a type argument</strong>, because <a href="#the-ado-net-store-is-a-store-not-a-base-class">both stores are internal</a>: it is the member rather than the name that selects one. <code>UsePersistentStore()</code> gives you the local-transaction store, as it always did and as nearly every application wants; <code>UseAmbientTransactions()</code> on the store builder selects the other:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> q.UsePersistentStore&lt;JobStoreCMT&gt;(store =&gt; store.UseSqlServer(connectionString));</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> q.UsePersistentStore(store =&gt; store</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">     .UseSqlServer(connectionString)</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">     .UseAmbientTransactions());</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>UsePersistentStore&lt;T&gt;(configure)</code> is unchanged and stays the seam for a store of your <strong>own</strong> — it takes a type the container can construct, which the two shipped stores no longer are. A <code>typeof</code> or a subclass of either has no replacement; see the table above for what an override becomes.</p><h3 id="the-vocabulary-follows" tabindex="-1"><a class="header-anchor" href="#the-vocabulary-follows"><span>The vocabulary follows</span></a></h3><p>The &quot;non-managed TX&quot; phrasing went with the old names. The members it appeared in are protected on a type that is now internal, so this reaches nothing outside Quartz — it is here because the same words appear in a 3.x subclass being ported, and <a href="#the-ado-net-store-is-a-store-not-a-base-class">The ADO.NET store is a store, not a base class</a> says what such a subclass becomes:</p><table><thead><tr><th>3.x / earlier 4.0 preview</th><th>4.0</th></tr></thead><tbody><tr><td><code>GetNonManagedTXConnection</code></td><td><code>GetLocalTransactionConnection</code></td></tr><tr><td><code>ExecuteInNonManagedTXLock</code></td><td><code>ExecuteInLocalTransactionLock</code></td></tr><tr><td><code>RetryExecuteInNonManagedTXLock</code></td><td><code>RetryExecuteInLocalTransactionLock</code></td></tr></tbody></table><p><code>ExternalTransactionJobStore.OpenConnection</code> is <code>AdoJobStoreOptions.OpenConnection</code> now. The store property was the one piece of store configuration outside the options system — settable only by resolving <code>IJobStore</code> and downcasting, after the container had already built the store, with no guarantee the write landed before <code>Initialize</code> ran. It is an ordinary option beside the store&#39;s others, and the store that reads it is selected by <code>UseAmbientTransactions()</code>:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> ((ExternalTransactionJobStore) store).OpenConnection = true;</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> services.AddQuartz(q =&gt; q.UsePersistentStore(store =&gt; store</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">     .UseAmbientTransactions()</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">     .ConfigureStore(options =&gt; options.OpenConnection = true)));</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Both spellings of the key reach it: <code>Quartz:JobStore:OpenConnection</code> binds like every other <code>AdoJobStoreOptions</code> member, and the flat <code>quartz.jobStore.openConnection</code> a 3.x file carries is read too.</p><h2 id="nine-execute-lock-overloads-became-four-members" tabindex="-1"><a class="header-anchor" href="#nine-execute-lock-overloads-became-four-members"><span>Nine <code>Execute…Lock</code> overloads became four members</span></a></h2><div class="hint-container warning"><p class="hint-container-title">Warning</p><p><code>AdoJobStoreBase</code> is internal, so none of this is a seam any longer — see <a href="#the-ado-net-store-is-a-store-not-a-base-class">The ADO.NET store is a store, not a base class</a>. It is here because a 3.x <code>JobStoreSupport</code> subclass being read for the port is full of these calls.</p></div><p><code>AdoJobStoreBase</code> had nine overlapping ways to run a callback under a lock, three of which existed only to adapt a <code>void</code> callback and did so by returning <code>object</code> — a value that was always <code>null</code> and was never read. Optional parameters replace the ladder:</p><table><thead><tr><th>3.x / earlier 4.0 preview</th><th>4.0</th></tr></thead><tbody><tr><td><code>ExecuteWithoutLock&lt;T&gt;(txCallback, ct)</code></td><td>unchanged</td></tr><tr><td><code>abstract ExecuteInLock&lt;T&gt;(lockName, txCallback, ct)</code></td><td><code>ExecuteInLock&lt;T&gt;(SchedulerLock? lockKind, txCallback, ct)</code></td></tr><tr><td><code>ExecuteInLock(lockName, txCallback, ct)</code> → <code>ValueTask&lt;object&gt;</code></td><td><code>ExecuteInLock(SchedulerLock? lockKind, txCallback, ct)</code> → <code>ValueTask</code></td></tr><tr><td><code>ExecuteInNonManagedTXLock</code> ×4</td><td><code>ExecuteInLocalTransactionLock&lt;T&gt;(SchedulerLock? lockKind, txCallback, txValidator = null, requestorId = null, ct)</code> plus one <code>ValueTask</code>-returning convenience</td></tr><tr><td><code>RetryExecuteInNonManagedTXLock</code> ×2</td><td><code>RetryExecuteInLocalTransactionLock&lt;T&gt;(SchedulerLock? lockKind, txCallback, requestorId = null, ct)</code> plus one <code>ValueTask</code>-returning convenience</td></tr></tbody></table><p>A call that passed a cancellation token positionally after the validator now has to name it (<code>cancellationToken: cancellationToken</code>), because the parameters between them became optional. <code>RecoverJobs(CancellationToken)</code> returns <code>ValueTask</code> rather than <code>ValueTask&lt;bool&gt;</code> — the <code>bool</code> was the constant <code>true</code> that the old void adapter produced.</p><h2 id="locks-are-a-schedulerlock-not-a-string" tabindex="-1"><a class="header-anchor" href="#locks-are-a-schedulerlock-not-a-string"><span>Locks are a <code>SchedulerLock</code>, not a string</span></a></h2><p><code>ILockHandler</code> took the lock as a <code>string</code> whose only two legal values were <code>&quot;TRIGGER_ACCESS&quot;</code> and <code>&quot;STATE_ACCESS&quot;</code>; anything else threw at run time. <code>Quartz.Impl.AdoJobStore.SchedulerLock</code> is now that type, with members <code>TriggerAccess</code> and <code>StateAccess</code>.</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> ValueTask&lt;bool&gt; ObtainLock(Guid requestorId, ConnectionAndTransactionHolder? conn, string lockName, CancellationToken ct = default);</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"> ValueTask ReleaseLock(Guid requestorId, string lockName, CancellationToken ct = default);</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> ValueTask&lt;bool&gt; AcquireLock(Guid requestorId, ConnectionAndTransactionHolder? conn, SchedulerLock lockKind, CancellationToken ct = default);</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line"> ValueTask ReleaseLock(Guid requestorId, SchedulerLock lockKind, CancellationToken ct = default);</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>AdoJobStoreBase.LockTriggerAccess</code> and <code>LockStateAccess</code> are gone with the strings they held. <strong>Nothing changes in the database</strong>: the <code>LOCK_NAME</code> column still holds <code>TRIGGER_ACCESS</code> and <code>STATE_ACCESS</code>, the conversion happens where the row is written, and a 4.0 node contends for the same rows as a 3.x one. The same applies to <code>Quartz.Extensions.Redis</code>, whose keys keep their <code>…:TRIGGER_ACCESS</code> spelling.</p><p><code>DbLockHandler.ExecuteSql</code> still receives the stored name as a <code>string</code> — that parameter really is the value bound into the statement.</p><h2 id="the-job-store-configuration-is-read-only-and-no-longer-a-public-currency" tabindex="-1"><a class="header-anchor" href="#the-job-store-configuration-is-read-only-and-no-longer-a-public-currency"><span>The job store configuration is read-only, and no longer a public currency</span></a></h2><p>Twenty-odd <code>AdoJobStoreBase</code> properties duplicated <code>AdoJobStoreOptions</code> and <code>QuartzSchedulerOptions</code> with a public setter. Writing one after the store had started did nothing useful in most cases and quietly diverged from the options everything else reads — and reading store configuration through a downcast <code>IJobStore</code> was a second currency for options that are already injectable. They are get-only and no longer public: the mirrors a derived store legitimately reads while doing its work are <code>protected</code> (<code>AcquireTriggersWithinLock</code>, <code>CanUseProperties</code>, <code>ClusterCheckinMisfireThreshold</code>, <code>DataSource</code>, <code>DoubleCheckLockMisfireHandler</code>, <code>LockOnInsert</code>, <code>MaxMisfiresToHandleAtATime</code>, <code>MaxTransientRetries</code>, <code>ObjectSerializer</code>, <code>SchemaProvisioning</code>, <code>SelectWithLockSql</code>, <code>TablePrefix</code>, <code>TransientRetryInterval</code>, <code>TransactionIsolationLevel</code>, <code>UseDbLocks</code>), and the ones only the store&#39;s own cluster and misfire machinery reads are internal (<code>AcceptEnlistedTransactions</code>, <code>ClusterCheckinInterval</code>, <code>DbRetryInterval</code>, <code>InstanceId</code>, <code>InstanceName</code>, <code>UseBackgroundThreads</code>, <code>MisfireHandlerFrequency</code>, <code>RetryableActionErrorLogThreshold</code>). <code>Clustered</code>, <code>SupportsPersistence</code> and <code>EstimatedTimeToReleaseAndAcquireTrigger</code> stay public — they are <code>IJobStore</code> members, which is what still reaches you now that <a href="#the-ado-net-store-is-a-store-not-a-base-class">the store itself is internal</a>. The <code>protected</code> half of that division is a note on the port rather than a seam you can reach.</p><p><code>EstimatedTimeToReleaseAndAcquireTrigger</code> did change type, though, on <code>IJobStore</code> itself and so on every store: it was a <code>long</code> holding milliseconds and is a <code>TimeSpan</code>. The unit used to live in the member&#39;s documentation, which is where a wrong answer hides — the scheduler thread subtracts this from the time it is prepared to sleep, so a store that meant seconds simply idled wrong. A store returning <code>70</code> returns <code>TimeSpan.FromMilliseconds(70)</code>; the compiler catches the port, so the only way to get it wrong now is to reach for <code>TimeSpan.FromTicks</code>.</p><p>Code that read configuration off the store resolves <code>IOptions&lt;AdoJobStoreOptions&gt;</code> instead, and configuring goes where it always did in 4.0:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> var store = new JobStoreTX(...) { Clustered = true, MaxTransientRetries = 5 };</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> services.AddQuartz(q =&gt; q.UsePersistentStore(store =&gt; store.ConfigureStore(options =&gt;</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line"> {</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">     options.Clustered = true;</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">     options.MaxTransientRetries = 5;</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line"> })));</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>MisfireThreshold</code> keeps a <strong>public getter</strong> on <code>RAMJobStore</code>, and on the ADO.NET store for as long as that type was public — it is read on every misfire pass rather than only at startup, so a store that wraps one needs to see it — but its setter is <code>internal</code> like the rest. Set it on the options type: <code>UsePersistentStore(store =&gt; store.ConfigureStore(o =&gt; o.MisfireThreshold = …))</code>, or <code>UseInMemoryStore(o =&gt; o.MisfireThreshold = …)</code>.</p><p>Two properties that nothing read are gone rather than made read-only: <code>DriverDelegateType</code> (the delegate is injected, not loaded from a type name here) and <code>DontSetAutoCommitFalse</code> (never consulted). <code>AdoJobStoreOptions.DontSetAutoCommitFalse</code> went with the store property: no code path ever read it and no configuration key ever set it, so an application that set it was configuring nothing. <code>LastCheckin</code> is internal — cluster check-in bookkeeping a subclass has no business in — and <code>LogWarnIfNonZero</code> is gone: its callers raise source-generated events instead, at the level its name always claimed, as <a href="#every-message-carries-an-event-id">Every message carries an event id</a> describes. The <code>[TimeSpanParseRule]</code> attributes on these properties are gone too; they are read only when a component&#39;s settings arrive as strings, which for this store they no longer do.</p><h3 id="adojobstorebase-s-overridable-surface-is-a-decision-now" tabindex="-1"><a class="header-anchor" href="#adojobstorebase-s-overridable-surface-is-a-decision-now"><span><code>AdoJobStoreBase</code>&#39;s overridable surface is a decision now</span></a></h3><div class="hint-container warning"><p class="hint-container-title">Warning</p><p>This section describes an intermediate state. The curation below happened first; then the whole hierarchy went internal, so none of it is a seam any more — see <a href="#the-ado-net-store-is-a-store-not-a-base-class">The ADO.NET store is a store, not a base class</a> for what replaced each of these. It is kept because it explains what the store now does <em>not</em> offer, and why that was never much of a loss.</p></div><p>The base store had 56 <code>protected virtual</code> members — every internal step of every operation was an override point, which 4.0 would have frozen as a behavior contract by default. The seam was curated instead, down to what derived stores demonstrably used:</p><ul><li><strong>Lifecycle</strong> — <code>Initialize</code> and <code>Shutdown</code> (the two shipped stores override both).</li><li><strong>Connections and transactions</strong> — <code>GetConnection</code>, <code>GetLocalTransactionConnection</code> and <code>ExecuteInLock&lt;T&gt;</code> (both abstract), and <code>IsTransient</code> for provider-specific transient-error classification. This is the seam an ambient-transaction store builds on.</li><li><strong>Acquisition</strong> — <code>AcquireNextTriggers</code>, <code>GetFiredTriggerRecordId</code>, and <code>CreateAcquisitionCriteria</code>, the seam for acquisition filtering (issue #2238): it builds the <code>TriggerAcquisitionCriteria</code> the delegate is asked with, and a derived store narrows what its node picks up by returning <code>base.CreateAcquisitionCriteria(request) with { … }</code>.</li></ul><p>Everything else — the per-entity add/get/delete internals, the pause/resume walkers, the fire path, the cluster check-in and recovery passes, the connection cleanup helpers — is non-virtual. Those members were virtual because the Java port made everything virtual, not because subclassing them was supported; overriding one changed the store&#39;s internal call order in ways no test covered. The seven conn-taking <code>PauseTrigger</code>/<code>PauseTriggerGroup</code>/<code>PauseAll</code>/<code>ResumeTrigger</code>/<code>ResumeTriggerGroups</code>/<code>ResumeAll</code>/ <code>RecoverMisfiredJobs</code> overloads are <code>protected</code> for the same reason: nothing outside the store can obtain the <code>ConnectionAndTransactionHolder</code> they demand. The dialect seam — statement text, paging, parameter binding — was never here; it belongs to <code>StdAdoDelegate</code>.</p><h2 id="the-semaphores-are-lock-handlers" tabindex="-1"><a class="header-anchor" href="#the-semaphores-are-lock-handlers"><span>The semaphores are lock handlers</span></a></h2><p>A semaphore is a counted permit. This thing is a mutual-exclusion lock over a named row — one holder, re-entrant per connection, released on the same connection — so &quot;semaphore&quot; named one implementation strategy rather than the thing itself, and the name came from the Java port rather than from anything it does. Every type in the family says <code>LockHandler</code> now, which is what the builder method (<code>UseLockHandler</code>), the configuration key (<code>quartz.jobStore.lockHandler.type</code>) and the how-to have said all along:</p><table><thead><tr><th>3.x</th><th>4.0</th></tr></thead><tbody><tr><td><code>ISemaphore</code></td><td><code>ILockHandler</code></td></tr><tr><td><code>DBSemaphore</code></td><td><code>DbLockHandler</code></td></tr><tr><td><code>StdRowLockSemaphore</code></td><td><code>SelectForUpdateLockHandler</code></td></tr><tr><td><code>PostgreSQLRowLockSemaphore</code></td><td><code>PostgreSqlSelectForUpdateLockHandler</code></td></tr><tr><td><code>UpdateLockRowSemaphore</code></td><td><code>UpdateRowLockHandler</code></td></tr><tr><td><code>UpdateLockRowSemaphoreMOT</code></td><td><code>SqlServerMemoryOptimizedUpdateRowLockHandler</code></td></tr><tr><td><code>SimpleSemaphore</code></td><td><code>InProcessLockHandler</code> (internal)</td></tr><tr><td>—</td><td><code>SqliteLockHandler</code> (internal)</td></tr><tr><td><code>RedisSemaphore</code></td><td><code>RedisLockHandler</code></td></tr><tr><td>—</td><td><code>LockHandlerContext</code></td></tr><tr><td><code>ISemaphore.ObtainLock</code></td><td><code>ILockHandler.AcquireLock</code></td></tr></tbody></table><p><code>ReleaseLock</code> and <code>RequiresConnection</code> keep their names — the verb pairs with <code>AcquireLock</code>, and the property was never about permits.</p><p>A <code>quartz.jobStore.lockHandler.type</code> naming any of the old types still resolves, with a warning — which includes the intermediate spellings a 4.0 pre-release used, listed in <a href="#appendix-if-you-ran-a-4-0-pre-release">Appendix: if you ran a 4.0 pre-release</a>.</p><p>The four database lock handlers had also named the same concept three different ways in 3.x: <code>StdRowLockSemaphore</code> and <code>UpdateLockRowSemaphore</code> transposed the same two words for two strategies that diverge only under load, and <code>MOT</code> was an acronym expanded nowhere. They are named for the SQL they issue now, which is the only thing that distinguishes them.</p><ul><li><p>The public static SQL fields settled on one convention and became <code>protected const</code>, because nothing outside the class hierarchy that owns them ever read them and every interpolation hole in them is itself a constant: <code>SelectForUpdateLockHandler.SelectForLock</code> / <code>.InsertLock</code> keep their member names, and <code>UpdateLockRowSemaphore.SqlUpdateForLock</code> / <code>.SqlInsertLock</code> are <code>UpdateRowLockHandler.UpdateForLock</code> / <code>.InsertLock</code>.</p></li><li><p><code>DbLockHandler.Sql</code> is <code>LockSql</code>, saying which of the two statements it holds and pairing with <code>InsertSql</code>. Both are get-only and arrive through the constructor. They were <code>protected</code> settable, which let a subclass swap a statement after the table prefix had already been folded into it — the select and the insert backing the same lock could end up naming different tables. A subclass that needs its own insert statement passes it up:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line"> public MyRowLockSemaphore(string tablePrefix, string schedulerName, string? selectWithLockSql, IDbProvider dbProvider)</span>
<span class="line"></span></span><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line">     : base(tablePrefix, schedulerName, selectWithLockSql, dbProvider)</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"> {</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line">     InsertSql = MyInsertLock;</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"> }</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line">     : base(tablePrefix, schedulerName, selectWithLockSql, MyInsertLock, dbProvider)</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line"> {</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line"> }</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></li></ul><h2 id="a-lock-handler-is-told-which-scheduler-it-locks-for" tabindex="-1"><a class="header-anchor" href="#a-lock-handler-is-told-which-scheduler-it-locks-for"><span>A lock handler is told which scheduler it locks for</span></a></h2><p><code>ITablePrefixAware</code> is gone. A lock handler used to learn its scheduler&#39;s identity through that get/set property pair, which the store property-injected after construction — and a handler that never touches a SQL table (the Redis one, say) still had to carry a dead <code>TablePrefix</code> property in order to be told its own scheduler&#39;s name. <code>ILockHandler</code> has a single initialization seam now:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token keyword">public</span> <span class="token keyword">interface</span> <span class="token class-name">ILockHandler</span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    <span class="token return-type class-name"><span class="token keyword">void</span></span> <span class="token function">Initialize</span><span class="token punctuation">(</span><span class="token class-name">LockHandlerContext</span> context<span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">{</span></span>
<span class="line">    <span class="token punctuation">}</span></span>
<span class="line"></span>
<span class="line">    <span class="token comment">// AcquireLock (was ObtainLock) / ReleaseLock / RequiresConnection otherwise unchanged</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>LockHandlerContext</code> carries the identity a handler locks under — <code>SchedulerName</code>, <code>InstanceId</code> and <code>TablePrefix</code> — and the environment it locks in: <code>TimeProvider</code>, the clock a handler backs off on between attempts, and <code>CommandTimeout</code>, how long its statements may run. The job store calls <code>Initialize</code> once, before the handler is used, whether the store built the handler itself or the container or configuration supplied it. The default implementation does nothing, so a handler that does not key its locks by scheduler identity implements nothing. <code>DbLockHandler</code> overrides it to re-expand its statements with the table prefix and rebuild its accessor with the timeout; its <code>TablePrefix</code> and <code>SchedulerName</code> properties are read-only now, and it exposes the clock as a <code>protected TimeProvider</code>.</p><p>Both shipped row-lock handlers wait on that clock rather than on wall time, so their retry behaviour is finally testable — the backoff was a <code>Task.Delay(TimeSpan, CancellationToken)</code>, which meant the only way to watch a retry was to sit out the real second, and neither retry loop had a test. <code>UpdateRowLockHandler</code> picked up a <code>RetryPeriod</code> in the process: its backoff was a literal <code>TimeSpan.FromSeconds(1)</code>, so it ignored <code>quartz.jobStore.lockHandler.retryPeriod</code> while <code>SelectForUpdateLockHandler</code> beside it honoured the same key.</p><p><code>SelectForUpdateLockHandler.MaxRetry</code> and <code>.RetryPeriod</code> are <code>init</code>-only. How many times a contended lock is retried is fixed for the life of the handler, and a setter invited changing it mid-retry. The flat <code>quartz.jobStore.lockHandler.maxRetry</code> / <code>.retryPeriod</code> keys still reach them: the property bridge writes the handler by reflection, and an init accessor is an ordinary setter to reflection. Code that assigned them after construction moves the values into the object initializer:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> var lockHandler = new StdRowLockSemaphore(dbProvider);</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"> lockHandler.MaxRetry = 5;</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> var lockHandler = new SelectForUpdateLockHandler(dbProvider) { MaxRetry = 5 };</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>A custom handler that implemented <code>ITablePrefixAware</code> replaces the property pair with <code>Initialize</code> and reads the same values from the context:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> public sealed class ConsulSemaphore : ISemaphore, ITablePrefixAware</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> public sealed class ConsulLockHandler : ILockHandler</span>
<span class="line"></span></span><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line"> {</span>
<span class="line"></span></span><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line">     public string TablePrefix { get; set; } = &quot;&quot;;</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line">     public string? SchedName { get; set; }</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line">     public string? SchedulerName { get; private set; }</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line"></span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">     public void Initialize(LockHandlerContext context)</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">     {</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">         SchedulerName = context.SchedulerName;</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">     }</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>With the setters gone, the configuration keys that reached them by property injection went too: <code>quartz.jobStore.lockHandler.tablePrefix</code> and <code>quartz.jobStore.lockHandler.schedName</code> are rejected with advice naming this seam. <code>schedName</code> is the spelling a file carried over from 3.x contains, because the key was derived from the <code>ITablePrefixAware.SchedName</code> property it wrote; <code>quartz.jobStore.lockHandler.schedulerName</code>, which is what the 4.x property name suggests, is rejected with the same advice rather than reported as a typo. The store hands the handler its own <code>quartz.jobStore.tablePrefix</code> value, so the lock rows follow the store&#39;s table prefix without separate configuration.</p><h2 id="a-cancelled-acquire-throws-and-false-means-only-re-entry" tabindex="-1"><a class="header-anchor" href="#a-cancelled-acquire-throws-and-false-means-only-re-entry"><span>A cancelled acquire throws, and <code>false</code> means only re-entry</span></a></h2><p><code>ILockHandler.AcquireLock</code> returns a release obligation rather than a report of success: <code>true</code> says this call took the lock and its caller has to give it back, <code>false</code> says the calling context already held it and must not. That was the rule in 3.x too, and the store still reads the answer that way — what was never written down is that <strong>re-entry is the only thing <code>false</code> may mean</strong>, so an acquire that did not take the lock throws: <code>LockException</code> when it was refused, <code>OperationCanceledException</code> when the token fired.</p><p><code>SimpleSemaphore.ObtainLock</code> and <code>RedisSemaphore.ObtainLock</code> swallowed the cancellation and answered <code>false</code>, which the store reads as <em>already held, do not release</em> — it would then run the guarded operation with no lock and release nothing on the way out. Their 4.0 counterparts throw. Nothing ran unlocked in a shipped configuration, because both answer <code>false</code> to <code>RequiresConnection</code> and the connection open immediately after observes the same token, but that is statement ordering rather than a guarantee, and a handler of your own inherits the hole silently.</p><p>A custom handler that caught <code>OperationCanceledException</code> around its wait rethrows instead, and gives back anything it had taken before the exception escapes:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line"> try</span>
<span class="line"></span><span class="token prefix unchanged"> </span><span class="token line"> {</span>
<span class="line"></span><span class="token prefix unchanged"> </span><span class="token line">     await gate.WaitAsync(cancellationToken).ConfigureAwait(false);</span>
<span class="line"></span><span class="token prefix unchanged"> </span><span class="token line"> }</span>
<span class="line"></span><span class="token prefix unchanged"> </span><span class="token line"> catch (OperationCanceledException)</span>
<span class="line"></span><span class="token prefix unchanged"> </span><span class="token line"> {</span>
<span class="line"></span></span><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line">     return false;</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line">     throw;</span>
<span class="line"></span></span><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line"> }</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="a-lock-handler-is-told-when-to-close-what-it-opened" tabindex="-1"><a class="header-anchor" href="#a-lock-handler-is-told-when-to-close-what-it-opened"><span>A lock handler is told when to close what it opened</span></a></h2><p><code>ILockHandler</code> carries a second default interface member:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token return-type class-name">ValueTask</span> <span class="token function">Shutdown</span><span class="token punctuation">(</span><span class="token class-name">CancellationToken</span> cancellationToken <span class="token operator">=</span> <span class="token keyword">default</span><span class="token punctuation">)</span> <span class="token operator">=&gt;</span> <span class="token keyword">default</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>The job store calls it at the end of its own <code>Shutdown</code>, after the misfire handler, the cluster manager and the database provider have stopped, so a handler may assume no acquire is in flight by the time it runs. <strong>An existing handler need not implement it</strong> — the default body does nothing, which is right for a handler that holds nothing of its own, and that is every row-lock handler in the box. A handler that throws on the way down is logged, and the scheduler finishes shutting down.</p><p>It exists because <code>RedisLockHandler</code> opens a <code>ConnectionMultiplexer</code> on its first lock and nothing could ever close it: a host that built a scheduler, ran it and shut it down left a live Redis connection and its heartbeat behind, once per scheduler, for the life of the process (#3639). The two in-process handlers close their <code>SemaphoreSlim</code> gates through the same hook.</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token keyword">public</span> <span class="token keyword">sealed</span> <span class="token keyword">class</span> <span class="token class-name">ConsulLockHandler</span> <span class="token punctuation">:</span> <span class="token type-list"><span class="token class-name">ILockHandler</span></span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    <span class="token keyword">private</span> <span class="token keyword">readonly</span> <span class="token class-name">HttpClient</span> consul <span class="token operator">=</span> <span class="token keyword">new</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line">    <span class="token keyword">public</span> <span class="token return-type class-name">ValueTask</span> <span class="token function">Shutdown</span><span class="token punctuation">(</span><span class="token class-name">CancellationToken</span> cancellationToken <span class="token operator">=</span> <span class="token keyword">default</span><span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">{</span></span>
<span class="line">        consul<span class="token punctuation">.</span><span class="token function">Dispose</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">        <span class="token keyword">return</span> <span class="token keyword">default</span><span class="token punctuation">;</span></span>
<span class="line">    <span class="token punctuation">}</span></span>
<span class="line"></span>
<span class="line">    <span class="token comment">// AcquireLock / ReleaseLock / RequiresConnection as before</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="a-job-store-of-your-own-can-join-your-transaction" tabindex="-1"><a class="header-anchor" href="#a-job-store-of-your-own-can-join-your-transaction"><span>A job store of your own can join your transaction</span></a></h2>`,68),e(`div`,f,[l[370]||=e(`p`,{class:`hint-container-title`},`Warning`,-1),e(`p`,null,[l[368]||=t(`The <code>GetEnlistedConnection</code> half of this section describes an intermediate state, for the same reason <a href="#adojobstorebase-s-overridable-surface-is-a-decision-now"><code>AdoJobStoreBase</code>&#39;s overridable surface</a> does: it was opened up while the store hierarchy was still public, and then the hierarchy went internal. What survives is the second half — the public <code>ConnectionAndTransactionHolder</code> shape below, which is what a store written against <code>IJobStore</code> borrows a connection with. Enlisting from the application side is unaffected: <code>AcceptEnlistedTransactions</code>, <code>IScheduler.EnlistTransaction</code> and <code>EnlistConnection</code> are public and are what `,15),c(g,{to:`/documentation/quartz-4.x/tutorial/job-stores.html#joining-an-existing-transaction`},{default:i(()=>[...l[367]||=[r(`Joining an existing transaction`,-1)]]),_:1}),l[369]||=r(` describes.`,-1)])]),l[892]||=t(`<p><code>AdoJobStoreBase</code> was public and abstract at the time, but everything needed to honour an enlisted transaction was <code>private protected</code>, so a store outside this assembly could not take part in one — it could only open a connection of its own while the caller believed the scheduling was inside their transaction.</p><p><code>GetEnlistedConnection</code> was made <code>protected</code>. A <code>GetLocalTransactionConnection</code> override started with it, and got <code>null</code> back when enlisted transactions are not accepted or nothing is enlisted:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token keyword">protected</span> <span class="token keyword">override</span> <span class="token keyword">async</span> <span class="token return-type class-name">ValueTask<span class="token punctuation">&lt;</span>ConnectionAndTransactionHolder<span class="token punctuation">&gt;</span></span> <span class="token function">GetLocalTransactionConnection</span><span class="token punctuation">(</span></span>
<span class="line">    <span class="token class-name">CancellationToken</span> cancellationToken <span class="token operator">=</span> <span class="token keyword">default</span><span class="token punctuation">)</span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    <span class="token class-name">ConnectionAndTransactionHolder<span class="token punctuation">?</span></span> enlisted <span class="token operator">=</span> <span class="token keyword">await</span> <span class="token function">GetEnlistedConnection</span><span class="token punctuation">(</span>cancellationToken<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">    <span class="token keyword">if</span> <span class="token punctuation">(</span>enlisted <span class="token keyword">is</span> <span class="token keyword">not</span> <span class="token keyword">null</span><span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">{</span></span>
<span class="line">        <span class="token keyword">return</span> enlisted<span class="token punctuation">;</span></span>
<span class="line">    <span class="token punctuation">}</span></span>
<span class="line"></span>
<span class="line">    <span class="token keyword">return</span> <span class="token keyword">await</span> <span class="token function">GetConnection</span><span class="token punctuation">(</span>cancellationToken<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Everything that makes an enlistment safe to use happens inside it: the transaction is checked to be alive and still current, the provider is checked to match, the connection is opened if it is not, and it is booked out for the operation so two concurrent scheduler calls cannot share it. Cleaning the holder up through <code>CleanupConnection</code> hands the booking back.</p><p><code>ConnectionAndTransactionHolder</code> gained a matching public constructor, <code>(DbConnection connection, DbTransaction? transaction, bool ownsResources)</code>, and a public <code>OwnsResources</code>, for a store that borrows a connection from somewhere else entirely. Owning nothing means committing nothing — <code>Commit</code>, <code>Rollback</code>, <code>Close</code> and <code>Dispose</code> all return without touching a borrowed connection.</p><p><code>Commit(bool)</code> and <code>Rollback(bool)</code> are internal: when the unit of work commits is the job store&#39;s decision, and the ADO.NET store makes it. <code>Close</code> stays public, for the store that borrowed the connection and has to give it back.</p><h2 id="the-driver-delegate-speaks-in-records" tabindex="-1"><a class="header-anchor" href="#the-driver-delegate-speaks-in-records"><span>The driver delegate speaks in records</span></a></h2><p>The six types <code>IDriverDelegate</code> hands back or takes in were mutable classes with settable properties, loose <code>string</code> pairs where a key belongs, and — in one case — properties that were non-nullable but unassigned. They are records now, and say what they hold.</p><table><thead><tr><th>Type</th><th>What changed</th></tr></thead><tbody><tr><td><code>FiredTriggerRecord</code></td><td><code>sealed record</code>, <code>[Serializable]</code> dropped, <code>FireInstanceState</code> is a <code>StoredTriggerState</code></td></tr><tr><td><code>RecoverMisfiredJobsResult</code></td><td><code>sealed record</code>; the property is <code>EarliestNewTimeUtc</code>, matching its constructor argument</td></tr><tr><td><code>DelegateInitializationArgs</code></td><td>Renamed <code>DriverDelegateContext</code>; <code>sealed record</code> with <code>required</code> / <code>init</code> members, and <code>InstanceName</code> is <code>SchedulerName</code> — see <a href="#the-initialization-seams-are-context-records">The initialization seams are context records</a></td></tr><tr><td><code>TriggerAcquireResult</code></td><td>carries a <code>TriggerKey</code> instead of <code>TriggerName</code> + <code>TriggerGroup</code></td></tr><tr><td><code>TriggerStatus</code></td><td>replaced by <code>StoredTriggerHeader</code>, returned by <code>SelectTriggerHeader</code></td></tr><tr><td><code>SchedulerStateRecord</code></td><td><code>sealed record</code> with a positional constructor <code>(string SchedulerInstanceId, DateTimeOffset CheckinTimestamp, TimeSpan CheckinInterval)</code> and <code>init</code>-only members; <code>[Serializable]</code> dropped, and the three properties are no longer <code>virtual</code>. Construction moves from <code>new SchedulerStateRecord { … }</code> to the constructor — all three values are read from one row, so none of them is optional</td></tr></tbody></table><p><code>FiredTriggerRecord.FireInstanceState</code> was the last place raw <code>AdoConstants.State*</code> string comparisons survived after the delegate&#39;s states were typed, in stale-acquired recovery and in cluster recovery. Its always-populated members are <code>required</code> and non-nullable now — <code>FireInstanceId</code>, <code>FireInstanceState</code>, <code>TriggerKey</code> and <code>SchedulerInstanceId</code> — and <code>JobKey</code> stays nullable because an ACQUIRED row is written before the job has been loaded. <code>[Serializable]</code> went with the class: nothing has serialized this record since binary serialization was dropped.</p><p><code>TriggerStatus</code> was a mutable class with a settable key, a <code>string</code> state and a name that said nothing. <code>StoredTriggerHeader</code> is the storage-side counterpart of <code>TriggerHeader</code>:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> TriggerStatus? status = await Delegate.SelectTriggerStatus(conn, triggerKey, cancellationToken);</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"> bool blocked = AdoConstants.StatePausedBlocked == status.Status;</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> StoredTriggerHeader? status = await Delegate.SelectTriggerHeader(conn, triggerKey, cancellationToken);</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line"> bool blocked = status.State == StoredTriggerState.PausedBlocked;</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>It speaks <code>StoredTriggerState</code> rather than the reported <code>TriggerState</code>, because resuming a trigger has to tell <code>PausedBlocked</code> from <code>Paused</code> and the reported state does not.</p><p><code>FiredTriggerQuery</code> stays unpaged, deliberately, and now says so in its own doc comment: FIRED_TRIGGERS holds one row per firing in flight, and every caller is a maintenance pass — recovery, cluster failover, blocked state checks — that has to see the whole set. Handing one of those a page would leave the rest unrecovered.</p><h2 id="the-initialization-seams-are-context-records" tabindex="-1"><a class="header-anchor" href="#the-initialization-seams-are-context-records"><span>The initialization seams are context records</span></a></h2><p>The ADO.NET store has three things it initializes after construction, and each of them used to say so differently: a lock handler took a <code>LockHandlerContext</code>, a driver delegate took a bag called <code>DelegateInitializationArgs</code>, and a trigger persistence delegate took three loose positional arguments. All three take a context record now, and the records agree on what the scheduler&#39;s name is called:</p><table><thead><tr><th>Seam</th><th>4.0</th></tr></thead><tbody><tr><td><code>ILockHandler.Initialize</code></td><td><code>LockHandlerContext</code> — unchanged</td></tr><tr><td><code>IDriverDelegate.Initialize</code></td><td><code>DriverDelegateContext</code> (was <code>DelegateInitializationArgs</code>), whose <code>InstanceName</code> is <code>SchedulerName</code></td></tr><tr><td><code>ITriggerPersistenceDelegate.Initialize</code></td><td><code>TriggerPersistenceDelegateContext</code> (was <code>(string tablePrefix, string schedulerName, IDbAccessor dbAccessor)</code>)</td></tr></tbody></table><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> public void Initialize(string tablePrefix, string schedulerName, IDbAccessor dbAccessor)</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> public void Initialize(TriggerPersistenceDelegateContext context)</span>
<span class="line"></span></span><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line"> {</span>
<span class="line"></span></span><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line">     TablePrefix = tablePrefix;</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line">     SchedulerName = schedulerName;</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line">     DbAccessor = dbAccessor;</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line">     TablePrefix = context.TablePrefix;</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">     SchedulerName = context.SchedulerName;</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">     DbAccessor = context.DbAccessor;</span>
<span class="line"></span></span><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line"> }</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>DelegateInitializationArgs.InstanceName</code> held the scheduler name, which is the confusion <a href="#one-term-for-a-scheduler-instance">One term for a scheduler instance</a> settled everywhere else, so it is <code>SchedulerName</code> — matching <code>LockHandlerContext.SchedulerName</code> beside it. <code>InstanceId</code> keeps its name and its meaning: the node&#39;s identity within the cluster.</p><p>Neither of the two delegate seams has a default implementation, unlike <code>ILockHandler.Initialize</code>. A lock handler that ignores its context is a legitimate handler — one that does not key its locks by scheduler identity has nothing to read. A delegate that ignores its context has no table prefix, no provider and no accessor, so a do-nothing default would only move the failure from startup to the first statement.</p><p>Both stay two-phase rather than moving to constructor injection, because <code>InstanceId</code> may be <em>generated</em> rather than configured: the store is built before the id generator has run, so the value does not exist when the container constructs the delegate.</p><h2 id="validateschema-is-part-of-idriverdelegate" tabindex="-1"><a class="header-anchor" href="#validateschema-is-part-of-idriverdelegate"><span><code>ValidateSchema</code> is part of <code>IDriverDelegate</code></span></a></h2><p>Startup schema validation was a <code>StdAdoDelegate</code> method the job store reached by type test, so a delegate that was not a <code>StdAdoDelegate</code> silently skipped the check that <code>quartz.jobStore.performSchemaValidation</code> had asked for. It is an interface member now, and every delegate participates:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token return-type class-name">ValueTask<span class="token punctuation">&lt;</span><span class="token keyword">int</span><span class="token punctuation">&gt;</span></span> <span class="token function">ValidateSchema</span><span class="token punctuation">(</span><span class="token class-name">ConnectionAndTransactionHolder</span> conn<span class="token punctuation">,</span> <span class="token class-name">CancellationToken</span> cancellationToken <span class="token operator">=</span> <span class="token keyword">default</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p><code>CreateSchema</code> sits beside it, for the <code>SchemaProvisioning.CreateIfMissing</code> the flag it replaced could not express — see <a href="#performschemavalidation-became-schemaprovisioning-which-has-a-third-position"><code>PerformSchemaValidation</code> became <code>SchemaProvisioning</code></a>.</p><p>A delegate of your own that derives from <code>StdAdoDelegate</code> inherits the implementation and can extend it to cover tables of its own. One written against the interface directly has to implement it; returning <code>0</code> without checking anything restores the old skip.</p><p>Three more <code>StdAdoDelegate</code> methods moved onto the interface for the same reason — the store called them through a type test, so a delegate that was not a <code>StdAdoDelegate</code> quietly did not participate:</p><table><thead><tr><th>Member</th><th>What the store does with it</th></tr></thead><tbody><tr><td><code>RepinTriggersFromDeadNode(conn, oldPreferredNode, newPreferredNode, ct)</code></td><td>Steals a dead node&#39;s pinned triggers during cluster recovery</td></tr><tr><td><code>UpdateMisfireOriginalFireTime(conn, triggerKey, fireTime, ct)</code></td><td>Records the fire time a misfire displaced</td></tr><tr><td><code>ClearMisfireOriginalFireTime(conn, triggerKey, ct)</code></td><td>Clears it once the trigger fires normally again</td></tr></tbody></table><p>All three are <strong>abstract</strong> on <code>IDriverDelegate</code>, not default interface members, because a store that silently does nothing for them loses node affinity on failover or reports the wrong original fire time — failures that surface far from their cause. A delegate deriving from <code>StdAdoDelegate</code> inherits all three.</p><h2 id="the-optional-columns-are-required-so-the-probes-are-gone" tabindex="-1"><a class="header-anchor" href="#the-optional-columns-are-required-so-the-probes-are-gone"><span>The optional columns are required, so the probes are gone</span></a></h2><p>3.x asked the database at startup whether <code>MISFIRE_ORIG_FIRE_TIME</code>, <code>EXECUTION_GROUP</code> and <code>PREFERRED_NODE</code> were present, and switched the matching feature off when they were not. <strong>4.x requires all of them</strong>, so there is no question left to ask, and the members that asked it are gone from <code>StdAdoDelegate</code>:</p><table><thead><tr><th>Removed</th><th>What it did</th></tr></thead><tbody><tr><td><code>HasMisfireOriginalFireTimeColumn</code>, <code>HasExecutionGroupColumn</code>, <code>HasPreferredNodeColumn</code></td><td>Reported the result of the matching probe</td></tr><tr><td><code>SupportsMisfireOriginalFireTimeColumn</code>, <code>SupportsExecutionGroupColumn</code>, <code>SupportsPreferredNodeColumn</code></td><td>Ran the probe, swallowing the provider error that meant &quot;no such column&quot;</td></tr><tr><td><code>VerifyTriggersTableReachable</code></td><td>Told a genuinely missing column apart from a database that was momentarily unreachable, so a transient failure did not disable a feature for the life of the process</td></tr></tbody></table><p><strong>The upgrade path is the schema migration, and it is mandatory.</strong> Run <code>schema_30_to_40_upgrade_&lt;dialect&gt;.sql</code> from <a href="https://github.com/quartznet/quartznet/tree/main/database/migrations/4.0" target="_blank" rel="noopener noreferrer">database/migrations/4.0/</a> before pointing 4.x at a 3.x database — <a href="#database-schema-migration">Database Schema Migration</a> lists the columns and what each script does. Skipping it no longer degrades gracefully, and it does not fail tidily either: <a href="#validateschema-is-part-of-idriverdelegate"><code>ValidateSchema</code></a> only checks that each table can be queried, not which columns it has, so what you reach instead is a provider-level missing-column error from the first statement that names one. That happens almost immediately — loading a trigger projects all four columns, and storing one names three of them.</p><p>A delegate of your own has nothing to override here any more. If it derived from <code>StdAdoDelegate</code> and overrode a probe — to hard-code <code>true</code> against a schema you knew was current, say — delete the override; the base class no longer declares it.</p><h3 id="the-three-extra-acquisition-sql-hooks-went-with-them" tabindex="-1"><a class="header-anchor" href="#the-three-extra-acquisition-sql-hooks-went-with-them"><span>The three extra acquisition SQL hooks went with them</span></a></h3><p>Because the columns were optional, 3.x carried four versions of the trigger acquisition statement and chose between them at run time from the probe results: plain, with <code>EXECUTION_GROUP</code>, with <code>PREFERRED_NODE</code>, and with both. Each had its own <code>protected virtual</code> hook, and all six dialect delegates overrode all four to hang their own row-limiting clause off the end. Three of the four are gone from <code>StdAdoDelegate</code> and from <code>FirebirdDelegate</code>, <code>MySQLDelegate</code>, <code>OracleDelegate</code>, <code>PostgreSQLDelegate</code>, <code>SQLiteDelegate</code> and <code>SqlServerDelegate</code>:</p><ul><li><code>GetSelectNextTriggerToAcquireWithExecutionGroupSql(int maxCount)</code></li><li><code>GetSelectNextTriggerToAcquireWithPreferredNodeSql(int maxCount)</code></li><li><code>GetSelectNextTriggerToAcquireWithPreferredNodeOnlySql(int maxCount)</code></li></ul><p>With the columns required there is one statement — <code>StdAdoConstants.SqlSelectNextTriggerToAcquire</code>, which always projects <code>EXECUTION_GROUP</code> and always carries the preferred-node filter — and one hook, which is the one that was already there:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token keyword">protected</span> <span class="token keyword">virtual</span> <span class="token return-type class-name"><span class="token keyword">string</span></span> <span class="token function">GetSelectNextTriggerToAcquireSql</span><span class="token punctuation">(</span><span class="token class-name">TriggerAcquisitionSqlShape</span> shape<span class="token punctuation">)</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>Its parameter is new: <code>TriggerAcquisitionSqlShape</code> carries everything about an acquisition attempt that changes the statement&#39;s text — <code>MaxCount</code>, and <code>ExcludedJobTypeBucket</code> for the job-type exclusion clause — so the next acquisition dimension is a property on the record rather than another parameter here. <strong>A dialect delegate of your own should delete the three removed overrides</strong>; if the one it keeps exists only to hang a row limit off the statement, delete that too and say the limit once — see <a href="#row-limiting-is-a-slot-not-a-splice">Row limiting is a slot, not a splice</a>.</p><p>The node-affinity parameters the statement now always carries are bound for you by the protected <code>AddPreferredNodeParameters(cmd, liveNodeCutoff)</code>, so an override that rewrites the statement text still does not have to know their names or the order they are bound in. The cutoff parameter is a <code>DateTimeOffset</code>; the binder converts it to the stored tick value itself.</p><h3 id="row-limiting-is-a-slot-not-a-splice" tabindex="-1"><a class="header-anchor" href="#row-limiting-is-a-slot-not-a-splice"><span>Row limiting is a slot, not a splice</span></a></h3><p>Two statements come in a row-limited form — trigger acquisition and the misfire scan — and each of the six dialects used to produce its own by editing the finished SQL: <code>SqlServerDelegate</code> cut the first six characters off and pasted <code>SELECT TOP n</code> back on, <code>OracleDelegate</code> wrapped the whole thing in a <code>rownum</code> filter, and the rest appended <code>LIMIT n</code> or <code>ROWS n</code>. Correct, and correct only while the statement began with exactly <code>SELECT</code> — which nothing enforced — and repeated once per statement per dialect, each with its own <code>count == -1</code> test.</p><p>A dialect now says where its clause goes, once, and the statement is built with it already there:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token keyword">protected</span> <span class="token keyword">virtual</span> <span class="token return-type class-name">SqlRowLimit</span> <span class="token function">GetRowLimit</span><span class="token punctuation">(</span><span class="token class-name"><span class="token keyword">int</span></span> count<span class="token punctuation">)</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p><code>SqlRowLimit</code> is new and names the three placements: <code>InProjection(&quot;TOP&quot;, count)</code> for SQL Server, <code>AtStatementEnd(&quot;LIMIT&quot;, count)</code> for PostgreSQL, MySQL and SQLite, <code>AtStatementEnd(&quot;ROWS&quot;, count)</code> for Firebird, <code>InEnclosingSelect(&quot;rownum&quot;, count)</code> for Oracle, and <code>Unlimited</code> for a database that cannot limit rows, which is what <code>StdAdoDelegate</code> returns. <code>count</code> is never the <code>-1</code> sentinel: that is turned into <code>Unlimited</code> before the member is called.</p><p><code>GetSelectNextTriggerToAcquireSql(shape)</code> and <code>GetSelectMisfiredTriggersToRecoverSql(count)</code> are still <code>protected virtual</code>, and still the seam for a statement that needs something a row limit cannot say — <code>MySQLDelegate</code> overrides both for its <code>FORCE INDEX</code> hint. The five other dialect delegates no longer override either.</p><p><strong>If your dialect delegate overrides one of the two only to add a row limit, replace both overrides with a single <code>GetRowLimit</code>.</strong> Nothing breaks if you do not: the two hooks behave as they always did, and an override that appends its own clause still works. It just spells the same thing twice, against a statement it does not own.</p><p>One shipped statement changed as a result. <code>MySQLDelegate</code>&#39;s misfire scan carries its <code>FORCE INDEX</code> hint for an unlimited sweep as well now; it used to lose the hint in exactly that case, because the hint and the row limit shared one early return. Which index the sweep should read does not depend on how many rows it returns, and the unlimited sweep is the one that reads the most.</p><p><strong>Both of <code>MySQLDelegate</code>&#39;s misfire hints now name <code>IDX_…_T_NFT_ST</code>, the acquisition index, rather than <code>IDX_…_T_NFT_ST_MISFIRE</code>.</strong> The sweep and its counting peek filter <code>SCHED_NAME</code> and <code>TRIGGER_STATE</code> by equality, range on <code>NEXT_FIRE_TIME</code>, order by <code>NEXT_FIRE_TIME ASC, PRIORITY DESC</code> and keep <code>MISFIRE_INSTR</code> as a residual — the acquisition index&#39;s shape exactly, and nothing like <code>(SCHED_NAME, MISFIRE_INSTR, NEXT_FIRE_TIME, TRIGGER_STATE)</code>, whose second column is compared with <code>&lt;&gt;</code> and so stops the seek dead. Every other dialect&#39;s optimizer picks the acquisition index on its own; MySQL could not, because the hint told it not to. On a 100,000-trigger table with a 5,000-row backlog the sweep went from 15,561 buffer pool reads to 129, and the counting peek — which runs on every misfire-handler pass, backlog or not — from 4,594 to 8 (<a href="https://github.com/quartznet/quartznet/issues/3608" target="_blank" rel="noopener noreferrer">#3608</a>). The retargeted hint left <code>IDX_…_T_NFT_ST_MISFIRE</code> with no reader on any dialect, which is why the schema no longer creates it — <a href="#the-misfire-index-is-dropped-optional">The misfire index is dropped</a>.</p><h2 id="the-connection-manager-is-gone" tabindex="-1"><a class="header-anchor" href="#the-connection-manager-is-gone"><span>The connection manager is gone</span></a></h2><p><code>DBConnectionManager</code> was a name-keyed registry of <code>IDbProvider</code>s, reached through a process-wide <code>Instance</code>. The container is that registry now: a scheduler&#39;s provider is registered under the scheduler&#39;s own name as the service key, and the job store is handed the one it was built with. The manager was left holding a copy of what the container already knew, which nothing read back — and two schedulers whose data sources happened to share a name silently overwrote each other in it.</p><p><code>IDbConnectionManager</code>, <code>DbConnectionManager</code> and <code>DBConnectionManager.Instance</code> are removed, with no replacement type. The two things they were used for have separate answers.</p><p><strong>Registering a connection provider of your own</strong> is a store configuration call. <code>UseConnectionProvider</code> is where the <code>quartz.dataSource.&lt;name&gt;.connectionProvider.type</code> key lands too, so the configuration and the code spelling say the same thing:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> DBConnectionManager.Instance.AddConnectionProvider(&quot;default&quot;, new MyDbProvider());</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> services.AddQuartz(q =&gt; q.UsePersistentStore(store =&gt; store.UseConnectionProvider&lt;MyDbProvider&gt;()));</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> DBConnectionManager.Instance.AddConnectionProvider(&quot;default&quot;, myProvider);</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> services.AddQuartz(q =&gt; q.UsePersistentStore(store =&gt; store.UseConnectionProvider(_ =&gt; myProvider)));</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>The provider replaces whichever one the database choice registered, in either order — <code>UseSqlServer</code> before or after <code>UseConnectionProvider</code> gives the same result — and it belongs to this scheduler alone. It also names the data source for you, so a store configured this way needs no <code>UseDataSource</code> call.</p><p><strong>Reading a provider back out</strong> is container resolution. The default scheduler&#39;s provider is unkeyed and a named scheduler&#39;s is keyed by its name:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> var provider = DBConnectionManager.Instance.GetConnectionProvider(&quot;default&quot;);</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> var provider = serviceProvider.GetRequiredService&lt;IDbProvider&gt;();</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line"> var reporting = serviceProvider.GetRequiredKeyedService&lt;IDbProvider&gt;(&quot;reporting&quot;);</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The manager&#39;s other three members have no equivalent, because each was one call on the provider: <code>GetConnection(name)</code> is <code>provider.CreateConnection()</code>, <code>GetDbMetadata(name)</code> is <code>provider.Metadata</code>, and <code>Shutdown(name)</code> is <code>provider.Shutdown()</code>.</p><p><code>IDbProvider</code> itself is constructor-shaped: <code>Initialize()</code> is gone (every implementation resolved its driver description during construction, so the member was an empty ritual), and <code>ConnectionString</code> is get-only — it arrives through the implementation&#39;s constructor, and a provider is fully usable once constructed. A custom <code>IDbProvider</code> deletes its empty <code>Initialize</code> and its <code>ConnectionString</code> setter.</p><h2 id="the-isolation-level-is-an-isolation-level" tabindex="-1"><a class="header-anchor" href="#the-isolation-level-is-an-isolation-level"><span>The isolation level is an isolation level</span></a></h2><p><code>TxIsolationLevelSerializable</code> was a <code>bool</code>, so the only two things a configuration could say were &quot;serializable&quot; and &quot;whatever Quartz picks&quot;. <code>Snapshot</code> — which is what a SQL Server deployment usually wants, and which reads without blocking writers — was not expressible at all, and neither were <code>RepeatableRead</code> or a deliberate <code>ReadUncommitted</code>.</p><table><thead><tr><th>3.x / earlier 4.0 preview</th><th>4.0</th></tr></thead><tbody><tr><td><code>AdoJobStoreOptions.TxIsolationLevelSerializable = true</code></td><td><code>AdoJobStoreOptions.TransactionIsolationLevel = IsolationLevel.Serializable</code></td></tr><tr><td><code>TxIsolationLevelSerializable = false</code>, or unset</td><td>leave <code>TransactionIsolationLevel</code> unset</td></tr><tr><td>—</td><td><code>TransactionIsolationLevel = IsolationLevel.Snapshot</code>, and every other <code>System.Data.IsolationLevel</code></td></tr></tbody></table><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> store.Configure(options =&gt; options.TxIsolationLevelSerializable = true);</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> store.ConfigureStore(options =&gt; options.TransactionIsolationLevel = IsolationLevel.Serializable);</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p><code>quartz.jobStore.txIsolationLevelSerializable</code> is still read: <code>true</code> becomes <code>Serializable</code>, and <code>false</code> leaves the level unset rather than becoming an explicit <code>ReadCommitted</code> — the flag&#39;s <code>false</code> was the absence of a choice, and things downstream read that absence. The typed configuration binds the enum by name, so <code>&quot;Quartz:JobStore:TransactionIsolationLevel&quot;: &quot;Snapshot&quot;</code> works with no further plumbing.</p><p>Unset means <code>ReadCommitted</code>, which is Quartz&#39;s default rather than the provider&#39;s. Provider defaults differ — MySQL&#39;s is repeatable read — so inheriting them would have changed how the store behaves depending on which database it happened to be talking to.</p><p>Two behaviours are unchanged. SQLite is forced to <code>Serializable</code> whatever this says, because concurrent SQLite transactions at a lower level fail with &quot;database is locked&quot;. And the setting applies only to connections the job store opens itself: an operation running on a connection the application enlisted uses the level that transaction was begun at, which the store still warns about at startup.</p><h2 id="ramjobstore-is-sealed" tabindex="-1"><a class="header-anchor" href="#ramjobstore-is-sealed"><span><code>RAMJobStore</code> is sealed</span></a></h2><p>Every public method of the in-memory store was <code>virtual</code>, which invited overriding operations that hold its lock, mutate its indexes in a fixed order and raise listener notifications after releasing the lock — none of which is a documented contract, and all of which the overriding code has to preserve. A job store is written against <code>IJobStore</code>; the implementations Quartz ships are not base classes.</p><p><code>RAMJobStore</code> is <code>sealed</code>, its <code>virtual</code>s are gone, and <code>GetFiredTriggerRecordId</code> is private. A store that wants the in-memory behaviour plus something of its own wraps it, deriving from the new <code>Quartz.Impl.DelegatingJobStore</code>:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> public class SlowJobStore : RAMJobStore</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> public sealed class SlowJobStore : DelegatingJobStore</span>
<span class="line"></span></span><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line"> {</span>
<span class="line"></span><span class="token prefix unchanged"> </span><span class="token line">     public SlowJobStore(ILoggerFactory loggerFactory, ISchedulerSignaler signaler, TimeProvider timeProvider)</span>
<span class="line"></span></span><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line">         : base(loggerFactory, signaler, timeProvider)</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line">         : base(new RAMJobStore(loggerFactory, signaler, timeProvider))</span>
<span class="line"></span></span><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line">     {</span>
<span class="line"></span><span class="token prefix unchanged"> </span><span class="token line">     }</span>
<span class="line"></span></span></span>
<span class="line"><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line">     public override async ValueTask&lt;List&lt;IOperableTrigger&gt;&gt; AcquireNextTriggers(</span>
<span class="line"></span><span class="token prefix unchanged"> </span><span class="token line">         TriggerAcquisitionRequest request, CancellationToken cancellationToken = default)</span>
<span class="line"></span><span class="token prefix unchanged"> </span><span class="token line">     {</span>
<span class="line"></span><span class="token prefix unchanged"> </span><span class="token line">         List&lt;IOperableTrigger&gt; triggers = await base.AcquireNextTriggers(request, cancellationToken);</span>
<span class="line"></span><span class="token prefix unchanged"> </span><span class="token line">         await Task.Delay(10, cancellationToken);</span>
<span class="line"></span><span class="token prefix unchanged"> </span><span class="token line">         return triggers;</span>
<span class="line"></span><span class="token prefix unchanged"> </span><span class="token line">     }</span>
<span class="line"></span><span class="token prefix unchanged"> </span><span class="token line"> }</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>UsePersistentStore&lt;TStore&gt;()</code> and <code>quartz.jobStore.type</code> take the wrapping store exactly as they took the derived one; the <code>Quartz.Examples.AspNetCore</code> sample&#39;s <code>CustomJobStore</code> shows the whole shape.</p><p><code>MisfireThreshold</code> is readable here as it is on <code>AdoJobStoreBase</code> — it is read on every misfire pass rather than only at startup — but the setter is <code>internal</code>. Configure it with <code>UseInMemoryStore(o =&gt; o.MisfireThreshold = …)</code>.</p><h3 id="delegatingjobstore-decorates-a-store" tabindex="-1"><a class="header-anchor" href="#delegatingjobstore-decorates-a-store"><span><code>DelegatingJobStore</code> decorates a store</span></a></h3><p><code>IJobStore</code> has sixty-odd members, so hand-writing a forwarder for the sake of one of them is a lot of code that only has to be revisited every time the interface changes. <code>Quartz.Impl.DelegatingJobStore</code> is the store-level counterpart of <code>DelegatingScheduler</code>: a <code>public class</code> that takes the store to wrap as its constructor argument, forwards every <code>IJobStore</code> member to it, and declares each one <code>virtual</code> so a derived store overrides only what it changes. The wrapped store is available to derived types as <code>InnerJobStore</code>.</p><p>It is the supported way to add logging, metrics, tenant routing or fault injection to a store — including a sealed one such as <code>RAMJobStore</code>. A store that keeps scheduling data somewhere new implements <code>IJobStore</code> directly instead; nothing forces it through this base.</p><p><code>DelegatingScheduler</code> gained the same shape: every one of its members is <code>virtual</code> now, so a scheduler decorator overrides only what it changes instead of shadowing members with <code>new</code> — which compiled, and then silently failed to intercept calls made through <code>IScheduler</code>.</p><p>Both types also declare every <strong>default interface member</strong> of the contract they forward, which matters more than it sounds: a member a forwarder does not declare is answered by the interface&#39;s own body running <em>on the forwarder</em>. <code>ResetTriggersFromErrorState(GroupMatcher&lt;TriggerKey&gt;, CancellationToken)</code> was the one that got away during 4.0&#39;s development, and its default decomposes into a <code>QueryTriggers</code> and a second reset — so the inner scheduler was asked two questions instead of the one that was put to it. The right answer in the wrong shape, and a live bug the day a default lands whose decomposition is not equivalent. <code>DelegatingForwardingTest</code> sweeps both types with <code>GetInterfaceMap</code> and no exemption list. The C# rule underneath is worth knowing while writing a decorator: a default interface member is not inherited into a class&#39;s member set, so it is callable only through an interface-typed reference unless the class declares it.</p><h2 id="jobs-take-a-cancellationtoken" tabindex="-1"><a class="header-anchor" href="#jobs-take-a-cancellationtoken"><span>Jobs take a CancellationToken</span></a></h2><p><code>IJob.Execute</code> now takes the cancellation token as a parameter alongside the context:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> public async ValueTask Execute(IJobExecutionContext context)</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> public async ValueTask Execute(IJobExecutionContext context, CancellationToken cancellationToken = default)</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>It is the <em>same</em> token as <code>IJobExecutionContext.CancellationToken</code>, which still works — so a job body that already reads the token off the context needs no further change. The parameter exists because a token on a context property is easy to never notice, and a job that never notices it cannot be interrupted by <code>IScheduler.Interrupt</code> and will hold up shutdown until it finishes on its own.</p><p>The practical benefit is that the compiler now helps. With the token as a parameter, the built-in <code>CA2016</code> analyzer flags every <code>await</code> inside a job that fails to pass it on:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line"> public async ValueTask Execute(IJobExecutionContext context, CancellationToken cancellationToken = default)</span>
<span class="line"></span><span class="token prefix unchanged"> </span><span class="token line"> {</span>
<span class="line"></span></span><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line">     await httpClient.GetAsync(url);              // CA2016: forward the cancellationToken parameter</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line">     await httpClient.GetAsync(url, cancellationToken);</span>
<span class="line"></span></span><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line"> }</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>When adding this to Quartz&#39;s own jobs it found nine places that were silently ignoring interruption, including the sample that exists to demonstrate interruption.</p><h2 id="the-job-factory-hands-out-a-scope" tabindex="-1"><a class="header-anchor" href="#the-job-factory-hands-out-a-scope"><span>The job factory hands out a scope</span></a></h2><p><code>IJobFactory</code> is built around a <code>JobScope</code> rather than a bare <code>IJob</code>, and <code>NewJob</code> is now <code>CreateJob</code>:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> ValueTask&lt;IJob&gt; NewJob(TriggerFiredBundle bundle, IScheduler scheduler, CancellationToken cancellationToken = default);</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"> ValueTask ReturnJob(IJob job);</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> ValueTask&lt;JobScope&gt; CreateJob(TriggerFiredBundle bundle, IScheduler scheduler, CancellationToken cancellationToken = default);</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line"> ValueTask ReturnJob(JobScope scope, CancellationToken cancellationToken = default);</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>JobScope</code> is a readonly struct holding the job plus an opaque <code>State</code> object. If your factory allocates something in order to build a job — a DI scope, a connection, a tenant context — put it in <code>State</code> and you get it back in <code>ReturnJob</code> instead of having to hide it inside the job instance:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> protected override IJob InstantiateJob(TriggerFiredBundle bundle, IScheduler scheduler)</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"> {</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line">     var scope = serviceProvider.CreateScope();</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line">     return new MyWrapperJob(scope, scope.ServiceProvider.GetRequiredService&lt;MyJob&gt;());</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"> }</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> protected override ValueTask&lt;JobScope&gt; CreateJobInstance(</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">     TriggerFiredBundle bundle, IScheduler scheduler, CancellationToken cancellationToken = default)</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line"> {</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">     var scope = serviceProvider.CreateScope();</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">     var job = ActivatorUtilities.CreateInstance&lt;MyJob&gt;(scope.ServiceProvider);</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">     return new ValueTask&lt;JobScope&gt;(new JobScope(job, scope));</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line"> }</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><div class="hint-container warning"><p class="hint-container-title">Warning</p><p>Note that this example <em>activates</em> the job rather than resolving it from the scope. <code>SimpleJobFactory.ReturnJob</code> disposes the job and then the state, so if you resolve the job from the scope — <code>GetRequiredService&lt;MyJob&gt;()</code> — the scope disposes it too and your job&#39;s <code>Dispose</code> is called twice. Either activate it as above, or override <code>ReturnJob</code> to skip the job when the container owns it, which is what <code>MicrosoftDependencyInjectionJobFactory</code> does.</p></div><p>Keep <code>CreateJobInstance</code> non-<code>async</code> when its body is synchronous. An async state machine restores the caller&#39;s execution context when its synchronous part returns, which would discard any <code>AsyncLocal</code> you set while building the job — including anything <code>ConfigureScope</code> establishes for the job to read.</p><p>Because of that, <strong><code>IJobWrapper</code> has been removed</strong> and <code>MicrosoftDependencyInjectionJobFactory</code> no longer wraps your job. <code>IJobExecutionContext.JobInstance</code> and every listener now see the type you actually wrote.</p><p><code>PropertySettingJobFactory.InstantiateJob</code> — the hook derived factories override — was replaced by the asynchronous <code>CreateJobInstance</code> shown above. The old hook was synchronous even after <code>NewJob</code> became asynchronous, so a factory that needed to do real work had to override <code>NewJob</code> outright and reimplement the property setting.</p><p>There is one job factory interface again. 3.x carried a second, internal <code>IJobWithAsyncReturnFactory</code> beside <code>IJobFactory</code>, so that a factory returning its job asynchronously could be recognised without changing the released interface; the asynchronous shape is <code>IJobFactory</code>&#39;s own now, and the internal one is gone.</p><p><code>SimpleJobFactory</code>&#39;s <code>protected static Dispose(object?)</code> helper is <code>DisposeIfDisposable(object?, CancellationToken)</code>, which is what it has always done: it disposes the argument only when the argument is disposable.</p><h3 id="scheduler-context-entries-are-no-longer-injected-into-job-properties" tabindex="-1"><a class="header-anchor" href="#scheduler-context-entries-are-no-longer-injected-into-job-properties"><span>Scheduler context entries are no longer injected into job properties</span></a></h3><p>On 3.x, <code>PropertySettingJobFactory.BuildJobDataMap</code> merged the whole <code>SchedulerContext</code> underneath the job&#39;s and trigger&#39;s data on every fire, so a context entry whose key matched a job property was silently injected into the job. That stops: the map applied to the job is the trigger&#39;s data merged over the job&#39;s, and nothing else. <strong>This is a silent behavioral change</strong> — a job that declared a property fed from <code>scheduler.Context[&quot;ConnectionString&quot;]</code> (or a <code>quartz.context.key.*</code> property) keeps its default value and nothing throws. <code>MicrosoftDependencyInjectionJobFactory</code> derives from <code>PropertySettingJobFactory</code>, so this covers the default DI path too.</p><p>The replacements:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token comment">// read the context where it lives…</span></span>
<span class="line"><span class="token keyword">public</span> <span class="token return-type class-name">ValueTask</span> <span class="token function">Execute</span><span class="token punctuation">(</span><span class="token class-name">IJobExecutionContext</span> context<span class="token punctuation">,</span> <span class="token class-name">CancellationToken</span> cancellationToken <span class="token operator">=</span> <span class="token keyword">default</span><span class="token punctuation">)</span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    <span class="token class-name"><span class="token keyword">var</span></span> connectionString <span class="token operator">=</span> context<span class="token punctuation">.</span>Scheduler<span class="token punctuation">.</span>Context<span class="token punctuation">.</span><span class="token function">GetString</span><span class="token punctuation">(</span><span class="token string">&quot;ConnectionString&quot;</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">    <span class="token comment">// …</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span>
<span class="line"><span class="token comment">// …or opt back into merging by overriding the hook, which is handed the scheduler for this reason</span></span>
<span class="line"><span class="token keyword">public</span> <span class="token keyword">class</span> <span class="token class-name">ContextMergingJobFactory</span> <span class="token punctuation">:</span> <span class="token type-list"><span class="token class-name">MicrosoftDependencyInjectionJobFactory</span></span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    <span class="token keyword">protected</span> <span class="token keyword">override</span> <span class="token return-type class-name">JobDataMap</span> <span class="token function">BuildJobDataMap</span><span class="token punctuation">(</span><span class="token class-name">TriggerFiredBundle</span> bundle<span class="token punctuation">,</span> <span class="token class-name">IScheduler</span> scheduler<span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">{</span></span>
<span class="line">        <span class="token class-name"><span class="token keyword">var</span></span> map <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token constructor-invocation class-name">JobDataMap</span><span class="token punctuation">(</span><span class="token punctuation">(</span>IDictionary<span class="token operator">&lt;</span><span class="token keyword">string</span><span class="token punctuation">,</span> <span class="token keyword">object</span><span class="token punctuation">?</span><span class="token operator">&gt;</span><span class="token punctuation">)</span> scheduler<span class="token punctuation">.</span>Context<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">        <span class="token keyword">foreach</span> <span class="token punctuation">(</span><span class="token class-name"><span class="token keyword">var</span></span> pair <span class="token keyword">in</span> <span class="token keyword">base</span><span class="token punctuation">.</span><span class="token function">BuildJobDataMap</span><span class="token punctuation">(</span>bundle<span class="token punctuation">,</span> scheduler<span class="token punctuation">)</span><span class="token punctuation">)</span></span>
<span class="line">        <span class="token punctuation">{</span></span>
<span class="line">            map<span class="token punctuation">[</span>pair<span class="token punctuation">.</span>Key<span class="token punctuation">]</span> <span class="token operator">=</span> pair<span class="token punctuation">.</span>Value<span class="token punctuation">;</span></span>
<span class="line">        <span class="token punctuation">}</span></span>
<span class="line">        <span class="token keyword">return</span> map<span class="token punctuation">;</span></span>
<span class="line">    <span class="token punctuation">}</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The merge was also defective in ways the removal fixes: 3.x&#39;s DI integration seeded a service-provider entry into every scheduler context, which no job has a property for, so every container-hosted fire logged a property miss — and threw, with <code>PropertyMismatchBehavior.Throw</code>; and the factory enumerated the context while plugins could still be writing to it. 4.0 seeds no such entry either — see <a href="#the-container-is-not-in-the-scheduler-context">The container is not in the scheduler context</a>.</p><h3 id="one-setting-says-what-a-property-miss-does" tabindex="-1"><a class="header-anchor" href="#one-setting-says-what-a-property-miss-does"><span>One setting says what a property miss does</span></a></h3><p><code>PropertySettingJobFactory</code> had two independent booleans for one three-way decision, and their fourth combination was unreachable: <code>ThrowIfPropertyNotFound</code> threw before <code>WarnIfPropertyNotFound</code> could log, so setting both meant &quot;throw&quot;. They are one <code>PropertyMismatchBehavior</code> property now.</p><table><thead><tr><th>3.x</th><th>4.x</th></tr></thead><tbody><tr><td>both <code>false</code> (the default)</td><td><code>PropertyMismatchBehavior.Ignore</code> (the default)</td></tr><tr><td><code>WarnIfPropertyNotFound = true</code></td><td><code>PropertyMismatchBehavior.Warn</code></td></tr><tr><td><code>ThrowIfPropertyNotFound = true</code></td><td><code>PropertyMismatchBehavior.Throw</code></td></tr><tr><td>both <code>true</code></td><td><code>PropertyMismatchBehavior.Throw</code> — the warning was never reached</td></tr></tbody></table><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> var factory = new PropertySettingJobFactory { ThrowIfPropertyNotFound = true };</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> var factory = new PropertySettingJobFactory { PropertyMismatchBehavior = PropertyMismatchBehavior.Throw };</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p><code>PropertyMismatchBehavior</code> is in <code>Quartz.Impl</code>, beside the factory. Neither boolean ever had a <code>quartz.*</code> property key, so nothing in the legacy configuration bridge changes.</p><h3 id="the-factory-is-set-where-the-scheduler-is-built" tabindex="-1"><a class="header-anchor" href="#the-factory-is-set-where-the-scheduler-is-built"><span>The factory is set where the scheduler is built</span></a></h3><p><code>IScheduler.JobFactory</code> was a setter-only property, and it is gone from <code>IScheduler</code>, <code>StdScheduler</code>, <code>DelegatingScheduler</code> and <code>HttpScheduler</code>. A job factory is part of how a scheduler is built, not something to swap underneath a running one — and on <code>HttpScheduler</code> the setter only ever threw, since the jobs run in another process.</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> scheduler.JobFactory = new MyJobFactory();</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> services.AddQuartz(q =&gt; q.UseJobFactory(new MyJobFactory()));</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p><code>UseJobFactory(IJobFactory)</code> is new on <code>IQuartzBuilder</code> — the generic <code>UseJobFactory&lt;T&gt;()</code> overload has always been there, but an already-constructed factory had nowhere to go:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token comment">// standalone</span></span>
<span class="line"><span class="token class-name">IScheduler</span> scheduler <span class="token operator">=</span> <span class="token keyword">await</span> QuartzSchedulerBuilder</span>
<span class="line">    <span class="token punctuation">.</span><span class="token function">Create</span><span class="token punctuation">(</span>q <span class="token operator">=&gt;</span> q<span class="token punctuation">.</span><span class="token function">UseJobFactory</span><span class="token punctuation">(</span><span class="token keyword">new</span> <span class="token constructor-invocation class-name">MyJobFactory</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">.</span><span class="token function">BuildScheduler</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>There is no by-hand path left: <code>QuartzScheduler</code> is internal, so the job factory is always configured through the builder or the container.</p><h4 id="when-the-factory-s-dependency-does-not-exist-yet" tabindex="-1"><a class="header-anchor" href="#when-the-factory-s-dependency-does-not-exist-yet"><span>When the factory&#39;s dependency does not exist yet</span></a></h4><p>Setting <code>IScheduler.JobFactory</code> late was how 3.x code handled a factory that needed something the application only has after startup — a connected bus, a tenant catalogue, an elected leader. Configuring the factory at build time does not mean <em>resolving</em> its dependency at build time. Two shapes cover it:</p><p><strong>Resolve per firing.</strong> <code>UseJobFactory&lt;T&gt;()</code> constructs the factory from the scheduler&#39;s own service provider, so a factory that takes <code>IServiceProvider</code> can resolve whatever it needs inside <code>CreateJob</code>, which runs once per firing rather than once per scheduler:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line">services<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">AddSingleton</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>LateBound<span class="token punctuation">&lt;</span>IMessageBus<span class="token punctuation">&gt;</span><span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">services<span class="token punctuation">.</span><span class="token function">AddQuartz</span><span class="token punctuation">(</span>q <span class="token operator">=&gt;</span> q<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">UseJobFactory</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>BusAwareJobFactory<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p><strong>Hand it a holder.</strong> When the dependency is not in the container at all — because whatever produces it also runs at startup — register a holder the factory can read through, and fill it when the value exists. That is the late-bound-holder pattern, and it is what the standalone <code>QuartzSchedulerBuilder</code> needs too, since it builds its own container and there is nothing to add to afterwards:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token doc-comment comment">/// <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>summary</span><span class="token punctuation">&gt;</span></span></span></span>
<span class="line"><span class="token doc-comment comment">/// Holds something the container cannot supply yet, so that a component built at startup can be</span></span>
<span class="line"><span class="token doc-comment comment">/// given the handle now and read the value later.</span></span>
<span class="line"><span class="token doc-comment comment">/// <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>summary</span><span class="token punctuation">&gt;</span></span></span></span>
<span class="line"><span class="token keyword">public</span> <span class="token keyword">sealed</span> <span class="token keyword">class</span> <span class="token class-name">LateBound<span class="token punctuation">&lt;</span>T<span class="token punctuation">&gt;</span></span> <span class="token keyword">where</span> <span class="token class-name">T</span> <span class="token punctuation">:</span> <span class="token type-list"><span class="token keyword">class</span></span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    <span class="token keyword">private</span> <span class="token class-name">T<span class="token punctuation">?</span></span> <span class="token keyword">value</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line">    <span class="token keyword">public</span> <span class="token return-type class-name">T</span> Value <span class="token operator">=&gt;</span> <span class="token keyword">value</span> <span class="token operator">??</span> <span class="token keyword">throw</span> <span class="token keyword">new</span> <span class="token constructor-invocation class-name">InvalidOperationException</span><span class="token punctuation">(</span><span class="token interpolation-string"><span class="token string">$&quot;</span><span class="token interpolation"><span class="token punctuation">{</span><span class="token expression language-csharp"><span class="token keyword">typeof</span><span class="token punctuation">(</span><span class="token type-expression class-name">T</span><span class="token punctuation">)</span><span class="token punctuation">.</span>Name</span><span class="token punctuation">}</span></span><span class="token string"> is not available yet.&quot;</span></span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line">    <span class="token keyword">public</span> <span class="token return-type class-name"><span class="token keyword">void</span></span> <span class="token function">Set</span><span class="token punctuation">(</span><span class="token class-name">T</span> instance<span class="token punctuation">)</span> <span class="token operator">=&gt;</span> <span class="token keyword">value</span> <span class="token operator">=</span> instance<span class="token punctuation">;</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span>
<span class="line"><span class="token keyword">public</span> <span class="token keyword">sealed</span> <span class="token keyword">class</span> <span class="token class-name">BusAwareJobFactory</span><span class="token punctuation">(</span><span class="token class-name">IServiceProvider</span> provider<span class="token punctuation">,</span> <span class="token class-name">LateBound<span class="token punctuation">&lt;</span>IMessageBus<span class="token punctuation">&gt;</span></span> bus<span class="token punctuation">)</span> <span class="token punctuation">:</span> IJobFactory</span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    <span class="token keyword">public</span> <span class="token return-type class-name">ValueTask<span class="token punctuation">&lt;</span>JobScope<span class="token punctuation">&gt;</span></span> <span class="token function">CreateJob</span><span class="token punctuation">(</span></span>
<span class="line">        <span class="token class-name">TriggerFiredBundle</span> bundle<span class="token punctuation">,</span></span>
<span class="line">        <span class="token class-name">IScheduler</span> scheduler<span class="token punctuation">,</span></span>
<span class="line">        <span class="token class-name">CancellationToken</span> cancellationToken <span class="token operator">=</span> <span class="token keyword">default</span><span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">{</span></span>
<span class="line">        <span class="token class-name">IServiceScope</span> scope <span class="token operator">=</span> provider<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">GetRequiredService</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>IServiceScopeFactory<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">CreateScope</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line">        <span class="token comment">// Read per firing rather than captured per scheduler, which is what makes a dependency that</span></span>
<span class="line">        <span class="token comment">// only exists once the bus has connected reachable from a factory built long before it.</span></span>
<span class="line">        <span class="token class-name">IJob</span> job <span class="token operator">=</span> <span class="token punctuation">(</span>IJob<span class="token punctuation">)</span> ActivatorUtilities<span class="token punctuation">.</span><span class="token function">CreateInstance</span><span class="token punctuation">(</span></span>
<span class="line">            scope<span class="token punctuation">.</span>ServiceProvider<span class="token punctuation">,</span></span>
<span class="line">            bundle<span class="token punctuation">.</span>JobDetail<span class="token punctuation">.</span>JobType<span class="token punctuation">.</span>Type<span class="token punctuation">,</span></span>
<span class="line">            bus<span class="token punctuation">.</span>Value<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line">        <span class="token keyword">return</span> <span class="token keyword">new</span> <span class="token constructor-invocation class-name">ValueTask<span class="token punctuation">&lt;</span>JobScope<span class="token punctuation">&gt;</span></span><span class="token punctuation">(</span><span class="token keyword">new</span> <span class="token constructor-invocation class-name">JobScope</span><span class="token punctuation">(</span>job<span class="token punctuation">,</span> scope<span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">    <span class="token punctuation">}</span></span>
<span class="line"></span>
<span class="line">    <span class="token keyword">public</span> <span class="token return-type class-name">ValueTask</span> <span class="token function">ReturnJob</span><span class="token punctuation">(</span><span class="token class-name">JobScope</span> scope<span class="token punctuation">,</span> <span class="token class-name">CancellationToken</span> cancellationToken <span class="token operator">=</span> <span class="token keyword">default</span><span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">{</span></span>
<span class="line">        <span class="token punctuation">(</span>scope<span class="token punctuation">.</span>State <span class="token keyword">as</span> <span class="token class-name">IServiceScope</span><span class="token punctuation">)</span><span class="token punctuation">?.</span><span class="token function">Dispose</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">        <span class="token keyword">return</span> <span class="token keyword">default</span><span class="token punctuation">;</span></span>
<span class="line">    <span class="token punctuation">}</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Keep <code>CreateJob</code> synchronous. An <code>async</code> method restores the caller&#39;s <code>ExecutionContext</code> when it resumes, which discards any <code>AsyncLocal&lt;T&gt;</code> the factory set while building the job — the job then runs without the ambient state the factory established.</p><h2 id="trigger-fire-times-are-properties" tabindex="-1"><a class="header-anchor" href="#trigger-fire-times-are-properties"><span>Trigger fire times are properties</span></a></h2><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> DateTimeOffset? next = trigger.GetNextFireTimeUtc();</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> DateTimeOffset? next = trigger.NextFireTimeUtc;</span>
<span class="line"></span></span></span>
<span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> operableTrigger.SetNextFireTimeUtc(value);</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> operableTrigger.NextFireTimeUtc = value;</span>
<span class="line"></span></span></span>
<span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> if (trigger.GetMayFireAgain()) { … }</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> if (trigger.MayFireAgain) { … }</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The three <code>Get</code> methods are gone — they spent a while as <code>[Obsolete]</code> forwarders on both <code>ITrigger</code> and <code>TriggerBase</code> and have now been removed — so fix the call by deleting <code>Get</code> and <code>()</code>. The <code>Set</code> methods likewise have no stand-in, because a method and a property setter cannot share a name.</p><p>The whole set, spelled out so that searching this page for the name you typed finds it. Every one of them is on <code>ITrigger</code>, <code>IOperableTrigger</code> or <code>TriggerBase</code>, and on all five <code>*TriggerImpl</code> types by inheritance:</p><table><thead><tr><th>3.x</th><th>4.x</th></tr></thead><tbody><tr><td><code>GetNextFireTimeUtc()</code></td><td><code>NextFireTimeUtc</code></td></tr><tr><td><code>SetNextFireTimeUtc(value)</code></td><td><code>NextFireTimeUtc = value</code></td></tr><tr><td><code>GetPreviousFireTimeUtc()</code></td><td><code>PreviousFireTimeUtc</code></td></tr><tr><td><code>SetPreviousFireTimeUtc(value)</code></td><td><code>PreviousFireTimeUtc = value</code></td></tr><tr><td><code>GetMayFireAgain()</code></td><td><code>MayFireAgain</code></td></tr></tbody></table><p>A <strong>custom trigger deriving from <code>TriggerBase</code></strong> overrides the <code>MayFireAgain</code> property now, because that is the abstract member:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> public override bool GetMayFireAgain() =&gt; NextFireTimeUtc is not null;</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> public override bool MayFireAgain =&gt; NextFireTimeUtc is not null;</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p><code>CronTriggerImpl.CronExpression</code> also gained a getter — it was setter-only — and is typed <code>CronExpression?</code>, which is what it always was underneath.</p><h2 id="the-thread-pool-is-asynchronous" tabindex="-1"><a class="header-anchor" href="#the-thread-pool-is-asynchronous"><span>The thread pool is asynchronous</span></a></h2><p>Only relevant if you implement <code>IThreadPool</code> yourself:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> bool RunInThread(Func&lt;Task&gt; runnable);</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"> int BlockForAvailableThreads();</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"> void Initialize();</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"> void Shutdown(bool waitForJobsToComplete = true);</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"> string InstanceId { set; }</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"> string InstanceName { set; }</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> ValueTask&lt;bool&gt; TryRun(Func&lt;ValueTask&gt; action, CancellationToken cancellationToken = default);</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line"> ValueTask&lt;int&gt; WaitForAvailableThreads(CancellationToken cancellationToken = default);</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line"> ValueTask Initialize(CancellationToken cancellationToken = default);</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line"> ValueTask Shutdown(bool waitForJobsToComplete = true, CancellationToken cancellationToken = default);</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line"> ValueTask&lt;bool&gt; Drain(CancellationToken cancellationToken = default);</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The two renamed methods used to block the calling thread on a semaphore, and the caller is the scheduler&#39;s own asynchronous loop, so waiting for pool capacity tied up a thread. Use <code>WaitAsync</code> in your implementation.</p><h3 id="drain-is-the-shutdown-that-can-be-given-a-deadline" tabindex="-1"><a class="header-anchor" href="#drain-is-the-shutdown-that-can-be-given-a-deadline"><span><code>Drain</code> is the shutdown that can be given a deadline</span></a></h3><p><code>Drain</code> stops the pool accepting work, waits for the work already running, and <strong>reports</strong> whether it finished waiting or gave up. It is what <code>Shutdown(waitForJobsToComplete: true)</code> could never be: that member&#39;s wait is unbounded by contract, and a caller that abandoned it by throwing would skip the job store shutdown, the plugin shutdown and the listener notification that follow it. So <code>IScheduler.Shutdown</code> now passes its own token down to <code>Drain</code> rather than <code>CancellationToken.None</code>, and a host stop that runs out of its budget stops <em>waiting</em> rather than being stuck.</p><p>The token cancels the wait and nothing else. Running jobs are not cancelled — whether a shutting-down scheduler interrupts its jobs is <code>ShutdownJobInterruption</code>&#39;s decision, and it still defaults to never.</p><p><strong>Implementing it is optional.</strong> A pool that does not override it gets a default implementation that calls <code>Shutdown(waitForJobsToComplete: true, CancellationToken.None)</code> and returns <code>true</code>, which is exactly what a 3.x-shaped pool did. Overriding it is worth it when your pool can honour a deadline, and the one rule to get right is what the barrier covers:</p><blockquote><p><code>TryRun</code> is handed the <em>whole</em> of a job&#39;s execution, and the last act of that execution is the job store update that completes the trigger. A pool that waits for its work items therefore waits for those writes too. A count of executing jobs does not: the job listeners are told the job was executed before the store update is issued, so <code>NumberOfJobsExecutingHere</code> reads zero while a persistent store is still being written to. Do not build the barrier on it.</p></blockquote><p><code>TaskSchedulingThreadPool</code> implements both members over one asynchronous barrier, so <code>Shutdown</code> no longer blocks a thread either — its wait is awaited rather than <code>CountdownEvent.Wait</code>-ed. A caller that never awaited the returned <code>ValueTask</code> used to get the wait anyway; it has to await it now.</p><p><code>TryRun</code>&#39;s work item is a <code>Func&lt;ValueTask&gt;</code>: it was the one place an extensibility SPI still made you write <code>async Task</code> in a surface that is <code>ValueTask</code> everywhere else, and the <code>Task</code>-shaped delegate cost the dispatch path a <code>Task&lt;Task&gt;</code>/<code>Unwrap</code> round trip per fire. A custom pool changes the parameter type; a lambda that returned <code>Task.CompletedTask</code> returns <code>ValueTask.CompletedTask</code>.</p><p><code>InstanceId</code> and <code>InstanceName</code> are gone rather than moved: Quartz set them and nothing ever read them. If your pool wants the scheduler&#39;s identity, take <code>IOptions&lt;QuartzSchedulerOptions&gt;</code> from the container.</p><p><code>TaskSchedulingThreadPool.ThreadCount</code> was removed as well; it read and wrote <code>MaxConcurrency</code>, so use that directly. <strong>The <code>quartz.threadPool.threadCount</code> configuration key is unaffected</strong> and still sets <code>MaxConcurrency</code>.</p><h2 id="quartz-spi-and-quartz-simpl-were-renamed" tabindex="-1"><a class="header-anchor" href="#quartz-spi-and-quartz-simpl-were-renamed"><span>Quartz.Spi and Quartz.Simpl were renamed</span></a></h2><p><code>Quartz.Spi</code> is now <code>Quartz.Extensibility</code>, and <code>Quartz.Simpl</code> merged into the existing <code>Quartz.Impl</code>. Both old names were transliterations of <code>org.quartz.spi</code> and <code>org.quartz.simpl</code>. For source code this is a find-and-replace over <code>using</code> directives that the compiler will walk you through. Quartz&#39;s own directory layout followed, so if you read the source or port a patch between the branches, <code>src/Quartz/SPI/</code> is <code>src/Quartz/Extensibility/</code> and <code>src/Quartz/Simpl/</code> is <code>src/Quartz/Impl/</code>.</p><p>Configuration is the part that would not have failed loudly, because it names types by string:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> quartz.jobStore.type = Quartz.Simpl.RAMJobStore, Quartz</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> quartz.jobStore.type = Quartz.Impl.RAMJobStore, Quartz</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p><strong>Existing configuration keeps working.</strong> A type name naming a pre-4.0 namespace that no longer resolves is retried under the new one, and a warning is logged naming both spellings. Treat that as a grace period rather than a promise.</p><p>The same fallback covers the assemblies that were merged into the core package, and composes with the namespace rename, so a string naming both still resolves:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> quartz.serializer.type = Quartz.Simpl.SystemTextJsonObjectSerializer, Quartz.Serialization.SystemTextJson</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> quartz.serializer.type = Quartz.Impl.SystemTextJsonObjectSerializer, Quartz</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>Configuration is not the only place a type is named by string. <code>JOB_CLASS_NAME</code> holds whatever spelling the version that wrote the row used, so a database carried over from 2.x or 3.x names jobs by namespaces and assemblies that have since moved. <strong>Those stored names now resolve through the same fallback</strong>, with the same warning naming both spellings, rather than through the runtime&#39;s lookup alone — a <code>Quartz.Job.NoOpJob, Quartz</code> written years ago finds the type in <code>Quartz.Jobs</code> today. The column itself is left alone: reading a job never rewrites what is persisted for it, so the fallback stays visible until you migrate the data. Previously such a job started up and listed perfectly well, because a job&#39;s type is resolved lazily, and then failed with a <code>TypeLoadException</code> the first time it fired.</p><h3 id="other-namespaces-that-moved" tabindex="-1"><a class="header-anchor" href="#other-namespaces-that-moved"><span>Other namespaces that moved</span></a></h3><p><code>Quartz.Spi</code> and <code>Quartz.Simpl</code> were not the only ones. 4.0 also settles the singular/plural split between a namespace, its assembly and its package — where the package is <code>Quartz.Jobs</code>, the namespace is <code>Quartz.Jobs</code> too — and empties the namespaces that held a single type or a handful of them. For source code each row is a <code>using</code> directive; where a row says the old spelling still resolves, the same fallback and the same warning as above cover configuration strings.</p><table><thead><tr><th>3.x namespace</th><th>4.x namespace</th><th>Notes</th></tr></thead><tbody><tr><td><code>Quartz.Job</code></td><td><code>Quartz.Jobs</code></td><td>Namespace, assembly and package now agree. A configuration string or a stored <code>JOB_CLASS_NAME</code> naming the old spelling still resolves, with a warning</td></tr><tr><td><code>Quartz.Extensibility.IDirectoryProvider</code></td><td><code>Quartz.Jobs.IDirectoryProvider</code></td><td>It exists for <code>DirectoryScanJob</code> alone, so it lives with it. It is resolved from <code>SchedulerContext</code> by key, never by type name. Its one member also changed shape: <code>GetDirectoriesToScan(JobDataMap)</code> returns <code>List&lt;string&gt;</code> rather than <code>IReadOnlyList&lt;string&gt;</code>, following the concrete-out convention, so an implementation returning an array or a <code>ToList()</code> result needs one edit</td></tr><tr><td><code>Quartz.Logging</code>, <code>Quartz.Logging.LogProviders</code></td><td><code>Quartz.Diagnostics</code></td><td><code>LogProvider</code>, <code>DiagnosticHeaders</code> (now <code>ActivityTags</code>) and <code>OperationName</code> moved; <code>ILogProvider</code>, <code>LogContext</code>, <code>LogLevel</code>, the <code>Logger</code> delegate, <code>IJobDiagnosticData</code> and <code>LogProviders.LibLogException</code> went with LibLog. A <code>using Quartz.Logging;</code> no longer resolves at all, and nothing names these namespaces in configuration, so there is no fallback — see <a href="#logging">Logging</a></td></tr><tr><td><code>Quartz.Plugin.History</code>, <code>Quartz.Plugin.Json</code>, <code>Quartz.Plugin.Xml</code></td><td><code>Quartz.Plugins.*</code></td><td>Same rule as the jobs: the package is <code>Quartz.Plugins</code>. A <code>quartz.plugin.&lt;name&gt;.type</code> naming the old spelling still resolves, with a warning. The <strong>configuration key</strong> prefix is still <code>quartz.plugin.</code>, singular — it is not a namespace</td></tr><tr><td><code>Quartz.Plugin.Interrupt</code>, <code>Quartz.Plugin.Management</code>, <code>Quartz.Plugin.TimeZoneConverter</code></td><td>—</td><td>No 4.x counterpart: the one public type each held is gone. See <a href="#jobinterruptmonitorplugin-is-retired-a-job-timeout-is-middleware"><code>JobInterruptMonitorPlugin</code> is retired; a job timeout is middleware</a>, <a href="#shutdownhookplugin-is-retired-the-host-already-shuts-the-scheduler-down"><code>ShutdownHookPlugin</code> is retired; the host already shuts the scheduler down</a> and <a href="#timezoneconverterplugin-is-a-resolver-registration"><code>TimeZoneConverterPlugin</code> is a resolver registration</a>. The <code>Quartz.Plugins.TimeZoneConverter</code> <strong>package</strong> is unaffected — it still ships <code>UseTimeZoneConverter</code></td></tr><tr><td><code>Quartz.Listener</code></td><td><code>Quartz.Listeners</code></td><td>Nothing names a listener by string any more — the <code>quartz.jobListener.&lt;name&gt;.type</code> and <code>quartz.triggerListener.&lt;name&gt;.type</code> keys are gone, see <a href="#the-listener-property-keys-are-retired">The listener property keys are retired</a> — so this is a <code>using</code> to change and nothing else. See also <a href="#the-three-support-base-classes-are-gone">The three <code>*Support</code> base classes are gone</a>: three of the seven types are not there under either name</td></tr><tr><td><code>Quartz.Impl.Matchers</code></td><td><code>Quartz</code></td><td>See <a href="#matchers-moved-to-quartz">Matchers moved to <code>Quartz</code></a>. No shim is needed: a matcher is passed as an object and is never named by a configuration string</td></tr><tr><td><code>Quartz.AspNetCore</code>, <code>Quartz.AspNetCore.HealthChecks</code>, <code>Quartz.AspNetCore.HttpApi</code></td><td><code>Quartz</code></td><td>Only the namespaces are gone; <code>AddQuartzHealthChecks</code>, <code>AddQuartzHttpApi</code> and <code>MapQuartzHttpApi</code> are extension methods and resolve through the <code>Quartz</code> you already have, so a <code>using Quartz.AspNetCore;</code> can simply be deleted. The <strong>packages</strong> differ: the HTTP API is still <code>Quartz.AspNetCore</code>, hosted by <code>QuartzAspNetCoreConfigurationExtensions</code> (renamed from <code>QuartzServiceCollectionExtensions</code> because the core package now has a class of that name in the same namespace), while the health check is in <code>Quartz</code>, hosted by <code>QuartzHealthCheckExtensions</code> — see <a href="#the-health-check-is-in-quartz-not-quartz-aspnetcore">The health check is in <code>Quartz</code>, not <code>Quartz.AspNetCore</code></a></td></tr><tr><td><code>Quartz.HttpClient</code></td><td><code>Quartz</code></td><td><code>HttpScheduler</code> and <code>HttpClientException</code>; the package is still <code>Quartz.HttpClient</code>. The namespace had to go because it shadowed <code>System.Net.Http.HttpClient</code> for every file under <code>Quartz.*</code>, including Quartz&#39;s own. <code>HttpScheduler</code> is also <code>sealed</code> now</td></tr><tr><td><code>Quartz.Serialization.Json</code>, <code>Quartz.Serialization.Json.Calendars</code>, <code>Quartz.Serialization.Json.Triggers</code></td><td><code>Quartz.Serialization.SystemTextJson[.Calendars|.Triggers]</code></td><td>These are the System.Text.Json types, which merged into the core package; the namespace was named after the <em>retired 3.x Newtonsoft package</em>. <code>Quartz.JsonConfigurationExtensions</code> is <code>Quartz.SystemTextJsonConfigurationExtensions</code> to match — the extension methods on it are unaffected. <strong>Read the warning below before changing a <code>using</code> on a ported serializer</strong></td></tr><tr><td><code>Quartz.Impl.Redis</code></td><td><code>Quartz.Extensions.Redis</code></td><td>One type, <code>RedisSemaphore</code>, filed under <code>Impl</code> as if it were part of the core; it is <code>RedisLockHandler</code> now. Namespace, assembly and package are the same string now; the <strong>package id is unchanged</strong>. A <code>quartz.jobStore.lockHandler.type</code> naming the old namespace or the old type name still resolves, with a warning</td></tr></tbody></table><div class="hint-container warning"><p class="hint-container-title">Porting a 3.x Newtonsoft serializer</p><p>In 3.x, <code>Quartz.Serialization.Json.Triggers</code> and <code>Quartz.Serialization.Json.Calendars</code> were the <strong>Newtonsoft</strong> package&#39;s namespaces. In 4.x the same spellings, minus <code>.Json</code>, plus <code>.SystemTextJson</code>, belong to System.Text.Json — and the Newtonsoft package&#39;s are <code>Quartz.Serialization.Newtonsoft.Triggers</code> and <code>Quartz.Serialization.Newtonsoft.Calendars</code>.</p><p>So a 3.x custom Newtonsoft serializer that is ported by changing its <code>using</code> to <code>Quartz.Serialization.SystemTextJson.Triggers</code> compiles against the <em>System.Text.Json</em> base class, and then fails on the overrides: the abstract members take a <code>Utf8JsonWriter</code> and a <code>JsonElement</code>, not a <code>JsonWriter</code> and a <code>JObject</code>. The signature mismatch is the symptom; the wrong base class is the bug. A Newtonsoft serializer stays on the Newtonsoft base — see <a href="#newtonsoft-types-moved-out-of-the-core-namespaces">Newtonsoft types moved out of the core namespaces</a>.</p></div><h2 id="the-scheduler-and-the-job-store-speak-the-same-verbs" tabindex="-1"><a class="header-anchor" href="#the-scheduler-and-the-job-store-speak-the-same-verbs"><span>The scheduler and the job store speak the same verbs</span></a></h2><p><code>IJobStore</code> had its own vocabulary — Store/Remove/Retrieve — for the operations <code>IScheduler</code> calls Schedule/Add/Delete/Get, so the two halves of one operation had different names, and a stack trace had to be translated on the way down. The store now uses the scheduler&#39;s words. If you implement a job store, rename; if you only call <code>IScheduler</code>, nothing here affects you.</p><table><thead><tr><th><code>IJobStore</code> in 3.x</th><th><code>IJobStore</code> in 4.x</th></tr></thead><tbody><tr><td><code>StoreJobAndTrigger(job, trigger)</code></td><td><code>ScheduleJob(job, trigger)</code></td></tr><tr><td><code>StoreJobsAndTriggers(triggersAndJobs, replace)</code></td><td><code>ScheduleJobs(triggersAndJobs, ScheduleJobOptions?)</code></td></tr><tr><td><code>StoreJob(job, replaceExisting)</code></td><td><code>AddJob(job, AddJobOptions?)</code></td></tr><tr><td><code>StoreTrigger(trigger, replaceExisting)</code></td><td><code>AddTrigger(trigger, AddTriggerOptions?)</code></td></tr><tr><td><code>RemoveJob(key)</code>, <code>RemoveJobs(keys)</code></td><td><code>DeleteJob(key)</code>, <code>DeleteJobs(keys)</code></td></tr><tr><td><code>RemoveTrigger(key)</code>, <code>RemoveTriggers(keys)</code></td><td><code>DeleteTrigger(key)</code>, <code>DeleteTriggers(keys)</code></td></tr><tr><td><code>RetrieveJob(key)</code></td><td><code>GetJob(key)</code></td></tr><tr><td><code>RetrieveTrigger(key)</code></td><td><code>GetTrigger(key)</code></td></tr><tr><td><code>StoreCalendar(name, cal, replaceExisting, updateTriggers)</code></td><td><code>AddCalendar(name, cal, AddCalendarOptions?)</code></td></tr><tr><td><code>RemoveCalendar(name)</code></td><td><code>DeleteCalendar(name)</code></td></tr><tr><td><code>RetrieveCalendar(name)</code></td><td><code>GetCalendar(name)</code></td></tr><tr><td><code>ClearAllSchedulingData()</code></td><td><code>Clear()</code></td></tr><tr><td><code>AcquireNextTriggers(noLaterThan, maxCount, timeWindow, executionLimits)</code></td><td><code>AcquireNextTriggers(TriggerAcquisitionRequest)</code></td></tr></tbody></table><p>3.x spelled the flag that decides whether an existing item is over-written <code>replaceExisting</code> on some members and <code>replace</code> on others. 4.x has no flag on any of them: each takes the same options record <code>IScheduler</code> does — see <a href="#the-store-takes-the-same-options">The store takes the same options</a>. The <code>protected</code><code>AdoJobStoreBase</code> members that mirror these — the <code>ConnectionAndTransactionHolder</code> overloads, and <code>AcquireNextTrigger</code> — were renamed with them and keep the <code>bool</code>, being the store&#39;s own machinery rather than its contract.</p><p>The activity names in <code>Quartz.Diagnostics.OperationName.JobStore</code> follow the methods they name, so a trace filter matching <code>&quot;Quartz.JobStore.StoreJob&quot;</code> now needs <code>&quot;Quartz.JobStore.AddJob&quot;</code>, and so on for every row above.</p><h3 id="acquisition-takes-a-request-record" tabindex="-1"><a class="header-anchor" href="#acquisition-takes-a-request-record"><span>Acquisition takes a request record</span></a></h3><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> await store.AcquireNextTriggers(noLaterThan, maxCount, timeWindow, executionLimits, ct);</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> await store.AcquireNextTriggers(new TriggerAcquisitionRequest</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line"> {</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">     NoLaterThan = noLaterThan,</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">     MaxCount = maxCount,</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">     TimeWindow = timeWindow,</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">     ExecutionLimits = executionLimits,</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line"> }, ct);</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>TriggerAcquisitionRequest</code> lives in <code>Quartz.Extensibility</code>. Acquisition keeps growing dimensions — the batching window, per-execution-group limits, node affinity — and each one used to be another parameter on the hot path of every store. As a record, the next one is an added optional property a store can ignore. It is the store-level counterpart of the delegate-level <code>TriggerAcquisitionCriteria</code>. <code>TimeWindow</code> rejects a negative value at construction, where <code>AdoJobStoreBase</code> used to throw from inside acquisition.</p><p>Both records also expose optional <code>ExcludedJobTypeNames</code> collections, and <strong>every shipped store honours the request-level one</strong>. <code>AdoJobStoreBase</code> copies it into its delegate criteria and the standard ADO.NET delegates keep the rows out in the acquisition SQL; <code>RAMJobStore</code> skips a candidate whose job type name is in the set. Names use the stored <code>TriggerAcquireResult.JobTypeName</code> spelling, which is <code>JobType.FullName</code>. <code>RAMJobStore</code> compares ordinally; SQL comparison follows the database job-class column&#39;s collation, including its case-sensitivity rules.</p><h2 id="options-records-replace-boolean-parameters" tabindex="-1"><a class="header-anchor" href="#options-records-replace-boolean-parameters"><span>Options records replace boolean parameters</span></a></h2><p><code>AddJob</code> and <code>AddCalendar</code> took bare booleans that say nothing at the call site about which is which, and <code>AddJob</code> had a second overload only because its second boolean was added later. Both are one member now, taking an optional record whose defaults are the conservative choice (<code>Replace = false</code>, <code>StoreNonDurableWhileAwaitingScheduling = false</code>, <code>UpdateTriggers = false</code>).</p><table><thead><tr><th>3.x</th><th>4.x</th></tr></thead><tbody><tr><td><code>AddJob(job, replace: false)</code></td><td><code>AddJob(job)</code></td></tr><tr><td><code>AddJob(job, replace: true)</code></td><td><code>AddJob(job, new AddJobOptions { Replace = true })</code></td></tr><tr><td><code>AddJob(job, true, true)</code></td><td><code>AddJob(job, new AddJobOptions { Replace = true, StoreNonDurableWhileAwaitingScheduling = true })</code></td></tr><tr><td><code>AddCalendar(name, cal, false, false)</code></td><td><code>AddCalendar(name, cal)</code></td></tr><tr><td><code>AddCalendar(name, cal, true, true)</code></td><td><code>AddCalendar(name, cal, new AddCalendarOptions { Replace = true, UpdateTriggers = true })</code></td></tr></tbody></table><p><code>replace: true</code> on its own is nearly all of what these options are ever asked for, so each record names it: <code>AddJobOptions.Replacing</code>, <code>ScheduleJobOptions.Replacing</code>, <code>AddCalendarOptions.Replacing</code> and <code>AddCalendarOptions.ReplacingAndUpdatingTriggers</code> are the initializers above under a name — see <a href="#five-shorthands-for-the-common-case">Five shorthands for the common case</a>. The initializer stays the way to say anything else.</p><p><code>AddJobOptions</code> and <code>AddCalendarOptions</code> are both in the <code>Quartz</code> namespace, and <code>IJobStore</code> takes them as well — see <a href="#the-store-takes-the-same-options">The store takes the same options</a>. <code>AddJobOptions.StoreNonDurableWhileAwaitingScheduling</code> is a scheduler-level rule that <code>IScheduler</code> has already applied by the time the store is called, so a store reads only <code>Replace</code>.</p><p>Both are <code>readonly record struct</code>s, and the parameter is <code>AddJobOptions options = default</code> rather than <code>AddJobOptions? options = null</code>. The defaults already were the conservative choice, so <code>default</code> <em>is</em> what passing nothing has always meant — and the signature stops claiming three states (not given, empty, configured) where there are two. Four implementations independently wrote <code>options ??= new()</code> to discover that for themselves; a store of your own no longer has to.</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> await scheduler.AddJob(job, null);</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> await scheduler.AddJob(job);</span>
<span class="line"></span></span></span>
<span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> AddJobOptions? options = null;</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> AddJobOptions options = default;</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>new AddJobOptions { Replace = true }</code> and <code>new AddCalendarOptions { … }</code> are unchanged, and so is every call that omits the argument. What stops compiling is passing an explicit <code>null</code>, or declaring a nullable local of the type and handing it in — and a <code>CalendarConfiguration.Options</code> read that expected <code>null</code> to mean &quot;the scheduler&#39;s own defaults&quot;, which it never did.</p><p>The DI-time builders — <code>q.AddJob&lt;T&gt;(…)</code> and <code>q.AddCalendar&lt;T&gt;(…)</code> on <code>IQuartzBuilder</code> — are unchanged.</p><h3 id="schedulejob-and-schedulejobs-take-the-same-treatment" tabindex="-1"><a class="header-anchor" href="#schedulejob-and-schedulejobs-take-the-same-treatment"><span><code>ScheduleJob</code> and <code>ScheduleJobs</code> take the same treatment</span></a></h3><p>The two <code>IScheduler</code> members that schedule a job together with a collection of triggers took a bare <code>bool replace</code> in the middle of their argument list, where it read as anonymous as <code>AddJob</code>&#39;s did.</p><table><thead><tr><th>3.x</th><th>4.x</th></tr></thead><tbody><tr><td><code>ScheduleJob(job, triggers, replace: false)</code></td><td><code>ScheduleJob(job, triggers)</code></td></tr><tr><td><code>ScheduleJob(job, triggers, replace: true)</code></td><td><code>ScheduleJob(job, triggers, new ScheduleJobOptions { Replace = true })</code></td></tr><tr><td><code>ScheduleJobs(triggersAndJobs, false)</code></td><td><code>ScheduleJobs(triggersAndJobs)</code></td></tr><tr><td><code>ScheduleJobs(triggersAndJobs, true)</code></td><td><code>ScheduleJobs(triggersAndJobs, new ScheduleJobOptions { Replace = true })</code></td></tr></tbody></table><p><code>ScheduleJobOptions</code> is a separate type from <code>AddJobOptions</code> rather than a reuse of it. <code>AddJobOptions</code> also carries <code>StoreNonDurableWhileAwaitingScheduling</code>, which has no meaning here — a trigger is always supplied, so the job is never awaiting scheduling — and its <code>Replace</code> is about the job alone, where this one covers the job <em>and</em> its triggers.</p><p>The HTTP API&#39;s <code>ScheduleJobsRequest</code> keeps its <code>Replace</code> property; the endpoint adapts.</p><h3 id="the-store-takes-the-same-options" tabindex="-1"><a class="header-anchor" href="#the-store-takes-the-same-options"><span>The store takes the same options</span></a></h3><p><code>IJobStore</code> had the same inconsistency the scheduler did, and for longer: <code>AddCalendar</code> took an options record while <code>AddJob</code>, <code>AddTrigger</code> and <code>ScheduleJobs</code> took a bare <code>bool</code>, so one interface spelled one decision two ways. All four take a record now, and it is the <em>same</em> record the scheduler takes — a decorator forwarding a call passes the value through rather than unpacking and rebuilding it.</p><table><thead><tr><th>3.x</th><th>4.x</th></tr></thead><tbody><tr><td><code>store.StoreJob(job, replaceExisting: false)</code></td><td><code>store.AddJob(job)</code></td></tr><tr><td><code>store.StoreJob(job, replaceExisting: true)</code></td><td><code>store.AddJob(job, AddJobOptions.Replacing)</code></td></tr><tr><td><code>store.StoreTrigger(trigger, replaceExisting: true)</code></td><td><code>store.AddTrigger(trigger, AddTriggerOptions.Replacing)</code></td></tr><tr><td><code>store.StoreJobsAndTriggers(batch, replace: true)</code></td><td><code>store.ScheduleJobs(batch, ScheduleJobOptions.Replacing)</code></td></tr></tbody></table><p><code>AddTriggerOptions</code> is new, and carries <code>Replace</code> alone. It has one call site, because <code>IScheduler</code> has no <code>AddTrigger</code>: a trigger reaches a scheduler through <code>ScheduleJob</code>, and <code>IJobStore.AddTrigger</code> is the storage half of that.</p><p>If you implement <code>IJobStore</code>, the three signatures change and the argument you already had is <code>options.Replace</code>. If you only call <code>IScheduler</code>, nothing here affects you.</p><h2 id="a-component-of-your-own-can-have-options-of-its-own" tabindex="-1"><a class="header-anchor" href="#a-component-of-your-own-can-have-options-of-its-own"><span>A component of your own can have options of its own</span></a></h2><p><code>IQuartzBuilder.ConfigureOptions&lt;TOptions&gt;(Action&lt;TOptions&gt;?)</code> is new. It registers the callback under this scheduler&#39;s options name and declares the type as belonging to the scheduler, so a component the container builds — which asks for <code>IOptions&lt;TOptions&gt;</code> and would otherwise be handed the <em>unnamed</em> instance — sees what was configured for its own scheduler.</p><p>That mechanism existed; it was reachable only through <code>AddPlugin&lt;T, TOptions&gt;()</code>, which is now sugar over it. Everything else built by the container — a thread pool, a job store, a lock handler, a listener, a job factory — had no way to reach it, and under <code>AddQuartz(&quot;name&quot;, …)</code> quietly saw defaults.</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line">services<span class="token punctuation">.</span><span class="token function">AddQuartz</span><span class="token punctuation">(</span><span class="token string">&quot;reporting&quot;</span><span class="token punctuation">,</span> q <span class="token operator">=&gt;</span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    q<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">ConfigureOptions</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>MyThreadPoolOptions<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span>options <span class="token operator">=&gt;</span> options<span class="token punctuation">.</span>Slots <span class="token operator">=</span> <span class="token number">20</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">    q<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">UseThreadPool</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>MyThreadPool<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"><span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>It is a default interface implementation, so an <code>IQuartzBuilder</code> implemented outside Quartz keeps compiling — and the default body is the whole mechanism rather than a stub, because the scheduler&#39;s options name is its <code>SchedulerName</code> (<code>Options.DefaultName</code> is the empty string, which is what <code>SchedulerName</code> is for the unnamed scheduler).</p><p>With a replacement to point at, <code>UseThreadPool&lt;T&gt;()</code> drops its <code>Action&lt;ThreadPoolOptions&gt;</code> parameter:</p><table><thead><tr><th>Before</th><th>After</th></tr></thead><tbody><tr><td><code>UseThreadPool&lt;T&gt;(options =&gt; options.MaxConcurrency = 20)</code></td><td><code>UseDefaultThreadPool(maxConcurrency: 20)</code>, or <code>ConfigureOptions&lt;TOwnOptions&gt;(…)</code> for a pool of your own</td></tr></tbody></table><p>The parameter was honoured for exactly one implementation — <code>TaskSchedulingThreadPool</code> and its descendants, which is what <code>MaxConcurrency</code> means. <code>UseThreadPool&lt;AnythingElse&gt;(o =&gt; o.MaxConcurrency = 5)</code> compiled, registered the callback and did nothing with it. <code>UseDefaultThreadPool</code> keeps the parameter, because that is where the options are read.</p><h2 id="overloads-that-differed-only-by-a-default" tabindex="-1"><a class="header-anchor" href="#overloads-that-differed-only-by-a-default"><span>Overloads that differed only by a default</span></a></h2><table><thead><tr><th>3.x</th><th>4.x</th></tr></thead><tbody><tr><td><code>Shutdown()</code>, <code>Shutdown(bool waitForJobsToComplete)</code></td><td><code>Shutdown(bool waitForJobsToComplete = false, …)</code></td></tr><tr><td><code>TriggerJob(jobKey)</code>, <code>TriggerJob(jobKey, data)</code></td><td><code>TriggerJob(JobKey jobKey, JobDataMap? data = null, …)</code></td></tr><tr><td><code>Interrupt(string fireInstanceId)</code></td><td><code>InterruptFireInstance(string fireInstanceId)</code></td></tr><tr><td><code>GetMetaData()</code></td><td><code>GetMetadata()</code>, returning <code>SchedulerMetadata</code></td></tr><tr><td><code>GetTriggersOfJob(jobKey)</code></td><td>extension method over <code>QueryTriggers(new TriggerQuery { Job = jobKey })</code></td></tr></tbody></table><p>Calls to <code>Shutdown</code> and <code>TriggerJob</code> compile unchanged unless they passed a <code>CancellationToken</code> positionally into the short overload, which now needs to be named:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> await scheduler.Shutdown(cancellationToken);</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> await scheduler.Shutdown(cancellationToken: cancellationToken);</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p><code>Interrupt</code> overloaded on <code>JobKey</code> versus <code>string</code> hid two different operations behind one name — cancel every execution of a job, versus cancel one specific fire — so picking the wrong one was a silent, type-driven mistake. <code>Interrupt(JobKey)</code> keeps its name.</p><p><code>GetTriggersOfJob</code> is an extension method on <code>SchedulerQueryExtensions</code> now, so existing call sites still compile. It runs <code>QueryTriggers</code> and then <code>GetTriggers</code> on the keys it found; when the state and fire times a listing needs are enough, call <code>QueryTriggers</code> with <code>TriggerQuery.Job</code> yourself and save the second round trip.</p><h3 id="schedulermetadata-replaces-schedulermetadata" tabindex="-1"><a class="header-anchor" href="#schedulermetadata-replaces-schedulermetadata"><span><code>SchedulerMetadata</code> replaces <code>SchedulerMetaData</code></span></a></h3><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> SchedulerMetaData metaData = await scheduler.GetMetaData();</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"> Console.WriteLine($&quot;Executed {metaData.NumberOfJobsExecuted} jobs.&quot;);</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"> Console.WriteLine(metaData.GetSummary());</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> SchedulerMetadata metadata = await scheduler.GetMetadata();</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line"> Console.WriteLine($&quot;Executed {metadata.JobsExecuted} jobs.&quot;);</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line"> Console.WriteLine(metadata);</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The old type was built through a fifteen-parameter constructor of which six were adjacent booleans. It is a <code>sealed record</code> with <code>init</code> properties now, so a snapshot is built and read by name. <code>GetSummary()</code> is gone — the record&#39;s <code>ToString()</code> prints every value, which is what the hand-written summary was for. Over HTTP, <code>SchedulerStatisticsDto.NumberOfJobsExecuted</code> is <code>JobsExecuted</code> to match.</p><p>The renamed and reshaped properties:</p><table><thead><tr><th>3.x / earlier 4.0 preview</th><th>4.0</th></tr></thead><tbody><tr><td><code>SchedulerType</code> (<code>Type</code>)</td><td><code>SchedulerTypeName</code> (<code>string</code>)</td></tr><tr><td><code>JobStoreType</code> (<code>Type</code>)</td><td><code>JobStoreTypeName</code> (<code>string</code>)</td></tr><tr><td><code>ThreadPoolType</code> (<code>Type</code>)</td><td><code>ThreadPoolTypeName</code> (<code>string</code>)</td></tr><tr><td><code>SchedulerRemote</code> / <code>IsRemote</code></td><td><code>IsProxy</code></td></tr><tr><td><code>NumberOfJobsExecuted</code></td><td><code>JobsExecuted</code></td></tr><tr><td><code>JobStoreSupportsPersistence</code></td><td><code>JobStorePersistent</code>, reading like its sibling <code>JobStoreClustered</code></td></tr><tr><td><code>Started</code>, <code>InStandbyMode</code>, <code>Shutdown</code></td><td>one <code>required SchedulerStatus Status</code> — see <a href="#a-scheduler-s-lifecycle-is-one-value">A scheduler&#39;s lifecycle is one value</a></td></tr></tbody></table><p>The <code>Type</code> members became assembly-qualified names (without version) because a proxy cannot promise to materialize them: an <code>HttpScheduler</code> reads the remote scheduler&#39;s metadata over the wire, and the remote&#39;s job store or thread pool type need not exist — and previously had to be <code>Type.GetType</code>-resolved — in the client process. <code>IsProxy</code> says what the flag always meant: this metadata describes a proxy to a scheduler running elsewhere, with the values read over the wire, rather than the in-process instance.</p><h2 id="single-key-mutations-answer-whether-they-applied" tabindex="-1"><a class="header-anchor" href="#single-key-mutations-answer-whether-they-applied"><span>Single-key mutations answer whether they applied</span></a></h2><p><code>IScheduler</code> already answered for some mutations — <code>DeleteJob</code>, <code>UnscheduleJob</code> and <code>UpdateTriggerDetails</code> return a <code>bool</code> — while the pause family returned nothing, so a caller pausing a mistyped key learned nothing. The rule is now uniform: a mutation aimed at one key returns <code>ValueTask&lt;bool&gt;</code> meaning &quot;the entity existed and the operation applied&quot;, and the group-matcher forms return the affected group names.</p><table><thead><tr><th>Member (on <code>IScheduler</code> and <code>IJobStore</code>)</th><th>3.x returned</th><th>4.x returns</th></tr></thead><tbody><tr><td><code>PauseTrigger(key)</code>, <code>ResumeTrigger(key)</code></td><td><code>ValueTask</code></td><td><code>ValueTask&lt;bool&gt;</code></td></tr><tr><td><code>PauseJob(key)</code>, <code>ResumeJob(key)</code></td><td><code>ValueTask</code></td><td><code>ValueTask&lt;bool&gt;</code></td></tr><tr><td><code>ResetTriggerFromErrorState(key)</code></td><td><code>ValueTask</code></td><td><code>ValueTask&lt;bool&gt;</code></td></tr><tr><td><code>PauseTriggerGroups(matcher)</code>, <code>ResumeTriggerGroups(matcher)</code></td><td><code>ValueTask</code> (scheduler)</td><td><code>ValueTask&lt;List&lt;string&gt;&gt;</code> of the group names affected</td></tr><tr><td><code>PauseJobGroups(matcher)</code>, <code>ResumeJobGroups(matcher)</code></td><td><code>ValueTask</code> (scheduler)</td><td><code>ValueTask&lt;List&lt;string&gt;&gt;</code> of the group names affected</td></tr></tbody></table><p>What the <code>bool</code> means, precisely:</p><ul><li><code>PauseTrigger</code> — the trigger exists and ended up paused because of this call. Already paused, complete, or missing → <code>false</code>.</li><li><code>ResumeTrigger</code> — the trigger existed in a paused state and was resumed. Not paused or missing → <code>false</code>.</li><li><code>PauseJob</code> / <code>ResumeJob</code> — the job exists. A job that currently has zero triggers returns <code>true</code>: the job was found and the operation applied to all (zero) of its triggers.</li><li><code>ResetTriggerFromErrorState</code> — the trigger existed in the <code>Error</code> state and was reset. Not in <code>Error</code>, or missing → <code>false</code>.</li></ul><p>When the result is <code>false</code>, no scheduler-listener events are raised — a no-op no longer looks like a state change to listeners. Awaiting call sites compile unchanged; only <code>ISchedulerListener</code> implementations that counted on being told about no-op pauses will notice.</p><p>Over the HTTP API these endpoints used to answer <code>200 OK</code> with an empty body. They now answer <code>200 OK</code> with a JSON body: <code>{&quot;applied&quot;: bool}</code> for the single-key forms and <code>{&quot;groups&quot;: [...]}</code> for the group-matcher forms. This is additive for clients that ignored the body — but a <strong>4.0-final <code>HttpScheduler</code> against a 4.0-preview server throws</strong> on these calls, because the client now reads a response body the old server never sends. Upgrade the server before, or together with, its remote clients.</p><h2 id="a-set-of-keys-pauses-resumes-or-resets-in-one-call" tabindex="-1"><a class="header-anchor" href="#a-set-of-keys-pauses-resumes-or-resets-in-one-call"><span>A set of keys pauses, resumes or resets in one call</span></a></h2><p>Pausing forty triggers took forty calls, and on a database store forty transactions and forty scheduling signals. <code>IScheduler</code> and <code>IJobStore</code> now take a key set as well as one key or a group matcher:</p><table><thead><tr><th>New member (on <code>IScheduler</code> and <code>IJobStore</code>)</th><th>Returns</th></tr></thead><tbody><tr><td><code>PauseTriggers(IReadOnlyCollection&lt;TriggerKey&gt;)</code>, <code>ResumeTriggers(…)</code></td><td><code>ValueTask&lt;List&lt;TriggerKey&gt;&gt;</code> of the keys it applied to</td></tr><tr><td><code>PauseJobs(IReadOnlyCollection&lt;JobKey&gt;)</code>, <code>ResumeJobs(…)</code></td><td><code>ValueTask&lt;List&lt;JobKey&gt;&gt;</code> of the keys it applied to</td></tr><tr><td><code>ResetTriggersFromErrorState(IReadOnlyCollection&lt;TriggerKey&gt;)</code></td><td><code>ValueTask&lt;List&lt;TriggerKey&gt;&gt;</code> of the keys it reset</td></tr></tbody></table><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token class-name">List<span class="token punctuation">&lt;</span>TriggerKey<span class="token punctuation">&gt;</span></span> paused <span class="token operator">=</span> <span class="token keyword">await</span> scheduler<span class="token punctuation">.</span><span class="token function">PauseTriggers</span><span class="token punctuation">(</span></span>
<span class="line">    <span class="token punctuation">[</span><span class="token keyword">new</span> <span class="token constructor-invocation class-name">TriggerKey</span><span class="token punctuation">(</span><span class="token string">&quot;nightly&quot;</span><span class="token punctuation">,</span> <span class="token string">&quot;reports&quot;</span><span class="token punctuation">)</span><span class="token punctuation">,</span> <span class="token keyword">new</span> <span class="token constructor-invocation class-name">TriggerKey</span><span class="token punctuation">(</span><span class="token string">&quot;hourly&quot;</span><span class="token punctuation">,</span> <span class="token string">&quot;reports&quot;</span><span class="token punctuation">)</span><span class="token punctuation">]</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>The answer is the plural of the single-key <code>bool</code>: a key the operation did not apply to — one that names nothing, one that was already paused, one that was not in the error state — is <strong>absent from the returned list</strong>, never an exception. The list keeps the order the keys were given in. These are overloads, so the existing single-key and group-matcher forms are untouched; only a <code>null</code> literal argument, which never compiled against <code>PauseJobs(null)</code> anyway, now needs a cast to pick an overload. The matcher forms are named <a href="#pausing-by-matcher-is-a-group-operation-and-is-named-for-one"><code>PauseJobGroups</code> and its three siblings</a>.</p><p>What the outside world sees:</p><ul><li><strong>Listener events stay per key.</strong> One <code>TriggerPaused</code>, <code>JobPaused</code>, <code>TriggerResumed</code> or <code>JobResumed</code> for every key the operation applied to, and nothing for the rest. There is no key-set listener event and none was added: <code>TriggersPaused(null)</code> means <em>every group</em>, so a listener watching for outages would read a bulk pause of two triggers as the whole scheduler going down, and a new event with a default implementation would silently stop telling existing listeners anything.</li><li><strong>One scheduling signal per call</strong>, not one per key — the scheduler thread reads the signal as a level, so collapsing them loses nothing. Resetting from the error state signals nothing at all, here as in the single-key form.</li><li><strong>One pass in the store.</strong> <code>RAMJobStore</code> takes its lock once; the ADO store runs the whole set inside one <code>SchedulerLock.TriggerAccess</code> scope and one transaction, so a bulk pause is atomic where forty single pauses were not.</li></ul><p><code>IJobStore</code>&#39;s five members are default implementations that walk the set one key at a time, so a job store of your own keeps compiling and stays correct. Override them to do the walk in one pass, as both shipped stores do.</p>`,216),e(`p`,null,[l[372]||=r(`Over HTTP the key-set forms are new endpoints, described in `,-1),c(g,{to:`/documentation/quartz-4.x/packages/http-api.html#a-whole-set-of-keys-in-one-call`},{default:i(()=>[...l[371]||=[r(`the HTTP API page`,-1)]]),_:1}),l[373]||=t(`: <code>POST …/jobs/keys/pause</code>, <code>…/jobs/keys/resume</code>, <code>…/triggers/keys/pause</code>, <code>…/triggers/keys/resume</code> and <code>…/triggers/keys/reset-from-error-state</code>. They live under <code>keys/</code> because the collection-level <code>pause</code> and <code>resume</code> already belong to the group-matcher forms.`,17)]),l[893]||=t(`<h2 id="the-key-set-delete-and-unschedule-answer-with-the-keys-they-removed" tabindex="-1"><a class="header-anchor" href="#the-key-set-delete-and-unschedule-answer-with-the-keys-they-removed"><span>The key-set delete and unschedule answer with the keys they removed</span></a></h2><p><code>DeleteJobs</code> and <code>UnscheduleJobs</code> predate that convention and used to answer one <code>bool</code> meaning &quot;every key given was found&quot;. That answer was lossy in the case a caller most needs to know about: delete three of five existing jobs and <strong>three jobs are deleted</strong> while the call answers <code>false</code>, which is indistinguishable from nothing having happened. A caller who retried on <code>false</code> never learned that most of the work had already succeeded. They now answer the way the rest of the key-set family does.</p><table><thead><tr><th>Member</th><th>3.x / 4.0 preview returned</th><th>4.0 returns</th></tr></thead><tbody><tr><td><code>IScheduler.DeleteJobs(IReadOnlyCollection&lt;JobKey&gt;)</code></td><td><code>ValueTask&lt;bool&gt;</code> — every key was found</td><td><code>ValueTask&lt;List&lt;JobKey&gt;&gt;</code> of the keys it deleted</td></tr><tr><td><code>IScheduler.UnscheduleJobs(IReadOnlyCollection&lt;TriggerKey&gt;)</code></td><td><code>ValueTask&lt;bool&gt;</code></td><td><code>ValueTask&lt;List&lt;TriggerKey&gt;&gt;</code> of the keys it removed</td></tr><tr><td><code>IJobStore.DeleteJobs(IReadOnlyCollection&lt;JobKey&gt;)</code></td><td><code>ValueTask&lt;bool&gt;</code></td><td><code>ValueTask&lt;List&lt;JobKey&gt;&gt;</code></td></tr><tr><td><code>IJobStore.DeleteTriggers(IReadOnlyCollection&lt;TriggerKey&gt;)</code></td><td><code>ValueTask&lt;bool&gt;</code></td><td><code>ValueTask&lt;List&lt;TriggerKey&gt;&gt;</code></td></tr></tbody></table><p><strong>To get the old answer back</strong>, compare the counts:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token class-name">List<span class="token punctuation">&lt;</span>JobKey<span class="token punctuation">&gt;</span></span> deleted <span class="token operator">=</span> <span class="token keyword">await</span> scheduler<span class="token punctuation">.</span><span class="token function">DeleteJobs</span><span class="token punctuation">(</span>jobKeys<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"><span class="token class-name"><span class="token keyword">bool</span></span> allFound <span class="token operator">=</span> deleted<span class="token punctuation">.</span>Count <span class="token operator">==</span> jobKeys<span class="token punctuation">.</span>Count<span class="token punctuation">;</span>   <span class="token comment">// what the bool used to say</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>That one line is why the change is a replacement rather than a second overload: the old answer is still available to anyone who wants it, while the question it could not answer — <em>which</em> keys — now has one.</p><p>Three consequences worth knowing:</p><ul><li><strong>A key that named nothing no longer raises a listener event.</strong> The old implementation raised one <code>ISchedulerListener.JobDeleted</code> / <code>JobUnscheduled</code> per key <strong>given</strong>, so a mistyped key told every listener that a job by that name had been deleted. The events now follow the keys the call applied to, which is what the single-key <code>DeleteJob</code> and the whole key-set pause family already did. The scheduling change is still signalled once for the call, and not at all when nothing was removed.</li><li><strong>An empty key set never reaches the store</strong>, and a <code>null</code> one is an <code>ArgumentNullException</code> rather than a <code>NullReferenceException</code> — again matching the key-set pause family.</li><li><strong>Repeating a key in the set no longer poisons the answer.</strong> The second pass finds nothing left to delete; before, that dragged the whole call&#39;s <code>bool</code> to <code>false</code>.</li></ul><p>If you implement <code>IJobStore</code>, both members are now <strong>default implementations</strong> that walk the set one key at a time, so a store that only implements the single-key <code>DeleteJob</code> / <code>DeleteTrigger</code> is correct without writing them. That has one trap: a store that still declares <code>ValueTask&lt;bool&gt; DeleteJobs(IReadOnlyCollection&lt;JobKey&gt;, CancellationToken)</code> <strong>still compiles</strong> — the method simply stops implementing the interface member, and the per-key default silently takes over. Change the return type. Both shipped stores override the pair to walk inside one lock and one transaction, and <code>JobStoreContractTest</code> fails a store that has left the default in place.</p><p>The ADO store&#39;s walk stays per key on purpose. Deleting a job there is a cascade rather than a statement — its triggers and their sub-table rows, the fired-trigger rows that would otherwise resurrect it, then the job detail row — and a set-based <code>DELETE … WHERE … IN (…)</code> reports a row count rather than which keys it hit. Naming the deleted keys therefore costs no extra round trip: each iteration&#39;s result was already there and was being folded into a boolean and thrown away.</p>`,10),e(`p`,null,[l[375]||=r(`Over HTTP, `,-1),l[376]||=e(`code`,null,`POST …/jobs/delete`,-1),l[377]||=r(` and `,-1),l[378]||=e(`code`,null,`POST …/triggers/unschedule`,-1),l[379]||=r(` answer `,-1),l[380]||=e(`code`,null,`{"jobs": [ … ]}`,-1),l[381]||=r(` and `,-1),l[382]||=e(`code`,null,`{"triggers": [ … ]}`,-1),l[383]||=r(` instead of `,-1),l[384]||=e(`code`,null,`{"allFound": …}`,-1),l[385]||=r(`, which makes them ordinary members of `,-1),c(g,{to:`/documentation/quartz-4.x/packages/http-api.html#a-whole-set-of-keys-in-one-call`},{default:i(()=>[...l[374]||=[r(`the key-set family`,-1)]]),_:1}),l[386]||=r(` and removes the one exception to the flag-naming rule.`,-1)]),l[894]||=t(`<h2 id="jobs-and-triggers-can-be-removed-by-group" tabindex="-1"><a class="header-anchor" href="#jobs-and-triggers-can-be-removed-by-group"><span>Jobs and triggers can be removed by group</span></a></h2><p>Pause has always had a group form and delete has not, so calling off everything that belonged to one saga, one tenant or one import meant listing the keys and then deleting them — two calls, and a window between them in which another node can schedule one more thing into the group that the delete will then miss. Both members now take a <code>GroupMatcher</code> as well as a key set:</p><table><thead><tr><th>New member (on <code>IScheduler</code>)</th><th>Returns</th></tr></thead><tbody><tr><td><code>DeleteJobs(GroupMatcher&lt;JobKey&gt;)</code></td><td><code>ValueTask&lt;List&lt;JobKey&gt;&gt;</code> of the jobs it deleted</td></tr><tr><td><code>UnscheduleJobs(GroupMatcher&lt;TriggerKey&gt;)</code></td><td><code>ValueTask&lt;List&lt;TriggerKey&gt;&gt;</code> of the triggers it removed</td></tr></tbody></table><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token comment">// Everything scheduled for one saga, called off in one call.</span></span>
<span class="line"><span class="token class-name">List<span class="token punctuation">&lt;</span>TriggerKey<span class="token punctuation">&gt;</span></span> calledOff <span class="token operator">=</span> <span class="token keyword">await</span> scheduler<span class="token punctuation">.</span><span class="token function">UnscheduleJobs</span><span class="token punctuation">(</span>GroupMatcher<span class="token operator">&lt;</span>TriggerKey<span class="token operator">&gt;</span><span class="token punctuation">.</span><span class="token function">GroupEquals</span><span class="token punctuation">(</span>sagaId<span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>The answer is the <strong>keys</strong>, not the group names that <a href="#pausing-by-matcher-is-a-group-operation-and-is-named-for-one"><code>PauseJobGroups</code></a> returns. A pause has something to remember per group — a group stays paused, and a job added to it later is born paused — and a delete has nothing: the group is simply emptier than it was. What a caller can still act on is what went, so that is what it gets.</p><ul><li><strong><code>null</code> is an <code>ArgumentNullException</code>, not &quot;the default group&quot;.</strong> 3.x&#39;s <code>PauseJobs(null)</code> and <code>ResumeJobs(null)</code> read a missing matcher as <code>GroupEquals(JobKey.DefaultGroup)</code>. These do not, and will not: a pause taken by mistake can be resumed. A <code>null</code> literal now also needs a cast to pick an overload, exactly as it did when the key-set forms arrived — <code>DeleteJobs((IReadOnlyCollection&lt;JobKey&gt;) null!)</code>.</li><li><strong>One scheduling signal per call and one listener event per key</strong>, the same shape the key-set forms have: an <code>ISchedulerListener.JobDeleted</code> or <code>JobUnscheduled</code> for each key removed, nothing for an empty group, and no group-level event — <code>JobsPaused(null)</code> means <em>every group</em> and a monitoring listener would read it as a total outage.</li><li><strong>A non-durable job orphaned by <code>UnscheduleJobs(matcher)</code> is deleted, and is not named.</strong> That is what the single-key <code>UnscheduleJob</code> and the key-set <code>DeleteTriggers</code> already do; the answer is about triggers.</li></ul><p><code>IJobStore</code> gains <code>DeleteJobs(GroupMatcher&lt;JobKey&gt;)</code> and <code>DeleteTriggers(GroupMatcher&lt;TriggerKey&gt;)</code> as <strong>default implementations</strong> that list the matching keys and then delete them, so a store of your own keeps compiling and stays correct. What the default cannot be is atomic — the listing and the deletion are two operations — so both shipped stores override the pair to resolve the group inside the lock that empties it: <code>RAMJobStore</code> under its single lock, the ADO store inside one <code>SchedulerLock.TriggerAccess</code> scope and one transaction, using the <code>SelectJobKeysInGroup</code> / <code>SelectTriggerKeysInGroup</code> its driver delegate already had. <code>JobStoreContractTest</code> fails a shipped store that has left the default in place.</p><p>The ADO store&#39;s walk stays per key once the group is resolved, for the reason <a href="#the-key-set-delete-and-unschedule-answer-with-the-keys-they-removed">the key-set forms give</a>: the answer has to name the keys it hit, and a set-based <code>DELETE … WHERE … IN (…)</code> reports a row count.</p>`,8),e(`p`,null,[l[388]||=r(`Over HTTP the group forms are new endpoints, `,-1),l[389]||=e(`code`,null,`POST …/jobs/delete-by-group`,-1),l[390]||=r(` and `,-1),l[391]||=e(`code`,null,`POST …/triggers/unschedule-by-group`,-1),l[392]||=r(`, taking the same four `,-1),l[393]||=e(`code`,null,`group*`,-1),l[394]||=r(` query parameters as `,-1),l[395]||=e(`code`,null,`…/jobs/pause`,-1),l[396]||=r(` and answering with the same `,-1),l[397]||=e(`code`,null,`{"jobs": […]}`,-1),l[398]||=r(` / `,-1),l[399]||=e(`code`,null,`{"triggers": […]}`,-1),l[400]||=r(` bodies the key-set forms do — `,-1),c(g,{to:`/documentation/quartz-4.x/packages/http-api.html#a-whole-group-in-one-call`},{default:i(()=>[...l[387]||=[r(`described here`,-1)]]),_:1}),l[401]||=r(`. They carry `,-1),l[402]||=e(`code`,null,`-by-group`,-1),l[403]||=r(` in the path because the plain `,-1),l[404]||=e(`code`,null,`delete`,-1),l[405]||=r(` and `,-1),l[406]||=e(`code`,null,`unschedule`,-1),l[407]||=r(` paths were taken by the key-set forms before there was a group form to give them to.`,-1)]),l[895]||=t(`<h2 id="pausing-by-matcher-is-a-group-operation-and-is-named-for-one" tabindex="-1"><a class="header-anchor" href="#pausing-by-matcher-is-a-group-operation-and-is-named-for-one"><span>Pausing by matcher is a group operation, and is named for one</span></a></h2><p><code>PauseJobs</code> had two overloads answering two different things: hand it keys and it answered with the keys it moved, hand it a <code>GroupMatcher</code> and it answered with <strong>group names</strong>. That is the only place in the API where one name meant two shapes, and the fix is not to make the matcher form answer with keys — it is to admit that pausing by matcher is a different operation from pausing a set of jobs, and to name it for what it does:</p><table><thead><tr><th>3.x / 4.0 preview</th><th>4.0</th></tr></thead><tbody><tr><td><code>PauseJobs(GroupMatcher&lt;JobKey&gt;)</code></td><td><code>PauseJobGroups(GroupMatcher&lt;JobKey&gt;)</code></td></tr><tr><td><code>ResumeJobs(GroupMatcher&lt;JobKey&gt;)</code></td><td><code>ResumeJobGroups(GroupMatcher&lt;JobKey&gt;)</code></td></tr><tr><td><code>PauseTriggers(GroupMatcher&lt;TriggerKey&gt;)</code></td><td><code>PauseTriggerGroups(GroupMatcher&lt;TriggerKey&gt;)</code></td></tr><tr><td><code>ResumeTriggers(GroupMatcher&lt;TriggerKey&gt;)</code></td><td><code>ResumeTriggerGroups(GroupMatcher&lt;TriggerKey&gt;)</code></td></tr></tbody></table><p>The key-set overloads keep their names and their <code>List&lt;JobKey&gt;</code> / <code>List&lt;TriggerKey&gt;</code> answers, so every name now has exactly one return shape. The rename is on <code>IScheduler</code>, <code>IJobStore</code> and every shipped implementation, and it is a compile error rather than a behaviour change: the bodies are untouched.</p><p>Why the group form is not simply made to answer with keys:</p><ul><li><strong>It writes group state, and the group outlives the keys.</strong> A paused group is a row in <code>QRTZ_PAUSED_TRIGGER_GRPS</code> or <code>QRTZ_PAUSED_JOB_GRPS</code>. It survives a restart, reaches every node of a cluster, is what <code>IsTriggerGroupPaused</code> reports, and — for trigger groups — is imposed on triggers stored into the group <em>afterwards</em>. The group names are exactly the rows the call wrote.</li><li><strong>An equality matcher pauses a group that holds nothing.</strong> <code>PauseTriggerGroups(GroupEquals(&quot;nightly&quot;))</code> on an empty group records the pause so that what is added next is born paused. There is no key to return for that, and a keys-shaped answer would report the operation as having done nothing.</li><li><strong>It would cost a query nobody asked for.</strong> The ADO store pauses a group with one set-based <code>UPDATE</code>. Answering with keys would mean selecting every affected trigger key first — unbounded, on the one path that had been careful not to be.</li></ul><p>Whatever a caller does next — resume, list, report — is per group as well, so the group names are the handles that fit. <code>TracingJobStore</code> opens <code>Quartz.JobStore.PauseTriggerGroups</code> and its three siblings now, where the group and key-set forms used to share one span name.</p><h2 id="resetting-a-group-from-the-error-state-is-an-ischeduler-member" tabindex="-1"><a class="header-anchor" href="#resetting-a-group-from-the-error-state-is-an-ischeduler-member"><span>Resetting a group from the error state is an <code>IScheduler</code> member</span></a></h2><p><code>ResetTriggersFromErrorState(GroupMatcher&lt;TriggerKey&gt;)</code> was an extension method on <code>SchedulerQueryExtensions</code>, which is a class named for queries. It is a mutation, its key-set twin is an <code>IScheduler</code> member, and every other matcher-shaped mutation — <code>DeleteJobs</code>, <code>UnscheduleJobs</code>, the four <code>*Groups</code> members above — is one too. It moved:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> List&lt;TriggerKey&gt; reset = await scheduler.ResetTriggersFromErrorState(matcher);   // extension</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> List&lt;TriggerKey&gt; reset = await scheduler.ResetTriggersFromErrorState(matcher);   // IScheduler member</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>The call site is unchanged for anything holding an <code>IScheduler</code>, which is nearly everything. It is a <strong>default interface member</strong>, implemented over <code>QueryTriggers</code> and the key-set <code>ResetTriggersFromErrorState</code>, so a scheduler written outside this repository needs no change and one whose store can do it in a single statement can override it. The two consequences of that:</p><ul><li>a variable typed as a <em>concrete</em> scheduler — <code>HttpScheduler</code>, <code>StdScheduler</code> — needs <code>((IScheduler) scheduler).ResetTriggersFromErrorState(matcher)</code>, because C# does not surface a default interface member on the implementing type;</li><li>it is still two round trips, exactly as the extension was: a listing filtered to <code>TriggerState.Error</code> and the group, then the key-set reset. A trigger that fails between them is left for the next call.</li></ul><h2 id="one-wire-contract-and-its-enums-have-names" tabindex="-1"><a class="header-anchor" href="#one-wire-contract-and-its-enums-have-names"><span>One wire contract, and its enums have names</span></a></h2><p>The DTOs that define the HTTP API&#39;s JSON used to live in <code>Quartz.HttpClient</code>, and <code>Quartz.AspNetCore</code> reached them by depending on the client package. They now live in <code>Quartz</code>, internal, visible to both — so the server package no longer ships a dependency on the client package. Nothing public moved: the contract was internal before and is internal still, and every JSON property name survived the move unchanged.</p><p>What did change is how the contract&#39;s enums are spelled. A trigger body has always said <code>&quot;repeatIntervalUnit&quot;: &quot;Hour&quot;</code>, because Quartz&#39;s own converters write enums by name, while the DTOs beside it said <code>&quot;status&quot;: 1</code>. One value now has one spelling everywhere:</p><table><thead><tr><th>Body</th><th>4.0 preview</th><th>4.0</th></tr></thead><tbody><tr><td><code>GET …/schedulers</code> item, <code>GET …/schedulers/{name}</code></td><td><code>&quot;status&quot;: 1</code></td><td><code>&quot;status&quot;: &quot;Running&quot;</code></td></tr><tr><td><code>GET …/schedulers/{name}/triggers</code> item</td><td><code>&quot;state&quot;: 1</code></td><td><code>&quot;state&quot;: &quot;Paused&quot;</code></td></tr><tr><td><code>GET …/schedulers/{name}/triggers/{group}/{name}/state</code></td><td><code>{&quot;state&quot;: 1}</code></td><td><code>{&quot;state&quot;: &quot;Paused&quot;}</code></td></tr></tbody></table><p>Reading still accepts both, so a request body or <code>?state=</code> filter written against a preview keeps working; only responses changed. <code>HttpScheduler</code> reads the new spelling, so a client and server upgraded together need no code change — a hand-written client that read <code>status</code>/<code>state</code> as numbers does.</p><p>One field was spelled twice for the same value, and now is not:</p><table><thead><tr><th>Body</th><th>4.0 preview</th><th>4.0</th></tr></thead><tbody><tr><td><code>GET …/schedulers/{name}/jobs</code> item</td><td><code>&quot;jobTypeName&quot;: &quot;Some.Job, Some.Assembly&quot;</code></td><td><code>&quot;jobType&quot;: &quot;Some.Job, Some.Assembly&quot;</code></td></tr></tbody></table><p>The value is unchanged — an assembly-qualified name in both places — and <code>jobType</code> is what <code>GET …/jobs/{group}/{name}</code> has always called it. Core&#39;s listing record keeps <code>JobHeader.JobTypeName</code>: the store&#39;s noun and the wire&#39;s need not agree, but the wire may not disagree with itself.</p><p>Only these two enums are affected, and the converters are registered per enum type: the HTTP API adds its converters to the application&#39;s shared <code>JsonOptions</code>, and a host&#39;s own endpoints must keep rendering their own enums the way they always did.</p><h2 id="one-flag-per-mutation-named-for-what-it-reports" tabindex="-1"><a class="header-anchor" href="#one-flag-per-mutation-named-for-what-it-reports"><span>One flag per mutation, named for what it reports</span></a></h2><p>Which body a <code>200</code> carries is now a rule rather than a per-endpoint fact: <strong>a <code>200</code> carries a body exactly when the operation has something to say that the caller could not have worked out for itself.</strong> A mutation that always acts — <code>AddJob</code>, <code>TriggerJob</code>, <code>PauseAll</code>, <code>ScheduleJobs</code>, <code>AddCalendar</code>, the scheduler and execution-limit writes — answers with an empty body; a mutation whose effect may be a no-op answers with one boolean flag; a mutation aimed at a group matcher or a key set answers with what it applied to; and a mutation that computed something answers with that value.</p><p>The empty-body and flag-carrying halves were already split that way. What was not consistent was the <em>name</em>: seven spellings of the same question, so a caller had to look up the body per endpoint instead of predicting it.</p><table><thead><tr><th>Endpoint</th><th>4.0 preview</th><th>4.0</th></tr></thead><tbody><tr><td><code>DELETE …/jobs/{group}/{name}</code></td><td><code>{&quot;jobFound&quot;: …}</code></td><td><code>{&quot;applied&quot;: …}</code></td></tr><tr><td><code>POST …/jobs/{group}/{name}/interrupt</code></td><td><code>{&quot;interrupted&quot;: …}</code></td><td><code>{&quot;applied&quot;: …}</code></td></tr><tr><td><code>POST …/jobs/interrupt/{fireInstanceId}</code></td><td><code>{&quot;interrupted&quot;: …}</code></td><td><code>{&quot;applied&quot;: …}</code></td></tr><tr><td><code>POST …/triggers/{group}/{name}/unschedule</code></td><td><code>{&quot;triggerFound&quot;: …}</code></td><td><code>{&quot;applied&quot;: …}</code></td></tr><tr><td><code>DELETE …/calendars/{name}</code></td><td><code>{&quot;calendarFound&quot;: …}</code></td><td><code>{&quot;applied&quot;: …}</code></td></tr><tr><td><code>POST …/jobs/delete</code></td><td><code>{&quot;allJobsFound&quot;: …}</code></td><td><code>{&quot;jobs&quot;: [ … ]}</code></td></tr><tr><td><code>POST …/triggers/unschedule</code></td><td><code>{&quot;allTriggersFound&quot;: …}</code></td><td><code>{&quot;triggers&quot;: [ … ]}</code></td></tr></tbody></table><p><code>applied</code> means the entity existed and the operation changed it, and there is no second spelling: an operation that cannot answer that question about a single entity is a key-set form, and answers with the keys instead. The last two rows are the ones that changed <em>shape</em> rather than name — they are key sets, and a single boolean could only say that not every key was found, which a caller could not tell from nothing having happened. See <a href="#the-key-set-delete-and-unschedule-answer-with-the-keys-they-removed">the key-set delete and unschedule</a> for the scheduler API behind them.</p><p><code>HttpScheduler</code> reads the new spellings, so a client and server upgraded together need no code change; a hand-written client reading the old names does.</p><p>Errors gained a rule of their own: <strong>a client-actionable error names the exception type it came from; a server fault does not.</strong> Every <code>400</code> and every <code>404</code> now carries the same members whichever layer raised it — <code>type</code>, <code>title</code>, <code>status</code>, <code>detail</code> and <code>Quartz-ExceptionType</code>:</p><div class="language-json line-numbers-mode" data-highlighter="prismjs" data-ext="json"><pre><code class="language-json"><span class="line"><span class="token punctuation">{</span></span>
<span class="line">  <span class="token property">&quot;type&quot;</span><span class="token operator">:</span> <span class="token string">&quot;https://tools.ietf.org/html/rfc9110#section-15.5.1&quot;</span><span class="token punctuation">,</span></span>
<span class="line">  <span class="token property">&quot;title&quot;</span><span class="token operator">:</span> <span class="token string">&quot;Bad Request&quot;</span><span class="token punctuation">,</span></span>
<span class="line">  <span class="token property">&quot;status&quot;</span><span class="token operator">:</span> <span class="token number">400</span><span class="token punctuation">,</span></span>
<span class="line">  <span class="token property">&quot;detail&quot;</span><span class="token operator">:</span> <span class="token string">&quot;The scheduler has been shut down&quot;</span><span class="token punctuation">,</span></span>
<span class="line">  <span class="token property">&quot;Quartz-ExceptionType&quot;</span><span class="token operator">:</span> <span class="token string">&quot;SchedulerException&quot;</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>Quartz-ExceptionType</code> used to ride only on the <code>SchedulerException</code> path, so a <code>400</code> from request validation and a <code>400</code> from the scheduler were two different shapes, and a client could not tell a body the member was missing from a body that never carries it. It now names the exception type on every <code>400</code> and <code>404</code>, including the framework&#39;s own — <code>BadHttpRequestException</code> for a request the endpoint rejected, <code>NotFoundException</code> for a <code>404</code>. Map the Quartz names back to typed exceptions and treat the rest as opaque, which is what <code>HttpScheduler</code> does.</p><p>A <code>500</code> deliberately does <strong>not</strong> carry it. It is a fault the caller cannot act on, so naming the type behind it would say something about the server&#39;s internals and nothing a client could use.</p><p>The one <code>400</code> that still has no body is not the API&#39;s: a query parameter the framework could not bind never reaches an endpoint, so nothing of ours writes the response.</p><h2 id="durations-are-timespans-wherever-they-are" tabindex="-1"><a class="header-anchor" href="#durations-are-timespans-wherever-they-are"><span>Durations are <code>TimeSpan</code>s, wherever they are</span></a></h2><p>A trigger body has always said <code>&quot;repeatIntervalTimeSpan&quot;: &quot;120.02:30:59.9990000&quot;</code>. Three duration members beside it counted whole milliseconds instead, which both disagreed with that and threw away everything below a millisecond:</p><table><thead><tr><th>4.0 preview</th><th>4.0</th></tr></thead><tbody><tr><td><code>POST …/schedulers/{name}/start?delayMilliseconds=30000</code></td><td><code>?delay=00:00:30</code></td></tr><tr><td><code>JobExecutionResultDto.RunTimeMs</code> (<code>long</code>)</td><td><code>RunTime</code> (<code>TimeSpan</code>)</td></tr><tr><td><code>DashboardHistoryEntry.DurationMs</code> (<code>long</code>)</td><td><code>Duration</code> (<code>TimeSpan</code>)</td></tr></tbody></table><p><code>HttpScheduler.StartDelayed</code> sends the new spelling, so a client and server upgraded together need no code change. A negative <code>delay</code> is now a <code>400</code> rather than a delay that runs backwards.</p><p>The two dashboard records are the ones the dashboard&#39;s SignalR hub and its history store put on the wire, so a browser or history store reading <code>runTimeMs</code> / <code>durationMs</code> as a number reads <code>runTime</code> / <code>duration</code> as an ISO-ish <code>TimeSpan</code> string instead.</p><h2 id="the-dashboard-s-client-speaks-one-currency" tabindex="-1"><a class="header-anchor" href="#the-dashboard-s-client-speaks-one-currency"><span>The dashboard&#39;s client speaks one currency</span></a></h2><p><code>IQuartzApiClient</code> is the dashboard&#39;s own projection of the HTTP API — public so that an application can replace it with its own data source. It used to answer in three vocabularies at once: an enum from <code>GetTriggers</code> and a <code>string</code> from <code>GetTriggerState</code>, a paging model of its own beside core&#39;s, and sixteen methods taking a loose <code>(schedulerName, group, name)</code> triplet next to a hub interface that already had <code>JobKeyDto</code> and <code>TriggerKeyDto</code>. Now it says what Quartz says:</p><table><thead><tr><th>3.x</th><th>4.x</th></tr></thead><tbody><tr><td><code>GetTriggerState(…)</code> → <code>string</code></td><td>→ <code>TriggerState</code></td></tr><tr><td><code>TriggerHeaderDto.State</code> is <code>string?</code></td><td><code>TriggerState?</code></td></tr><tr><td><code>SchedulerHeaderDto.Status</code>, <code>SchedulerDetailDto.Status</code> are <code>string</code></td><td><code>SchedulerStatus</code>, a new public enum in <code>Quartz</code></td></tr><tr><td><code>GetJobs(name, string? groupFilter, int page, int pageSize)</code> → <code>JobPageDto</code></td><td><code>QueryJobs(name, DashboardJobQuery)</code> → <code>PagedResult&lt;JobKeyDto&gt;</code></td></tr><tr><td><code>GetTriggers(name, string? groupFilter, TriggerState?, int page, int pageSize)</code> → <code>TriggerPageDto</code></td><td><code>QueryTriggers(name, DashboardTriggerQuery)</code> → <code>PagedResult&lt;TriggerHeaderDto&gt;</code></td></tr><tr><td><code>GetHistory(JobHistoryQueryDto)</code> → <code>JobHistoryPageDto</code> (a <code>JsonElement</code>)</td><td><code>QueryExecutions(DashboardHistoryQuery)</code> → <code>PagedResult&lt;DashboardHistoryEntry&gt;</code></td></tr><tr><td><code>GetCurrentlyExecutingJobs(name)</code> → <code>List&lt;CurrentlyExecutingJobDto&gt;</code></td><td><code>QueryFireInstances(name, DashboardFireInstanceQuery)</code> → <code>PagedResult&lt;FireInstanceDto&gt;</code>, following <code>IScheduler</code> — see <a href="#what-is-running-is-a-listing-not-a-list-of-contexts">What is running is a listing, not a list of contexts</a></td></tr><tr><td><code>CurrentlyExecutingJobDto</code></td><td><code>FireInstanceDto</code>: <code>FireInstanceId</code> is non-null and leads, <code>JobKey</code> is nullable (an acquired firing has no job loaded yet), and <code>SchedulerInstanceId</code>, <code>FireInstanceState State</code> and <code>ScheduledFireTimeUtc</code> are new</td></tr><tr><td>—</td><td><code>QueryClusterNodes(name)</code> → <code>List&lt;ClusterNodeDto&gt;</code> (new), behind the <strong>Cluster</strong> page — see <a href="#the-nodes-of-a-cluster-are-a-listing">The nodes of a cluster are a listing</a></td></tr><tr><td><code>IsJobGroupPaused(name, group)</code> → <code>bool</code></td><td><code>QueryJobGroups(name, DashboardGroupQuery)</code> → <code>PagedResult&lt;JobGroupDto&gt;</code>, each carrying <code>Name</code> and <code>Paused</code>; one call answers for every group instead of one, and <code>Take = 0</code> with <code>Paused = true</code> counts the paused ones without listing them</td></tr><tr><td>—</td><td><code>QueryTriggerGroups(name, DashboardGroupQuery)</code> → <code>PagedResult&lt;TriggerGroupDto&gt;</code> (new), the trigger-group twin of the above</td></tr><tr><td>—</td><td><code>CountMisfires(name, since)</code> → <code>int</code> (new), what the overview&#39;s misfire tile reads — see <a href="#history-and-live-events-say-which-node-they-came-from">History and live events say which node they came from</a></td></tr><tr><td><code>ExecutionLimitsDto(IReadOnlyDictionary&lt;string, int?&gt; Limits)</code></td><td><code>ExecutionLimitsDto(Dictionary&lt;string, DashboardExecutionLimit&gt; Limits, bool UsesTriggerGroupWhenUnset = false, bool CanReport = true)</code> — concrete out, as everywhere else; each entry carries the limit&#39;s <a href="#an-execution-limit-can-be-cluster-wide">scope</a> as well as its number, and the keys are the spellings configuration and the HTTP API use (<code>_</code> for the ungrouped bucket, <code>*</code> for the catch-all) so that a firing can be joined to the limit governing it</td></tr><tr><td><code>GetExecutionLimits(name)</code> → <code>ExecutionLimitsDto?</code>, null both for &quot;nothing is limited&quot; and for &quot;this scheduler cannot say&quot;</td><td>→ <code>ExecutionLimitsDto</code>, never null: nothing limited is an empty <code>Limits</code>, and a scheduler that cannot answer is <code>ExecutionLimitsDto.CannotReport</code>, whose <code>CanReport</code> is <code>false</code></td></tr><tr><td><code>IDashboardHistoryStore.GetPage(name, page, pageSize, jobFilter, triggerFilter)</code> → <code>DashboardHistoryPage</code></td><td><code>QueryExecutions(DashboardHistoryQuery)</code> → <code>PagedResult&lt;DashboardHistoryEntry&gt;</code></td></tr><tr><td><code>…Job(name, string group, string jobName)</code> — eight members</td><td><code>…Job(name, JobKeyDto)</code></td></tr><tr><td><code>…Trigger(name, string group, string triggerName)</code> — seven members</td><td><code>…Trigger(name, TriggerKeyDto)</code></td></tr></tbody></table><p><code>DashboardJobQuery</code>, <code>DashboardTriggerQuery</code> and <code>DashboardHistoryQuery</code> derive from <code>PagedQuery</code>, so they carry <code>Skip</code>, <code>Take</code> and <code>IncludeTotalCount</code> with the meanings the job stores give them, and every listing returns <code>PagedResult&lt;T&gt;</code> with <code>HasMore</code> and a nullable <code>TotalCount</code>. A 1-based page becomes <code>Skip = (page - 1) * pageSize, Take = pageSize</code>, computed once where the pager lives instead of at every call site. <code>JobPageDto</code>, <code>TriggerPageDto</code>, <code>DashboardHistoryPage</code>, <code>JobHistoryPageDto</code> and <code>JobHistoryQueryDto</code> are gone.</p><p><code>SchedulerStatus</code> is the enum the HTTP API has always put on the wire; it is public now because the dashboard&#39;s contract needed a name for it, and it is what <a href="#a-scheduler-s-lifecycle-is-one-value"><code>IScheduler.Status</code></a> reports. Note that a running scheduler is <code>Running</code>: the in-process client used to call it <code>&quot;Started&quot;</code> while the HTTP-backed client called the same state <code>&quot;Running&quot;</code>, and code that matched on either string now matches on the enum.</p><p>The dashboard hub&#39;s <code>SchedulerStateDto</code> follows: it is <code>(string SchedulerName, string SchedulerInstanceId, SchedulerStatus Status)</code> rather than <code>(string SchedulerName, string State)</code>, and it is pushed once per state the scheduler arrives in. <code>SchedulerStarting</code> pushes nothing, being an event rather than a state. The instance id has no 3.x counterpart — see <a href="#history-and-live-events-say-which-node-they-came-from">History and live events say which node they came from</a>.</p><h3 id="the-client-speaks-the-scheduler-s-verbs" tabindex="-1"><a class="header-anchor" href="#the-client-speaks-the-scheduler-s-verbs"><span>The client speaks the scheduler&#39;s verbs</span></a></h3><p>The names went with the types. An operation <code>IQuartzApiClient</code> forwards to a scheduler is spelled the way <code>IScheduler</code> spells it, so a reader who knows one knows the other; only what has no counterpart on <code>IScheduler</code> — the scheduler listing, the execution history — names itself. The DTO suffixes and the <code>PagedResult&lt;T&gt;</code> returns are unchanged.</p><table><thead><tr><th>4.0 preview</th><th>4.0</th></tr></thead><tbody><tr><td><code>StartScheduler</code>, <code>StandbyScheduler</code>, <code>ShutdownScheduler</code></td><td><code>Start</code>, <code>Standby</code>, <code>Shutdown</code></td></tr><tr><td><code>InterruptJob(name, JobKeyDto)</code></td><td><code>Interrupt(name, JobKeyDto)</code>; <code>InterruptFireInstance</code> is unchanged</td></tr><tr><td><code>TriggerJob(name, JobKeyDto)</code> <strong>and</strong> <code>TriggerJobWithData(name, JobKeyDto, JobDataMap)</code></td><td>one <code>TriggerJob(name, JobKeyDto, JobDataMap? jobDataMap = null)</code>, as <code>IScheduler.TriggerJob</code> is</td></tr><tr><td><code>GetJobs</code>, <code>GetJobGroups</code>, <code>GetTriggers</code>, <code>GetTriggerGroups</code>, <code>GetFireInstances</code>, <code>GetClusterNodes</code></td><td><code>QueryJobs</code>, <code>QueryJobGroups</code>, <code>QueryTriggers</code>, <code>QueryTriggerGroups</code>, <code>QueryFireInstances</code>, <code>QueryClusterNodes</code></td></tr><tr><td><code>GetJob(name, JobKeyDto)</code> → <code>JobDetailDto</code></td><td><code>GetJobDetail(name, JobKeyDto)</code>, the noun <code>IScheduler</code> uses</td></tr><tr><td><code>GetJobTriggers(name, JobKeyDto)</code></td><td><code>GetTriggersOfJob(name, JobKeyDto)</code></td></tr><tr><td><code>GetHistory(DashboardHistoryQuery)</code> → <code>PagedResult&lt;DashboardHistoryEntry&gt;?</code></td><td><code>QueryExecutions(…)</code> → <code>PagedResult&lt;DashboardHistoryEntry&gt;</code>, never null</td></tr><tr><td><code>GetMisfires(DashboardMisfireQuery)</code> → <code>PagedResult&lt;DashboardMisfireEntry&gt;?</code></td><td><code>QueryMisfires(…)</code> → <code>PagedResult&lt;DashboardMisfireEntry&gt;</code>, never null</td></tr><tr><td><code>CountMisfires(name, since)</code> → <code>int?</code></td><td>→ <code>int</code></td></tr><tr><td><code>IDashboardHistoryStore.Add</code>, <code>GetPage</code>, <code>GetMisfires</code></td><td><code>AddExecution</code>, <code>QueryExecutions</code>, <code>QueryMisfires</code>; <code>AddMisfire</code> and <code>CountMisfires</code> are unchanged</td></tr><tr><td><code>Interrupt</code>, <code>InterruptFireInstance</code>, <code>DeleteJob</code>, <code>UnscheduleJob</code>, <code>DeleteCalendar</code> → <code>ValueTask</code></td><td>→ <code>ValueTask&lt;bool&gt;</code>, the flag <code>IScheduler</code> returns</td></tr></tbody></table><p><code>GetSchedulers</code>, <code>GetScheduler</code> and <code>GetCalendarNames</code> keep their names: the first two list and read the container&#39;s registrations, which is not an operation a scheduler has, and <code>GetCalendarNames</code> reads <em>every</em> page of <code>IScheduler.QueryCalendarNames</code> rather than one, so naming it <code>Query*</code> would promise a paged query it does not take.</p><p>The five that answered with a bare <code>ValueTask</code> now answer with the applied flag their <code>IScheduler</code> counterparts always returned, so all ten mutations report it and none of them silently drop it: the HTTP API has always put it on the wire as <code>OperationAppliedResponse</code>, and a client member sharing a scheduler&#39;s verb has to share its answer or read as the same operation while not being one. An implementation of <code>IQuartzApiClient</code> returns what the scheduler told it; a page can now tell &quot;deleted&quot; from &quot;was already gone&quot; and says which.</p><p>The three nullable returns were nullable because the deleted HTTP-backed client turned a 404 into &quot;this data source keeps no history&quot;. Nothing in this process has that answer: <code>IDashboardHistoryStore</code> always answers, and a store holding nothing returns an empty page and a count of zero. The History page&#39;s &quot;execution history is unavailable&quot; state went with the null, and the overview&#39;s misfire tile shows a dash only while no scheduler has answered yet.</p><h3 id="a-trigger-is-an-itrigger-a-calendar-is-an-icalendar" tabindex="-1"><a class="header-anchor" href="#a-trigger-is-an-itrigger-a-calendar-is-an-icalendar"><span>A trigger is an <code>ITrigger</code>, a calendar is an <code>ICalendar</code></span></a></h3><p>Six members of that contract still spoke <code>System.Text.Json.JsonElement</code>. They were placeholders from the dashboard&#39;s build-out, and they made every consumer of <code>IQuartzApiClient</code> parse JSON to read something Quartz already has a type for:</p><table><thead><tr><th>4.0 preview</th><th>4.0</th></tr></thead><tbody><tr><td><code>GetTrigger(…)</code> → <code>TriggerDetailDto(JsonElement Value)</code></td><td>→ <code>ITrigger</code></td></tr><tr><td><code>GetCalendar(…)</code> → <code>CalendarDetailDto(JsonElement Value)</code></td><td>→ <code>ICalendar</code></td></tr><tr><td><code>ScheduleJobRequest(JsonElement Trigger, JobDetailDto? Job)</code></td><td><code>ScheduleJobRequest(ITrigger Trigger, JobDetailDto? Job)</code></td></tr><tr><td><code>RescheduleRequest(JsonElement NewTrigger)</code></td><td><code>RescheduleRequest(ITrigger NewTrigger)</code></td></tr><tr><td><code>AddCalendarRequest(string, JsonElement Calendar, bool, bool)</code></td><td><code>AddCalendarRequest(string, ICalendar Calendar, bool, bool)</code></td></tr><tr><td><code>JobDetailDto.JobDataMap</code> is a <code>JsonElement</code></td><td>a <code>JobDataMap</code></td></tr><tr><td><code>TriggerJobWithData(…, JsonElement jobDataMap, …)</code></td><td><code>TriggerJob(…, JobDataMap? jobDataMap = null, …)</code></td></tr></tbody></table><p><code>TriggerDetailDto</code> and <code>CalendarDetailDto</code> are gone with them; nothing remains for them to wrap.</p><p>The polymorphism a trigger needs is Quartz&#39;s own: the serializer registry maps each kind to its own serializer, custom kinds an application registered included, and the HTTP API&#39;s wire format <em>is</em> that discriminated shape. A per-kind DTO family in the dashboard would have to grow a member for every trigger kind and would still have nothing to say about a kind it had never heard of, so the contract speaks <code>ITrigger</code> and lets the registry do what it is for. <code>JobDataMap</code> is the same argument at the other end of the scale: the map holds arbitrary user values, which is exactly what <code>JobDataMap</code> is, and typing it as one keeps an <code>int</code> an <code>int</code> where a JSON round trip made it whatever the reader guessed.</p><p>One consequence worth knowing: <strong>the client no longer serializes anything.</strong> It used to write the trigger to JSON and read it back for no reason but the contract&#39;s type, and it fell back to reflecting over the trigger for kinds the registry did not know — which produced a payload that could not be posted back. A custom trigger type now reaches the detail page as itself.</p><p><code>TriggerHeaderDto</code> was half a positional record and half property-initialised, which read as an accident because it was one. It is positional throughout:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> new TriggerHeaderDto(group, name, executionGroup) { TriggerType = …, ScheduleSummary = …, State = … }</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> new TriggerHeaderDto(group, name, triggerType, scheduleSummary, state, executionGroup)</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="the-dashboard-reads-the-schedulers-in-its-own-process" tabindex="-1"><a class="header-anchor" href="#the-dashboard-reads-the-schedulers-in-its-own-process"><span>The dashboard reads the schedulers in its own process</span></a></h3><p><code>Quartz.Dashboard.Services.QuartzApiClient</code> — the <code>IQuartzApiClient</code> implementation that called a Quartz HTTP API over the network — is gone, and <code>QuartzDashboardOptions.BaseUrl</code> and <code>QuartzDashboardOptions.ApiPath</code> are gone with it. Nothing ever resolved it: <code>AddQuartzDashboard</code> registers the in-process client, which reads <code>ISchedulerRepository</code> directly, and it did so in every 4.0 preview. Two implementations of one interface where only one ever ran had already disagreed twice about the same fact — <code>&quot;Started&quot;</code> against <code>&quot;Running&quot;</code> for a running scheduler, <code>CronTrigger</code> against <code>Cron</code> for a trigger&#39;s kind — because nothing exercised the one that never ran.</p><p><code>AddQuartzDashboard</code> still registers <code>IQuartzApiClient</code> with <code>TryAdd</code>, so an application that registers its own implementation first is the one the pages read. A dashboard rendering a scheduler in another process is a product with its own questions — authentication forwarding, execution limits, a history endpoint no Quartz HTTP API serves — and it is designed in <a href="https://github.com/quartznet/quartznet/issues/3387" target="_blank" rel="noopener noreferrer">#3387</a> rather than left half-built here.</p><p><code>QuartzHttpApiOptions.ApiPath</code>, which is where the HTTP API itself is served, is unaffected: it is a different option on a different type, and <code>AddQuartzHttpApi</code> still reads it.</p><h2 id="history-and-live-events-say-which-node-they-came-from" tabindex="-1"><a class="header-anchor" href="#history-and-live-events-say-which-node-they-came-from"><span>History and live events say which node they came from</span></a></h2><p>A cluster is one scheduler running in several processes. Each node keeps its own history of its own executions and pushes its own live events, and neither said which node it was — so the History page could not attribute a row to a machine and the Live Logs view could not tell a local event from a peer&#39;s. Both feeds carry the node now, and the history is bounded by age as well as by count.</p><table><thead><tr><th>4.0 preview</th><th>4.0</th></tr></thead><tbody><tr><td><code>DashboardHistoryEntry(SchedulerName, JobGroup, …)</code></td><td><code>DashboardHistoryEntry(SchedulerName, SchedulerInstanceId, JobGroup, …)</code></td></tr><tr><td>—</td><td><code>DashboardMisfireEntry(SchedulerName, SchedulerInstanceId, TriggerGroup, TriggerName, JobKeyDto? JobKey, MisfiredAtUtc, DateTimeOffset? ScheduledFireTimeUtc)</code> (new)</td></tr><tr><td>—</td><td><code>DashboardMisfireQuery : PagedQuery</code>, with <code>SchedulerName</code>, <code>SchedulerInstanceId</code> and <code>TriggerFilter</code> (new)</td></tr><tr><td><code>DashboardHistoryQuery</code></td><td>gained <code>string? SchedulerInstanceId</code> — null lists every node&#39;s</td></tr><tr><td><code>IDashboardHistoryStore</code></td><td>gained <code>AddMisfire</code>, <code>QueryMisfires(DashboardMisfireQuery)</code> and <code>CountMisfires(name, since)</code></td></tr><tr><td><code>IQuartzApiClient</code></td><td>gained <code>QueryMisfires(DashboardMisfireQuery)</code> → <code>PagedResult&lt;DashboardMisfireEntry&gt;</code> and <code>CountMisfires(name, since)</code> → <code>int</code>, which is what the overview&#39;s misfire tile reads</td></tr><tr><td><code>QuartzDashboardOptions</code></td><td>gained <code>TimeSpan HistoryRetention</code> (24 hours) and <code>int HistoryMaxEntriesPerScheduler</code> (2000)</td></tr><tr><td><code>DashboardHistoryPlugin(IServiceProvider)</code></td><td><code>DashboardHistoryPlugin(IServiceProvider, TimeProvider)</code>, and it implements <code>ITriggerListener</code> as well as <code>IJobListener</code></td></tr><tr><td><code>SchedulerStateDto(SchedulerName, Status)</code></td><td><code>SchedulerStateDto(SchedulerName, SchedulerInstanceId, Status)</code></td></tr><tr><td><code>SchedulerErrorDto(SchedulerName, Message, …)</code></td><td><code>SchedulerErrorDto(SchedulerName, SchedulerInstanceId, Message, …)</code></td></tr><tr><td><code>JobEventDto(JobKey, …)</code>, <code>JobExecutionResultDto(JobKey, …)</code>, <code>TriggerEventDto(TriggerKey, …)</code></td><td>each leads with <code>string SchedulerInstanceId</code></td></tr><tr><td><code>IQuartzDashboardHubClient.TriggerPaused(TriggerKeyDto)</code> / <code>TriggerResumed</code></td><td>take <code>TriggerLifecycleDto(SchedulerInstanceId, TriggerKey)</code></td></tr><tr><td><code>IQuartzDashboardHubClient.JobPaused(JobKeyDto)</code> / <code>JobResumed</code></td><td>take <code>JobLifecycleDto(SchedulerInstanceId, JobKey)</code></td></tr></tbody></table><p><strong><code>IDashboardHistoryStore</code> is public and is the documented persistence seam, so the three new members are a breaking change for anyone who implemented it.</strong> A store that records no misfires answers with an empty page and a count of zero; the History page renders the section saying so.</p><p>A pause and a resume get a payload of their own rather than growing <code>JobKeyDto</code> / <code>TriggerKeyDto</code>: those are keys, <code>IQuartzApiClient</code> uses them everywhere, and a key does not belong to a node.</p><p>The two new options are what bounds the shipped in-memory store. It was bounded by count alone, which says nothing about a scheduler that has gone quiet — it keeps whatever it last recorded, so its page shows executions from an arbitrary distance in the past. The window is measured on the scheduler&#39;s <code>TimeProvider</code> and applied when history is read as well as when it is written.</p><h2 id="a-scheduler-can-be-authorized-on-its-own-name" tabindex="-1"><a class="header-anchor" href="#a-scheduler-can-be-authorized-on-its-own-name"><span>A scheduler can be authorized on its own name</span></a></h2><p>Both management surfaces served every scheduler in the process behind one uniformly applied policy, so a tenant who could reach one scheduler could reach all of them. Each now takes a policy of its own that is evaluated per request against the scheduler the request is for.</p><table><thead><tr><th>4.0 preview</th><th>4.0</th></tr></thead><tbody><tr><td>—</td><td><code>Quartz.SchedulerResource(string SchedulerName)</code>, in <code>Quartz.AspNetCore</code> (new)</td></tr><tr><td>—</td><td><code>QuartzHttpApiOptions.SchedulerAuthorizationPolicy</code> (new, <code>null</code>)</td></tr><tr><td>—</td><td><code>QuartzDashboardOptions.SchedulerAuthorizationPolicy</code> (new, <code>null</code>)</td></tr></tbody></table><p>Nothing is removed and nothing changes for an application that leaves them null: the API is still whatever <code>RequireAuthorization(…)</code> the application put on the mapped group, and the dashboard is still whatever <code>QuartzDashboardOptions.AuthorizationPolicy</code> says.</p><p>Set one and Quartz asks <code>IAuthorizationService.AuthorizeAsync(user, new SchedulerResource(name), policy)</code> — ASP.NET Core&#39;s own resource-based authorization, so the application writes one <code>AuthorizationHandler&lt;TRequirement, SchedulerResource&gt;</code> and there is no Quartz callback type or claim-name convention to learn. The API answers <code>403</code> with problem details, decided before the scheduler is looked up so that a <code>404</code> only ever answers a name the caller was allowed to ask about, and filters <code>GET {ApiPath}/schedulers</code> to what the caller may act on. The dashboard filters its scheduler picker and its Schedulers page, renders a <em>not authorized</em> frame instead of a page for a scheduler the visitor fails for, and refuses the live-events hub group for it. Setting either option in a container with no authorization services is refused at startup.</p>`,72),e(`p`,null,[l[409]||=r(`See `,-1),c(g,{to:`/documentation/quartz-4.x/multi-tenancy.html#authorizing-a-tenant-on-its-own-scheduler`},{default:i(()=>[...l[408]||=[r(`Authorizing a tenant on its own scheduler`,-1)]]),_:1}),l[410]||=r(` for the handler and the registration.`,-1)]),l[896]||=t(`<h2 id="checkexists-is-exists" tabindex="-1"><a class="header-anchor" href="#checkexists-is-exists"><span><code>CheckExists</code> is <code>Exists</code></span></a></h2><p>Both overloads, on <code>IScheduler</code> and <code>IJobStore</code>:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> if (await scheduler.CheckExists(jobKey)) { … }</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> if (await scheduler.Exists(jobKey)) { … }</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>&quot;Check&quot; said only that the member does what calling it does; the return value already answers the question. The HTTP API routes (<code>…/jobs/{group}/{name}/exists</code>) are unchanged.</p><h2 id="names-that-were-normalized" tabindex="-1"><a class="header-anchor" href="#names-that-were-normalized"><span>Names that were normalized</span></a></h2><p>Renames only — the behavior behind each is unchanged, and a rename that also changes a configuration key is called out.</p><table><thead><tr><th>3.x</th><th>4.x</th></tr></thead><tbody><tr><td><code>QuartzScheduler.NumJobsExecuted</code></td><td><code>NumberOfJobsExecuted</code> (the type is internal now — read <code>IScheduler.GetMetadata()</code>)</td></tr><tr><td><code>QuartzScheduler.JobStoreClass</code>, <code>.ThreadPoolClass</code></td><td><code>JobStoreType</code>, <code>ThreadPoolType</code> (they return a <code>Type</code>; the type is internal now)</td></tr><tr><td><code>JobStoreSupport.UseDBLocks</code>, <code>.SelectWithLockSQL</code></td><td><code>UseDbLocks</code>, <code>SelectWithLockSql</code></td></tr><tr><td><code>DBSemaphore.SQL</code>, <code>.InsertSQL</code>, <code>.ExecuteSQL</code></td><td><code>LockSql</code>, <code>InsertSql</code> (both readable now), <code>ExecuteSql</code> — see <a href="#the-semaphores-are-lock-handlers">The semaphores are lock handlers</a></td></tr><tr><td><code>DbMetadata.Init()</code></td><td>Gone entirely: <code>DbMetadata</code> is an init-only record now, and the reflection its description implies happens internally instead of in a second phase you had to remember. <code>UseGenericDatabase</code>&#39;s describing overloads take a <code>Func&lt;DbMetadata&gt;</code> returning <code>new DbMetadata { … }</code>; the dead <code>ParameterIsNullableProperty</code> went too</td></tr><tr><td><code>DbMetadata.DbBinaryType</code> and <code>.ParameterDbTypeProperty</code> are internal</td><td>Everything else on the record is something you <em>say</em> about a driver; these two were the lookups Quartz then performed — an <code>Enum.Parse</code> of <code>DbBinaryTypeName</code> against <code>ParameterDbType</code>, and a <code>GetProperty(ParameterDbTypePropertyName)</code> on <code>ParameterType</code>. Describe the driver with the four naming properties, which are unchanged and public; the resolved results are Quartz&#39;s business</td></tr><tr><td><code>AdoConstants.ColumnMifireInstruction</code></td><td><code>ColumnMisfireInstruction</code> (a typo; the column name is unchanged)</td></tr><tr><td><code>SchedulerConstants.FailedJobOriginalTriggerFiretime</code></td><td><code>SchedulerConstants.FailedJobOriginalTriggerFireTime</code> (the string value is unchanged)</td></tr><tr><td><code>SchedulerConstants.FailedJobOriginalTriggerScheduledFiretime</code></td><td><code>SchedulerConstants.FailedJobOriginalTriggerScheduledFireTime</code> (the string value is unchanged)</td></tr><tr><td><code>SchedulingOptions.OverWriteExistingData</code></td><td><code>OverwriteExistingData</code>. The configuration key is spelled <code>Quartz:Scheduling:OverwriteExistingData</code> now; keys are matched case-insensitively, so an existing file keeps binding, but code assigning the property has to change</td></tr><tr><td><code>RedisSemaphore.LockTtlMilliseconds</code>, <code>.LockRetryIntervalMilliseconds</code></td><td><code>RedisLockHandler.LockTimeToLive</code>, <code>.LockRetryInterval</code>, both <code>TimeSpan</code> — <strong>also the config keys <code>lockTtlMilliseconds</code> → <code>lockTimeToLive</code> and <code>lockRetryIntervalMilliseconds</code> → <code>lockRetryInterval</code></strong></td></tr><tr><td><code>IObjectSerializer.DeSerialize</code></td><td><code>Deserialize</code>. <code>IObjectSerializer.Initialize()</code> went at the same time: a serializer builds whatever it needs on first use, and nothing was left for a separate initialization call to do</td></tr><tr><td><code>TriggerFiredBundle.PrevFireTimeUtc</code></td><td><code>PreviousFireTimeUtc</code>, matching the spelling used everywhere else. The type is a required-init record now: the eight-positional constructor ended in three interchangeable <code>DateTimeOffset?</code> values, so transposing <code>scheduledFireTimeUtc</code> and <code>previousFireTimeUtc</code> compiled cleanly and reported wrong fire times to every listener. A custom job store&#39;s <code>TriggerFired</code> writes <code>new TriggerFiredBundle { JobDetail = …, Trigger = …, Recovering = …, FireTimeUtc = …, ScheduledFireTimeUtc = …, PreviousFireTimeUtc = …, NextFireTimeUtc = … }</code>; only <code>Calendar</code> is optional</td></tr><tr><td><code>Quartz.Plugin.Xml.XMLSchedulingDataProcessorPlugin</code></td><td><code>Quartz.Plugins.Xml.XmlSchedulingDataProcessorPlugin</code> — the namespace moved and the casing follows .NET rules. A <code>quartz.plugin.&lt;name&gt;.type</code> naming either old spelling still resolves, with a warning. Its nested <code>JobFile</code> class and its <code>JobFiles</code> property are internal now: they are how the plugin tracks what it has read, not something to call. So are <code>FileNames</code>, <code>ScanInterval</code>, <code>FailOnFileNotFound</code>, <code>FailOnSchedulingError</code>, <code>Name</code> and <code>Scheduler</code>, and <code>ProcessFile</code> is private — see <a href="#the-two-scheduling-data-plugins-have-one-surface">The two scheduling-data plugins have one surface</a></td></tr><tr><td><code>XMLSchedulingDataProcessorPlugin.ProcessFile(fileName)</code></td><td><code>FileUpdated(fileName)</code>, the <code>IFileScanListener</code> member the file-scan job calls. The plugin-typed one is private</td></tr><tr><td><code>Quartz.Xml.ValidationException</code></td><td><code>Quartz.SchedulingDataValidationException</code>. The old name collided with <code>System.ComponentModel.DataAnnotations.ValidationException</code> in any file that used both, and it was never XML-specific — the JSON processor throws it too. Its <code>ValidationExceptions</code> is an <code>IReadOnlyList&lt;Exception&gt;</code>; it was a <code>List&lt;Exception&gt;</code> a caller could add to</td></tr></tbody></table><h3 id="one-spelling-per-constant" tabindex="-1"><a class="header-anchor" href="#one-spelling-per-constant"><span>One spelling per constant</span></a></h3><p>Three values were public twice under two names. The values are unchanged; one spelling of each survives, the one that sits next to the thing it describes.</p><table><thead><tr><th>Removed</th><th>Write instead</th></tr></thead><tbody><tr><td><code>SchedulerConstants.DefaultGroup</code></td><td><code>JobKey.DefaultGroup</code> / <code>TriggerKey.DefaultGroup</code> (both <code>Key&lt;T&gt;.DefaultGroup</code>, still <code>&quot;DEFAULT&quot;</code>)</td></tr><tr><td><code>AdoJobStoreOptions.DefaultTablePrefix</code></td><td><code>AdoConstants.DefaultTablePrefix</code>, alongside the table and column names it prefixes (still <code>&quot;QRTZ_&quot;</code>)</td></tr><tr><td><code>TaskSchedulingThreadPool.DefaultMaxConcurrency</code> (<code>protected</code>)</td><td><code>ThreadPoolOptions.DefaultMaxConcurrency</code>, where the option it defaults is (still <code>10</code>)</td></tr></tbody></table><h3 id="abbreviated-parameter-names-were-spelled-out" tabindex="-1"><a class="header-anchor" href="#abbreviated-parameter-names-were-spelled-out"><span>Abbreviated parameter names were spelled out</span></a></h3><p>Parameter names inherited from the Java port were spelled out across the public surface. Only named arguments and overriding signatures are affected — a positional call site compiles unchanged.</p><p><code>cal</code> → <code>calendar</code>, <code>sched</code> → <code>scheduler</code>, <code>schedName</code> → <code>schedulerName</code>, <code>calName</code> → <code>calendarName</code>, <code>schedInstId</code> → <code>schedulerInstanceId</code>, <code>triggerInstCode</code> / <code>instCode</code> → <code>triggerInstructionCode</code> / <code>instructionCode</code>, <code>jec</code> → <code>context</code>, <code>prevFireTimeUtc</code> → <code>previousFireTimeUtc</code>, <code>tz</code> / <code>timezone</code> → <code>timeZone</code> on the <code>InTimeZone</code> schedule-builder methods, and <code>je</code> → <code>jobExecutionException</code>.</p><p>Every abbreviated constructor parameter of <code>SchedulerMetaData</code> was spelled out as well, on what is now <a href="#schedulermetadata-replaces-schedulermetadata"><code>SchedulerMetadata</code></a>: <code>schedInst</code> → <code>schedulerInstanceId</code>, <code>schedType</code> → <code>schedulerType</code>, <code>numberOfJobsExec</code> → <code>numberOfJobsExecuted</code>, <code>jsType</code> → <code>jobStoreType</code>, <code>jsPersistent</code> → <code>jobStoreSupportsPersistence</code>, <code>jsClustered</code> → <code>jobStoreClustered</code>, <code>tpType</code> → <code>threadPoolType</code>, <code>tpSize</code> → <code>threadPoolSize</code>.</p><h2 id="matchers-moved-to-quartz" tabindex="-1"><a class="header-anchor" href="#matchers-moved-to-quartz"><span>Matchers moved to <code>Quartz</code></span></a></h2><p>A matcher is something you hand to <code>IScheduler</code>, not an implementation detail of one, and every signature that takes one already lives in <code>Quartz</code>, so the types moved there rather than into a namespace of their own:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> using Quartz.Impl.Matchers;</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p><code>GroupMatcher&lt;T&gt;</code>, <code>NameMatcher&lt;T&gt;</code>, <code>KeyMatcher&lt;T&gt;</code>, <code>EverythingMatcher&lt;T&gt;</code>, <code>AndMatcher&lt;T&gt;</code>, <code>OrMatcher&lt;T&gt;</code>, <code>NotMatcher&lt;T&gt;</code>, <code>StringMatcher&lt;T&gt;</code> and <code>StringOperator</code> all moved. <code>GroupMatcher&lt;T&gt;</code> and <code>NameMatcher&lt;T&gt;</code> keep their factory methods (<code>GroupEquals</code>, <code>NameStartsWith</code>, <code>AnyGroup</code>, …).</p><p><code>IMatcher&lt;T&gt;</code> no longer redeclares <code>Equals(object)</code> and <code>GetHashCode()</code>. They are <code>object</code>&#39;s own members, so declaring them on the interface added no requirement and told an implementer nothing — but a matcher is still expected to behave as a value, so that comparing two of them, or holding them in a set, answers to the shape of the matcher rather than to its identity.</p><h3 id="matchers-is-the-entry-point-combinators-are-extensions" tabindex="-1"><a class="header-anchor" href="#matchers-is-the-entry-point-combinators-are-extensions"><span><code>Matchers</code> is the entry point; combinators are extensions</span></a></h3><p>Building a matcher used to start from whichever concrete type held the factory you needed, and some of those statics ignored the class&#39;s own type parameter — <code>EverythingMatcher&lt;JobKey&gt;.AllTriggers()</code> compiled, and answered a matcher for a different key type than the one you named. The roots now live on one non-generic entry class, and the combinators are extension methods on any <code>IMatcher&lt;TKey&gt;</code>, so an expression reads left to right:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token class-name">IMatcher<span class="token punctuation">&lt;</span>JobKey<span class="token punctuation">&gt;</span></span> matcher <span class="token operator">=</span> Matchers<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">Group</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>JobKey<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span>StringOperator<span class="token punctuation">.</span>StartsWith<span class="token punctuation">,</span> <span class="token string">&quot;reporting&quot;</span><span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">.</span><span class="token function">And</span><span class="token punctuation">(</span>Matchers<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">Name</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>JobKey<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span>StringOperator<span class="token punctuation">.</span>Contains<span class="token punctuation">,</span> <span class="token string">&quot;cleanup&quot;</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">Not</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line">scheduler<span class="token punctuation">.</span>ListenerManager<span class="token punctuation">.</span><span class="token function">AddJobListener</span><span class="token punctuation">(</span>listener<span class="token punctuation">,</span> Matchers<span class="token punctuation">.</span><span class="token function">AllJobs</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><table><thead><tr><th>3.x / earlier 4.0 preview</th><th>4.0</th></tr></thead><tbody><tr><td><code>EverythingMatcher&lt;JobKey&gt;.AllJobs()</code></td><td><code>Matchers.AllJobs()</code></td></tr><tr><td><code>EverythingMatcher&lt;TriggerKey&gt;.AllTriggers()</code></td><td><code>Matchers.AllTriggers()</code></td></tr><tr><td><code>EverythingMatcher&lt;TKey&gt;.All()</code> (4.0 preview)</td><td><code>Matchers.AllJobs()</code> / <code>Matchers.AllTriggers()</code> — the factory is internal now</td></tr><tr><td><code>NameMatcher&lt;TKey&gt;.AnyName()</code> (4.0 preview)</td><td>leave the filter null; every property that takes one is nullable</td></tr><tr><td><code>KeyMatcher&lt;JobKey&gt;.KeyEquals(key)</code></td><td><code>Matchers.Key(key)</code> (overloaded for <code>JobKey</code> and <code>TriggerKey</code>)</td></tr><tr><td><code>AndMatcher&lt;JobKey&gt;.And(left, right)</code></td><td><code>left.And(right)</code></td></tr><tr><td><code>OrMatcher&lt;JobKey&gt;.Or(left, right)</code></td><td><code>left.Or(right)</code></td></tr><tr><td><code>NotMatcher&lt;JobKey&gt;.Not(matcher)</code></td><td><code>matcher.Not()</code></td></tr><tr><td>—</td><td><code>Matchers.Group&lt;TKey&gt;(StringOperator, string)</code>, <code>Matchers.Name&lt;TKey&gt;(StringOperator, string)</code></td></tr></tbody></table><p>The concrete matcher types stay public — they are what the expressions above return, and what a custom <code>IMatcher&lt;TKey&gt;</code> composes with.</p><p>That leaves two ways to build a <code>GroupMatcher</code> or a <code>NameMatcher</code>: the factory on the type (<code>GroupEquals</code>, <code>NameStartsWith</code>, …) and the root on <code>Matchers</code>. Both stay, and the split is the ruling rather than an unfinished move: a factory on the type <strong>names its comparison</strong>, which is what a call site that knows the comparison it wants should read like, while the roots on <code>Matchers</code> take the comparison as a <code>StringOperator</code> <strong>value</strong>, which is what a caller holding one read from configuration or off the wire has — the HTTP API builds every matcher it receives that way, and no method-per- operator can express it. What does <em>not</em> live on a concrete type is a root that names no comparison: <code>Matchers.AllJobs()</code>, <code>Matchers.AllTriggers()</code> and <code>Matchers.Key(key)</code> have nothing to name themselves after, which is why <code>EverythingMatcher&lt;TKey&gt;.All()</code> and <code>KeyMatcher&lt;TKey&gt;.KeyEquals()</code> are not there.</p><h3 id="stringoperator-exposes-properties-and-a-name" tabindex="-1"><a class="header-anchor" href="#stringoperator-exposes-properties-and-a-name"><span><code>StringOperator</code> exposes properties and a name</span></a></h3><p>The five built-in operators (<code>Equality</code>, <code>StartsWith</code>, <code>EndsWith</code>, <code>Contains</code>, <code>Anything</code>) are static get-only properties now; they were <code>public static readonly</code> fields. Call sites compile unchanged. <code>StringOperator</code> also gained an abstract <code>Name</code> property that discriminates the operator — it is what identifies an operator when a matcher crosses a process boundary, and what <code>ToString()</code> returns. A custom <code>StringOperator</code> subclass must now implement <code>Name</code> alongside <code>Evaluate</code>.</p><h2 id="key-t-moved-to-quartz-and-is-immutable" tabindex="-1"><a class="header-anchor" href="#key-t-moved-to-quartz-and-is-immutable"><span><code>Key&lt;T&gt;</code> moved to <code>Quartz</code> and is immutable</span></a></h2><p><code>JobKey</code> and <code>TriggerKey</code> are part of the public model, and their base type sat in a utility namespace:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> using Quartz.Util;   // for Key&lt;T&gt;</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> // Key&lt;T&gt; is in Quartz, alongside JobKey and TriggerKey</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>The <code>Name</code> and <code>Group</code> setters are gone. A key is a value, and it is a dictionary key throughout the job stores — mutating one in place could move an entry out of reach of its own hash bucket. Build a new key instead:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> jobKey.Group = &quot;reports&quot;;</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> jobKey = new JobKey(jobKey.Name, &quot;reports&quot;);</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p><code>JobKey.Create</code> is gone too; use the constructors, which is what <code>TriggerKey</code> always offered:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> JobKey key = JobKey.Create(&quot;myJob&quot;, &quot;reports&quot;);</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> JobKey key = new JobKey(&quot;myJob&quot;, &quot;reports&quot;);</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>Payloads written by earlier versions still read: both serializers build a key through its <code>(name, group)</code> constructor now, and the JSON itself is unchanged.</p><p><code>JobType</code>, the type of <code>IJobDetail.JobType</code>, moved from <code>Quartz.Impl</code> to <code>Quartz</code> for the same reason.</p><p>Keys can also be sorted now. <code>JobKey</code> and <code>TriggerKey</code> implement <code>IComparable&lt;JobKey&gt;</code> / <code>IComparable&lt;TriggerKey&gt;</code> and <code>Key&lt;T&gt;</code> implements the non-generic <code>IComparable</code>, so <code>Comparer&lt;JobKey&gt;.Default</code> finds a real comparison. On 3.x only <code>IComparable&lt;Key&lt;T&gt;&gt;</code> was there, which the default comparer does not recognise, and <code>keys.Sort()</code>, <code>keys.OrderBy(k =&gt; k)</code>, <code>SortedSet&lt;JobKey&gt;</code> and <code>SortedDictionary&lt;JobKey, _&gt;</code> all compiled and then threw at runtime. The rule that puts the default group first is unchanged; the comparison under it is not. 3.x compared group and name with <code>CultureInfo.CurrentCulture.CompareInfo</code>, and 4.x compares them with <code>StringComparer.Ordinal</code> — so a sorted listing reorders, most visibly by putting every upper-case letter before every lower-case one. The order no longer depends on the machine&#39;s culture, which is the point, but it is a different order from the one 3.x produced.</p><h2 id="listing-queries-can-filter-by-name" tabindex="-1"><a class="header-anchor" href="#listing-queries-can-filter-by-name"><span>Listing queries can filter by name</span></a></h2><table><thead><tr><th>Query</th><th>New property</th></tr></thead><tbody><tr><td><code>JobQuery</code></td><td><code>NameMatcher&lt;JobKey&gt;? Name</code></td></tr><tr><td><code>TriggerQuery</code></td><td><code>NameMatcher&lt;TriggerKey&gt;? Name</code></td></tr><tr><td><code>JobGroupQuery</code></td><td><code>NameMatcher? Name</code></td></tr><tr><td><code>TriggerGroupQuery</code></td><td><code>NameMatcher? Name</code></td></tr><tr><td><code>CalendarQuery</code></td><td><code>NameMatcher? Name</code></td></tr></tbody></table><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token class-name">PagedResult<span class="token punctuation">&lt;</span>JobHeader<span class="token punctuation">&gt;</span></span> nightly <span class="token operator">=</span> <span class="token keyword">await</span> scheduler<span class="token punctuation">.</span><span class="token function">QueryJobs</span><span class="token punctuation">(</span><span class="token keyword">new</span> <span class="token constructor-invocation class-name">JobQuery</span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    Group <span class="token operator">=</span> GroupMatcher<span class="token operator">&lt;</span>JobKey<span class="token operator">&gt;</span><span class="token punctuation">.</span><span class="token function">GroupEquals</span><span class="token punctuation">(</span><span class="token string">&quot;reports&quot;</span><span class="token punctuation">)</span><span class="token punctuation">,</span></span>
<span class="line">    Name <span class="token operator">=</span> NameMatcher<span class="token operator">&lt;</span>JobKey<span class="token operator">&gt;</span><span class="token punctuation">.</span><span class="token function">NameStartsWith</span><span class="token punctuation">(</span><span class="token string">&quot;nightly&quot;</span><span class="token punctuation">)</span></span>
<span class="line"><span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The filters combine with AND. <code>RAMJobStore</code> and <code>StdAdoDelegate</code> both honor them; the ADO store escapes the matcher&#39;s own wildcards in the LIKE it generates, so a job literally named <code>50%</code> is matched literally and is not a pattern. Over HTTP every one of these listings — jobs, triggers, calendars <strong>and the two group listings</strong> — takes <code>nameEquals</code>, <code>nameStartsWith</code>, <code>nameEndsWith</code> or <code>nameContains</code>, at most one.</p><h3 id="one-name-filter-in-two-arities" tabindex="-1"><a class="header-anchor" href="#one-name-filter-in-two-arities"><span>One name filter, in two arities</span></a></h3><p>A calendar and a group are identified by a bare name rather than by a <code>Key&lt;T&gt;</code>, so their filter cannot be a <code>NameMatcher&lt;TKey&gt;</code>, which is written against a key. It is <code>NameMatcher</code> — the same four factories, the same wire spellings, the same escaping, without the type parameter:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token class-name">PagedResult<span class="token punctuation">&lt;</span><span class="token keyword">string</span><span class="token punctuation">&gt;</span></span> holidays <span class="token operator">=</span> <span class="token keyword">await</span> scheduler<span class="token punctuation">.</span><span class="token function">QueryCalendarNames</span><span class="token punctuation">(</span><span class="token keyword">new</span> <span class="token constructor-invocation class-name">CalendarQuery</span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    Name <span class="token operator">=</span> NameMatcher<span class="token punctuation">.</span><span class="token function">NameStartsWith</span><span class="token punctuation">(</span><span class="token string">&quot;holiday-&quot;</span><span class="token punctuation">)</span></span>
<span class="line"><span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line"><span class="token class-name">PagedResult<span class="token punctuation">&lt;</span>JobGroup<span class="token punctuation">&gt;</span></span> tenant <span class="token operator">=</span> <span class="token keyword">await</span> scheduler<span class="token punctuation">.</span><span class="token function">QueryJobGroups</span><span class="token punctuation">(</span><span class="token keyword">new</span> <span class="token constructor-invocation class-name">JobGroupQuery</span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    Name <span class="token operator">=</span> NameMatcher<span class="token punctuation">.</span><span class="token function">NameStartsWith</span><span class="token punctuation">(</span><span class="token string">&quot;tenant-42-&quot;</span><span class="token punctuation">)</span></span>
<span class="line"><span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Two changes from the 4.0 previews come with that:</p><ul><li><strong><code>CalendarNameMatcher</code> is <code>NameMatcher</code>.</strong> The name was accurate while calendars were the only listing with a bare-name filter; it is not now that group listings have one. The two arities are the family: <code>NameMatcher&lt;TKey&gt;</code> for a name that is half of a key, <code>NameMatcher</code> for one that is not.</li><li><strong><code>JobGroupQuery.Name</code> and <code>TriggerGroupQuery.Name</code> were <code>string</code>, matched exactly.</strong> They are matchers now, so a group listing filters by prefix, suffix or substring like every other name filter — which is what a dashboard listing one tenant&#39;s groups needs. <code>NameMatcher.NameEquals(group)</code> is the old behaviour, and it still generates an <code>=</code> rather than a <code>LIKE</code>.</li></ul><p>There is no <code>AnyName()</code> on either arity: every filter that takes one is nullable, and null already means every name. <code>GroupMatcher&lt;TKey&gt;.AnyGroup()</code> remains, because the members that take a <code>GroupMatcher</code> — <code>PauseTriggerGroups</code>, <code>DeleteJobs</code>, and their kind — do not take a null.</p><p>If you wrote a dialect delegate that overrides <code>StdAdoDelegate.ToSqlLikeClause&lt;T&gt;(StringMatcher&lt;T&gt;)</code>, move the override to <code>ToSqlLikeClause(StringOperator, string)</code>. That overload is the virtual one now, so that a calendar&#39;s matcher and a key&#39;s matcher translate through the same code; the generic form forwards to it and is no longer virtual.</p><p><code>IsJobGroupPaused</code> and <code>IsTriggerGroupPaused</code> are built on the group-name filter now — a <code>NameMatcher.NameEquals</code> and <code>Take = 1</code> — so they ask the store about the one group instead of listing every paused group and searching it.</p><p><code>PagedResult&lt;T&gt;.Items</code> is an <code>IReadOnlyList&lt;T&gt;</code> rather than a <code>List&lt;T&gt;</code> — a page is a result to read, not a list to mutate. The two <code>List&lt;T&gt;</code> members a caller is most likely to have used:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> IReadOnlyList&lt;JobKey&gt; keys = result.Items.ConvertAll(x =&gt; x.Key);</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> List&lt;JobKey&gt; keys = result.Items.Select(x =&gt; x.Key).ToList();</span>
<span class="line"></span></span></span>
<span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> bool found = result.Items.Exists(x =&gt; x.Name == group);</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> bool found = result.Items.Any(x =&gt; x.Name == group);</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="ischedulerrepository-overloads-collapsed" tabindex="-1"><a class="header-anchor" href="#ischedulerrepository-overloads-collapsed"><span><code>ISchedulerRepository</code> overloads collapsed</span></a></h2><table><thead><tr><th>3.x</th><th>4.x</th></tr></thead><tbody><tr><td><code>Bind(scheduler)</code>, <code>Bind(scheduler, instanceId)</code></td><td><code>Bind(IScheduler scheduler, string? instanceId = null)</code></td></tr><tr><td><code>Lookup(name)</code>, <code>Lookup(name, instanceId)</code></td><td><code>Lookup(string schedulerName, string? instanceId = null)</code></td></tr><tr><td><code>Remove(name)</code> → <code>void</code>, <code>Remove(name, instanceId)</code> → <code>bool</code></td><td><code>Remove(string schedulerName, string? instanceId = null)</code> → <code>bool</code></td></tr></tbody></table><p>Existing calls compile unchanged. A null instance ID means the same as the old one-argument overload: bind under the scheduler&#39;s own <code>SchedulerInstanceId</code>, and look up or remove the first scheduler registered under the name. <code>Remove</code> returns <code>bool</code> in both shapes now, where the one-argument form used to return nothing. <code>LookupAll</code> and <code>LookupByName</code> are unchanged — cluster scenarios disambiguate through them.</p><h2 id="trigger-fire-state-is-read-only-on-the-interfaces" tabindex="-1"><a class="header-anchor" href="#trigger-fire-state-is-read-only-on-the-interfaces"><span>Trigger fire state is read-only on the interfaces</span></a></h2><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> int Priority { get; set; }   // ITrigger</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> int Priority { get; }</span>
<span class="line"></span></span></span>
<span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> int TimesTriggered { get; set; }   // ISimpleTrigger, ICalendarIntervalTrigger,</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> int TimesTriggered { get; }        // IDailyTimeIntervalTrigger, IRecurrenceTrigger</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>IMutableTrigger.Priority</code> still has its setter, which is what <code>TriggerBuilder.WithPriority</code> and the job stores go through. <code>TimesTriggered</code> is fire-count state the scheduler maintains; the concrete <code>SimpleTriggerImpl</code>, <code>CalendarIntervalTriggerImpl</code>, <code>DailyTimeIntervalTriggerImpl</code> and <code>RecurrenceTriggerImpl</code> keep their settable property. A trigger handed back by <code>IScheduler.GetTrigger</code> is a snapshot, so writing to either never reached the store to begin with — to change a stored trigger, build a new one and reschedule.</p><p>The built-in JSON trigger serializers are typed on the concrete triggers now, because they restore <code>TimesTriggered</code> when deserializing. A custom serializer deriving from one of them has to follow:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> public class MySimpleTriggerSerializer : TriggerSerializer&lt;ISimpleTrigger&gt;</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> public class MySimpleTriggerSerializer : TriggerSerializer&lt;SimpleTriggerImpl&gt;</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>This applies to <code>SimpleTriggerSerializer</code>, <code>CalendarIntervalTriggerSerializer</code>, <code>DailyTimeIntervalTriggerSerializer</code> and <code>RecurrenceTriggerSerializer</code>, in both <code>Quartz.Serialization.SystemTextJson.Triggers</code> and <code>Quartz.Serialization.Newtonsoft.Triggers</code>. <code>CronTriggerSerializer</code> is unchanged — it has no fire-count state to restore.</p><h2 id="the-trigger-family-interfaces-are-read-models" tabindex="-1"><a class="header-anchor" href="#the-trigger-family-interfaces-are-read-models"><span>The trigger family interfaces are read models</span></a></h2><p>The same reasoning removes the rest of the schedule setters from the five family interfaces. Setting one on a trigger you got back from the scheduler compiled, ran, and changed nothing: <code>RAMJobStore.GetTrigger</code> hands out <code>Trigger.Clone()</code> and the ADO.NET store materializes a fresh instance per read, so every trigger the scheduler gives you is a detached copy.</p><table><thead><tr><th>3.x</th><th>4.x</th></tr></thead><tbody><tr><td><code>int ISimpleTrigger.RepeatCount { get; set; }</code></td><td><code>{ get; }</code></td></tr><tr><td><code>TimeSpan ISimpleTrigger.RepeatInterval { get; set; }</code></td><td><code>{ get; }</code></td></tr><tr><td><code>string? ICronTrigger.CronExpressionString { get; set; }</code></td><td><code>{ get; }</code></td></tr><tr><td><code>TimeZoneInfo ICronTrigger.TimeZone { get; set; }</code></td><td><code>{ get; }</code></td></tr><tr><td><code>IntervalUnit ICalendarIntervalTrigger.RepeatIntervalUnit { get; set; }</code></td><td><code>{ get; }</code></td></tr><tr><td><code>int ICalendarIntervalTrigger.RepeatInterval { get; set; }</code></td><td><code>{ get; }</code></td></tr><tr><td><code>IReadOnlyCollection&lt;DayOfWeek&gt; IDailyTimeIntervalTrigger.DaysOfWeek { get; set; }</code></td><td><code>{ get; }</code></td></tr><tr><td><code>string IRecurrenceTrigger.RecurrenceRule { get; set; }</code></td><td><code>{ get; }</code></td></tr><tr><td><code>TimeZoneInfo IRecurrenceTrigger.TimeZone { get; set; }</code></td><td><code>{ get; }</code></td></tr></tbody></table><p>There are two sanctioned ways to change a stored trigger, and both were already public:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line"> ICronTrigger t = (ICronTrigger) await scheduler.GetTrigger(key);</span>
<span class="line"></span></span></span>
<span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> t.CronExpressionString = &quot;0 0 12 * * ?&quot;;                 // compiled, did nothing</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> ITrigger updated = t.GetTriggerBuilder()</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">     .WithCronSchedule(&quot;0 0 12 * * ?&quot;)</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">     .Build();</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line"> await scheduler.RescheduleJob(key, updated);             // reshape the schedule</span>
<span class="line"></span></span></span>
<span class="line"><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> await scheduler.UpdateTriggerDetails(key,                // edit metadata in place</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">     new TriggerDetailsUpdate().WithDescription(&quot;noon&quot;));</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Code that owns a concrete <code>CronTriggerImpl</code> / <code>SimpleTriggerImpl</code> / … is unaffected: <code>TriggerBase</code> and the concrete triggers keep their public setters, and <code>IMutableTrigger</code> — the contract job stores and custom trigger authors write through — is unchanged.</p><p><code>ICalendar</code> deliberately keeps its two setters (<code>Description</code>, <code>CalendarBase</code>). It is an implementable SPI: the built-in calendar serializers assign through the interface while rebuilding a calendar, so they are part of its contract in a way the trigger setters never were.</p><p><strong><code>ITrigger</code> no longer implements <code>IComparable&lt;ITrigger&gt;</code></strong>, and neither do the five family interfaces that re-declared it. It compared keys, which is what <code>ITrigger.Key.CompareTo</code> says out loud; ordering triggers by identity is rarely what a caller wants, and where it is, <code>Key</code> is a <code>Key&lt;T&gt;</code> with the full comparison operator set. Two call shapes change:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> if (a.CompareTo(b) &lt; 0) { … }</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> if (a.Key.CompareTo(b.Key) &lt; 0) { … }</span>
<span class="line"></span></span></span>
<span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> triggers.Sort();                       // List&lt;ITrigger&gt;, now throws InvalidOperationException</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> triggers.Sort((x, y) =&gt; x.Key.CompareTo(y.Key));</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The second one is the one to watch: <code>List&lt;ITrigger&gt;.Sort()</code>, <code>SortedSet&lt;ITrigger&gt;</code>, <code>OrderBy(t =&gt; t)</code> and anything else that reaches for <code>Comparer&lt;ITrigger&gt;.Default</code> still <strong>compile</strong>, and throw at run time. Comparing by fire time — which is usually the intent — was never what this gave you; sort on <code>NextFireTimeUtc</code> explicitly. <code>TriggerBase.CompareTo(ITrigger)</code> went with the interface.</p><p>If you author an <code>ITriggerPersistenceDelegate</code> of your own, <code>TriggerPropertyBundle</code> no longer carries the parallel <code>StatePropertyNames</code> / <code>StatePropertyValues</code> arrays that were applied to the trigger by reflection. The bundle takes the schedule builder plus an optional applier delegate, checked by the compiler instead of resolved from strings at trigger-load time:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> return new TriggerPropertyBundle(sb, [&quot;timesTriggered&quot;], [timesTriggered]);</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> return new TriggerPropertyBundle(sb, t =&gt; ((SimpleTriggerImpl) t).TimesTriggered = timesTriggered);</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>The single-argument constructor is unchanged for a delegate that carries no state beyond the schedule — the Cron delegate passes none, and a null applier is simply skipped. The lambda casts to the concrete trigger type because the family interfaces (<code>ISimpleTrigger</code> and its siblings) expose <code>TimesTriggered</code> get-only; the four <code>Quartz.Impl.Triggers</code> trigger classes stay public with public <code>TimesTriggered</code> setters precisely so this write path exists.</p><p>Registering the delegate is a typed builder call now, discoverable by dot-typing from the store builder like the driver delegate and lock handler beside it:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line">services<span class="token punctuation">.</span><span class="token function">AddQuartz</span><span class="token punctuation">(</span>q <span class="token operator">=&gt;</span> q<span class="token punctuation">.</span><span class="token function">UsePersistentStore</span><span class="token punctuation">(</span>store <span class="token operator">=&gt;</span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    store<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">UseTriggerPersistenceDelegate</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>MyTriggerPersistenceDelegate<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">    <span class="token comment">// or, when it needs configuring first:</span></span>
<span class="line">    store<span class="token punctuation">.</span><span class="token function">UseTriggerPersistenceDelegate</span><span class="token punctuation">(</span>provider <span class="token operator">=&gt;</span> <span class="token keyword">new</span> <span class="token constructor-invocation class-name">MyTriggerPersistenceDelegate</span><span class="token punctuation">(</span><span class="token range operator">..</span><span class="token punctuation">.</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"><span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The delimited <code>quartz.jobStore.driverDelegateInitString</code> format this replaces — split on <code>|</code> or <code>\\</code>, one supported setting under two spellings, type names instantiated by reflection — is gone from the API: <code>DelegateInitializationArgs.InitString</code> was replaced by a typed <code>DriverDelegateContext.TriggerPersistenceDelegates</code> collection, and <code>AdoJobStoreOptions.DriverDelegateInitString</code> went with it (as did the <code>AdoJobStoreBase</code> property mirroring it). <strong>The legacy key itself keeps working</strong>: the property bridge translates <code>quartz.jobStore.driverDelegateInitString = triggerPersistenceDelegateTypes=...</code> (and the older <code>triggerPersistenceDelegateClasses</code> spelling, with both of its list separators) into the same registrations <code>UseTriggerPersistenceDelegate&lt;T&gt;()</code> produces. A misspelled setting name inside the string is rejected at <code>AddQuartz</code> time instead of at store startup.</p><h2 id="cronexpression-is-immutable" tabindex="-1"><a class="header-anchor" href="#cronexpression-is-immutable"><span><code>CronExpression</code> is immutable</span></a></h2><p><code>CronExpression</code> always read like a value — value equality, a get-only expression string — but it was an open class with a settable <code>TimeZone</code> and a <code>protected</code> parser underneath. It is a <strong><code>sealed</code> immutable value</strong> now: the time zone arrives through a constructor or through <code>WithTimeZone</code>, which returns a retimed copy.</p><table><thead><tr><th>3.x</th><th>4.x</th></tr></thead><tbody><tr><td><code>expr.TimeZone = tz;</code></td><td><code>expr = expr.WithTimeZone(tz);</code></td></tr><tr><td><code>new CronExpression(s) { TimeZone = tz }</code></td><td><code>new CronExpression(s, tz)</code></td></tr><tr><td><code>expr.GetTimeAfter(d)</code></td><td><code>expr.GetNextValidTimeAfter(d)</code> — the two were verbatim aliases; one name remains</td></tr><tr><td><code>expr.GetFinalFireTime()</code></td><td>Removed — it was never implemented and always returned <code>null</code></td></tr><tr><td><code>expr.Clone()</code></td><td>Removed — the type is sealed and immutable, so reuse the instance</td></tr><tr><td><code>CronExpression.MaxYear</code></td><td>Removed — it was <code>DateTime.Now.Year + 100</code> evaluated once per process, so its value depended on when the process started. The cap is internal; a schedule that has to stop at a year should say which year</td></tr></tbody></table><p><code>null</code> still means the system&#39;s local time zone, and an expression that was never given a zone still serializes with the local zone&#39;s id, so persisted payloads keep their meaning.</p><p>The setter&#39;s removal also fixes a real defect: <code>CronScheduleBuilder</code> handed the <em>same</em> <code>CronExpression</code> instance to every trigger it built, so calling <code>InTimeZone</code> after the first <code>Build()</code> silently retimed triggers that were already built — and writing <code>TimeZone</code> on an expression you had passed to a builder retimed the builder behind your back. Both are now impossible: the builder reshapes its own copy, and already-built triggers keep the zone they were built with.</p><p><code>CronCalendar</code> had a defect of its own in the same plumbing: <code>new CronCalendar(baseCalendar, expression, timeZone)</code> handed the zone to <code>BaseCalendar</code> but built the expression without it, and <code>CronCalendar.TimeZone</code> reads off that expression — so the argument was dropped and the calendar excluded the <em>local</em> machine&#39;s hours while reporting the local zone back (<a href="https://github.com/quartznet/quartznet/issues/3321" target="_blank" rel="noopener noreferrer">#3321</a>). The constructor now builds the expression with the zone. Assigning <code>TimeZone</code> after construction always worked and still does, and the stored form is unchanged — the zone has always been persisted with the nested expression, so <strong>no migration is needed</strong>. What changes is which times such a calendar excludes: code that passed a zone and quietly got local-time exclusions now gets what it asked for.</p><p><code>CronTriggerImpl.FinalFireTimeUtc</code> now returns <code>null</code> directly for a trigger with no end time, which is the value it always produced through <code>GetFinalFireTime</code>.</p><p><code>Clone()</code> went with the setter. A copy of an immutable sealed value is an allocation and nothing else, so the two places in Quartz that cloned one — <code>CronCalendar.Clone</code> and <code>CronTriggerImpl.Clone</code> — share the instance instead. If you called it, drop the call.</p><h3 id="the-parser-is-not-a-subclassing-seam" tabindex="-1"><a class="header-anchor" href="#the-parser-is-not-a-subclassing-seam"><span>The parser is not a subclassing seam</span></a></h3><p>3.x&#39;s <code>CronExpression</code> was an open <code>public class</code> that handed a derived type the whole parser: eleven <code>protected const</code> field indices (<code>Second</code>, <code>Minute</code>, <code>Hour</code>, <code>DayOfMonth</code>, <code>Month</code>, <code>DayOfWeek</code>, <code>Year</code>, <code>AllSpec</code>, <code>AllSpecInt</code>, <code>NoSpec</code>, <code>NoSpecInt</code>), thirteen <code>protected</code> fields holding the parsed sets (<code>seconds</code>, <code>minutes</code>, <code>hours</code>, <code>daysOfMonth</code>, <code>months</code>, <code>daysOfWeek</code>, <code>years</code>, <code>lastdayOfWeek</code>, <code>nthdayOfWeek</code>, <code>everyNthWeek</code>, <code>calendardayOfWeek</code>, <code>calendardayOfMonth</code>, <code>expressionParsed</code>) and twelve <code>protected virtual</code> parse and arithmetic hooks (<code>AddToSet</code>, <code>BuildExpression</code>, <code>CheckNext</code>, <code>GetExpressionSetSummary</code>, <code>GetLastDayOfMonth</code>, <code>GetSet</code>, <code>GetTime</code>, <code>IsLeapYear</code>, <code>SkipWhiteSpace</code>, <code>StoreExpressionVals</code>, <code>CreateDateTimeWithoutMillis</code>, <code>SetCalendarHour</code>). Its public members were <code>virtual</code> for the same reason.</p><p>None of it survives. The type is <code>sealed</code>, so those fields are private and the <code>virtual</code> keywords are gone. The lowercase field names give the seam away as an artifact of the Java port rather than a design: it exposed the mid-parse state of a value type, and any override that touched it could leave an expression that parsed to something its own string does not say. To vary cron syntax, produce a Quartz expression string — <code>CronExpressionBuilder</code> builds one field by field — or write a trigger type of your own; do not reach into the parser.</p><p><code>CronExpression</code> also stops implementing <code>IDeserializationCallback</code>, so <code>OnDeserialization(object?)</code> is gone from its surface. It keeps <code>[Serializable]</code>, <code>ISerializable</code> and <code>GetObjectData</code>, because a <code>CronCalendar</code> inside a 3.x <code>CALENDARS</code> blob is made of one — see <a href="#serializable-survives-only-where-a-database-blob-needs-it"><code>[Serializable]</code> survives only where a database blob needs it</a>. The re-parse that <code>OnDeserialization</code> used to perform happens in the deserialization constructor now, which is where it belongs on a type whose fields are set once. Blobs written by 3.x still read back: the payload is the expression string and the time zone id, unchanged.</p><h3 id="cronexpression-parses-without-throwing-and-says-it-is-equatable" tabindex="-1"><a class="header-anchor" href="#cronexpression-parses-without-throwing-and-says-it-is-equatable"><span><code>CronExpression</code> parses without throwing, and says it is equatable</span></a></h3><p><code>IsValidExpression</code> was a <code>try</code>/<code>catch</code> around the constructor and <code>ValidateExpression</code> was a construction it discarded, which is the shape <code>TryParse</code> and <code>Parse</code> exist to replace. Those are the members now, alongside <code>IParsable&lt;CronExpression&gt;</code> — the same pair <code>JobKey</code> and <code>TriggerKey</code> already had.</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token keyword">if</span> <span class="token punctuation">(</span>CronExpression<span class="token punctuation">.</span><span class="token function">TryParse</span><span class="token punctuation">(</span>userInput<span class="token punctuation">,</span> <span class="token keyword">out</span> <span class="token class-name">CronExpression<span class="token punctuation">?</span></span> expression<span class="token punctuation">)</span><span class="token punctuation">)</span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    <span class="token comment">// expression is non-null here</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span>
<span class="line"><span class="token class-name">CronExpression</span> parsed <span class="token operator">=</span> CronExpression<span class="token punctuation">.</span><span class="token function">Parse</span><span class="token punctuation">(</span>userInput<span class="token punctuation">)</span><span class="token punctuation">;</span>   <span class="token comment">// ArgumentNullException / FormatException</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><strong>All four validation members are gone</strong>, because each restated one of the two:</p><table><thead><tr><th>3.x</th><th>4.0</th></tr></thead><tbody><tr><td><code>CronExpression.IsValidExpression(expr)</code></td><td><code>CronExpression.TryParse(expr, out _)</code></td></tr><tr><td><code>CronExpression.ValidateExpression(expr)</code></td><td><code>CronExpression.Parse(expr)</code></td></tr><tr><td><code>CronExpression.IsValidExpression(expr, hashKey)</code></td><td><code>CronExpression.TryParseWithHash(expr, hashKey, out _)</code></td></tr><tr><td><code>CronExpression.ValidateExpression(expr, hashKey)</code></td><td><code>CronExpression.ParseWithHash(expr, hashKey)</code></td></tr></tbody></table><p>The replacements hand back the expression they parsed, so validating one and then using it is a single parse rather than two. Where the old call was genuinely only a check, discard the result — <code>out _</code>, or the return value of <code>Parse</code>.</p><p>There are no <code>ReadOnlySpan&lt;char&gt;</code> overloads, deliberately: the type keeps its source string, so a span argument would only be copied back into one.</p><h3 id="the-hash-key-is-a-parse-argument-not-a-constructor-overload" tabindex="-1"><a class="header-anchor" href="#the-hash-key-is-a-parse-argument-not-a-constructor-overload"><span>The hash key is a <code>Parse</code> argument, not a constructor overload</span></a></h3><p>3.x resolved <code>H</code> (hash) tokens through two constructors, <code>CronExpression(string, string hashKey)</code> and <code>CronExpression(string, int hashSeed)</code>. Between them and <code>CronExpression(string, TimeZoneInfo?)</code> they made the null literal ambiguous, so <code>new CronExpression(expr, null)</code> — the documented way to say &quot;the system&#39;s local zone&quot; — did not compile at all. The type keeps two constructors, and the hash resolution moves to statics beside <code>Parse</code>:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token class-name">CronExpression</span> expression <span class="token operator">=</span> CronExpression<span class="token punctuation">.</span><span class="token function">ParseWithHash</span><span class="token punctuation">(</span><span class="token string">&quot;0 H H(0-7) * * ?&quot;</span><span class="token punctuation">,</span> <span class="token string">&quot;nightly-cleanup&quot;</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"><span class="token class-name">CronExpression</span> seeded <span class="token operator">=</span> CronExpression<span class="token punctuation">.</span><span class="token function">ParseWithHash</span><span class="token punctuation">(</span><span class="token string">&quot;0 H H(0-7) * * ?&quot;</span><span class="token punctuation">,</span> <span class="token number">42</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line"><span class="token keyword">if</span> <span class="token punctuation">(</span>CronExpression<span class="token punctuation">.</span><span class="token function">TryParseWithHash</span><span class="token punctuation">(</span>userInput<span class="token punctuation">,</span> <span class="token string">&quot;nightly-cleanup&quot;</span><span class="token punctuation">,</span> <span class="token keyword">out</span> <span class="token class-name">CronExpression<span class="token punctuation">?</span></span> parsed<span class="token punctuation">)</span><span class="token punctuation">)</span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    <span class="token comment">// parsed is non-null here</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span>
<span class="line"><span class="token class-name">CronExpression</span> local <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token constructor-invocation class-name">CronExpression</span><span class="token punctuation">(</span><span class="token string">&quot;0 H H(0-7) * * ?&quot;</span><span class="token punctuation">,</span> <span class="token keyword">null</span><span class="token punctuation">)</span><span class="token punctuation">;</span>   <span class="token comment">// compiles now</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Resolving <code>H</code> <strong>is</strong> a parse — it reads the expression, and it throws <code>FormatException</code> when the expression is malformed — so <code>ParseWithHash</code> is where it belongs. <code>CronExpression.ResolveHash</code>, which returns the resolved string rather than an expression, is unchanged.</p><p>Equality was already implemented; it is now declared. <code>CronExpression</code> states <code>IEquatable&lt;CronExpression&gt;</code>, and <code>Equals</code> takes a nullable argument, as the contract requires. <code>GetHashCode</code> hashes the <code>TimeZone</code> property that <code>Equals</code> compares rather than the nullable backing field, so an expression that was never given a zone and one given <code>TimeZoneInfo.Local</code> explicitly no longer hash apart while comparing equal.</p><h3 id="the-derived-calendars-declare-the-equality-they-implement" tabindex="-1"><a class="header-anchor" href="#the-derived-calendars-declare-the-equality-they-implement"><span>The derived calendars declare the equality they implement</span></a></h3><p><code>AnnualCalendar</code>, <code>CronCalendar</code>, <code>DailyCalendar</code>, <code>HolidayCalendar</code>, <code>MonthlyCalendar</code> and <code>WeeklyCalendar</code> each had a <code>public bool Equals(X obj)</code> that no interface asked for and that could not be handed a <code>null</code>. Each declares <code>IEquatable&lt;X&gt;</code> now, with the nullable parameter that entails — matching <code>BaseCalendar</code>, which always did it correctly. Existing calls compile unchanged.</p><p>All six are also <code>sealed</code>, so there is nothing left to override on them; a calendar of your own derives from <code>BaseCalendar</code>, which stays open and needs the <code>?</code> on its <code>Equals</code> parameter — see <a href="#the-shipped-implementations-are-sealed">The shipped implementations are sealed</a>.</p><h3 id="cronexpressionbuilder-s-list-fields-take-a-span" tabindex="-1"><a class="header-anchor" href="#cronexpressionbuilder-s-list-fields-take-a-span"><span><code>CronExpressionBuilder</code>&#39;s list fields take a span</span></a></h3><p>The seven members that take a list of field values — <code>WithSeconds</code>, <code>WithMinutes</code>, <code>WithHours</code>, <code>WithDaysOfMonth</code>, <code>WithMonths</code>, <code>WithDaysOfWeek</code>, <code>WithYears</code> — gained <code>params ReadOnlySpan&lt;…&gt;</code> overloads, so the common call no longer allocates an array per field. The <code>params</code> array overloads stay, so an argument that is already a collection still binds. Nothing at an existing call site changes: C# 13 prefers the span overload in expanded form, and both do the same thing.</p><h3 id="itrigger-is-quartz-implemented" tabindex="-1"><a class="header-anchor" href="#itrigger-is-quartz-implemented"><span><code>ITrigger</code> is Quartz-implemented</span></a></h3><p>The read-model split makes explicit what was always true operationally: Quartz owns the implementations of <code>ITrigger</code>. Build triggers with <code>TriggerBuilder</code>; a custom trigger type derives from <code>TriggerBase</code>, which carries the mutable and operational contracts the scheduler and the stores need. An object that implements only <code>ITrigger</code> cannot be scheduled — <code>ScheduleJob</code> and <code>RescheduleJob</code> used to fail on it with an <code>InvalidCastException</code> from inside the scheduler; they now reject it with a <code>SchedulerException</code> that names the type and says what to do instead. <code>ITrigger</code>&#39;s own documentation states the rule.</p><p><code>IJobStore.ScheduleJobs</code> follows the rest of the store contract and takes <code>IReadOnlyDictionary&lt;IJobDetail, IReadOnlyCollection&lt;IOperableTrigger&gt;&gt;</code> — the scheduler validates and downcasts the caller&#39;s triggers before the store sees them, so a custom store no longer casts each <code>ITrigger</code> itself. <code>IScheduler.ScheduleJobs</code> is unchanged.</p><h2 id="the-trigger-implementations-construct-then-initialize" tabindex="-1"><a class="header-anchor" href="#the-trigger-implementations-construct-then-initialize"><span>The trigger implementations construct, then initialize</span></a></h2><p>The five <code>*TriggerImpl</code> types carried thirty-four constructors between them, spelling out every combination of name, group, job name, job group, start time, end time and schedule. Every value they set is a settable property, so each combination was a second way to say the same thing — and the longest of them took nine positional arguments, most of which read as anonymous at the call site.</p><p>Each type now has one no-settings constructor taking an optional <code>TimeProvider</code>, and at most one convenience constructor. Everything else is an object initializer.</p><table><thead><tr><th>Type</th><th>Constructors before</th><th>Constructors now</th></tr></thead><tbody><tr><td><code>SimpleTriggerImpl</code></td><td>11</td><td><code>(TimeProvider? = null)</code>, and <code>(name, group, jobName, jobGroup, startTimeUtc, endTimeUtc, repeatCount, repeatInterval, TimeProvider? = null)</code></td></tr><tr><td><code>CronTriggerImpl</code></td><td>9</td><td><code>(TimeProvider? = null)</code>, and <code>(name, group, cronExpression, TimeProvider? = null)</code></td></tr><tr><td><code>CalendarIntervalTriggerImpl</code></td><td>6</td><td><code>(TimeProvider? = null)</code></td></tr><tr><td><code>DailyTimeIntervalTriggerImpl</code></td><td>6</td><td><code>(TimeProvider? = null)</code></td></tr><tr><td><code>RecurrenceTriggerImpl</code></td><td>2</td><td><code>(TimeProvider? = null)</code>, and <code>(name, group, recurrenceRule, TimeProvider? = null)</code></td></tr><tr><td><code>TriggerBase</code> (protected)</td><td>5</td><td><code>(TimeProvider? = null)</code></td></tr></tbody></table><p>Every surviving convenience constructor takes a <strong>non-nullable</strong> <code>group</code>. <code>RecurrenceTriggerImpl</code> is the one that had a <code>string?</code> there, so <code>new RecurrenceTriggerImpl(name, null, rule)</code> becomes a nullability warning — an error under <code>TreatWarningsAsErrors</code>. Name the group you meant, or <code>TriggerKey.DefaultGroup</code> if you had none, or use the object initializer and set <code>Key</code>.</p><p>The recipe is mechanical: the name and group become <code>Key</code>, the job name and group become <code>JobKey</code>, and the rest keep their property names.</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> var trigger = new SimpleTriggerImpl(&quot;nightly&quot;, &quot;reports&quot;, startAt, endAt, 5, TimeSpan.FromHours(1));</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> var trigger = new SimpleTriggerImpl</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line"> {</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">     Key = new TriggerKey(&quot;nightly&quot;, &quot;reports&quot;),</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">     StartTimeUtc = startAt,</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">     EndTimeUtc = endAt,</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">     RepeatCount = 5,</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">     RepeatInterval = TimeSpan.FromHours(1)</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line"> };</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Set <code>StartTimeUtc</code> before <code>EndTimeUtc</code>: each setter validates against the other, exactly as the constructors did in that order.</p><p>One behavioural detail to carry over. The overloads that took no start time — <code>SimpleTriggerImpl(name)</code>, <code>(name, group)</code>, <code>(name, repeatCount, repeatInterval)</code>, <code>(name, group, repeatCount, repeatInterval)</code>, and the <code>CalendarIntervalTriggerImpl</code> / <code>DailyTimeIntervalTriggerImpl</code> equivalents — set <code>StartTimeUtc</code> to <em>now</em> rather than leaving it at its default. Write that out if you relied on it:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> var trigger = new SimpleTriggerImpl(&quot;nightly&quot;, &quot;reports&quot;);</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> var trigger = new SimpleTriggerImpl</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line"> {</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">     Key = new TriggerKey(&quot;nightly&quot;, &quot;reports&quot;),</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">     StartTimeUtc = TimeProvider.System.GetUtcNow()</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line"> };</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>CronTriggerImpl</code>&#39;s surviving constructors already set <code>StartTimeUtc</code> to the time provider&#39;s now and <code>TimeZone</code> to <code>TimeZoneInfo.Local</code>, as they always did.</p><p><code>SimpleTriggerImpl</code> and <code>CalendarIntervalTriggerImpl</code> had both a parameterless constructor and a <code>TimeProvider</code> one; those merged into a single <code>(TimeProvider? timeProvider = null)</code>, matching the shape <code>CronTriggerImpl</code> and <code>DailyTimeIntervalTriggerImpl</code> already had. <code>new SimpleTriggerImpl()</code> still compiles. What no longer works is a <code>where T : new()</code> constraint or <code>Activator.CreateInstance(type)</code> over these types — an all-optional constructor is not a parameterless one as far as the runtime is concerned. Derive a type of your own and it gets an implicit parameterless constructor, which satisfies both again.</p><p>The <code>[Serializable]</code> blob contract pins fields, not constructors, so persisted binary payloads are unaffected.</p><h2 id="nine-usingjobdata-overloads-became-one" tabindex="-1"><a class="header-anchor" href="#nine-usingjobdata-overloads-became-one"><span>Nine <code>UsingJobData</code> overloads became one</span></a></h2><p><code>JobDataMap</code>&#39;s indexer takes an <code>object?</code>, and every one of the nine primitive <code>UsingJobData</code> overloads had the same one-line body writing through it. The overload set decided nothing except which of nine identical methods the compiler picked, and it cost forty-four declarations across <code>IJobConfigurator&lt;TJob&gt;</code>, <code>JobBuilder&lt;TJob&gt;</code>, <code>ITriggerConfigurator&lt;TJob&gt;</code> and <code>TriggerBuilder&lt;TJob&gt;</code>. There are twelve now.</p><table><thead><tr><th>3.x</th><th>4.x</th></tr></thead><tbody><tr><td><code>UsingJobData(string key, string? value)</code></td><td><code>UsingJobData(string key, object? value)</code></td></tr><tr><td><code>UsingJobData(string key, int value)</code></td><td><code>UsingJobData(string key, object? value)</code></td></tr><tr><td><code>UsingJobData(string key, long value)</code></td><td><code>UsingJobData(string key, object? value)</code></td></tr><tr><td><code>UsingJobData(string key, float value)</code></td><td><code>UsingJobData(string key, object? value)</code></td></tr><tr><td><code>UsingJobData(string key, double value)</code></td><td><code>UsingJobData(string key, object? value)</code></td></tr><tr><td><code>UsingJobData(string key, decimal value)</code></td><td><code>UsingJobData(string key, object? value)</code></td></tr><tr><td><code>UsingJobData(string key, bool value)</code></td><td><code>UsingJobData(string key, object? value)</code></td></tr><tr><td><code>UsingJobData(string key, Guid value)</code></td><td><code>UsingJobData(string key, object? value)</code></td></tr><tr><td><code>UsingJobData(string key, char value)</code></td><td><code>UsingJobData(string key, object? value)</code></td></tr><tr><td><code>UsingJobData(JobDataMap newJobDataMap)</code></td><td>unchanged — merges into what the builder already holds</td></tr><tr><td><code>UsingJobData&lt;TValue&gt;(Expression&lt;Func&lt;TJob, TValue&gt;&gt; jobProperty, TValue value)</code></td><td>unchanged</td></tr><tr><td><code>SetJobData(JobDataMap newJobDataMap)</code></td><td>removed</td></tr></tbody></table><p><strong>Existing calls compile and store exactly what they stored before.</strong> An <code>int</code> argument still lands in the map boxed as an <code>int</code>, a <code>Guid</code> as a <code>Guid</code>, a <code>null</code> as a <code>null</code>. Nothing is converted on the way in, and what a persistent store can hold is still whatever its serializer round-trips — AdoJobStore&#39;s string-only mode, strings only.</p><p>To store a value in a job property&#39;s own type — an <code>int</code> literal narrowed to the <code>byte</code> the property declares, an enum written as its name — name the property instead of its key:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line">JobBuilder<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">Create</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>MyJob<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">UsingJobData</span><span class="token punctuation">(</span>job <span class="token operator">=&gt;</span> job<span class="token punctuation">.</span>RetryCount<span class="token punctuation">,</span> <span class="token number">3</span><span class="token punctuation">)</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><h3 id="setjobdata-is-gone" tabindex="-1"><a class="header-anchor" href="#setjobdata-is-gone"><span><code>SetJobData</code> is gone</span></a></h3><p><code>SetJobData</code> <em>replaced</em> the builder&#39;s map where <code>UsingJobData(JobDataMap)</code> merges into it: one character of difference in the call, the opposite meaning. Replacing is only what a job store rebuilding a stored job wants, so it is internal now.</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> JobBuilder.Create&lt;MyJob&gt;().UsingJobData(&quot;a&quot;, 1).SetJobData(map)   // &quot;a&quot; silently discarded</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> JobBuilder.Create&lt;MyJob&gt;().UsingJobData(map)                      // merge, or start from a fresh builder</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="one-family-of-withxschedule-extensions" tabindex="-1"><a class="header-anchor" href="#one-family-of-withxschedule-extensions"><span>One family of <code>WithXSchedule</code> extensions</span></a></h2><p>Attaching a schedule to a trigger was spread over six static extension classes and twenty-nine methods, half of them existing only because <code>TriggerBuilder&lt;TJob&gt;</code> and <code>ITriggerConfigurator&lt;TJob&gt;</code> each needed their own copy of the same body. <code>Quartz.TriggerConfiguratorExtensions</code> replaces all six with twelve methods that are generic in the receiver and return it unchanged, so one method serves both and the chain keeps its type.</p><p>Deleted: <code>SimpleScheduleTriggerBuilderExtensions</code>, <code>CronScheduleTriggerBuilderExtensions</code>, <code>CalendarIntervalTriggerBuilderExtensions</code>, <code>DailyTimeIntervalTriggerBuilderExtensions</code>, <code>RecurrenceTriggerBuilderExtensions</code>, <code>TriggerExtensions</code>.</p><table><thead><tr><th>3.x</th><th>4.x</th></tr></thead><tbody><tr><td><code>WithSimpleSchedule()</code></td><td><code>WithSimpleSchedule()</code></td></tr><tr><td><code>WithSimpleSchedule(Action&lt;SimpleScheduleBuilder&gt;)</code></td><td><code>WithSimpleSchedule(Action&lt;SimpleScheduleBuilder&gt;? configure = null)</code></td></tr><tr><td><code>WithSimpleSchedule(SimpleScheduleBuilder)</code></td><td><code>WithSimpleSchedule(SimpleScheduleBuilder schedule)</code></td></tr><tr><td><code>WithCronSchedule(string)</code></td><td><code>WithCronSchedule(string cronExpression, Action&lt;CronScheduleBuilder&gt;? configure = null)</code></td></tr><tr><td><code>WithCronSchedule(string, Action&lt;CronScheduleBuilder&gt;)</code></td><td>same member</td></tr><tr><td><code>WithCronSchedule(string expr, string hashKey)</code></td><td><code>WithCronSchedule(CronExpression.ParseWithHash(expr, hashKey))</code></td></tr><tr><td><code>WithCronSchedule(string expr, string hashKey, Action&lt;CronScheduleBuilder&gt;)</code></td><td><code>WithCronSchedule(CronExpression.ParseWithHash(expr, hashKey), configure)</code></td></tr><tr><td><code>WithCronSchedule(CronScheduleBuilder)</code></td><td><code>WithCronSchedule(CronScheduleBuilder schedule)</code></td></tr><tr><td>—</td><td><code>WithCronSchedule(CronExpression cronExpression, Action&lt;CronScheduleBuilder&gt;? configure = null)</code> — new; also the home of the hash-key shape</td></tr><tr><td>—</td><td><code>WithCronSchedule(CronExpressionBuilder cronExpression, Action&lt;CronScheduleBuilder&gt;? configure = null)</code> — new; closes the <a href="#cronschedulebuilder-s-convenience-factories-are-gone"><code>CronExpressionBuilder</code></a> chain without naming <code>CronScheduleBuilder</code></td></tr><tr><td><code>WithCalendarIntervalSchedule()</code></td><td><code>WithCalendarIntervalSchedule(Action&lt;CalendarIntervalScheduleBuilder&gt;? configure = null)</code></td></tr><tr><td><code>WithCalendarIntervalSchedule(Action&lt;CalendarIntervalScheduleBuilder&gt;)</code></td><td>same member</td></tr><tr><td><code>WithCalendarIntervalSchedule(CalendarIntervalScheduleBuilder)</code></td><td><code>WithCalendarIntervalSchedule(CalendarIntervalScheduleBuilder schedule)</code></td></tr><tr><td><code>WithDailyTimeIntervalSchedule()</code></td><td><code>WithDailyTimeIntervalSchedule(Action&lt;DailyTimeIntervalScheduleBuilder&gt;? configure = null)</code></td></tr><tr><td><code>WithDailyTimeIntervalSchedule(Action&lt;DailyTimeIntervalScheduleBuilder&gt;)</code></td><td>same member</td></tr><tr><td><code>WithDailyTimeIntervalSchedule(int interval, IntervalUnit unit, Action&lt;…&gt;? action = null)</code></td><td><code>WithDailyTimeIntervalSchedule(x =&gt; x.WithInterval(interval, unit))</code></td></tr><tr><td><code>WithDailyTimeIntervalSchedule(DailyTimeIntervalScheduleBuilder)</code></td><td><code>WithDailyTimeIntervalSchedule(DailyTimeIntervalScheduleBuilder schedule)</code></td></tr><tr><td><code>WithRecurrenceSchedule(string)</code></td><td><code>WithRecurrenceSchedule(string recurrenceRule, Action&lt;RecurrenceScheduleBuilder&gt;? configure = null)</code></td></tr><tr><td><code>WithRecurrenceSchedule(string, Action&lt;RecurrenceScheduleBuilder&gt;)</code></td><td>same member</td></tr><tr><td><code>WithRecurrenceSchedule(RecurrenceScheduleBuilder)</code></td><td><code>WithRecurrenceSchedule(RecurrenceScheduleBuilder schedule)</code></td></tr></tbody></table><p>Only two call shapes need editing:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> .WithDailyTimeIntervalSchedule(interval: 10, intervalUnit: IntervalUnit.Second)</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> .WithDailyTimeIntervalSchedule(x =&gt; x.WithInterval(10, IntervalUnit.Second))</span>
<span class="line"></span></span></span>
<span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> .WithCronSchedule(&quot;0 H H(0-7) * * ?&quot;, &quot;nightly-cleanup&quot;)</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> .WithCronSchedule(CronExpression.ParseWithHash(&quot;0 H H(0-7) * * ?&quot;, &quot;nightly-cleanup&quot;))</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The hash-key overloads went because a hash key belongs to the expression, not to the way the expression is attached to a trigger — <a href="#the-hash-key-is-a-parse-argument-not-a-constructor-overload"><code>CronExpression.ParseWithHash</code></a> takes it, and the <code>CronExpression</code>-taking overload carries the result. Without a key, <code>H</code> tokens still hash on the trigger&#39;s identity.</p><h3 id="itriggerconfigurator-gained-a-non-generic-base" tabindex="-1"><a class="header-anchor" href="#itriggerconfigurator-gained-a-non-generic-base"><span><code>ITriggerConfigurator</code> gained a non-generic base</span></a></h3><p>The extensions are written against a new non-generic <code>ITriggerConfigurator</code>, which holds the one member they need:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token keyword">public</span> <span class="token keyword">interface</span> <span class="token class-name">ITriggerConfigurator</span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    <span class="token return-type class-name">ITriggerConfigurator</span> <span class="token function">WithSchedule</span><span class="token punctuation">(</span><span class="token class-name">IScheduleBuilder</span> scheduleBuilder<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span>
<span class="line"><span class="token keyword">public</span> <span class="token keyword">interface</span> <span class="token class-name">ITriggerConfigurator<span class="token punctuation">&lt;</span>TJob<span class="token punctuation">&gt;</span></span> <span class="token punctuation">:</span> <span class="token type-list"><span class="token class-name">ITriggerConfigurator</span></span> <span class="token keyword">where</span> <span class="token class-name">TJob</span> <span class="token punctuation">:</span> <span class="token type-list"><span class="token class-name">IJob</span></span> <span class="token punctuation">{</span> … <span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The generic interface redeclares <code>WithSchedule</code> with its own return type, so a chain there keeps <code>TJob</code> and the job-property overload of <code>UsingJobData</code> that comes with it. Code implementing <code>ITriggerConfigurator&lt;TJob&gt;</code> is unaffected; <code>TriggerBuilder&lt;TJob&gt;</code> implements both.</p><h2 id="modifiedbycalendar-is-withcalendarname" tabindex="-1"><a class="header-anchor" href="#modifiedbycalendar-is-withcalendarname"><span><code>ModifiedByCalendar</code> is <code>WithCalendarName</code></span></a></h2><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line"> ITrigger trigger = TriggerBuilder.Create()</span>
<span class="line"></span><span class="token prefix unchanged"> </span><span class="token line">     .WithIdentity(&quot;trigger1&quot;)</span>
<span class="line"></span></span><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line">     .ModifiedByCalendar(&quot;myHolidays&quot;)</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line">     .WithCalendarName(&quot;myHolidays&quot;)</span>
<span class="line"></span></span><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line">     .Build();</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>It sets <code>ITrigger.CalendarName</code> and every other setter on the builder is named for the property it sets. The rename applies to <code>TriggerBuilder&lt;TJob&gt;</code> and <code>ITriggerConfigurator&lt;TJob&gt;</code> alike. The old name also read as though it modified the calendar.</p><h2 id="the-preferred-node-is-a-value" tabindex="-1"><a class="header-anchor" href="#the-preferred-node-is-a-value"><span>The preferred node is a value</span></a></h2><p>Node affinity was two properties that only made sense read together: a <code>string?</code> in which <code>&quot;*&quot;</code> meant something other than a node name, and a <code>bool</code> that meant nothing unless the string was one. Copying a pin from one trigger to another through the setter silently dropped the flag, turning an auto-claim into a named pin that would never fail over. <code>Quartz.PreferredNode</code> — a <code>readonly record struct</code> — carries both.</p><table><thead><tr><th>3.x</th><th>4.x</th></tr></thead><tbody><tr><td><code>string? ITrigger.PreferredNode</code></td><td><code>PreferredNode ITrigger.PreferredNode</code></td></tr><tr><td><code>bool ITrigger.IsPreferredNodeAuto</code></td><td><code>trigger.PreferredNode.IsAutomatic</code></td></tr><tr><td><code>trigger.PreferredNode</code> (the node name)</td><td><code>trigger.PreferredNode.Node</code> — null for no pin <em>and</em> for an unclaimed auto-pin</td></tr><tr><td><code>trigger.PreferredNode is null</code></td><td><code>trigger.PreferredNode.IsNone</code></td></tr><tr><td><code>trigger.PreferredNode == &quot;*&quot;</code></td><td><code>trigger.PreferredNode == PreferredNode.Auto</code></td></tr><tr><td><code>WithPreferredNode(null)</code></td><td><code>WithPreferredNode(PreferredNode.None)</code></td></tr><tr><td><code>WithPreferredNode(&quot;*&quot;)</code></td><td><code>WithPreferredNode(PreferredNode.Auto)</code></td></tr><tr><td><code>WithPreferredNode(&quot;node-1&quot;)</code></td><td><code>WithPreferredNode(PreferredNode.For(&quot;node-1&quot;))</code></td></tr><tr><td><code>new TriggerDetailsUpdate().WithPreferredNode(string?)</code></td><td><code>.WithPreferredNode(PreferredNode)</code></td></tr><tr><td><code>IMutableTrigger.PreferredNode { get; set; }</code> → <code>string?</code></td><td>→ <code>PreferredNode</code></td></tr></tbody></table><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> .WithPreferredNode(&quot;production-node-1&quot;)</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> .WithPreferredNode(PreferredNode.For(&quot;production-node-1&quot;))</span>
<span class="line"></span></span></span>
<span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> string? node = t.PreferredNode;</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"> bool auto = t.IsPreferredNodeAuto;</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> string? node = t.PreferredNode.Node;</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line"> bool auto = t.PreferredNode.IsAutomatic;</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>PreferredNode.For</code> trims its argument and rejects a blank one and the pinning protocol&#39;s own markers (<code>*</code>, <code>_</code>, <code>null</code>) — names that could never identify a node, and which used to be accepted and then quietly mean something else. Any other name is legal; the node name is stored verbatim, so <code>auto:thing</code> or <code>*-west</code> is fine. Assigning a pin records it as the pin it was, auto-claim included, so copying one between triggers is lossless.</p><p><strong>Storage is unchanged.</strong> <code>QRTZ_TRIGGERS.PREFERRED_NODE</code> and <code>PREFERRED_NODE_AUTO</code> still hold the string and the flag; the mapping happens at the store boundary and the sentinel is an internal constant rather than something a caller spells. Databases written by 3.19&#39;s node-affinity migration or by an earlier 4.0 preview read back identically, and the JSON trigger payloads never carried the pin.</p><h2 id="misfire-instructions-are-enums" tabindex="-1"><a class="header-anchor" href="#misfire-instructions-are-enums"><span>Misfire instructions are enums</span></a></h2><p>Each schedule builder had one no-argument method per misfire policy — eighteen of them across five builders, with a slightly different vocabulary each. A method name is a poor place to keep a value: it cannot be read from configuration, cannot be switched on, and cannot be defaulted. Every builder now has one <code>WithMisfireInstruction</code> taking its family&#39;s enum.</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line"> .WithSimpleSchedule(x =&gt; x</span>
<span class="line"></span><span class="token prefix unchanged"> </span><span class="token line">     .WithInterval(TimeSpan.FromMinutes(5))</span>
<span class="line"></span><span class="token prefix unchanged"> </span><span class="token line">     .RepeatForever()</span>
<span class="line"></span></span><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line">     .WithMisfireHandlingInstructionNextWithExistingCount())</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line">     .WithMisfireInstruction(SimpleTriggerMisfireInstruction.NextWithExistingCount))</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Earlier 4.0 previews spelled that method <code>WithMisfireHandlingInstruction</code>. It is <code>WithMisfireInstruction</code> now, on all five builders, matching <code>TriggerDetailsUpdate.WithMisfireInstruction</code> and the XML element name.</p><h3 id="simpleschedulebuilder" tabindex="-1"><a class="header-anchor" href="#simpleschedulebuilder"><span>SimpleScheduleBuilder</span></a></h3><table><thead><tr><th>3.x</th><th>4.x</th></tr></thead><tbody><tr><td><code>WithMisfireHandlingInstructionIgnoreMisfires()</code></td><td><code>WithMisfireInstruction(SimpleTriggerMisfireInstruction.IgnoreMisfires)</code></td></tr><tr><td><code>WithMisfireHandlingInstructionFireNow()</code></td><td><code>WithMisfireInstruction(SimpleTriggerMisfireInstruction.FireNow)</code></td></tr><tr><td><code>WithMisfireHandlingInstructionNowWithExistingCount()</code></td><td><code>WithMisfireInstruction(SimpleTriggerMisfireInstruction.NowWithExistingCount)</code></td></tr><tr><td><code>WithMisfireHandlingInstructionNowWithRemainingCount()</code></td><td><code>WithMisfireInstruction(SimpleTriggerMisfireInstruction.NowWithRemainingCount)</code></td></tr><tr><td><code>WithMisfireHandlingInstructionNextWithRemainingCount()</code></td><td><code>WithMisfireInstruction(SimpleTriggerMisfireInstruction.NextWithRemainingCount)</code></td></tr><tr><td><code>WithMisfireHandlingInstructionNextWithExistingCount()</code></td><td><code>WithMisfireInstruction(SimpleTriggerMisfireInstruction.NextWithExistingCount)</code></td></tr><tr><td>(call nothing)</td><td><code>WithMisfireInstruction(SimpleTriggerMisfireInstruction.SmartPolicy)</code>, still the default</td></tr></tbody></table><h3 id="cronschedulebuilder" tabindex="-1"><a class="header-anchor" href="#cronschedulebuilder"><span>CronScheduleBuilder</span></a></h3><table><thead><tr><th>3.x</th><th>4.x</th></tr></thead><tbody><tr><td><code>WithMisfireHandlingInstructionIgnoreMisfires()</code></td><td><code>WithMisfireInstruction(CronTriggerMisfireInstruction.IgnoreMisfires)</code></td></tr><tr><td><code>WithMisfireHandlingInstructionFireAndProceed()</code></td><td><code>WithMisfireInstruction(CronTriggerMisfireInstruction.FireAndProceed)</code></td></tr><tr><td><code>WithMisfireHandlingInstructionDoNothing()</code></td><td><code>WithMisfireInstruction(CronTriggerMisfireInstruction.DoNothing)</code></td></tr></tbody></table><h3 id="calendarintervalschedulebuilder" tabindex="-1"><a class="header-anchor" href="#calendarintervalschedulebuilder"><span>CalendarIntervalScheduleBuilder</span></a></h3><table><thead><tr><th>3.x</th><th>4.x</th></tr></thead><tbody><tr><td><code>WithMisfireHandlingInstructionIgnoreMisfires()</code></td><td><code>WithMisfireInstruction(CalendarIntervalTriggerMisfireInstruction.IgnoreMisfires)</code></td></tr><tr><td><code>WithMisfireHandlingInstructionFireAndProceed()</code></td><td><code>WithMisfireInstruction(CalendarIntervalTriggerMisfireInstruction.FireAndProceed)</code></td></tr><tr><td><code>WithMisfireHandlingInstructionDoNothing()</code></td><td><code>WithMisfireInstruction(CalendarIntervalTriggerMisfireInstruction.DoNothing)</code></td></tr></tbody></table><h3 id="dailytimeintervalschedulebuilder" tabindex="-1"><a class="header-anchor" href="#dailytimeintervalschedulebuilder"><span>DailyTimeIntervalScheduleBuilder</span></a></h3><table><thead><tr><th>3.x</th><th>4.x</th></tr></thead><tbody><tr><td><code>WithMisfireHandlingInstructionIgnoreMisfires()</code></td><td><code>WithMisfireInstruction(DailyTimeIntervalTriggerMisfireInstruction.IgnoreMisfires)</code></td></tr><tr><td><code>WithMisfireHandlingInstructionFireAndProceed()</code></td><td><code>WithMisfireInstruction(DailyTimeIntervalTriggerMisfireInstruction.FireAndProceed)</code></td></tr><tr><td><code>WithMisfireHandlingInstructionDoNothing()</code></td><td><code>WithMisfireInstruction(DailyTimeIntervalTriggerMisfireInstruction.DoNothing)</code></td></tr></tbody></table><h3 id="recurrenceschedulebuilder" tabindex="-1"><a class="header-anchor" href="#recurrenceschedulebuilder"><span>RecurrenceScheduleBuilder</span></a></h3><table><thead><tr><th>3.x</th><th>4.x</th></tr></thead><tbody><tr><td><code>WithMisfireHandlingInstructionIgnoreMisfires()</code></td><td><code>WithMisfireInstruction(RecurrenceTriggerMisfireInstruction.IgnoreMisfires)</code></td></tr><tr><td><code>WithMisfireHandlingInstructionFireAndProceed()</code></td><td><code>WithMisfireInstruction(RecurrenceTriggerMisfireInstruction.FireAndProceed)</code></td></tr><tr><td><code>WithMisfireHandlingInstructionDoNothing()</code></td><td><code>WithMisfireInstruction(RecurrenceTriggerMisfireInstruction.DoNothing)</code></td></tr></tbody></table><h3 id="triggerdetailsupdate-takes-the-same-enums" tabindex="-1"><a class="header-anchor" href="#triggerdetailsupdate-takes-the-same-enums"><span><code>TriggerDetailsUpdate</code> takes the same enums</span></a></h3><p>The update object had a single <code>WithMisfireInstruction(int)</code>, which let a simple trigger&#39;s code be applied to a cron trigger: the number is in range for both families and means a different policy in each. It now has one overload per family, plus a code form for callers that genuinely have a number and no family — a value read off the wire, from configuration, or from a trigger.</p><table><thead><tr><th>4.0 preview</th><th>4.0</th></tr></thead><tbody><tr><td><code>.WithMisfireInstruction(2)</code></td><td><code>.WithMisfireInstruction(CronTriggerMisfireInstruction.DoNothing)</code></td></tr><tr><td><code>.WithMisfireInstruction(MisfireInstruction.CronTrigger.DoNothing)</code></td><td><code>.WithMisfireInstruction(CronTriggerMisfireInstruction.DoNothing)</code></td></tr><tr><td><code>.WithMisfireInstruction(someInt)</code></td><td><code>.WithMisfireInstructionCode(someInt)</code></td></tr></tbody></table><p>The typed overloads are the taught path: the store rejects an update whose family is not the stored trigger&#39;s, so a cron policy sent to a simple trigger is now an error rather than a silently different policy. <code>WithMisfireInstructionCode</code> keeps only the range check the trigger itself applies.</p><h3 id="the-enums-are-the-vocabulary" tabindex="-1"><a class="header-anchor" href="#the-enums-are-the-vocabulary"><span>The enums are the vocabulary</span></a></h3><p>A trigger&#39;s misfire policy is read from its family interface, typed. The family-agnostic number a trigger stores is still on <code>ITrigger</code>, renamed so it no longer competes with the typed property for the good name, and <code>IMutableTrigger</code> still carries the settable one.</p><table><thead><tr><th>3.x</th><th>4.x</th></tr></thead><tbody><tr><td><code>int ITrigger.MisfireInstruction { get; }</code></td><td><code>int ITrigger.MisfireInstructionCode { get; }</code></td></tr><tr><td><code>int IMutableTrigger.MisfireInstruction { get; set; }</code></td><td><code>int IMutableTrigger.MisfireInstructionCode { get; set; }</code></td></tr><tr><td><code>int AbstractTrigger.MisfireInstruction { get; set; }</code></td><td><code>int TriggerBase.MisfireInstructionCode { get; set; }</code></td></tr><tr><td><code>(CronTriggerMisfireInstruction) trigger.MisfireInstruction</code></td><td><code>((ICronTrigger) trigger).MisfireInstruction</code></td></tr><tr><td>(new)</td><td><code>SimpleTriggerMisfireInstruction ISimpleTrigger.MisfireInstruction { get; }</code>, and one per family</td></tr></tbody></table><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> var policy = (CronTriggerMisfireInstruction) trigger.MisfireInstruction;</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> CronTriggerMisfireInstruction policy = ((ICronTrigger) trigger).MisfireInstruction;</span>
<span class="line"></span></span></span>
<span class="line"><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line"> // still there, for code generic over every family - serializers, the wire, logging</span>
<span class="line"></span></span><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> int stored = trigger.MisfireInstruction;</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> int stored = trigger.MisfireInstructionCode;</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>An enum member&#39;s underlying value <em>is</em> the code, so the two convert freely, and the numbers in <code>QRTZ_TRIGGERS.MISFIRE_INSTR</code> and in JSON trigger payloads are unchanged.</p><p><strong>The <code>MisfireInstruction</code> constant class is internal.</strong> Its members were a third spelling of a vocabulary that already had two, and the enums cover all five families where it covered them unevenly. Replace <code>MisfireInstruction.CronTrigger.DoNothing</code> with <code>CronTriggerMisfireInstruction.DoNothing</code>, <code>MisfireInstruction.SimpleTrigger.RescheduleNowWithExistingRepeatCount</code> with <code>SimpleTriggerMisfireInstruction.NowWithExistingCount</code>, <code>MisfireInstruction.IgnoreMisfirePolicy</code> with the family&#39;s <code>IgnoreMisfires</code>, and <code>MisfireInstruction.SmartPolicy</code> with the family&#39;s <code>SmartPolicy</code>.</p><h3 id="the-xml-and-json-names-are-resolved-per-family" tabindex="-1"><a class="header-anchor" href="#the-xml-and-json-names-are-resolved-per-family"><span>The XML and JSON names are resolved per family</span></a></h3><p>Both scheduling-data readers used to resolve a misfire instruction name by reflecting over the constant class and <em>all</em> of its nested types at once, so any family&#39;s name resolved for any family&#39;s trigger. In JSON, which has no schema to catch it, a cron trigger configured with <code>&quot;MisfireInstruction&quot;: &quot;RescheduleNowWithExistingRepeatCount&quot;</code> became <code>DoNothing</code> — both are 2 — and nothing said so. Explicit per-family maps replace the reflection.</p><p>Every name that parses today still parses, and each family additionally accepts its own enum member names, so <code>FireAndProceed</code> and <code>NowWithExistingCount</code> can be written in configuration as well as in code. A name belonging to another family is still resolved when the code is legal for this one, but it is logged as a warning naming the policy the value actually selects; a name whose code is out of the family&#39;s range is rejected with a message listing the names that work, where it used to fail later with &quot;The misfire instruction code is invalid for this type of trigger&quot;.</p><p>The XML processor&#39;s <code>ReadMisfireInstructionFromString</code> was <code>protected virtual</code> and is now private — as is the processor itself: it could not tell the families apart, because it was not told which one it was reading. XML itself is unaffected either way — <code>job_scheduling_data_2_0.xsd</code> restricts <code>misfire-instruction</code> per trigger type, so the schema rejects a foreign name before the resolver sees it.</p><h2 id="every-builder-starts-with-create" tabindex="-1"><a class="header-anchor" href="#every-builder-starts-with-create"><span>Every builder starts with <code>Create</code></span></a></h2><p>3.x taught a different way to start each builder: <code>Create()</code> on some, <code>CronSchedule(...)</code> on <code>CronScheduleBuilder</code>, <code>NewDate()</code> on <code>DateBuilder</code>, <code>new</code> on others. In 4.x every builder in the DSL is reached the same way — a static <code>Create(...)</code> taking only what cannot be defaulted.</p><table><thead><tr><th>3.x</th><th>4.x</th></tr></thead><tbody><tr><td><code>CronScheduleBuilder.CronSchedule(string)</code></td><td><code>CronScheduleBuilder.Create(string)</code></td></tr><tr><td><code>CronScheduleBuilder.CronSchedule(CronExpression)</code></td><td><code>CronScheduleBuilder.Create(CronExpression)</code></td></tr><tr><td><code>DateBuilder.NewDate()</code></td><td><code>DateBuilder.Create()</code></td></tr><tr><td><code>DateBuilder.NewDateInTimeZone(tz)</code></td><td><code>DateBuilder.CreateInTimeZone(tz)</code></td></tr><tr><td><code>JobBuilder.Create(Type)</code></td><td><code>JobBuilder.Create().OfType(type)</code></td></tr></tbody></table><p><code>ExecutionLimitsBuilder</code>, new in 4.0, follows the same convention: its constructor is no longer public, so <code>new ExecutionLimitsBuilder()</code> becomes <code>ExecutionLimitsBuilder.Create()</code>.</p><p>The <code>JobBuilder</code> row is the only one that is more than a rename. <code>Create(Type)</code> duplicated <code>OfType(Type)</code> on the same builder; when the job type only arrives at runtime — read from configuration, a database row, or a message — build the detail as:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token class-name">IJobDetail</span> job <span class="token operator">=</span> JobBuilder<span class="token punctuation">.</span><span class="token function">Create</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">OfType</span><span class="token punctuation">(</span>jobType<span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">WithIdentity</span><span class="token punctuation">(</span>name<span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">Build</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p><code>JobBuilder.Create()</code> / <code>Create&lt;TJob&gt;()</code> and <code>TriggerBuilder.Create()</code> / <code>Create&lt;TJob&gt;()</code> are unchanged — that generic/non-generic split carries real type information (see <a href="#the-builders-carry-the-job-type">The builders carry the job type</a>).</p><h2 id="intervals-are-said-once-per-builder" tabindex="-1"><a class="header-anchor" href="#intervals-are-said-once-per-builder"><span>Intervals are said once per builder</span></a></h2><p>Every schedule builder had a <code>WithIntervalIn&lt;Unit&gt;</code> method per unit alongside a general <code>WithInterval</code>. The per-unit methods are gone; the general one stays.</p><h3 id="simpleschedulebuilder-—-withinterval-timespan" tabindex="-1"><a class="header-anchor" href="#simpleschedulebuilder-—-withinterval-timespan"><span>SimpleScheduleBuilder — <code>WithInterval(TimeSpan)</code></span></a></h3><table><thead><tr><th>3.x</th><th>4.x</th></tr></thead><tbody><tr><td><code>WithIntervalInSeconds(n)</code></td><td><code>WithInterval(TimeSpan.FromSeconds(n))</code></td></tr><tr><td><code>WithIntervalInMinutes(n)</code></td><td><code>WithInterval(TimeSpan.FromMinutes(n))</code></td></tr><tr><td><code>WithIntervalInHours(n)</code></td><td><code>WithInterval(TimeSpan.FromHours(n))</code></td></tr></tbody></table><h3 id="calendarintervalschedulebuilder-—-withinterval-int-intervalunit" tabindex="-1"><a class="header-anchor" href="#calendarintervalschedulebuilder-—-withinterval-int-intervalunit"><span>CalendarIntervalScheduleBuilder — <code>WithInterval(int, IntervalUnit)</code></span></a></h3><table><thead><tr><th>3.x</th><th>4.x</th></tr></thead><tbody><tr><td><code>WithIntervalInSeconds(n)</code></td><td><code>WithInterval(n, IntervalUnit.Second)</code></td></tr><tr><td><code>WithIntervalInMinutes(n)</code></td><td><code>WithInterval(n, IntervalUnit.Minute)</code></td></tr><tr><td><code>WithIntervalInHours(n)</code></td><td><code>WithInterval(n, IntervalUnit.Hour)</code></td></tr><tr><td><code>WithIntervalInDays(n)</code></td><td><code>WithInterval(n, IntervalUnit.Day)</code></td></tr><tr><td><code>WithIntervalInWeeks(n)</code></td><td><code>WithInterval(n, IntervalUnit.Week)</code></td></tr><tr><td><code>WithIntervalInMonths(n)</code></td><td><code>WithInterval(n, IntervalUnit.Month)</code></td></tr><tr><td><code>WithIntervalInYears(n)</code></td><td><code>WithInterval(n, IntervalUnit.Year)</code></td></tr></tbody></table><h3 id="dailytimeintervalschedulebuilder-—-withinterval-int-intervalunit" tabindex="-1"><a class="header-anchor" href="#dailytimeintervalschedulebuilder-—-withinterval-int-intervalunit"><span>DailyTimeIntervalScheduleBuilder — <code>WithInterval(int, IntervalUnit)</code></span></a></h3><table><thead><tr><th>3.x</th><th>4.x</th></tr></thead><tbody><tr><td><code>WithIntervalInSeconds(n)</code></td><td><code>WithInterval(n, IntervalUnit.Second)</code></td></tr><tr><td><code>WithIntervalInMinutes(n)</code></td><td><code>WithInterval(n, IntervalUnit.Minute)</code></td></tr><tr><td><code>WithIntervalInHours(n)</code></td><td><code>WithInterval(n, IntervalUnit.Hour)</code></td></tr></tbody></table><p>A calendar interval and a simple interval are genuinely different things — a <code>TimeSpan</code> is a fixed amount of time, while <code>1, IntervalUnit.Month</code> is however long the next month happens to be — so the two shapes differ on purpose.</p><h2 id="simpleschedulebuilder-s-twelve-repeat-factories-are-gone" tabindex="-1"><a class="header-anchor" href="#simpleschedulebuilder-s-twelve-repeat-factories-are-gone"><span><code>SimpleScheduleBuilder</code>&#39;s twelve <code>Repeat*</code> factories are gone</span></a></h2><p>Three units × forever-or-a-count × with-or-without an explicit count, twelve static factories in all, each of which built a <code>TimeSpan</code> and set a repeat count.</p><table><thead><tr><th>3.x</th><th>4.x</th></tr></thead><tbody><tr><td><code>SimpleScheduleBuilder.RepeatSecondlyForever()</code></td><td><code>SimpleScheduleBuilder.Create().WithInterval(TimeSpan.FromSeconds(1)).RepeatForever()</code></td></tr><tr><td><code>SimpleScheduleBuilder.RepeatSecondlyForever(n)</code></td><td><code>SimpleScheduleBuilder.Create().WithInterval(TimeSpan.FromSeconds(n)).RepeatForever()</code></td></tr><tr><td><code>SimpleScheduleBuilder.RepeatMinutelyForever()</code></td><td><code>SimpleScheduleBuilder.Create().WithInterval(TimeSpan.FromMinutes(1)).RepeatForever()</code></td></tr><tr><td><code>SimpleScheduleBuilder.RepeatMinutelyForever(n)</code></td><td><code>SimpleScheduleBuilder.Create().WithInterval(TimeSpan.FromMinutes(n)).RepeatForever()</code></td></tr><tr><td><code>SimpleScheduleBuilder.RepeatHourlyForever()</code></td><td><code>SimpleScheduleBuilder.Create().WithInterval(TimeSpan.FromHours(1)).RepeatForever()</code></td></tr><tr><td><code>SimpleScheduleBuilder.RepeatHourlyForever(n)</code></td><td><code>SimpleScheduleBuilder.Create().WithInterval(TimeSpan.FromHours(n)).RepeatForever()</code></td></tr><tr><td><code>SimpleScheduleBuilder.RepeatSecondlyForTotalCount(c)</code></td><td><code>SimpleScheduleBuilder.Create().WithInterval(TimeSpan.FromSeconds(1)).WithRepeatCount(c - 1)</code></td></tr><tr><td><code>SimpleScheduleBuilder.RepeatSecondlyForTotalCount(c, n)</code></td><td><code>SimpleScheduleBuilder.Create().WithInterval(TimeSpan.FromSeconds(n)).WithRepeatCount(c - 1)</code></td></tr><tr><td><code>SimpleScheduleBuilder.RepeatMinutelyForTotalCount(c)</code></td><td><code>SimpleScheduleBuilder.Create().WithInterval(TimeSpan.FromMinutes(1)).WithRepeatCount(c - 1)</code></td></tr><tr><td><code>SimpleScheduleBuilder.RepeatMinutelyForTotalCount(c, n)</code></td><td><code>SimpleScheduleBuilder.Create().WithInterval(TimeSpan.FromMinutes(n)).WithRepeatCount(c - 1)</code></td></tr><tr><td><code>SimpleScheduleBuilder.RepeatHourlyForTotalCount(c)</code></td><td><code>SimpleScheduleBuilder.Create().WithInterval(TimeSpan.FromHours(1)).WithRepeatCount(c - 1)</code></td></tr><tr><td><code>SimpleScheduleBuilder.RepeatHourlyForTotalCount(c, n)</code></td><td><code>SimpleScheduleBuilder.Create().WithInterval(TimeSpan.FromHours(n)).WithRepeatCount(c - 1)</code></td></tr></tbody></table><div class="hint-container warning"><p class="hint-container-title">Warning</p><p>Mind the <code>- 1</code> in the <code>ForTotalCount</code> rows. The repeat count is one fewer than the number of firings, because the trigger also fires at its start time — <code>RepeatMinutelyForTotalCount(3)</code> fires three times, and the equivalent repeat count is 2. This subtraction is the only thing the old factories said that <code>WithInterval</code> does not, and it is now said on <code>WithRepeatCount</code>, where the trigger says it.</p></div><p>Inside a <code>WithSimpleSchedule</code> delegate you never needed the factories at all:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> .WithSchedule(SimpleScheduleBuilder.RepeatMinutelyForever(5))</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> .WithSimpleSchedule(x =&gt; x.WithInterval(TimeSpan.FromMinutes(5)).RepeatForever())</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>And an interval overload shortens even that, which is as close as 4.x comes to bringing the factories back — see <a href="#five-shorthands-for-the-common-case">Five shorthands for the common case</a>:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token punctuation">.</span><span class="token function">WithSimpleSchedule</span><span class="token punctuation">(</span>TimeSpan<span class="token punctuation">.</span><span class="token function">FromMinutes</span><span class="token punctuation">(</span><span class="token number">5</span><span class="token punctuation">)</span><span class="token punctuation">)</span>                   <span class="token comment">// the same schedule</span></span>
<span class="line"><span class="token punctuation">.</span><span class="token function">WithSimpleSchedule</span><span class="token punctuation">(</span>TimeSpan<span class="token punctuation">.</span><span class="token function">FromMinutes</span><span class="token punctuation">(</span><span class="token number">5</span><span class="token punctuation">)</span><span class="token punctuation">,</span> <span class="token named-parameter punctuation">repeatCount</span><span class="token punctuation">:</span> <span class="token number">2</span><span class="token punctuation">)</span>   <span class="token comment">// three firings rather than forever</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="cronschedulebuilder-s-convenience-factories-are-gone" tabindex="-1"><a class="header-anchor" href="#cronschedulebuilder-s-convenience-factories-are-gone"><span><code>CronScheduleBuilder</code>&#39;s convenience factories are gone</span></a></h2><p><code>CronSchedule(string)</code> and <code>CronSchedule(CronExpression)</code> stay, spelled <code>Create(...)</code> now (see <a href="#every-builder-starts-with-create">Every builder starts with <code>Create</code></a>). The six factories that assembled an expression from numbers are replaced by <code>CronExpressionBuilder</code>, which names each field instead of relying on argument order — the old set used three different orders for the same three numbers.</p><p><code>CronExpressionBuilder</code> spells &quot;restrict this field to these values&quot; as <code>With*</code> on every field, day-of-week included (<code>WithDaysOfWeek</code>, <code>WithDayOfWeekRange</code>, <code>WithDayOfWeekIncrements</code>); the <code>On*</code> prefix is only for the positional and special forms (<code>OnWeekdays</code>, <code>OnLastDayOfMonth</code>, <code>OnLastDayOfWeek</code>, <code>OnLastDayOfWeekOfMonth</code>, <code>OnNthDayOfWeekOfMonth</code>, <code>OnNearestWeekdayOfMonth</code>). A trigger takes the builder — or a built <code>CronExpression</code> — directly through <code>WithCronSchedule</code>, so the chain closes without naming <code>CronScheduleBuilder</code>.</p><p>The three day-of-week members that 3.x spelled with <code>On*</code> were renamed by that rule. Only the name changed; each takes the same arguments and means the same thing:</p><table><thead><tr><th>3.x</th><th>4.x</th></tr></thead><tbody><tr><td><code>CronExpressionBuilder.OnDaysOfWeek(params DayOfWeek[])</code></td><td><code>CronExpressionBuilder.WithDaysOfWeek(params DayOfWeek[])</code></td></tr><tr><td><code>CronExpressionBuilder.OnDayOfWeekRange(DayOfWeek, DayOfWeek)</code></td><td><code>CronExpressionBuilder.WithDayOfWeekRange(DayOfWeek, DayOfWeek)</code></td></tr><tr><td><code>CronExpressionBuilder.OnDayOfWeekIncrements(DayOfWeek, int)</code></td><td><code>CronExpressionBuilder.WithDayOfWeekIncrements(DayOfWeek, int)</code></td></tr></tbody></table><p>The four that took a time of day take it as one <code>TimeOnly</code> now, through <code>AtTime</code> — which sets the second, minute and hour fields together, so the &quot;at 09:30&quot; half of each old factory is one call and the &quot;on these days&quot; half is <code>WithDaysOfWeek</code> or <code>WithDayOfMonth</code> beside it. Cron resolves to a whole second, so any sub-second part of the <code>TimeOnly</code> is ignored.</p><table><thead><tr><th>3.x</th><th>4.x</th></tr></thead><tbody><tr><td><code>CronScheduleBuilder.DailyAtHourAndMinute(h, m)</code></td><td><code>CronExpressionBuilder.Create().AtTime(new TimeOnly(h, m))</code></td></tr><tr><td><code>CronScheduleBuilder.AtHourAndMinuteOnGivenDaysOfWeek(h, m, days)</code></td><td><code>CronExpressionBuilder.Create().AtTime(new TimeOnly(h, m)).WithDaysOfWeek(days)</code></td></tr><tr><td><code>CronScheduleBuilder.WeeklyOnDayAndHourAndMinute(day, h, m)</code></td><td><code>CronExpressionBuilder.Create().AtTime(new TimeOnly(h, m)).WithDaysOfWeek(day)</code></td></tr><tr><td><code>CronScheduleBuilder.MonthlyOnDayAndHourAndMinute(dom, h, m)</code></td><td><code>CronExpressionBuilder.Create().AtTime(new TimeOnly(h, m)).WithDayOfMonth(dom)</code></td></tr><tr><td><code>CronScheduleBuilder.CronScheduleWithHash(expr, hashKey)</code></td><td><code>CronScheduleBuilder.Create(CronExpression.ParseWithHash(expr, hashKey))</code></td></tr><tr><td><code>CronScheduleBuilder.CronScheduleWithHash(expr, hashSeed)</code></td><td><code>CronScheduleBuilder.Create(CronExpression.ParseWithHash(expr, hashSeed))</code></td></tr></tbody></table><p><code>WithCronSchedule</code> takes the builder, so nothing in the first four rows names <code>CronScheduleBuilder</code> at all:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> .WithSchedule(CronScheduleBuilder.DailyAtHourAndMinute(9, 30))</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> .WithCronSchedule(CronExpressionBuilder.Create().AtTime(new TimeOnly(9, 30)))</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>A literal expression is still the shortest thing to write when you have one:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> .WithSchedule(CronScheduleBuilder.DailyAtHourAndMinute(9, 30))</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> .WithCronSchedule(&quot;0 30 9 ? * *&quot;)</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="five-shorthands-for-the-common-case" tabindex="-1"><a class="header-anchor" href="#five-shorthands-for-the-common-case"><span>Five shorthands for the common case</span></a></h2><p>Replacing a family of static factories with a builder makes the unusual call sayable and the usual one longer. Five additions give the usual one its short spelling back: an interval overload of <code>WithSimpleSchedule</code>, <code>CronExpressionBuilder.AtTime</code>, <code>IScheduler.ScheduleJob&lt;TJob&gt;</code>, a named static on each of the four option records, and <code>TriggerBuilder&lt;TJob&gt;.Key</code>. All are additive — nothing they shorten stopped working, and the longer form is still what to reach for the moment the call needs more than the shorthand says.</p><table><thead><tr><th>Instead of</th><th>Write</th></tr></thead><tbody><tr><td><code>WithSimpleSchedule(x =&gt; x.WithInterval(i).RepeatForever())</code></td><td><code>WithSimpleSchedule(i)</code></td></tr><tr><td><code>WithSimpleSchedule(x =&gt; x.WithInterval(i).WithRepeatCount(n))</code></td><td><code>WithSimpleSchedule(i, n)</code></td></tr><tr><td><code>CronExpressionBuilder.Create().WithSecond(0).WithMinute(m).WithHour(h)</code></td><td><code>CronExpressionBuilder.Create().AtTime(new TimeOnly(h, m))</code></td></tr><tr><td><code>scheduler.ScheduleJob(JobBuilder.Create&lt;TJob&gt;().WithIdentity(trigger.Key.Name, trigger.Key.Group).Build(), trigger)</code></td><td><code>scheduler.ScheduleJob&lt;TJob&gt;(trigger)</code></td></tr><tr><td><code>new AddJobOptions { Replace = true }</code></td><td><code>AddJobOptions.Replacing</code></td></tr><tr><td><code>new ScheduleJobOptions { Replace = true }</code></td><td><code>ScheduleJobOptions.Replacing</code></td></tr><tr><td><code>new AddTriggerOptions { Replace = true }</code></td><td><code>AddTriggerOptions.Replacing</code></td></tr><tr><td><code>new AddCalendarOptions { Replace = true }</code></td><td><code>AddCalendarOptions.Replacing</code></td></tr><tr><td><code>new AddCalendarOptions { Replace = true, UpdateTriggers = true }</code></td><td><code>AddCalendarOptions.ReplacingAndUpdatingTriggers</code></td></tr></tbody></table><p>The repeat count in <code>WithSimpleSchedule(i, n)</code> is the trigger&#39;s own <code>RepeatCount</code>, so it fires <code>n + 1</code> times — the same number <code>WithRepeatCount</code> takes, with none of the <code>- 1</code> arithmetic the 3.x <code>ForTotalCount</code> factories did. Omitting it repeats forever.</p><p><code>IScheduler.ScheduleJob&lt;TJob&gt;(trigger, configure)</code> is the imperative twin of the container&#39;s <code>q.ScheduleJob&lt;TJob&gt;(...)</code>, and names the job the same way: whatever <code>configure</code> gave it, else the job the trigger already points at through <code>ForJob</code>, else the trigger&#39;s own key. So neither of these names a <code>JobBuilder</code>, and the two read alike:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token comment">// at start-up, in AddQuartz</span></span>
<span class="line">q<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">ScheduleJob</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>ReportJob<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span>trigger <span class="token operator">=&gt;</span> trigger<span class="token punctuation">.</span><span class="token function">WithIdentity</span><span class="token punctuation">(</span><span class="token string">&quot;nightly&quot;</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">WithCronSchedule</span><span class="token punctuation">(</span><span class="token string">&quot;0 30 9 ? * *&quot;</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line"><span class="token comment">// at run time, against a scheduler</span></span>
<span class="line"><span class="token class-name">ITrigger</span> trigger <span class="token operator">=</span> TriggerBuilder<span class="token punctuation">.</span><span class="token function">Create</span><span class="token punctuation">(</span><span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">.</span><span class="token function">WithIdentity</span><span class="token punctuation">(</span><span class="token string">&quot;nightly&quot;</span><span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">.</span><span class="token function">WithCronSchedule</span><span class="token punctuation">(</span><span class="token string">&quot;0 30 9 ? * *&quot;</span><span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">.</span><span class="token function">Build</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line"><span class="token keyword">await</span> scheduler<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">ScheduleJob</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>ReportJob<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span>trigger<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>TriggerBuilder&lt;TJob&gt;.Key</code> rounds out the same pass: it reports the identity the trigger was given, the way <code>JobBuilder&lt;TJob&gt;.Key</code> already did, so code that has to agree with a trigger can read its key rather than build the trigger to find it out. It differs in one way — a trigger builder keeps the key <code>Build()</code> generated when none was named, so building twice produces the same trigger, and reading <code>Key</code> after <code>Build()</code> reports that generated key rather than <code>null</code>.</p><h2 id="day-selection-on-dailytimeintervalschedulebuilder" tabindex="-1"><a class="header-anchor" href="#day-selection-on-dailytimeintervalschedulebuilder"><span>Day selection on <code>DailyTimeIntervalScheduleBuilder</code></span></a></h2><p>The two <code>OnDaysOfTheWeek</code> overloads are one C# 13 params collection, so both old call shapes still compile:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line">x<span class="token punctuation">.</span><span class="token function">OnDaysOfTheWeek</span><span class="token punctuation">(</span>DayOfWeek<span class="token punctuation">.</span>Monday<span class="token punctuation">,</span> DayOfWeek<span class="token punctuation">.</span>Wednesday<span class="token punctuation">)</span><span class="token punctuation">;</span>   <span class="token comment">// still fine</span></span>
<span class="line">x<span class="token punctuation">.</span><span class="token function">OnDaysOfTheWeek</span><span class="token punctuation">(</span>daysFromConfiguration<span class="token punctuation">)</span><span class="token punctuation">;</span>                   <span class="token comment">// still fine</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>The three <code>public static readonly</code> day sets are gone. They existed only to be handed straight back to <code>OnDaysOfTheWeek</code>, which the named methods already do:</p><table><thead><tr><th>3.x</th><th>4.x</th></tr></thead><tbody><tr><td><code>OnDaysOfTheWeek(DailyTimeIntervalScheduleBuilder.AllDaysOfTheWeek)</code></td><td><code>OnEveryDay()</code> — also the default when you say nothing</td></tr><tr><td><code>OnDaysOfTheWeek(DailyTimeIntervalScheduleBuilder.MondayThroughFriday)</code></td><td><code>OnMondayThroughFriday()</code></td></tr><tr><td><code>OnDaysOfTheWeek(DailyTimeIntervalScheduleBuilder.SaturdayAndSunday)</code></td><td><code>OnSaturdayAndSunday()</code></td></tr></tbody></table><p>If you used a set for something other than the builder, <code>Enum.GetValues&lt;DayOfWeek&gt;()</code> is the whole week.</p><h2 id="endingdailyaftercount-is-computed-at-build" tabindex="-1"><a class="header-anchor" href="#endingdailyaftercount-is-computed-at-build"><span><code>EndingDailyAfterCount</code> is computed at <code>Build()</code></span></a></h2><p>In 3.x, <code>EndingDailyAfterCount(count)</code> resolved the daily window immediately, against the wall clock and against whatever start time, interval and time zone happened to be configured <em>before</em> the call — which is why its documentation had to insist on call order. The computation now runs when the trigger is built:</p><ul><li>It sees the schedule as finally configured, so <code>EndingDailyAfterCount</code> no longer has to be the last method in the chain.</li><li>It runs against the clock of the <code>TriggerBuilder</code> that builds the trigger, so a scheduler configured with a custom <code>TimeProvider</code> (a <code>FakeTimeProvider</code> in tests) is honored. Previously the builder read the wall clock and a configured clock was silently ignored.</li><li>Validation moves with it: &quot;count too large&quot; and &quot;start time not set&quot; are now reported by <code>Build()</code>, not by the <code>EndingDailyAfterCount</code> call. A count that is not positive is still rejected immediately.</li></ul><p><code>DailyTimeIntervalScheduleBuilder.Create()</code> takes no <code>TimeProvider</code> — none of the schedule builders do; the clock belongs to <code>TriggerBuilder.Create(TimeProvider?)</code>, and it reaches the trigger itself as well as the schedule — see <a href="#a-trigger-holds-the-clock-that-built-it">A trigger holds the clock that built it</a>.</p><h2 id="a-trigger-holds-the-clock-that-built-it" tabindex="-1"><a class="header-anchor" href="#a-trigger-holds-the-clock-that-built-it"><span>A trigger holds the clock that built it</span></a></h2><p>3.x had one clock. <code>SystemTime.UtcNow</code> was a process-wide hook, so a trigger, the store that swept it up and the scheduler that fired it could not disagree about what time it was. 4.x replaced it with <code>TimeProvider</code>, which is an object rather than a hook — which means each trigger <em>has</em> a clock, and it matters where it got one.</p><p>It gets one from whoever produced it:</p><ul><li><code>TriggerBuilder.Create(clock)...Build()</code> gives the built trigger that clock. Previously it used the clock only for the default start time and for the schedule builder&#39;s own arithmetic, and the trigger itself was left on <code>TimeProvider.System</code> — so the past-due clamp in <code>ComputeFirstFireTimeUtc</code> and the whole of <code>UpdateAfterMisfire</code> read the machine&#39;s wall clock. <code>TriggerBuilder.Create()</code> with no argument still means <code>TimeProvider.System</code>.</li><li><code>trigger.GetTriggerBuilder()</code> carries the trigger&#39;s clock into the rebuilt trigger.</li><li>A job store hands its scheduler&#39;s clock to every trigger it materializes. <code>RAMJobStore</code> hands back the object it was given, clock and all; the ADO.NET store builds triggers from rows and stamps each one — the blob path included, since the clock does not serialize.</li></ul><p>In production every one of these is <code>TimeProvider.System</code> and nothing looks different. It shows up in a scheduler configured with a <code>FakeTimeProvider</code>: a misfire that the store <em>selected</em> on the test&#39;s clock used to be <em>recovered</em> onto the machine&#39;s, which is the ADO.NET half of what <a href="https://github.com/quartznet/quartznet/issues/3456" target="_blank" rel="noopener noreferrer">#3456</a> reported.</p><p>The clock is not part of a trigger&#39;s public surface. It is a construction-or-store decision, because the decision that a trigger has misfired and the arithmetic that recovers it have to be made against one reading of &quot;now&quot;.</p><h2 id="intimezone-is-nullable-everywhere" tabindex="-1"><a class="header-anchor" href="#intimezone-is-nullable-everywhere"><span><code>InTimeZone</code> is nullable everywhere</span></a></h2><p><code>CronScheduleBuilder</code> and <code>DailyTimeIntervalScheduleBuilder</code> declared <code>InTimeZone(TimeZoneInfo)</code> while <code>CalendarIntervalScheduleBuilder</code> and <code>RecurrenceScheduleBuilder</code> declared <code>InTimeZone(TimeZoneInfo?)</code>, so passing a zone that may be absent needed a <code>!</code> on two of the four. All four — and <code>DateBuilder.InTimeZone</code> — now take <code>TimeZoneInfo?</code>. <code>null</code> means what it always meant: the system&#39;s local time zone.</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> .InTimeZone(configuredZone!)</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> .InTimeZone(configuredZone)</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="schedulebuilder-t-is-gone" tabindex="-1"><a class="header-anchor" href="#schedulebuilder-t-is-gone"><span><code>ScheduleBuilder&lt;T&gt;</code> is gone</span></a></h2><p>The five schedule builders implement <code>IScheduleBuilder</code> directly. The abstract base declared one member that <code>IScheduleBuilder</code> already declares, and nothing ever used its type parameter. A schedule builder of your own implements the interface and drops the <code>override</code>:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> private sealed class MyScheduleBuilder : ScheduleBuilder&lt;MyTrigger&gt;</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> private sealed class MyScheduleBuilder : IScheduleBuilder</span>
<span class="line"></span></span><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line"> {</span>
<span class="line"></span></span><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line">     public override IMutableTrigger Build() =&gt; new MyTrigger();</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line">     public IMutableTrigger Build() =&gt; new MyTrigger();</span>
<span class="line"></span></span><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line"> }</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="datebuilder-s-static-factories-are-gone" tabindex="-1"><a class="header-anchor" href="#datebuilder-s-static-factories-are-gone"><span><code>DateBuilder</code>&#39;s static factories are gone</span></a></h2><p>The fluent API keeps its shape: <code>DateBuilder.Create()</code> (named <code>NewDate()</code> in 3.x), <code>CreateInTimeZone()</code> (3.x <code>NewDateInTimeZone()</code>), the <code>At*</code>/<code>On*</code>/<code>In*</code> setters and <code>Build()</code>. The seventeen statics were doing two unrelated jobs under one name — naming a specific date, which the fluent API does, and arithmetic on a <code>DateTimeOffset</code>, which <code>DateTimeOffset</code> does.</p><h3 id="naming-a-date" tabindex="-1"><a class="header-anchor" href="#naming-a-date"><span>Naming a date</span></a></h3><table><thead><tr><th>3.x</th><th>4.x</th></tr></thead><tbody><tr><td><code>DateBuilder.DateOf(h, m, s)</code></td><td><code>DateBuilder.Create().AtHourMinuteAndSecond(h, m, s).Build()</code></td></tr><tr><td><code>DateBuilder.TodayAt(h, m, s)</code></td><td><code>DateBuilder.Create().AtHourMinuteAndSecond(h, m, s).Build()</code></td></tr><tr><td><code>DateBuilder.DateOf(h, m, s, day, month)</code></td><td><code>DateBuilder.Create().InMonthOnDay(month, day).AtHourMinuteAndSecond(h, m, s).Build()</code></td></tr><tr><td><code>DateBuilder.DateOf(h, m, s, day, month, year)</code></td><td><code>DateBuilder.Create().InYear(year).InMonthOnDay(month, day).AtHourMinuteAndSecond(h, m, s).Build()</code></td></tr><tr><td><code>DateBuilder.TomorrowAt(h, m, s)</code></td><td><code>DateBuilder.Create().AtHourMinuteAndSecond(h, m, s).Build().AddDays(1)</code></td></tr></tbody></table><p>Note that <code>InMonthOnDay</code> takes the month first, where <code>DateOf</code> took the day first.</p><h3 id="now-plus-something" tabindex="-1"><a class="header-anchor" href="#now-plus-something"><span>Now plus something</span></a></h3><table><thead><tr><th>3.x</th><th>4.x</th></tr></thead><tbody><tr><td><code>DateBuilder.FutureDate(n, IntervalUnit.Second)</code></td><td><code>DateTimeOffset.UtcNow.AddSeconds(n)</code></td></tr><tr><td><code>DateBuilder.FutureDate(n, IntervalUnit.Minute)</code></td><td><code>DateTimeOffset.UtcNow.AddMinutes(n)</code></td></tr><tr><td><code>DateBuilder.FutureDate(n, IntervalUnit.Hour)</code></td><td><code>DateTimeOffset.UtcNow.AddHours(n)</code></td></tr><tr><td><code>DateBuilder.FutureDate(n, IntervalUnit.Day)</code></td><td><code>DateTimeOffset.UtcNow.AddDays(n)</code></td></tr><tr><td><code>DateBuilder.FutureDate(n, IntervalUnit.Week)</code></td><td><code>DateTimeOffset.UtcNow.AddDays(n * 7)</code></td></tr><tr><td><code>DateBuilder.FutureDate(n, IntervalUnit.Month)</code></td><td><code>DateTimeOffset.UtcNow.AddMonths(n)</code></td></tr><tr><td><code>DateBuilder.FutureDate(n, IntervalUnit.Year)</code></td><td><code>DateTimeOffset.UtcNow.AddYears(n)</code></td></tr><tr><td><code>DateBuilder.FutureDate(n, IntervalUnit.Millisecond)</code></td><td><code>DateTimeOffset.UtcNow.AddMilliseconds(n)</code></td></tr></tbody></table><h3 id="rounding" tabindex="-1"><a class="header-anchor" href="#rounding"><span>Rounding</span></a></h3><p>Each of these was one line of <code>DateTimeOffset</code> construction:</p><table><thead><tr><th>3.x</th><th>4.x</th></tr></thead><tbody><tr><td><code>DateBuilder.EvenSecondDateBefore(d)</code></td><td><code>new DateTimeOffset(d.Year, d.Month, d.Day, d.Hour, d.Minute, d.Second, d.Offset)</code></td></tr><tr><td><code>DateBuilder.EvenSecondDate(d)</code></td><td>the same, on <code>d.AddSeconds(1)</code></td></tr><tr><td><code>DateBuilder.EvenSecondDateAfterNow()</code></td><td>the same, on <code>DateTimeOffset.Now.AddSeconds(1)</code></td></tr><tr><td><code>DateBuilder.EvenMinuteDateBefore(d)</code></td><td><code>new DateTimeOffset(d.Year, d.Month, d.Day, d.Hour, d.Minute, 0, d.Offset)</code></td></tr><tr><td><code>DateBuilder.EvenMinuteDate(d)</code></td><td>the same, on <code>d.AddMinutes(1)</code></td></tr><tr><td><code>DateBuilder.EvenMinuteDateAfterNow()</code></td><td>the same, on <code>DateTimeOffset.Now.AddMinutes(1)</code></td></tr><tr><td><code>DateBuilder.EvenHourDateBefore(d)</code></td><td><code>new DateTimeOffset(d.Year, d.Month, d.Day, d.Hour, 0, 0, d.Offset)</code></td></tr><tr><td><code>DateBuilder.EvenHourDate(d)</code></td><td>the same, on <code>d.AddHours(1)</code></td></tr><tr><td><code>DateBuilder.EvenHourDateAfterNow()</code></td><td>the same, on <code>DateTimeOffset.Now.AddHours(1)</code></td></tr><tr><td><code>DateBuilder.NextGivenMinuteDate(d, minuteBase)</code></td><td>round <code>d</code> up to the next multiple of <code>minuteBase</code> minutes</td></tr><tr><td><code>DateBuilder.NextGivenSecondDate(d, secondBase)</code></td><td>round <code>d</code> up to the next multiple of <code>secondBase</code> seconds</td></tr></tbody></table><p>Most start times do not need the rounding at all. A trigger that starts at an arbitrary instant and repeats every minute keeps the same schedule as one whose start was rounded up first — it just begins a fraction of a second earlier. Reach for rounding when you want the <em>displayed</em> times to be tidy, and write the line where you want it.</p><h2 id="timeofday-became-timeonly" tabindex="-1"><a class="header-anchor" href="#timeofday-became-timeonly"><span><code>TimeOfDay</code> became <code>TimeOnly</code></span></a></h2><p>The hand-written <code>TimeOfDay</code> class predates <code>System.TimeOnly</code>. It is gone, and <code>IDailyTimeIntervalTrigger</code> and <code>DailyTimeIntervalScheduleBuilder</code> speak <code>TimeOnly</code>.</p><table><thead><tr><th>3.x</th><th>4.x</th></tr></thead><tbody><tr><td><code>TimeOfDay.HourAndMinuteOfDay(8, 0)</code></td><td><code>new TimeOnly(8, 0)</code></td></tr><tr><td><code>TimeOfDay.HourMinuteAndSecondOfDay(8, 0, 30)</code></td><td><code>new TimeOnly(8, 0, 30)</code></td></tr><tr><td><code>new TimeOfDay(8, 0)</code> / <code>new TimeOfDay(8, 0, 30)</code></td><td><code>new TimeOnly(8, 0)</code> / <code>new TimeOnly(8, 0, 30)</code></td></tr><tr><td><code>IDailyTimeIntervalTrigger.StartTimeOfDay</code> returning <code>TimeOfDay</code></td><td>returning <code>TimeOnly</code></td></tr><tr><td><code>IDailyTimeIntervalTrigger.EndTimeOfDay</code> returning <code>TimeOfDay</code></td><td>returning <code>TimeOnly</code></td></tr><tr><td><code>StartingDailyAt(TimeOfDay)</code> / <code>EndingDailyAt(TimeOfDay)</code></td><td><code>StartingDailyAt(TimeOnly)</code> / <code>EndingDailyAt(TimeOnly)</code></td></tr><tr><td><code>a.Before(b)</code></td><td><code>a &lt; b</code></td></tr><tr><td><code>timeOfDay.GetTimeOfDayForDate(date)</code></td><td><code>new DateTimeOffset(date.Date, date.Offset).Add(timeOfDay.ToTimeSpan())</code></td></tr></tbody></table><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token comment">// 3.x</span></span>
<span class="line"><span class="token punctuation">.</span><span class="token function">WithDailyTimeIntervalSchedule</span><span class="token punctuation">(</span>x <span class="token operator">=&gt;</span> x</span>
<span class="line">    <span class="token punctuation">.</span><span class="token function">StartingDailyAt</span><span class="token punctuation">(</span>TimeOfDay<span class="token punctuation">.</span><span class="token function">HourAndMinuteOfDay</span><span class="token punctuation">(</span><span class="token number">8</span><span class="token punctuation">,</span> <span class="token number">0</span><span class="token punctuation">)</span><span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">.</span><span class="token function">EndingDailyAt</span><span class="token punctuation">(</span>TimeOfDay<span class="token punctuation">.</span><span class="token function">HourAndMinuteOfDay</span><span class="token punctuation">(</span><span class="token number">17</span><span class="token punctuation">,</span> <span class="token number">0</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">)</span></span>
<span class="line"></span>
<span class="line"><span class="token comment">// 4.x</span></span>
<span class="line"><span class="token punctuation">.</span><span class="token function">WithDailyTimeIntervalSchedule</span><span class="token punctuation">(</span>x <span class="token operator">=&gt;</span> x</span>
<span class="line">    <span class="token punctuation">.</span><span class="token function">StartingDailyAt</span><span class="token punctuation">(</span><span class="token keyword">new</span> <span class="token constructor-invocation class-name">TimeOnly</span><span class="token punctuation">(</span><span class="token number">8</span><span class="token punctuation">,</span> <span class="token number">0</span><span class="token punctuation">)</span><span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">.</span><span class="token function">EndingDailyAt</span><span class="token punctuation">(</span><span class="token keyword">new</span> <span class="token constructor-invocation class-name">TimeOnly</span><span class="token punctuation">(</span><span class="token number">17</span><span class="token punctuation">,</span> <span class="token number">0</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">)</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Two things to know:</p><ul><li>The two properties are non-nullable now. A <code>TimeOnly</code> is a struct, and the defaults are the ones the builder always applied anyway — <code>00:00:00</code> and <code>23:59:59</code>.</li><li>A value with sub-second precision is rejected with an <code>ArgumentException</code>. The job store keeps the window in hour, minute and second columns, so <code>TimeOnly.FromDateTime(DateTime.Now)</code> would lose its fractional part the moment the trigger were persisted. Round it yourself if that is what you meant.</li></ul><p>Nothing about storage changed: the <code>SIMPROP_INT_PROP</code> columns are the same, and the JSON <code>StartTimeOfDay</code>/<code>EndTimeOfDay</code> objects keep their <code>{ Hour, Minute, Second }</code> shape, so existing triggers load unchanged.</p><h2 id="dailycalendar-takes-two-timeonly-values" tabindex="-1"><a class="header-anchor" href="#dailycalendar-takes-two-timeonly-values"><span><code>DailyCalendar</code> takes two <code>TimeOnly</code> values</span></a></h2><p>Eight constructors and four <code>SetTimeRange</code> overloads described one pair of times four different ways. One constructor and one property replace them.</p><table><thead><tr><th>3.x</th><th>4.x</th></tr></thead><tbody><tr><td><code>new DailyCalendar(&quot;08:00&quot;, &quot;17:00&quot;)</code></td><td><code>new DailyCalendar(new TimeOnly(8, 0), new TimeOnly(17, 0))</code></td></tr><tr><td><code>new DailyCalendar(&quot;08:00:00:500&quot;, &quot;17:00:00:000&quot;)</code></td><td><code>new DailyCalendar(new TimeOnly(8, 0, 0, 500), new TimeOnly(17, 0))</code></td></tr><tr><td><code>new DailyCalendar(baseCal, &quot;08:00&quot;, &quot;17:00&quot;)</code></td><td><code>new DailyCalendar(new TimeOnly(8, 0), new TimeOnly(17, 0), baseCal)</code></td></tr><tr><td><code>new DailyCalendar(8, 0, 0, 0, 17, 0, 0, 0)</code></td><td><code>new DailyCalendar(new TimeOnly(8, 0), new TimeOnly(17, 0))</code></td></tr><tr><td><code>new DailyCalendar(startDateTime, endDateTime)</code></td><td><code>new DailyCalendar(TimeOnly.FromDateTime(startDateTime), TimeOnly.FromDateTime(endDateTime))</code></td></tr><tr><td><code>new DailyCalendar(startTicks, endTicks)</code></td><td><code>new DailyCalendar(new TimeOnly(startTicks), new TimeOnly(endTicks))</code></td></tr><tr><td><code>calendar.SetTimeRange(...)</code> (four overloads)</td><td><code>calendar.TimeRange = (start, end)</code></td></tr><tr><td><code>calendar.RangeStartingTime</code> (a string)</td><td><code>calendar.TimeRange.Start</code></td></tr><tr><td><code>calendar.RangeEndingTime</code> (a string)</td><td><code>calendar.TimeRange.End</code></td></tr></tbody></table><p><code>TimeRange</code> is a <code>readonly record struct</code> of <code>Start</code> and <code>End</code> in the <code>Quartz</code> namespace. A <code>(start, end)</code> tuple converts to it implicitly, so the assignment in the table above reads as it always did; a variable holding the range is a <code>TimeRange</code> rather than a <code>ValueTuple</code>, and an equality comparison against a bare tuple needs the type spelled out — <code>calendar.TimeRange == new TimeRange(start, end)</code>. What the calendar stores did not change: its serialized form is the same eight numbers it has always been.</p><p>The <code>&quot;HH:MM:SS:mmm&quot;</code> string form — note the colon before the milliseconds — was a format nothing else in .NET parses. <code>InvertTimeRange</code>, <code>GetTimeRangeStartingTimeUtc</code> and <code>GetTimeRangeEndingTimeUtc</code> are unchanged. The constructor no longer takes a <code>TimeProvider</code>; it only ever used one to check that the range starts before it ends, which two <code>TimeOnly</code> values answer directly. Precision finer than a millisecond is rejected, matching what the calendar&#39;s serialized form can carry.</p><p>Persisted <code>DailyCalendar</code> blobs load unchanged: the serializers write <code>RangeStart</code>/<code>RangeEnd</code> now but still read the old <code>RangeStartingTime</code>/<code>RangeEndingTime</code> strings.</p><h2 id="excluded-days-are-a-read-only-set" tabindex="-1"><a class="header-anchor" href="#excluded-days-are-a-read-only-set"><span>Excluded days are a read-only set</span></a></h2><p>The four day-excluding calendars had four idioms for one idea. They now share one: a read-only set of the thing being excluded, plus <code>AddExcludedDay</code> and <code>RemoveExcludedDay</code>, which return whether the set changed.</p><table><thead><tr><th>3.x</th><th>4.x</th></tr></thead><tbody><tr><td><code>AnnualCalendar.DaysExcluded</code> as a settable <code>IReadOnlyCollection&lt;DateTime&gt;</code></td><td><code>IReadOnlySet&lt;MonthDay&gt;</code>, get-only</td></tr><tr><td><code>annual.SetDayExcluded(day, true)</code></td><td><code>annual.AddExcludedDay(MonthDay)</code></td></tr><tr><td><code>annual.SetDayExcluded(day, false)</code></td><td><code>annual.RemoveExcludedDay(MonthDay)</code></td></tr><tr><td><code>annual.IsDayExcluded(DateTimeOffset)</code></td><td><code>annual.IsDayExcluded(MonthDay)</code></td></tr><tr><td><code>HolidayCalendar.ExcludedDates</code> as a <code>List&lt;DateTime&gt;</code> copy</td><td><code>HolidayCalendar.DaysExcluded</code> as <code>IReadOnlySet&lt;DateOnly&gt;</code></td></tr><tr><td><code>holiday.AddExcludedDate(DateTime)</code></td><td><code>holiday.AddExcludedDay(DateOnly)</code></td></tr><tr><td><code>holiday.RemoveExcludedDate(DateTime)</code></td><td><code>holiday.RemoveExcludedDay(DateOnly)</code></td></tr><tr><td>(nothing)</td><td><code>holiday.IsDayExcluded(DateOnly)</code></td></tr><tr><td><code>MonthlyCalendar.DaysExcluded</code> as a settable <code>bool[31]</code></td><td><code>IReadOnlySet&lt;int&gt;</code>, get-only, days 1 through 31</td></tr><tr><td><code>monthly.SetDayExcluded(15, true)</code> / <code>(15, false)</code></td><td><code>monthly.AddExcludedDay(15)</code> / <code>monthly.RemoveExcludedDay(15)</code></td></tr><tr><td><code>WeeklyCalendar.DaysExcluded</code> as a settable <code>bool[7]</code></td><td><code>IReadOnlySet&lt;DayOfWeek&gt;</code>, get-only</td></tr><tr><td><code>weekly.SetDayExcluded(DayOfWeek.Friday, true)</code> / <code>(…, false)</code></td><td><code>weekly.AddExcludedDay(DayOfWeek.Friday)</code> / <code>weekly.RemoveExcludedDay(DayOfWeek.Friday)</code></td></tr><tr><td><code>CronCalendar.SetCronExpressionString(expr)</code></td><td><code>cron.CronExpression = new CronExpression(expr)</code></td></tr></tbody></table><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token comment">// 3.x</span></span>
<span class="line"><span class="token class-name"><span class="token keyword">var</span></span> holidays <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token constructor-invocation class-name">HolidayCalendar</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">holidays<span class="token punctuation">.</span><span class="token function">AddExcludedDate</span><span class="token punctuation">(</span><span class="token keyword">new</span> <span class="token constructor-invocation class-name">DateTime</span><span class="token punctuation">(</span><span class="token number">2025</span><span class="token punctuation">,</span> <span class="token number">12</span><span class="token punctuation">,</span> <span class="token number">25</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line"><span class="token class-name"><span class="token keyword">var</span></span> christmas <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token constructor-invocation class-name">AnnualCalendar</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">christmas<span class="token punctuation">.</span><span class="token function">SetDayExcluded</span><span class="token punctuation">(</span><span class="token keyword">new</span> <span class="token constructor-invocation class-name">DateTime</span><span class="token punctuation">(</span><span class="token number">2025</span><span class="token punctuation">,</span> <span class="token number">12</span><span class="token punctuation">,</span> <span class="token number">25</span><span class="token punctuation">)</span><span class="token punctuation">,</span> <span class="token boolean">true</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line"><span class="token class-name"><span class="token keyword">var</span></span> weekends <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token constructor-invocation class-name">WeeklyCalendar</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">weekends<span class="token punctuation">.</span><span class="token function">SetDayExcluded</span><span class="token punctuation">(</span>DayOfWeek<span class="token punctuation">.</span>Friday<span class="token punctuation">,</span> <span class="token boolean">true</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line"><span class="token comment">// 4.x</span></span>
<span class="line"><span class="token class-name"><span class="token keyword">var</span></span> holidays <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token constructor-invocation class-name">HolidayCalendar</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">holidays<span class="token punctuation">.</span><span class="token function">AddExcludedDay</span><span class="token punctuation">(</span><span class="token keyword">new</span> <span class="token constructor-invocation class-name">DateOnly</span><span class="token punctuation">(</span><span class="token number">2025</span><span class="token punctuation">,</span> <span class="token number">12</span><span class="token punctuation">,</span> <span class="token number">25</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>   <span class="token comment">// a specific date, once</span></span>
<span class="line"></span>
<span class="line"><span class="token class-name"><span class="token keyword">var</span></span> christmas <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token constructor-invocation class-name">AnnualCalendar</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">christmas<span class="token punctuation">.</span><span class="token function">AddExcludedDay</span><span class="token punctuation">(</span><span class="token keyword">new</span> <span class="token constructor-invocation class-name">MonthDay</span><span class="token punctuation">(</span><span class="token number">12</span><span class="token punctuation">,</span> <span class="token number">25</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>        <span class="token comment">// the same date, every year</span></span>
<span class="line"></span>
<span class="line"><span class="token class-name"><span class="token keyword">var</span></span> weekends <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token constructor-invocation class-name">WeeklyCalendar</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">weekends<span class="token punctuation">.</span><span class="token function">AddExcludedDay</span><span class="token punctuation">(</span>DayOfWeek<span class="token punctuation">.</span>Friday<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Two behaviors worth knowing:</p><ul><li><code>AnnualCalendar</code> speaks <code>MonthDay</code> — a <code>readonly record struct</code> of month and day in the <code>Quartz</code> namespace, with <code>MonthDay.From(DateOnly)</code> for when you hold a date. A <code>DateOnly</code> always carries a year, so a set of them had to lie: what you put in was not what you read back, and <code>DaysExcluded.Contains</code> disagreed with <code>AddExcludedDay</code>. <code>MonthDay</code> says exactly what is stored — the same date every year — and February 29th is a valid value. Its text form is the ISO 8601 spelling of a recurring month-day, <code>--MM-DD</code>, and it implements the full BCL set around that one form: <code>IParsable&lt;MonthDay&gt;</code>, <code>ISpanParsable&lt;MonthDay&gt;</code>, <code>ISpanFormattable</code> and <code>IUtf8SpanFormattable</code>, so it reads out of configuration, a route value or a JSON string like any other primitive. There is nothing to vary, so the format string and the format provider those interfaces pass are ignored. What <code>AnnualCalendar</code> payloads carry is unchanged — a serialized day is still the date-shaped form pinned to the year 2000.</li><li><code>AnnualCalendar.IsDayExcluded</code> now answers only about the calendar&#39;s own set. The base calendar is consulted by <code>IsTimeIncluded</code>, which is the member that asks a question about an instant.</li></ul><p><code>MonthlyCalendar.AreAllDaysExcluded</code> and <code>WeeklyCalendar.AreAllDaysExcluded</code> are unchanged, and a fresh <code>WeeklyCalendar</code> still starts out excluding Saturday and Sunday.</p><p>Existing calendar blobs load unchanged. Both serializers write the new shapes and read the old ones: an <code>ExcludedDays</code>/<code>ExcludedDates</code> array may hold timestamps or dates, and per-day booleans or day numbers or day names.</p><h2 id="serializable-survives-only-where-a-database-blob-needs-it" tabindex="-1"><a class="header-anchor" href="#serializable-survives-only-where-a-database-blob-needs-it"><span><code>[Serializable]</code> survives only where a database blob needs it</span></a></h2><p><code>BinaryFormatter</code> is obsolete on .NET 8 (SYSLIB0051) and throws on .NET 9 and later, and Quartz 4 ships no binary serializer. The attributes that only <code>BinaryFormatter</code> ever read were on the greater part of the public surface — every exception, every matcher, the job execution context, the job detail. <strong>Seventeen types keep them</strong>, and that is the whole of the keep side; everything else lost them.</p>`,276),e(`p`,null,[l[412]||=r(`The line is drawn at the database. A type keeps `,-1),l[413]||=e(`code`,null,`[Serializable]`,-1),l[414]||=r(`, `,-1),l[415]||=e(`code`,null,`ISerializable`,-1),l[416]||=r(` and `,-1),l[417]||=e(`code`,null,`GetObjectData`,-1),l[418]||=r(` when a job store blob can be made of it, so that a 3.x database whose blobs were written by `,-1),l[419]||=e(`code`,null,`BinaryFormatter`,-1),l[420]||=r(` stays readable while you migrate it to JSON — see `,-1),c(g,{to:`/documentation/quartz-4.x/packages/json-serialization.html#migrating-from-binary-serialization`},{default:i(()=>[...l[411]||=[r(`Migrating from binary serialization`,-1)]]),_:1}),l[421]||=r(`.`,-1)]),l[897]||=t(`<table><thead><tr><th>Blob column</th><th>Types that keep the attributes</th></tr></thead><tbody><tr><td><code>JOB_DETAILS.JOB_DATA</code>, <code>TRIGGERS.JOB_DATA</code></td><td><code>JobDataMap</code>, <code>Key&lt;T&gt;</code>, <code>JobKey</code>, <code>TriggerKey</code></td></tr><tr><td><code>CALENDARS.CALENDAR</code></td><td><code>BaseCalendar</code>, <code>AnnualCalendar</code>, <code>CronCalendar</code>, <code>DailyCalendar</code>, <code>HolidayCalendar</code>, <code>MonthlyCalendar</code>, <code>WeeklyCalendar</code>, <code>CronExpression</code></td></tr><tr><td><code>BLOB_TRIGGERS.BLOB_DATA</code></td><td><code>TriggerBase</code>, <code>SimpleTriggerImpl</code>, <code>CronTriggerImpl</code>, <code>CalendarIntervalTriggerImpl</code>, <code>DailyTimeIntervalTriggerImpl</code></td></tr></tbody></table><p>A trigger reaches that third row when no trigger persistence delegate handles it — a type of your own, or one deriving from any of the five shipped implementations with <code>HasAdditionalProperties</code> returning <code>true</code>. The store writes the whole object into <code>BLOB_TRIGGERS</code>, so the trigger class hierarchy is part of the blob graph.</p><p><code>RecurrenceTriggerImpl</code> is the one trigger implementation <em>not</em> on that list: it does not carry <code>[Serializable]</code>, where <code>TriggerBase</code> and the other four do. In practice a recurrence trigger is written to <code>SIMPROP_TRIGGERS</code> by <code>RecurrenceTriggerPersistenceDelegate</code> and never takes the blob path. It did carry the attribute on 3.x, though, so a 3.x database that somehow holds a binary recurrence-trigger blob (which takes a delegate list with that delegate removed) should have that row migrated to JSON on 3.x before the upgrade.</p><p>3.x listed <code>StringKeyDirtyFlagMap</code> and <code>DirtyFlagMap&lt;TKey, TValue&gt;</code> in the first row; both are internal now. That costs nothing for existing blobs: <code>BinaryFormatter</code> records an <code>ISerializable</code> graph under the runtime type name — <code>Quartz.JobDataMap</code> — plus its <code>version</code>/<code>dirty</code>/<code>map</code> entries, never the base chain, and <code>JobDataMap</code> carries that exact constructor and entry set itself.</p><p>Everything else lost <code>[Serializable]</code>. These are the ones still public enough for you to notice; the rest went with a type that is internal, removed or renamed, and is answered by <a href="#appendix-what-happened-to-a-name">the appendix</a>:</p><table><thead><tr><th>Where</th><th>Types</th></tr></thead><tbody><tr><td>Exceptions</td><td><code>SchedulerException</code>, <code>JobExecutionException</code>, <code>JobPersistenceException</code>, <code>ObjectAlreadyExistsException</code>, <code>SchedulerConfigException</code>, <code>JsonSerializationException</code>, <code>LockException</code>, <code>NoSuchDelegateException</code>, <code>SchedulingDataValidationException</code></td></tr><tr><td>Matchers</td><td><code>AndMatcher&lt;TKey&gt;</code>, <code>GroupMatcher&lt;TKey&gt;</code>, <code>KeyMatcher&lt;TKey&gt;</code>, <code>NameMatcher&lt;TKey&gt;</code>, <code>NotMatcher&lt;TKey&gt;</code>, <code>OrMatcher&lt;TKey&gt;</code>, <code>StringMatcher&lt;TKey&gt;</code>, <code>StringOperator</code></td></tr><tr><td>Everything else</td><td><code>JobType</code>, <code>SchedulerContext</code>, <code>JobExecutionContextImpl</code></td></tr></tbody></table><p>The <code>protected</code> / <code>public</code> <code>(SerializationInfo, StreamingContext)</code> constructors went with them, on <code>SchedulerException</code>, <code>JobPersistenceException</code>, <code>SchedulerConfigException</code> and <code>HttpClientException</code>. If you derive from one of those and forward a <code>SerializationInfo</code> to the base, delete your constructor — the base class library&#39;s <code>Exception(SerializationInfo, StreamingContext)</code> is obsolete too, and nothing calls yours.</p><p><code>Key&lt;T&gt;</code> and its two subclasses are on the keep side because a key can be a <em>value</em> inside a job data map. Quartz never puts one there itself — the recovery entries it writes are strings, and both <code>TriggerBase</code> and <code>JobDetailImpl</code> deliberately mark their key fields <code>[NonSerialized]</code> and serialize the name and group as separate strings — but a job data map holds arbitrary <code>object</code> values and serializes them all, so an application that did <code>jobDataMap.Put(&quot;parent&quot;, jobKey)</code> on 3.x has a <code>JobKey</code> sitting in its <code>JOB_DATA</code>. <code>BinaryFormatter</code> refuses to deserialize an instance whose type is not marked serializable, so removing the attribute would make that blob unreadable even through the compatibility package. Blob-reachable means what the graph <em>can</em> contain, not only what Quartz itself puts there.</p><h2 id="jobexecutionexception-has-four-constructors-and-init-only-flags" tabindex="-1"><a class="header-anchor" href="#jobexecutionexception-has-four-constructors-and-init-only-flags"><span><code>JobExecutionException</code> has four constructors and init-only flags</span></a></h2><p>The exception had seven constructors, three of which existed only to smuggle the <code>refireImmediately</code> flag in positionally — <code>new JobExecutionException(ex, true)</code> said nothing at the call site about what the <code>true</code> meant. The four that remain mirror every other exception&#39;s shape — <code>()</code>, <code>(message)</code>, <code>(cause)</code>, <code>(message, cause)</code> — and the three scheduler directives (<code>RefireImmediately</code>, <code>UnscheduleFiringTrigger</code>, <code>UnscheduleAllTriggers</code>) are init-only properties:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> throw new JobExecutionException(msg, ex, true);</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> throw new JobExecutionException(msg, ex) { RefireImmediately = true };</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>An exception&#39;s instructions are fixed at the throw site, which is what init-only says; nothing could meaningfully flip them on a caught instance in flight. <code>JobDetail</code> — which the scheduler fills in on the way to the listeners — is read-only outside Quartz for the same reason.</p><h3 id="it-is-sealed-and-exception-data-is-what-a-subclass-was-for" tabindex="-1"><a class="header-anchor" href="#it-is-sealed-and-exception-data-is-what-a-subclass-was-for"><span>It is sealed, and <code>Exception.Data</code> is what a subclass was for</span></a></h3><p><code>JobExecutionException</code> is <code>sealed</code> in 4.0. What it says is three instructions to the scheduler, and the scheduler reads exactly those three: a subclass adding a fourth would be a directive nothing acts on. Frameworks that subclassed it were not adding a directive — they were carrying their own state past the scheduler to their own listener, and <code>Exception.Data</code> does that:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> public sealed class ImportJobException : JobExecutionException</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"> {</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line">     public string TenantId { get; init; }</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"> }</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"> throw new ImportJobException(ex) { TenantId = tenantId, RefireImmediately = true };</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> throw new JobExecutionException(ex) { RefireImmediately = true, Data = { [&quot;tenant&quot;] = tenantId } };</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>An instance a job throws is handed to <code>IJobListener.JobWasExecuted</code> <strong>unchanged</strong> — the run shell only fills in <code>JobDetail</code> — so the listener reads <code>jobException.Data[&quot;tenant&quot;]</code> straight off it.</p><p>Wrapping an exception of your own as the cause carries typed state just as well, and is the closer replacement for a subclass:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token keyword">throw</span> <span class="token keyword">new</span> <span class="token constructor-invocation class-name">JobExecutionException</span><span class="token punctuation">(</span><span class="token keyword">new</span> <span class="token constructor-invocation class-name">ImportFailed</span><span class="token punctuation">(</span>tenantId<span class="token punctuation">)</span><span class="token punctuation">)</span> <span class="token punctuation">{</span> RefireImmediately <span class="token operator">=</span> <span class="token boolean">true</span> <span class="token punctuation">}</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line"><span class="token comment">// in the listener</span></span>
<span class="line"><span class="token keyword">if</span> <span class="token punctuation">(</span>jobException<span class="token punctuation">?.</span>InnerException <span class="token keyword">is</span> <span class="token class-name">ImportFailed</span> failure<span class="token punctuation">)</span> <span class="token punctuation">{</span> … <span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>One shape to know when you write that listener: an exception that is <em>not</em> a <code>JobExecutionException</code> arrives wrapped twice — <code>JobExecutionException</code> → <code>JobExecutionProcessException</code> → your exception — so a listener that wants to handle both routes walks <code>InnerException</code> until it finds what it knows.</p><h2 id="the-two-exceptions-moved-out-of-quartz-core" tabindex="-1"><a class="header-anchor" href="#the-two-exceptions-moved-out-of-quartz-core"><span>The two exceptions moved out of <code>Quartz.Core</code></span></a></h2><p><code>Quartz.Core</code> held exactly two public types, and both were exceptions: <code>JobExecutionProcessException</code> and <code>JobInstantiationException</code>. Every one of their siblings — <code>SchedulerException</code>, which they both derive from, <code>JobExecutionException</code>, <code>SchedulerConfigException</code> — lives in <code>Quartz</code>. The two moved there too, and <code>Quartz.Core</code> is now internal from top to bottom: nothing in it is a type you can name.</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> using Quartz.Core;</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"></span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line"> public ValueTask SchedulerError(string msg, SchedulerException cause, CancellationToken ct = default)</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> public ValueTask SchedulerError(IScheduler scheduler, SchedulerErrorContext error, CancellationToken ct = default)</span>
<span class="line"></span></span><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line"> {</span>
<span class="line"></span></span><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line">     if (cause is JobInstantiationException failure) { … }</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line">     if (error.Exception is JobInstantiationException failure) { … }</span>
<span class="line"></span></span><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line">     return default;</span>
<span class="line"></span><span class="token prefix unchanged"> </span><span class="token line"> }</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Catching or type-testing either exception needs no change beyond deleting the <code>using Quartz.Core;</code>, which the compiler flags as unused. <code>JobInstantiationException</code> is new in 4.0 and has never shipped, so nobody is catching it under the old namespace yet.</p><h2 id="execution-limits-are-built-once-then-frozen" tabindex="-1"><a class="header-anchor" href="#execution-limits-are-built-once-then-frozen"><span>Execution limits are built once, then frozen</span></a></h2><p><code>ExecutionLimits</code> used to be two things at the same time. It was a mutable fluent builder — <code>ForGroup</code>, <code>ForDefaultGroup</code>, <code>ForOtherGroups</code> and <code>Unlimited</code> all mutated <code>this</code> and returned <code>this</code> — and it was an <code>IReadOnlyDictionary&lt;string, int?&gt;</code>, a collection whose name had to suppress CA1710 to survive analysis. The scheduler defended itself against the mutable half by snapshotting whatever it was handed, which is a hint that the type was doing one job too many. It is now two types: <code>ExecutionLimitsBuilder</code> mutates, <code>ExecutionLimits</code> is the immutable snapshot that <code>Build()</code> returns and that the scheduler thread reads.</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> await scheduler.SetExecutionLimits(new ExecutionLimits()</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> await scheduler.SetExecutionLimits(ExecutionLimitsBuilder.Create()</span>
<span class="line"></span></span><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line">     .ForGroup(&quot;batch-jobs&quot;, 2)</span>
<span class="line"></span><span class="token prefix unchanged"> </span><span class="token line">     .ForDefaultGroup(10)</span>
<span class="line"></span></span><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line">     .ForOtherGroups(5));</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line">     .ForOtherGroups(5)</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">     .Build());</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>IQuartzBuilder.UseExecutionLimits</code> takes an <code>Action&lt;ExecutionLimitsBuilder&gt;</code>, so a configuration lambda is unchanged and still reads the same way:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line">q<span class="token punctuation">.</span><span class="token function">UseExecutionLimits</span><span class="token punctuation">(</span>limits <span class="token operator">=&gt;</span> limits<span class="token punctuation">.</span><span class="token function">ForGroup</span><span class="token punctuation">(</span><span class="token string">&quot;high-cpu&quot;</span><span class="token punctuation">,</span> <span class="token number">3</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>Reading limits back is what changed shape, because the snapshot is not a dictionary:</p><table><thead><tr><th>Before</th><th>4.0</th></tr></thead><tbody><tr><td><code>limits[&quot;heavy&quot;]</code></td><td><code>limits.TryGetLimit(ExecutionGroupScope.Named(&quot;heavy&quot;), out int? maxConcurrent)</code></td></tr><tr><td><code>limits[ExecutionLimits.DefaultGroupKey]</code></td><td><code>limits.TryGetLimit(ExecutionGroupScope.Default, out int? maxConcurrent)</code></td></tr><tr><td><code>limits[&quot;*&quot;]</code></td><td><code>limits.TryGetLimit(ExecutionGroupScope.OtherGroups, out int? maxConcurrent)</code></td></tr><tr><td><code>limits.ContainsKey(&quot;heavy&quot;)</code></td><td><code>limits.TryGetLimit(ExecutionGroupScope.Named(&quot;heavy&quot;), out _)</code></td></tr><tr><td><code>limits.Count</code></td><td><code>limits.Groups.Count</code>, or <code>limits.IsEmpty</code> for the question actually being asked</td></tr><tr><td><code>foreach (KeyValuePair&lt;string, int?&gt; pair in limits)</code></td><td><code>foreach (ExecutionGroupLimit limit in limits.Groups)</code></td></tr></tbody></table><p><code>TryGetLimit</code> returning <code>false</code> is not the same as unlimited: it means the scope has no entry of its own, and a named group without one still falls back to <code>ExecutionGroupScope.OtherGroups</code>. That distinction was invisible when the type was a dictionary, and it is the reason the lookup is a <code>TryGet</code> rather than an indexer.</p><p>The read side is typed instead of spelled. <code>ExecutionGroupScope</code> is a readonly record struct with exactly the three cases the builder can write — <code>Default</code> (triggers with no execution group), <code>OtherGroups</code> (the catch-all) and <code>Named(name)</code> — mirroring how <code>PreferredNode</code> models none/auto/named rather than using a nullable string with a sentinel. <code>ExecutionGroupLimit.Group</code> is that value, so enumerating <code>Groups</code> no longer requires knowing that <code>null</code> meant the default bucket and <code>&quot;*&quot;</code> meant the catch-all:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token keyword">foreach</span> <span class="token punctuation">(</span><span class="token class-name">ExecutionGroupLimit</span> limit <span class="token keyword">in</span> limits<span class="token punctuation">.</span>Groups<span class="token punctuation">)</span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    <span class="token class-name"><span class="token keyword">string</span></span> label <span class="token operator">=</span> limit<span class="token punctuation">.</span>Group<span class="token punctuation">.</span>IsDefault <span class="token punctuation">?</span> <span class="token string">&quot;(default)&quot;</span></span>
<span class="line">        <span class="token punctuation">:</span> limit<span class="token punctuation">.</span>Group<span class="token punctuation">.</span>IsOtherGroups <span class="token punctuation">?</span> <span class="token string">&quot;(other groups)&quot;</span></span>
<span class="line">        <span class="token punctuation">:</span> limit<span class="token punctuation">.</span>Group<span class="token punctuation">.</span>Name<span class="token operator">!</span><span class="token punctuation">;</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>ExecutionGroupLimit.Scope</code> is a different thing — <code>ExecutionLimitScope.Node</code> or <code>.Cluster</code>, added by <a href="#an-execution-limit-can-be-cluster-wide">cluster-wide ceilings</a>. The two words sit side by side on purpose: <code>Group</code> says <em>which</em> bucket the limit is for, <code>Scope</code> says <em>what it is counted against</em>.</p><p>The configuration spellings are unchanged: <code>quartz.executionLimit.*</code> keys and the HTTP API still say <code>*</code> for the catch-all and <code>_</code> or <code>null</code> for the default bucket (neither a property key nor a JSON object key can be empty). All three remain reserved: a trigger cannot have <code>&quot;*&quot;</code>, <code>&quot;_&quot;</code> or <code>&quot;null&quot;</code> as its execution group, and <code>ExecutionGroupScope.Named</code> rejects them the same way <code>ExecutionLimitsBuilder.ForGroup</code> does.</p><h3 id="a-job-store-is-handed-the-limits-and-a-way-to-spend-them" tabindex="-1"><a class="header-anchor" href="#a-job-store-is-handed-the-limits-and-a-way-to-spend-them"><span>A job store is handed the limits, and a way to spend them</span></a></h3><p><code>TriggerAcquisitionRequest.ExecutionLimits</code> and <code>TriggerAcquisitionCriteria.ExecutionLimits</code> carry <code>ExecutionLimits</code> rather than <code>IReadOnlyDictionary&lt;string, int?&gt;</code>. What they carry is still the <em>available</em> slots — the configured limits less what is already running on this node — not the configuration.</p><p>A store acquiring triggers has to count those slots down as it takes them, and the rule for doing so is not obvious: a group&#39;s own entry wins, a named group without one falls back to <code>OtherGroups</code>, a trigger with no execution group never does, and an unlisted group that borrows from <code>OtherGroups</code> gets its own allowance rather than sharing one. That rule used to live inside Quartz where only the two built-in stores could reach it, so a third-party store either reimplemented it or ignored execution groups. It is now <code>ExecutionSlots</code>:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token keyword">public</span> <span class="token keyword">override</span> <span class="token keyword">async</span> <span class="token return-type class-name">ValueTask<span class="token punctuation">&lt;</span>List<span class="token punctuation">&lt;</span>IOperableTrigger<span class="token punctuation">&gt;</span><span class="token punctuation">&gt;</span></span> <span class="token function">AcquireNextTriggers</span><span class="token punctuation">(</span></span>
<span class="line">    <span class="token class-name">TriggerAcquisitionRequest</span> request<span class="token punctuation">,</span> <span class="token class-name">CancellationToken</span> cancellationToken <span class="token operator">=</span> <span class="token keyword">default</span><span class="token punctuation">)</span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    <span class="token class-name">ExecutionSlots<span class="token punctuation">?</span></span> slots <span class="token operator">=</span> request<span class="token punctuation">.</span>ExecutionLimits<span class="token punctuation">?.</span><span class="token function">CreateSlots</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line">    <span class="token keyword">foreach</span> <span class="token punctuation">(</span><span class="token class-name">IOperableTrigger</span> candidate <span class="token keyword">in</span> <span class="token function">Candidates</span><span class="token punctuation">(</span>request<span class="token punctuation">)</span><span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">{</span></span>
<span class="line">        <span class="token keyword">if</span> <span class="token punctuation">(</span>slots <span class="token keyword">is</span> <span class="token keyword">not</span> <span class="token keyword">null</span> <span class="token operator">&amp;&amp;</span> <span class="token operator">!</span>slots<span class="token punctuation">.</span><span class="token function">TryTake</span><span class="token punctuation">(</span>candidate<span class="token punctuation">.</span>ExecutionGroup<span class="token punctuation">,</span> candidate<span class="token punctuation">.</span>Key<span class="token punctuation">.</span>Group<span class="token punctuation">)</span><span class="token punctuation">)</span></span>
<span class="line">        <span class="token punctuation">{</span></span>
<span class="line">            <span class="token keyword">continue</span><span class="token punctuation">;</span> <span class="token comment">// this group is forbidden here, or has run out for this pass</span></span>
<span class="line">        <span class="token punctuation">}</span></span>
<span class="line">        …</span>
<span class="line">    <span class="token punctuation">}</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Create the ledger per acquisition attempt, not per store: it is mutable and not thread-safe by design, and a retried acquisition has to start from the limits again rather than from what the failed attempt had counted down. <code>CreateSlots()</code> leaves the snapshot untouched, so the same <code>ExecutionLimits</code> can produce as many as you need.</p><p>A clustered store of your own has one more thing to do — see <a href="#an-execution-limit-can-be-cluster-wide">cluster-wide ceilings</a>.</p><h2 id="interruption-has-two-names-not-three" tabindex="-1"><a class="header-anchor" href="#interruption-has-two-names-not-three"><span>Interruption has two names, not three</span></a></h2><p>Stopping a running job had three names in the public API. <code>IScheduler.Interrupt(JobKey)</code> and <code>IScheduler.InterruptFireInstance(fireInstanceId)</code> requested it, <code>IJobExecutionContext.CancellationToken</code> — the same token a job receives as the <code>cancellationToken</code> parameter of <code>IJob.Execute</code> — observed it, and <code>ICancellableJobExecutionContext.Cancel()</code> sat between them looking like a third, supported way to do it. It was not: calling it bypassed the scheduler, so no <code>ISchedulerListener.JobInterrupted</code> was raised, and it worked only for a context you happened to be holding in the same process.</p><p><code>ICancellableJobExecutionContext</code> is gone from the public API, and <code>JobExecutionContextImpl.Cancel()</code> with it. The plumbing still exists inside Quartz under the name the public API already uses for the concept. Ask the scheduler instead:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> ((ICancellableJobExecutionContext) context).Cancel();</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> await scheduler.InterruptFireInstance(context.FireInstanceId);</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>The fire instance id the call needs comes from <code>IScheduler.QueryFireInstances</code>, which replaced <code>GetCurrentlyExecutingJobs</code> — see <a href="#what-is-running-is-a-listing-not-a-list-of-contexts">what is running is a listing</a>. Its element type never was the cancellable interface either, only the documentation said so.</p><h3 id="unabletointerruptjobexception-is-gone" tabindex="-1"><a class="header-anchor" href="#unabletointerruptjobexception-is-gone"><span><code>UnableToInterruptJobException</code> is gone</span></a></h3><p>The exception dates from the Java lineage, where interrupting a job that did not implement <code>InterruptableJob</code> had to fail. In 4.0 interruption is cancellation: every job receives the cancellation token, so there is no such thing as a job that cannot be asked to stop, and the exception had no throw site left. <code>Interrupt(JobKey)</code> and <code>InterruptFireInstance(fireInstanceId)</code> keep their semantics — they set the token on the matching executions and return whether they found any; whether the job honors the token remains the job&#39;s business. A <code>catch (UnableToInterruptJobException)</code> block can simply be deleted; <code>HttpScheduler</code>&#39;s error mapping no longer resurrects the type either, so a remote scheduler fault on an interrupt call surfaces as the <code>SchedulerException</code>-derived type the server actually reported.</p><h2 id="timezoneutil-became-quartz-timezones" tabindex="-1"><a class="header-anchor" href="#timezoneutil-became-quartz-timezones"><span><code>TimeZoneUtil</code> became <code>Quartz.TimeZones</code></span></a></h2><p><code>FindTimeZoneById</code> is how a trigger built with <code>InTimeZone(...)</code> comes back out of a job store, and the wall-clock <code>GetUtcOffset(DateTime, TimeZoneInfo)</code> overload <em>is</em> the scheduler-wide daylight saving policy — an ambiguous local time resolves to the daylight offset, the first of the two occurrences. That is scheduling API, not a utility, so 4.0 stops calling it one: the type is <code>TimeZones</code> — a static class named by its domain, the way <code>Matchers</code> is — and it lives in the root namespace next to the builders whose behavior it defines. <code>FindTimeZoneById</code> sheds the words the type name now carries and reads <code>TimeZones.FindById(id)</code>. Every file under a <code>Quartz.*</code> namespace sees it without any <code>using</code>; elsewhere, <code>using Quartz.Util;</code> becomes <code>using Quartz;</code>:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> using Quartz.Util;</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> using Quartz;</span>
<span class="line"></span></span></span>
<span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> var zone = TimeZoneUtil.FindTimeZoneById(&quot;Europe/Helsinki&quot;);</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> var zone = TimeZones.FindById(&quot;Europe/Helsinki&quot;);</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>No configuration shim is needed: <code>TimeZones</code> is a static class that is called, never a type a configuration string names and the scheduler instantiates.</p><p>Two members went internal on the way: <code>ConvertTime(DateTimeOffset, TimeZoneInfo)</code> and <code>GetUtcOffset(DateTimeOffset, TimeZoneInfo)</code> were Mono-era shims over the <code>TimeZoneInfo</code> calls they forward to — call <code>TimeZoneInfo</code> directly. The wall-clock <code>GetUtcOffset(DateTime, TimeZoneInfo)</code> overload is the daylight saving policy, not a shim, and stays public.</p><p>The id alias table also stays: on Windows, &quot;Coordinated Universal Time&quot; and &quot;CET&quot; fail <code>TimeZoneInfo.FindSystemTimeZoneById</code> and both <code>TryConvert*</code> conversions even on ICU, and resolve through the table alone. Its one dead entry — &quot;US Central Standard Time&quot; ↔ &quot;US/Indiana-Stark&quot;, where each side aliased the other and neither is a system id on Windows — was pruned; both ids now fail with the exception that points at <code>Quartz.Plugins.TimeZoneConverter</code>. When the direct lookup and the aliases have both failed, <code>FindById</code> now also asks <code>TimeZoneInfo.TryConvertIanaIdToWindowsId</code> before consulting the registered resolvers. The conversion runs <em>after</em> the direct lookup on purpose: run first, it would turn &quot;US/Eastern&quot; into a <code>TimeZoneInfo</code> whose <code>Id</code> is &quot;Eastern Standard Time&quot;, and that rewritten Id is what a job store writes back to <code>TIME_ZONE_ID</code>.</p><h3 id="customresolver-became-addresolver" tabindex="-1"><a class="header-anchor" href="#customresolver-became-addresolver"><span><code>CustomResolver</code> became <code>AddResolver</code></span></a></h3><p><code>CustomResolver</code> was one settable delegate, which made every installer overwrite the previous one: two schedulers running <code>Quartz.Plugins.TimeZoneConverter</code> in the same process fought over the slot, and shutting either scheduler down left the winner&#39;s resolver installed for the rest of the process&#39;s life. <code>AddResolver</code> composes instead — each caller gets an <code>IDisposable</code> registration back, and disposing it removes exactly that resolver:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> TimeZoneUtil.CustomResolver = id =&gt; Resolve(id);</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> IDisposable registration = TimeZones.AddResolver(id =&gt; Resolve(id));</span>
<span class="line"></span></span><span class="token unchanged"><span class="token prefix unchanged"> </span><span class="token line"> ...</span>
<span class="line"></span></span><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> TimeZoneUtil.CustomResolver = null;</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> registration.Dispose();</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Resolvers are consulted <strong>most recently added first</strong>, so a later registration shadows an earlier one for the ids it resolves — which is exactly the last-write-wins behavior assigning <code>CustomResolver</code> had, minus the data loss. A resolver declines an id by returning <code>null</code>, or by throwing <code>TimeZoneNotFoundException</code> — the search catches it and continues with the next resolver, and <code>FindById</code> itself throws only after every fallback has failed.</p><p><code>UseTimeZoneConverter</code> is what registers the resolver now, targeting <code>TZConvert.TryGetTimeZoneInfo</code> so that an unknown id declines quietly instead of by exception — see <a href="#timezoneconverterplugin-is-a-resolver-registration"><code>TimeZoneConverterPlugin</code> is a resolver registration</a>.</p><h3 id="timezoneconverterplugin-is-a-resolver-registration" tabindex="-1"><a class="header-anchor" href="#timezoneconverterplugin-is-a-resolver-registration"><span><code>TimeZoneConverterPlugin</code> is a resolver registration</span></a></h3><p><code>Quartz.Plugins.TimeZoneConverter</code> stays, and <code>UseTimeZoneConverter</code> stays; what is gone is the plugin between them. <code>TimeZoneConverterPlugin</code> was one <code>TimeZones.AddResolver</code> call wearing a plugin&#39;s lifecycle — no per-scheduler state, no scheduler to depend on, nothing for <code>Initialize</code> to be handed — so <code>UseTimeZoneConverter</code> performs the registration itself.</p><table><thead><tr><th>3.x / earlier 4.0 preview</th><th>4.0</th></tr></thead><tbody><tr><td><code>q.UseTimeZoneConverter()</code></td><td>unchanged</td></tr><tr><td><code>q.AddPlugin&lt;TimeZoneConverterPlugin&gt;()</code></td><td><code>q.UseTimeZoneConverter()</code></td></tr><tr><td><code>quartz.plugin.timeZoneConverter.type = Quartz.Plugin.TimeZoneConverter.TimeZoneConverterPlugin, Quartz.Plugins.TimeZoneConverter</code></td><td>remove the key; the type no longer exists, so a configuration still naming it fails to load. Call <code>UseTimeZoneConverter()</code> instead</td></tr><tr><td><code>new TimeZoneConverterPlugin()</code></td><td>nothing; <code>TimeZones.AddResolver(id =&gt; …)</code> is the public seam if you want a resolver of your own</td></tr></tbody></table><p>Two things change for the better, and one is worth planning around:</p><ul><li><strong>The registration happens while you are configuring, not when the scheduler starts.</strong> Time zone lookup is reached from places with no scheduler in scope — building a trigger, parsing a <code>CronExpression</code>, deserializing a trigger out of a job store — so a trigger built before the host starts now resolves its zone too.</li><li><strong>Nothing removes the resolver again.</strong> The plugin disposed its own registration on <code>Shutdown</code>, and had to take care not to disturb the other schedulers in the process while doing so. One registration that outlives every scheduler is the same guarantee with none of the bookkeeping, and a second <code>UseTimeZoneConverter</code> is a no-op rather than a duplicate resolver on a list nothing prunes.</li><li><strong>A test that asserted an id does <em>not</em> resolve now has to account for the process-wide resolver.</strong> It was never really removable — the plugin only removed it once every scheduler had shut down — but it is now unambiguously permanent for the life of the process that installed it.</li></ul><h2 id="triggerutils-became-triggerfiretimes" tabindex="-1"><a class="header-anchor" href="#triggerutils-became-triggerfiretimes"><span><code>TriggerUtils</code> became <code>TriggerFireTimes</code></span></a></h2><p>It computes fire times by advancing a copy of a trigger through its schedule, applying the calendar at each step, which is exactly what <code>IOperableTrigger</code> adds over <code>ITrigger</code> — so it is named for the fire times it computes and lives in <code>Quartz.Extensibility</code> with that contract, rather than in the root namespace next to <code>IScheduler</code>. The type name carries the &quot;fire times&quot; half, so the methods stop repeating it:</p><table><thead><tr><th>3.x</th><th>4.x</th></tr></thead><tbody><tr><td><code>TriggerUtils.ComputeFireTimes(...)</code></td><td><code>TriggerFireTimes.Compute(...)</code></td></tr><tr><td><code>TriggerUtils.ComputeFireTimesBetween(...)</code></td><td><code>TriggerFireTimes.ComputeBetween(...)</code></td></tr><tr><td><code>TriggerUtils.ComputeEndTimeToAllowParticularNumberOfFirings(...)</code></td><td><code>TriggerFireTimes.ComputeEndTimeForCount(...)</code></td></tr></tbody></table><p>Parameters and behavior are unchanged:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> using Quartz.Extensibility;</span>
<span class="line"></span></span></span>
<span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> var times = TriggerUtils.ComputeFireTimes((IOperableTrigger) trigger, calendar, 10);</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> var times = TriggerFireTimes.Compute(trigger, calendar, 10);</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The cast is gone: each of the three has an <code>ITrigger</code> overload beside its <code>IOperableTrigger</code> one, which does the cast and answers with an <code>ArgumentException</code> naming the type if the trigger is one that cannot be advanced. Testing a schedule is the most mainstream use these have, and requiring the cast taught a <code>Quartz.Extensibility</code> type to people who needed neither. Additive: the <code>IOperableTrigger</code> overloads are unchanged, and a call site that casts still binds to them.</p><h2 id="other-breaking-changes" tabindex="-1"><a class="header-anchor" href="#other-breaking-changes"><span>Other Breaking Changes</span></a></h2>`,71),e(`table`,null,[l[714]||=e(`thead`,null,[e(`tr`,null,[e(`th`,null,`Change`),e(`th`,null,`Details`)])],-1),e(`tbody`,null,[l[464]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`SimpleTriggerImpl.GetFireTimeBefore(DateTimeOffset? endUtc)`),r(` takes a non-nullable `),e(`code`,null,`DateTimeOffset`)]),e(`td`,null,[r(`The nullable parameter was a lie: after the one guard, 3.x dereferenced `),e(`code`,null,`endUtc!.Value`),r(`, so passing null threw rather than meaning "no bound". A caller holding a nullable end checks it first. `),e(`code`,null,`EndTimeUtc`),r(` itself is still `),e(`code`,null,`DateTimeOffset?`)])],-1),l[465]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`QuartzScheduler`),r(` and `),e(`code`,null,`QuartzSchedulerResources`),r(` are internal`)]),e(`td`,null,[r(`Resolve `),e(`code`,null,`IScheduler`),r(` / `),e(`code`,null,`ISchedulerFactory`),r(`; scheduler-wide settings are `),e(`code`,null,`QuartzSchedulerOptions`)])],-1),l[466]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`JobType`),r(` introduced`)]),e(`td`,null,[r(`Stores job type info without requiring an actual `),e(`code`,null,`Type`),r(` instance. A `),e(`code`,null,`Type`),r(` converts implicitly (validated: it must implement `),e(`code`,null,`IJob`),r(`); a string converts only explicitly or via the constructor, because resolving the name is deferred and can fail — `),e(`code`,null,`Type`),r(` throws for a name that does not resolve, `),e(`code`,null,`TryResolve`),r(` is the non-throwing probe. Equality (`),e(`code`,null,`Equals`),r(`, `),e(`code`,null,`==`),r(`/`),e(`code`,null,`!=`),r(`) is by `),e(`code`,null,`FullName`),r(`. There is deliberately `),e(`strong`,null,`no`),r(` implicit conversion back to `),e(`code`,null,`Type`),r(`: `),e(`code`,null,`jobDetail.JobType.Type`),r(` spells out that assembly probing may happen, and can throw, at that read`)])],-1),l[467]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`JobBuilder.OfType(JobType)`),r(` added, and it is the only name-taking spelling`)]),e(`td`,null,[r(`It carries a stored type name and its resolver through a rebuild without forcing the name to resolve. The `),e(`code`,null,`OfType(string)`),r(` an earlier 4.0 preview had is gone: `),e(`code`,null,`OfType((JobType) typeName)`),r(` says the same thing and shows the leap the explicit cast exists to show — see `),e(`a`,{href:`#the-builder-surface-says-each-thing-once`},`The builder surface says each thing once`)])],-1),l[468]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`IJobDetail.GetJobBuilder()`),r(` removed from the interface`)]),e(`td`,null,[r(`The same call still compiles: it is an extension method on `),e(`code`,null,`IJobDetail`),r(` in the `),e(`code`,null,`Quartz`),r(` namespace, built from the detail's public state. Only an implementation of the interface has to change — see `),e(`a`,{href:`#an-ijobdetail-of-your-own`},[r(`An `),e(`code`,null,`IJobDetail`),r(` of your own`)])])],-1),l[469]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`IJobDetail.WithJobData(JobDataMap)`),r(` added`)]),e(`td`,null,[r(`The one member a job store needs of a detail it cannot construct: a copy of it carrying the given data. `),e(`code`,null,`RAMJobStore`),r(` calls it where it used to rebuild the detail through `),e(`code`,null,`JobBuilder`),r(`, so a custom `),e(`code`,null,`IJobDetail`),r(` survives its first `),e(`code`,null,`[PersistJobDataAfterExecution]`),r(` completion — see `),e(`a`,{href:`#an-ijobdetail-of-your-own`},[r(`An `),e(`code`,null,`IJobDetail`),r(` of your own`)])])],-1),l[470]||=e(`tr`,null,[e(`td`,null,[r(`The `),e(`code`,null,`Try*`),r(` members carry the nullability attributes their shape implies`)]),e(`td`,null,[r(`Not breaking, and nothing behaves differently: `),e(`code`,null,`CronExpression.TryParse`),r(` marks its input `),e(`code`,null,`[NotNullWhen(true)]`),r(`, as `),e(`code`,null,`JobKey.TryParse`),r(`, `),e(`code`,null,`TriggerKey.TryParse`),r(` and `),e(`code`,null,`MonthDay.TryParse`),r(` already did, and `),e(`code`,null,`JobDataMap.TryGetValue`),r(` / `),e(`code`,null,`SchedulerContext.TryGetValue`),r(` mark `),e(`code`,null,`value`),r(),e(`code`,null,`[MaybeNullWhen(false)]`),r(`, as `),e(`code`,null,`Dictionary<,>.TryGetValue`),r(` does. They show up in the public API baselines, which is why they are listed here`)])],-1),l[471]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`ObjectAlreadyExistsException.JobKey`),r(` / `),e(`code`,null,`.TriggerKey`),r(` added`)]),e(`td`,null,[r(`The identity that clashed, set by the constructor that already took the job detail or the trigger, and `),e(`code`,null,`null`),r(` on the message-only constructor. The message is unchanged, so a handler that was parsing the key back out of it still works`)])],-1),l[472]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`StdAdoDelegate.ConvertFromProperty`),r(` returns `),e(`code`,null,`Dictionary<string, object?>`)]),e(`td`,null,[r(`It returned the non-generic `),e(`code`,null,`System.Collections.IDictionary`),r(`, the last one on the public surface, and its only caller copied the entries into a `),e(`code`,null,`Dictionary<string, object?>`),r(` to build a `),e(`code`,null,`JobDataMap`),r(` out of them. An override changes its return type and drops nothing else`)])],-1),l[473]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`DailyTimeIntervalTriggerImpl`),r(` rounds `),e(`code`,null,`StartTimeUtc`),r(` and `),e(`code`,null,`EndTimeUtc`),r(` down to the whole second`)]),e(`td`,null,[e(`code`,null,`CronTriggerImpl.StartTimeUtc`),r(` always did; this trigger did not, and its fire times are a whole number of intervals counted in whole seconds from the start of the day. A start of `),e(`code`,null,`22:50:00.68`),r(` with an 02:15 start-of-day and a five-minute interval therefore computed a first fire time of `),e(`code`,null,`22:50:00.000`),r(` — before the trigger's own start (`),e(`a`,{href:`https://github.com/quartznet/quartznet/issues/3386`,target:`_blank`,rel:`noopener noreferrer`},`#3386`),r(`). A boundary time carrying milliseconds now loses them; the fire times themselves are unchanged`)])],-1),l[474]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`RescheduleJob`),r(` recomputes a fire time that has fallen behind the start time`)]),e(`td`,null,[r(`Handing `),e(`code`,null,`IScheduler.RescheduleJob`),r(` a trigger that already carries fire times still keeps a next fire time you set — that is what makes `),e(`code`,null,`GetTrigger`),r(` → mutate → `),e(`code`,null,`RescheduleJob`),r(` work, and it is how a replacement carries the cadence of the trigger it replaces. What it no longer keeps is one `),e(`em`,null,`behind the trigger's own start time`),r(`: rescheduling advances the start of a never-fired repeating simple trigger whose start is in the past, and a trigger stored with a next fire time before its start fires there and then immediately again at the start, because that is what its own `),e(`code`,null,`GetFireTimeAfter`),r(` answers (`),e(`a`,{href:`https://github.com/quartznet/quartznet/issues/3554`,target:`_blank`,rel:`noopener noreferrer`},`#3554`),r(`). The first fire time is computed from the adjusted start instead. No job store recomputed anything here — both of them store the trigger as it was handed over`)])],-1),l[475]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`SchedulingDataValidationException`),r(` derives from `),e(`code`,null,`SchedulerException`)]),e(`td`,null,[r(`It derived straight from `),e(`code`,null,`System.Exception`),r(`, the one break in the hierarchy, so a `),e(`code`,null,`catch (SchedulerException)`),r(` around loading an XML or JSON scheduling data file missed exactly the failure the file was most likely to have. `),e(`code`,null,`catch (Exception)`),r(` and `),e(`code`,null,`catch (SchedulingDataValidationException)`),r(` behave as before`)])],-1),e(`tr`,null,[l[434]||=e(`td`,null,[e(`code`,null,`HttpScheduler.Context`),r(` and `),e(`code`,null,`.ListenerManager`),r(` throw `),e(`code`,null,`NotSupportedException`)],-1),e(`td`,null,[l[423]||=r(`A scheduler reached over HTTP has neither in this process. `,-1),l[424]||=e(`code`,null,`Context`,-1),l[425]||=r(` used to make a `,-1),l[426]||=e(`strong`,null,`synchronous`,-1),l[427]||=r(` HTTP call from a property getter and hand back a detached copy that could not be written back; `,-1),l[428]||=e(`code`,null,`ListenerManager`,-1),l[429]||=r(` threw a `,-1),l[430]||=e(`code`,null,`SchedulerException`,-1),l[431]||=r(`. `,-1),l[432]||=e(`code`,null,`UpdateTriggerDetails`,-1),l[433]||=r(` throws the same exception with the same message shape, because the API has no endpoint for it — see `,-1),c(g,{to:`/documentation/quartz-4.x/packages/http-client.html#what-is-not-supported-remotely`},{default:i(()=>[...l[422]||=[r(`what is not supported remotely`,-1)]]),_:1})])]),l[476]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`RecoveringTriggerKey`),r(` behavior`)]),e(`td`,null,[e(`code`,null,`IJobExecutionContext.RecoveringTriggerKey`),r(` now returns `),e(`code`,null,`null`),r(` when not recovering instead of throwing`)])],-1),l[477]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`IScheduler.Shutdown`),r(`'s token bounds the wait for running jobs`)]),e(`td`,null,[r(`It was ignored on that path: 3.x and early 4.0 handed the thread pool `),e(`code`,null,`CancellationToken.None`),r(`, so `),e(`code`,null,`Shutdown(waitForJobsToComplete: true, ct)`),r(` waited however long the slowest job took. It now stops waiting when the token fires — the rest of the shutdown still runs, and the jobs are not cancelled — see `),e(`a`,{href:`#drain-is-the-shutdown-that-can-be-given-a-deadline`},[e(`code`,null,`Drain`),r(` is the shutdown that can be given a deadline`)])])],-1),l[478]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`QuartzHostedService`),r(` shuts its schedulers down concurrently`)]),e(`td`,null,[r(`With more than one scheduler registered, the host's stop time was the `),e(`em`,null,`sum`),r(` of their waits for running jobs, which is what overran `),e(`code`,null,`HostOptions.ShutdownTimeout`),r(`. Each scheduler owns its own pool, store and thread, so there was nothing for them to serialize behind`)])],-1),l[479]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`DictionaryExtensions`),r(` removed`)]),e(`td`,null,[e(`code`,null,`Quartz.Util.DictionaryExtensions`),r(` type was removed`)])],-1),l[480]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`AdoJobStoreBase`),r(` connection methods`)]),e(`td`,null,[e(`code`,null,`GetLocalTransactionConnection`),r(` (was `),e(`code`,null,`GetNonManagedTXConnection`),r(`) and `),e(`code`,null,`GetConnection`),r(` now return `),e(`code`,null,`ValueTask<ConnectionAndTransactionHolder>`)])],-1),l[481]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`JobStoreSupport.UseProperties`),r(),e(`code`,null,`string`),r(` setter removed`)]),e(`td`,null,[r(`The `),e(`code`,null,`bool`),r(),e(`code`,null,`AdoJobStoreOptions.StoreJobDataAsStrings`),r(` option and the read-only `),e(`code`,null,`CanUseProperties`),r(` remain; the property bridge parses the key`)])],-1),l[482]||=e(`tr`,null,[e(`td`,null,[r(`Protected `),e(`code`,null,`AdoJobStoreBase`),r(` / `),e(`code`,null,`StdAdoDelegate`),r(` members take a `),e(`code`,null,`CancellationToken`)]),e(`td`,null,`Overrides have to add the parameter; callers do not`)],-1),l[483]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`ConnectionAndTransactionHolder.Close`),r(` takes a `),e(`code`,null,`CancellationToken`)]),e(`td`,null,[e(`code`,null,`.Commit`),r(` and `),e(`code`,null,`.Rollback`),r(` took one too, and are now internal — see `),e(`a`,{href:`#a-job-store-of-your-own-can-join-your-transaction`},`A job store of your own can join your transaction`)])],-1),l[484]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`IJobConfigurator<TJob>`),r(` members return `),e(`code`,null,`IJobConfigurator<TJob>`)]),e(`td`,null,[e(`code`,null,`JobBuilder<TJob>`),r(` implements them explicitly and keeps its own `),e(`code`,null,`JobBuilder<TJob>`),r(`-returning members, so `),e(`code`,null,`JobBuilder.Create()…`),r(` chains are unaffected — see `),e(`a`,{href:`#job-data-can-name-the-property`},`Job data can name the property`),r(` for the type parameter`)])],-1),l[485]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`UsingJobData`),r(` takes an `),e(`code`,null,`object?`)]),e(`td`,null,[r(`The nine primitive overloads collapsed into one — see `),e(`a`,{href:`#nine-usingjobdata-overloads-became-one`},[r(`Nine `),e(`code`,null,`UsingJobData`),r(` overloads became one`)])])],-1),l[486]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`IDirectoryScanListener`),r(` is asynchronous`)]),e(`td`,null,[e(`code`,null,`FilesUpdatedOrAdded`),r(` and `),e(`code`,null,`FilesDeleted`),r(` return `),e(`code`,null,`ValueTask`),r(` and take a `),e(`code`,null,`CancellationToken`)])],-1),l[487]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`SendMailJob.Send`),r(` is asynchronous`)]),e(`td`,null,[e(`code`,null,`protected virtual ValueTask Send(MailInfo mailInfo, CancellationToken cancellationToken = default)`),r(`. It uses `),e(`code`,null,`SmtpClient.SendMailAsync`),r(`, so a job fired on the scheduler's thread pool no longer blocks it for the length of an SMTP round trip, and `),e(`code`,null,`Execute`),r(` forwards its token. An override returns `),e(`code`,null,`default`),r(` where it used to return nothing`)])],-1),l[488]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`SendMailJob.MailInfo`),r(` is `),e(`code`,null,`Quartz.Jobs.MailInfo`)]),e(`td`,null,[r(`The nested class became a top-level `),e(`code`,null,`sealed`),r(` type in the same namespace, with `),e(`code`,null,`required`),r(`/`),e(`code`,null,`init`),r(` members: `),e(`code`,null,`MailMessage`),r(` and `),e(`code`,null,`SmtpHost`),r(` are required, `),e(`code`,null,`SmtpPort`),r(` and `),e(`code`,null,`Credentials`),r(` are optional. An override of `),e(`code`,null,`Send`),r(` keeps compiling — `),e(`code`,null,`MailInfo`),r(` resolves through the `),e(`code`,null,`Quartz.Jobs`),r(` using — and code that constructed one with an object initializer keeps compiling too. Assigning to a property after construction does not: build the whole value in the initializer. The two credential strings became one `),e(`code`,null,`Credentials`),r(`, see `),e(`a`,{href:`#the-smtp-password-does-not-belong-in-job-data`},`The SMTP password does not belong in job data`)])],-1),l[489]||=e(`tr`,null,[e(`td`,null,[r(`The jobs in `),e(`code`,null,`Quartz.Jobs`),r(` have options types`)]),e(`td`,null,[e(`code`,null,`DirectoryScanOptions`),r(`, `),e(`code`,null,`FileScanOptions`),r(`, `),e(`code`,null,`NativeJobOptions`),r(` and `),e(`code`,null,`SendMailOptions`),r(`, written by `),e(`code`,null,`Using*Options(…)`),r(` on the job's configurator. The job data keys are unchanged and configuring key by key still works — see `),e(`a`,{href:`#the-shipped-jobs-are-configured-by-name`},`The shipped jobs are configured by name`)])],-1),l[490]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`JobDataMap.GetEnumerator`),r(` returns the interface`)]),e(`td`,null,[e(`code`,null,`IEnumerator<KeyValuePair<string, object?>>`),r(` rather than `),e(`code`,null,`Dictionary<string, object?>.Enumerator`),r(`, matching `),e(`code`,null,`SchedulerContext`),r(`. `),e(`code`,null,`foreach`),r(` is unaffected; a variable declared as the concrete struct type needs retyping`)])],-1),l[491]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`CronTriggerImpl.WillFireOn`),r(` is one method`)]),e(`td`,null,[e(`code`,null,`WillFireOn(DateTimeOffset timeUtc, bool dayOnly = false)`),r(`. The two overloads differed only by that default. Both call shapes compile unchanged`)])],-1),l[492]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`JobExecutionContextImpl.IncrementRefireCount()`),r(` and the `),e(`code`,null,`JobRunTime`),r(` setter are internal`)]),e(`td`,null,[r(`Both record what the scheduler observed while running the job; `),e(`code`,null,`JobRunShell`),r(` is the only caller, and writing either from a job or a listener reported a fire that never happened`)])],-1),l[493]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`LoggingJobHistoryPlugin.Name`),r(`, `),e(`code`,null,`LoggingTriggerHistoryPlugin.Name`),r(` are get-only`)]),e(`td`,null,[r(`The name is handed to a plugin by `),e(`code`,null,`Initialize`),r(`; writing it afterwards did nothing`)])],-1),l[494]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`TimeSpanParseRuleAttribute`),r(` is public`)]),e(`td`,null,[r(`It says how a bare number in configuration is read as a `),e(`code`,null,`TimeSpan`),r(`, which a component configured by the same keys needs to be able to say`)])],-1),l[495]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`TimeZoneUtil.CustomResolver`),r(` became `),e(`code`,null,`TimeZones.AddResolver(...)`)]),e(`td`,null,[r(`Returns an `),e(`code`,null,`IDisposable`),r(` whose disposal removes exactly that resolver; resolvers are consulted most recently added first — see `),e(`a`,{href:`#customresolver-became-addresolver`},[e(`code`,null,`CustomResolver`),r(` became `),e(`code`,null,`AddResolver`)])])],-1),l[496]||=e(`tr`,null,[e(`td`,null,`Setter-only members gained getters`),e(`td`,null,[e(`code`,null,`DbMetadata.DbBinaryTypeName`),r(` and `),e(`code`,null,`.ParameterDbTypePropertyName`),r(`, both now `),e(`code`,null,`string?`),r(`. A description that names a `),e(`code`,null,`DbBinaryTypeName`),r(` without the property to write it to used to fail as `),e(`code`,null,`Type.GetProperty(null)`),r(`, two lines away from the deliberate message its neighbours raise; it raises that message now, and the annotation says which of the pair may be absent`)])],-1),l[497]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`TriggerState.Executing`),r(` added`)]),e(`td`,null,[r(`Reported where `),e(`code`,null,`Normal`),r(`, `),e(`code`,null,`Complete`),r(` or `),e(`code`,null,`Blocked`),r(` used to be, and `),e(`code`,null,`Blocked`),r(` narrowed to mean a sibling trigger is running (see `),e(`a`,{href:`#executing-is-a-trigger-state`},`Executing is a trigger state`),r(`)`)])],-1),l[498]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`IDriverDelegate.IsTriggerCurrentlyExecuting`),r(` removed`)]),e(`td`,null,[r(`Replaced by `),e(`code`,null,`SelectTriggerStateWithExecuting`),r(`, which reads the state and the execution in one statement and returns `),e(`code`,null,`TriggerExecutionState`)])],-1),l[499]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`StdAdoConstants.SqlSelectCountExecutingFiredTriggersOfTrigger`),r(` removed`)]),e(`td`,null,[r(`Removed with the method that used it; the per-job `),e(`code`,null,`SqlSelectCountExecutingFiredTriggersOfJob`),r(` remains — both on what is now an internal type`)])],-1),l[500]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`StdAdoConstants`),r(` and `),e(`code`,null,`IAdoUtil`),r(` are internal`)]),e(`td`,null,[r(`Statement text is not a contract; the schema names stay public on `),e(`code`,null,`AdoConstants`),r(`, which is a static class rather than a base class`)])],-1),l[501]||=e(`tr`,null,[e(`td`,null,[r(`Trigger persistence delegates are all public and `),e(`code`,null,`sealed`)]),e(`td`,null,[e(`code`,null,`CronTriggerPersistenceDelegate`),r(`, `),e(`code`,null,`SimpleTriggerPersistenceDelegate`),r(` and `),e(`code`,null,`DailyTimeIntervalTriggerPersistenceDelegate`),r(` were internal; derive from `),e(`code`,null,`SimplePropertiesTriggerPersistenceDelegateBase`),r(` for a delegate of your own`)])],-1),l[502]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`SimplePropertiesTriggerProperties`),r(` is an init-only `),e(`code`,null,`record`)]),e(`td`,null,[r(`The payload of that seam: one side of it builds a row, the other reads one, and neither edits what the other made. Build it with an object initializer — `),e(`code`,null,`new SimplePropertiesTriggerProperties { Int1 = …, String1 = … }`),r(` — where a property was assigned after construction, and use a `),e(`code`,null,`with`),r(` expression to derive one from another`)])],-1),l[503]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`SchedulerConstants`),r(` is a `),e(`code`,null,`static class`)]),e(`td`,null,[r(`It was a `),e(`code`,null,`struct`),r(` holding only `),e(`code`,null,`const`),r(`s; constant references are unchanged`)])],-1),l[504]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`MisfireInstruction`),r(` is internal`)]),e(`td`,null,`The five per-family enums are the vocabulary; every constant has an enum member with the same value`)],-1),l[505]||=e(`tr`,null,[e(`td`,null,[r(`Every public `),e(`code`,null,`*Options`),r(` type is `),e(`code`,null,`sealed`),r(`, including `),e(`code`,null,`QuartzHttpApiOptions`),r(` and `),e(`code`,null,`HttpClientOptions`)]),e(`td`,null,[r(`None had a virtual member, and Quartz constructs each of them itself, so a subclass was never resolved. `),e(`code`,null,`QuartzHostedService`),r(` itself stays open for `),e(`code`,null,`AddQuartzHostedService<T>`)])],-1),l[506]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`QuartzOptions.Scheduling`),r(` is get-only`)]),e(`td`,null,[r(`Set its properties rather than assigning a new instance; assignment discarded what other callbacks and `),e(`code`,null,`Quartz:Scheduling`),r(` had already set`)])],-1),l[507]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`InternalTriggerState.Executing`),r(` removed`)]),e(`td`,null,`It was never assigned or read; RAMJobStore counts executions separately from the state that drives scheduling`)],-1),l[508]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`ScheduleBuilder<T>`),r(` removed`)]),e(`td`,null,[r(`The five schedule builders implement `),e(`code`,null,`IScheduleBuilder`),r(` directly — see `),e(`a`,{href:`#schedulebuilder-t-is-gone`},[e(`code`,null,`ScheduleBuilder<T>`),r(` is gone`)])])],-1),l[509]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`DailyTimeIntervalScheduleBuilder`),r(`'s day-set fields are internal`)]),e(`td`,null,[e(`code`,null,`AllDaysOfTheWeek`),r(`, `),e(`code`,null,`MondayThroughFriday`),r(` and `),e(`code`,null,`SaturdayAndSunday`),r(` are reached through `),e(`code`,null,`OnEveryDay()`),r(`, `),e(`code`,null,`OnMondayThroughFriday()`),r(` and `),e(`code`,null,`OnSaturdayAndSunday()`)])],-1),l[510]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`PreserveHourOfDayAcrossDaylightSavings`),r(` and `),e(`code`,null,`SkipDayIfHourDoesNotExist`),r(` default to `),e(`code`,null,`true`)]),e(`td`,null,`Turning the flag on reads as a call with no argument; passing the value still works`)],-1),l[511]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`TimeOfDay`),r(` removed`)]),e(`td`,null,[e(`code`,null,`TimeOnly`),r(` replaces it — see `),e(`a`,{href:`#timeofday-became-timeonly`},[e(`code`,null,`TimeOfDay`),r(` became `),e(`code`,null,`TimeOnly`)])])],-1),l[512]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`DailyCalendar`),r(` has one constructor`)]),e(`td`,null,[r(`Two `),e(`code`,null,`TimeOnly`),r(` values and an optional base calendar — see `),e(`a`,{href:`#dailycalendar-takes-two-timeonly-values`},[e(`code`,null,`DailyCalendar`),r(` takes two `),e(`code`,null,`TimeOnly`),r(` values`)])])],-1),l[513]||=e(`tr`,null,[e(`td`,null,[r(`Calendar `),e(`code`,null,`SetDayExcluded`),r(` / `),e(`code`,null,`AddExcludedDate`),r(` removed`)]),e(`td`,null,[e(`code`,null,`AddExcludedDay`),r(` / `),e(`code`,null,`RemoveExcludedDay`),r(` over a read-only set — see `),e(`a`,{href:`#excluded-days-are-a-read-only-set`},`Excluded days are a read-only set`)])],-1),l[514]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`CronCalendar.SetCronExpressionString`),r(` removed`)]),e(`td`,null,[r(`Assign `),e(`code`,null,`CronExpression`),r(` instead; the property already accepted a parsed expression`)])],-1),l[515]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`JobDataMap(IDictionary)`),r(` removed`)]),e(`td`,null,[e(`code`,null,`JobDataMap(IDictionary<string, object?>)`),r(` remains and absorbed the dirty-marker handling`)])],-1),l[516]||=e(`tr`,null,[e(`td`,null,[r(`A `),e(`code`,null,`decimal`),r(` reader was added to the accessor set`)]),e(`td`,null,[r(`A `),e(`code`,null,`decimal`),r(` could be written but not read back; it is `),e(`code`,null,`Get<decimal>`),r(` / `),e(`code`,null,`TryGet<decimal>`),r(` — see `),e(`a`,{href:`#one-accessor-per-type-for-the-types-job-data-is-made-of`},`One accessor per type`)])],-1),l[517]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`DirtyFlagMap<TKey, TValue>`),r(` and `),e(`code`,null,`StringKeyDirtyFlagMap`),r(` are internal`)]),e(`td`,null,[e(`code`,null,`JobDataMap`),r(` and `),e(`code`,null,`SchedulerContext`),r(` are sealed, self-contained dictionaries; the typed accessors are extension members — see `),e(`a`,{href:`#jobdatamap-and-schedulercontext-stand-alone`},`JobDataMap and SchedulerContext stand alone`)])],-1),l[518]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`JobDataMap.Dirty`),r(` / `),e(`code`,null,`ClearDirtyFlag()`),r(` are internal`)]),e(`td`,null,[r(`Clearing the flag from a job silently skipped the `),e(`code`,null,`[PersistJobDataAfterExecution]`),r(` rewrite; `),e(`code`,null,`SchedulerConstants.ForceJobDataMapDirty`),r(` remains the supported way to force one`)])],-1),l[519]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`JobDataMap.Equals`),r(` compares values, not just keys`)]),e(`td`,null,[r(`Two maps with the same keys but different values no longer compare equal, and equal maps hash equally; `),e(`code`,null,`SchedulerContext`),r(` compares by reference`)])],-1),l[520]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`SchedulerContext`),r(` is backed by `),e(`code`,null,`ConcurrentDictionary`)]),e(`td`,null,`Reading and writing it concurrently is safe; enumeration no longer races plugin writes`)],-1),l[521]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`PropertySettingJobFactory`),r(` no longer merges the scheduler context into job properties`)]),e(`td`,null,[e(`strong`,null,`Silent behavioral change`),r(` — read `),e(`code`,null,`context.Scheduler.Context`),r(` in `),e(`code`,null,`Execute`),r(`, or override `),e(`code`,null,`BuildJobDataMap`),r(` — see `),e(`a`,{href:`#scheduler-context-entries-are-no-longer-injected-into-job-properties`},`Scheduler context entries are no longer injected into job properties`)])],-1),l[522]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`JobKey`),r(` and `),e(`code`,null,`TriggerKey`),r(` implement `),e(`code`,null,`IEquatable<T>`),r(` and `),e(`code`,null,`IParsable<T>`)]),e(`td`,null,[r(`Additive. `),e(`code`,null,`TryParse`),r(`/`),e(`code`,null,`Parse`),r(` invert `),e(`code`,null,`ToString`),r(`'s `),e(`code`,null,`<group>.<name>`),r(` form, splitting at the first '.' — a `),e(`em`,null,`group`),r(` containing '.' is the ambiguous case. Every job-store dictionary probe also stops paying for the object-comparer path, and the hash is computed once`)])],-1),l[523]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`PutAsString`),r(` writes round-trip ("O") formats for `),e(`code`,null,`DateTime`),r(`/`),e(`code`,null,`DateTimeOffset`)]),e(`td`,null,[r(`Sub-second precision and Kind/offset survive; the dedicated `),e(`code`,null,`DateTime`),r(` overload also `),e(`strong`,null,`rebinds`),r(` calls that used to hit the `),e(`code`,null,`IConvertible`),r(` one — see `),e(`a`,{href:`#putasstring-writes-round-trip-formats-now`},[e(`code`,null,`PutAsString`),r(` writes round-trip formats now`)])])],-1),l[524]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Get<DateTime>`),r(` parses with `),e(`code`,null,`DateTimeStyles.RoundtripKind`)]),e(`td`,null,[e(`strong`,null,`Behavioral`),r(`: a stored string ending in `),e(`code`,null,`Z`),r(` now returns the UTC clock reading with `),e(`code`,null,`Kind=Utc`),r(` instead of a local-shifted `),e(`code`,null,`Kind=Local`),r(` value — see `),e(`a`,{href:`#putasstring-writes-round-trip-formats-now`},[e(`code`,null,`PutAsString`),r(` writes round-trip formats now`)])])],-1),l[525]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`PutAsString(string, Guid?)`),r(` removed`)]),e(`td`,null,[e(`code`,null,`null`),r(` wrote a present-but-null entry no reader could read back — see `),e(`a`,{href:`#putasstring-string-guid-is-gone`},[e(`code`,null,`PutAsString(string, Guid?)`),r(` is gone`)])])],-1),l[526]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`DateOnly`),r(`/`),e(`code`,null,`TimeOnly`),r(`/enum accessors, `),e(`code`,null,`PutAsString(DateOnly/TimeOnly)`),r(` and `),e(`code`,null,`TryGet<T>`),r(` added`)]),e(`td`,null,`Additive; job data catches up with the types 4.0 made primary`)],-1),l[527]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`SystemTextJsonSerializerOptions`),r(` and `),e(`code`,null,`NewtonsoftJsonSerializerOptions`),r(` removed`)]),e(`td`,null,[r(`The `),e(`code`,null,`Use*JsonSerializer`),r(` callback hands you the registry itself; lambda bodies compile unchanged, `),e(`code`,null,`RegisterTriggerConverters`),r(` is a parameter — see `),e(`a`,{href:`#custom-trigger-and-calendar-serializers-are-no-longer-static`},`Custom trigger and calendar serializers are no longer static`)])],-1),l[528]||=e(`tr`,null,[e(`td`,null,[r(`Newtonsoft `),e(`code`,null,`ICalendarSerializer.CalendarTypeName`),r(` added (default interface member)`)]),e(`td`,null,[r(`Existing implementations compile unchanged; the registry indexes a named serializer under both the assembly-qualified name (which 3.x payloads carry, and always stays) and the discriminator, case-insensitively — see `),e(`a`,{href:`#newtonsoft-types-moved-out-of-the-core-namespaces`},`Newtonsoft types moved out of the core namespaces`)])],-1),l[529]||=e(`tr`,null,[e(`td`,null,[r(`Newtonsoft calendar contracts moved to `),e(`code`,null,`Quartz.Serialization.Newtonsoft.Calendars`)]),e(`td`,null,[r(`Namespace symmetry with `),e(`code`,null,`Quartz.Serialization.SystemTextJson.Calendars`),r(`; source-only`)])],-1),l[530]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`RecurrenceTriggerSerializer`),r(` unsealed in both packages`)]),e(`td`,null,`It was the one sealed built-in trigger serializer; deriving from a built-in serializer for a subclassed trigger is a supported scenario on all five`)],-1),l[531]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`CalendarIntervalTriggerImpl`),r(`, `),e(`code`,null,`DailyTimeIntervalTriggerImpl`),r(` and `),e(`code`,null,`RecurrenceTriggerImpl`),r(` unsealed`)]),e(`td`,null,[r(`The other half of that scenario: three of the five concrete triggers were sealed, so there was no subclassed trigger for a derived serializer to serialize. All five are subclassable now, as `),e(`code`,null,`CronTriggerImpl`),r(` and `),e(`code`,null,`SimpleTriggerImpl`),r(` always were. Purely a loosening — nothing was widened to `),e(`code`,null,`protected`),r(`, and a sealed type becoming unsealed breaks no caller`)])],-1),l[532]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`ISchedulerFactory.GetAllSchedulers`),r(` returns `),e(`code`,null,`ValueTask<List<IScheduler>>`)]),e(`td`,null,`Quartz returns concrete collection types from its query members for allocation and enumeration cost; this was the one that did not`)],-1),l[533]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`IInstanceIdGenerator.GenerateInstanceId`),r(` returns `),e(`code`,null,`ValueTask<string>`)]),e(`td`,null,`It never returned null, and a null instance id is not a usable one`)],-1),l[534]||=e(`tr`,null,[e(`td`,null,[r(`An `),e(`code`,null,`IJobStore`),r(` that implements `),e(`code`,null,`IJobListener`),r(` no longer receives events automatically`)]),e(`td`,null,[r(`Register it as a job listener through the scheduler's `),e(`code`,null,`IListenerManager`)])],-1),l[535]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`[Serializable]`),r(` removed from `),e(`code`,null,`TriggerFiredBundle`),r(` and `),e(`code`,null,`TriggerFiredResult`)]),e(`td`,null,`It has meant nothing since binary serialization was dropped`)],-1),l[536]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`TriggerFiredResult`),r(` is made by three factories`)]),e(`td`,null,[e(`code`,null,`TriggerFiredResult.Fired(bundle)`),r(`, `),e(`code`,null,`TriggerFiredResult.NotFired`),r(` and `),e(`code`,null,`TriggerFiredResult.Failed(exception)`),r(` replace the two constructors, which between them could build a result carrying both a bundle and an exception, or neither by accident — `),e(`code`,null,`new TriggerFiredResult((TriggerFiredBundle?) null)`),r(` was how a store said "this trigger turned out not to be firable". The three outcomes are now named, and the cast that used to be needed to pick a constructor overload is gone`)])],-1),l[537]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`StdSchedulerFactory`),r(` removed`)]),e(`td`,null,[e(`code`,null,`QuartzSchedulerBuilder.Create().UseProperties(properties)`),r(` — see `),e(`a`,{href:`#stdschedulerfactory-is-gone`},[e(`code`,null,`StdSchedulerFactory`),r(` is gone`)]),r(` for every removed constant`)])],-1),l[538]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`GetScheduler()`),r(` after `),e(`code`,null,`Shutdown()`),r(` throws`)]),e(`td`,null,[r(`3.x built a fresh scheduler, because the factory constructed every part itself. The container owns those lifetimes now, so the same call would re-initialize the thread pool and job store the shutdown just tore down and hand back the same closed instance. It throws `),e(`code`,null,`SchedulerException`),r(` instead. Use `),e(`code`,null,`Standby()`),r(`/`),e(`code`,null,`Start()`),r(` to pause and resume a scheduler, or build a new host or container for a fresh one`)])],-1),l[539]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`QuartzSchedulerBuilder.Create`),r(` takes the `),e(`code`,null,`AddQuartz`),r(` callback`)]),e(`td`,null,[r(`Its five duplicated members and `),e(`code`,null,`Configure(Action<IQuartzBuilder>)`),r(` are gone; configuration is written in `),e(`code`,null,`Create(q => …)`),r(`, against the same `),e(`code`,null,`IQuartzBuilder`),r(),e(`code`,null,`AddQuartz`),r(` hands out, and the builder itself declares only `),e(`code`,null,`Create`),r(`, `),e(`code`,null,`Build`),r(`, `),e(`code`,null,`BuildScheduler`),r(`, `),e(`code`,null,`UseConfiguration`),r(` and `),e(`code`,null,`UseProperties`),r(` ×2 — see `),e(`a`,{href:`#the-standalone-builder-is-the-same-builder`},`The standalone builder is the same builder`)])],-1),l[540]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`IQuartzBuilder`),r(` gained `),e(`code`,null,`UseThreadPool(IThreadPool)`),r(` and `),e(`code`,null,`UseJobStore(IJobStore)`)]),e(`td`,null,[r(`A pre-built part can be handed to a scheduler registered with `),e(`code`,null,`AddQuartz`),r(`, not only to a standalone one`)])],-1),l[541]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`IQuartzBuilder.ConfigureJobScope(...)`)]),e(`td`,null,[r(`The `),e(`code`,null,`ConfigureScope`),r(` hook as a delegate, so preparing a job's scope no longer needs a job factory of your own — see `),e(`a`,{href:`#the-job-scope-is-prepared-without-writing-a-job-factory`},`The job scope is prepared without writing a job factory`)])],-1),l[542]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`IQuartzBuilder`),r(` gained `),e(`code`,null,`UseJobStore<T>`),r(` / `),e(`code`,null,`<T, TOptions>`),r(` / factory`)]),e(`td`,null,[r(`The seam for a job store of your own, built by the container with its scheduler's collaborators — see `),e(`a`,{href:`#a-component-of-your-own-is-chosen-the-same-way-a-shipped-one-is`},`A component of your own is chosen the same way a shipped one is`)])],-1),l[543]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`IQuartzBuilder`),r(` gained `),e(`code`,null,`UseInstanceIdGenerator<T>`),r(` / `),e(`code`,null,`<T, TOptions>`),r(` / instance`)]),e(`td`,null,[r(`Replaces `),e(`code`,null,`quartz.scheduler.instanceIdGenerator.type`),r(`, and sets `),e(`code`,null,`GenerateInstanceId`),r(` because choosing a generator means the id is generated`)])],-1),l[544]||=e(`tr`,null,[e(`td`,null,[r(`The builder's listener overloads take `),e(`code`,null,`params IReadOnlyCollection<IMatcher<T>>`)]),e(`td`,null,[r(`Aligned with `),e(`code`,null,`IListenerManager`),r(`; existing call sites are unaffected`)])],-1),l[545]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`AddQuartzHttpApi`),r(` is on `),e(`code`,null,`IServiceCollection`),r(`, and only there`)]),e(`td`,null,[r(`The API is container-wide, not one scheduler's — `),e(`code`,null,`services.AddQuartzHttpApi()`),r(`. The `),e(`code`,null,`IQuartzBuilder`),r(` form an earlier 4.0 preview kept is gone`)])],-1),l[546]||=e(`tr`,null,[e(`td`,null,[r(`Clustering settings moved to `),e(`code`,null,`ClusteringOptions`)]),e(`td`,null,[e(`code`,null,`AdoJobStoreOptions.Clustered`),r(` and the two `),e(`code`,null,`ClusterCheckin*`),r(` settings are gone; `),e(`code`,null,`IJobStore.Clustered`),r(` reports the state rather than setting it — see `),e(`a`,{href:`#clustering-is-configured-in-one-place`},`Clustering is configured in one place`)])],-1),l[547]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`AdoJobStoreBase`),r(`'s constructor takes `),e(`code`,null,`IOptions<ClusteringOptions>`)]),e(`td`,null,[r(`Between `),e(`code`,null,`storeOptions`),r(` and `),e(`code`,null,`objectSerializer`),r(`; a job store deriving from it has to pass one on`)])],-1),l[548]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`UseSQLite`),r(` is `),e(`code`,null,`UseSystemDataSqlite`),r(`, `),e(`code`,null,`UseMicrosoftSQLite`),r(` is `),e(`code`,null,`UseSqlite`)]),e(`td`,null,[e(`strong`,null,`The short name changed meaning`),r(` — see `),e(`a`,{href:`#the-sqlite-extension-methods-swapped-names`},`The SQLite extension methods swapped names`)])],-1),l[549]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`UseDataSourceConnectionProvider()`),r(` removed`)]),e(`td`,null,[e(`code`,null,`DataSourceOptions.UseRegisteredDataSource`),r(`, which is what it set`)])],-1),l[550]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`AddDataSourceProvider()`),r(` removed`)]),e(`td`,null,[r(`Its other half. It registered `),e(`code`,null,`DataSourceDbProvider`),r(` in the container for `),e(`code`,null,`UseDataSourceConnectionProvider()`),r(` to name; `),e(`code`,null,`UseRegisteredDataSource`),r(` builds the provider itself from the registered `),e(`code`,null,`DbDataSource`),r(` — see `),e(`a`,{href:`#adddatasourceprovider-went-with-it`},[e(`code`,null,`AddDataSourceProvider()`),r(` went with it`)])])],-1),e(`tr`,null,[l[444]||=e(`td`,null,[r(`Every `),e(`code`,null,`Use<Db>`),r(` gained a `),e(`code`,null,`(DbProviderFactory factory, string connectionString)`),r(` overload`)],-1),e(`td`,null,[l[436]||=e(`code`,null,`UseSqlServer(SqlClientFactory.Instance, connectionString)`,-1),l[437]||=r(` and its siblings reach the driver through the factory it ships, so no type is resolved from a string and none is constructed by reflection. This is the registration a `,-1),l[438]||=e(`code`,null,`PublishTrimmed`,-1),l[439]||=r(` or `,-1),l[440]||=e(`code`,null,`PublishAot`,-1),l[441]||=r(` application uses; it takes the connection string directly, so `,-1),l[442]||=e(`code`,null,`ConnectionStringName`,-1),l[443]||=r(` does not apply to it — see `,-1),c(g,{to:`/documentation/quartz-4.x/configuration/reference.html#naming-a-driver-or-handing-over-its-factory`},{default:i(()=>[...l[435]||=[r(`Naming a driver, or handing over its factory`,-1)]]),_:1})])]),l[551]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`UseOracle(factory, connectionString, configureCommand, configureBinaryParameter)`)]),e(`td`,null,[r(`Oracle is the one shipped driver that needs more than a factory: ODP.NET binds by position unless `),e(`code`,null,`OracleCommand.BindByName`),r(` is set, and reads `),e(`code`,null,`DbType.Binary`),r(` as `),e(`code`,null,`OracleDbType.Raw`),r(`, which holds two kilobytes of a job data map. Naming the driver instead says both for you`)])],-1),l[552]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`UseGenericDatabase(factory, connectionString, DbMetadata)`)]),e(`td`,null,`A driver Quartz ships no description for, reached through its own factory and described in code. No provider name, because the description arrived rather than being looked up`)],-1),l[553]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`DbMetadata`),r(` gained `),e(`code`,null,`ConfigureCommand`),r(` and `),e(`code`,null,`ConfigureBinaryParameter`)]),e(`td`,null,[r(`The two things the name path reaches by reflecting over `),e(`code`,null,`CommandType`),r(` and `),e(`code`,null,`ParameterType`),r(`, said as lambdas by the application that references the driver. Every `),e(`code`,null,`Type`),r(` on `),e(`code`,null,`DbMetadata`),r(` is optional now: a description behind a factory or a `),e(`code`,null,`DbDataSource`),r(` names none, and a binary parameter with neither a seam nor a described parameter type binds as `),e(`code`,null,`DbType.Binary`)])],-1),l[554]||=e(`tr`,null,[e(`td`,null,[r(`The `),e(`code`,null,`Use<Db>`),r(` overloads that take a name carry `),e(`code`,null,`[RequiresUnreferencedCode]`)]),e(`td`,null,[r(`Including `),e(`code`,null,`UseGenericDatabase(provider, …)`),r(`. The warning surfaces inside your `),e(`code`,null,`UsePersistentStore`),r(` callback and names the two ways out; it does not reach `),e(`code`,null,`AddQuartz`),r(`, so an application on the in-memory store is told nothing`)])],-1),l[555]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`ProviderFactoryDbProvider`),r(` added`)]),e(`td`,null,[r(`The `),e(`code`,null,`IDbProvider`),r(` those overloads register, public so that `),e(`code`,null,`UseConnectionProvider(_ => new ProviderFactoryDbProvider(metadata, factory, connectionString))`),r(` can build one over a factory no overload knows about`)])],-1),l[556]||=e(`tr`,null,[e(`td`,null,[r(`The Oracle driver description says `),e(`code`,null,`DbBinaryTypeName = "Blob"`)]),e(`td`,null,[r(`It named no binary type, so ODP.NET inferred `),e(`code`,null,`Raw`),r(` from the `),e(`code`,null,`byte[]`),r(` and capped a job data map at 2000 bytes in SQL. Both ways of reaching Oracle now write a `),e(`code`,null,`BLOB`)])],-1),l[557]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`QuartzOptions.SchedulerName`),r(`, `),e(`code`,null,`.SchedulerId`),r(`, `),e(`code`,null,`.MisfireThreshold`),r(` removed`)]),e(`td`,null,[r(`Each duplicated a typed option — see `),e(`a`,{href:`#quartzoptions-lost-its-three-typed-settings`},[e(`code`,null,`QuartzOptions`),r(` lost its three typed settings`)])])],-1),l[558]||=e(`tr`,null,[e(`td`,null,`Job execution metrics are published by every scheduler`),e(`td`,null,[r(`The meters were configured only by `),e(`code`,null,`StdSchedulerFactory`),r(`, so a scheduler registered with `),e(`code`,null,`AddQuartz`),r(` published none`)])],-1),l[559]||=e(`tr`,null,[e(`td`,null,[e(`strong`,null,`Every instrument and attribute was renamed, and two instruments were dropped`)]),e(`td`,null,[e(`code`,null,`scheduling.quartz.*`),r(` became `),e(`code`,null,`quartz.job.execution.*`),r(`, unprefixed attributes became `),e(`code`,null,`quartz.*`),r(`, and the two counters gave way to the histogram's own count. Every dashboard, alert and recording rule keyed on the old names breaks — the `),e(`a`,{href:`#old-and-new-telemetry-names`},`old → new table`),r(` is the complete mapping`)])],-1),l[560]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`quartz.job.execution.duration`),r(` records `),e(`strong`,null,`seconds`),r(`, not milliseconds`)]),e(`td`,null,[r(`A histogram's default bucket boundaries assume seconds, so every execution over ten seconds landed in the top bucket. A chart with a hard-coded millisecond axis reads 1000× low until it is changed — see `),e(`a`,{href:`#job-execution-metrics`},`Job execution metrics`)])],-1),l[561]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`quartz.job.execution.active`),r(` is an `),e(`code`,null,`UpDownCounter<long>`),r(`, unit `),e(`code`,null,`{job}`)]),e(`td`,null,[r(`It was a `),e(`code`,null,`Counter<long>`),r(` receiving the `),e(`code`,null,`-1`),r(` that ends an execution, which an exporter aggregating a monotonic sum may drop; `),e(`code`,null,`ea`),r(` is not a UCUM unit — see `),e(`a`,{href:`#job-execution-metrics`},`Job execution metrics`)])],-1),l[562]||=e(`tr`,null,[e(`td`,null,[r(`Every job execution measurement is tagged with `),e(`code`,null,`quartz.scheduler.name`)]),e(`td`,null,[r(`A process can run several schedulers, whose measurements used to arrive as one series. One series per scheduler where there was one in total: a query that aggregated across everything needs to say so — see `),e(`a`,{href:`#job-execution-metrics`},`Job execution metrics`)])],-1),l[563]||=e(`tr`,null,[e(`td`,null,[r(`Every measurement is tagged with `),e(`code`,null,`quartz.scheduler.id`),r(` too`)]),e(`td`,null,[r(`A cluster is several schedulers sharing one name, so the name alone never answered `),e(`em`,null,`which node`),r(`. The id was a span attribute only. One series per node where there was one per scheduler — see `),e(`a`,{href:`#job-execution-metrics`},`Job execution metrics`)])],-1),l[564]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`quartz.execution.group`),r(` on the execution span and the execution measurements`)]),e(`td`,null,`The bucket a thread limit is applied per, so a saturated group can be seen in the signals rather than inferred. A trigger that names no group carries no such attribute at all, rather than an empty one`)],-1),l[565]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`ActivityTags.ExecutionGroup`),r(`, `),e(`code`,null,`.JobStoreOperation`),r(`, `),e(`code`,null,`.RecoveredInstanceId`),r(` added`)]),e(`td`,null,`The three new attribute names, as constants beside the ones that were already there`)],-1),e(`tr`,null,[l[456]||=e(`td`,null,`Five new instruments cover misfires, acquisition and the cluster`,-1),e(`td`,null,[l[446]||=e(`code`,null,`quartz.trigger.misfire`,-1),l[447]||=r(`, `,-1),l[448]||=e(`code`,null,`quartz.trigger.acquisition.duration`,-1),l[449]||=r(`, `,-1),l[450]||=e(`code`,null,`quartz.trigger.acquired`,-1),l[451]||=r(`, `,-1),l[452]||=e(`code`,null,`quartz.cluster.checkin.duration`,-1),l[453]||=r(`, `,-1),l[454]||=e(`code`,null,`quartz.cluster.recovery.trigger`,-1),l[455]||=r(` — none of those events produced any measurement before — see `,-1),c(g,{to:`/documentation/quartz-4.x/packages/opentelemetry-integration.html#metrics`},{default:i(()=>[...l[445]||=[r(`the observability page`,-1)]]),_:1})])]),l[566]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`quartz.jobstore.operation.duration`),r(` measures every store round trip`)]),e(`td`,null,[r(`Named by `),e(`code`,null,`quartz.jobstore.operation`),r(`, whose value is the same string the operation's span is called, and tagged with `),e(`code`,null,`error.type`),r(` when the call failed`)])],-1),l[567]||=e(`tr`,null,[e(`td`,null,[e(`strong`,null,[r(`Every store emits `),e(`code`,null,`Quartz.JobStore.*`),r(` spans, not only the ADO.NET one`)])]),e(`td`,null,[r(`Store tracing was thirty-three call sites inside `),e(`code`,null,`AdoJobStoreBase`),r(`, so the in-memory store, a community package's store and a store of your own produced none. It is a decorator over `),e(`code`,null,`IJobStore`),r(` now, applied once by the registration that builds a scheduler's resources. The span names, kind and attributes are unchanged. A store the container builds is therefore not the object the scheduler holds — anything comparing `),e(`code`,null,`IJobStore`),r(` instances or type-testing one has to unwrap `),e(`code`,null,`Quartz.Impl.DelegatingJobStore`),r(` first, and `),e(`code`,null,`IScheduler.GetMetadata().JobStoreTypeName`),r(` still reports the inner store`)])],-1),l[568]||=e(`tr`,null,[e(`td`,null,[r(`The meter is built from the container's `),e(`code`,null,`IMeterFactory`)]),e(`td`,null,[r(`It was a process-wide static, so two containers in one process shared one set of instruments and `),e(`code`,null,`MetricCollector`),r(` could not see them. The meter's name and what an exporter subscribes to are unchanged, and an application that never calls `),e(`code`,null,`AddMetrics()`),r(` still gets a meter — see `),e(`a`,{href:`#job-execution-metrics`},`Job execution metrics`)])],-1),l[569]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`scheduling.quartz.exception_type`),r(` is `),e(`code`,null,`error.type`),r(`, naming the exception the job threw`)]),e(`td`,null,[r(`The tag was added to a copy of the tag list and discarded, so the counter said only that something failed — and the type it named was the `),e(`code`,null,`JobExecutionException`),r(` the run shell wraps everything in. It is OpenTelemetry's conventional name now — the one attribute Quartz does not namespace — it is on the duration histogram and the execution's span, and a query matching the old name or expecting the old value has to be rewritten — see `),e(`a`,{href:`#job-execution-metrics`},`Job execution metrics`)])],-1),l[570]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Diagnostics.QuartzInstrumentation`),r(` publishes the `),e(`code`,null,`ActivitySource`),r(` and `),e(`code`,null,`Meter`),r(` names`)]),e(`td`,null,[e(`code`,null,`AddSource("Quartz")`),r(` / `),e(`code`,null,`AddMeter("Quartz")`),r(` were the only strings on the instrumentation surface with no constant behind them. Both values are still `),e(`code`,null,`"Quartz"`),r(`, so existing wiring keeps working; `),e(`code`,null,`InstrumentationOptions`),r(` is gone with the change — see `),e(`a`,{href:`#job-execution-metrics`},`Job execution metrics`)])],-1),l[571]||=e(`tr`,null,[e(`td`,null,[r(`The vetoed-fire span is `),e(`code`,null,`Quartz.Job.Veto`)]),e(`td`,null,[e(`code`,null,`OperationName.Job.Veto`),r(` read `),e(`code`,null,`"Quartz.Job.Vetoed"`),r(` — the constant's name and its value disagreed. `),e(`code`,null,`Quartz.Job.Execute`),r(` and the `),e(`code`,null,`Quartz.JobStore.*`),r(` span names are unchanged`)])],-1),l[572]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`XmlSchedulingOptions`),r(` and `),e(`code`,null,`JsonSchedulingOptions`),r(` merged`)]),e(`td`,null,`They were byte-for-byte identical and are now one type`)],-1),l[573]||=e(`tr`,null,[e(`td`,null,`Constructing a scheduler no longer starts a thread`),e(`td`,null,[e(`code`,null,`QuartzScheduler`),r(` starts its scheduler thread from `),e(`code`,null,`Start()`),r(` rather than its constructor, so resolving the service graph, running a `),e(`code`,null,`ValidateOnBuild`),r(` pass or asserting on registrations no longer spins one up. The thread always started paused, so this changes when the thread exists, not when jobs run`)])],-1),e(`tr`,null,[l[463]||=e(`td`,null,[e(`code`,null,`IPersistentStoreBuilder.AcceptEnlistedTransactions()`)],-1),e(`td`,null,[l[458]||=r(`Never shipped; the setting is `,-1),l[459]||=e(`code`,null,`ConfigureStore(o => o.AcceptEnlistedTransactions = true)`,-1),l[460]||=r(`, like the other nineteen `,-1),l[461]||=e(`code`,null,`AdoJobStoreOptions`,-1),l[462]||=r(` settings — see `,-1),c(g,{to:`/documentation/quartz-4.x/tutorial/job-stores.html#joining-an-existing-transaction`},{default:i(()=>[...l[457]||=[r(`Joining an existing transaction`,-1)]]),_:1})])]),l[574]||=e(`tr`,null,[e(`td`,null,`Group matchers translate to SQL correctly`),e(`td`,null,[e(`code`,null,`SelectTriggerGroups`),r(`, `),e(`code`,null,`DeletePausedTriggerGroup`),r(` and both `),e(`code`,null,`UpdateTriggerGroupStateFromOtherState(s)`),r(` members always built a `),e(`code`,null,`LIKE`),r(`, even for an equality matcher; they take the `),e(`code`,null,`=`),r(` path now, which is exact and index-friendly. `),e(`code`,null,`LIKE`),r(` patterns escape `),e(`code`,null,`%`),r(`, `),e(`code`,null,`_`),r(` and the escape character in the matcher's own text with an explicit `),e(`code`,null,`ESCAPE`),r(` clause, so a group literally named `),e(`code`,null,`50%`),r(` matches itself. The escape character is `),e(`code`,null,`!`),r(` rather than a backslash, because MySQL applies C-style escaping inside string literals and `),e(`code`,null,`ESCAPE '\\'`),r(` is a syntax error there`)])],-1),l[575]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`StdAdoConstants`),r(` group and fired-trigger statements were split`)]),e(`td`,null,[e(`code`,null,`SqlDeletePausedTriggerGroup`),r(`, `),e(`code`,null,`SqlSelectTriggerGroupsFiltered`),r(`, `),e(`code`,null,`SqlUpdateTriggerGroupStateFromState`),r(` and `),e(`code`,null,`SqlUpdateTriggerGroupStateFromStates`),r(` are `),e(`code`,null,`…Equals`),r(` / `),e(`code`,null,`…Like`),r(` pairs, and the FIRED_TRIGGERS statements are one `),e(`code`,null,`SqlSelectFiredTriggers`),r(` / `),e(`code`,null,`SqlDeleteFiredTriggers`),r(` base plus `),e(`code`,null,`SqlFiredTrigger*Predicate`),r(` fragments. The type is internal`)])],-1),l[576]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`IDashboardAuthorizationFilter`),r(` and `),e(`code`,null,`QuartzDashboardOptions.AuthorizationFilter`),r(` removed`)]),e(`td`,null,[r(`Nothing ever invoked the filter, so setting it bought a false sense of security. Use `),e(`code`,null,`AuthorizationPolicy`),r(`, which is enforced`)])],-1),l[577]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`IDashboardHistoryStore`),r(` is asynchronous`)]),e(`td`,null,[e(`code`,null,`ValueTask AddExecution`),r(`, `),e(`code`,null,`ValueTask<PagedResult<DashboardHistoryEntry>> QueryExecutions(DashboardHistoryQuery)`),r(`, so a store can talk to a database. `),e(`code`,null,`SearchFilter.DebounceMilliseconds`),r(` is a `),e(`code`,null,`TimeSpan Debounce`),r(`, and `),e(`code`,null,`InProcessQuartzApiClient`),r(` is internal — resolve `),e(`code`,null,`IQuartzApiClient`)])],-1),l[578]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`IDashboardHistoryStore`),r(` carries the misfire feed`)]),e(`td`,null,[e(`code`,null,`AddMisfire`),r(`, `),e(`code`,null,`QueryMisfires(DashboardMisfireQuery)`),r(` and `),e(`code`,null,`CountMisfires(name, since)`),r(` are new members on the interface, so `),e(`strong`,null,`an application with its own store has to implement three more`),r(` — see `),e(`a`,{href:`#history-and-live-events-say-which-node-they-came-from`},`History and live events say which node they came from`)])],-1),l[579]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`IQuartzApiClient`),r(` speaks Quartz's vocabulary, with `),e(`code`,null,`IScheduler`),r(`'s verbs`)]),e(`td`,null,[r(`The renames are a table of their own — see `),e(`a`,{href:`#the-client-speaks-the-scheduler-s-verbs`},`The client speaks the scheduler's verbs`)])],-1),l[580]||=e(`tr`,null,[e(`td`,null,`The dashboard's HTTP-backed API client is gone`),e(`td`,null,[e(`code`,null,`QuartzApiClient`),r(` was never registered; the dashboard renders the schedulers in its own process, and `),e(`code`,null,`QuartzDashboardOptions.BaseUrl`),r(` and `),e(`code`,null,`.ApiPath`),r(` went with it — see `),e(`a`,{href:`#the-dashboard-reads-the-schedulers-in-its-own-process`},`The dashboard reads the schedulers in its own process`)])],-1),l[581]||=e(`tr`,null,[e(`td`,null,`Serializers outside a scheduler read a container-wide registry`),e(`td`,null,[r(`Because the serializer maps are per-serializer, the HTTP API and `),e(`code`,null,`Quartz.HttpClient`),r(` read a `),e(`code`,null,`SystemTextJsonSerializerRegistry`),r(` registered in the container. Register it as a singleton to make a custom serializer visible to them. The dashboard no longer registers one of its own: it passes triggers and calendars through as themselves`)])],-1),l[582]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`SystemTextJsonSerializerRegistry`),r(` gained `),e(`code`,null,`AddTypeInfoResolver(IJsonTypeInfoResolver)`)]),e(`td`,null,[r(`Where reflection-based serialization is off — a `),e(`code`,null,`PublishTrimmed`),r(` or `),e(`code`,null,`PublishAot`),r(` application — this is how job-data values of the application's own types are answered for. Hand it a generated `),e(`code`,null,`JsonSerializerContext`),r(`'s `),e(`code`,null,`Default`),r(`. Everything Quartz writes, and every custom trigger or calendar registered with the registry, is already covered; with reflection on it changes nothing — see `),e(`a`,{href:`#trimming-annotations`},`Trimming annotations`)])],-1),l[583]||=e(`tr`,null,[e(`td`,null,[r(`The `),e(`code`,null,`Quartz`),r(` package declares `),e(`code`,null,`IsAotCompatible`),r(`, and binds configuration with the source-generated binder`)]),e(`td`,null,[r(`No API moved. Quartz reports no `),e(`code`,null,`IL3050`),r(` at all now: the `),e(`code`,null,`Quartz`),r(` section binds through code the compiler wrote rather than through `),e(`code`,null,`ConfigurationBinder`),r(`'s reflection, so an application configured from `),e(`code`,null,`appsettings.json`),r(` publishes native AOT as safely as one configured in code — with the reflection binder that preceded it, `),e(`code`,null,`MaxBatchSize`),r(`, `),e(`code`,null,`ShutdownJobInterruption`),r(` and the whole scheduler context arrived as their defaults out of a native publish, silently. The `),e(`code`,null,`IL2xxx`),r(` for the string-named paths are unchanged — see `),e(`a`,{href:`#trimming-annotations`},`Trimming annotations`)])],-1),l[584]||=e(`tr`,null,[e(`td`,null,`Every other shipped package says whether it is trimmable`),e(`td`,null,[r(`No API moved, and no public member gained `),e(`code`,null,`[RequiresUnreferencedCode]`),r(` or `),e(`code`,null,`[DynamicallyAccessedMembers]`),r(`. Six packages are marked `),e(`code`,null,`IsTrimmable`),r(` and report their own reflective call sites where 3.x reported one `),e(`code`,null,`IL2104`),r(` per assembly; `),e(`code`,null,`Quartz.Serialization.Newtonsoft`),r(` and `),e(`code`,null,`Quartz.Dashboard`),r(` say `),e(`code`,null,`IsTrimmable=false`),r(` on purpose, and their readmes say why — see `),e(`a`,{href:`#trimming-annotations`},`Trimming annotations`)])],-1),l[585]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`IDriverDelegate`),r(` trigger states are `),e(`code`,null,`StoredTriggerState`)]),e(`td`,null,[r(`Eighteen members took the state as a `),e(`code`,null,`string`),r(`; the database still stores the same values — see `),e(`a`,{href:`#trigger-states-are-typed-on-the-driver-delegate`},`Trigger states are typed on the driver delegate`)])],-1),l[586]||=e(`tr`,null,[e(`td`,null,[r(`The `),e(`code`,null,`…FromOtherStates`),r(` members take a state collection`)]),e(`td`,null,[r(`Two or three fixed old-state parameters became one `),e(`code`,null,`IReadOnlyCollection<StoredTriggerState>`)])],-1),l[587]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`FiredTriggerQuery.InstanceName`),r(` is `),e(`code`,null,`InstanceId`)]),e(`td`,null,[r(`With the `),e(`code`,null,`instanceName`),r(` parameters of the scheduler-state members; the `),e(`code`,null,`INSTANCE_NAME`),r(` column is unchanged`)])],-1),l[588]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`IDriverDelegate.IsJobCurrentlyExecuting`),r(` takes a `),e(`code`,null,`JobKey`)]),e(`td`,null,[r(`It took `),e(`code`,null,`(string jobName, string jobGroup)`)])],-1),l[589]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`IDriverDelegate.SelectJobForTrigger`),r(`'s `),e(`code`,null,`loadJobType`),r(` is required`)]),e(`td`,null,[r(`It defaulted in front of the cancellation token; pass `),e(`code`,null,`loadJobType: true`),r(` for the old default`)])],-1),l[590]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`IDriverDelegate.UpdateTriggerPreferredNodeConditional`),r(` takes a `),e(`code`,null,`PreferredNodeTransition`)]),e(`td`,null,[r(`Four loose compare-and-swap parameters became one record naming `),e(`code`,null,`Expected`),r(` and `),e(`code`,null,`New`)])],-1),l[591]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`TriggerAcquisitionCriteria.LiveNodeCutoff`),r(` is a `),e(`code`,null,`required DateTimeOffset`)]),e(`td`,null,[r(`It was an optional `),e(`code`,null,`long`),r(` of `),e(`code`,null,`UtcTicks`),r(` beside two required `),e(`code`,null,`DateTimeOffset`),r(` siblings, and omitting it silently meant "every node is dead". The tick conversion happens in `),e(`code`,null,`AddPreferredNodeParameters`),r(`, whose parameter is a `),e(`code`,null,`DateTimeOffset`),r(` too`)])],-1),l[592]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`TriggerAcquisitionRequest.ExcludedJobTypeNames`),r(` and `),e(`code`,null,`TriggerAcquisitionCriteria.ExcludedJobTypeNames`),r(` added`)]),e(`td`,null,[r(`Optional exact job-type-name exclusions for acquisition, honoured by every shipped store. The ADO.NET store applies them in SQL, where comparison follows the `),e(`code`,null,`JOB_CLASS_NAME`),r(` column's collation; `),e(`code`,null,`RAMJobStore`),r(` compares `),e(`code`,null,`JobType.FullName`),r(` ordinally`)])],-1),l[593]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`StdAdoDelegate.AddPagingParameters(cmd, int skip, int take, bool)`)]),e(`td`,null,[e(`code`,null,`skip`),r(`/`),e(`code`,null,`take`),r(` were `),e(`code`,null,`long`),r(` while `),e(`code`,null,`PagedQuery.Skip`),r(`/`),e(`code`,null,`.Take`),r(` are `),e(`code`,null,`int`),r(`; the dialect override contract now matches the query object it serves`)])],-1),l[594]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`StdAdoDelegate.ReadBytesFromBlob`),r(` takes a `),e(`code`,null,`DbDataReader`),r(` and is asynchronous`)]),e(`td`,null,[r(`It took a `),e(`code`,null,`System.Data.IDataReader`),r(` — the last legacy `),e(`code`,null,`System.Data.I*`),r(` interface anywhere in the public surface — which forced a synchronous, thread-blocking read that ignored the cancellation token it was handed. It is `),e(`code`,null,`async ValueTask<byte[]?>`),r(` over `),e(`code`,null,`IsDBNullAsync`),r(` / `),e(`code`,null,`GetFieldValueAsync<byte[]>`),r(`. `),e(`strong`,null,`The behaviour contract is unchanged`),r(`: a `),e(`code`,null,`NULL`),r(` column still yields `),e(`code`,null,`null`),r(`, and an empty blob still yields an empty array, which the only caller (`),e(`code`,null,`GetObjectFromBlob`),r(`) has always treated the same as `),e(`code`,null,`null`),r(` by testing `),e(`code`,null,`Length > 0`),r(`. An override changes its parameter type; every reader the store passes was already a `),e(`code`,null,`DbDataReader`)])],-1),l[595]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`StdAdoDelegate`),r(`'s date/time and time-span conversions are non-virtual`)]),e(`td`,null,[r(`UTC ticks and whole milliseconds are the schema contract the liveness SQL assumes; only the boolean pair remains a dialect seam. A delegate that changes the storage format implements `),e(`code`,null,`IDriverDelegate`),r(` itself — see `),e(`a`,{href:`#if-you-implement-idriverdelegate-the-listing-members`},[r(`If you implement `),e(`code`,null,`IDriverDelegate`),r(`: the listing members`)])])],-1),l[596]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`JobStoreTX`),r(` is `),e(`code`,null,`LocalTransactionJobStore`),r(`, `),e(`code`,null,`JobStoreCMT`),r(` is `),e(`code`,null,`ExternalTransactionJobStore`)]),e(`td`,null,[r(`The names now say whose transaction the store uses. `),e(`code`,null,`quartz.jobStore.type = Quartz.Impl.AdoJobStore.JobStoreTX, Quartz`),r(` and the `),e(`code`,null,`JobStoreCMT`),r(` spelling still resolve, with a warning — see `),e(`a`,{href:`#the-ado-net-job-stores-are-named-for-whose-transaction-they-use`},`The ADO.NET job stores are named for whose transaction they use`)])],-1),l[597]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`GetNonManagedTXConnection`),r(`, `),e(`code`,null,`ExecuteInNonManagedTXLock`),r(`, `),e(`code`,null,`RetryExecuteInNonManagedTXLock`),r(` renamed`)]),e(`td`,null,[e(`code`,null,`GetLocalTransactionConnection`),r(`, `),e(`code`,null,`ExecuteInLocalTransactionLock`),r(`, `),e(`code`,null,`RetryExecuteInLocalTransactionLock`),r(`; protected, so only a `),e(`code`,null,`AdoJobStoreBase`),r(` subclass sees them`)])],-1),l[598]||=e(`tr`,null,[e(`td`,null,[r(`The three ADO store constructors take one `),e(`code`,null,`AdoJobStoreDependencies`)]),e(`td`,null,[e(`code`,null,`AdoJobStoreBase`),r(`, `),e(`code`,null,`LocalTransactionJobStore`),r(` and `),e(`code`,null,`ExternalTransactionJobStore`),r(` each took the same twelve parameters, and a store deriving from one of them restated all twelve to forward them. They take one record instead, whose properties are exactly those parameters: `),e(`code`,null,`new LocalTransactionJobStore(signaler, typeLoader, …)`),r(` becomes `),e(`code`,null,`new LocalTransactionJobStore(new AdoJobStoreDependencies(signaler, typeLoader, …))`),r(`, and a derived store chains `),e(`code`,null,`: base(dependencies)`),r(`. The container registers the record per scheduler and builds it the way it built the store, so `),e(`code`,null,`UseJobStore<MyStore>()`),r(` and `),e(`code`,null,`UsePersistentStore<MyStore>(…)`),r(` were unchanged. Moot now: all four types are internal and nothing derives from them — see `),e(`a`,{href:`#the-ado-net-store-is-a-store-not-a-base-class`},`The ADO.NET store is a store, not a base class`)])],-1),l[599]||=e(`tr`,null,[e(`td`,null,`The ADO store reports a failure once, not once per nesting level`),e(`td`,null,[r(`Every operation wraps a provider exception in a `),e(`code`,null,`JobPersistenceException`),r(` naming what it could not do, as before. What it no longer does is wrap a `),e(`code`,null,`JobPersistenceException`),r(` again: a failure the store raised on purpose — `),e(`code`,null,`The job (…) referenced by the trigger does not exist.`),r(`, `),e(`code`,null,`Calendar cannot be removed if it is referenced by a trigger!`),r(`, an `),e(`code`,null,`ObjectAlreadyExistsException`),r(` — now reaches the caller as itself, where 3.x prefixed it with the operation in progress (`),e(`code`,null,`Couldn't store trigger '…' for '…' job: The job (…) …`),r(`). The type is unchanged, so `),e(`code`,null,`catch (JobPersistenceException)`),r(` and the HTTP API's exception mapping are unaffected; code matching on the `),e(`em`,null,`text`),r(` of a nested message reads the inner message now`)])],-1),l[600]||=e(`tr`,null,[e(`td`,null,[r(`Cancelling a store operation raises `),e(`code`,null,`OperationCanceledException`)]),e(`td`,null,[r(`It was reported as a `),e(`code`,null,`JobPersistenceException`),r(` wrapping one. Cancellation is not a persistence failure, and callers match on the type. This holds through the lock and transaction plumbing as well as through the guard around each statement: an operation cancelled while it holds a lock and an open transaction rolls back, releases the lock, and reports the cancellation as itself rather than as `),e(`code`,null,`Unexpected runtime exception`),r(` or `),e(`code`,null,`Failed to obtain DB connection`),r(`. An `),e(`code`,null,`OperationCanceledException`),r(` raised while the caller's own token is `),e(`em`,null,`not`),r(` cancelled keeps the classification it had — the caller did not ask to stop, so from where they stand the store failed`)])],-1),l[601]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`AdoJobStoreBase`),r(`'s nine `),e(`code`,null,`Execute…Lock`),r(` overloads became four members`)]),e(`td`,null,[r(`Optional parameters replace the ladder, and no member returns `),e(`code`,null,`object`),r(` as a stand-in for `),e(`code`,null,`void`),r(` any more — see `),e(`a`,{href:`#nine-execute-lock-overloads-became-four-members`},[r(`Nine `),e(`code`,null,`Execute…Lock`),r(` overloads became four members`)])])],-1),l[602]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`ExternalTransactionJobStore.OpenConnection`),r(` moved to `),e(`code`,null,`AdoJobStoreOptions.OpenConnection`)]),e(`td`,null,[r(`The last store setting outside the options system; the store reads it once at construction — see `),e(`a`,{href:`#nine-execute-lock-overloads-became-four-members`},[r(`Nine `),e(`code`,null,`Execute…Lock`),r(` overloads became four members`)])])],-1),l[603]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`ILockHandler`),r(` takes a `),e(`code`,null,`SchedulerLock`)]),e(`td`,null,[r(`The `),e(`code`,null,`string lockName`),r(` had two legal values. The `),e(`code`,null,`LOCK_NAME`),r(` column and the Redis keys are unchanged — see `),e(`a`,{href:`#locks-are-a-schedulerlock-not-a-string`},[r(`Locks are a `),e(`code`,null,`SchedulerLock`),r(`, not a string`)])])],-1),l[604]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`AdoJobStoreBase.LockTriggerAccess`),r(` / `),e(`code`,null,`.LockStateAccess`),r(` removed`)]),e(`td`,null,[e(`code`,null,`SchedulerLock.TriggerAccess`),r(` / `),e(`code`,null,`.StateAccess`),r(` replace the two protected constants`)])],-1),l[605]||=e(`tr`,null,[e(`td`,null,[r(`~25 `),e(`code`,null,`AdoJobStoreBase`),r(` configuration properties are read-only and `),e(`code`,null,`protected`),r(`/internal`)]),e(`td`,null,[r(`They duplicated `),e(`code`,null,`AdoJobStoreOptions`),r(` / `),e(`code`,null,`QuartzSchedulerOptions`),r(`; resolve `),e(`code`,null,`IOptions<AdoJobStoreOptions>`),r(` to read, configure the options to write. `),e(`code`,null,`MisfireThreshold`),r(` deliberately stays publicly `),e(`em`,null,`readable`),r(` (its setter is internal), and the `),e(`code`,null,`IJobStore`),r(` members stay public — see `),e(`a`,{href:`#the-job-store-configuration-is-read-only-and-no-longer-a-public-currency`},`The job store configuration is read-only`)])],-1),l[606]||=e(`tr`,null,[e(`td`,null,[r(`The seven conn-taking `),e(`code`,null,`Pause…`),r(`/`),e(`code`,null,`Resume…`),r(`/`),e(`code`,null,`RecoverMisfiredJobs`),r(` overloads are `),e(`code`,null,`protected`)]),e(`td`,null,[r(`They took a `),e(`code`,null,`ConnectionAndTransactionHolder`),r(` no caller outside the store can obtain; call the public keyed overloads, which take the lock and the connection themselves`)])],-1),l[607]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`AdoJobStoreBase`),r(`'s virtual surface is a curated seam`)]),e(`td`,null,[r(`Only `),e(`code`,null,`Initialize`),r(`, `),e(`code`,null,`Shutdown`),r(`, `),e(`code`,null,`GetConnection`),r(`, `),e(`code`,null,`GetLocalTransactionConnection`),r(`, `),e(`code`,null,`ExecuteInLock<T>`),r(`, `),e(`code`,null,`IsTransient`),r(`, `),e(`code`,null,`AcquireNextTriggers`),r(`, `),e(`code`,null,`CreateAcquisitionCriteria`),r(` and `),e(`code`,null,`GetFiredTriggerRecordId`),r(` remain overridable; the other ~75 members were virtual by default, not by design, and freezing the store's internal call order as a behavior contract would have made it unrefactorable`)])],-1),l[608]||=e(`tr`,null,[e(`td`,null,`A transaction the database rolled back is retried whatever the driver calls it`),e(`td`,null,[e(`code`,null,`AdoJobStoreBase.IsTransient`),r(` also reads the exception's SQLSTATE and retries class `),e(`code`,null,`40`),r(`, the standard's "transaction rollback", with `),e(`code`,null,`40002`),r(` excepted. 3.x read only the driver's own `),e(`code`,null,`IsTransient`),r(` property, SQL Server's error numbers and SQLite's busy codes, so a Firebird write conflict (`),e(`code`,null,`40001`),r(`) and a MySQL `),e(`code`,null,`1213`),r(` deadlock on MySql.Data were fatal — neither driver reports the condition as transient — where SQL Server's 1205 and SQLite's `),e(`code`,null,`BUSY`),r(` were retried. They are now retried too, bounded by the same `),e(`code`,null,`MaxTransientRetries`),r(` and `),e(`code`,null,`TransientRetryInterval`),r(`. An `),e(`code`,null,`IsTransient`),r(` override still replaces the whole verdict`)])],-1),l[609]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`AdoJobStoreBase.DriverDelegateType`),r(` and `),e(`code`,null,`.DontSetAutoCommitFalse`),r(` removed`)]),e(`td`,null,`Nothing read either one; the driver delegate is injected`)],-1),l[610]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`AdoJobStoreOptions.DontSetAutoCommitFalse`),r(` removed`)]),e(`td`,null,[r(`The option the deleted store property mirrored. No code path read it and no `),e(`code`,null,`quartz.*`),r(` key set it, so setting it configured nothing`)])],-1),l[611]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`AdoJobStoreBase.LastCheckin`),r(` is internal, `),e(`code`,null,`LogWarnIfNonZero`),r(` is removed`)]),e(`td`,null,[r(`Cluster check-in bookkeeping and a logging helper, neither of them an extension point. The helper's callers raise source-generated events, at the level its name always claimed — see `),e(`a`,{href:`#every-message-carries-an-event-id`},`Every message carries an event id`)])],-1),l[612]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`AdoJobStoreBase.DoCheckin`),r(` and `),e(`code`,null,`.DoRecoverMisfires`),r(` lost the `),e(`code`,null,`Do`),r(` prefix`)]),e(`td`,null,[e(`code`,null,`CheckIn`),r(` and `),e(`code`,null,`RecoverMisfires`),r(`. The prefix distinguished each from nothing — neither had a same-named member to be told apart from — and in the box only `),e(`code`,null,`ClusterManager`),r(` and `),e(`code`,null,`MisfireHandler`),r(` call them`)])],-1),l[613]||=e(`tr`,null,[e(`td`,null,[r(`Four `),e(`code`,null,`AdoJobStoreBase`),r(` parameters are spelled out`)]),e(`td`,null,[e(`code`,null,`CloseConnection`),r(`, `),e(`code`,null,`RollbackConnection`),r(` and `),e(`code`,null,`CommitConnection`),r(` take `),e(`code`,null,`connection`),r(` where they took `),e(`code`,null,`cth`),r(`; `),e(`code`,null,`CalcFailedIfAfter`),r(` takes `),e(`code`,null,`record`),r(` where it took `),e(`code`,null,`rec`),r(`. Only a call that named its argument is affected`)])],-1),l[614]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`AdoJobStoreBase.RecoverJobs(CancellationToken)`),r(` returns `),e(`code`,null,`ValueTask`)]),e(`td`,null,[r(`The `),e(`code`,null,`bool`),r(` it returned was the constant `),e(`code`,null,`true`)])],-1),l[615]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`DbLockHandler.LockSql`),r(` (was `),e(`code`,null,`Sql`),r(`) and `),e(`code`,null,`InsertSql`),r(` are get-only, fed by the constructor`)]),e(`td`,null,[r(`Assigning one after construction left it un-prefixed relative to its pair — see `),e(`a`,{href:`#the-semaphores-are-lock-handlers`},`The semaphores are lock handlers`)])],-1),l[616]||=e(`tr`,null,[e(`td`,null,`The row-lock handlers are named for the SQL they issue`),e(`td`,null,[e(`code`,null,`StdRowLockSemaphore`),r(` is `),e(`code`,null,`SelectForUpdateLockHandler`),r(`, `),e(`code`,null,`UpdateLockRowSemaphore`),r(` is `),e(`code`,null,`UpdateRowLockHandler`),r(`, `),e(`code`,null,`PostgreSQLRowLockSemaphore`),r(` is `),e(`code`,null,`PostgreSqlSelectForUpdateLockHandler`),r(`, `),e(`code`,null,`UpdateLockRowSemaphoreMOT`),r(` is `),e(`code`,null,`SqlServerMemoryOptimizedUpdateRowLockHandler`),r(`. `),e(`code`,null,`quartz.jobStore.lockHandler.type`),r(` naming an old one still resolves, with a warning — see `),e(`a`,{href:`#the-semaphores-are-lock-handlers`},`The semaphores are lock handlers`)])],-1),l[617]||=e(`tr`,null,[e(`td`,null,[r(`Row-lock handler SQL fields are `),e(`code`,null,`protected const`),r(` and consistently named`)]),e(`td`,null,[e(`code`,null,`UpdateLockRowSemaphore.SqlUpdateForLock`),r(` / `),e(`code`,null,`.SqlInsertLock`),r(` are `),e(`code`,null,`UpdateRowLockHandler.UpdateForLock`),r(` / `),e(`code`,null,`.InsertLock`),r(`; `),e(`code`,null,`SelectForUpdateLockHandler.SelectForLock`),r(` / `),e(`code`,null,`.InsertLock`),r(` keep their member names`)])],-1),l[618]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`ISemaphore`),r(` is `),e(`code`,null,`ILockHandler`),r(`, and every implementation says `),e(`code`,null,`LockHandler`)]),e(`td`,null,[r(`A semaphore is a counted permit; this is a mutual-exclusion lock with one holder. `),e(`code`,null,`DBSemaphore`),r(` is `),e(`code`,null,`DbLockHandler`),r(`, `),e(`code`,null,`SimpleSemaphore`),r(` is the internal `),e(`code`,null,`InProcessLockHandler`),r(`, `),e(`code`,null,`RedisSemaphore`),r(` is `),e(`code`,null,`RedisLockHandler`),r(` — see `),e(`a`,{href:`#the-semaphores-are-lock-handlers`},`The semaphores are lock handlers`)])],-1),l[619]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`ISemaphore.ObtainLock`),r(` is `),e(`code`,null,`ILockHandler.AcquireLock`)]),e(`td`,null,[e(`code`,null,`ReleaseLock`),r(` and `),e(`code`,null,`RequiresConnection`),r(` are unchanged; the verb pairs with the one that gives the lock back`)])],-1),l[620]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`SemaphoreContext`),r(` is `),e(`code`,null,`LockHandlerContext`)]),e(`td`,null,[r(`The record `),e(`code`,null,`Initialize`),r(` takes. Its members are unchanged`)])],-1),l[621]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`ILockHandler.AcquireLock`),r(` throws on cancellation instead of answering `),e(`code`,null,`false`)]),e(`td`,null,[e(`code`,null,`false`),r(` means a re-entrant acquire and nothing else, so a cancelled acquire that answered it told the store the lock was already held. `),e(`code`,null,`SimpleSemaphore`),r(` and `),e(`code`,null,`RedisSemaphore`),r(` did; their 4.0 counterparts throw — see `),e(`a`,{href:`#a-cancelled-acquire-throws-and-false-means-only-re-entry`},[r(`A cancelled acquire throws, and `),e(`code`,null,`false`),r(` means only re-entry`)])])],-1),l[622]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`ILockHandler.Initialize(LockHandlerContext)`),r(` replaces `),e(`code`,null,`ITablePrefixAware`)]),e(`td`,null,[r(`Identity arrives through one initialization call instead of a property pair; the default implementation does nothing — see `),e(`a`,{href:`#a-lock-handler-is-told-which-scheduler-it-locks-for`},`A lock handler is told which scheduler it locks for`)])],-1),l[623]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`ILockHandler.Shutdown(CancellationToken)`),r(` added as a default interface member`)]),e(`td`,null,[r(`The job store calls it after its own teardown, so a handler can close what it opened. `),e(`strong`,null,`An existing handler need not implement it`),r(`: the default body does nothing. `),e(`code`,null,`RedisLockHandler`),r(` closes its multiplexer through it, which nothing did before — see `),e(`a`,{href:`#a-lock-handler-is-told-when-to-close-what-it-opened`},`A lock handler is told when to close what it opened`)])],-1),l[624]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`LockHandlerContext`),r(` also carries `),e(`code`,null,`TimeProvider`),r(` and `),e(`code`,null,`CommandTimeout`)]),e(`td`,null,[r(`The environment a handler locks in, beside the identity it locks under. `),e(`code`,null,`DbLockHandler`),r(` exposes the clock as a `),e(`code`,null,`protected TimeProvider`),r(` and both shipped row-lock handlers back off on it, so a retry is observable without waiting out the real second`)])],-1),l[625]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`LockHandlerContext.LoggerFactory`),r(` and `),e(`code`,null,`DriverDelegateContext.LoggerFactory`),r(` added`)]),e(`td`,null,[r(`Where the handler and the delegate create their loggers, defaulting to `),e(`code`,null,`NullLoggerFactory.Instance`),r(`. The job store passes the factory its container gave it, so lock contention and statement failures reach an application that never called `),e(`code`,null,`LogProvider.SetLogProvider`),r(` — see `),e(`a`,{href:`#the-ambient-logger-factory-stays-ambient`},`The ambient logger factory stays ambient`)])],-1),l[626]||=e(`tr`,null,[e(`td`,null,[r(`The ADO stores take an optional `),e(`code`,null,`ILoggerFactory?`),r(`, through `),e(`code`,null,`AdoJobStoreDependencies.LoggerFactory`)]),e(`td`,null,[r(`The container fills it in, and the store, its cluster manager, its misfire handler and its units of work log through it. A store constructed by hand leaves it unset and keeps reading the ambient factory — see `),e(`a`,{href:`#the-ambient-logger-factory-stays-ambient`},`The ambient logger factory stays ambient`)])],-1),l[627]||=e(`tr`,null,[e(`td`,null,[r(`The components `),e(`code`,null,`Use*<T>()`),r(` builds take an optional logger`)]),e(`td`,null,[e(`code`,null,`TaskSchedulingThreadPool`),r(` (both constructors) and `),e(`code`,null,`DefaultThreadPool`),r(`, `),e(`code`,null,`ZeroSizeThreadPool`),r(` and `),e(`code`,null,`HostNameBasedIdGenerator`),r(` take an `),e(`code`,null,`ILogger<T>?`),r(`; `),e(`code`,null,`SimpleJobFactory`),r(`, `),e(`code`,null,`PropertySettingJobFactory`),r(` and `),e(`code`,null,`MicrosoftDependencyInjectionJobFactory`),r(` take an `),e(`code`,null,`ILoggerFactory?`),r(`, because the factory chain logs under two categories. All are optional and `),e(`code`,null,`null`),r(` means the ambient factory, so `),e(`code`,null,`new DefaultThreadPool()`),r(` and a derived type's `),e(`code`,null,`: base()`),r(` compile unchanged; a subclass that wants the container's logger passes the parameter through — see `),e(`a`,{href:`#the-ambient-logger-factory-stays-ambient`},`The ambient logger factory stays ambient`)])],-1),l[628]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`DbLockHandler.PrepareCommand`),r(` / `),e(`code`,null,`.AddCommandParameter`),r(` are `),e(`code`,null,`protected`)]),e(`td`,null,[r(`The two things a subclass needs to implement `),e(`code`,null,`ExecuteSql`),r(`, which `),e(`code`,null,`private protected AdoUtil`),r(` had left it unable to do at all — see `),e(`a`,{href:`#sealed-and-internalized-types`},`Sealed and Internalized Types`)])],-1),l[629]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`SelectForUpdateLockHandler.MaxRetry`),r(` / `),e(`code`,null,`.RetryPeriod`),r(` are `),e(`code`,null,`init`),r(`-only`)]),e(`td`,null,[r(`Assign them in an object initializer. `),e(`code`,null,`quartz.jobStore.lockHandler.maxRetry`),r(` / `),e(`code`,null,`.retryPeriod`),r(` still reach them — the property bridge writes by reflection, which an init accessor does not stop`)])],-1),l[630]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`UpdateRowLockHandler.RetryPeriod`),r(` added`)]),e(`td`,null,[r(`Its backoff was a literal one second, so it ignored `),e(`code`,null,`quartz.jobStore.lockHandler.retryPeriod`),r(` while its sibling honoured it. The default is unchanged at one second`)])],-1),l[631]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`AdoJobStoreBase.GetEnlistedConnection`),r(` was made `),e(`code`,null,`protected`),r(`, then went internal with its type`)]),e(`td`,null,[r(`It opened enlistment to a derived store while the hierarchy was still public; there is no derived store now. Enlisting from the application side is unaffected — see `),e(`a`,{href:`#a-job-store-of-your-own-can-join-your-transaction`},`A job store of your own can join your transaction`)])],-1),l[632]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`ConnectionAndTransactionHolder`),r(` gained an ownership-aware constructor and `),e(`code`,null,`OwnsResources`)]),e(`td`,null,[e(`code`,null,`(connection, transaction, ownsResources)`),r(` for a store running on a connection it did not open`)])],-1),l[633]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`ConnectionAndTransactionHolder`),r(` is `),e(`code`,null,`IAsyncDisposable`)]),e(`td`,null,[e(`code`,null,`await using`),r(` is the form to prefer in an async method — the provider closes its connection without blocking a thread. Purely additive; `),e(`code`,null,`using`),r(` keeps working, and both disposal paths now log failures at debug instead of swallowing them`)])],-1),l[634]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`FiredTriggerRecord`),r(`, `),e(`code`,null,`RecoverMisfiredJobsResult`),r(`, `),e(`code`,null,`DriverDelegateContext`),r(` are `),e(`code`,null,`sealed record`),r(`s`)]),e(`td`,null,[r(`Immutable, with `),e(`code`,null,`required`),r(` / `),e(`code`,null,`init`),r(` members instead of settable ones — see `),e(`a`,{href:`#the-driver-delegate-speaks-in-records`},`The driver delegate speaks in records`)])],-1),l[635]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`DelegateInitializationArgs`),r(` is `),e(`code`,null,`DriverDelegateContext`),r(`, and its `),e(`code`,null,`InstanceName`),r(` is `),e(`code`,null,`SchedulerName`)]),e(`td`,null,[r(`It holds the scheduler name, not the instance id, and the three ADO.NET initialization seams now agree on the term — see `),e(`a`,{href:`#the-initialization-seams-are-context-records`},`The initialization seams are context records`)])],-1),l[636]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`ITriggerPersistenceDelegate.Initialize`),r(` takes a `),e(`code`,null,`TriggerPersistenceDelegateContext`)]),e(`td`,null,[r(`It took `),e(`code`,null,`(string tablePrefix, string schedulerName, IDbAccessor dbAccessor)`),r(` — two transposable strings and an accessor. Neither delegate seam has a default implementation, because a delegate that skips initialization has nothing to issue a statement with — see `),e(`a`,{href:`#the-initialization-seams-are-context-records`},`The initialization seams are context records`)])],-1),l[637]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`DriverDelegateContext.InitString`),r(` replaced by `),e(`code`,null,`TriggerPersistenceDelegates`)]),e(`td`,null,[r(`The delimited string became a typed collection; register delegates with `),e(`code`,null,`UseTriggerPersistenceDelegate<T>()`),r(`. The legacy `),e(`code`,null,`quartz.jobStore.driverDelegateInitString`),r(` key still translates`)])],-1),l[638]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`FiredTriggerRecord.FireInstanceState`),r(` is a `),e(`code`,null,`StoredTriggerState`)]),e(`td`,null,[r(`The last raw `),e(`code`,null,`AdoConstants.State*`),r(` comparisons in the store; `),e(`code`,null,`[Serializable]`),r(` is gone with it, and the always-populated members are non-nullable`)])],-1),l[639]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`StoredTriggerState`),r(` moved to `),e(`code`,null,`Quartz.Extensibility`),r(`; `),e(`code`,null,`TriggerStateResolver.Resolve`),r(` is public`)]),e(`td`,null,[r(`The stored-state vocabulary and its reporting precedence belong to every job store, not just the ADO one; members and stored strings are unchanged, so a delegate updates a `),e(`code`,null,`using`),r(` directive — see `),e(`a`,{href:`#trigger-states-are-typed-on-the-driver-delegate`},`Trigger states are typed on the driver delegate`)])],-1),l[640]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`RecoverMisfiredJobsResult.EarliestNewTime`),r(` is `),e(`code`,null,`EarliestNewTimeUtc`)]),e(`td`,null,[r(`The property and its constructor argument disagreed about the `),e(`code`,null,`Utc`),r(` suffix`)])],-1),l[641]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`RecoverMisfiredJobsResult.NoOp`),r(` is a static property`)]),e(`td`,null,[r(`It was a `),e(`code`,null,`public static readonly`),r(` field beside sentinels that are properties; source keeps compiling, a recompile is required`)])],-1),l[642]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`TriggerAcquireResult`),r(` carries a `),e(`code`,null,`TriggerKey`)]),e(`td`,null,[r(`It carried `),e(`code`,null,`TriggerName`),r(` and `),e(`code`,null,`TriggerGroup`),r(`, which every caller immediately paired back up`)])],-1),l[643]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`TriggerStatus`),r(` removed, `),e(`code`,null,`IDriverDelegate.SelectTriggerStatus`),r(` is `),e(`code`,null,`SelectTriggerHeader`)]),e(`td`,null,[r(`It returns `),e(`code`,null,`StoredTriggerHeader`),r(`, an immutable record whose state is a `),e(`code`,null,`StoredTriggerState`),r(` — see `),e(`a`,{href:`#the-driver-delegate-speaks-in-records`},`The driver delegate speaks in records`)])],-1),l[644]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`IDriverDelegate.ValidateSchema`),r(` added`)]),e(`td`,null,[r(`Schema validation was a `),e(`code`,null,`StdAdoDelegate`),r(` method reached by type test, so a delegate of your own silently skipped it — see `),e(`a`,{href:`#validateschema-is-part-of-idriverdelegate`},[e(`code`,null,`ValidateSchema`),r(` is part of `),e(`code`,null,`IDriverDelegate`)])])],-1),l[645]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`IDriverDelegate.ApplyTriggerFired`),r(` added`)]),e(`td`,null,[r(`One trigger fire is one round trip's worth of writes rather than five to eight; `),e(`code`,null,`TriggerFiredUpdate`),r(` describes it — see `),e(`a`,{href:`#batched-trigger-fire`},`Batched trigger fire`)])],-1),l[646]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`IDriverDelegate`),r(` gains a transition-list `),e(`code`,null,`UpdateTriggerStatesForJobFromOtherState`),r(` and a state-filtered `),e(`code`,null,`SelectTriggerKeysForJob`)]),e(`td`,null,[r(`Overloads beside the existing ones, so the blocking and unblocking of a job's triggers is one round trip and completion stops reading a state per trigger — see `),e(`a`,{href:`#batched-trigger-fire`},`Batched trigger fire`)])],-1),l[647]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`IDriverDelegate`),r(` gains set-taking `),e(`code`,null,`UpdateTriggerStatesForJobsFromOtherState`),r(` and `),e(`code`,null,`DeleteFiredTriggers`)]),e(`td`,null,[r(`Overloads beside the single-key ones, so recovering a failed node issues one round trip per kind of change rather than one per fired-trigger row. Its fourth step, releasing what the node had reserved, uses the key-set `),e(`code`,null,`UpdateTriggerStatesFromOtherStates`),r(` listed with the acquisition members — see `),e(`a`,{href:`#batched-cluster-recovery`},`Batched cluster recovery`)])],-1),l[648]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`IDriverDelegate.SelectTriggerKeysInState`),r(` added`)]),e(`td`,null,[r(`Which of a set of triggers are in a state, in one read; recovery asked `),e(`code`,null,`SelectTriggerState`),r(` once per trigger a dead node had left behind — see `),e(`a`,{href:`#batched-cluster-recovery`},`Batched cluster recovery`)])],-1),l[649]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`StoredTriggerHeader`),r(` carries `),e(`code`,null,`TriggerType`)]),e(`td`,null,[r(`A fifth positional parameter; it comes off the row the state came from, which is what removes the separate type lookup — see `),e(`a`,{href:`#batched-trigger-fire`},`Batched trigger fire`)])],-1),l[650]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`ITriggerPersistenceDelegate.TryDescribeUpdateExtendedTriggerProperties`),r(` added`)]),e(`td`,null,[r(`A default interface member returning `),e(`code`,null,`false`),r(`, so an existing persistence delegate is unaffected; implementing it puts a trigger's schedule in the same round trip as its row — see `),e(`a`,{href:`#batched-trigger-fire`},`Batched trigger fire`)])],-1),l[651]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`IDriverDelegate.UpdateFiredTrigger`),r(` removed`)]),e(`td`,null,[e(`code`,null,`ApplyTriggerFired`),r(` writes the fired-trigger row as one command of its batch, and nothing else called it; an override of it would have stopped taking effect silently — see `),e(`a`,{href:`#batched-trigger-fire`},`Batched trigger fire`)])],-1),l[652]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`IDriverDelegate`),r(` gains six set-shaped members`)]),e(`td`,null,[e(`code`,null,`InsertFiredTriggers`),r(`, `),e(`code`,null,`SelectStoredTriggerHeaders`),r(`, a key-set `),e(`code`,null,`UpdateTriggerStatesFromOtherStates`),r(`, `),e(`code`,null,`SelectTriggerKeysForJobs`),r(`, `),e(`code`,null,`SelectPausedJobGroups`),r(` and `),e(`code`,null,`InsertPausedJobGroups`),r(`. All are default interface members whose default is the per-key loop the store used to make, so an existing delegate is unaffected — see `),e(`a`,{href:`#batched-trigger-acquisition-and-the-sets-pause-and-resume-work-on`},`Batched trigger acquisition, and the sets pause and resume work on`)])],-1),l[653]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`IDriverDelegate.FiltersAcquisitionJobTypeExclusions`),r(` added`)]),e(`td`,null,[r(`A default interface member answering `),e(`code`,null,`false`),r(`; `),e(`code`,null,`StdAdoDelegate`),r(` answers `),e(`code`,null,`true`),r(`. A delegate that says it filters the excluded job types itself is taken at its word, and the store's backstop is skipped — see `),e(`a`,{href:`#batched-trigger-acquisition-and-the-sets-pause-and-resume-work-on`},`Batched trigger acquisition, and the sets pause and resume work on`)])],-1),l[654]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`StdAdoDelegate`),r(`'s column probes removed`)]),e(`td`,null,[r(`The three `),e(`code`,null,`Has*Column`),r(` properties, the three `),e(`code`,null,`Supports*Column`),r(` probes and `),e(`code`,null,`VerifyTriggersTableReachable`),r(`. The columns they probed for are required on 4.x, so the schema migration replaces them — see `),e(`a`,{href:`#the-optional-columns-are-required-so-the-probes-are-gone`},`The optional columns are required, so the probes are gone`)])],-1),l[655]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`GetSelectNextTriggerToAcquireWith*Sql`),r(` removed`)]),e(`td`,null,[r(`The `),e(`code`,null,`…WithExecutionGroupSql`),r(`, `),e(`code`,null,`…WithPreferredNodeSql`),r(` and `),e(`code`,null,`…WithPreferredNodeOnlySql`),r(` hooks, on `),e(`code`,null,`StdAdoDelegate`),r(` and all six dialect delegates. One statement covers every case now, so a dialect delegate keeps only its `),e(`code`,null,`GetSelectNextTriggerToAcquireSql`),r(` override — see `),e(`a`,{href:`#the-three-extra-acquisition-sql-hooks-went-with-them`},`The three extra acquisition SQL hooks went with them`)])],-1),l[656]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`StdAdoDelegate.GetSelectNextTriggerToAcquireSql(int maxCount)`)]),e(`td`,null,[e(`code`,null,`GetSelectNextTriggerToAcquireSql(TriggerAcquisitionSqlShape shape)`),r(`; a custom dialect override passes the whole shape to `),e(`code`,null,`base`),r(`, so the next acquisition dimension is a property on the record rather than another parameter`)])],-1),l[657]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`StdAdoDelegate.GetRowLimit(int count)`),r(` added`)]),e(`td`,null,[r(`Where a dialect's row-limiting clause goes, said once for both statements that carry one, as a `),e(`code`,null,`SqlRowLimit`),r(` rather than an edit to finished SQL. Five of the six dialect delegates stopped overriding `),e(`code`,null,`GetSelectNextTriggerToAcquireSql`),r(` and `),e(`code`,null,`GetSelectMisfiredTriggersToRecoverSql`),r(` entirely; both hooks stay `),e(`code`,null,`protected virtual`),r(` and an override that splices its own clause still works — see `),e(`a`,{href:`#row-limiting-is-a-slot-not-a-splice`},`Row limiting is a slot, not a splice`)])],-1),l[658]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Impl.AdoJobStore.SqlRowLimit`),r(` added`)]),e(`td`,null,[r(`A `),e(`code`,null,`readonly record struct`),r(` naming the three placements a row limit can take — `),e(`code`,null,`InProjection`),r(`, `),e(`code`,null,`AtStatementEnd`),r(`, `),e(`code`,null,`InEnclosingSelect`),r(` — and `),e(`code`,null,`Unlimited`),r(` for a database that cannot limit rows`)])],-1),l[659]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`IDbConnectionManager`),r(` / `),e(`code`,null,`DbConnectionManager`),r(` removed`)]),e(`td`,null,[r(`The container is the provider registry, keyed by scheduler name; register a provider with `),e(`code`,null,`UseConnectionProvider`),r(` — see `),e(`a`,{href:`#the-connection-manager-is-gone`},`The connection manager is gone`)])],-1),l[660]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`AdoJobStoreOptions.TxIsolationLevelSerializable`),r(` is `),e(`code`,null,`TransactionIsolationLevel`)]),e(`td`,null,[r(`An `),e(`code`,null,`IsolationLevel?`),r(` rather than a `),e(`code`,null,`bool`),r(`, so `),e(`code`,null,`Snapshot`),r(` and the rest are expressible. The legacy key still translates — see `),e(`a`,{href:`#the-isolation-level-is-an-isolation-level`},`The isolation level is an isolation level`)])],-1),l[661]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`AdoJobStoreOptions.CommandTimeout`),r(` added`)]),e(`td`,null,[r(`Bounds every statement the store issues, the lock handler's included; it reaches them through `),e(`code`,null,`DriverDelegateContext.CommandTimeout`),r(` and `),e(`code`,null,`LockHandlerContext.CommandTimeout`),r(`. Unset keeps each provider's own default, so nothing changes for a store that does not set it. 3.x had no way to say this at all — there was no `),e(`code`,null,`quartz.*`),r(` key for it, so nothing needs translating`)])],-1),l[662]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`DbMetadataFactory`),r(` is internal`)]),e(`td`,null,[r(`Every implementation was already internal and no public member accepted one; describe a driver through `),e(`code`,null,`UseGenericDatabase`),r(`'s metadata factory`)])],-1),l[663]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`DbProvider.PropertyDbProvider`),r(` and `),e(`code`,null,`.DbProviderResourceName`),r(` removed`)]),e(`td`,null,[r(`Two `),e(`code`,null,`protected const`),r(`s nothing read, left over from the process-wide provider registry`)])],-1),l[664]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`SimplePropertiesTriggerPersistenceDelegateBase`),r(`'s four SQL statements are private`)]),e(`td`,null,[e(`code`,null,`SelectSimplePropsTrigger`),r(`, `),e(`code`,null,`DeleteSimplePropsTrigger`),r(`, `),e(`code`,null,`InsertSimplePropsTrigger`),r(` and `),e(`code`,null,`UpdateSimplePropsTrigger`),r(` name every column the base class binds, so replacing one could not work. The column name constants stay `),e(`code`,null,`protected`),r(` — they are the schema contract — and `),e(`code`,null,`TableSimplePropertiesTriggers`),r(` is `),e(`code`,null,`protected`),r(` still, now aliasing the `),e(`code`,null,`public`),r(),e(`code`,null,`AdoConstants.TableSimplePropertiesTriggers`),r(` that schema validation walks`)])],-1),l[665]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`RAMJobStore`),r(` is `),e(`code`,null,`sealed`),r(` and has no `),e(`code`,null,`virtual`),r(` members`)]),e(`td`,null,[r(`Wrap it in a store deriving from the new `),e(`code`,null,`DelegatingJobStore`),r(` instead of deriving from it — see `),e(`a`,{href:`#ramjobstore-is-sealed`},[e(`code`,null,`RAMJobStore`),r(` is sealed`)])])],-1),l[666]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Impl.DelegatingJobStore`),r(` added`)]),e(`td`,null,[r(`Forwards every `),e(`code`,null,`IJobStore`),r(` member to a wrapped store, each one `),e(`code`,null,`virtual`),r(`, so a decorating store overrides only what it changes — see `),e(`a`,{href:`#delegatingjobstore-decorates-a-store`},[e(`code`,null,`DelegatingJobStore`),r(` decorates a store`)])])],-1),l[667]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`HostnameInstanceIdGenerator`),r(` is `),e(`code`,null,`HostNameInstanceIdGenerator`)]),e(`td`,null,[r(`Casing matched to `),e(`code`,null,`HostNameBasedIdGenerator`),r(`. The type is internal; a `),e(`code`,null,`quartz.scheduler.instanceIdGenerator.type`),r(` still naming the old spelling resolves, with a warning`)])],-1),l[668]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`AddJob<T>`),r(` and `),e(`code`,null,`ScheduleJob<T>`),r(` register the job type`)]),e(`td`,null,[r(`Scoped, with `),e(`code`,null,`TryAdd`),r(`, so an unresolvable job fails `),e(`code`,null,`ValidateOnBuild`),r(` instead of at fire time — see `),e(`a`,{href:`#addjob-registers-the-job-with-the-container`},[e(`code`,null,`AddJob`),r(` registers the job with the container`)])])],-1),l[669]||=e(`tr`,null,[e(`td`,null,`A registered job's constructor may not take a scheduler's parts`),e(`td`,null,[e(`code`,null,`IScheduler`),r(`, `),e(`code`,null,`ISchedulerFactory`),r(`, `),e(`code`,null,`IJobStore`),r(`, `),e(`code`,null,`IThreadPool`),r(` and `),e(`code`,null,`IOptions<QuartzSchedulerOptions>`),r(` belong to one scheduler, and the container resolves them unkeyed, so a registered job taking one is refused when the host starts rather than handed the default scheduler's. Read the scheduler from `),e(`code`,null,`IJobExecutionContext.Scheduler`),r(`, or register the job with `),e(`code`,null,`AddJobType<T>(factory)`),r(` and resolve the part by key there — see `),e(`a`,{href:`#addjob-registers-the-job-with-the-container`},[e(`code`,null,`AddJob`),r(` registers the job with the container`)])])],-1),l[670]||=e(`tr`,null,[e(`td`,null,[r(`The `),e(`code`,null,`JobKey`),r(`-taking `),e(`code`,null,`AddJob`),r(` overloads removed`)]),e(`td`,null,[r(`Identity is set inside the configurator with `),e(`code`,null,`WithIdentity`),r(` — see `),e(`a`,{href:`#one-shape-per-registration-method`},`One shape per registration method`)])],-1),l[671]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`IServiceCollectionQuartzConfigurator`),r(` is `),e(`code`,null,`IQuartzBuilder`)]),e(`td`,null,[r(`And the `),e(`code`,null,`AddQuartz`),r(` overloads taking an `),e(`code`,null,`(configurator, IServiceProvider)`),r(` callback are gone; use the `),e(`code`,null,`(IServiceProvider, configurator)`),r(` shape of `),e(`code`,null,`AddJob`),r(` / `),e(`code`,null,`AddTrigger`),r(` / `),e(`code`,null,`ScheduleJob`)])],-1),l[672]||=e(`tr`,null,[e(`td`,null,[r(`DI `),e(`code`,null,`AddCalendar`),r(` takes `),e(`code`,null,`AddCalendarOptions`)]),e(`td`,null,[r(`The two adjacent bools are gone, and `),e(`code`,null,`calendarName`),r(` is `),e(`code`,null,`name`)])],-1),l[673]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`AddPlugin`),r(` shapes aligned to the listener trio`)]),e(`td`,null,[r(`The name is an optional trailing argument on all three — see `),e(`a`,{href:`#plugins-are-registered-like-listeners`},`Plugins are registered like listeners`)])],-1),l[674]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`AddQuartzSchedulers(IConfiguration, …)`),r(` added`)]),e(`td`,null,[e(`code`,null,`AddQuartz(configuration)`),r(` no longer fans out over a `),e(`code`,null,`Schedulers`),r(` section; it throws and points here`)])],-1),l[675]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`QuartzHostedService`),r(` takes an `),e(`code`,null,`IServiceProvider`),r(` and an `),e(`code`,null,`IOptionsMonitor`)]),e(`td`,null,[r(`It resolves every scheduler in the container when the host starts — see `),e(`a`,{href:`#the-hosted-service-starts-every-scheduler`},`The hosted service starts every scheduler`)])],-1),l[676]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`AddQuartzHostedService(string schedulerName, …)`),r(` added`)]),e(`td`,null,[e(`code`,null,`QuartzHostedServiceOptions`),r(` are named options; the unnamed call still configures every scheduler`)])],-1),l[677]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`QuartzHostedServiceOptions.AutoStart`),r(` added`)]),e(`td`,null,[r(`Defaults to `),e(`code`,null,`true`),r(`, so nothing changes for an existing application. `),e(`code`,null,`false`),r(` has the scheduler resolved, initialized and bound but left in `),e(`code`,null,`Created`),r(` for the application to start, and the health check reports it degraded rather than unhealthy — see `),e(`a`,{href:`#a-hosted-scheduler-can-be-started-by-the-application`},`A hosted scheduler can be started by the application`)])],-1),l[678]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`IQuartzBuilder.AddHttpApi`),r(` / `),e(`code`,null,`MapQuartzApi`),r(` renamed`)]),e(`td`,null,[e(`code`,null,`services.AddQuartzHttpApi()`),r(` / `),e(`code`,null,`MapQuartzHttpApi`),r(`, the first of them on the service collection rather than a scheduler's builder; `),e(`code`,null,`AddQuartzHealthChecks`),r(` gained an `),e(`code`,null,`IQuartzBuilder`),r(` overload, which it keeps because a health check really is one scheduler's`)])],-1),l[679]||=e(`tr`,null,[e(`td`,null,[r(`The health check is added on `),e(`code`,null,`IHealthChecksBuilder`)]),e(`td`,null,[e(`code`,null,`AddHealthChecks().AddQuartz()`),r(` / `),e(`code`,null,`.AddQuartz("reporting")`),r(`, so it composes with an application's other checks. `),e(`code`,null,`IServiceCollection.AddQuartzHealthChecks()`),r(` was shorthand for the first and is gone; `),e(`code`,null,`IQuartzBuilder.AddQuartzHealthChecks()`),r(` stays, because a scheduler's builder knows which scheduler the check is for — see `),e(`a`,{href:`#the-asp-net-core-methods-say-quartz-once`},`The ASP.NET Core methods say Quartz once`)])],-1),l[680]||=e(`tr`,null,[e(`td`,null,[r(`The health check ships in `),e(`code`,null,`Quartz`)]),e(`td`,null,[r(`3.x registered it from `),e(`code`,null,`Quartz.AspNetCore`),r(`, whose `),e(`code`,null,`FrameworkReference`),r(` a worker on a `),e(`code`,null,`dotnet/runtime`),r(` image cannot satisfy. Drop that package reference if the check was the only reason for it — see `),e(`a`,{href:`#the-health-check-is-in-quartz-not-quartz-aspnetcore`},[r(`The health check is in `),e(`code`,null,`Quartz`),r(`, not `),e(`code`,null,`Quartz.AspNetCore`)])])],-1),l[681]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`QuartzHealthCheckOptions`),r(` goes through the options pipeline`)]),e(`td`,null,[r(`It was constructed and read inside the registration call, so `),e(`code`,null,`services.Configure<QuartzHealthCheckOptions>(...)`),r(` did nothing. `),e(`code`,null,`Name`),r(` is nullable and defaults to the scheduler's check name`)])],-1),l[682]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`QuartzHealthCheckOptions.Tags`),r(` is a get-only `),e(`code`,null,`List<string>`)]),e(`td`,null,[r(`It was a settable `),e(`code`,null,`IReadOnlyCollection<string>`),r(`. Add to it — `),e(`code`,null,`options.Tags.AddRange(["ready", "live"])`),r(` — rather than assigning; one `),e(`code`,null,`configure`),r(` callback can no longer discard the tags another added — see `),e(`a`,{href:`#a-shipped-component-is-configured-through-its-options-type-and-only-there`},`A shipped component is configured through its options type, and only there`)])],-1),l[683]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`QuartzSchedulerBuilder.Build()`),r(` returns `),e(`code`,null,`StandaloneSchedulerFactory`)]),e(`td`,null,[r(`It is an `),e(`code`,null,`ISchedulerFactory`),r(` that is also `),e(`code`,null,`IAsyncDisposable`),r(` and `),e(`code`,null,`IDisposable`),r(`, so disposing the container needs no cast`)])],-1),l[684]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`JobBuilder<TJob>.Key`),r(` is public`)]),e(`td`,null,[r(`Reports the identity the builder was given, or `),e(`code`,null,`null`),r(` when none was set, so a trigger registered alongside a job can agree with it`)])],-1),l[685]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`ISchedulerProxyFactory`),r(` and `),e(`code`,null,`HttpSchedulerProxyFactory`),r(` removed`)]),e(`td`,null,[r(`Nothing read them — see `),e(`a`,{href:`#remoting-a-scheduler-is-not-a-quartz-concern`},`Remoting a scheduler is not a Quartz concern`)])],-1),l[686]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`quartz.scheduler.proxy*`),r(` and `),e(`code`,null,`quartz.scheduler.exporter*`),r(` are rejected`)]),e(`td`,null,`They were whitelisted but read by nobody; the exception names the replacement`)],-1),l[687]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`[Serializable]`),r(` removed from 30 types`)]),e(`td`,null,[r(`It stays only on the types a job store blob can be made of — see `),e(`a`,{href:`#serializable-survives-only-where-a-database-blob-needs-it`},[e(`code`,null,`[Serializable]`),r(` survives only where a database blob needs it`)])])],-1),l[688]||=e(`tr`,null,[e(`td`,null,[r(`The `),e(`code`,null,`(SerializationInfo, StreamingContext)`),r(` constructors removed`)]),e(`td`,null,[r(`On `),e(`code`,null,`SchedulerException`),r(`, `),e(`code`,null,`JobPersistenceException`),r(`, `),e(`code`,null,`SchedulerConfigException`),r(` and `),e(`code`,null,`HttpClientException`),r(`. `),e(`code`,null,`BinaryFormatter`),r(` was their only caller, and the base class library's equivalent is obsolete`)])],-1),l[689]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`[Serializable]`),r(` removed from `),e(`code`,null,`JobExecutionContextImpl`),r(` and `),e(`code`,null,`SchedulerContext`)]),e(`td`,null,[r(`Neither is persisted; `),e(`code`,null,`SchedulerContext`),r(` also lost its private deserialization constructor`)])],-1),l[690]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`JobExecutionProcessException`),r(` and `),e(`code`,null,`JobInstantiationException`),r(` moved to `),e(`code`,null,`Quartz`)]),e(`td`,null,[e(`code`,null,`Quartz.Core`),r(` is internal now — see `),e(`a`,{href:`#the-two-exceptions-moved-out-of-quartz-core`},[r(`The two exceptions moved out of `),e(`code`,null,`Quartz.Core`)])])],-1),l[691]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`ExecutionLimits`),r(` split into a builder and a snapshot`)]),e(`td`,null,[e(`code`,null,`ExecutionLimitsBuilder`),r(` mutates, `),e(`code`,null,`ExecutionLimits`),r(` is immutable and is no longer an `),e(`code`,null,`IReadOnlyDictionary<string, int?>`),r(` — see `),e(`a`,{href:`#execution-limits-are-built-once-then-frozen`},`Execution limits are built once, then frozen`)])],-1),l[692]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`IQuartzBuilder.UseExecutionLimits`),r(` takes an `),e(`code`,null,`Action<ExecutionLimitsBuilder>`)]),e(`td`,null,`The lambda body is unchanged`)],-1),l[693]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`TriggerAcquisitionRequest.ExecutionLimits`),r(` and `),e(`code`,null,`TriggerAcquisitionCriteria.ExecutionLimits`),r(` are `),e(`code`,null,`ExecutionLimits?`)]),e(`td`,null,[r(`They were `),e(`code`,null,`IReadOnlyDictionary<string, int?>?`),r(`; spend the slots through `),e(`code`,null,`CreateSlots()`),r(` — see `),e(`a`,{href:`#a-job-store-is-handed-the-limits-and-a-way-to-spend-them`},`A job store is handed the limits, and a way to spend them`)])],-1),l[694]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.ExecutionSlots`),r(`, `),e(`code`,null,`Quartz.ExecutionGroupLimit`),r(` and `),e(`code`,null,`Quartz.ExecutionGroupScope`),r(` added`)]),e(`td`,null,[r(`The slot-counting rule, one scope's entry in a snapshot, and the typed default/other/named scope those entries carry — see `),e(`a`,{href:`#execution-limits-are-built-once-then-frozen`},`Execution limits are built once, then frozen`)])],-1),l[695]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.MonthDay`),r(` added`)]),e(`td`,null,[r(`The month-and-day value `),e(`code`,null,`AnnualCalendar`),r(` excludes — see `),e(`a`,{href:`#excluded-days-are-a-read-only-set`},`Excluded days are a read-only set`)])],-1),l[696]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`ICancellableJobExecutionContext`),r(` removed`)]),e(`td`,null,[r(`Interruption is `),e(`code`,null,`IScheduler.Interrupt`),r(` / `),e(`code`,null,`InterruptFireInstance`),r(` to request and `),e(`code`,null,`IJobExecutionContext.CancellationToken`),r(` to observe — see `),e(`a`,{href:`#interruption-has-two-names-not-three`},`Interruption has two names, not three`)])],-1),l[697]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Diagnostics.IJobDiagnosticData`),r(` removed`)]),e(`td`,null,[r(`It was the payload contract of the `),e(`code`,null,`DiagnosticSource`),r(` events `),e(`code`,null,`Quartz.OpenTracing`),r(` consumed. Both the package and the events are gone; job execution is on `),e(`code`,null,`Activity`),r(` through `),e(`code`,null,`QuartzActivitySource`),r(`, and `),e(`code`,null,`IJobExecutionContext`),r(` is what a listener reads`)])],-1),l[698]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`CronExpression.Clone()`),r(` removed`)]),e(`td`,null,[r(`The type is sealed and immutable, so a copy was an allocation and nothing else — reuse the instance. See `),e(`a`,{href:`#cronexpression-is-immutable`},[e(`code`,null,`CronExpression`),r(` is immutable`)])])],-1),l[699]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`IJobExecutionContext.Put`),r(` / `),e(`code`,null,`.Get`),r(` removed`)]),e(`td`,null,[r(`The volatile per-execution side-map had no reader in Quartz and no way for its two ends to find each other but a shared string. A job talking to its listeners sets `),e(`code`,null,`Result`),r(`, whose type the two agree on between themselves; state that must survive the execution belongs in `),e(`code`,null,`JobDataMap`),r(`. `),e(`code`,null,`JobExecutionContextImpl`),r(` is public, so this is a class-member removal too`)])],-1),l[700]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`JobDataMap`),r(`'s sixty typed accessors removed`)]),e(`td`,null,[r(`One set of extension members does the same job — see `),e(`a`,{href:`#jobdatamap-s-typed-accessors-are-extension-members`},[e(`code`,null,`JobDataMap`),r(`'s typed accessors are extension members`)])])],-1),l[701]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.AspNetCore.AddQuartzServer`),r(` removed`)]),e(`td`,null,[e(`code`,null,`AddQuartzHostedService`),r(` starts the scheduler and `),e(`code`,null,`AddHealthChecks().AddQuartz()`),r(` registers the check — see `),e(`a`,{href:`#addquartzserver-is-addquartzhostedservice`},[e(`code`,null,`AddQuartzServer`),r(` is `),e(`code`,null,`AddQuartzHostedService`)])])],-1),l[702]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`ISchedulerFactory.GetScheduler(name)`),r(` is `),e(`code`,null,`LookupScheduler(name)`)]),e(`td`,null,[r(`Two members named `),e(`code`,null,`GetScheduler`),r(` differed only in nullability. `),e(`code`,null,`GetScheduler()`),r(` builds this factory's scheduler and cannot return null; `),e(`code`,null,`LookupScheduler(name)`),r(` looks one up in the container's repository and can, which is what the verb now says. `),e(`code`,null,`Lookup`),r(` matches `),e(`code`,null,`ISchedulerRepository.Lookup`)])],-1),l[703]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`TriggerUtils`),r(` became `),e(`code`,null,`Quartz.Extensibility.TriggerFireTimes`)]),e(`td`,null,[r(`A fire-time calculator over `),e(`code`,null,`IOperableTrigger`),r(`, named for what it computes; the three `),e(`code`,null,`Compute*`),r(` methods shortened with it — see `),e(`a`,{href:`#triggerutils-became-triggerfiretimes`},[e(`code`,null,`TriggerUtils`),r(` became `),e(`code`,null,`TriggerFireTimes`)])])],-1),l[704]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`TimeZoneUtil`),r(` became `),e(`code`,null,`Quartz.TimeZones`)]),e(`td`,null,[e(`code`,null,`FindTimeZoneById`),r(` — now `),e(`code`,null,`FindById`),r(` — and the wall-clock `),e(`code`,null,`GetUtcOffset`),r(` are scheduling API, not utilities — see `),e(`a`,{href:`#timezoneutil-became-quartz-timezones`},[e(`code`,null,`TimeZoneUtil`),r(` became `),e(`code`,null,`Quartz.TimeZones`)])])],-1),l[705]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Util.ObjectExtensions`),r(` is internal`)]),e(`td`,null,[e(`code`,null,`AssemblyQualifiedNameWithoutVersion()`),r(` is how Quartz spells a type name into a blob or onto the wire, not a general-purpose helper`)])],-1),l[706]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Diagnostics.ActivityOptions`),r(` is `),e(`code`,null,`ActivityTags`)]),e(`td`,null,[r(`It holds `),e(`code`,null,`Activity`),r(` tag names, not options, and `),e(`code`,null,`*Options`),r(` names an options type everywhere else. It replaced 3.x's `),e(`code`,null,`DiagnosticHeaders`),r(`; the constant names are unchanged, but their `),e(`strong`,null,`values`),r(` are all `),e(`code`,null,`quartz.*`),r(` now — see `),e(`a`,{href:`#job-execution-metrics`},`Job execution metrics`)])],-1),l[707]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`DBSemaphore`),r(` is `),e(`code`,null,`DbLockHandler`)]),e(`td`,null,[r(`The last `),e(`code`,null,`DB`),r(` spelling left in the ADO.NET surface, and the noun the whole family settled on — see `),e(`a`,{href:`#the-semaphores-are-lock-handlers`},`The semaphores are lock handlers`),r(`. The type is abstract and is never named in configuration`)])],-1),l[708]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`StartingDailyAt`),r(` / `),e(`code`,null,`EndingDailyAt`),r(` take a `),e(`code`,null,`timeOfDay`)]),e(`td`,null,[r(`The parameter was `),e(`code`,null,`timeOfDayUtc`),r(`, and the value is wall-clock in the trigger's time zone rather than UTC — the property it sets, `),e(`code`,null,`StartTimeOfDay`),r(`, never claimed otherwise. `),e(`code`,null,`DailyTimeIntervalTriggerImpl`),r(`'s five constructors say `),e(`code`,null,`startTimeOfDay`),r(` / `),e(`code`,null,`endTimeOfDay`),r(` for the same reason`)])],-1),l[709]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`ITriggerSerializer.TriggerTypeForJson`),r(` is `),e(`code`,null,`TriggerTypeName`)]),e(`td`,null,[e(`code`,null,`ICalendarSerializer.CalendarTypeName`),r(` names the same concept; both JSON serializers changed`)])],-1),l[710]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`IDriverDelegate.SelectNumTriggersForJob`),r(` is `),e(`code`,null,`CountTriggersForJob`)]),e(`td`,null,[r(`Matching `),e(`code`,null,`CountMisfiredTriggersInState`),r(`, and spelling out the last `),e(`code`,null,`Num`)])],-1),l[711]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`SimpleTriggerImpl.ComputeNumTimesFiredBetween`),r(` is `),e(`code`,null,`ComputeNumberOfTimesFiredBetween`)]),e(`td`,null,`As above`)],-1),l[712]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`TriggerAcquireResult.JobType`),r(` is `),e(`code`,null,`JobTypeName`)]),e(`td`,null,[r(`It holds `),e(`code`,null,`JOB_CLASS_NAME`),r(` — a type name, the same thing `),e(`code`,null,`JobHeader.JobTypeName`),r(` carries — and was documented as a discriminator, which it is not. `),e(`code`,null,`TriggerHeader.TriggerType`),r(` really is a discriminator and keeps its name`)])],-1),l[713]||=e(`tr`,null,[e(`td`,null,`Parameter names spelled out on the ADO.NET surface`),e(`td`,null,[e(`code`,null,`IDriverDelegate.SelectJobDetail`),r(`'s `),e(`code`,null,`classLoadHelper`),r(` is `),e(`code`,null,`loadHelper`),r(` (the implementation already called it that, so named arguments disagreed with the interface); `),e(`code`,null,`ts`),r(` is `),e(`code`,null,`misfireTime`),r(`; `),e(`code`,null,`AdoJobStoreBase.ReleaseLock`),r(`'s `),e(`code`,null,`doIt`),r(` is `),e(`code`,null,`shouldRelease`),r(`; `),e(`code`,null,`StdAdoDelegate.AddTriggerPersistenceDelegate`),r(`'s `),e(`code`,null,`del`),r(` is `),e(`code`,null,`persistenceDelegate`),r(`; `),e(`code`,null,`TriggerPropertyBundle`),r(`'s `),e(`code`,null,`sb`),r(` is `),e(`code`,null,`scheduleBuilder`),r(`; `),e(`code`,null,`CronTriggerImpl.WillFireOn`),r(`'s `),e(`code`,null,`test`),r(` is `),e(`code`,null,`timeUtc`),r(`; `),e(`code`,null,`TriggerFireTimes.Compute`),r(`'s `),e(`code`,null,`numTimes`),r(` is `),e(`code`,null,`numberOfTimes`)])],-1)])]),l[898]||=t(`<h2 id="appendix-what-happened-to-a-name" tabindex="-1"><a class="header-anchor" href="#appendix-what-happened-to-a-name"><span>Appendix: what happened to a name</span></a></h2><p>The guide above is organised by topic, which is the right shape for reading it and the wrong shape for the question a broken build actually asks — <em>what happened to this one type?</em> This appendix is the index for that question, and it links back to the section that explains each entry rather than explaining it a second time.</p><p>It is derived mechanically, by diffing the public API baselines both branches keep under <code>src/Quartz.Tests.Unit/Verify/</code> and <code>src/Quartz.Tests.AspNetCore/Verify/</code>, rather than from the names that came to mind, so it is complete: <strong>every</strong> public type 3.x had and 4.0 does not is answered here, across every package. Where a whole family went at once the family has one entry rather than a row per name — the fourteen <code>xsd.exe</code>-generated classes of <code>Quartz.Xml.JobSchedulingData20</code>, the nested structs of <code>MisfireInstruction</code>, the three <code>*Support</code> listener bases and the dashboard DTOs that were replaced wholesale are each explained once, in the section that explains the change. If you want the raw delta for one package rather than this summary, <code>git diff</code> its baseline across the two branches; package boundaries moved, so match the files up with this first:</p><table><thead><tr><th>3.x baseline</th><th>4.x baseline</th></tr></thead><tbody><tr><td><code>PublicApiTest_Quartz</code></td><td><code>PublicApiTest_Quartz</code> — which also absorbs DI, Hosting and SystemTextJson</td></tr><tr><td><code>PublicApiTest_Quartz.Extensions.DependencyInjection</code></td><td>folded into <code>Quartz</code> (<code>Quartz.Configuration</code>)</td></tr><tr><td><code>PublicApiTest_Quartz.Extensions.Hosting</code></td><td>folded into <code>Quartz</code> (<code>src/Quartz/Hosting/</code>)</td></tr><tr><td><code>PublicApiTest_Quartz.Serialization.SystemTextJson</code></td><td>folded into <code>Quartz</code> (<code>SystemTextJsonObjectSerializer</code>)</td></tr><tr><td><code>PublicApiTest_Quartz.Serialization.Json</code></td><td><code>PublicApiTest_Quartz.Serialization.Newtonsoft</code></td></tr><tr><td><code>Quartz.Jobs</code>, <code>Quartz.Plugins</code>, <code>Quartz.Plugins.TimeZoneConverter</code>, <code>Quartz.Extensions.Redis</code>, <code>Quartz.AspNetCore</code>, <code>Quartz.Dashboard</code></td><td>same name on both sides</td></tr><tr><td><code>PublicApiTest_Quartz.OpenTracing</code></td><td>dropped; there is no 4.x package</td></tr><tr><td>(no 3.x baseline — its ancient <code>OpenTelemetry</code> dependency fails restore there)</td><td>dropped; subscribe to the <code>Quartz</code> activity source directly</td></tr><tr><td>—</td><td><code>PublicApiTest_Quartz.HttpClient</code> — new in 4.x</td></tr></tbody></table><p>3.x snapshots <code>net10.0</code> only, so its <code>net472</code> and <code>REMOTING</code> surface never appears in that diff.</p><p>The 4.x baselines also <em>render</em> more than the 3.x ones do, which a cross-branch diff shows on every line it touches, and which is not an API change:</p><table><thead><tr><th>In a 4.x baseline</th><th>What it says</th></tr></thead><tbody><tr><td><code>public sealed record Foo</code> / <code>public readonly record struct Foo</code></td><td>The type is a record. 3.x renders both as a class or a struct, so turning one into the other — losing value equality, the copy constructor and <code>with</code> — moved nothing</td></tr><tr><td>A trailing <code>// default implementation</code> on an interface member</td><td>An implementor may leave that member unwritten. Making it abstract is a source break for every implementor, and 4.0&#39;s rule that a member added to a public interface arrives as a default implementation is only worth something if the baseline says which members are one</td></tr><tr><td><code>// explicit interface implementation: …</code> inside a type</td><td>The type implements that member explicitly. Such a member is private in metadata, so it was rendered nowhere at all, and removing one moved nothing</td></tr></tbody></table><p>The three were added together in one pass over all ten baselines; the generator is <code>PublicApiRendering</code> in the two test projects, and it fails rather than annotating silently when its reading of a rendered line and the rendering itself disagree.</p><p>Two whole-surface changes are deliberately left out, because repeating them per type would bury everything else: <code>Task</code> became <a href="#tasks-changed-to-valuetask"><code>ValueTask</code></a> on nearly every member, and the namespaces moved — <a href="#quartz-spi-and-quartz-simpl-were-renamed"><code>Quartz.Spi</code> is <code>Quartz.Extensibility</code> and <code>Quartz.Simpl</code> is <code>Quartz.Impl</code></a>, and three packages folded into <code>Quartz</code> (see <a href="#package-changes">Package Changes</a>). <strong>A type that only changed namespace keeps its name and is not listed here</strong>, so if you cannot find one below, that is the first thing to check.</p><p><em>Internal</em> below means the type is still there and still doing its job — it is simply no longer part of the contract. If you were deriving from one of them, please <a href="https://github.com/quartznet/quartznet/issues" target="_blank" rel="noopener noreferrer">open an issue</a> rather than working around it.</p><p>Both tables are ordered by the name you would have typed — the type&#39;s own name, ignoring its namespace, and <code>Type.Member</code> for the second one. A few rows name a type that only a 4.0 <em>pre-release</em> had, <code>AdoJobStoreDependencies</code> among them, because that is the name someone upgrading from one of those would look up; see <a href="#appendix-if-you-ran-a-4-0-pre-release">Appendix: if you ran a 4.0 pre-release</a>.</p><h3 id="types-that-were-removed-internalized-or-renamed" tabindex="-1"><a class="header-anchor" href="#types-that-were-removed-internalized-or-renamed"><span>Types that were removed, internalized or renamed</span></a></h3>`,12),e(`table`,null,[l[842]||=e(`thead`,null,[e(`tr`,null,[e(`th`,null,`Type`),e(`th`,null,`What happened`),e(`th`,null,`What to use instead`)])],-1),e(`tbody`,null,[l[724]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Impl.AdoJobStore.AdoJobStoreBase`),r(` (3.x `),e(`code`,null,`JobStoreSupport`),r(`)`)]),e(`td`,null,`Internal`),e(`td`,null,[e(`code`,null,`IJobStore`),r(` for a store of your own, `),e(`code`,null,`DelegatingJobStore`),r(` to wrap one, `),e(`code`,null,`IDriverDelegate`),r(` for a database with no shipped dialect — see `),e(`a`,{href:`#the-ado-net-store-is-a-store-not-a-base-class`},`The ADO.NET store is a store, not a base class`)])],-1),l[725]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Impl.AdoJobStore.AdoJobStoreDependencies`)]),e(`td`,null,`Internal`),e(`td`,null,`Nothing; it existed so a derived store could chain one constructor argument, and there is no derived store — as above`)],-1),l[726]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Impl.AdoJobStore.AdoJobStoreUtil`)]),e(`td`,null,`Internal`),e(`td`,null,[r(`Nothing; it built statement text, which is not a contract — see `),e(`a`,{href:`#sealed-and-internalized-types`},`Sealed and Internalized Types`)])],-1),l[727]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.AdoProviderExtensions`)]),e(`td`,null,[r(`Renamed `),e(`code`,null,`PersistentStoreBuilderExtensions`)]),e(`td`,null,[r(`Same `),e(`code`,null,`Use*`),r(` methods, extending `),e(`code`,null,`IPersistentStoreBuilder`),r(` instead of `),e(`code`,null,`SchedulerBuilder.PersistentStoreOptions`),r(`. Two of them swapped meaning — see `),e(`a`,{href:`#the-sqlite-extension-methods-swapped-names`},`The SQLite extension methods swapped names`)])],-1),l[728]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.SchedulerBuilder.AdoProviderOptions`)]),e(`td`,null,`Removed`),e(`td`,null,[e(`code`,null,`DataSourceOptions`),r(` — see `),e(`a`,{href:`#a-data-source-is-defined-referred-to-or-handed-over`},`A data source is defined, referred to, or handed over`)])],-1),l[729]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.AdoProviderOptionsExtensions`)]),e(`td`,null,`Removed`),e(`td`,null,[r(`It held only `),e(`code`,null,`UseDataSourceConnectionProvider()`),r(`; set `),e(`code`,null,`DataSourceOptions.UseRegisteredDataSource`),r(` — see `),e(`a`,{href:`#adddatasourceprovider-went-with-it`},[e(`code`,null,`AddDataSourceProvider()`),r(` went with it`)])])],-1),l[730]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Impl.AdoJobStore.AdoUtil`)]),e(`td`,null,[r(`Internal, with `),e(`code`,null,`IAdoUtil`)]),e(`td`,null,[r(`Nothing; parameter binding is not an extension point — see `),e(`a`,{href:`#sealed-and-internalized-types`},`Sealed and Internalized Types`)])],-1),l[731]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Simpl.BinaryObjectSerializer`)]),e(`td`,null,`Removed`),e(`td`,null,[e(`code`,null,`SystemTextJsonObjectSerializer`),r(` or `),e(`code`,null,`NewtonsoftJsonObjectSerializer`),r(`; there is no binary serializer, because `),e(`code`,null,`BinaryFormatter`),r(` throws on .NET 9 — see `),e(`a`,{href:`#serializable-survives-only-where-a-database-blob-needs-it`},[e(`code`,null,`[Serializable]`),r(` survives only where a database blob needs it`)])])],-1),l[732]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Listener.BroadcastJobListener`)]),e(`td`,null,`Removed`),e(`td`,null,[r(`Register the listeners individually; the listener manager already notifies every one of them — see `),e(`a`,{href:`#the-broadcast-listeners-are-gone`},`The broadcast listeners are gone`)])],-1),l[733]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Listener.BroadcastSchedulerListener`)]),e(`td`,null,`Removed`),e(`td`,null,`As above`)],-1),l[734]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Listener.BroadcastTriggerListener`)]),e(`td`,null,`Removed`),e(`td`,null,`As above`)],-1),l[735]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.CalendarIntervalTriggerBuilderExtensions`)]),e(`td`,null,`Removed`),e(`td`,null,[e(`code`,null,`TriggerConfiguratorExtensions`),r(` — see `),e(`a`,{href:`#one-family-of-withxschedule-extensions`},[r(`One family of `),e(`code`,null,`WithXSchedule`),r(` extensions`)])])],-1),l[736]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.SchedulerBuilder.ClusterOptions`)]),e(`td`,null,`Removed`),e(`td`,null,[e(`code`,null,`ClusteringOptions`),r(` — see `),e(`a`,{href:`#clustering-is-configured-in-one-place`},`Clustering is configured in one place`)])],-1),l[737]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Impl.AdoJobStore.ExternalTransactionJobStore`),r(` (3.x `),e(`code`,null,`JobStoreCMT`),r(`)`)]),e(`td`,null,`Internal`),e(`td`,null,[e(`code`,null,`UsePersistentStore(store => store.UseAmbientTransactions())`),r(` selects it in code, and `),e(`code`,null,`quartz.jobStore.type`),r(` still names it as a configuration string — see `),e(`a`,{href:`#the-ado-net-store-is-a-store-not-a-base-class`},`The ADO.NET store is a store, not a base class`)])],-1),l[738]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Impl.AdoJobStore.LocalTransactionJobStore`),r(` (3.x `),e(`code`,null,`JobStoreTX`),r(`)`)]),e(`td`,null,`Internal`),e(`td`,null,`As above`)],-1),l[739]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Impl.AdoJobStore.RecoverMisfiredJobsResult`)]),e(`td`,null,`Internal`),e(`td`,null,[r(`Nothing; it was a `),e(`code`,null,`protected`),r(` method's return type`)])],-1),l[740]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Impl.AdoJobStore.Common.ConfigurationBasedDbMetadataFactory`)]),e(`td`,null,`Internal`),e(`td`,null,[r(`The metadata factory on `),e(`code`,null,`UseGenericDatabase`)])],-1),l[741]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.CronScheduleTriggerBuilderExtensions`)]),e(`td`,null,`Removed`),e(`td`,null,[e(`code`,null,`TriggerConfiguratorExtensions`),r(` — see `),e(`a`,{href:`#one-family-of-withxschedule-extensions`},[r(`One family of `),e(`code`,null,`WithXSchedule`),r(` extensions`)])])],-1),l[742]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.DailyTimeIntervalTriggerBuilderExtensions`)]),e(`td`,null,`Removed`),e(`td`,null,[e(`code`,null,`TriggerConfiguratorExtensions`),r(` — see `),e(`a`,{href:`#one-family-of-withxschedule-extensions`},[r(`One family of `),e(`code`,null,`WithXSchedule`),r(` extensions`)])])],-1),l[743]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Util.DataReaderExtensions`)]),e(`td`,null,`Internal`),e(`td`,null,[r(`No replacement; they were `),e(`code`,null,`IDataReader`),r(` conveniences Quartz used on its own reads`)])],-1),l[744]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Util.DBConnectionManager`)]),e(`td`,null,`Removed`),e(`td`,null,[r(`Register a provider with `),e(`code`,null,`UseConnectionProvider`),r(`, and resolve `),e(`code`,null,`IDbProvider`),r(` from the container; `),e(`code`,null,`.Instance`),r(` is gone — see `),e(`a`,{href:`#the-connection-manager-is-gone`},`The connection manager is gone`)])],-1),l[745]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Impl.AdoJobStore.Common.DbMetadataFactory`)]),e(`td`,null,`Internal`),e(`td`,null,[r(`The metadata factory on `),e(`code`,null,`UseGenericDatabase`)])],-1),l[746]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Impl.AdoJobStore.DBSemaphore`)]),e(`td`,null,[r(`Renamed `),e(`code`,null,`DbLockHandler`)]),e(`td`,null,[r(`Same abstract base, still public — see `),e(`a`,{href:`#the-semaphores-are-lock-handlers`},`The semaphores are lock handlers`)])],-1),l[747]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Simpl.DedicatedThreadPool`)]),e(`td`,null,`Internal`),e(`td`,null,[e(`code`,null,`IQuartzBuilder.UseThreadPool(IThreadPool)`),r(` for a pool of your own — see `),e(`a`,{href:`#the-thread-pool-is-asynchronous`},`The thread pool is asynchronous`)])],-1),l[748]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Logging.DiagnosticHeaders`)]),e(`td`,null,[r(`Renamed `),e(`code`,null,`Quartz.Diagnostics.ActivityTags`)]),e(`td`,null,[r(`The constant names are unchanged; every value is `),e(`code`,null,`quartz.*`),r(` now — see `),e(`a`,{href:`#job-execution-metrics`},`Job execution metrics`)])],-1),l[749]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Util.DictionaryExtensions`)]),e(`td`,null,`Removed`),e(`td`,null,[r(`No replacement — see `),e(`a`,{href:`#other-breaking-changes`},`Other Breaking Changes`)])],-1),l[750]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Impl.DirectSchedulerFactory`)]),e(`td`,null,`Removed`),e(`td`,null,[e(`code`,null,`QuartzSchedulerBuilder`),r(`, with `),e(`code`,null,`UseThreadPool(IThreadPool)`),r(` / `),e(`code`,null,`UseJobStore(IJobStore)`),r(` for pre-built parts — see `),e(`a`,{href:`#removed`},`Removed`)])],-1),l[751]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Impl.AdoJobStore.Common.EmbeddedAssemblyResourceDbMetadataFactory`)]),e(`td`,null,`Internal`),e(`td`,null,[r(`The metadata factory on `),e(`code`,null,`UseGenericDatabase`)])],-1),l[752]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Util.FileUtil`)]),e(`td`,null,`Internal`),e(`td`,null,`No replacement; it resolved a path relative to the base directory`)],-1),l[753]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Simpl.HostnameInstanceIdGenerator`)]),e(`td`,null,[r(`Renamed `),e(`code`,null,`HostNameInstanceIdGenerator`),r(`, and internal`)]),e(`td`,null,[r(`A `),e(`code`,null,`quartz.scheduler.instanceIdGenerator.type`),r(` naming the old spelling still resolves, with a warning; in code, register your own `),e(`code`,null,`IInstanceIdGenerator`),r(` — see `),e(`a`,{href:`#other-breaking-changes`},`Other Breaking Changes`)])],-1),l[754]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.ICancellableJobExecutionContext`)]),e(`td`,null,`Removed`),e(`td`,null,[e(`code`,null,`IScheduler.Interrupt`),r(` to request, `),e(`code`,null,`IJobExecutionContext.CancellationToken`),r(` to observe — see `),e(`a`,{href:`#interruption-has-two-names-not-three`},`Interruption has two names, not three`)])],-1),l[755]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.IDashboardAuthorizationFilter`)]),e(`td`,null,`Removed`),e(`td`,null,[e(`code`,null,`QuartzDashboardOptions.AuthorizationPolicy`),r(`; nothing ever invoked the filter — see `),e(`a`,{href:`#other-breaking-changes`},`Other Breaking Changes`)])],-1),l[756]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Logging.IJobDiagnosticData`)]),e(`td`,null,`Removed`),e(`td`,null,[e(`code`,null,`IJobExecutionContext`),r(`, read from a listener; the `),e(`code`,null,`DiagnosticSource`),r(` events it was the payload of are gone — see `),e(`a`,{href:`#other-breaking-changes`},`Other Breaking Changes`)])],-1),l[757]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Core.IJobRunShellFactory`)]),e(`td`,null,`Internal`),e(`td`,null,`No replacement; how a fire is wrapped is not a contract`)],-1),l[758]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.IJobWrapper`)]),e(`td`,null,`Removed`),e(`td`,null,[r(`Per-fire state rides in `),e(`code`,null,`JobScope.State`),r(` — see `),e(`a`,{href:`#the-job-factory-hands-out-a-scope`},`The job factory hands out a scope`)])],-1),l[759]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Logging.ILogProvider`)]),e(`td`,null,`Removed with LibLog`),e(`td`,null,[e(`code`,null,`ILoggerFactory`),r(` — see `),e(`a`,{href:`#logging`},`Logging`)])],-1),l[760]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.SchedulerBuilder.InMemoryStoreOptions`)]),e(`td`,null,`Removed`),e(`td`,null,[e(`code`,null,`InMemoryJobStoreOptions`),r(`, through `),e(`code`,null,`UseInMemoryStore(configure)`)])],-1),l[761]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Dashboard.Services.InProcessQuartzApiClient`)]),e(`td`,null,`Internal`),e(`td`,null,[r(`Resolve `),e(`code`,null,`IQuartzApiClient`),r(` — see `),e(`a`,{href:`#other-breaking-changes`},`Other Breaking Changes`)])],-1),l[762]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Simpl.InternalTriggerState`)]),e(`td`,null,`Removed`),e(`td`,null,[e(`code`,null,`Quartz.Extensibility.StoredTriggerState`),r(`, the stored-state vocabulary every store now shares — see `),e(`a`,{href:`#trigger-states-are-typed-on-the-driver-delegate`},`Trigger states are typed on the driver delegate`)])],-1),l[763]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.IPropertyConfigurationRoot`)]),e(`td`,null,`Removed`),e(`td`,null,[r(`Typed options — see `),e(`a`,{href:`#code-first-configuration-is-typed`},`Code-first configuration is typed`)])],-1),l[764]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Impl.AdoJobStore.ISemaphore`)]),e(`td`,null,[r(`Renamed `),e(`code`,null,`ILockHandler`)]),e(`td`,null,[e(`code`,null,`ObtainLock`),r(` is `),e(`code`,null,`AcquireLock`),r(`; `),e(`code`,null,`ReleaseLock`),r(` and `),e(`code`,null,`RequiresConnection`),r(` are unchanged — see `),e(`a`,{href:`#the-semaphores-are-lock-handlers`},`The semaphores are lock handlers`)])],-1),l[765]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Impl.AdoJobStore.ITablePrefixAware`)]),e(`td`,null,`Removed`),e(`td`,null,[e(`code`,null,`ILockHandler.Initialize(LockHandlerContext)`),r(` — see `),e(`a`,{href:`#a-lock-handler-is-told-which-scheduler-it-locks-for`},`A lock handler is told which scheduler it locks for`)])],-1),l[766]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.IPropertyConfigurer`)]),e(`td`,null,`Removed`),e(`td`,null,[r(`Typed options — see `),e(`a`,{href:`#code-first-configuration-is-typed`},`Code-first configuration is typed`)])],-1),l[767]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.IPropertySetter`)]),e(`td`,null,`Removed`),e(`td`,null,[r(`Typed options — see `),e(`a`,{href:`#code-first-configuration-is-typed`},`Code-first configuration is typed`)])],-1),l[768]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Dashboard.Services.IQuartzApiClientExecutionLimits`)]),e(`td`,null,`Removed`),e(`td`,null,[e(`code`,null,`IQuartzApiClient`),r(`, which carries `),e(`code`,null,`GetExecutionLimits`),r(` itself`)])],-1),l[769]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Simpl.IRemotableQuartzScheduler`)]),e(`td`,null,`Removed`),e(`td`,null,[r(`Nothing; .NET Remoting is not supported — see `),e(`a`,{href:`#remoting-a-scheduler-is-not-a-quartz-concern`},`Remoting a scheduler is not a Quartz concern`)])],-1),l[770]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Spi.IRemotableSchedulerProxyFactory`)]),e(`td`,null,`Removed`),e(`td`,null,[r(`Nothing; `),e(`code`,null,`Quartz.HttpClient`),r(` talks to a remote scheduler over HTTP — see `),e(`a`,{href:`#remoting-a-scheduler-is-not-a-quartz-concern`},`Remoting a scheduler is not a Quartz concern`)])],-1),l[771]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Spi.ISchedulerExporter`)]),e(`td`,null,`Removed`),e(`td`,null,[r(`Nothing; `),e(`code`,null,`AddQuartzHttpApi`),r(` / `),e(`code`,null,`MapQuartzHttpApi`),r(` serve a scheduler over HTTP — see `),e(`a`,{href:`#remoting-a-scheduler-is-not-a-quartz-concern`},`Remoting a scheduler is not a Quartz concern`)])],-1),l[772]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.IServiceCollectionQuartzConfigurator`)]),e(`td`,null,[r(`Renamed `),e(`code`,null,`IQuartzBuilder`)]),e(`td`,null,[r(`The same members, on one interface shared with the standalone builder — see `),e(`a`,{href:`#the-standalone-builder-is-the-same-builder`},`The standalone builder is the same builder`)])],-1),l[773]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Spi.ITypeLoadHelper`)]),e(`td`,null,[r(`Renamed `),e(`code`,null,`Quartz.Extensibility.ITypeLoader`)]),e(`td`,null,[r(`The last `),e(`code`,null,`*Helper`),r(` left in the public surface; the builder method `),e(`code`,null,`UseTypeLoader<T>()`),r(` already had the new spelling. `),e(`code`,null,`Initialize()`),r(` is gone; `),e(`code`,null,`LoadType`),r(` is the whole interface. `),e(`code`,null,`AdoJobStoreBase.TypeLoadHelper`),r(` and `),e(`code`,null,`DriverDelegateContext.TypeLoadHelper`),r(` are `),e(`code`,null,`TypeLoader`),r(` to match`)])],-1),l[774]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Impl.JobDetailImpl`)]),e(`td`,null,`Internal`),e(`td`,null,[e(`code`,null,`JobBuilder.Create<TJob>()`),r(`; read an `),e(`code`,null,`IJobDetail`)])],-1),l[775]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.JobFactoryOptions`)]),e(`td`,null,`Kept, but emptied and refilled`),e(`td`,null,[r(`Its two 3.x properties — `),e(`code`,null,`AllowDefaultConstructor`),r(` and `),e(`code`,null,`CreateScope`),r(` — were already `),e(`code`,null,`[Obsolete]`),r(` no-ops and are gone. The type is `),e(`code`,null,`sealed`),r(` and carries `),e(`code`,null,`ConfigureScope`),r(`, the per-scheduler hook for a scope Quartz opens — see `),e(`a`,{href:`#the-job-factory-hands-out-a-scope`},`The job factory hands out a scope`)])],-1),l[776]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Plugin.Interrupt.JobInterruptMonitorPlugin`)]),e(`td`,null,`Removed`),e(`td`,null,[e(`code`,null,`q.AddJobTimeout(TimeSpan)`),r(` and `),e(`code`,null,`[JobTimeout("hh:mm:ss")]`),r(`, both in the core package. Its `),e(`code`,null,`JobDataMapKeyAutoInterruptable`),r(` and `),e(`code`,null,`JobDataMapKeyMaxRunTime`),r(` constants go with it — see `),e(`a`,{href:`#jobinterruptmonitorplugin-is-retired-a-job-timeout-is-middleware`},[e(`code`,null,`JobInterruptMonitorPlugin`),r(` is retired; a job timeout is middleware`)])])],-1),l[777]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Core.JobRunShell`)]),e(`td`,null,`Internal`),e(`td`,null,[r(`No replacement; use `),e(`code`,null,`IJobListener`),r(` to observe a fire`)])],-1),l[778]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Impl.AdoJobStore.JobStoreCMT`)]),e(`td`,null,[r(`Renamed `),e(`code`,null,`ExternalTransactionJobStore`),r(`, and internal`)]),e(`td`,null,[r(`The old spelling still resolves in `),e(`code`,null,`quartz.jobStore.type`),r(`, with a warning, so a configuration file needs no change; what is gone is naming the type in code — see `),e(`a`,{href:`#the-ado-net-job-stores-are-named-for-whose-transaction-they-use`},`The ADO.NET job stores are named for whose transaction they use`),r(` and `),e(`a`,{href:`#the-ado-net-store-is-a-store-not-a-base-class`},`The ADO.NET store is a store, not a base class`)])],-1),l[779]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Impl.AdoJobStore.JobStoreTX`)]),e(`td`,null,[r(`Renamed `),e(`code`,null,`LocalTransactionJobStore`),r(`, and internal`)]),e(`td`,null,`As above`)],-1),l[780]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Impl.AdoJobStore.JobStoreSupport`)]),e(`td`,null,[r(`Renamed `),e(`code`,null,`AdoJobStoreBase`),r(`, and internal`)]),e(`td`,null,[r(`Abstract, so it was never a configuration string and there is nothing to fall back for. There is no derived store to update either: implement `),e(`code`,null,`IJobStore`),r(`, or wrap one with `),e(`code`,null,`DelegatingJobStore`),r(` — see `),e(`a`,{href:`#the-ado-net-store-is-a-store-not-a-base-class`},`The ADO.NET store is a store, not a base class`)])],-1),l[781]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Impl.Triggers.AbstractTrigger`)]),e(`td`,null,[r(`Renamed `),e(`code`,null,`TriggerBase`)]),e(`td`,null,[r(`Abstract, so never a `),e(`code`,null,`$type`),r(` value in stored JSON; the five concrete `),e(`code`,null,`*TriggerImpl`),r(` names are unchanged — see `),e(`a`,{href:`#triggerbase-property-removals`},`TriggerBase Property Removals`),r(` for the rename and its one binary-blob caveat`)])],-1),l[782]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Impl.AdoJobStore.SimplePropertiesTriggerPersistenceDelegateSupport`)]),e(`td`,null,[r(`Renamed `),e(`code`,null,`SimplePropertiesTriggerPersistenceDelegateBase`)]),e(`td`,null,`As above`)],-1),l[783]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Simpl.JsonObjectSerializer`)]),e(`td`,null,[r(`Renamed `),e(`code`,null,`Quartz.Impl.NewtonsoftJsonObjectSerializer`),r(`, still in the `),e(`code`,null,`Quartz.Serialization.Newtonsoft`),r(` package`)]),e(`td`,null,[e(`code`,null,`UseNewtonsoftJsonSerializer()`),r(` registers it. Spelled into `),e(`code`,null,`quartz.serializer.type`),r(` it is `),e(`code`,null,`Quartz.Impl.NewtonsoftJsonObjectSerializer, Quartz.Serialization.Newtonsoft`),r(` — see `),e(`a`,{href:`#json-serialization`},`JSON Serialization`)])],-1),l[784]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.JsonSchedulingOptions`)]),e(`td`,null,[r(`Merged into `),e(`code`,null,`FileSchedulingOptions`)]),e(`td`,null,[r(`It was byte-for-byte identical to `),e(`code`,null,`XmlSchedulingOptions`),r(` — see `),e(`a`,{href:`#other-breaking-changes`},`Other Breaking Changes`)])],-1),l[785]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.JsonSerializerOptions`)]),e(`td`,null,`Removed`),e(`td`,null,[e(`code`,null,`UseNewtonsoftJsonSerializer`),r(`'s callback hands you the `),e(`code`,null,`NewtonsoftJsonSerializerRegistry`),r(` itself, with `),e(`code`,null,`registerTriggerConverters`),r(` as a parameter of the method — see `),e(`a`,{href:`#custom-trigger-and-calendar-serializers-are-no-longer-static`},`Custom trigger and calendar serializers are no longer static`)])],-1),l[786]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Logging.LogProviders.LibLogException`)]),e(`td`,null,`Removed with LibLog`),e(`td`,null,[r(`No replacement — see `),e(`a`,{href:`#logging`},`Logging`)])],-1),l[787]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Core.ListenerManagerImpl`)]),e(`td`,null,`Internal`),e(`td`,null,[e(`code`,null,`IScheduler.ListenerManager`),r(`, typed `),e(`code`,null,`IListenerManager`),r(` — see `),e(`a`,{href:`#listener-api-changes`},`Listener API Changes`)])],-1),l[788]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Logging.LogContext`)]),e(`td`,null,`Removed with LibLog`),e(`td`,null,[e(`code`,null,`LogProvider.SetLogProvider(ILoggerFactory)`),r(` — see `),e(`a`,{href:`#logging`},`Logging`)])],-1),l[789]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Logging.Logger`),r(` (delegate)`)]),e(`td`,null,`Removed with LibLog`),e(`td`,null,[e(`code`,null,`ILogger`),r(` — see `),e(`a`,{href:`#logging`},`Logging`)])],-1),l[790]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Logging.LogLevel`)]),e(`td`,null,`Removed with LibLog`),e(`td`,null,[e(`code`,null,`Microsoft.Extensions.Logging.LogLevel`),r(` — see `),e(`a`,{href:`#logging`},`Logging`)])],-1),l[791]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Util.ObjectExtensions`)]),e(`td`,null,`Internal`),e(`td`,null,[r(`No replacement — see `),e(`a`,{href:`#other-breaking-changes`},`Other Breaking Changes`)])],-1),l[792]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Util.ObjectUtils`)]),e(`td`,null,`Removed`),e(`td`,null,[r(`No replacement; it set properties reflectively from strings, which typed options removed the need for — see `),e(`a`,{href:`#code-first-configuration-is-typed`},`Code-first configuration is typed`)])],-1),l[793]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.SchedulerBuilder.PersistentStoreOptions`)]),e(`td`,null,`Removed`),e(`td`,null,[e(`code`,null,`IPersistentStoreBuilder`),r(`, through `),e(`code`,null,`UsePersistentStore(configure)`)])],-1),l[794]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.PropertiesHolder`)]),e(`td`,null,`Removed`),e(`td`,null,[r(`Typed options — see `),e(`a`,{href:`#removed`},`Removed`)])],-1),l[795]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Util.PropertiesParser`)]),e(`td`,null,`Internal`),e(`td`,null,[r(`No replacement; `),e(`code`,null,`QuartzPropertyBridge`),r(` is the only reader of flat `),e(`code`,null,`quartz.*`),r(` keys now — see `),e(`a`,{href:`#flat-keys-still-work`},`Flat keys still work`)])],-1),l[796]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.PropertiesSetter`)]),e(`td`,null,`Removed`),e(`td`,null,[r(`Typed options — see `),e(`a`,{href:`#removed`},`Removed`)])],-1),l[797]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.QuartzConfiguratorExecutionLimitsExtensions`)]),e(`td`,null,`Removed`),e(`td`,null,[e(`code`,null,`IQuartzBuilder.UseExecutionLimits(Action<ExecutionLimitsBuilder>)`),r(` — see `),e(`a`,{href:`#execution-limits-are-built-once-then-frozen`},`Execution limits are built once, then frozen`)])],-1),e(`tr`,null,[l[722]||=e(`td`,null,[e(`code`,null,`Quartz.OpenTracing.QuartzDiagnosticOptions`)],-1),l[723]||=e(`td`,null,`Removed with its package`,-1),e(`td`,null,[l[716]||=e(`code`,null,`AddSource(QuartzInstrumentation.ActivitySourceName)`,-1),l[717]||=r(`; job execution is on `,-1),l[718]||=e(`code`,null,`Activity`,-1),l[719]||=r(` through `,-1),l[720]||=e(`code`,null,`QuartzActivitySource`,-1),l[721]||=r(` — see `,-1),c(g,{to:`/documentation/quartz-4.x/packages/opentelemetry-integration.html`},{default:i(()=>[...l[715]||=[r(`Observability`,-1)]]),_:1})])]),l[798]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Util.QuartzEnvironment`)]),e(`td`,null,`Internal`),e(`td`,null,[e(`code`,null,`System.Environment`),r(`, or `),e(`code`,null,`IConfiguration`),r(` for settings`)])],-1),l[799]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Core.QuartzRandom`)]),e(`td`,null,`Internal`),e(`td`,null,[e(`code`,null,`System.Random`)])],-1),l[800]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Core.QuartzScheduler`)]),e(`td`,null,`Internal`),e(`td`,null,[r(`Resolve `),e(`code`,null,`IScheduler`),r(` or `),e(`code`,null,`ISchedulerFactory`),r(` — see `),e(`a`,{href:`#sealed-and-internalized-types`},`Sealed and Internalized Types`)])],-1),l[801]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Core.QuartzSchedulerResources`)]),e(`td`,null,`Internal`),e(`td`,null,[e(`code`,null,`QuartzSchedulerOptions`),r(` — see `),e(`a`,{href:`#sealed-and-internalized-types`},`Sealed and Internalized Types`)])],-1),l[802]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Core.QuartzSchedulerThread`)]),e(`td`,null,`Internal`),e(`td`,null,`No replacement; the scheduling loop is not an extension point`)],-1),l[803]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.RecurrenceTriggerBuilderExtensions`)]),e(`td`,null,`Removed`),e(`td`,null,[e(`code`,null,`TriggerConfiguratorExtensions`),r(` — see `),e(`a`,{href:`#one-family-of-withxschedule-extensions`},[r(`One family of `),e(`code`,null,`WithXSchedule`),r(` extensions`)])])],-1),l[804]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Impl.RemoteScheduler`)]),e(`td`,null,`Removed`),e(`td`,null,[e(`code`,null,`Quartz.HttpClient`),r(` — see `),e(`a`,{href:`#remoting-a-scheduler-is-not-a-quartz-concern`},`Remoting a scheduler is not a Quartz concern`)])],-1),l[805]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Simpl.RemotingSchedulerProxyFactory`)]),e(`td`,null,`Removed`),e(`td`,null,[r(`As above — see `),e(`a`,{href:`#remoting-a-scheduler-is-not-a-quartz-concern`},`Remoting a scheduler is not a Quartz concern`)])],-1),l[806]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.ScheduleBuilder<T>`)]),e(`td`,null,`Removed`),e(`td`,null,[r(`Implement `),e(`code`,null,`IScheduleBuilder`),r(` directly — see `),e(`a`,{href:`#schedulebuilder-t-is-gone`},[e(`code`,null,`ScheduleBuilder<T>`),r(` is gone`)])])],-1),l[807]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.SchedulerBuilder`)]),e(`td`,null,[r(`Renamed `),e(`code`,null,`QuartzSchedulerBuilder`)]),e(`td`,null,[r(`And `),e(`code`,null,`Create`),r(` takes the `),e(`code`,null,`AddQuartz`),r(` callback, so configuration is written against `),e(`code`,null,`IQuartzBuilder`),r(` — see `),e(`a`,{href:`#the-standalone-builder-is-the-same-builder`},`The standalone builder is the same builder`)])],-1),l[808]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.SchedulerExtensions`)]),e(`td`,null,`Removed`),e(`td`,null,[r(`Its three members are on `),e(`code`,null,`IScheduler`),r(` itself: `),e(`code`,null,`GetExecutionLimits`),r(`, `),e(`code`,null,`SetExecutionLimits`),r(`, `),e(`code`,null,`UpdateTriggerDetails`)])],-1),l[809]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.SchedulerMetaData`)]),e(`td`,null,[r(`Renamed `),e(`code`,null,`SchedulerMetadata`)]),e(`td`,null,[r(`A `),e(`code`,null,`sealed record`),r(`, returned by `),e(`code`,null,`IScheduler.GetMetadata()`),r(` — see `),e(`a`,{href:`#schedulermetadata-replaces-schedulermetadata`},[e(`code`,null,`SchedulerMetadata`),r(` replaces `),e(`code`,null,`SchedulerMetaData`)])])],-1),l[810]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.SchedulerPluginConfigurationExtensions`)]),e(`td`,null,`Removed`),e(`td`,null,[e(`code`,null,`IQuartzBuilder.AddPlugin<T>()`),r(` — see `),e(`a`,{href:`#plugins-are-registered-like-listeners`},`Plugins are registered like listeners`)])],-1),l[811]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Core.SchedulerSignalerImpl`)]),e(`td`,null,`Internal`),e(`td`,null,[r(`Take `),e(`code`,null,`ISchedulerSignaler`),r(` through your constructor — see `),e(`a`,{href:`#spi-changes`},`SPI changes`)])],-1),l[812]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Configuration.QuartzConfigurationHelper`)]),e(`td`,null,`Internal`),e(`td`,null,[r(`Hand the section to `),e(`code`,null,`AddQuartz(configuration)`),r(`, or to `),e(`code`,null,`QuartzSchedulerBuilder.UseConfiguration(configuration)`),r(` without a host — both flatten it themselves. `),e(`code`,null,`QuartzOptions.ToProperties()`),r(` stays for a caller that holds a `),e(`code`,null,`QuartzOptions`),r(`. This empties the public `),e(`code`,null,`Quartz.Configuration`),r(` namespace`)])],-1),l[813]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.ServiceCollectionExtensions`)]),e(`td`,null,[r(`Split into `),e(`code`,null,`Quartz.QuartzServiceCollectionExtensions`),r(` and `),e(`code`,null,`Quartz.QuartzBuilderExtensions`)]),e(`td`,null,[r(`Extension-form call sites are unaffected; only a static-form call (`),e(`code`,null,`ServiceCollectionExtensions.AddQuartz(services, …)`),r(`) has to change. `),e(`code`,null,`ServiceCollectionExtensions`),r(` is the most common helper-class name in .NET, and claiming it in `),e(`code`,null,`Quartz`),r(` gave CS0104 in any file that had one of its own and a `),e(`code`,null,`using Quartz;`)])],-1),l[814]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Simpl.SimpleInstanceIdGenerator`)]),e(`td`,null,`Internal`),e(`td`,null,[r(`It is still the default; register your own `),e(`code`,null,`IInstanceIdGenerator`),r(` to replace it`)])],-1),l[815]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.SimpleScheduleTriggerBuilderExtensions`)]),e(`td`,null,`Removed`),e(`td`,null,[e(`code`,null,`TriggerConfiguratorExtensions`),r(` — see `),e(`a`,{href:`#one-family-of-withxschedule-extensions`},[r(`One family of `),e(`code`,null,`WithXSchedule`),r(` extensions`)])])],-1),l[816]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Impl.AdoJobStore.SimpleSemaphore`)]),e(`td`,null,[r(`Internal, renamed `),e(`code`,null,`InProcessLockHandler`)]),e(`td`,null,[r(`It is the in-process lock the ADO.NET store falls back to when database locking is off; implement `),e(`code`,null,`ILockHandler`),r(` for a lock of your own — see `),e(`a`,{href:`#the-semaphores-are-lock-handlers`},`The semaphores are lock handlers`)])],-1),l[817]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Xml.XMLSchedulingDataProcessor`)]),e(`td`,null,[r(`Internal, respelled `),e(`code`,null,`XmlSchedulingDataProcessor`)]),e(`td`,null,[e(`code`,null,`UseXmlSchedulingConfiguration()`),r(` — the plugin `),e(`em`,null,`is`),r(` the supported entry point. The type's only constructor needed an `),e(`code`,null,`ITypeLoader`),r(`, whose every implementation is internal; `),e(`code`,null,`OverwriteExistingData = false`),r(` was reverted by any file carrying `),e(`code`,null,`<processing-directives>`),r(`; and `),e(`code`,null,`ProcessFile(fileName, systemId)`),r(` wanted an identifier from a specification Quartz no longer uses. With it goes the last public type in `),e(`code`,null,`Quartz.Xml`)])],-1),l[818]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Simpl.SimpleTypeLoadHelper`)]),e(`td`,null,[r(`Internal, renamed `),e(`code`,null,`SimpleTypeLoader`)]),e(`td`,null,[r(`Register your own `),e(`code`,null,`ITypeLoader`),r(`; a configuration string naming the old type still resolves, with a warning`)])],-1),l[819]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Plugin.Management.ShutdownHookPlugin`)]),e(`td`,null,`Removed`),e(`td`,null,[e(`code`,null,`AddQuartzHostedService(o => o.WaitForJobsToComplete = …)`),r(` under a host, or `),e(`code`,null,`scheduler.Shutdown(waitForJobsToComplete: true)`),r(` on your own exit path. `),e(`code`,null,`UseShutdownHook`),r(` and `),e(`code`,null,`ShutdownHookOptions`),r(` go with it — see `),e(`a`,{href:`#shutdownhookplugin-is-retired-the-host-already-shuts-the-scheduler-down`},[e(`code`,null,`ShutdownHookPlugin`),r(` is retired; the host already shuts the scheduler down`)])])],-1),l[820]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Impl.AdoJobStore.StdAdoConstants`)]),e(`td`,null,`Internal`),e(`td`,null,[e(`code`,null,`AdoConstants`),r(` for table, column and state names; statement text is not a contract — see `),e(`a`,{href:`#sealed-and-internalized-types`},`Sealed and Internalized Types`)])],-1),l[821]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Impl.AdoJobStore.StdRowLockSemaphore`)]),e(`td`,null,[r(`Renamed `),e(`code`,null,`SelectForUpdateLockHandler`)]),e(`td`,null,[r(`The old spelling still resolves in configuration, with a warning — see `),e(`a`,{href:`#the-semaphores-are-lock-handlers`},`The semaphores are lock handlers`)])],-1),l[822]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Impl.AdoJobStore.PostgreSQLRowLockSemaphore`)]),e(`td`,null,[r(`Renamed `),e(`code`,null,`PostgreSqlSelectForUpdateLockHandler`)]),e(`td`,null,`As above`)],-1),l[823]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Impl.AdoJobStore.UpdateLockRowSemaphore`)]),e(`td`,null,[r(`Renamed `),e(`code`,null,`UpdateRowLockHandler`)]),e(`td`,null,`As above`)],-1),l[824]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Impl.AdoJobStore.UpdateLockRowSemaphoreMOT`)]),e(`td`,null,[r(`Renamed `),e(`code`,null,`SqlServerMemoryOptimizedUpdateRowLockHandler`)]),e(`td`,null,`As above`)],-1),l[825]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Impl.StdJobRunShellFactory`)]),e(`td`,null,`Internal`),e(`td`,null,[r(`No replacement; see `),e(`code`,null,`IJobRunShellFactory`),r(` above`)])],-1),l[826]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Impl.StdScheduler`)]),e(`td`,null,`Internal`),e(`td`,null,[r(`Resolve `),e(`code`,null,`IScheduler`),r(` — see `),e(`a`,{href:`#sealed-and-internalized-types`},`Sealed and Internalized Types`)])],-1),l[827]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Impl.StdSchedulerFactory`)]),e(`td`,null,`Removed, with all 47 constants`),e(`td`,null,[e(`code`,null,`QuartzSchedulerBuilder.Create().UseProperties(properties)`),r(` — see `),e(`a`,{href:`#stdschedulerfactory-is-gone`},[e(`code`,null,`StdSchedulerFactory`),r(` is gone`)]),r(` for every constant and member`)])],-1),l[828]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.SchedulerBuilder.StoreOptions`)]),e(`td`,null,`Removed`),e(`td`,null,[r(`Nothing; it was the base of the two store option classes, which are now `),e(`code`,null,`InMemoryJobStoreOptions`),r(` and `),e(`code`,null,`IPersistentStoreBuilder`)])],-1),l[829]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Util.StringExtensions`)]),e(`td`,null,`Internal`),e(`td`,null,`No replacement`)],-1),l[830]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Simpl.SystemPropertyInstanceIdGenerator`)]),e(`td`,null,`Internal`),e(`td`,null,[e(`code`,null,`quartz.scheduler.instanceId = SYS_PROP`),r(` still selects it; in code, register your own `),e(`code`,null,`IInstanceIdGenerator`)])],-1),l[831]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.SystemTime`)]),e(`td`,null,`Removed`),e(`td`,null,[e(`code`,null,`TimeProvider`),r(` — see `),e(`a`,{href:`#systemtime-replaced-with-timeprovider`},`SystemTime Replaced with TimeProvider`)])],-1),l[832]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.TimeOfDay`)]),e(`td`,null,`Removed`),e(`td`,null,[e(`code`,null,`TimeOnly`),r(` — see `),e(`a`,{href:`#timeofday-became-timeonly`},[e(`code`,null,`TimeOfDay`),r(` became `),e(`code`,null,`TimeOnly`)])])],-1),l[833]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Plugin.TimeZoneConverter.TimeZoneConverterPlugin`)]),e(`td`,null,`Removed`),e(`td`,null,[e(`code`,null,`UseTimeZoneConverter()`),r(`, which registers the resolver itself. The `),e(`code`,null,`Quartz.Plugins.TimeZoneConverter`),r(` package is unchanged — see `),e(`a`,{href:`#timezoneconverterplugin-is-a-resolver-registration`},[e(`code`,null,`TimeZoneConverterPlugin`),r(` is a resolver registration`)])])],-1),l[834]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Util.TimeZoneUtil`)]),e(`td`,null,[r(`Renamed `),e(`code`,null,`Quartz.TimeZones`)]),e(`td`,null,[e(`code`,null,`FindTimeZoneById`),r(` is `),e(`code`,null,`FindById`),r(`; `),e(`code`,null,`CustomResolver`),r(` is `),e(`code`,null,`AddResolver(...)`),r(`, whose `),e(`code`,null,`IDisposable`),r(` undoes the registration; the Mono-era `),e(`code`,null,`ConvertTime`),r(` / `),e(`code`,null,`GetUtcOffset(DateTimeOffset, TimeZoneInfo)`),r(` shims went internal, while the wall-clock `),e(`code`,null,`GetUtcOffset(DateTime, TimeZoneInfo)`),r(` stays public — see `),e(`a`,{href:`#timezoneutil-became-quartz-timezones`},[e(`code`,null,`TimeZoneUtil`),r(` became `),e(`code`,null,`Quartz.TimeZones`)])])],-1),l[835]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.TriggerExtensions`)]),e(`td`,null,`Removed`),e(`td`,null,[e(`code`,null,`TriggerConfiguratorExtensions`),r(` — see `),e(`a`,{href:`#one-family-of-withxschedule-extensions`},[r(`One family of `),e(`code`,null,`WithXSchedule`),r(` extensions`)])])],-1),l[836]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Impl.AdoJobStore.TriggerStatus`)]),e(`td`,null,`Removed`),e(`td`,null,[e(`code`,null,`StoredTriggerHeader`),r(`, returned by `),e(`code`,null,`IDriverDelegate.SelectTriggerHeader`),r(` — see `),e(`a`,{href:`#the-driver-delegate-speaks-in-records`},`The driver delegate speaks in records`)])],-1),l[837]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.TriggerTimeComparator`)]),e(`td`,null,`Internal`),e(`td`,null,`No replacement; it ordered by next fire time, then priority descending, then key — write that inline if you need it`)],-1),l[838]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.TriggerUtils`)]),e(`td`,null,[r(`Renamed `),e(`code`,null,`Quartz.Extensibility.TriggerFireTimes`)]),e(`td`,null,[e(`code`,null,`ComputeFireTimes`),r(` is `),e(`code`,null,`Compute`),r(`, `),e(`code`,null,`ComputeFireTimesBetween`),r(` is `),e(`code`,null,`ComputeBetween`),r(` and `),e(`code`,null,`ComputeEndTimeToAllowParticularNumberOfFirings`),r(` is `),e(`code`,null,`ComputeEndTimeForCount`),r(`, with parameters and behavior unchanged — see `),e(`a`,{href:`#triggerutils-became-triggerfiretimes`},[e(`code`,null,`TriggerUtils`),r(` became `),e(`code`,null,`TriggerFireTimes`)])])],-1),l[839]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.Simpl.TriggerWrapper`)]),e(`td`,null,`Internal`),e(`td`,null,[r(`No replacement; it is `),e(`code`,null,`RAMJobStore`),r(`'s per-trigger state — see `),e(`a`,{href:`#ramjobstore-is-sealed`},[e(`code`,null,`RAMJobStore`),r(` is sealed`)])])],-1),l[840]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.UnableToInterruptJobException`)]),e(`td`,null,`Removed`),e(`td`,null,[r(`Nothing throws it: interruption is cancellation, and every job receives the token — see `),e(`a`,{href:`#unabletointerruptjobexception-is-gone`},[e(`code`,null,`UnableToInterruptJobException`),r(` is gone`)])])],-1),l[841]||=e(`tr`,null,[e(`td`,null,[e(`code`,null,`Quartz.XmlSchedulingOptions`)]),e(`td`,null,[r(`Merged into `),e(`code`,null,`FileSchedulingOptions`)]),e(`td`,null,[r(`See `),e(`a`,{href:`#other-breaking-changes`},`Other Breaking Changes`)])],-1)])]),l[899]||=t(`<h3 id="members-that-were-removed" tabindex="-1"><a class="header-anchor" href="#members-that-were-removed"><span>Members that were removed</span></a></h3><p>A member whose own type is listed above is not repeated here, and neither is one that went with a sealing — a type that became <code>sealed</code> took its <code>protected</code> surface with it. What is left is the removals on types that are still public and still open, which no section above names.</p><table><thead><tr><th>3.x member</th><th>What happened</th><th>What to use instead</th></tr></thead><tbody><tr><td><code>AbstractTrigger.CompareTo(ITrigger)</code></td><td>Removed; neither <code>TriggerBase</code> nor <code>ITrigger</code> itself implements <code>IComparable&lt;ITrigger&gt;</code> any more</td><td>It compared keys — <code>trigger.Key.CompareTo(other.Key)</code>, or <code>triggers.OrderBy(t =&gt; t.Key)</code>, both of which sort properly now that the key types implement <code>IComparable&lt;JobKey&gt;</code> / <code>IComparable&lt;TriggerKey&gt;</code>. <code>List&lt;ITrigger&gt;.Sort()</code> and friends still compile and now throw — see <a href="#the-trigger-family-interfaces-are-read-models">The trigger family interfaces are read models</a></td></tr><tr><td><code>AbstractTrigger.FullJobName</code></td><td>Removed</td><td><code>JobKey.ToString()</code>, alongside the rest in <a href="#triggerbase-property-removals">TriggerBase Property Removals</a></td></tr><tr><td><code>AdoConstants.AliasColumnNextFireTime</code></td><td>Removed</td><td>No replacement, and none is needed: it named a column alias (<code>ALIAS_NXT_FR_TM</code>) that no 4.x statement uses and the schema has never had a column for. It was a carryover from the Java acquisition sub-select, and a delegate that copied 3.x SQL is the only thing that could have named it</td></tr><tr><td><code>new CronExpression(expr, hashKey)</code>, <code>new CronExpression(expr, hashSeed)</code></td><td>Removed; they made <code>new CronExpression(expr, null)</code> ambiguous with the time-zone overload, so the null literal did not compile</td><td><code>CronExpression.ParseWithHash(expr, hashKey)</code> / <code>ParseWithHash(expr, hashSeed)</code> — see <a href="#the-hash-key-is-a-parse-argument-not-a-constructor-overload">The hash key is a <code>Parse</code> argument, not a constructor overload</a></td></tr><tr><td><code>CronExpression</code>&#39;s <code>protected</code> constants, fields and parse hooks, and <code>OnDeserialization</code></td><td>Gone with the type, which is <code>sealed</code> now and no longer implements <code>IDeserializationCallback</code></td><td>No replacement; the parsed sets were never a contract — see <a href="#the-parser-is-not-a-subclassing-seam">The parser is not a subclassing seam</a></td></tr><tr><td><code>CronExpression.GetExpressionSummary()</code>, <code>ICronTrigger.GetExpressionSummary()</code>, <code>CronTriggerImpl.GetExpressionSummary()</code></td><td>Removed</td><td>No replacement. It dumped the parsed field sets in an undocumented format; <code>CronExpressionString</code> is the expression, and the parsed sets were never a supported view — see <a href="#the-parser-is-not-a-subclassing-seam">The parser is not a subclassing seam</a></td></tr><tr><td><code>CronExpression.GetTimeBefore(t)</code></td><td>Renamed</td><td><code>GetPreviousValidTimeBefore(t)</code>, which reads as the pair of <code>GetNextValidTimeAfter</code> that it is</td></tr><tr><td><code>CronExpression.IsValidExpression(expr)</code></td><td>Removed</td><td><code>CronExpression.TryParse(expr, out _)</code> — see <a href="#cronexpression-parses-without-throwing-and-says-it-is-equatable"><code>CronExpression</code> parses without throwing</a></td></tr><tr><td><code>CronExpression.IsValidExpression(expr, hashKey)</code></td><td>Removed</td><td><code>CronExpression.TryParseWithHash(expr, hashKey, out _)</code></td></tr><tr><td><code>CronExpression.MaxYear</code></td><td>Removed (a <code>public static readonly int</code>)</td><td>No replacement. It was <code>DateTime.Now.Year + 100</code>, computed once per process — see <a href="#cronexpression-is-immutable"><code>CronExpression</code> is immutable</a></td></tr><tr><td><code>CronExpression.ValidateExpression(expr)</code></td><td>Removed; its whole body was a construction it discarded</td><td><code>CronExpression.Parse(expr)</code>, which hands back the expression it parsed</td></tr><tr><td><code>CronExpression.ValidateExpression(expr, hashKey)</code></td><td>Removed</td><td><code>CronExpression.ParseWithHash(expr, hashKey)</code></td></tr><tr><td><code>CronTriggerImpl.GetTimeAfter(DateTimeOffset)</code></td><td>Removed (it was <code>protected</code>)</td><td><code>GetFireTimeAfter(DateTimeOffset?)</code>, or <code>CronExpression.GetNextValidTimeAfter</code> for the expression on its own</td></tr><tr><td><code>CronTriggerImpl.GetTimeBefore(DateTimeOffset)</code></td><td>Renamed (it is <code>protected</code>)</td><td><code>GetPreviousValidTimeBefore(DateTimeOffset)</code>, renamed with the <code>CronExpression</code> member it forwards to</td></tr><tr><td><code>CronTriggerImpl.YearToGiveupSchedulingAt</code></td><td>Removed (a <code>protected const</code>)</td><td>No replacement; where the search stops is the expression&#39;s business</td></tr><tr><td><code>DateBuilder.ValidateDayOfMonth</code>, <code>.ValidateHour</code>, <code>.ValidateMinute</code>, <code>.ValidateMonth</code>, <code>.ValidateSecond</code>, <code>.ValidateYear</code></td><td>Removed</td><td>No replacement; the builder validates its own arguments, and it is <code>sealed</code> — see <a href="#datebuilder-s-static-factories-are-gone"><code>DateBuilder</code>&#39;s static factories are gone</a></td></tr><tr><td><code>DbProvider.CreateParameter()</code></td><td>Removed</td><td><code>CreateCommand().CreateParameter()</code></td></tr><tr><td><code>DbProvider.DbProviderSectionName</code>, <code>.GenerateValidProviderNamesInfo()</code></td><td>Removed (<code>protected</code>)</td><td>No replacement; leftovers of the process-wide provider registry, like the two named in <a href="#other-breaking-changes">Other Breaking Changes</a></td></tr><tr><td><code>DirtyFlagMap&lt;TKey, TValue&gt;</code>, <code>StringKeyDirtyFlagMap</code></td><td>Internal</td><td><code>JobDataMap</code> / <code>SchedulerContext</code> are self-contained; the typed accessors are extension members — see <a href="#jobdatamap-and-schedulercontext-stand-alone">JobDataMap and SchedulerContext stand alone</a></td></tr><tr><td><code>DirtyFlagMap.Clone()</code></td><td>Removed</td><td>Construct a new map from the old one</td></tr><tr><td><code>DirtyFlagMap.Dirty</code>, <code>.ClearDirtyFlag()</code></td><td>Internal</td><td><code>SchedulerConstants.ForceJobDataMapDirty</code> forces a rewrite; clearing the flag from a job silently lost data</td></tr><tr><td><code>DirtyFlagMap.EntrySet()</code></td><td>Removed</td><td><code>GetEnumerator()</code>, or <code>foreach</code> over the map</td></tr><tr><td><code>DirtyFlagMap.KeySet()</code></td><td>Removed</td><td><code>Keys</code></td></tr><tr><td><code>DirtyFlagMap.Put()</code>, <code>.PutAll()</code></td><td>Removed; both were <code>[Obsolete]</code> in 3.x</td><td><code>map[key] = value</code>, in a loop for <code>PutAll</code> — see <a href="#jobdatamap-and-schedulercontext-stand-alone">JobDataMap and SchedulerContext stand alone</a></td></tr><tr><td><code>DirtyFlagMap.WrappedMap</code></td><td>Removed</td><td>No replacement; the map <em>is</em> the dictionary, and handing out the inner one let a caller write past the dirty flag</td></tr><tr><td><code>IDriverDelegate.UpdateTriggerPreferredNode</code>, <code>StdAdoDelegate.UpdateTriggerPreferredNode</code></td><td>Removed</td><td><code>UpdateTriggerPreferredNodeConditional</code>, which is a compare-and-swap, or <code>IScheduler.UpdateTriggerDetails</code> from outside the store — see <a href="#the-preferred-node-is-a-value">The preferred node is a value</a></td></tr><tr><td><code>InvalidConfigurationException()</code></td><td>Removed; the type is <code>sealed</code> and keeps only <code>(string message)</code></td><td>Say what was invalid — an exception with no message is one nobody can act on</td></tr><tr><td><code>IListenerManager.AddJobListenerMatcher</code>, <code>.RemoveJobListenerMatcher</code>, <code>.SetJobListenerMatchers</code>, <code>.GetJobListenerMatchers</code>, and their <code>TriggerListener</code> twins</td><td>Removed</td><td><code>AddJobListener(listener, params matchers)</code> / <code>AddTriggerListener(listener, params matchers)</code>, which is registration; registering the same name again replaces the listener and its matchers together — see <a href="#matchers-are-given-at-registration">Matchers are given at registration</a></td></tr><tr><td><code>IObjectSerializer.Initialize()</code></td><td>Removed</td><td>No replacement; a serializer builds what it needs on first use — see <a href="#names-that-were-normalized">Names that were normalized</a></td></tr><tr><td><code>IScheduler.InStandbyMode</code></td><td>Removed</td><td><code>Status is SchedulerStatus.Created or SchedulerStatus.Standby or SchedulerStatus.ShuttingDown</code> — see <a href="#a-scheduler-s-lifecycle-is-one-value">A scheduler&#39;s lifecycle is one value</a></td></tr><tr><td><code>IScheduler.IsShutdown</code></td><td>Removed</td><td><code>Status is SchedulerStatus.Shutdown</code> — see <a href="#a-scheduler-s-lifecycle-is-one-value">A scheduler&#39;s lifecycle is one value</a></td></tr><tr><td><code>IScheduler.IsStarted</code></td><td>Removed</td><td><code>Status is not SchedulerStatus.Created</code> faithfully; <code>Status is SchedulerStatus.Running</code> if what you meant was &quot;is running now&quot;, which it did not say — see <a href="#a-scheduler-s-lifecycle-is-one-value">A scheduler&#39;s lifecycle is one value</a></td></tr><tr><td><code>IScheduler.JobFactory</code> (setter-only)</td><td>Removed, from <code>IScheduler</code>, <code>StdScheduler</code>, <code>DelegatingScheduler</code> and <code>HttpScheduler</code></td><td><code>IQuartzBuilder.UseJobFactory(IJobFactory)</code> or <code>UseJobFactory&lt;T&gt;()</code> where the scheduler is built; <code>ConfigureJobScope(...)</code> when all it did was seed the DI scope. A factory whose dependency does not exist yet resolves it in <code>CreateJob</code> — see <a href="#the-factory-is-set-where-the-scheduler-is-built">The factory is set where the scheduler is built</a></td></tr><tr><td><code>IScheduler.PauseJobs(GroupMatcher&lt;JobKey&gt;)</code>, <code>.ResumeJobs(GroupMatcher&lt;JobKey&gt;)</code>, <code>.PauseTriggers(GroupMatcher&lt;TriggerKey&gt;)</code>, <code>.ResumeTriggers(GroupMatcher&lt;TriggerKey&gt;)</code>, and the same four on <code>IJobStore</code></td><td>Renamed</td><td><code>PauseJobGroups</code>, <code>ResumeJobGroups</code>, <code>PauseTriggerGroups</code>, <code>ResumeTriggerGroups</code>. The key-set overloads keep the old names, so each name now has one return shape — see <a href="#pausing-by-matcher-is-a-group-operation-and-is-named-for-one">Pausing by matcher is a group operation</a></td></tr><tr><td><code>JobBuilder.CreateForAsync&lt;T&gt;()</code></td><td>Removed</td><td><code>JobBuilder.Create&lt;T&gt;()</code>; every job has been asynchronous since 3.0</td></tr><tr><td><code>JobDataMap.GetCharFromString</code>, <code>.GetDateTimeValue</code>, <code>.GetGuidValue</code>, <code>.GetTimeSpanValue</code> and their <code>…FromString</code> twins</td><td>Removed with the rest of the <code>…Value</code> accessors</td><td><code>Get&lt;char&gt;</code>, <code>Get&lt;DateTime&gt;</code>, <code>Get&lt;Guid&gt;</code>, <code>Get&lt;TimeSpan&gt;</code> — the generic accessor coerces exactly as a named one would — see <a href="#one-accessor-per-type-for-the-types-job-data-is-made-of">One accessor per type</a></td></tr><tr><td><code>JobStoreSupport.calendarCache</code>, <code>.delegateType</code>, <code>.firstCheckIn</code></td><td>Removed (<code>protected</code> fields)</td><td>No replacement; they are the base class&#39;s own bookkeeping</td></tr><tr><td><code>JobStoreSupport.GetTriggerNames(conn, matcher, ct)</code></td><td>Removed (<code>protected</code>)</td><td>The listing members became queries — see <a href="#job-store-listings-became-queries">Job store listings became queries</a></td></tr><tr><td><code>LogProvider.IsDisabled</code></td><td>Removed</td><td>No replacement; filter through the <code>ILoggerFactory</code> — see <a href="#logging">Logging</a></td></tr><tr><td><code>LogProvider.SetCurrentLogProvider(ILogProvider)</code></td><td>Removed with LibLog</td><td><code>LogProvider.SetLogProvider(ILoggerFactory)</code> — see <a href="#logging">Logging</a></td></tr><tr><td><code>QuartzDashboardOptions.ApiPath</code></td><td>Removed</td><td>No replacement; it addressed the HTTP API the dashboard&#39;s remote client called, and that client is gone — see <a href="#the-dashboard-reads-the-schedulers-in-its-own-process">The dashboard reads the schedulers in its own process</a>. <code>QuartzHttpApiOptions.ApiPath</code>, which is where the API is served, is a different option and unchanged</td></tr><tr><td><code>SchedulerMetadata.Started</code>, <code>.InStandbyMode</code>, <code>.Shutdown</code></td><td>Removed</td><td><code>Status</code>, a <code>required SchedulerStatus</code> — see <a href="#a-scheduler-s-lifecycle-is-one-value">A scheduler&#39;s lifecycle is one value</a></td></tr><tr><td><code>SimplePropertiesTriggerPersistenceDelegateSupport.SchedNameLiteral</code>, and the same member on <code>DbLockHandler</code></td><td>Removed; both were <code>[Obsolete]</code> in 3.x</td><td>No replacement; the scheduler name is a SQL parameter, not literal text</td></tr><tr><td><code>StdAdoDelegate.GetStorableJobTypeName(Type)</code></td><td>Removed (<code>protected</code>)</td><td><code>new JobType(type).FullName</code>, which is the spelling the <code>JOB_CLASS_NAME</code> column holds</td></tr><tr><td><code>StdAdoDelegate.SchedulerNameLiteral</code></td><td>Removed; it was <code>[Obsolete]</code> in 3.x</td><td>No replacement; as above</td></tr><tr><td><code>StringKeyDirtyFlagMap.GetKeys()</code></td><td>Removed</td><td><code>Keys</code></td></tr><tr><td><code>StringKeyDirtyFlagMap.GetNullableGuid()</code>, <code>.TryGetNullableGuid()</code></td><td>Removed</td><td><code>TryGetGuid(key, out var value)</code>, whose <code>false</code> says the same thing as a <code>null</code> did — see <a href="#jobdatamap-s-typed-accessors-are-extension-members"><code>JobDataMap</code>&#39;s typed accessors are extension members</a></td></tr><tr><td><code>StringKeyDirtyFlagMap.Put()</code> (eight overloads), <code>.PutAll()</code></td><td>Removed; all were <code>[Obsolete]</code> in 3.x</td><td><code>map[key] = value</code></td></tr><tr><td><code>TaskSchedulingThreadPool.ThreadPriority</code></td><td>Removed</td><td>No replacement; work runs on a <code>TaskScheduler</code>, which has no thread to prioritise — see <a href="#the-thread-pool-is-asynchronous">The thread pool is asynchronous</a></td></tr><tr><td><code>ZeroSizeThreadPool.AvailableThreadCount</code></td><td>Removed</td><td><code>PoolSize</code>, which is <code>0</code> — the pool never had a thread to report</td></tr></tbody></table><h3 id="types-4-0-added-to-the-quartz-namespace" tabindex="-1"><a class="header-anchor" href="#types-4-0-added-to-the-quartz-namespace"><span>Types 4.0 added to the <code>Quartz</code> namespace</span></a></h3><p>The two tables above answer &quot;where did my type go&quot;. This one answers the opposite question, which a build only asks once and then asks loudly: <strong>CS0104, &quot;ambiguous reference&quot;</strong>, because a name Quartz now declares collides with one of yours under <code>using Quartz;</code>. Three packages folded into <code>Quartz</code> and the namespace absorbed the matchers, <code>Key&lt;T&gt;</code> and the options types, so the namespace is much larger than it was — and the errors it produces name the right members on the wrong type, which reads like your own code having gone wrong.</p><p>Derived the same mechanical way as the tables above: types in the 4.x <code>Quartz</code> namespace that are in the 3.x <code>Quartz</code> namespace of none of <code>Quartz</code>, <code>Quartz.Extensions.DependencyInjection</code> or <code>Quartz.Extensions.Hosting</code>. <strong>Ninety-nine names.</strong> A project that also referenced <code>Quartz.Serialization.SystemTextJson</code> on 3.x already had <code>JsonSerializationException</code>.</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text"><pre><code class="language-text"><span class="line">AddCalendarOptions                      AddJobOptions                      AddTriggerOptions</span>
<span class="line">AdoJobStoreOptions                      AndMatcher&lt;T&gt;                      CalendarIntervalTriggerMisfireInstruction</span>
<span class="line">CalendarQuery                           ClusterNode                        ClusterNodeState</span>
<span class="line">ClusteringOptions                       CronFormat                         CronTriggerMisfireInstruction</span>
<span class="line">DailyTimeIntervalTriggerMisfireInstruction                                 DataMapExtensions</span>
<span class="line">DataSourceOptions                       EverythingMatcher&lt;T&gt;               ExecutionGroupInFlight</span>
<span class="line">ExecutionGroupLimit                     ExecutionGroupScope                ExecutionLimitScope</span>
<span class="line">ExecutionLimitsBuilder                  ExecutionSlots                     FireInstance</span>
<span class="line">FireInstanceQuery                       FireInstanceState                  GroupMatcher&lt;T&gt;</span>
<span class="line">IJob&lt;T&gt;                                 IJobConfigurator&lt;T&gt;                IJobExecutionContextAccessor</span>
<span class="line">IJobExecutionMiddleware                 IPersistentStoreBuilder            IQuartzBuilder</span>
<span class="line">ISchedulerRegistry                      ITriggerConfigurator&lt;T&gt;            InMemoryJobStoreOptions</span>
<span class="line">JobBuilder&lt;T&gt;                           JobDetailExtensions                JobExecutionContextInputExtensions</span>
<span class="line">JobExecutionDelegate                    JobExecutionProcessException       JobGroup</span>
<span class="line">JobGroupQuery                           JobHeader                          JobInputBuilderExtensions</span>
<span class="line">JobInstantiationException               JobQuery                           JobTimeoutAttribute</span>
<span class="line">JobType                                 JsonSerializationException         Key&lt;T&gt;</span>
<span class="line">KeyMatcher&lt;T&gt;                           Matchers                           MonthDay</span>
<span class="line">NameMatcher                             NameMatcher&lt;T&gt;                     NotMatcher&lt;T&gt;</span>
<span class="line">OneOffJobOptions                        OrMatcher&lt;T&gt;                       PagedQuery</span>
<span class="line">PagedResult&lt;T&gt;                          PersistentStoreBuilderExtensions   PreferredNode</span>
<span class="line">QuartzBuilderExtensions                 QuartzHealthCheckExtensions        QuartzHealthCheckOptions</span>
<span class="line">QuartzHostApplicationBuilderExtensions  QuartzSchedulerBuilder             QuartzSchedulerOptions</span>
<span class="line">RecurrenceTriggerMisfireInstruction     RetryPolicy                        ScheduleJobOptions</span>
<span class="line">ScheduledOneOffJob                      SchedulerErrorContext              SchedulerFactoryExtensions</span>
<span class="line">SchedulerJobExtensions                  SchedulerMetadata                  SchedulerNotFoundException</span>
<span class="line">SchedulerOrigin                         SchedulerQueryExtensions           SchedulerRegistration</span>
<span class="line">SchedulerStatus                         SchedulingDataValidationException  SchemaProvisioning</span>
<span class="line">ShutdownJobInterruption                 SimpleTriggerMisfireInstruction    StandaloneSchedulerFactory</span>
<span class="line">StringMatcher&lt;T&gt;                        StringOperator                     SystemTextJsonConfigurationExtensions</span>
<span class="line">ThreadPoolOptions                       TimeRange                          TimeZones</span>
<span class="line">TriggerBuilder&lt;T&gt;                       TriggerConfiguratorExtensions      TriggerGroup</span>
<span class="line">TriggerGroupQuery                       TriggerHeader                      TriggerQuery</span>
<span class="line">TypeLoaderOptions</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Most of those are unmistakably Quartz&#39;s. These are the ones a host application or an integration library plausibly declares itself, which makes them the ones to check first:</p><table><thead><tr><th>Name</th><th>Who else has one</th></tr></thead><tbody><tr><td><code>QuartzSchedulerOptions</code></td><td>the known case — it shadowed MassTransit&#39;s own <code>QuartzSchedulerOptions</code>, producing six compile errors that named the right members on the wrong type. 3.x&#39;s equivalent was <code>Quartz.Core.QuartzSchedulerResources</code>, safely out of the way</td></tr><tr><td><code>JsonSerializationException</code></td><td><strong>Newtonsoft.Json declares this exact name</strong>, so a file with both <code>using Quartz;</code> and <code>using Newtonsoft.Json;</code> is ambiguous</td></tr><tr><td><code>RetryPolicy</code></td><td>Polly, MassTransit, the Azure SDKs, and most in-house resilience code</td></tr><tr><td><code>TimeRange</code>, <code>MonthDay</code></td><td>ordinary domain vocabulary; NodaTime has <code>MonthDay</code></td></tr><tr><td><code>PagedResult&lt;T&gt;</code>, <code>PagedQuery</code></td><td>near-universal in an API or repository layer</td></tr><tr><td><code>Key&lt;T&gt;</code></td><td>was <code>Quartz.Util.Key&lt;T&gt;</code>; now a one-word name at the top level</td></tr><tr><td><code>Matchers</code>, <code>TimeZones</code></td><td>one-word static helpers, previously <code>Quartz.Impl.Matchers</code> and <code>Quartz.Util.TimeZoneUtil</code></td></tr><tr><td><code>ThreadPoolOptions</code>, <code>DataSourceOptions</code></td><td>common options-class names</td></tr><tr><td><code>JobType</code>, <code>SchedulerStatus</code>, <code>ClusterNode</code>, <code>ClusterNodeState</code></td><td>anything with a job or scheduler model of its own</td></tr><tr><td><code>StringOperator</code>, <code>ExecutionSlots</code>, <code>ExecutionGroupScope</code>, <code>ExecutionLimitScope</code></td><td>generic concurrency and matching vocabulary</td></tr></tbody></table><p>The fix is a using-alias in the affected file, which beats a namespace import and needs no change to either library:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token keyword">using</span> <span class="token class-name">QuartzSchedulerOptions</span> <span class="token operator">=</span> <span class="token class-name">Acme<span class="token punctuation">.</span>Bus<span class="token punctuation">.</span>QuartzSchedulerOptions</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>One collision went away: 3.x&#39;s <code>Quartz.ServiceCollectionExtensions</code> — the most common helper-class name in .NET — is not in the <code>Quartz</code> namespace on 4.x. Its members live on <code>QuartzServiceCollectionExtensions</code> and <code>QuartzBuilderExtensions</code>, and since both are extension methods, only a static-form call has to change.</p><h2 id="appendix-if-you-ran-a-4-0-pre-release" tabindex="-1"><a class="header-anchor" href="#appendix-if-you-ran-a-4-0-pre-release"><span>Appendix: if you ran a 4.0 pre-release</span></a></h2><p>Nothing here concerns an application coming from 3.x — every one of these corrections is described above as a plain statement about 4.0, which is what 4.0.0 ships. This is the other reading of the same list: which pre-release each correction landed in, so that an application already running one of them knows what moved under it.</p><p>The 4.0 public surface froze at <code>4.0.0-alpha.5</code>. Everything after that row is either an exception taken deliberately — because the API was making a statement that was not true — or a new refusal, where no shape changed and only what a mis-stated configuration does is different.</p><details class="hint-container details"><summary>The build-by-build list</summary><table><thead><tr><th>Build</th><th>What changed</th><th>Where the 4.0 shape is described</th></tr></thead><tbody><tr><td>alpha.2</td><td>Every listener callback gained its <code>IScheduler</code> parameter. A listener written against alpha.1 is refused as it is registered</td><td><a href="#listeners-are-told-which-scheduler-is-calling">Listeners are told which scheduler is calling</a></td></tr><tr><td>alpha.3</td><td>The dashboard&#39;s history and live events gained the instance id of the node they came from</td><td><a href="#history-and-live-events-say-which-node-they-came-from">History and live events say which node they came from</a></td></tr><tr><td>alpha.4</td><td>The health check moved from <code>Quartz.AspNetCore</code> to <code>Quartz</code>. <code>QuartzHealthCheckOptions</code> moved with it; the four extension methods moved from <code>QuartzAspNetCoreConfigurationExtensions</code> to <code>QuartzHealthCheckExtensions</code>, and <code>QuartzHealthCheck</code> / <code>SchedulerHealthCheckTarget</code> from internal-to-<code>Quartz.AspNetCore</code> to internal-to-<code>Quartz</code>. <strong>No call site changes</strong> — the namespace is <code>Quartz</code> on both sides and all four are extension methods — only the package reference</td><td><a href="#the-health-check-is-in-quartz-not-quartz-aspnetcore">The health check is in <code>Quartz</code>, not <code>Quartz.AspNetCore</code></a></td></tr><tr><td>alpha.4</td><td>An expression with a wildcard in one day field and values in the other fired on every day of the month; it fires only on the days the restricted field names</td><td><a href="#cron-parser-enhancements">Cron Parser Enhancements</a></td></tr><tr><td>alpha.5</td><td><code>QuartzSchedulerBuilder</code> stopped re-declaring every <code>IQuartzBuilder</code> member with a covariant return — roughly sixty signatures. Whatever was chained between <code>Create()</code> and the terminal call moves into <code>Create(q =&gt; q…)</code>; <code>Services</code> and <code>SchedulerName</code> are read off the callback&#39;s argument</td><td><a href="#the-standalone-builder-is-the-same-builder">The standalone builder is the same builder</a></td></tr><tr><td>alpha.5</td><td><code>TriggerMisfired</code> took its scheduler in <strong>first</strong> position and now takes it after the trigger. A listener that keeps the old order compiles and is refused at registration</td><td><a href="#triggermisfired-takes-the-trigger-first"><code>TriggerMisfired</code> takes the trigger first</a></td></tr><tr><td>beta.1</td><td><code>IScheduler</code>&#39;s mutation members raise <code>ArgumentNullException</code> where they raised <code>SchedulerException(&quot;JobDetail cannot be null&quot;)</code></td><td><a href="#null-arguments-raise-argumentnullexception">Null arguments raise <code>ArgumentNullException</code></a></td></tr><tr><td>beta.1</td><td><code>CronExpression.TryParse(s, format, out)</code> answers <code>false</code> for an unknown <code>CronFormat</code> instead of throwing; <code>new CronExpression(null)</code> and <code>CronScheduleBuilder.Create(null)</code> raise <code>ArgumentNullException</code> rather than <code>ArgumentException</code></td><td><a href="#a-bad-argument-rather-than-a-bad-expression">A bad argument, rather than a bad expression</a></td></tr><tr><td>beta.1</td><td><code>IOperableTrigger.FireInstanceId</code>, <code>TriggerBase.FireInstanceId</code> and <code>DbMetadata.ParameterDbTypePropertyName</code> are <code>string?</code>. All three were non-nullable <code>string</code> initialised to <code>null!</code>, so a store, trigger or driver description of your own may now need a <code>!</code> or a null check</td><td><a href="#triggerbase-property-removals">TriggerBase Property Removals</a>, <a href="#other-breaking-changes">Other Breaking Changes</a></td></tr><tr><td>beta.1</td><td>A <code>500</code> from the HTTP API stopped returning the exception&#39;s message. A client matching on a <code>500</code>&#39;s <code>detail</code> was matching on whichever driver failed</td><td><a href="#a-500-says-nothing-about-the-server">A <code>500</code> says nothing about the server</a></td></tr><tr><td>beta.1</td><td>A paged HTTP API request is bounded by <code>QuartzHttpApiOptions.MaxPageSize</code>, which is <code>1000</code>; <code>take</code> used to reject only negatives. Set it to <code>0</code> for the old behaviour</td><td><a href="#a-paged-request-is-bounded-by-maxpagesize">A paged request is bounded by <code>MaxPageSize</code></a></td></tr><tr><td>beta.1</td><td><code>MapQuartzHttpApi()</code> and <code>MapQuartzDashboard()</code> refuse to start when nothing authorizes them. <strong>An application that mapped a bare API or dashboard has to add one call</strong>, and <code>AllowAnonymous()</code> is a supported answer</td><td><a href="#the-http-api-and-the-dashboard-will-not-serve-anonymously-by-accident">The HTTP API and the dashboard will not serve anonymously by accident</a></td></tr><tr><td>beta.1</td><td><code>MapQuartzDashboard()</code> returns <code>IEndpointConventionBuilder</code> rather than <code>RazorComponentsEndpointConventionBuilder</code>, so one <code>RequireAuthorization()</code> covers the pages <em>and</em> the hub</td><td><a href="#one-builder-covers-the-dashboard-s-pages-and-its-hub">One builder covers the dashboard&#39;s pages and its hub</a></td></tr><tr><td>beta.1</td><td>An ADO store refuses a <code>SimpleTrigger</code> repeat interval it cannot hold to the millisecond, instead of storing <code>0</code> and leaving the row stuck in <code>ACQUIRED</code></td><td><a href="#the-two-job-stores-answer-the-same-way">The two job stores answer the same way</a></td></tr><tr><td>beta.1</td><td><code>ILockHandler.Shutdown</code> was added as a default interface member. An existing handler need not implement it</td><td><a href="#a-lock-handler-is-told-when-to-close-what-it-opened">A lock handler is told when to close what it opened</a></td></tr><tr><td>beta.1</td><td><code>DelegatingScheduler</code> declares <code>ResetTriggersFromErrorState(GroupMatcher&lt;TriggerKey&gt;, …)</code>, which it had been letting the interface default answer on the forwarder</td><td><a href="#delegatingjobstore-decorates-a-store"><code>DelegatingJobStore</code> decorates a store</a></td></tr><tr><td>beta.1</td><td><code>TriggerFireTimes</code>&#39;s three members gained <code>ITrigger</code> overloads, so the <code>IOperableTrigger</code> cast is no longer needed</td><td><a href="#triggerutils-became-triggerfiretimes"><code>TriggerUtils</code> became <code>TriggerFireTimes</code></a></td></tr><tr><td>beta.1</td><td><code>UseXmlSchedulingConfiguration</code> / <code>UseJsonSchedulingConfiguration</code> gained a <code>params string[]</code> overload; <code>QuartzHealthCheckOptions.StandbyStatus</code> and the nine <code>QuartzInstrumentation.Instruments</code> constants were added. All additive</td><td><a href="#plugins-are-registered-like-listeners">Plugins are registered like listeners</a>, <a href="#old-and-new-telemetry-names">Old and new telemetry names</a></td></tr><tr><td>rc.1</td><td><code>Quartz.Dashboard.Services.JobDetailDto</code>&#39;s <code>ConcurrentExecutionDisallowed</code> and <code>PersistJobDataAfterExecution</code> are <code>bool?</code>, as are the same two fields in every HTTP API job body. Present means stated, absent means &quot;whatever the type says&quot; — a request that omitted the field used to store a <code>[DisallowConcurrentExecution]</code> job as safe to run concurrently</td><td><a href="#a-job-s-two-attribute-flags-are-nullable-on-the-wire">A job&#39;s two attribute flags are nullable on the wire</a></td></tr><tr><td>rc.1</td><td>A job type name that arrived over the wire is never resolved while a DTO is converted, on either side. A client can schedule a job whose type only the server has, and a job whose type resolves nowhere reads as <code>200</code> with both flags absent rather than a permanent <code>500</code></td><td><a href="#a-job-type-name-is-never-resolved-by-the-contract-types">A job type name is never resolved by the contract types</a></td></tr><tr><td>rc.1</td><td>A job type named by a string is checked against <code>IJob</code> before anything constructs it. A firing of a type that is not one fails with a <code>JobInstantiationException</code> naming it, where it used to be an <code>InvalidCastException</code> after the constructor had run</td><td><a href="#a-named-type-is-checked-against-ijob-before-it-is-constructed">A named type is checked against <code>IJob</code> before it is constructed</a></td></tr><tr><td>rc.1</td><td><code>Scheduling.IgnoreDuplicates</code> on its own turns <code>OverwriteExistingData</code> off, rather than being ignored (3.x) or refused (beta.1). Writing both down is still refused</td><td><a href="#ignoreduplicates-on-its-own-turns-overwriting-off"><code>IgnoreDuplicates</code> on its own turns overwriting off</a></td></tr><tr><td>rc.1</td><td><code>IScheduler</code>&#39;s <strong>read</strong> members raise <code>ArgumentNullException</code> too, which beta.1 left alone; <code>CronExpression.ResolveHash</code> raises it where it raised <code>ArgumentException</code></td><td><a href="#null-arguments-raise-argumentnullexception">Null arguments raise <code>ArgumentNullException</code></a></td></tr><tr><td>rc.1</td><td><code>IScheduler.Interrupt(jobKey)</code> raises <code>JobInterrupted</code> once per firing it cancelled, where it raised one notification however many it stopped. The new overload naming the fire instance is a default interface member, so an existing listener keeps working</td><td><a href="#jobinterrupted-says-which-firing-was-interrupted"><code>JobInterrupted</code> says which firing was interrupted</a></td></tr><tr><td>rc.1</td><td><code>SendMailJob</code> refuses to send when the registered credential answers for every host — a bare <code>NetworkCredential</code> — and the host comes from job data. Register a <code>CredentialCache</code> bound to the server. <code>SendMailOptions.EnableSsl</code> is new and off by default</td><td><a href="#the-smtp-password-does-not-belong-in-job-data">The SMTP password does not belong in job data</a></td></tr><tr><td>rc.1</td><td><code>DirectoryScanJob</code> finds its listener among what the application registered rather than by sweeping every loaded assembly. <code>AddSingleton&lt;InboxListener&gt;()</code> alone no longer resolves: register it as <code>IDirectoryScanListener</code>, key it, or put it in the <code>SchedulerContext</code></td><td><a href="#directoryscanjob-finds-its-listener-among-what-you-registered"><code>DirectoryScanJob</code> finds its listener among what you registered</a></td></tr><tr><td>rc.1</td><td><code>DirectoryScanJob</code> stores its previous scan as a <code>Dictionary&lt;string, string&gt;</code> rather than a <code>List&lt;FileInfo&gt;</code>, which no serializer could write. Its first firing against a persistent store used to fail to persist</td><td><a href="#directoryscanjob-stores-its-file-list-as-something-a-job-store-can-write"><code>DirectoryScanJob</code> stores its file list as something a job store can write</a></td></tr><tr><td>rc.1</td><td><code>NativeJob</code> redirects the child&#39;s streams only when <code>ConsumeStreams</code> is on, and waits with <code>WaitForExitAsync</code>. A process writing more than a pipe buffer holds used to block for ever with the defaults</td><td><a href="#nativejob-no-longer-redirects-a-stream-nobody-reads"><code>NativeJob</code> no longer redirects a stream nobody reads</a></td></tr><tr><td>rc.1</td><td>The health check reports <code>Degraded</code> for a <strong>clustered</strong> node whose last check-in is older than <code>QuartzHealthCheckOptions.ClusterCheckinTolerance</code> (<code>3</code>) times its own interval. <code>null</code> or <code>0</code> makes no such query, and an unclustered scheduler is not asked</td><td><a href="#the-health-check-reports-the-state-it-found">The health check reports the state it found</a></td></tr><tr><td>rc.1</td><td>Each scheduling path logs under its own category — a scheduling file, <code>AddQuartz</code>, and the JSON plugin — where all three logged as <code>Quartz.Xml.XmlSchedulingDataProcessor</code>. The event ids are unchanged</td><td><a href="#each-scheduling-path-logs-under-its-own-category">Each scheduling path logs under its own category</a></td></tr><tr><td>rc.1</td><td>The log catalogue moved: <code>7010</code> and <code>7011</code> are gone with the assembly sweep that raised them, and <code>1020</code> (a shutdown abandoning running work), <code>1021</code> (something subscribed where 3.x published telemetry), <code>3156</code> (a schema that was already complete) and <code>9100</code>–<code>9103</code> (the dashboard&#39;s own events) are new</td><td><a href="#every-message-carries-an-event-id">Every message carries an event id</a></td></tr><tr><td>rc.1</td><td>Schema validation probes the columns the 4.0 migration adds as well as the tables, and <code>CreateIfMissing</code> refuses a schema 4.x did not create rather than building around it. <strong>A 4.x node against an unmigrated 3.x schema is now refused at startup</strong> where <code>ProvisionSchema()</code> used to start it and fire nothing; both messages name <code>database/migrations/4.0/</code></td><td><a href="#database-schema-migration">Database Schema Migration</a></td></tr><tr><td>rc.1</td><td>A paused <strong>job</strong> group binds what is added to it on the ADO store, as it always did in memory: a trigger stored for a job in a recorded-paused group is born <code>PAUSED</code>. A pre-release that paused a job group and then deployed a job into it ran that job</td><td><a href="#the-two-job-stores-answer-the-same-way">The two job stores answer the same way</a></td></tr><tr><td>rc.1</td><td>The 3.x-to-4.0 migration is <strong>two files</strong>: <code>schema_30_to_40_upgrade_&lt;database&gt;.sql</code> is the mandatory half and is safe during a mixed window, and the index set moved out of it into <code>schema_30_to_40_indexes_&lt;database&gt;.sql</code>, which is the half that waits for the last 3.x node. Section 6 of the old single file <em>is</em> that new file</td><td><a href="#database-schema-migration">Database Schema Migration</a></td></tr><tr><td>rc.1</td><td>The schema no longer creates <code>IDX_QRTZ_T_NFT_ST_MISFIRE</code>, and the 3.x-to-4.0 migration drops it. <strong>Run <code>schema_30_to_40_indexes_&lt;database&gt;.sql</code></strong> — section 6 of the old single file — on a schema built from an earlier pre-release; nothing fails if you do not, the index is only maintained for nothing</td><td><a href="#the-misfire-index-is-dropped-optional">The misfire index is dropped</a></td></tr><tr><td>rc.2</td><td>The persistent store no longer resolves a job&#39;s class on <code>RescheduleJob</code> or <code>UpdateTriggerDetails</code>, and answers both attribute flags from <code>IS_NONCONCURRENT</code> / <code>IS_UPDATE_DATA</code> rather than from the type. A process that cannot load its job classes needs no <code>ITypeLoader</code> of its own, and no placeholder type</td><td><a href="#a-job-type-name-is-never-resolved-by-the-contract-types">A job type name is never resolved by the contract types</a></td></tr><tr><td>rc.2</td><td>Documented; nothing changed in the parser. An expression carried over from a crontab-derived .NET library parses on 4.0 where 3.x refused it, and two shapes — a numeric day-of-week, and both day fields restricted — fire on a different day</td><td><a href="#if-your-expressions-came-from-another-cron-library">If your expressions came from another cron library</a></td></tr></tbody></table></details><h3 id="names-a-pre-release-had-and-4-0-does-not" tabindex="-1"><a class="header-anchor" href="#names-a-pre-release-had-and-4-0-does-not"><span>Names a pre-release had and 4.0 does not</span></a></h3><p>The lock-handler family was renamed twice: once from the Java-derived 3.x spellings, and once again before 4.0.0. A <code>quartz.jobStore.lockHandler.type</code> naming any generation still resolves, with a warning.</p><table><thead><tr><th>A 4.0 pre-release called it</th><th>4.0</th></tr></thead><tbody><tr><td><code>ISemaphore</code></td><td><code>ILockHandler</code></td></tr><tr><td><code>DbSemaphore</code></td><td><code>DbLockHandler</code></td></tr><tr><td><code>SelectForUpdateSemaphore</code></td><td><code>SelectForUpdateLockHandler</code></td></tr><tr><td><code>PostgreSqlSelectForUpdateSemaphore</code></td><td><code>PostgreSqlSelectForUpdateLockHandler</code></td></tr><tr><td><code>UpdateRowSemaphore</code></td><td><code>UpdateRowLockHandler</code></td></tr><tr><td><code>SqlServerMemoryOptimizedUpdateRowSemaphore</code></td><td><code>SqlServerMemoryOptimizedUpdateRowLockHandler</code></td></tr><tr><td><code>SimpleSemaphore</code> (internal)</td><td><code>InProcessLockHandler</code> (internal)</td></tr><tr><td><code>SQLiteSemaphore</code> (internal)</td><td><code>SqliteLockHandler</code> (internal)</td></tr><tr><td><code>RedisSemaphore</code></td><td><code>RedisLockHandler</code></td></tr><tr><td><code>SemaphoreContext</code></td><td><code>LockHandlerContext</code></td></tr><tr><td><code>ISemaphore.ObtainLock</code></td><td><code>ILockHandler.AcquireLock</code></td></tr></tbody></table><p><code>AdoJobStoreBase</code> was a public base class in the alphas and is internal in 4.0; if you derived from it, <a href="#the-ado-net-store-is-a-store-not-a-base-class">The ADO.NET store is a store, not a base class</a> has the four answers, which are the same four an application deriving from 3.x&#39;s <code>JobStoreSupport</code> gets. <code>AdoJobStoreDependencies</code> is in <a href="#types-that-were-removed-internalized-or-renamed">Types that were removed, internalized or renamed</a> for the same reason.</p><p>One key is worth knowing about: <code>quartz.jobStore.openConnection</code> had no reader through <code>4.0.0-alpha.5</code>. It sits under a supported prefix, so the unknown-key check passed it and nothing then looked at it — see <a href="#the-ado-net-job-stores-are-named-for-whose-transaction-they-use">The ADO.NET job stores are named for whose transaction they use</a>.</p>`,21)])}var m=s(u,[[`render`,p]]);export{l as _pageData,m as default};