import{a as e,c as t,d as n,i as r,o as i,r as a,s as o,t as s,u as c}from"./app-4ShAy9Q6.js";var l=JSON.parse(`{"path":"/documentation/quartz-3.x/tutorial/node-affinity.html","title":"Node Affinity (Preferred Node)","lang":"en-US","frontmatter":{"title":"Node Affinity (Preferred Node)"},"git":{"updatedTime":1784832616000,"contributors":[{"name":"Marko Lahma","username":"","email":"marko.lahma@gmail.com","commits":1},{"name":"Claude Opus 4.8 (1M context)","username":"","email":"noreply@anthropic.com","commits":5}],"changelog":[{"hash":"023060d417f19c71edd8114673307e0260f38047","time":1784832616000,"email":"marko.lahma@gmail.com","author":"Marko Lahma","message":"Target tests and examples against NET 10 (#3164)","coAuthors":[{"name":"Claude Opus 4.8 (1M context)","email":"noreply@anthropic.com"},{"name":"Claude Opus 4.8 (1M context)","email":"noreply@anthropic.com"},{"name":"Claude Opus 4.8 (1M context)","email":"noreply@anthropic.com"},{"name":"Claude Opus 4.8 (1M context)","email":"noreply@anthropic.com"},{"name":"Claude Opus 4.8 (1M context)","email":"noreply@anthropic.com"}]}]},"filePathRelative":"documentation/quartz-3.x/tutorial/node-affinity.md"}`),u={name:`node-affinity.md`};function d(s,l,u,d,f,p){let m=c(`RouteLink`);return t(),r(`div`,null,[l[15]||=a(`p`,null,[i(`Node affinity lets you control `),a(`strong`,null,`which cluster node runs a specific trigger`),i(`. This is useful when a job maintains in-memory state (such as a cache or a warmed-up connection) between runs and should keep executing on the same node.`)],-1),a(`p`,null,[l[1]||=i(`See also `,-1),o(m,{to:`/documentation/quartz-3.x/tutorial/execution-groups.html`},{default:n(()=>[...l[0]||=[i(`Execution Groups`,-1)]]),_:1}),l[2]||=i(`, which limits `,-1),l[3]||=a(`em`,null,`how many`,-1),l[4]||=i(` threads a category of job may use on a node. The two features compose: affinity decides `,-1),l[5]||=a(`em`,null,`where`,-1),l[6]||=i(` a trigger runs, execution groups decide `,-1),l[7]||=a(`em`,null,`how much`,-1),l[8]||=i(` of a node it may consume.`,-1)]),l[16]||=e(`<h2 id="concepts" tabindex="-1"><a class="header-anchor" href="#concepts"><span>Concepts</span></a></h2><p>A <strong>preferred node</strong> is an optional property of a <strong>trigger</strong> naming the scheduler instance that should acquire it. Because the setting lives on the trigger rather than the job, a job with several triggers could in principle have different preferred nodes — set the same value on all of a job&#39;s triggers if you want job-level affinity.</p><ul><li>A specific scheduler instance id (e.g. <code>&quot;node-1&quot;</code>, matching <code>quartz.scheduler.instanceId</code>) pins the trigger to that node.</li><li>The sentinel <code>&quot;*&quot;</code> requests <strong>auto-pin</strong>: the first node to fire the trigger claims it.</li><li><code>null</code> (the default) means no preference — standard Quartz behavior.</li></ul><p>Preferred node is a <strong>strong preference with automatic failover</strong>, not a hard constraint. Acquisition filters out triggers pinned to <em>live</em> nodes, but if the pinned node is not currently checking in, other nodes take over. See <a href="#failover-behavior">Failover behavior</a>.</p><p>Two columns back this on <code>QRTZ_TRIGGERS</code>:</p><table><thead><tr><th><code>PREFERRED_NODE</code></th><th><code>PREFERRED_NODE_AUTO</code></th><th>Meaning</th></tr></thead><tbody><tr><td><code>NULL</code></td><td>false</td><td>No affinity (default)</td></tr><tr><td><code>&#39;*&#39;</code></td><td>false</td><td>Auto-pin requested, not yet claimed</td></tr><tr><td><code>&#39;node-1&#39;</code></td><td>true</td><td>Auto-claimed by <code>node-1</code></td></tr><tr><td><code>&#39;node-1&#39;</code></td><td>false</td><td>Explicit pin to <code>node-1</code></td></tr></tbody></table><p>The node name is stored verbatim and the auto-claim is recorded separately, so <strong>no instance id is reserved</strong> — a node may legitimately be called <code>auto:thing</code> or <code>*-west</code> without confusing Quartz.</p><h2 id="setting-the-preferred-node" tabindex="-1"><a class="header-anchor" href="#setting-the-preferred-node"><span>Setting the preferred node</span></a></h2><p>Use <code>TriggerBuilder.WithPreferredNode()</code>:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token comment">// Pin to a specific node</span></span>
<span class="line"><span class="token class-name">ITrigger</span> trigger <span class="token operator">=</span> TriggerBuilder<span class="token punctuation">.</span><span class="token function">Create</span><span class="token punctuation">(</span><span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">.</span><span class="token function">WithIdentity</span><span class="token punctuation">(</span><span class="token string">&quot;myTrigger&quot;</span><span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">.</span><span class="token function">ForJob</span><span class="token punctuation">(</span>job<span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">.</span><span class="token function">WithPreferredNode</span><span class="token punctuation">(</span><span class="token string">&quot;production-node-1&quot;</span><span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">.</span><span class="token function">WithCronSchedule</span><span class="token punctuation">(</span><span class="token string">&quot;0 0/5 * * * ?&quot;</span><span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">.</span><span class="token function">Build</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token comment">// Auto-pin: the first node to fire it claims it</span></span>
<span class="line"><span class="token class-name">ITrigger</span> trigger <span class="token operator">=</span> TriggerBuilder<span class="token punctuation">.</span><span class="token function">Create</span><span class="token punctuation">(</span><span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">.</span><span class="token function">WithIdentity</span><span class="token punctuation">(</span><span class="token string">&quot;myTrigger&quot;</span><span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">.</span><span class="token function">ForJob</span><span class="token punctuation">(</span>job<span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">.</span><span class="token function">WithPreferredNode</span><span class="token punctuation">(</span><span class="token string">&quot;*&quot;</span><span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">.</span><span class="token function">WithCronSchedule</span><span class="token punctuation">(</span><span class="token string">&quot;0 0/5 * * * ?&quot;</span><span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">.</span><span class="token function">Build</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Read it back by casting to <code>AbstractTrigger</code> (in 3.x the properties live there rather than on <code>ITrigger</code>, which cannot gain members without a breaking change):</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token class-name">ITrigger</span> t <span class="token operator">=</span> <span class="token keyword">await</span> scheduler<span class="token punctuation">.</span><span class="token function">GetTrigger</span><span class="token punctuation">(</span><span class="token keyword">new</span> <span class="token constructor-invocation class-name">TriggerKey</span><span class="token punctuation">(</span><span class="token string">&quot;myTrigger&quot;</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"><span class="token class-name"><span class="token keyword">var</span></span> at <span class="token operator">=</span> <span class="token punctuation">(</span>AbstractTrigger<span class="token punctuation">)</span> t<span class="token punctuation">;</span></span>
<span class="line"><span class="token class-name"><span class="token keyword">string</span><span class="token punctuation">?</span></span> node <span class="token operator">=</span> at<span class="token punctuation">.</span>PreferredNode<span class="token punctuation">;</span>      <span class="token comment">// &quot;production-node-1&quot;</span></span>
<span class="line"><span class="token class-name"><span class="token keyword">bool</span></span> auto <span class="token operator">=</span> at<span class="token punctuation">.</span>IsPreferredNodeAuto<span class="token punctuation">;</span>   <span class="token comment">// false for an explicit pin</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><div class="hint-container warning"><p class="hint-container-title">Warning</p><p>The value must match the instance id <strong>exactly</strong>. Pin comparisons happen in SQL using the database&#39;s string collation, so a value differing only in case is a different node — and on a case-sensitive database, one that never matches.</p></div><h2 id="auto-pin-mode" tabindex="-1"><a class="header-anchor" href="#auto-pin-mode"><span>Auto-pin mode</span></a></h2><p>When a trigger&#39;s preferred node is <code>&quot;*&quot;</code>:</p><ol><li>The trigger is acquirable by any node, as usual.</li><li>The first node to fire it writes its own instance id to <code>PREFERRED_NODE</code> and sets <code>PREFERRED_NODE_AUTO</code>. The write is a compare-and-swap against the value seen at acquisition, so a concurrent re-pin or clear wins over the claim rather than being clobbered by it.</li><li>From then on only that node acquires the trigger — until it stops checking in.</li></ol><p>This is ideal when you don&#39;t know node names at configuration time but still want a trigger to stay put.</p><p>Rebuilding an auto-pinned trigger preserves the auto-claim:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token comment">// The rebuilt trigger is still auto-pinned, so it will still fail over if that node dies</span></span>
<span class="line"><span class="token class-name">ITrigger</span> rebuilt <span class="token operator">=</span> trigger<span class="token punctuation">.</span><span class="token function">GetTriggerBuilder</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">WithDescription</span><span class="token punctuation">(</span><span class="token string">&quot;updated&quot;</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">Build</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>Assigning <code>PreferredNode</code> directly always records an <strong>explicit</strong> pin, since it expresses your intent rather than a claim Quartz made:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line">trigger<span class="token punctuation">.</span>PreferredNode <span class="token operator">=</span> <span class="token string">&quot;node-2&quot;</span><span class="token punctuation">;</span>   <span class="token comment">// explicit pin; IsPreferredNodeAuto becomes false</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><h2 id="failover-behavior" tabindex="-1"><a class="header-anchor" href="#failover-behavior"><span>Failover behavior</span></a></h2><p>When the preferred node stops checking in:</p><ol><li><strong>Acquisition.</strong> The acquisition query treats a node whose last check-in is older than the cluster check-in threshold as dead, so surviving nodes may acquire its pinned triggers immediately — without waiting for cluster recovery.</li><li><strong>Steal on fire.</strong> A node that fires a trigger still auto-claimed by another (stale) node takes the pin over via compare-and-swap. Affinity converges on a live node instead of bouncing.</li><li><strong>Cluster recovery.</strong> When recovery confirms a node dead, auto-claimed pins belonging to it are reset to <code>&quot;*&quot;</code> before its state row is deleted, so any <em>eligible</em> node can claim them — which correctly respects execution group limits.</li><li><strong>Explicit pins are preserved.</strong> They are never re-pinned. While the node is down other nodes run the trigger; when it returns and checks in again, it naturally reclaims it.</li></ol><h2 id="updating-the-preferred-node-at-runtime" tabindex="-1"><a class="header-anchor" href="#updating-the-preferred-node-at-runtime"><span>Updating the preferred node at runtime</span></a></h2><p>You can re-pin without rescheduling:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token keyword">await</span> scheduler<span class="token punctuation">.</span><span class="token function">UpdateTriggerDetails</span><span class="token punctuation">(</span></span>
<span class="line">    <span class="token keyword">new</span> <span class="token constructor-invocation class-name">TriggerKey</span><span class="token punctuation">(</span><span class="token string">&quot;myTrigger&quot;</span><span class="token punctuation">)</span><span class="token punctuation">,</span></span>
<span class="line">    <span class="token keyword">new</span> <span class="token constructor-invocation class-name">TriggerDetailsUpdate</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">WithPreferredNode</span><span class="token punctuation">(</span><span class="token string">&quot;node-2&quot;</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Pass <code>null</code> to clear the preference entirely:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token keyword">await</span> scheduler<span class="token punctuation">.</span><span class="token function">UpdateTriggerDetails</span><span class="token punctuation">(</span></span>
<span class="line">    <span class="token keyword">new</span> <span class="token constructor-invocation class-name">TriggerKey</span><span class="token punctuation">(</span><span class="token string">&quot;myTrigger&quot;</span><span class="token punctuation">)</span><span class="token punctuation">,</span></span>
<span class="line">    <span class="token keyword">new</span> <span class="token constructor-invocation class-name">TriggerDetailsUpdate</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">WithPreferredNode</span><span class="token punctuation">(</span><span class="token keyword">null</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="requirements-and-limitations" tabindex="-1"><a class="header-anchor" href="#requirements-and-limitations"><span>Requirements and limitations</span></a></h2>`,31),a(`ul`,null,[l[13]||=e(`<li><strong>Clustering and a stable instance id.</strong> Affinity only means anything with <code>quartz.jobStore.clustered = true</code> and a <em>stable</em> <code>quartz.scheduler.instanceId</code>. With <code>AUTO</code>, the id changes on every restart and a stored pin refers to a node that no longer exists. Quartz warns at startup when it detects an auto-generated id.</li><li><strong>RAMJobStore ignores it.</strong> A pin is stored and returned as metadata but never filters acquisition — a single-node in-memory scheduler always runs the trigger.</li><li><strong>Custom driver delegates.</strong> If a delegate customizes trigger acquisition (by overriding <code>SelectTriggerToAcquire</code> or the <code>GetSelectNextTriggerToAcquire*Sql</code> builders) but does not extend the preferred-node variants, Quartz keeps acquisition on that delegate&#39;s own path and logs a warning: the custom SQL keeps working, but SQL-level affinity filtering is not applied for it.</li><li><strong>Pinned to a node that never registers.</strong> If the target instance id has never checked in, the trigger is eligible everywhere. Affinity is a preference, not a guarantee, so verify the id is spelled right.</li>`,4),a(`li`,null,[l[10]||=a(`strong`,null,`A live but saturated node still holds its pin.`,-1),l[11]||=i(` If the pinned node is up but its `,-1),o(m,{to:`/documentation/quartz-3.x/tutorial/execution-groups.html`},{default:n(()=>[...l[9]||=[i(`execution group`,-1)]]),_:1}),l[12]||=i(` is at its limit, the trigger waits for that node rather than moving. Failover reacts to node death, not to node busyness.`,-1)]),l[14]||=a(`li`,null,[a(`strong`,null,`Brief spread during failover.`),i(` Between a node dying and ownership settling, a fast-firing trigger may run on more than one surviving node before converging.`)],-1)]),l[17]||=e(`<h2 id="schema" tabindex="-1"><a class="header-anchor" href="#schema"><span>Schema</span></a></h2><p><code>PREFERRED_NODE</code> and <code>PREFERRED_NODE_AUTO</code> are <strong>optional</strong> columns on <code>QRTZ_TRIGGERS</code> in 3.x. Add them with <code>database/schema_30_add_preferred_node.sql</code> (both columns must be added together).</p><p>Quartz probes for them at startup. Without them the feature is simply unavailable: the scheduler logs a warning, pins are ignored, and everything else works exactly as before — so upgrading the assembly without touching the database is safe. The probe tolerates a database that is temporarily unreachable at startup and retries on the first successful operation.</p><p>Both columns are mandatory in 4.x, which stores pins in exactly the same way — so upgrading needs no data migration.</p>`,4)])}var f=s(u,[[`render`,d]]);export{l as _pageData,f as default};