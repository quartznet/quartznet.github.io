import{a as e,c as t,d as n,l as r,m as i,p as a,s as o,t as s,u as c}from"./app-emje7bwI.js";var l=JSON.parse(`{"path":"/documentation/quartz-4.x/packages/opentelemetry-integration.html","title":"Observability","lang":"en-US","frontmatter":{"title":"Observability"},"git":{"updatedTime":1790451733000,"contributors":[{"name":"Marko Lahma","username":"","email":"marko.lahma@gmail.com","commits":1},{"name":"Claude Opus 5.5","username":"","email":"noreply@anthropic.com","commits":1}],"changelog":[{"hash":"4a29e014ef1e61227140e96264c49cf64cd3183d","time":1790451733000,"email":"marko.lahma@gmail.com","author":"Marko Lahma","message":"A captured log reaches its column whole on Oracle and SQL Server","coAuthors":[{"name":"Claude Opus 5.5","email":"noreply@anthropic.com"}]}]},"filePathRelative":"documentation/quartz-4.x/packages/opentelemetry-integration.md"}`),u={name:`opentelemetry-integration.md`},d={class:`hint-container warning`};function f(s,l,u,f,p,m){let h=a(`RouteLink`);return n(),o(`div`,null,[l[22]||=t(`<h1 id="observability" tabindex="-1"><a class="header-anchor" href="#observability"><span>Observability</span></a></h1><p>Quartz publishes traces and metrics through <code>System.Diagnostics</code>: an <code>ActivitySource</code> and a <code>Meter</code>, both named <code>Quartz</code>. No Quartz package is needed; install only the collector. Subscribe with the public constants:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line">builder<span class="token punctuation">.</span>Services<span class="token punctuation">.</span><span class="token function">AddOpenTelemetry</span><span class="token punctuation">(</span><span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">.</span><span class="token function">WithTracing</span><span class="token punctuation">(</span>tracing <span class="token operator">=&gt;</span> tracing</span>
<span class="line">        <span class="token punctuation">.</span><span class="token function">AddSource</span><span class="token punctuation">(</span>QuartzInstrumentation<span class="token punctuation">.</span>ActivitySourceName<span class="token punctuation">)</span></span>
<span class="line">        <span class="token punctuation">.</span><span class="token function">AddOtlpExporter</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">.</span><span class="token function">WithMetrics</span><span class="token punctuation">(</span>metrics <span class="token operator">=&gt;</span> metrics</span>
<span class="line">        <span class="token punctuation">.</span><span class="token function">AddMeter</span><span class="token punctuation">(</span>QuartzInstrumentation<span class="token punctuation">.</span>MeterName<span class="token punctuation">)</span></span>
<span class="line">        <span class="token punctuation">.</span><span class="token function">AddOtlpExporter</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>QuartzInstrumentation</code> is in the <code>Quartz.Diagnostics</code> namespace. Both constants are <code>&quot;Quartz&quot;</code>, so an existing <code>AddSource(&quot;Quartz&quot;)</code> keeps working.</p>`,4),e(`div`,d,[l[3]||=e(`p`,{class:`hint-container-title`},`Upgrading from 3.x`,-1),e(`p`,null,[l[1]||=r(`4.0 renamed every instrument and attribute, removed two of 3.x's four instruments, and added five for misfires, acquisition, cluster check-in and recovery, and store round trips. Dashboards and alerts on the old names break; see the `,-1),c(h,{to:`/documentation/quartz-4.x/migration-guide.html#old-and-new-telemetry-names`},{default:i(()=>[...l[0]||=[r(`complete old → new table`,-1)]]),_:1}),l[2]||=r(`.`,-1)])]),l[23]||=t(`<h2 id="traces" tabindex="-1"><a class="header-anchor" href="#traces"><span>Traces</span></a></h2><table><thead><tr><th>Span</th><th>Kind</th><th>When</th></tr></thead><tbody><tr><td><code>Quartz.Job.Execute</code></td><td><code>Internal</code></td><td>A job runs; covers the whole fire and records a thrown exception</td></tr><tr><td><code>Quartz.Job.Veto</code></td><td><code>Internal</code></td><td>A trigger listener vetoed the fire; the job did not run</td></tr><tr><td><code>Quartz.JobStore.&lt;operation&gt;</code></td><td><code>Client</code></td><td>One per store operation; names are the members of <code>Quartz.Diagnostics.OperationName.JobStore</code></td></tr></tbody></table><p>The thirty-three store operations are those that change something or hand work to the scheduler:</p><p><code>AcquireNextTriggers</code>, <code>TriggersFired</code>, <code>TriggeredJobComplete</code>, <code>ReleaseAcquiredTrigger</code>, <code>ScheduleJob</code>, <code>ScheduleJobs</code>, <code>AddJob</code>, <code>AddTrigger</code>, <code>AddCalendar</code>, <code>DeleteJob</code>, <code>DeleteJobs</code>, <code>DeleteTrigger</code>, <code>DeleteTriggers</code>, <code>DeleteCalendar</code>, <code>ReplaceTrigger</code>, <code>UpdateTriggerDetails</code>, <code>PauseTrigger</code>, <code>PauseTriggers</code>, <code>PauseTriggerGroups</code>, <code>PauseJob</code>, <code>PauseJobs</code>, <code>PauseJobGroups</code>, <code>ResumeTrigger</code>, <code>ResumeTriggers</code>, <code>ResumeTriggerGroups</code>, <code>ResumeJob</code>, <code>ResumeJobs</code>, <code>ResumeJobGroups</code>, <code>PauseAll</code>, <code>ResumeAll</code>, <code>ResetTriggerFromErrorState</code>, <code>ResetTriggersFromErrorState</code>, <code>Clear</code>.</p><ul><li>Pausing by key and pausing by group matcher are separate spans. The first moves the given triggers; the second records that a group is paused and also catches triggers added to it later.</li><li>Reads (<code>GetJob</code>, <code>Exists</code>, the <code>Query*</code> members) are not spans, so a dashboard listing triggers does not produce a span per page.</li></ul><h3 id="how-the-spans-are-shaped-into-traces" tabindex="-1"><a class="header-anchor" href="#how-the-spans-are-shaped-into-traces"><span>How the spans are shaped into traces</span></a></h3><ul><li><strong>A firing is its own trace.</strong> <code>Quartz.Job.Execute</code> and <code>Quartz.Job.Veto</code> are always trace roots and never take the ambient <code>Activity</code> as parent. What the job traces (an <code>HttpClient</code> call, an EF Core query, your own spans) are children of the firing.</li><li><strong>A store span belongs to the caller.</strong> <code>scheduler.ScheduleJob(…)</code> inside an HTTP request puts <code>Quartz.JobStore.ScheduleJob</code> in that request&#39;s trace.</li><li><strong>The scheduler loop&#39;s store calls are roots.</strong> <code>AcquireNextTriggers</code>, <code>TriggersFired</code> and <code>TriggeredJobComplete</code> belong to no request, and the loop runs for the life of the process.</li></ul><div class="hint-container warning"><p class="hint-container-title">Fixed in 4.2.0</p><p>Before 4.2.0, each span the scheduler&#39;s loop opened became the parent of the next, and each firing hung off whichever span was current at dispatch. A scheduler produced one trace that grew for the life of the process: a day of a quiet staging pod was one tree of several thousand spans, with the jobs&#39; <code>HttpClient</code> and EF Core spans buried in it (<a href="https://github.com/quartznet/quartznet/issues/3797" target="_blank" rel="noopener noreferrer">#3797</a>). Upgrading fixes it; no configuration change is needed.</p></div><div class="hint-container tip"><p class="hint-container-title">Every store, not just the database one</p><p>Store tracing is a decorator over <code>IJobStore</code>, applied to any store: in-memory, a community package&#39;s, or your own. Before 4.0.0 only the ADO.NET store emitted store spans.</p></div><p>Span names are constants on <code>Quartz.Diagnostics.OperationName</code>. Attributes are namespaced <code>quartz.*</code> and are constants on <code>Quartz.Diagnostics.ActivityTags</code>:</p><table><thead><tr><th>Attribute</th><th>On</th></tr></thead><tbody><tr><td><code>quartz.scheduler.name</code>, <code>quartz.scheduler.id</code></td><td>every span</td></tr><tr><td><code>quartz.job.name</code>, <code>quartz.job.group</code>, <code>quartz.job.type</code></td><td>job spans</td></tr><tr><td><code>quartz.trigger.name</code>, <code>quartz.trigger.group</code></td><td>job spans; store spans about one trigger</td></tr><tr><td><code>quartz.execution.group</code></td><td>job spans, when the trigger names an execution group</td></tr><tr><td><code>quartz.fire.instance.id</code></td><td>job spans</td></tr><tr><td><code>quartz.job.name</code>, <code>quartz.job.group</code></td><td>store spans about one job</td></tr><tr><td><code>quartz.jobstore.batch.size</code></td><td><code>Quartz.JobStore.AcquireNextTriggers</code>: triggers requested</td></tr><tr><td><code>quartz.jobstore.trigger.count</code></td><td><code>Quartz.JobStore.AcquireNextTriggers</code> (returned) and <code>.TriggersFired</code> (fired)</td></tr><tr><td><code>error.type</code></td><td>any span that ended in a failure</td></tr></tbody></table><p><code>quartz.fire.instance.id</code> identifies one firing; it is the id <code>IScheduler.InterruptFireInstance</code> takes.</p><h3 id="linking-a-firing-to-what-scheduled-it" tabindex="-1"><a class="header-anchor" href="#linking-a-firing-to-what-scheduled-it"><span>Linking a firing to what scheduled it</span></a></h3><p>When the scheduling call runs inside an <code>Activity</code>, the scheduler stores its W3C trace context on the trigger, under the reserved keys <code>SchedulerConstants.TraceParent</code> and <code>SchedulerConstants.TraceState</code>. The firing&#39;s <code>Quartz.Job.Execute</code> span, and a <code>Quartz.Job.Veto</code> span, carry an <code>ActivityLink</code> back to it.</p><ul><li>The firing may run much later and on another node. It is linked, not parented, so no trace stays open across the wait. This is OpenTelemetry&#39;s shape for an asynchronous producer and consumer.</li><li>Nothing needs configuring. An HTTP API request gets it automatically, because the endpoint runs inside ASP.NET Core&#39;s server span.</li><li>Cost: two string entries on each trigger&#39;s data map, visible in <code>MergedJobDataMap</code>, the dashboard and <code>GET /triggers</code>.</li></ul><p>Turn it off with:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line">q<span class="token punctuation">.</span><span class="token function">ConfigureScheduler</span><span class="token punctuation">(</span>options <span class="token operator">=&gt;</span> options<span class="token punctuation">.</span>PropagateTraceContext <span class="token operator">=</span> <span class="token boolean">false</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><div class="hint-container tip"><p class="hint-container-title">The trigger&#39;s map, never the job&#39;s</p><p><code>[PersistJobDataAfterExecution]</code> writes back only the job&#39;s map, so a persisted job cannot carry a <code>traceparent</code> into its next firing.</p></div><h2 id="metrics" tabindex="-1"><a class="header-anchor" href="#metrics"><span>Metrics</span></a></h2><p>Eleven instruments, all on the <code>Quartz</code> meter. <strong>Every measurement carries <code>quartz.scheduler.name</code> and <code>quartz.scheduler.id</code></strong>: the name identifies the scheduler, the id its node in a cluster.</p><p>Each instrument name is a <code>const string</code> on <code>QuartzInstrumentation.Instruments</code>; for example <code>QuartzInstrumentation.Instruments.JobExecutionDuration</code> is <code>quartz.job.execution.duration</code>. Tests hold the constants equal to what the meter emits and snapshot the name, kind, unit and description of every instrument.</p><table><thead><tr><th>Instrument</th><th>Type</th><th>Unit</th><th>Extra attributes</th><th>Measures</th></tr></thead><tbody><tr><td><code>quartz.job.execution.duration</code></td><td><code>Histogram&lt;double&gt;</code></td><td><code>s</code></td><td><code>quartz.trigger.group</code>, <code>quartz.trigger.name</code>, <code>quartz.job.group</code>, <code>quartz.job.name</code>, <code>quartz.execution.group</code>¹, <code>error.type</code>²</td><td>Job duration; <strong>count</strong> = executions</td></tr><tr><td><code>quartz.job.execution.active</code></td><td><code>UpDownCounter&lt;long&gt;</code></td><td><code>{job}</code></td><td>the same identity attributes, <code>quartz.execution.group</code>¹</td><td>Jobs running now</td></tr><tr><td><code>quartz.trigger.misfire</code></td><td><code>Counter&lt;long&gt;</code></td><td><code>{trigger}</code></td><td><code>quartz.trigger.group</code>, <code>quartz.execution.group</code>¹</td><td>Firings not made on time</td></tr><tr><td><code>quartz.trigger.retry</code></td><td><code>Counter&lt;long&gt;</code></td><td><code>{trigger}</code></td><td><code>quartz.trigger.group</code>, <code>quartz.execution.group</code>¹</td><td>Retries scheduled after a job failed</td></tr><tr><td><code>quartz.trigger.retries_exhausted</code></td><td><code>Counter&lt;long&gt;</code></td><td><code>{trigger}</code></td><td><code>quartz.trigger.group</code>, <code>quartz.execution.group</code>¹</td><td>Failed occurrences whose retry policy ran out</td></tr><tr><td><code>quartz.trigger.acquisition.duration</code></td><td><code>Histogram&lt;double&gt;</code></td><td><code>s</code></td><td>—</td><td>Loop&#39;s wait on the store for the next batch</td></tr><tr><td><code>quartz.trigger.acquired</code></td><td><code>Counter&lt;long&gt;</code></td><td><code>{trigger}</code></td><td>—</td><td>Triggers returned by those rounds</td></tr><tr><td><code>quartz.cluster.checkin.duration</code></td><td><code>Histogram&lt;double&gt;</code></td><td><code>s</code></td><td><code>error.type</code>²</td><td>Cluster check-in duration, per attempt</td></tr><tr><td><code>quartz.cluster.recovery.trigger</code></td><td><code>Counter&lt;long&gt;</code></td><td><code>{trigger}</code></td><td><code>quartz.cluster.recovered.instance.id</code></td><td>Fired-trigger rows recovered from a failed node</td></tr><tr><td><code>quartz.jobstore.operation.duration</code></td><td><code>Histogram&lt;double&gt;</code></td><td><code>s</code></td><td><code>quartz.jobstore.operation</code>, <code>error.type</code>²</td><td>Every store round trip, by operation</td></tr><tr><td><code>quartz.jobstore.lock.wait.duration</code></td><td><code>Histogram&lt;double&gt;</code></td><td><code>s</code></td><td><code>quartz.jobstore.lock</code>, <code>error.type</code>²</td><td>One attempt to take a job store lock</td></tr></tbody></table><p>¹ Only when the trigger names an execution group. A trigger in no group has no such attribute (not an empty one), so the two are separate series. ² Only when the operation failed. The value is the exception type&#39;s fully-qualified name.</p><ul><li><code>quartz.trigger.retry</code> counts retries scheduled, not attempts configured: an unused policy adds nothing.</li><li><code>quartz.trigger.retries_exhausted</code> counts once per occurrence that gave up, never per attempt. It has the same attributes as <code>quartz.trigger.retry</code>, so the two divide.</li><li><code>quartz.cluster.checkin.duration</code> records each attempt: a retried check-in is two measurements.</li><li><code>quartz.jobstore.lock.wait.duration</code> does not record a re-entrant acquisition, which waited for nothing.</li><li><code>quartz.jobstore.operation</code> is one of the thirty-three <code>Quartz.JobStore.*</code> names above, so one string finds a slow operation in traces and metrics. The histogram count is the number of each operation; the <code>error.type</code>-tagged part is the failures.</li><li><code>quartz.jobstore.lock</code> is the <code>LOCK_NAME</code> value: <code>TRIGGER_ACCESS</code> (taken by every scheduling operation) or <code>STATE_ACCESS</code> (taken by cluster check-in).</li></ul>`,24),e(`p`,null,[l[6]||=r(`The lock-wait histogram is the one instrument that reports `,-1),l[7]||=e(`em`,null,`during`,-1),l[8]||=r(` a stall. A lock statement blocked behind another session returns and throws nothing, so no operation or failure is recorded. The matching warning is event 3716 in `,-1),c(h,{to:`/documentation/quartz-4.x/log-events.html`},{default:i(()=>[...l[4]||=[r(`Log Events`,-1)]]),_:1}),l[9]||=r(`; the case is `,-1),c(h,{to:`/documentation/troubleshooting.html#a-lock-held-by-a-connection-that-is-gone`},{default:i(()=>[...l[5]||=[r(`A Lock Held by a Connection That Is Gone`,-1)]]),_:1}),l[10]||=r(`.`,-1)]),l[24]||=t(`<p>The two cluster instruments and the lock-wait histogram come from the ADO.NET store, the only clustered one. The other eight work with any store.</p><h3 id="reading-the-numbers" tabindex="-1"><a class="header-anchor" href="#reading-the-numbers"><span>Reading the numbers</span></a></h3><ul><li>There is no execution counter and no error counter. Executions are the count of <code>quartz.job.execution.duration</code>; failures are the part tagged with <code>error.type</code>, which also names the exception.</li><li><code>error.type</code> is the OpenTelemetry convention. It is not on <code>quartz.job.execution.active</code>: an up-down counter&#39;s increment and decrement need identical attributes, and failure is not known at start.</li></ul><div class="hint-container warning"><p class="hint-container-title">Cardinality</p><p><code>quartz.job.name</code> and <code>quartz.trigger.name</code> are per job and per trigger, and <code>quartz.scheduler.id</code> is per node, so a backend can get a series per node per trigger. Unless you need them, drop the name attributes in a view; the group attributes are usually the ones to keep.</p></div><p>The meter is created from the container&#39;s <code>IMeterFactory</code> when there is one. <code>AddMetrics()</code>, and so every generic-host application, registers it. This keeps measurements of two schedulers, or two hosts in one test process, apart.</p><h2 id="opentelemetry-instrumentation-quartz" tabindex="-1"><a class="header-anchor" href="#opentelemetry-instrumentation-quartz"><span>OpenTelemetry.Instrumentation.Quartz</span></a></h2><p><a href="https://www.nuget.org/packages/OpenTelemetry.Instrumentation.Quartz" target="_blank" rel="noopener noreferrer">OpenTelemetry.Instrumentation.Quartz</a> is the OpenTelemetry community&#39;s instrumentation library, written for 3.x.</p><div class="hint-container caution"><p class="hint-container-title">It produces nothing against 4.0, and does not say so</p><p><code>AddQuartzInstrumentation()</code> yields <strong>zero spans</strong> on Quartz 4.x. Nothing throws or warns and the call still compiles; an upgraded application silently loses its job spans.</p></div><table><thead><tr><th></th><th>3.x</th><th>4.x</th></tr></thead><tbody><tr><td>Publishes through</td><td>a <code>DiagnosticListener</code> named <code>Quartz</code>, <code>new Activity(...)</code> with no <code>ActivitySource</code></td><td>an <code>ActivitySource</code> named <code>Quartz</code></td></tr><tr><td>The package subscribes with</td><td><code>DiagnosticSourceSubscriber</code> for that listener, plus <code>AddLegacySource(&quot;Quartz.Job.Execute&quot;)</code> and <code>AddLegacySource(&quot;Quartz.Job.Veto&quot;)</code></td><td>matches nothing</td></tr></tbody></table><p>In the OpenTelemetry SDK a &quot;legacy source&quot; is an activity with <em>no</em> <code>ActivitySource</code>. 4.x activities have one, and 4.x writes nothing to a <code>DiagnosticListener</code>, so neither half of the subscription matches.</p><p>Replace it with the two calls at the top of this page, <code>AddSource(QuartzInstrumentation.ActivitySourceName)</code> and <code>AddMeter(QuartzInstrumentation.MeterName)</code>, and remove the package:</p><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> builder.Services.AddOpenTelemetry()</span>
<span class="line"></span><span class="token prefix deleted">-</span><span class="token line">     .WithTracing(tracing =&gt; tracing.AddQuartzInstrumentation());</span>
<span class="line"></span></span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span><span class="token line"> builder.Services.AddOpenTelemetry()</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">     .WithTracing(tracing =&gt; tracing.AddSource(QuartzInstrumentation.ActivitySourceName))</span>
<span class="line"></span><span class="token prefix inserted">+</span><span class="token line">     .WithMetrics(metrics =&gt; metrics.AddMeter(QuartzInstrumentation.MeterName));</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><div class="language-diff line-numbers-mode" data-highlighter="prismjs" data-ext="diff"><pre><code class="language-diff"><span class="line"><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span><span class="token line"> &lt;PackageReference Include=&quot;OpenTelemetry.Instrumentation.Quartz&quot; Version=&quot;1.*&quot; /&gt;</span>
<span class="line"></span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><ul><li>Lost: the package&#39;s <code>QuartzInstrumentationOptions.TracedOperations</code> filter. Subscribing directly records both <code>Quartz.Job.Execute</code> and <code>Quartz.Job.Veto</code>; drop one with an OpenTelemetry <a href="https://opentelemetry.io/docs/languages/dotnet/" target="_blank" rel="noopener noreferrer">processor or sampler</a>.</li><li>Gained: everything 4.0 added, including the store spans and all eleven instruments.</li></ul><h2 id="older-packages" tabindex="-1"><a class="header-anchor" href="#older-packages"><span>Older packages</span></a></h2><p><code>Quartz.OpenTelemetry.Instrumentation</code> is obsolete and not part of 4.x. Subscribe to the activity source directly, as at the top of this page.</p><h3 id="coming-from-quartz-opentracing" tabindex="-1"><a class="header-anchor" href="#coming-from-quartz-opentracing"><span>Coming from Quartz.OpenTracing</span></a></h3><p><code>Quartz.OpenTracing</code> is not part of 4.x and has no 4.x release; the OpenTracing project is archived. It used the <code>DiagnosticSource</code> events that 4.x replaced with <code>System.Diagnostics.Activity</code>. Replace an <code>AddQuartzOpenTracing</code> call with the OpenTelemetry setup at the top of this page.</p><h2 id="logging" tabindex="-1"><a class="header-anchor" href="#logging"><span>Logging</span></a></h2><p>Quartz logs through <code>Microsoft.Extensions.Logging</code>, using the application&#39;s configuration; nothing to wire up. Types no container builds (a listener or trigger you constructed, the static helpers, the jobs in <code>Quartz.Jobs</code>) use the factory set with <code>Quartz.Diagnostics.LogProvider.SetLogProvider(loggerFactory)</code>.</p>`,20),e(`table`,null,[l[18]||=e(`thead`,null,[e(`tr`,null,[e(`th`,null,`Log scope`),e(`th`,null,`Carries`),e(`th`,null,`Opened`)])],-1),e(`tbody`,null,[l[17]||=e(`tr`,null,[e(`td`,null,`the scheduler's`),e(`td`,null,[e(`code`,null,`quartz.scheduler.name`),r(`, `),e(`code`,null,`quartz.scheduler.id`)]),e(`td`,null,`once per scheduling loop; always`)],-1),e(`tr`,null,[l[15]||=e(`td`,null,`the firing's`,-1),l[16]||=e(`td`,null,[e(`code`,null,`quartz.job.name`),r(`, `),e(`code`,null,`quartz.job.group`),r(`, `),e(`code`,null,`quartz.trigger.name`),r(`, `),e(`code`,null,`quartz.trigger.group`),r(`, `),e(`code`,null,`quartz.fire.instance.id`)],-1),e(`td`,null,[l[12]||=r(`once per firing, from 4.3, with `,-1),l[13]||=e(`code`,null,`q.AddJobLogScope()`,-1),l[14]||=r(` — see `,-1),c(h,{to:`/documentation/quartz-4.x/tutorial/job-execution-middleware.html#a-log-scope-per-firing`},{default:i(()=>[...l[11]||=[r(`A log scope per firing`,-1)]]),_:1})])])])]),l[25]||=e(`p`,null,[r(`The names are the span attributes above. Set `),e(`code`,null,`IncludeScopes = true`),r(` on the OpenTelemetry logger to export them.`)],-1),e(`p`,null,[l[20]||=r(`For job and trigger history as log entries rather than traces, use the `,-1),c(h,{to:`/documentation/quartz-4.x/packages/quartz-plugins.html`},{default:i(()=>[...l[19]||=[r(`history plugins`,-1)]]),_:1}),l[21]||=r(`.`,-1)])])}var p=s(u,[[`render`,f]]);export{l as _pageData,p as default};