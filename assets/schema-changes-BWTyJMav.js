import{a as e,c as t,d as n,l as r,m as i,p as a,s as o,t as s,u as c}from"./app-emje7bwI.js";var l=JSON.parse(`{"path":"/documentation/database/schema-changes.html","title":"Database Schema Changes","lang":"en-US","frontmatter":{"title":"Database Schema Changes"},"git":{"updatedTime":1790451733000,"contributors":[{"name":"Marko Lahma","username":"","email":"marko.lahma@gmail.com","commits":1},{"name":"Claude Opus 5.5","username":"","email":"noreply@anthropic.com","commits":1}],"changelog":[{"hash":"4a29e014ef1e61227140e96264c49cf64cd3183d","time":1790451733000,"email":"marko.lahma@gmail.com","author":"Marko Lahma","message":"A captured log reaches its column whole on Oracle and SQL Server","coAuthors":[{"name":"Claude Opus 5.5","email":"noreply@anthropic.com"}]}]},"filePathRelative":"documentation/database/schema-changes.md"}`),u={name:`schema-changes.md`};function d(s,l,u,d,f,p){let m=a(`RouteLink`);return n(),o(`div`,null,[l[34]||=e(`p`,null,`Every Quartz.NET release that changed the database schema, in order, with the migration to run and what happens if you skip it.`,-1),e(`p`,null,[l[1]||=r(`Quartz.NET never migrates your schema. A 4.x store asked to `,-1),c(m,{to:`/documentation/quartz-4.x/tutorial/job-stores.html#creating-the-schema`},{default:i(()=>[...l[0]||=[r(`provision its own`,-1)]]),_:1}),l[2]||=r(` creates a missing schema, but cannot move an existing one forward: a guarded `,-1),l[3]||=e(`code`,null,`CREATE TABLE`,-1),l[4]||=r(` skips a table that exists without looking inside, so a table missing a column stays missing it. Read from your current version down to your target version and run what each section lists.`,-1)]),l[35]||=t(`<div class="hint-container warning"><p class="hint-container-title">Warning</p><p>Always run migration scripts in a test environment against a copy of your production database first.</p></div><h2 id="how-to-use-this-page" tabindex="-1"><a class="header-anchor" href="#how-to-use-this-page"><span>How to use this page</span></a></h2><p>Scripts live in <a href="https://github.com/quartznet/quartznet/tree/main/database/migrations" target="_blank" rel="noopener noreferrer"><code>database/migrations/</code></a>, one folder per version. In each folder, run the file whose suffix matches your database: <code>_sqlServer</code>, <code>_postgres</code>, <code>_mysql_innodb</code>, <code>_oracle</code>, <code>_sqlite</code>, <code>_firebird</code>. Each file runs as is, with no editing.</p><ul><li><strong>Apply every version between where you are and where you are going, in ascending order, and skip none.</strong> Some migrations are optional (you can defer them), but they are <em>cumulative</em>, not alternatives. Skipping 3.17 and later running 3.19 leaves you without the 3.17 column; nothing goes back and adds it.</li><li><strong>Re-running is safe.</strong> Every script except the SQL Server 1.0→2.0 one checks before it acts, so re-running a migration is a no-op and a half-applied migration can be re-run. The exception is SQLite <code>ADD COLUMN</code>: SQLite has no conditional DDL, so those statements fail on a second run.</li><li><strong>A fresh install needs none of this.</strong> The scripts in <a href="https://github.com/quartznet/quartznet/tree/main/database/tables" target="_blank" rel="noopener noreferrer"><code>database/tables/</code></a> already create everything below.</li></ul><h2 id="what-do-i-need" tabindex="-1"><a class="header-anchor" href="#what-do-i-need"><span>What do I need?</span></a></h2><table><thead><tr><th>Coming from</th><th>Going to</th><th>What to run</th></tr></thead><tbody><tr><td>1.x</td><td>any 2.x/3.x</td><td><a href="#version-2-0">2.0</a>, <a href="#version-2-2">2.2</a>, <a href="#version-2-6">2.6</a>, then 3.x as below</td></tr><tr><td>2.0 / 2.1</td><td>3.x</td><td><a href="#version-2-2">2.2</a>, <a href="#version-2-6">2.6</a>, <a href="#version-3-0">3.0</a>, then the optional 3.x ones</td></tr><tr><td>2.2–2.5</td><td>3.x</td><td><a href="#version-2-6">2.6</a>, <a href="#version-3-0">3.0</a>, then the optional 3.x ones</td></tr><tr><td>2.6</td><td>3.x</td><td><a href="#version-3-0">3.0</a>, then the optional 3.x ones</td></tr><tr><td>3.0–3.16</td><td>latest 3.x</td><td><a href="#version-3-17">3.17</a>, <a href="#version-3-18">3.18</a>, <a href="#version-3-19">3.19</a>, <a href="#version-3-20">3.20</a> — all optional</td></tr><tr><td>any 3.x</td><td>4.0 / 4.1</td><td><a href="#version-4-0">4.0</a> — <strong>mandatory</strong>, and it folds in everything from 3.17 onward</td></tr><tr><td>any 3.x</td><td>4.2</td><td><a href="#version-4-0">4.0</a>, then <a href="#version-4-2">4.2</a> — both <strong>mandatory</strong></td></tr><tr><td>any 3.x</td><td>4.3+</td><td><a href="#version-4-0">4.0</a>, <a href="#version-4-2">4.2</a>, then <a href="#version-4-3">4.3</a> — all <strong>mandatory</strong></td></tr><tr><td>4.0 / 4.1</td><td>4.2</td><td><a href="#version-4-2">4.2</a> — <strong>mandatory</strong></td></tr><tr><td>4.0 / 4.1</td><td>4.3+</td><td><a href="#version-4-2">4.2</a>, then <a href="#version-4-3">4.3</a> — both <strong>mandatory</strong></td></tr><tr><td>4.2</td><td>4.3+</td><td><a href="#version-4-3">4.3</a> — <strong>mandatory</strong></td></tr></tbody></table><h2 id="upgrading-to-4-x-is-mandatory" tabindex="-1"><a class="header-anchor" href="#upgrading-to-4-x-is-mandatory"><span>Upgrading to 4.x is mandatory</span></a></h2><p>This is the one migration you cannot defer.</p><ul><li><strong>3.x</strong> probes for <code>MISFIRE_ORIG_FIRE_TIME</code>, <code>EXECUTION_GROUP</code>, <code>PREFERRED_NODE</code> and <code>PREFERRED_NODE_AUTO</code> when the scheduler starts. If a column is missing, it logs a warning and turns the feature off. That is why those migrations are optional on 3.x.</li><li><strong>4.x removed those probes</strong> and assumes all four columns exist. It also needs two columns 3.x never had, <code>RETRY_POLICY</code> and <code>RETRY_ATTEMPT</code> on <code>QRTZ_TRIGGERS</code>, and a table 3.x never had, <code>QRTZ_PAUSED_JOB_GRPS</code>.</li></ul><p>So even a 3.x database with every optional migration applied does not work with 4.x until <a href="#version-4-0">4.0</a> has been applied.</p><p>At startup, 4.x checks that every table it needs is queryable, and runs one <code>SELECT &lt;column&gt; … WHERE 1 = 0</code> per column this migration adds to a table 3.x already had. A database missing the table or any of those columns is refused, and the message names the column and the script. The check cannot see a column&#39;s type or width, so run the whole script.</p><hr><h2 id="version-2-0" tabindex="-1"><a class="header-anchor" href="#version-2-0"><span>Version 2.0</span></a></h2><p><strong>Required</strong> when upgrading from 1.x. Nothing later applies until this has run.</p><p>The 1.x → 2.x schema overhaul:</p><ul><li><p>the listener tables are dropped;</p></li><li><p><code>varchar(1)</code> flag columns become real <code>bit</code> columns;</p></li><li><p><code>IS_STATEFUL</code> is replaced by <code>IS_NONCONCURRENT</code> and <code>IS_UPDATE_DATA</code>;</p></li><li><p><code>SCHED_NAME</code> is added to every table, with the primary keys and indexes rebuilt around it.</p></li><li><p>Script: <a href="https://github.com/quartznet/quartznet/blob/main/database/migrations/2.0/schema_10_to_20_upgrade_sqlServer.sql" target="_blank" rel="noopener noreferrer"><code>migrations/2.0/schema_10_to_20_upgrade_sqlServer.sql</code></a></p></li><li><p>SQL Server only. It is a sample, and needs adapting for other databases.</p></li><li><p><strong>Not idempotent.</strong> It drops and recreates objects unconditionally, so it fails on a partially-migrated database. Run it once, on a restorable copy.</p></li></ul><div class="hint-container warning"><p class="hint-container-title">Warning</p><p>The script defaults <code>SCHED_NAME</code> to <code>TestScheduler</code>. If you have existing data, change it to match your <code>quartz.scheduler.instanceName</code>.</p></div><h2 id="version-2-2" tabindex="-1"><a class="header-anchor" href="#version-2-2"><span>Version 2.2</span></a></h2><p><strong>Required</strong> when upgrading from 2.0 or 2.1.</p><p>Adds <code>SCHED_TIME</code> to <code>QRTZ_FIRED_TRIGGERS</code>, so recovery jobs can see both the scheduled and the actual fire time (<a href="https://github.com/quartznet/quartznet/issues/113" target="_blank" rel="noopener noreferrer">#113</a>).</p><ul><li>Scripts: <a href="https://github.com/quartznet/quartznet/tree/main/database/migrations/2.2" target="_blank" rel="noopener noreferrer"><code>migrations/2.2/</code></a> — all databases</li></ul><p>The column is <code>NOT NULL</code> with no default, so the <code>ALTER</code> fails on a table that holds rows. <code>QRTZ_FIRED_TRIGGERS</code> only holds in-flight entries, so stop the scheduler and clear it first:</p><div class="language-sql line-numbers-mode" data-highlighter="prismjs" data-ext="sql"><pre><code class="language-sql"><span class="line"><span class="token keyword">DELETE</span> <span class="token keyword">FROM</span> QRTZ_FIRED_TRIGGERS<span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><h2 id="version-2-6" tabindex="-1"><a class="header-anchor" href="#version-2-6"><span>Version 2.6</span></a></h2><p><strong>Required</strong> when upgrading from 2.5 or earlier.</p><p>Adds <code>TIME_ZONE_ID</code> to <code>QRTZ_SIMPROP_TRIGGERS</code> and <code>QRTZ_CRON_TRIGGERS</code>, so a trigger&#39;s time zone survives a restart (<a href="https://github.com/quartznet/quartznet/issues/136" target="_blank" rel="noopener noreferrer">#136</a>).</p><ul><li>Scripts: <a href="https://github.com/quartznet/quartznet/tree/main/database/migrations/2.6" target="_blank" rel="noopener noreferrer"><code>migrations/2.6/</code></a> — all databases</li></ul><div class="hint-container tip"><p class="hint-container-title">Tips</p><p>Older copies of this migration only altered <code>QRTZ_SIMPROP_TRIGGERS</code>. <code>QRTZ_CRON_TRIGGERS</code> needs the column too (<a href="https://github.com/quartznet/quartznet/issues/1985" target="_blank" rel="noopener noreferrer">#1985</a>). If you upgraded 2.5→2.6 some time ago, check that both tables have it.</p></div><h2 id="version-3-0" tabindex="-1"><a class="header-anchor" href="#version-3-0"><span>Version 3.0</span></a></h2><p><strong>Required</strong> when upgrading a SQL Server database from 2.6.</p><p>Converts the deprecated <code>IMAGE</code> columns to <code>VARBINARY(MAX)</code> (<a href="https://github.com/quartznet/quartznet/issues/291" target="_blank" rel="noopener noreferrer">#291</a>): <code>QRTZ_CALENDARS.CALENDAR</code>, <code>QRTZ_JOB_DETAILS.JOB_DATA</code>, <code>QRTZ_BLOB_TRIGGERS.BLOB_DATA</code> and <code>QRTZ_TRIGGERS.JOB_DATA</code>.</p><ul><li>Script: <a href="https://github.com/quartznet/quartznet/blob/main/database/migrations/3.0/schema_26_to_30_upgrade_sqlServer.sql" target="_blank" rel="noopener noreferrer"><code>migrations/3.0/schema_26_to_30_upgrade_sqlServer.sql</code></a></li><li>SQL Server only; no other dialect used <code>IMAGE</code>.</li></ul><h2 id="version-3-17" tabindex="-1"><a class="header-anchor" href="#version-3-17"><span>Version 3.17</span></a></h2><p><strong>Optional on 3.x. Required on 4.x.</strong></p><p>Adds <code>MISFIRE_ORIG_FIRE_TIME</code> to <code>QRTZ_TRIGGERS</code> (<a href="https://github.com/quartznet/quartznet/issues/2899" target="_blank" rel="noopener noreferrer">#2899</a>). It stores the original scheduled fire time before misfire handling overwrites it.</p><ul><li>Scripts: <a href="https://github.com/quartznet/quartznet/tree/main/database/migrations/3.17" target="_blank" rel="noopener noreferrer"><code>migrations/3.17/</code></a> — all databases</li></ul><p><strong>If you skip it:</strong> AdoJobStore keeps working, but for misfired triggers under the &quot;fire now&quot; misfire policies <code>ScheduledFireTimeUtc</code> equals <code>FireTimeUtc</code>, instead of the time the trigger was <em>supposed</em> to fire. <code>RAMJobStore</code> is unaffected.</p><div class="language-sql line-numbers-mode" data-highlighter="prismjs" data-ext="sql"><pre><code class="language-sql"><span class="line"><span class="token comment">-- SQL Server</span></span>
<span class="line"><span class="token keyword">ALTER</span> <span class="token keyword">TABLE</span> QRTZ_TRIGGERS <span class="token keyword">ADD</span> MISFIRE_ORIG_FIRE_TIME <span class="token keyword">bigint</span> <span class="token boolean">NULL</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="version-3-18" tabindex="-1"><a class="header-anchor" href="#version-3-18"><span>Version 3.18</span></a></h2><p><strong>Optional on 3.x. Required on 4.x.</strong></p><p>Adds <code>EXECUTION_GROUP</code> to <code>QRTZ_TRIGGERS</code> <em>and</em> <code>QRTZ_FIRED_TRIGGERS</code> (<a href="https://github.com/quartznet/quartznet/pull/3004" target="_blank" rel="noopener noreferrer">#3004</a>). It carries the execution group tag that per-node thread limits are enforced against. Both tables need it.</p><ul><li>Scripts: <a href="https://github.com/quartznet/quartznet/tree/main/database/migrations/3.18" target="_blank" rel="noopener noreferrer"><code>migrations/3.18/</code></a> — all databases</li></ul>`,42),e(`p`,null,[l[6]||=e(`strong`,null,`If you skip it:`,-1),l[7]||=r(),c(m,{to:`/documentation/quartz-3.x/tutorial/execution-groups.html`},{default:i(()=>[...l[5]||=[r(`execution groups`,-1)]]),_:1}),l[8]||=r(` still work, but the limit is applied by in-memory filtering after acquisition rather than in the acquire query, so a node acquires triggers it then has to put back.`,-1)]),l[36]||=t(`<div class="language-sql line-numbers-mode" data-highlighter="prismjs" data-ext="sql"><pre><code class="language-sql"><span class="line"><span class="token comment">-- SQL Server</span></span>
<span class="line"><span class="token keyword">ALTER</span> <span class="token keyword">TABLE</span> QRTZ_TRIGGERS <span class="token keyword">ADD</span> EXECUTION_GROUP nvarchar<span class="token punctuation">(</span><span class="token number">200</span><span class="token punctuation">)</span> <span class="token boolean">NULL</span><span class="token punctuation">;</span></span>
<span class="line"><span class="token keyword">ALTER</span> <span class="token keyword">TABLE</span> QRTZ_FIRED_TRIGGERS <span class="token keyword">ADD</span> EXECUTION_GROUP nvarchar<span class="token punctuation">(</span><span class="token number">200</span><span class="token punctuation">)</span> <span class="token boolean">NULL</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="version-3-19" tabindex="-1"><a class="header-anchor" href="#version-3-19"><span>Version 3.19</span></a></h2><p><strong>Optional on 3.x. Required on 4.x.</strong></p>`,3),e(`p`,null,[l[10]||=r(`Adds `,-1),l[11]||=e(`code`,null,`PREFERRED_NODE`,-1),l[12]||=r(` and `,-1),l[13]||=e(`code`,null,`PREFERRED_NODE_AUTO`,-1),l[14]||=r(` to `,-1),l[15]||=e(`code`,null,`QRTZ_TRIGGERS`,-1),l[16]||=r(` (`,-1),l[17]||=e(`a`,{href:`https://github.com/quartznet/quartznet/pull/3013`,target:`_blank`,rel:`noopener noreferrer`},`#3013`,-1),l[18]||=r(`, `,-1),l[19]||=e(`a`,{href:`https://github.com/quartznet/quartznet/pull/3144`,target:`_blank`,rel:`noopener noreferrer`},`#3144`,-1),l[20]||=r(`), which back `,-1),c(m,{to:`/documentation/quartz-3.x/tutorial/node-affinity.html`},{default:i(()=>[...l[9]||=[r(`node affinity`,-1)]]),_:1}),l[21]||=r(`.`,-1)]),l[37]||=t(`<ul><li>Scripts: <a href="https://github.com/quartznet/quartznet/tree/main/database/migrations/3.19" target="_blank" rel="noopener noreferrer"><code>migrations/3.19/</code></a> — all databases</li></ul><p><strong>Add both columns together.</strong> Quartz enables node affinity only when both are present.</p><p><strong>If you skip it:</strong> node affinity is unavailable. The scheduler logs a warning at startup and otherwise behaves exactly as in 3.18.</p><div class="language-sql line-numbers-mode" data-highlighter="prismjs" data-ext="sql"><pre><code class="language-sql"><span class="line"><span class="token comment">-- SQL Server</span></span>
<span class="line"><span class="token keyword">ALTER</span> <span class="token keyword">TABLE</span> QRTZ_TRIGGERS <span class="token keyword">ADD</span> PREFERRED_NODE nvarchar<span class="token punctuation">(</span><span class="token number">200</span><span class="token punctuation">)</span> <span class="token boolean">NULL</span><span class="token punctuation">;</span></span>
<span class="line"><span class="token keyword">ALTER</span> <span class="token keyword">TABLE</span> QRTZ_TRIGGERS <span class="token keyword">ADD</span> PREFERRED_NODE_AUTO <span class="token keyword">bit</span> <span class="token operator">NOT</span> <span class="token boolean">NULL</span> <span class="token keyword">DEFAULT</span> <span class="token number">0</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="version-3-20" tabindex="-1"><a class="header-anchor" href="#version-3-20"><span>Version 3.20</span></a></h2><p><strong>Optional, performance only.</strong></p><p>Realigns the index set with the statements AdoJobStore issues (<a href="https://github.com/quartznet/quartznet/pull/3203" target="_blank" rel="noopener noreferrer">#3203</a>).</p><ul><li><p>Scripts: <a href="https://github.com/quartznet/quartznet/tree/main/database/migrations/3.20" target="_blank" rel="noopener noreferrer"><code>migrations/3.20/</code></a> — all databases</p></li><li><p>Every Quartz statement filters <code>SCHED_NAME</code> first, but several shipped indexes did not lead with it and could not serve a single-scheduler lookup. PostgreSQL was worst: 9 of 11 indexes were affected, and <code>IDX_QRTZ_T_NFT_ST</code> had its columns in the wrong order.</p></li><li><p>Indexes that are a leftmost prefix of a wider one, or that no statement can drive a scan from, are dropped.</p></li><li><p>SQLite previously had no secondary indexes, so every acquire poll was a full table scan; this adds them.</p></li></ul><p><strong>If you skip it:</strong> everything works, with more index maintenance on writes and worse plans on reads. A database created from the current <code>tables/</code> script already matches.</p><div class="hint-container tip"><p class="hint-container-title">Tips</p><p>On a busy PostgreSQL database use <code>CREATE INDEX CONCURRENTLY</code> / <code>DROP INDEX CONCURRENTLY</code>. Neither can run inside a transaction block, so run those statements one at a time.</p></div><h2 id="version-4-0" tabindex="-1"><a class="header-anchor" href="#version-4-0"><span>Version 4.0</span></a></h2><p><strong>Mandatory.</strong> See <a href="#upgrading-to-4-x-is-mandatory">above</a>.</p><ul><li>Scripts: <a href="https://github.com/quartznet/quartznet/tree/main/database/migrations/4.0" target="_blank" rel="noopener noreferrer"><code>migrations/4.0/</code></a> — all databases</li></ul><p>There are <strong>two files</strong>, run at different moments:</p><table><thead><tr><th>File</th><th>Status</th><th>When to run it</th></tr></thead><tbody><tr><td><code>schema_30_to_40_upgrade_&lt;db&gt;.sql</code></td><td><strong>Mandatory</strong></td><td>Now. Everything in it is safe while 3.x nodes are still up.</td></tr><tr><td><code>schema_30_to_40_indexes_&lt;db&gt;.sql</code></td><td>Optional, performance only</td><td>Once the last 3.x node has shut down, or straight afterwards on an offline upgrade.</td></tr></tbody></table><h3 id="the-upgrade-file" tabindex="-1"><a class="header-anchor" href="#the-upgrade-file"><span>The upgrade file</span></a></h3><p>It applies everything from <a href="#version-3-17">3.17</a>, <a href="#version-3-18">3.18</a> and <a href="#version-3-19">3.19</a>, plus the retry columns and the <code>QRTZ_PAUSED_JOB_GRPS</code> table. Run it whether or not you applied the optional migrations; every statement is guarded, so it is safe on a partially-migrated database.</p><table><thead><tr><th>#</th><th>Change</th><th>Status</th></tr></thead><tbody><tr><td>1</td><td><code>MISFIRE_ORIG_FIRE_TIME</code> column</td><td>required</td></tr><tr><td>2</td><td><code>EXECUTION_GROUP</code> columns</td><td>required</td></tr><tr><td>3</td><td><code>PREFERRED_NODE</code> / <code>PREFERRED_NODE_AUTO</code> columns</td><td>required</td></tr><tr><td>4</td><td><code>RETRY_POLICY</code> / <code>RETRY_ATTEMPT</code> columns</td><td>required</td></tr><tr><td>5</td><td><code>QRTZ_PAUSED_JOB_GRPS</code> table</td><td>required</td></tr></tbody></table><p>Sections 1–3 fold in their 3.x counterparts. <strong>Sections 4 and 5 are new in 4.x</strong>, and are why this migration is required even for a database fully migrated on 3.x.</p><ul><li><strong><code>RETRY_POLICY</code> and <code>RETRY_ATTEMPT</code></strong> on <code>QRTZ_TRIGGERS</code> back a trigger&#39;s retry policy (<a href="https://github.com/quartznet/quartznet/issues/3520" target="_blank" rel="noopener noreferrer">#3520</a>): the policy in its stored string form, and how many retries of the executing occurrence have been made. Both are nullable with no default, so existing rows read as &quot;no retry policy&quot; and nothing needs backfilling.</li><li><strong><code>QRTZ_PAUSED_JOB_GRPS</code></strong> holds one row per paused job group, mirroring <code>QRTZ_PAUSED_TRIGGER_GRPS</code> (<a href="https://github.com/quartznet/quartznet/issues/3336" target="_blank" rel="noopener noreferrer">#3336</a>). 3.x pauses a job group without recording it, so <code>IsJobGroupPaused</code> answered <code>false</code> for every group and the pause was lost on restart. 4.x records the group names, so <code>JobGroup.Paused</code> is accurate and <code>QueryJobGroups(new JobGroupQuery { Paused = true })</code> lists them. A group with no jobs can be paused.</li></ul><p><strong>From an earlier 4.0 preview:</strong> run both files again. Every statement is guarded, so the second run only applies what the preview lacked: the retry columns, this table on a preview old enough to predate it, and the index set as a pre-release moved it. On SQLite the upgrade file&#39;s sections 1–4 are not guarded (it has no conditional <code>ADD COLUMN</code>), so check <code>PRAGMA table_info(QRTZ_TRIGGERS)</code> and run only the sections whose columns are missing. The index file is guarded on SQLite too.</p><h3 id="the-index-file" tabindex="-1"><a class="header-anchor" href="#the-index-file"><span>The index file</span></a></h3><p>It supersedes <a href="#version-3-20">3.20</a> and lands the 4.x index shape. It is separate because it cannot run while 3.x nodes are up: it drops <code>IDX_QRTZ_T_NFT_ST_MISFIRE</code>, which 3.x drives its misfire sweep from and 4.x does not read (<a href="https://github.com/quartznet/quartznet/issues/3656" target="_blank" rel="noopener noreferrer">#3656</a>).</p><ul><li>Run it top to bottom: the drops assume the creates above them succeeded. Re-run it as often as you like; every statement is guarded.</li><li>A 4.x node starts fine against a database with the upgrade file but not this one; it scans where it would otherwise seek.</li></ul><p>What it changes:</p><ul><li><strong>Adds <code>IDX_QRTZ_J_G_N</code> and <code>IDX_QRTZ_T_G_N</code>.</strong> The 4.x listing queries page with <code>ORDER BY JOB_GROUP, JOB_NAME</code> and <code>ORDER BY TRIGGER_GROUP, TRIGGER_NAME</code>, and the primary keys are name-before-group. Without these indexes each page is a scan plus a sort.</li><li><strong>Reshapes <code>IDX_QRTZ_T_NFT_ST</code></strong>, the acquisition index, from <code>(SCHED_NAME, TRIGGER_STATE, NEXT_FIRE_TIME)</code> to <code>(SCHED_NAME, TRIGGER_STATE, NEXT_FIRE_TIME ASC, PRIORITY DESC, MISFIRE_INSTR)</code> (<a href="https://github.com/quartznet/quartznet/issues/3510" target="_blank" rel="noopener noreferrer">#3510</a>). <ul><li>Acquisition orders by <code>NEXT_FIRE_TIME ASC, PRIORITY DESC</code>; an index with matching directions lets the engine take the first entry instead of reading and sorting every candidate. On SQL Server, a round trip against 5,000 due triggers goes from 21.6 ms and 20,395 logical reads to 0.6 ms and 8.</li><li><code>MISFIRE_INSTR</code> lets a backlog of misfired triggers below the acquisition window be skipped inside the index rather than one table lookup at a time.</li><li>The name is unchanged but the columns are not, so the script drops the index before recreating it; a guarded <code>CREATE INDEX</code> would find the name taken and keep the old shape.</li><li><strong>Firebird keeps the three-column index.</strong> Its indexes take one direction for the whole index, so it cannot express this one.</li></ul></li><li><strong>Drops <code>IDX_QRTZ_T_NFT_ST_MISFIRE</code></strong>, which SQL Server, MySQL, Oracle and Firebird created over <code>(SCHED_NAME, MISFIRE_INSTR, NEXT_FIRE_TIME, TRIGGER_STATE)</code> and PostgreSQL and SQLite never did (<a href="https://github.com/quartznet/quartznet/issues/3656" target="_blank" rel="noopener noreferrer">#3656</a>). <ul><li>It leads with <code>MISFIRE_INSTR</code>, which both misfire statements compare with <code>&lt;&gt; -1</code>, and no B-tree can seek past that. Those statements filter <code>SCHED_NAME</code> and <code>TRIGGER_STATE</code> by equality and <code>NEXT_FIRE_TIME</code> by range: the reshaped acquisition index&#39;s leading columns.</li><li>Measured plan by plan on all four engines that had it, no optimizer picks it for either statement (<a href="https://github.com/quartznet/quartznet/issues/3608" target="_blank" rel="noopener noreferrer">#3608</a>, <a href="https://github.com/quartznet/quartznet/issues/3656" target="_blank" rel="noopener noreferrer">#3656</a>). MySQL only appeared to, because <code>MySQLDelegate</code> named it in a <code>FORCE INDEX</code> hint; the hint now names the acquisition index.</li><li><strong>The drop comes after the reshape in the same file on purpose</strong>, so a schema that never took the reshape takes it first and is never left with neither index.</li></ul></li></ul><p>Node affinity needs no data migration: 3.x and 4.x store pins identically.</p><h3 id="sqlite-trigger-names" tabindex="-1"><a class="header-anchor" href="#sqlite-trigger-names"><span>SQLite trigger names</span></a></h3><p><code>database/tables/tables_sqlite.sql</code> now names its four referential-integrity triggers <code>QRTZ_DELETE_SIMPLE_TRIGGER</code>, <code>QRTZ_DELETE_SIMPROP_TRIGGER</code>, <code>QRTZ_DELETE_CRON_TRIGGER</code> and <code>QRTZ_DELETE_BLOB_TRIGGER</code>. They were <code>DELETE_SIMPLE_TRIGGER</code> and so on, with no table prefix, so two Quartz schemas could not share one SQLite database whatever their table prefixes: the second <code>CREATE TRIGGER</code> collided with the first.</p><p><strong>No action is required on an existing database.</strong> Nothing in Quartz names these triggers; SQLite runs them itself when a row leaves <code>QRTZ_TRIGGERS</code>, so an older schema keeps working under the old names. Rename them only to put a second Quartz schema in the same file:</p><div class="language-sql line-numbers-mode" data-highlighter="prismjs" data-ext="sql"><pre><code class="language-sql"><span class="line"><span class="token keyword">DROP</span> <span class="token keyword">TRIGGER</span> DELETE_SIMPLE_TRIGGER<span class="token punctuation">;</span></span>
<span class="line"><span class="token keyword">DROP</span> <span class="token keyword">TRIGGER</span> DELETE_SIMPROP_TRIGGER<span class="token punctuation">;</span></span>
<span class="line"><span class="token keyword">DROP</span> <span class="token keyword">TRIGGER</span> DELETE_CRON_TRIGGER<span class="token punctuation">;</span></span>
<span class="line"><span class="token keyword">DROP</span> <span class="token keyword">TRIGGER</span> DELETE_BLOB_TRIGGER<span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>then re-create them from the current <code>tables_sqlite.sql</code>, replacing <code>QRTZ_</code> with your table prefix. There is no migration script for this; it changes nothing Quartz stores or reads.</p><div class="hint-container tip"><p class="hint-container-title">Tips</p><p>The <code>4.0</code> scripts live on <code>main</code> only. The <code>3.x</code> branch links to them rather than carrying a copy, because this upgrade is decided by 4.x&#39;s schema and a copy would go stale. The links above are the right ones.</p></div><hr><h2 id="version-4-2" tabindex="-1"><a class="header-anchor" href="#version-4-2"><span>Version 4.2</span></a></h2><p>Two scripts; only the first is mandatory.</p><ul><li><a href="https://github.com/quartznet/quartznet/tree/main/database/migrations/4.2" target="_blank" rel="noopener noreferrer"><code>migrations/4.2/add_continuations_&lt;db&gt;.sql</code></a> — <strong>mandatory</strong> for 4.2 and later, all databases</li><li><a href="https://github.com/quartznet/quartznet/tree/main/database/migrations/4.2" target="_blank" rel="noopener noreferrer"><code>migrations/4.2/add_execution_history_&lt;db&gt;.sql</code></a> — <strong>optional</strong>, needed only by a store configured with <code>UseExecutionHistory()</code></li></ul><h3 id="the-continuation-columns" tabindex="-1"><a class="header-anchor" href="#the-continuation-columns"><span>The continuation columns</span></a></h3><p><strong>Mandatory</strong> for 4.2 and later. The first schema change since 4.0.</p><p>Three nullable columns on <code>QRTZ_TRIGGERS</code> carry a <em>conditional continuation</em>: a trigger that waits, in the store, for another trigger&#39;s firing to end (<a href="https://github.com/quartznet/quartznet/issues/3805" target="_blank" rel="noopener noreferrer">#3805</a>).</p><table><thead><tr><th>Column</th><th>What it holds</th></tr></thead><tbody><tr><td><code>CONTINUES_TRIGGER_NAME</code></td><td>The name of the trigger whose firing this one waits for</td></tr><tr><td><code>CONTINUES_TRIGGER_GROUP</code></td><td>That trigger&#39;s group</td></tr><tr><td><code>CONTINUATION_CONDITION</code></td><td>The outcomes that release the wait, as flags (below)</td></tr></tbody></table><p><code>CONTINUATION_CONDITION</code> flags: <code>1</code> on success, <code>2</code> on failure, <code>4</code> on cancellation, <code>8</code> on veto, <code>15</code> however it ends. A row that names a parent but has no condition reads as <code>15</code>.</p><ul><li>The waiting trigger is in a new <code>TRIGGER_STATE</code>, <code>AWAITING</code>, and is never acquired while there.</li><li>The parent&#39;s completion resolves it inside the parent&#39;s own lock and transaction. An outcome the condition names releases the trigger into the ordinary schedule, with its next fire time the later of &quot;now&quot; and its own <code>START_TIME</code>. Any other outcome deletes it.</li><li>The node that ran the parent resolves it, so a continuation survives the failure of the node that scheduled it.</li><li>All three columns are nullable with no default, so existing rows are valid at once and nothing needs backfilling.</li><li>No index is added. The lookup is <code>SCHED_NAME = ? AND TRIGGER_STATE = &#39;AWAITING&#39; AND CONTINUES_TRIGGER_NAME = ? AND CONTINUES_TRIGGER_GROUP = ?</code>; its two leading equality columns are what <code>IDX_QRTZ_T_NFT_ST</code> already leads with, and <code>AWAITING</code> is a small partition even in a schedule that uses continuations heavily. Measure before adding one.</li></ul><h3 id="rolling-4-1-→-4-2" tabindex="-1"><a class="header-anchor" href="#rolling-4-1-→-4-2"><span>Rolling 4.1 → 4.2</span></a></h3><p><strong>Run the migration while 4.1 nodes are still up.</strong> A 4.1 node&#39;s trigger <code>INSERT</code> names its own columns, its acquisition and misfire sweeps select <code>WAITING</code>, and its cluster recovery touches <code>ACQUIRED</code> and <code>BLOCKED</code>, so nothing it does on its own picks up an <code>AWAITING</code> row. But it reads an unknown state string as waiting:</p><ul><li>it <em>reports</em> an <code>AWAITING</code> row as <code>Normal</code>;</li><li>its single-trigger <code>PauseTrigger</code> writes <code>PAUSED</code> over one, which a resume then releases without its parent;</li><li>its reschedule rewrites one as an ordinary trigger.</li></ul><p>A 4.1 node cannot <strong>resolve</strong> a continuation at all: a parent completing there leaves the triggers waiting on that firing where they are. So while any 4.1 node runs, do not pause, resume or reschedule a continuation from it, and:</p><ol><li>Run <code>add_continuations_&lt;db&gt;.sql</code>.</li><li>Roll every node to 4.2.</li><li><em>Then</em> start scheduling continuations.</li></ol><p>A 4.2 node refuses to start against a database without this migration, because it names all three columns in the statement that stores every trigger. The startup check says which column and which script.</p><p><code>ProvisionSchema()</code> does not help here: it creates missing tables and never adds a column to an existing table. Run the script.</p><h3 id="the-execution-history-tables" tabindex="-1"><a class="header-anchor" href="#the-execution-history-tables"><span>The execution history tables</span></a></h3><p><strong>Optional.</strong> <code>add_execution_history_&lt;db&gt;.sql</code> creates two tables that only <code>UsePersistentStore(store =&gt; store.UseExecutionHistory())</code> reads or writes (<a href="https://github.com/quartznet/quartznet/issues/3771" target="_blank" rel="noopener noreferrer">#3771</a>):</p><table><thead><tr><th>Table</th><th>One row per</th></tr></thead><tbody><tr><td><code>QRTZ_EXECUTION_HISTORY</code></td><td>Finished execution</td></tr><tr><td><code>QRTZ_MISFIRE_HISTORY</code></td><td>Missed firing</td></tr></tbody></table><ul><li><strong><code>QRTZ_EXECUTION_HISTORY</code></strong> holds the node that ran it, the job and trigger, when it fired, how long it took in ticks, whether it threw, what it said, which attempt at the occurrence it was (<code>RETRY_ATTEMPT</code>) and whether the trigger answered it with another (<code>RETRY_SCHEDULED</code>).</li><li><strong><code>QRTZ_MISFIRE_HISTORY</code></strong> holds the trigger, the node that noticed, when it noticed, and the firing that was missed. Nothing ran, so there is no duration or outcome; that is why it is a separate table.</li><li>Both are keyed by <code>(SCHED_NAME, ENTRY_ID)</code>. The store writes <code>ENTRY_ID</code> exactly as it writes <code>QRTZ_FIRED_TRIGGERS.ENTRY_ID</code>, because no two dialects spell an identity column the same way, and nothing reads the number.</li><li>Neither table has a foreign key, since a history row outlives the trigger and job it names. No other statement in the schema mentions them.</li><li>Each has two indexes: <code>(SCHED_NAME, &lt;time&gt;)</code> for the age query and the retention sweep, and <code>(SCHED_NAME, INSTANCE_NAME)</code> for the dashboard&#39;s node filter.</li><li>A search by job or trigger name is a scan, deliberately: it lowercases the key to match the in-memory history, which no index can serve, and the feed it searches is bounded by the retention window. <code>RETRY_ATTEMPT</code> and <code>RETRY_SCHEDULED</code> are not indexed either: the &quot;failed after retries&quot; filter (<code>SUCCEEDED = 0 AND RETRY_SCHEDULED = 0</code>) runs over the same bounded feed.</li></ul><p>When you need it:</p><ul><li>A scheduler that keeps no history never probes for these tables, so a database created by 4.0 or 4.1 keeps working untouched.</li><li>A store configured for history refuses to start without them, naming this script.</li><li>A fresh install from <code>database/tables/</code> creates them either way, and so does <code>ProvisionSchema()</code>. This is the one 4.2 script provisioning can replace, because what is missing is whole tables, not columns of an existing one.</li></ul><p>It is safe in a mixed cluster in both directions. A 4.0 or 4.1 node cannot see these tables, and a 4.2 node that does not ask for history neither writes nor reads them. The nodes that do ask share one history, and every row carries the instance id that produced it.</p>`,57),e(`p`,null,[l[23]||=r(`The store trims both tables itself, to `,-1),l[24]||=e(`code`,null,`ExecutionHistoryOptions.Retention`,-1),l[25]||=r(` (24 hours by default) and `,-1),l[26]||=e(`code`,null,`MaxEntriesPerScheduler`,-1),l[27]||=r(` (2,000); see `,-1),c(m,{to:`/documentation/quartz-4.x/tutorial/job-stores.html#execution-history-in-the-database`},{default:i(()=>[...l[22]||=[r(`the persistent store's execution history`,-1)]]),_:1}),l[28]||=r(`.`,-1)]),l[38]||=t(`<h2 id="version-4-3" tabindex="-1"><a class="header-anchor" href="#version-4-3"><span>Version 4.3</span></a></h2><p>Two scripts; only the first is mandatory (<a href="https://github.com/quartznet/quartznet/issues/3874" target="_blank" rel="noopener noreferrer">#3874</a>).</p><table><thead><tr><th>Script</th><th>Status</th><th>Adds</th></tr></thead><tbody><tr><td><a href="https://github.com/quartznet/quartznet/tree/main/database/migrations/4.3" target="_blank" rel="noopener noreferrer"><code>migrations/4.3/add_fire_progress_&lt;db&gt;.sql</code></a></td><td><strong>Mandatory</strong> for 4.3 and later</td><td><code>PROGRESS</code>, <code>PROGRESS_MESSAGE</code> on <code>QRTZ_FIRED_TRIGGERS</code></td></tr><tr><td><a href="https://github.com/quartznet/quartznet/tree/main/database/migrations/4.3" target="_blank" rel="noopener noreferrer"><code>migrations/4.3/add_execution_log_&lt;db&gt;.sql</code></a></td><td>Optional: only with <code>UseExecutionHistory()</code></td><td><code>EXECUTION_LOG</code> on <code>QRTZ_EXECUTION_HISTORY</code></td></tr></tbody></table><h3 id="the-progress-columns" tabindex="-1"><a class="header-anchor" href="#the-progress-columns"><span>The progress columns</span></a></h3><table><thead><tr><th>Column</th><th>What it holds</th></tr></thead><tbody><tr><td><code>PROGRESS</code></td><td>The percentage, 0 to 100, a running job last passed to <code>ReportProgress</code></td></tr><tr><td><code>PROGRESS_MESSAGE</code></td><td>The message it passed with it, truncated to 250 characters</td></tr></tbody></table><ul><li>Written at most once a second per firing, and only when the value changes, by <code>ENTRY_ID</code>. The row is the writing node&#39;s own, so no lock is taken.</li><li>Oracle declares <code>PROGRESS_MESSAGE</code> as <code>VARCHAR2(1000)</code>: <code>VARCHAR2</code> counts bytes, and 250 characters of UTF-8 can take 1,000.</li><li>Read by the fire-instance listing, which is what the dashboard&#39;s Currently Executing page shows cluster-wide.</li><li>No index: the write is by primary key.</li><li>A 4.3 node refuses to start without them; the startup check names the column and the script.</li></ul><h3 id="the-execution-log-column" tabindex="-1"><a class="header-anchor" href="#the-execution-log-column"><span>The execution log column</span></a></h3><p><code>EXECUTION_LOG</code> holds the log lines an execution wrote, captured by <code>UseExecutionLogCapture()</code>. A large object on every dialect: <code>nvarchar(max)</code>, <code>TEXT</code>, <code>LONGTEXT</code>, <code>CLOB</code>, <code>BLOB SUB_TYPE TEXT</code>.</p><ul><li>The history listing never selects it; only the read of one entry does.</li><li>A store configured with <code>UseExecutionHistory()</code> refuses to start without it. No other store probes for it.</li><li>Run it only on a database that has <code>QRTZ_EXECUTION_HISTORY</code>: it alters that table, and fails where the table is missing. A database without the table runs <code>4.2/add_execution_history_&lt;db&gt;.sql</code> first.</li></ul><h3 id="rolling-4-2-→-4-3" tabindex="-1"><a class="header-anchor" href="#rolling-4-2-→-4-3"><span>Rolling 4.2 → 4.3</span></a></h3><p>Run both scripts while 4.2 nodes are still up. Every column is nullable with no default, and a 4.2 node never names them: its firings read as having reported no progress, and its history rows carry no log.</p><p>A fresh install from <code>database/tables/</code>, and <code>ProvisionSchema()</code>, create all three columns.</p><h2 id="see-also" tabindex="-1"><a class="header-anchor" href="#see-also"><span>See also</span></a></h2>`,13),e(`ul`,null,[l[33]||=e(`li`,null,[e(`a`,{href:`https://github.com/quartznet/quartznet/blob/main/database/README.md`,target:`_blank`,rel:`noopener noreferrer`},[e(`code`,null,`database/README.md`)]),r(`: the same table, in the repository`)],-1),e(`li`,null,[c(m,{to:`/documentation/quartz-3.x/db/`},{default:i(()=>[...l[29]||=[r(`Database Schema`,-1)]]),_:1}),l[30]||=r(`: what each table holds`,-1)]),e(`li`,null,[c(m,{to:`/documentation/quartz-4.x/migration-guide.html`},{default:i(()=>[...l[31]||=[r(`Migration Guide`,-1)]]),_:1}),l[32]||=r(`: the rest of the 3.x → 4.x upgrade`,-1)])])])}var f=s(u,[[`render`,d]]);export{l as _pageData,f as default};