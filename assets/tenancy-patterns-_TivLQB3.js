import{a as e,c as t,d as n,l as r,m as i,p as a,s as o,t as s,u as c}from"./app-BsOuDHI-.js";var l=JSON.parse(`{"path":"/documentation/tenancy-patterns.html","title":"Tenancy Patterns","lang":"en-US","frontmatter":{"title":"Tenancy Patterns"},"git":{"updatedTime":1788441191000,"contributors":[{"name":"Marko Lahma","username":"","email":"marko.lahma@gmail.com","commits":1},{"name":"Claude Fable 5.1","username":"","email":"noreply@anthropic.com","commits":1}],"changelog":[{"hash":"8dee9f767ead84b6e47c023533864d1d3e3178ae","time":1788441191000,"email":"marko.lahma@gmail.com","author":"Marko Lahma","message":"The release post carries the day 4.0.0 shipped","coAuthors":[{"name":"Claude Fable 5.1","email":"noreply@anthropic.com"}]}]},"filePathRelative":"documentation/tenancy-patterns.md"}`),u={name:`tenancy-patterns.md`};function d(s,l,u,d,f,p){let m=a(`RouteLink`);return n(),o(`div`,null,[l[22]||=e(`h1`,{id:`tenancy-patterns`,tabindex:`-1`},[e(`a`,{class:`header-anchor`,href:`#tenancy-patterns`},[e(`span`,null,`Tenancy Patterns`)])],-1),l[23]||=e(`p`,null,[r(`Quartz.NET has no `),e(`code`,null,`Tenant`),r(` type and is not going to get one. What it has are three separations you can build one out of — a scheduler, a group, and a `),e(`code`,null,`SCHED_NAME`),r(` — and the hard part is not wiring any of them up. It is deciding which one you need before you have twelve tenants in production and a migration to write.`)],-1),l[24]||=e(`p`,null,`This page is about that decision. It surveys how other schedulers and job systems partition tenants, names the axes that actually decide the model, and maps those axes onto the mechanisms Quartz.NET really has. The mechanics live in the per-version guides:`,-1),e(`ul`,null,[e(`li`,null,[c(m,{to:`/documentation/quartz-4.x/multi-tenancy.html`},{default:i(()=>[...l[0]||=[r(`Multi-Tenancy (Quartz 4.x)`,-1)]]),_:1})]),e(`li`,null,[c(m,{to:`/documentation/quartz-3.x/multi-tenancy.html`},{default:i(()=>[...l[1]||=[r(`Multi-Tenancy (Quartz 3.x)`,-1)]]),_:1})])]),l[25]||=t(`<h2 id="isolation-is-not-authorization-and-partitioning-is-not-isolation" tabindex="-1"><a class="header-anchor" href="#isolation-is-not-authorization-and-partitioning-is-not-isolation"><span>Isolation is not authorization, and partitioning is not isolation</span></a></h2><p>Three points decide most arguments about tenancy, and all are worth getting straight before looking at any mechanism.</p><p>The first is that <strong>tenant isolation is not the same thing as authentication and authorization</strong>. AWS&#39;s SaaS guidance is unusually blunt about it: &quot;the fact that a tenant user is authenticated does not mean that your system has achieved isolation … a user could be authenticated and authorized, and still access the resources of another tenant.&quot; Isolation is the separate, enforced use of tenant context to bound which resources a request can reach at all.</p><p>The second is that <strong>partitioning is not isolation</strong>. Putting a tenant id in a column, a key prefix or a group name arranges your data by tenant. It does not by itself stop code from reading across the boundary. Every system in the survey below distinguishes a partition it merely <em>names</em> from a boundary it actually <em>enforces</em>, and the ones that blur the two are the ones whose users get hurt.</p><p>The third thing worth internalising is that isolation is not one choice. Microsoft&#39;s guidance says to treat it as a spectrum rather than a discrete property, and that &quot;you can use different levels of isolation for each tier&quot;. AWS says the same thing from the other direction: under its bridge model, &quot;your view of silo and pool will be much more granular for environments that are decomposed into a collection of services that have varying isolation requirements&quot;, and you should be &quot;thinking about the tradeoffs of silo and pool models for each resource or layer of your architecture.&quot;</p><p>For a scheduler that means the interesting question is rarely &quot;siloed or pooled?&quot; It is &quot;which of the scheduling loop, the thread pool, the job store and the database is siloed, and which is pooled?&quot; Those four can be answered differently, and usually should be.</p><h3 id="two-vocabularies-for-the-same-thing" tabindex="-1"><a class="header-anchor" href="#two-vocabularies-for-the-same-thing"><span>Two vocabularies for the same thing</span></a></h3><p>The two most-cited bodies of guidance use different words. They line up well enough to translate, and knowing both is useful because the literature you find will use one or the other.</p><table><thead><tr><th>AWS</th><th>Microsoft</th><th>Meaning</th></tr></thead><tbody><tr><td>Silo</td><td>Automated single-tenant deployments</td><td>Each tenant gets a dedicated stack</td></tr><tr><td>Pool</td><td>Fully multitenant deployments</td><td>Every tenant shares one set of infrastructure</td></tr><tr><td>Bridge</td><td>Vertically partitioned deployments</td><td>Some tenants siloed, some pooled — usually a premium tier</td></tr><tr><td>Bridge (applied per layer)</td><td>Horizontally partitioned deployments</td><td>Shared compute, dedicated data store per tenant</td></tr><tr><td>Cell</td><td>Deployment stamp</td><td>A whole copy of the system serving a bounded set of tenants</td></tr></tbody></table><p>The last row is the one people conflate with silo. A cell or stamp is <strong>not</strong> one tenant — it holds many. Microsoft says a stamp is &quot;sometimes a <em>service unit</em>, <em>scale unit</em>, or <em>cell</em>&quot;, and that &quot;each stamp serves a predefined number of tenants&quot;. AWS frames cells through the bulkhead metaphor: &quot;If a workload uses 10 cells to service 100 requests, when a failure occurs in one cell, 90% of the overall requests would be unaffected.&quot; Silo bounds <em>access</em>; a cell bounds <em>blast radius</em>. You can want one without the other.</p><h2 id="how-other-systems-partition-tenants" tabindex="-1"><a class="header-anchor" href="#how-other-systems-partition-tenants"><span>How other systems partition tenants</span></a></h2><p>The pattern across the field is consistent enough to state up front: <strong>almost every system&#39;s tenancy primitive is a naming, configuration and access boundary, and almost none of them make it a fairness boundary.</strong> Quotas and rate limits usually attach one level up — to the account, the cluster or the process — and where a system does give per-tenant fairness, it is frequently the paid tier.</p><table><thead><tr><th>System</th><th>Primitive</th><th>Enforced by</th><th>Quota attaches to</th><th>Tenant added at runtime?</th></tr></thead><tbody><tr><td>Temporal</td><td>Namespace</td><td>Server</td><td>Namespace <em>and</em> cluster</td><td>Yes, <code>RegisterNamespace</code></td></tr><tr><td>Cadence</td><td>Domain</td><td>Server</td><td>Domain and task list</td><td>Yes</td></tr><tr><td>Kubernetes <code>CronJob</code></td><td>Namespace</td><td>API server + RBAC</td><td>Namespace, via <code>ResourceQuota</code></td><td>Yes, one API call</td></tr><tr><td>AWS EventBridge Scheduler</td><td>Schedule group</td><td>IAM, via the ARN</td><td><strong>Account and Region, never the group</strong></td><td>Yes, <code>CreateScheduleGroup</code></td></tr><tr><td>Azure Durable Functions</td><td>Task hub</td><td>Storage naming, or RBAC on the managed provider</td><td>Scheduler resource, not the hub</td><td>Yes, implicitly</td></tr><tr><td>HashiCorp Nomad</td><td>Namespace</td><td>Server + ACL</td><td>Namespace — but quotas are Enterprise-only</td><td>Yes</td></tr><tr><td>Google Cloud Scheduler</td><td>Project</td><td>IAM</td><td>Project</td><td>Only by creating a project</td></tr><tr><td>Hangfire</td><td>Queue, server, storage</td><td>Convention</td><td>Server process</td><td>Storage yes, queue set no</td></tr><tr><td>Celery</td><td>Queue, broker vhost</td><td>Broker</td><td><strong>Worker process, not the cluster</strong></td><td>Yes, <code>add_consumer</code></td></tr><tr><td>Sidekiq</td><td>Queue, Redis instance</td><td>Convention</td><td>Process, or capsule</td><td>Queue set fixed at start</td></tr><tr><td>Airflow</td><td>Team, pool, queue</td><td>Pools: the metadata database. Teams: the API layer</td><td>Pool, cluster-wide</td><td>A pool yes; a team needs new processes</td></tr><tr><td>Quartz (Java)</td><td><code>instanceName</code>, group</td><td><code>SCHED_NAME</code> in every statement</td><td>Scheduler, not the group</td><td>Yes, <code>DirectSchedulerFactory</code></td></tr></tbody></table><p>A few of these repay a closer look, because each teaches something the table cannot.</p><h3 id="temporal-ranks-namespace-per-tenant-last" tabindex="-1"><a class="header-anchor" href="#temporal-ranks-namespace-per-tenant-last"><span>Temporal ranks namespace-per-tenant last</span></a></h3><p>Temporal&#39;s Namespace is the textbook tenancy primitive: &quot;A Namespace is a unit of isolation within the Temporal Platform.&quot; Workflow id uniqueness is scoped to it, task queues belong to it, and retention, archival, replication and rate limits are configured on it. It is genuinely server-enforced, and a tenant is one <code>RegisterNamespace</code> call away.</p><p>Temporal nevertheless tells you not to use it that way. Its own multi-tenant patterns guidance ranks four approaches and puts namespace-per-tenant <strong>fourth</strong>, describing it as &quot;Only practical for a smaller number of high-value tenants&quot;, manageable &quot;for fewer than 50 tenants&quot;, and &quot;not a good fit if you expect a very large number of tenants (10,000+)&quot;. The recommended pattern is <strong>task queue per tenant</strong> — that is, a client-side naming convention — scaling to &quot;thousands of tenants per Namespace&quot;, with a <code>TenantId</code> search attribute for querying. Their own summary: &quot;For most SaaS use cases, a shared Namespace with per-tenant Task Queues is simpler and more scalable.&quot;</p><p>The reason is the part the marketing does not cover. Namespaces share the persistence store and the history shard set; the shard is chosen by hashing workflow id <em>and</em> namespace, so namespaces are an input to the hash rather than a partition of it. The shard count &quot;cannot be changed&quot; after the database is integrated. And in the open-source server there is no built-in RBAC at all: without an <code>Authorizer</code> of your own, Temporal &quot;allows every API request, with no authentication or access control&quot;, which means namespace-per-tenant in OSS is a security boundary only if you write the enforcement yourself.</p><p>Cadence, from which Temporal forked, reaches the same conclusion from the other side: its engineering blog states plainly that &quot;Shards are shared across the different domains in a Cadence cluster&quot;, and it added per-workflow-id rate limits precisely because the domain was not a fine enough unit.</p><h3 id="kubernetes-documents-what-its-namespace-does-not-isolate" tabindex="-1"><a class="header-anchor" href="#kubernetes-documents-what-its-namespace-does-not-isolate"><span>Kubernetes documents what its namespace does not isolate</span></a></h3><p>The Kubernetes multi-tenancy documentation is the model for the register this page tries to hit. It recommends namespaces and then immediately lists what they fail to cover: namespace isolation &quot;doesn&#39;t apply to Kubernetes resources that can&#39;t be namespaced, such as Custom Resource Definitions, Storage Classes, and Webhooks&quot;; a <code>PersistentVolume</code> &quot;is a cluster-wide resource and has a lifecycle independent of workloads and namespaces&quot;; &quot;By default, the Kubernetes DNS service allows lookups across all namespaces&quot;; and &quot;Quotas cannot protect against all kinds of resource sharing, such as network traffic.&quot;</p>`,21),e(`p`,null,[l[3]||=r(`Two of those leaks have direct analogues in a scheduler. The control plane is shared, so there is one `,-1),l[4]||=e(`code`,null,`CronJob`,-1),l[5]||=r(` controller for every tenant in the cluster; and `,-1),l[6]||=e(`code`,null,`PriorityClass`,-1),l[7]||=r(` is cluster-scoped, so a paying tenant's pods can preempt a free tenant's across the namespace boundary. Kubernetes is also explicit that `,-1),l[8]||=e(`code`,null,`CronJob`,-1),l[9]||=r(` delivery is not exactly-once — "the Jobs that you define should be `,-1),l[10]||=e(`em`,null,`idempotent`,-1),l[11]||=r(`" — which is the same advice Quartz.NET's `,-1),c(m,{to:`/documentation/best-practices.html`},{default:i(()=>[...l[2]||=[r(`best practices`,-1)]]),_:1}),l[12]||=r(` give about recoverable jobs, and for the same reason.`,-1)]),l[26]||=t(`<p>Where namespaces are not enough, the escalation ladder Kubernetes describes is a virtual control plane per tenant, and past that a dedicated cluster. That ladder — shared partition, then per-tenant quota, then per-tenant control plane, then separate deployment — is the same one this page ends on.</p><h3 id="eventbridge-scheduler-s-group-is-a-billing-boundary-not-a-fairness-one" tabindex="-1"><a class="header-anchor" href="#eventbridge-scheduler-s-group-is-a-billing-boundary-not-a-fairness-one"><span>EventBridge Scheduler&#39;s group is a billing boundary, not a fairness one</span></a></h3><p>AWS EventBridge Scheduler has an explicit <em>schedule group</em> primitive, and the group is embedded in every schedule&#39;s ARN, which makes it a natural IAM boundary. It is also the only thing you can tag: &quot;With EventBridge Scheduler, you organize schedule <strong>groups</strong>, instead of individual schedules, by applying tags.&quot; So it is the unit of cost allocation and the only dimension on the service&#39;s CloudWatch metrics.</p><p>What it is not is a quota. Every published quota — 10,000,000 schedules, 500 schedule groups, the 1,000 TPS invocation throttle — is per account and Region. A tenant that floods its own group consumes the same invocation budget as every other tenant. The group names and authorizes; it does not ration.</p><h3 id="azure-s-task-hub-is-a-sched-name-by-another-name" tabindex="-1"><a class="header-anchor" href="#azure-s-task-hub-is-a-sched-name-by-another-name"><span>Azure&#39;s task hub is a <code>SCHED_NAME</code> by another name</span></a></h3><p>Azure Durable Functions partitions by <em>task hub</em>, and on the SQL backend the implementation is strikingly close to Quartz&#39;s: &quot;each table includes a <code>TaskHub</code> column as part of its primary key.&quot; On the Azure Storage backend the hub name is a prefix on queue, table and blob names in a shared storage account, which means RBAC granularity is the storage account and not the hub.</p><p>Microsoft&#39;s warning about sharing one is the sharpest statement any vendor in this survey makes: &quot;If multiple apps use the same task hub, they compete for messages, which can result in undefined behavior — including orchestrations getting unexpectedly stuck.&quot; Quartz.NET users will recognise the failure mode; it is exactly what happens when two unrelated schedulers are given the same <code>SCHED_NAME</code>. Microsoft&#39;s mitigation is to make the safe thing the default: the hub name is derived from the app name so that &quot;accidental sharing doesn&#39;t happen.&quot;</p><h3 id="airflow-spent-a-decade-discovering-where-the-boundary-belongs" tabindex="-1"><a class="header-anchor" href="#airflow-spent-a-decade-discovering-where-the-boundary-belongs"><span>Airflow spent a decade discovering where the boundary belongs</span></a></h3><p>Airflow is the field&#39;s longest-running attempt to retrofit tenancy onto a scheduler, and the outcome is the single best argument for the position this page takes.</p><p>AIP-1 proposed multi-tenancy in 2016, diagnosing that &quot;every task has full access to the Airflow database including connection details like usernames, passwords etc&quot;. It was eventually marked superseded by four successors. AIP-43 shipped in Airflow 2.4, separating DAG-file parsing from the scheduler. AIP-44 aimed to keep workers off the metadata database behind an internal API, shipped experimentally in 2.10 — and was then deleted wholesale, in favour of AIP-72&#39;s Task SDK, which landed in Airflow 3.0 and finally established that &quot;in Airflow 3 direct DB access from workers will not be allowed at all&quot;.</p><p>What shipped after all that is called <strong>multi-team</strong>, not multi-tenant, and its own documentation is careful about the difference: it &quot;provides <em>logical isolation</em> for a secure perimeter around teams, not complete isolation. All teams share the same metadata database and common Airflow infrastructure. For absolutely strict security requirements, consider separate Airflow deployments.&quot; The giveaway detail is that &quot;Dag IDs, Variable keys, and Connection IDs must be unique across the entire Airflow deployment, regardless of which team owns them&quot; — a single flat namespace, which is a naming convention by definition.</p><p>The two halves are worth separating, because Airflow got one of them right and it is the half Quartz.NET users ask about. <strong>Pools are real.</strong> A pool&#39;s concurrency bound is enforced in the metadata database, with a row lock over the pool table, so it holds across multiple schedulers — genuinely cluster-wide. <strong>Identity is not.</strong> Airflow&#39;s security model states that &quot;All Dag authors have access to all Dags in the Airflow deployment&quot; and that DAG authors &quot;should be trusted&quot;.</p><p>Note also the trap in the neighbouring setting: a <em>pool</em> binds cluster-wide, while <code>[core] parallelism</code> binds &quot;per scheduler&quot;. Two knobs that read as siblings, at different scopes. That is exactly the confusion Quartz.NET&#39;s execution limits invite, which is why this page states their scope in the first sentence that mentions them.</p><h3 id="where-the-limit-binds-celery-hangfire-and-the-per-worker-trap" tabindex="-1"><a class="header-anchor" href="#where-the-limit-binds-celery-hangfire-and-the-per-worker-trap"><span>Where the limit binds: Celery, Hangfire, and the per-worker trap</span></a></h3><p>The single most transferable lesson in this survey concerns <em>where</em> a concurrency or rate limit is counted, because it is the one everybody gets wrong.</p><p>Celery&#39;s documentation is admirably direct about it: &quot;Note that this is a <em>per worker instance</em> rate limit, and not a global rate limit. To enforce a global rate limit … you must restrict to a given queue.&quot; A <code>rate_limit</code> of 10/s across four workers is 40/s.</p><p>Quartz.NET&#39;s execution limits have exactly this property by default, for exactly this reason: the running count lives in memory in the scheduler. On 4.x a limit can opt out of it — declared <code>ExecutionLimitScope.Cluster</code>, it is counted from the fired-triggers table instead, which is the same move Airflow&#39;s pools make and the one Hangfire charges for. See <a href="#what-quartz-net-does-not-give-you">what Quartz.NET does not give you</a>.</p><p>The contrast is Hangfire. Its free tier has no per-queue or per-tenant cap either — worker count is per server — but <code>Hangfire.Throttling</code>, part of the paid <strong>Hangfire Ace</strong> set, adds mutexes, semaphores and fixed/sliding/dynamic window rate limiters, throttling by rescheduling a job rather than blocking a worker. A throttler&#39;s scope is its <em>storage&#39;s</em> scope: on SQL Server or Redis the limit therefore holds across every server sharing that storage, and on the in-memory storage it collapses back to one process. Even so the docs are careful — &quot;Everything works on a best-effort basis,&quot; because &quot;it&#39;s very hard to achieve it due to the complexity of distributed processing.&quot;</p><p>Hangfire&#39;s own documentation pitches one of these at tenancy explicitly, and it is worth noting that it is the paid one: dynamic window counters give &quot;some kind of fair processing, where one participant can&#39;t capture all the available resources that&#39;s especially useful for multi-tenant applications.&quot;</p><p>Hangfire also shows what happens when the queue is pressed into service as the tenant boundary. A server&#39;s queue list is fixed when it starts, so a tenant that gets its own queue costs a restart — the request to make that dynamic has been open since 2017, filed by someone describing exactly this scenario. And on SQL Server the fetch statement has no <code>ORDER BY</code> at all; ordering falls out of an index on the queue name, so tenants are served alphabetically and a busy <code>acme</code> can starve <code>zeta</code>. Ordering that emerges from an index is not a fairness policy.</p><p>BullMQ splits the same way, and its history is instructive. The open-source limiter is genuinely global — &quot;The rate limiter is global, so if you have for example 10 workers for one queue with the above settings, still only 10 jobs will be processed by second&quot; — but per-<em>group</em> limiting was removed: &quot;From BullMQ 3.0 and onwards, group keys support is removed to improve global rate limit.&quot; Per-tenant fairness came back as <strong>Groups</strong> in the paid BullMQ Pro, motivated by precisely the SaaS case: &quot;one user could fill the queue with jobs and the rest of the users will need to wait.&quot;</p><h3 id="upstream-quartz-has-no-tenancy-guidance-and-that-is-informative" tabindex="-1"><a class="header-anchor" href="#upstream-quartz-has-no-tenancy-guidance-and-that-is-informative"><span>Upstream Quartz has no tenancy guidance, and that is informative</span></a></h3><p>Java Quartz — the project Quartz.NET is a port of — has the same three separations, and says nothing about tenancy at all. There is no page, no cookbook entry and no FAQ answer; the only issue asking how to handle a database per tenant was closed by a stale bot without an answer. <code>instanceName</code> is documented as a way &quot;to distinguish schedulers when multiple instances are used within the same program&quot;, and <code>tablePrefix</code> as a way to &quot;have multiple sets of Quartz&#39;s tables within the same database&quot;. Neither mentions a tenant.</p><p>Groups are documented as categorisation and nothing more — &quot;useful for organizing your jobs and triggers into categories such as &#39;reporting jobs&#39; and &#39;maintenance jobs&#39;&quot;. There is no per-group concurrency limit anywhere in Java Quartz; the thread pool is scheduler-wide and <code>@DisallowConcurrentExecution</code> is scoped to a job key. Upstream&#39;s answer to &quot;different concurrency for different sets of jobs&quot; is a second scheduler.</p><p>The one place upstream does recommend partitioning, it is for a reason that has nothing to do with tenants and everything to do with the shared lock: &quot;If you need to scale out to support thousands of short-running (e.g 1 second) jobs, consider partitioning the set of jobs by using multiple distinct schedulers … The scheduler makes use of a cluster-wide lock, a pattern that degrades performance as you add more nodes (when going beyond about three nodes …).&quot; That is a throughput argument, and it happens to point the same way as the tenancy one.</p><p>Also worth knowing if you lean on group matchers: Java Quartz warns that <code>pauseTriggers(GroupMatcher)</code> has &quot;a limitation that only exactly matched groups can be remembered as paused&quot;, so pausing by prefix does not keep newly-added triggers paused. Quartz.NET records the groups a prefix <em>matched</em>, so those keep imposing the pause; a group that held nothing when the prefix ran was never matched and so is not one of them. Job group pause state carries a second caveat: 3.x&#39;s ADO store does not persist it at all, and 4.x persists it but still does not impose it on jobs added after the pause. Both per-version guides therefore tell you to suspend a tenant by <em>trigger</em> group.</p><h3 id="sidekiq-removed-its-namespaces-on-purpose" tabindex="-1"><a class="header-anchor" href="#sidekiq-removed-its-namespaces-on-purpose"><span>Sidekiq removed its namespaces on purpose</span></a></h3><p>Sidekiq supported Redis key namespacing through the <code>redis-namespace</code> gem and removed it in 7.0. The upgrade guide is terse — &quot;Support for <code>redis-namespace</code> has been removed&quot;, &quot;I have advised against its usage for many years now&quot; — and the reasoning is on the maintainer&#39;s blog: namespacing &quot;increases the size of every key by the size of the prefix&quot;, and it means &quot;you don&#39;t get to tune Redis for the individual needs of &#39;cache&#39; and &#39;transactional&#39;.&quot; His verdict on the practice was that it suits &quot;hobbyists only who only want to pay for a single Redis database from a SaaS; you do not want to build a business on top of this hack.&quot; The Sidekiq wiki&#39;s current line: &quot;Redis namespaces do not allow for this configuration and come with many other problems, so using discrete Redis instances is always preferred.&quot;</p><p>The transferable point is not &quot;prefixes are bad&quot;. It is that a prefix bought naming and nothing else — no separate tuning, no separate durability policy, no separate failure domain — while adding cost to every operation. A partition that gives you no new <em>capability</em> is usually not worth its complexity. This is worth holding in mind when reaching for a per-tenant table prefix, which has the same shape.</p><h2 id="the-axes-that-decide" tabindex="-1"><a class="header-anchor" href="#the-axes-that-decide"><span>The axes that decide</span></a></h2><p>Walk your own situation down these. They are ordered roughly by how often they turn out to be the deciding one.</p><table><thead><tr><th>Axis</th><th>Ask yourself</th><th>Pushes toward shared</th><th>Pushes toward dedicated</th></tr></thead><tbody><tr><td>Onboarding cadence</td><td>Does a tenant appear while the process runs?</td><td>Runtime arrival</td><td>Tenants known at deploy time</td></tr><tr><td>Tenant count</td><td>How many, and how skewed?</td><td>Hundreds or thousands</td><td>Tens, or a few whales</td></tr><tr><td>Blast radius</td><td>What is the cost of one bad tenant taking everything down?</td><td>Tolerable</td><td>Unacceptable</td></tr><tr><td>Noisy neighbours</td><td>Can one tenant&#39;s work starve another&#39;s?</td><td>Workloads are small and similar</td><td>Workloads are bursty or heavy</td></tr><tr><td>Per-tenant quotas and SLAs</td><td>Do you sell tiers with different guarantees?</td><td>One tier</td><td>Contractual per-tenant limits</td></tr><tr><td>Data residency</td><td>Must a tenant&#39;s data live somewhere specific, or under its own key?</td><td>No constraint</td><td>Regulated or sovereign data</td></tr><tr><td>Cost per idle tenant</td><td>What does a tenant cost when it schedules nothing?</td><td>Must be ~zero</td><td>Amortised by the contract</td></tr><tr><td>Customisation</td><td>Do tenants differ in configuration, calendars, or schema?</td><td>Uniform</td><td>Divergent</td></tr><tr><td>Observability</td><td>Must you answer &quot;what is tenant X doing right now?&quot;</td><td>Group dimension is enough</td><td>Needs its own dashboards</td></tr></tbody></table><h3 id="onboarding-cadence-decides-more-than-anything-else" tabindex="-1"><a class="header-anchor" href="#onboarding-cadence-decides-more-than-anything-else"><span>Onboarding cadence decides more than anything else</span></a></h3><p>This is the axis to evaluate first, because on most platforms it eliminates an option outright rather than trading off against one.</p><p>Runtime tenant arrival is cheap where the primitive is a row or an API call and expensive where it is a deployment. Temporal registers a namespace over gRPC, though registration &quot;takes up to 10 seconds to complete&quot; and the API is itself rate-limited. Kubernetes creates a namespace instantly — but a <em>usable</em> tenant is a namespace plus RBAC plus a <code>ResourceQuota</code> plus a <code>LimitRange</code> plus a <code>NetworkPolicy</code>, and the docs concede that this &quot;requires configuration of several other Kubernetes resources&quot;. Celery can tell a running worker to consume a new queue with <code>add_consumer</code>; Hangfire and Sidekiq fix a server&#39;s queue set when it starts.</p><p>AWS&#39;s silo model is where the cost shows: onboarding a siloed tenant &quot;will require the provisioning of new infrastructure and, potentially, the configuration of new account limits&quot;, which makes what was one database insert into a deployment.</p><p>In Quartz.NET the same split appears between the group-per-tenant model, where onboarding is writing a trigger, and the scheduler-per-tenant model, where it is closer to a deployment. It need not be a <em>redeploy</em> on either version — see <a href="#onboarding-a-tenant-while-the-process-runs">onboarding a tenant while the process runs</a> — but it is still several orders of magnitude more work than a <code>ScheduleJob</code> call.</p><h3 id="tenant-count-and-the-shape-of-the-distribution" tabindex="-1"><a class="header-anchor" href="#tenant-count-and-the-shape-of-the-distribution"><span>Tenant count, and the shape of the distribution</span></a></h3><p>Nobody in this literature will give you a threshold, because the real limit is operational rather than technical — but two sources come close enough to be useful.</p><p>AWS on silo: &quot;If you have 20 siloed accounts for each of your tenants, for example, that may be manageable. However, if you have a thousand tenants, that would likely begin to impact operational efficiency and agility.&quot; Temporal on namespace-per-tenant: &quot;most teams find this manageable for fewer than 50 tenants.&quot;</p><p>Both numbers describe the same thing — the point at which a per-tenant <em>deployment artefact</em> stops being something a human can reason about. For Quartz.NET, where a per-tenant scheduler costs a scheduling loop, a thread pool and (with a persistent store) a connection pool and a cluster check-in, the same order of magnitude applies. Tens of schedulers in a process is ordinary; thousands is a different program.</p><p>The distribution matters as much as the count, and this is where the bridge model earns its keep. Microsoft&#39;s vertically partitioned model exists for exactly the common SaaS shape: most tenants on shared infrastructure, &quot;single-tenant infrastructures for customers who require higher performance or data isolation&quot;, with the option to &quot;charge customers a higher rate to use a single-tenant deployment&quot;. A long tail on one shared scheduler and a handful of whales on their own is not a compromise; it is the recommended answer.</p><h3 id="blast-radius-and-noisy-neighbours-are-different-problems" tabindex="-1"><a class="header-anchor" href="#blast-radius-and-noisy-neighbours-are-different-problems"><span>Blast radius and noisy neighbours are different problems</span></a></h3><p>They are easy to conflate and have different remedies. Noisy neighbours are a <em>capacity</em> problem — one tenant consuming a disproportionate share — and the fix is resource governance: quotas, throttling, priorities. Blast radius is a <em>failure</em> problem, and the fix is partitioning into independent failure domains: cells, stamps, separate deployments.</p><p>Microsoft&#39;s Noisy Neighbor guidance is that this is &quot;a resource governance problem&quot; to be met with &quot;usage quotas, throttling, and governance controls&quot; — plus one piece of scheduling advice worth lifting wholesale: &quot;Consider whether you have background processes or resource-intensive workloads that aren&#39;t time-sensitive. Run these workloads asynchronously at off-peak times to preserve your resource capacity for time-sensitive workloads.&quot; For a scheduler, that is a cron expression, and it is often cheaper than any isolation mechanism.</p><p>Blast radius is what cells and stamps address, and it is the reason the deployment-stamp guidance insists you &quot;deploy at least two stamps of your solution. If you deploy only a single stamp, you can easily hard-code assumptions into your code or configuration that don&#39;t apply when you scale out.&quot; The Quartz.NET equivalent: if you intend ever to run more than one scheduler, run two from the start.</p><h3 id="cost-per-idle-tenant" tabindex="-1"><a class="header-anchor" href="#cost-per-idle-tenant"><span>Cost per idle tenant</span></a></h3><p>Worth checking explicitly, because scheduling workloads are unusually sparse — a tenant with one nightly job is idle 99.9% of the time, and a model that charges for existence rather than execution scales badly against that shape.</p><p>The managed services divide cleanly. EventBridge Scheduler charges per invocation, so a dormant schedule is free. Google Cloud Scheduler charges &quot;$0.10 per job per month&quot; for the job&#39;s existence, and &quot;A paused job is counted as a job&quot; — with a free tier of three jobs <em>per billing account</em>, not per project, so it does not scale with tenant count. Azure&#39;s Durable Task Scheduler is free when idle on Consumption and charges per capacity unit on Dedicated.</p><p>Quartz.NET has no billing model, but it has the same asymmetry in resources. A tenant that is a group costs a row. A tenant that is a scheduler costs a scheduling loop that wakes on its own idle timer whether or not it has work, plus its pools. Multiply by the number of dormant tenants before choosing.</p><h3 id="observability" tabindex="-1"><a class="header-anchor" href="#observability"><span>Observability</span></a></h3><p>Ask whether you can answer &quot;what is tenant X doing right now?&quot; without a deployment, and whether doing so will bankrupt your metrics backend.</p><p>Under a group-per-tenant model the tenant is a dimension you already have, which is a strong argument for making the group the raw tenant id rather than a decorated string. Under a scheduler-per-tenant model the scheduler name is that dimension.</p><p>The trap is cardinality. Job and trigger names are per job and per trigger; multiplying them by a tenant dimension produces a series per tenant per trigger. Drop the name tags in a view before they reach the backend unless you know you need them.</p><p>The group is also not automatically everywhere you might assume. It is a tag on Quartz.NET&#39;s execution traces on both versions, and on metrics on 4.x — 3.x publishes no metrics at all — but neither version puts it into a logging scope, so per-tenant log correlation is something you add.</p><h2 id="mapping-the-axes-onto-quartz-net" tabindex="-1"><a class="header-anchor" href="#mapping-the-axes-onto-quartz-net"><span>Mapping the axes onto Quartz.NET</span></a></h2><p>Quartz.NET gives you three separations. They compose, and the useful designs use more than one.</p><p><strong>The scheduler</strong> is the strongest boundary in the process. Each one owns its job store, thread pool, listeners, plugins, calendars and — with a persistent store — its own connection pool and cluster check-in. In silo/pool terms, a scheduler per tenant is siloed compute.</p><p><strong>The group</strong> is the group half of every <code>JobKey</code> and <code>TriggerKey</code>. It is a logical partition, not an enforced one: nothing stops a job in group <code>acme</code> from touching group <code>initech</code>. What it buys is that every matcher-taking API becomes tenant-scoped — listing, pausing, resuming, deleting — and that the group is already a tag on the execution traces. In silo/pool terms, a group per tenant is pooled compute with a tenant discriminator, which is the same trade Temporal recommends when it steers you to task queues over namespaces.</p><p><strong><code>SCHED_NAME</code></strong> is the database-level separation. Every Quartz table has it as the first column of its primary key and every statement filters on it, so two schedulers with different names share tables without seeing each other&#39;s rows. That is a property of the schema rather than of the code paths — the same design Azure&#39;s SQL backend uses for task hubs. The table prefix is a second, coarser axis: separate table <em>sets</em> in one database, which is a backup, restore and permissions decision rather than an isolation one.</p><h3 id="which-mechanism-exists-on-which-version" tabindex="-1"><a class="header-anchor" href="#which-mechanism-exists-on-which-version"><span>Which mechanism exists on which version</span></a></h3><table><thead><tr><th>Mechanism</th><th>3.x</th><th>4.x</th></tr></thead><tbody><tr><td>Multiple schedulers in one process</td><td>Yes</td><td>Yes</td></tr><tr><td>Named schedulers through Microsoft DI</td><td>Yes, <code>AddQuartz(name, …)</code></td><td>Yes, <code>AddQuartz(name, …)</code></td></tr><tr><td>Resolving one by name</td><td><code>ISchedulerRepository</code></td><td>Keyed <code>IScheduler</code>, or <code>ISchedulerRepository</code></td></tr><tr><td>Groups and group matchers</td><td>Yes, <code>GroupMatcher&lt;T&gt;</code></td><td>Yes, plus the paged query API</td></tr><tr><td><code>SCHED_NAME</code> row separation</td><td>Yes</td><td>Yes</td></tr><tr><td>Per-scheduler table prefix</td><td>Yes</td><td>Yes</td></tr><tr><td>Startup schema validation</td><td>Yes, <code>PerformSchemaValidation</code> on by default</td><td>Yes, <code>SchemaProvisioning.Validate</code> by default</td></tr><tr><td>Shared database, mismatched table prefix</td><td>No — silent; each scheduler sees an empty table set</td><td>Yes, warns naming both schedulers and both prefixes</td></tr><tr><td>Listing tenants without starting them</td><td>No — the repository lists live schedulers only</td><td>Yes, <code>ISchedulerRegistry.QuerySchedulers()</code></td></tr><tr><td>Execution groups and per-node limits</td><td>Yes</td><td>Yes</td></tr><tr><td>Trigger group as the execution group</td><td>No — tag every trigger explicitly</td><td>Yes, <code>UseTriggerGroupWhenUnset()</code></td></tr><tr><td>Cluster-wide concurrency quota</td><td>No</td><td>Yes, <code>ExecutionLimitScope.Cluster</code> — approximate unless <code>AcquireTriggersWithinLock</code></td></tr><tr><td>Rate limiting (N per window)</td><td>No</td><td>No</td></tr><tr><td>Node affinity (persisted, cluster-aware)</td><td>Yes, <code>WithPreferredNode</code></td><td>Yes, <code>WithPreferredNode</code></td></tr><tr><td>Per-scheduler job type registration</td><td>No — one unkeyed registration, first wins</td><td>Yes, <code>AddJobType&lt;TJob, TImplementation&gt;()</code></td></tr><tr><td>Per-scheduler plugin instance from <code>quartz.plugin.*</code></td><td>Yes for an activated type, no for a registered one</td><td>Yes — the probe is keyed by scheduler</td></tr><tr><td>Preparing the job&#39;s DI scope</td><td>Subclass and override <code>ConfigureScope</code></td><td><code>ConfigureJobScope(…)</code> delegate</td></tr><tr><td>Reading the current firing without being handed it</td><td>No — your own <code>AsyncLocal</code></td><td>Yes, <code>IJobExecutionContextAccessor</code></td></tr><tr><td>Per-scheduler health check</td><td>No — one check, on the default scheduler</td><td>Yes, <code>q.AddQuartzHealthChecks()</code> per scheduler</td></tr><tr><td>Metrics</td><td>No</td><td>Yes</td></tr><tr><td>Runtime tenant onboarding without a container</td><td>Yes, <code>StdSchedulerFactory</code> / <code>DirectSchedulerFactory</code></td><td>Yes, <code>QuartzSchedulerBuilder</code></td></tr></tbody></table><p>Both trees carry the mechanics in full:</p>`,63),e(`ul`,null,[e(`li`,null,[c(m,{to:`/documentation/quartz-4.x/multi-tenancy.html`},{default:i(()=>[...l[13]||=[r(`Multi-Tenancy (Quartz 4.x)`,-1)]]),_:1})]),e(`li`,null,[c(m,{to:`/documentation/quartz-3.x/multi-tenancy.html`},{default:i(()=>[...l[14]||=[r(`Multi-Tenancy (Quartz 3.x)`,-1)]]),_:1})])]),l[27]||=t(`<h3 id="choosing" tabindex="-1"><a class="header-anchor" href="#choosing"><span>Choosing</span></a></h3><p>Work down this list and stop at the first that applies.</p><ol><li><strong>Tenants&#39; data must be physically separate, or must live in a particular place.</strong> A database per tenant, and therefore a scheduler per tenant, because a job store binds to one data source. This is the silo, with the silo&#39;s onboarding cost.</li><li><strong>A few tenants need isolation and the rest do not.</strong> The bridge: one shared scheduler with a group per tenant for the long tail, and a dedicated scheduler for each tenant that bought isolation. This is the most common shape for a SaaS and the one to reach for by default when the answer is not obviously 3 or 4.</li><li><strong>Tenants arrive while the process is running, and there are many of them.</strong> Group per tenant. Onboarding is a <code>ScheduleJob</code> call; there is no registration, no restart, and no per-tenant resource cost beyond the rows.</li><li><strong>Tenants are few, known at deployment, and differ in configuration.</strong> Scheduler per tenant, named, with per-scheduler options and health checks.</li></ol><p>Then, whichever you chose, decide the database question separately: one <code>SCHED_NAME</code> per tenant is usually enough, a table prefix per tenant if backup or permissions demand separate tables, and a separate database only if the data must not sit beside another tenant&#39;s.</p><h2 id="what-quartz-net-does-not-give-you" tabindex="-1"><a class="header-anchor" href="#what-quartz-net-does-not-give-you"><span>What Quartz.NET does not give you</span></a></h2><p>A recommendation that oversells is worse than none, so these are the things multi-tenant deployments ask Quartz.NET for and do not get. They apply to both 3.x and 4.x unless noted.</p><p><strong>Concurrency limits are per node unless you say otherwise, and on 3.x that is the only option.</strong> Execution groups cap how many threads a category of work may use, and by default the running count lives in a dictionary in memory on the scheduler thread: nothing is persisted, nodes do not coordinate, and a group limited to 3 can run up to 3×N across an N-node cluster. This is the same trap Celery documents for <code>rate_limit</code>, and the same one Hangfire only escapes in a paid add-on that coordinates through storage. On 3.x the closest approximation is dividing the cap by the node count, which is wrong whenever a node is down.</p><p>4.x adds the real thing: <code>ForGroup(&quot;acme&quot;, 8, ExecutionLimitScope.Cluster)</code> is counted from <code>QRTZ_FIRED_TRIGGERS</code>, which is already the cluster&#39;s reservation ledger. Read what it promises before relying on it — the ceiling holds within one acquisition round and can transiently overshoot by <code>nodes − 1</code>, one trigger per node, because the lock-free acquisition path that allows the overshoot exists only when a round acquires a single trigger. It fails closed, so a node that cannot reach the store fires nothing rather than firing unmetered.</p><p><strong>There is no rate limiting.</strong> Execution limits cap <em>concurrency</em>, not throughput. &quot;This tenant may run 100 jobs an hour&quot; is not something Quartz.NET can express; build it in the job, or in the thing the job calls.</p><p><strong>A starved group misfires; it does not queue.</strong> When a group is at its limit its triggers are skipped during acquisition and left where they are. They keep their original next fire time, so if the starvation lasts longer than the misfire threshold the ordinary misfire machinery claims them, and the trigger&#39;s misfire instruction — not the limit — decides whether the occurrence is skipped or rescheduled. Set per-tenant limits with that in mind, and pick misfire instructions deliberately for triggers in limited groups.</p><p><strong>On 3.x, job types are not keyed by scheduler.</strong> Jobs are resolved from the one container by type, with no scheduler key, so two 3.x schedulers in one container cannot have different implementations or lifetimes of the same job type: nothing is registered for you, the factory looks the type up unkeyed, and whatever the application registered is what every scheduler gets. 4.x keeps the unkeyed registration as the default — <code>AddJob&lt;T&gt;</code> still registers the type with <code>TryAdd</code> semantics — but <code>AddJobType&lt;TJob, TImplementation&gt;()</code>, <code>AddJobType&lt;TJob&gt;(lifetime)</code> and <code>AddJobType&lt;TJob&gt;(factory)</code> register under one scheduler&#39;s key, and the job factory reads that key first. On either version, a job type per tenant stops scaling long before groups do; one job type that reads its tenant from the firing and resolves what it needs, by key if you like, inside <code>Execute</code> is the shape to prefer.</p><p><strong>Nothing stops a job reaching another tenant&#39;s data.</strong> Groups are a naming partition. In AWS&#39;s terms Quartz.NET gives you partitioning, and isolation is your application&#39;s job — a tenant id read from the firing and threaded through every query, not a boundary the scheduler enforces.</p><p><strong>On 3.x, dashboard authorization is per process, all or nothing.</strong> One policy, one read-only flag, no per-scheduler policy and no scheduler-name claim check. If 3.x tenants must reach their own scheduler and not each other&#39;s, enforce that outside Quartz.NET — a process per tenant, or middleware that authorizes on the scheduler-name route segment.</p><p>4.x holds each caller to its own scheduler on both surfaces: <code>QuartzDashboardOptions.SchedulerAuthorizationPolicy</code> and <code>QuartzHttpApiOptions.SchedulerAuthorizationPolicy</code> name a policy evaluated per request against a <code>SchedulerResource</code> carrying the scheduler&#39;s name, so one <code>AuthorizationHandler&lt;TRequirement, SchedulerResource&gt;</code> covers every route and every page. What a caller may <em>do</em> to the scheduler it reaches is still process-wide — the dashboard&#39;s read-only flag — so &quot;this tenant may look, that one may act&quot; remains outside Quartz.NET on either version.</p><p><strong>A shut-down scheduler cannot be restarted.</strong> <code>Standby()</code> / <code>Start()</code> is the pause-and-resume pair. Shutdown is terminal: the scheduler refuses to start again and every other operation throws. You can build a <em>new</em> scheduler with the same name, but it is a new one, not a revived one.</p><p><strong>The tenant does not reach your logs by itself.</strong> The job and trigger group are tags on the execution traces, and on 4.x&#39;s metrics, but neither version puts them into a logging scope; if you want per-tenant log correlation you add it. Note also that a tenant carried only in an <em>execution</em> group is invisible to those signals — one more reason to make the trigger group the tenant.</p><h3 id="onboarding-a-tenant-while-the-process-runs" tabindex="-1"><a class="header-anchor" href="#onboarding-a-tenant-while-the-process-runs"><span>Onboarding a tenant while the process runs</span></a></h3><p>Under the group-per-tenant model this is a non-question: scheduling a job for a new group is an ordinary API call.</p><p>Under the scheduler-per-tenant model, the DI path is closed once the container is built — <code>AddQuartz</code> mutates <code>IServiceCollection</code>, and the hosted service enumerates schedulers once at start. But that is a limit of the DI path, not of the library. Both versions can build a scheduler at runtime outside the container: 3.x through <code>StdSchedulerFactory</code> or <code>DirectSchedulerFactory</code>, and 4.x through <code>QuartzSchedulerBuilder</code>, which creates and owns a container of its own.</p><p>What that costs you is worth knowing before you build on it: a scheduler created this way gets no hosted-service lifetime (you start and dispose it), its jobs resolve from its own container rather than the application&#39;s unless you give it a job factory that bridges, and it is not covered by health checks registered at startup. The per-version guides spell out the API and the trade-offs.</p><h2 id="anti-patterns" tabindex="-1"><a class="header-anchor" href="#anti-patterns"><span>Anti-patterns</span></a></h2><p><strong>Smuggling the tenant into a job name and parsing it back out.</strong> <code>JobKey(&quot;nightly-report-acme&quot;)</code> looks harmless until something needs every job for a tenant, and the only way to get it is to fetch every key and split strings. The group half of the key exists for this; use it, and keep the name for what the job <em>is</em>. This is the same mistake as encoding a tenant in a Redis key when the system offers a database — and Sidekiq&#39;s history is what happens next.</p><p><strong>One scheduler per tenant at thousands of tenants.</strong> Each scheduler is a scheduling loop that wakes on its own timer, a thread pool, a connection pool and a cluster check-in, whether or not that tenant has anything to run. This is AWS&#39;s silo scaling problem in miniature, and their guidance holds: fine at twenty, a serious operational burden at a thousand. Groups scale where schedulers do not.</p><p><strong>A shared database with a mismatched table prefix.</strong> Nothing derives the prefix from the DDL or the DDL from the prefix; you run the scripts with the prefix substituted. A prefix pointing at tables that do not exist is caught at startup — 3.x has <code>PerformSchemaValidation</code> on by default and 4.x <code>SchemaProvisioning.Validate</code> — and it names the missing table rather than letting the first failing operation surface an hour later. What validation cannot catch is a prefix pointing at tables that <em>do</em> exist and belong to another tenant: that configuration is indistinguishable from a correct one, and it will run happily on the wrong data. Derive the prefix from the tenant id in code rather than pasting it into per-environment configuration.</p><p><strong>Two unrelated schedulers sharing a database with the same <code>SCHED_NAME</code>.</strong> By construction they are indistinguishable from two nodes of one cluster, because that is exactly what they look like to the schema. They will steal each other&#39;s triggers. Duplicate-name checks protect you only within one container; across processes the name is a contract you keep. Microsoft hit the same problem with task hubs and solved it by deriving the default name from the app name — a good idea to copy, by deriving the scheduler name from the tenant id rather than from a configuration file someone can copy-paste.</p><p><strong>Assuming a per-node limit is a per-cluster limit.</strong> Covered above, and repeated here because it is the failure that only shows up in production, after the second node is added. It is still the default on 4.x — a limit is cluster-wide only when it says <code>ExecutionLimitScope.Cluster</code>.</p><p><strong>Letting per-tenant metrics multiply without a view.</strong> A tenant dimension times a trigger-name dimension is a series per tenant per trigger. Aggregate before the data leaves the process.</p><h2 id="see-also" tabindex="-1"><a class="header-anchor" href="#see-also"><span>See also</span></a></h2>`,28),e(`ul`,null,[e(`li`,null,[c(m,{to:`/documentation/quartz-4.x/multi-tenancy.html`},{default:i(()=>[...l[15]||=[r(`Multi-Tenancy (Quartz 4.x)`,-1)]]),_:1}),l[16]||=r(` — the 4.x mechanics in full`,-1)]),e(`li`,null,[c(m,{to:`/documentation/quartz-3.x/multi-tenancy.html`},{default:i(()=>[...l[17]||=[r(`Multi-Tenancy (Quartz 3.x)`,-1)]]),_:1}),l[18]||=r(` — the 3.x mechanics in full`,-1)]),e(`li`,null,[c(m,{to:`/documentation/best-practices.html`},{default:i(()=>[...l[19]||=[r(`Best Practices`,-1)]]),_:1}),l[20]||=r(` — including why never to point two non-clustered schedulers at one database`,-1)]),e(`li`,null,[c(m,{to:`/documentation/troubleshooting.html`},{default:i(()=>[...l[21]||=[r(`Troubleshooting`,-1)]]),_:1})])]),l[28]||=t(`<h2 id="sources" tabindex="-1"><a class="header-anchor" href="#sources"><span>Sources</span></a></h2><p>The prior-art survey above is drawn from these primary sources, read in August 2026.</p><ul><li>AWS, <a href="https://docs.aws.amazon.com/whitepapers/latest/saas-architecture-fundamentals/tenant-isolation.html" target="_blank" rel="noopener noreferrer">SaaS Architecture Fundamentals: Tenant isolation</a> and <a href="https://docs.aws.amazon.com/wellarchitected/latest/saas-lens/silo-pool-and-bridge-models.html" target="_blank" rel="noopener noreferrer">SaaS Lens: Silo, Pool, and Bridge Models</a></li><li>AWS, <a href="https://docs.aws.amazon.com/whitepapers/latest/saas-tenant-isolation-strategies/silo-isolation.html" target="_blank" rel="noopener noreferrer">SaaS Tenant Isolation Strategies</a> — silo, pool and bridge pros and cons</li><li>AWS, <a href="https://docs.aws.amazon.com/wellarchitected/latest/reducing-scope-of-impact-with-cell-based-architecture/what-is-a-cell-based-architecture.html" target="_blank" rel="noopener noreferrer">Reducing the Scope of Impact with Cell-Based Architecture</a></li><li>Microsoft, <a href="https://learn.microsoft.com/azure/architecture/guide/multitenant/considerations/tenancy-models" target="_blank" rel="noopener noreferrer">Tenancy models to consider for a multitenant solution</a>, <a href="https://learn.microsoft.com/azure/architecture/guide/multitenant/approaches/compute" target="_blank" rel="noopener noreferrer">Architectural approaches for compute</a>, <a href="https://learn.microsoft.com/azure/architecture/patterns/deployment-stamp" target="_blank" rel="noopener noreferrer">Deployment Stamps pattern</a> and the <a href="https://learn.microsoft.com/azure/architecture/antipatterns/noisy-neighbor/noisy-neighbor" target="_blank" rel="noopener noreferrer">Noisy Neighbor antipattern</a></li><li>Temporal, <a href="https://docs.temporal.io/production-deployment/multi-tenant-patterns" target="_blank" rel="noopener noreferrer">Multi-tenant patterns</a>, <a href="https://docs.temporal.io/namespaces" target="_blank" rel="noopener noreferrer">Namespaces</a> and <a href="https://docs.temporal.io/best-practices/managing-namespace" target="_blank" rel="noopener noreferrer">Managing namespaces</a></li><li>Cadence, <a href="https://cadenceworkflow.io/blog/2024/09/05/workflow-specific-rate-limits" target="_blank" rel="noopener noreferrer">Workflow ID-based rate limits</a></li><li>Kubernetes, <a href="https://kubernetes.io/docs/concepts/security/multi-tenancy/" target="_blank" rel="noopener noreferrer">Multi-tenancy</a> and <a href="https://kubernetes.io/docs/concepts/workloads/controllers/cron-jobs/" target="_blank" rel="noopener noreferrer">CronJob</a></li><li>Apache Airflow, <a href="https://airflow.apache.org/docs/apache-airflow/stable/security/security_model.html" target="_blank" rel="noopener noreferrer">Security model</a>, <a href="https://airflow.apache.org/docs/apache-airflow/stable/core-concepts/multi-team.html" target="_blank" rel="noopener noreferrer">Multi-team</a>, <a href="https://airflow.apache.org/docs/apache-airflow/stable/administration-and-deployment/pools.html" target="_blank" rel="noopener noreferrer">Pools</a> and <a href="https://cwiki.apache.org/confluence/pages/viewpage.action?pageId=89066609" target="_blank" rel="noopener noreferrer">AIP-1</a>, <a href="https://cwiki.apache.org/confluence/display/AIRFLOW/AIP-44+Airflow+Internal+API" target="_blank" rel="noopener noreferrer">AIP-44</a>, <a href="https://cwiki.apache.org/confluence/display/AIRFLOW/AIP-72+Task+Execution+Interface+aka+Task+SDK" target="_blank" rel="noopener noreferrer">AIP-72</a></li><li>AWS, <a href="https://docs.aws.amazon.com/scheduler/latest/UserGuide/scheduler-quotas.html" target="_blank" rel="noopener noreferrer">EventBridge Scheduler quotas</a> and <a href="https://docs.aws.amazon.com/scheduler/latest/UserGuide/managing-schedule-group.html" target="_blank" rel="noopener noreferrer">schedule groups</a></li><li>Microsoft, <a href="https://learn.microsoft.com/azure/durable-task/common/durable-task-hubs" target="_blank" rel="noopener noreferrer">Durable task hubs</a></li><li>Celery, <a href="https://docs.celeryq.dev/en/stable/userguide/tasks.html" target="_blank" rel="noopener noreferrer">Tasks: <code>Task.rate_limit</code></a></li><li>Hangfire, <a href="https://docs.hangfire.io/en/latest/background-processing/throttling.html" target="_blank" rel="noopener noreferrer">Concurrency and rate limiting</a>, <a href="https://docs.hangfire.io/en/latest/background-processing/configuring-queues.html" target="_blank" rel="noopener noreferrer">Configuring queues</a>, <a href="https://docs.hangfire.io/en/latest/configuration/using-dashboard.html" target="_blank" rel="noopener noreferrer">Using the dashboard</a> and issue <a href="https://github.com/HangfireIO/Hangfire/issues/879" target="_blank" rel="noopener noreferrer">#879</a></li><li>Quartz (Java), <a href="https://www.quartz-scheduler.org/documentation/quartz-2.3.0/cookbook/MultipleSchedulers.html" target="_blank" rel="noopener noreferrer">Multiple schedulers cookbook</a>, <a href="https://www.quartz-scheduler.org/documentation/quartz-2.3.0/configuration/ConfigJDBCJobStoreClustering.html" target="_blank" rel="noopener noreferrer">JDBC-JobStore clustering</a> and <a href="https://www.quartz-scheduler.org/documentation/quartz-2.3.0/tutorials/tutorial-lesson-02.html" target="_blank" rel="noopener noreferrer">tutorial lesson 2</a></li><li>BullMQ, <a href="https://docs.bullmq.io/guide/rate-limiting" target="_blank" rel="noopener noreferrer">Rate limiting</a> and <a href="https://docs.bullmq.io/bullmq-pro/groups" target="_blank" rel="noopener noreferrer">Pro: Groups</a></li><li>Sidekiq, <a href="https://github.com/sidekiq/sidekiq/blob/main/docs/7.0-Upgrade.md" target="_blank" rel="noopener noreferrer">7.0 upgrade notes</a> and <a href="https://www.mikeperham.com/2015/09/24/storing-data-with-redis/" target="_blank" rel="noopener noreferrer">Storing data with Redis</a></li><li>Quartz (Java), <a href="https://www.quartz-scheduler.org/documentation/quartz-2.3.0/configuration/ConfigMain.html" target="_blank" rel="noopener noreferrer">Configuration reference</a></li></ul>`,3)])}var f=s(u,[[`render`,d]]);export{l as _pageData,f as default};