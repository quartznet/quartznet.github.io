import{a as e,c as t,d as n,l as r,m as i,p as a,s as o,t as s,u as c}from"./app-emje7bwI.js";var l=JSON.parse(`{"path":"/documentation/tenancy-patterns.html","title":"Tenancy Patterns","lang":"en-US","frontmatter":{"title":"Tenancy Patterns"},"git":{"updatedTime":1790451733000,"contributors":[{"name":"Marko Lahma","username":"","email":"marko.lahma@gmail.com","commits":1},{"name":"Claude Opus 5.5","username":"","email":"noreply@anthropic.com","commits":1}],"changelog":[{"hash":"4a29e014ef1e61227140e96264c49cf64cd3183d","time":1790451733000,"email":"marko.lahma@gmail.com","author":"Marko Lahma","message":"A captured log reaches its column whole on Oracle and SQL Server","coAuthors":[{"name":"Claude Opus 5.5","email":"noreply@anthropic.com"}]}]},"filePathRelative":"documentation/tenancy-patterns.md"}`),u={name:`tenancy-patterns.md`};function d(s,l,u,d,f,p){let m=a(`RouteLink`);return n(),o(`div`,null,[l[19]||=e(`h1`,{id:`tenancy-patterns`,tabindex:`-1`},[e(`a`,{class:`header-anchor`,href:`#tenancy-patterns`},[e(`span`,null,`Tenancy Patterns`)])],-1),l[20]||=e(`p`,null,[r(`Quartz.NET has no `),e(`code`,null,`Tenant`),r(` type and will not get one. You build tenancy from three separations: a scheduler, a group and a `),e(`code`,null,`SCHED_NAME`),r(`. Choose between them before you have twelve tenants in production and a migration to write. The mechanics are in the per-version guides:`)],-1),e(`ul`,null,[e(`li`,null,[c(m,{to:`/documentation/quartz-4.x/multi-tenancy.html`},{default:i(()=>[...l[0]||=[r(`Multi-Tenancy (Quartz 4.x)`,-1)]]),_:1})]),e(`li`,null,[c(m,{to:`/documentation/quartz-3.x/multi-tenancy.html`},{default:i(()=>[...l[1]||=[r(`Multi-Tenancy (Quartz 3.x)`,-1)]]),_:1})])]),l[21]||=t(`<h2 id="isolation-is-not-authorization-and-partitioning-is-not-isolation" tabindex="-1"><a class="header-anchor" href="#isolation-is-not-authorization-and-partitioning-is-not-isolation"><span>Isolation is not authorization, and partitioning is not isolation</span></a></h2><ul><li><strong>Isolation is not authentication and authorization.</strong> An authenticated, authorized user can still reach another tenant&#39;s resources. Isolation is the enforced use of tenant context to bound which resources a request can reach at all.</li><li><strong>Partitioning is not isolation.</strong> A tenant id in a column, a key prefix or a group name arranges data by tenant. It does not stop code reading across the boundary.</li><li><strong>Isolation is chosen per layer.</strong> For a scheduler the question is which of the scheduling loop, the thread pool, the job store and the database are shared, and which are dedicated. They can be answered differently, and usually should be.</li></ul><h3 id="two-vocabularies-for-the-same-thing" tabindex="-1"><a class="header-anchor" href="#two-vocabularies-for-the-same-thing"><span>Two vocabularies for the same thing</span></a></h3><p>AWS (<a href="https://docs.aws.amazon.com/wellarchitected/latest/saas-lens/silo-pool-and-bridge-models.html" target="_blank" rel="noopener noreferrer">silo, pool and bridge</a>) and Microsoft (<a href="https://learn.microsoft.com/azure/architecture/guide/multitenant/considerations/tenancy-models" target="_blank" rel="noopener noreferrer">tenancy models</a>) use different words for the same models:</p><table><thead><tr><th>AWS</th><th>Microsoft</th><th>Meaning</th></tr></thead><tbody><tr><td>Silo</td><td>Automated single-tenant deployments</td><td>Each tenant gets a dedicated stack</td></tr><tr><td>Pool</td><td>Fully multitenant deployments</td><td>Every tenant shares one set of infrastructure</td></tr><tr><td>Bridge</td><td>Vertically partitioned deployments</td><td>Some tenants siloed, some pooled; usually a premium tier</td></tr><tr><td>Bridge (applied per layer)</td><td>Horizontally partitioned deployments</td><td>Shared compute, dedicated data store per tenant</td></tr><tr><td>Cell</td><td>Deployment stamp</td><td>A whole copy of the system serving a bounded set of tenants</td></tr></tbody></table><p>A cell or stamp holds <strong>many</strong> tenants, not one. A silo bounds <em>access</em>; a cell bounds <em>blast radius</em>. You can want one without the other.</p><h2 id="how-other-systems-partition-tenants" tabindex="-1"><a class="header-anchor" href="#how-other-systems-partition-tenants"><span>How other systems partition tenants</span></a></h2><p>In almost every system the tenancy primitive is a naming, configuration and access boundary, not a fairness boundary. Quotas and rate limits usually attach one level up (the account, the cluster or the process), and per-tenant fairness is often a paid tier.</p><table><thead><tr><th>System</th><th>Primitive</th><th>Enforced by</th><th>Quota attaches to</th><th>Tenant added at runtime?</th></tr></thead><tbody><tr><td>Temporal</td><td>Namespace</td><td>Server</td><td>Namespace <em>and</em> cluster</td><td>Yes, <code>RegisterNamespace</code></td></tr><tr><td>Cadence</td><td>Domain</td><td>Server</td><td>Domain and task list</td><td>Yes</td></tr><tr><td>Kubernetes <code>CronJob</code></td><td>Namespace</td><td>API server + RBAC</td><td>Namespace, via <code>ResourceQuota</code></td><td>Yes, one API call</td></tr><tr><td>AWS EventBridge Scheduler</td><td>Schedule group</td><td>IAM, via the ARN</td><td><strong>Account and Region, never the group</strong></td><td>Yes, <code>CreateScheduleGroup</code></td></tr><tr><td>Azure Durable Functions</td><td>Task hub</td><td>Storage naming, or RBAC on the managed provider</td><td>Scheduler resource, not the hub</td><td>Yes, implicitly</td></tr><tr><td>HashiCorp Nomad</td><td>Namespace</td><td>Server + ACL</td><td>Namespace; quotas are Enterprise-only</td><td>Yes</td></tr><tr><td>Google Cloud Scheduler</td><td>Project</td><td>IAM</td><td>Project</td><td>Only by creating a project</td></tr><tr><td>Hangfire</td><td>Queue, server, storage</td><td>Convention</td><td>Server process</td><td>Storage yes, queue set no</td></tr><tr><td>Celery</td><td>Queue, broker vhost</td><td>Broker</td><td><strong>Worker process, not the cluster</strong></td><td>Yes, <code>add_consumer</code></td></tr><tr><td>Sidekiq</td><td>Queue, Redis instance</td><td>Convention</td><td>Process, or capsule</td><td>Queue set fixed at start</td></tr><tr><td>Airflow</td><td>Team, pool, queue</td><td>Pools: the metadata database. Teams: the API layer</td><td>Pool, cluster-wide</td><td>A pool yes; a team needs new processes</td></tr><tr><td>Quartz (Java)</td><td><code>instanceName</code>, group</td><td><code>SCHED_NAME</code> in every statement</td><td>Scheduler, not the group</td><td>Yes, <code>DirectSchedulerFactory</code></td></tr></tbody></table><h3 id="what-carries-over-to-quartz-net" tabindex="-1"><a class="header-anchor" href="#what-carries-over-to-quartz-net"><span>What carries over to Quartz.NET</span></a></h3><ul><li><strong>Where a limit is counted matters most.</strong> Celery&#39;s <code>rate_limit</code> is per worker, so 10/s across four workers is 40/s. Airflow&#39;s pools hold cluster-wide, but <code>[core] parallelism</code> is per scheduler. Quartz.NET&#39;s execution limits are per node by default; see <a href="#what-quartz-net-does-not-give-you">What Quartz.NET does not give you</a>.</li><li><strong>A shared partition with a tenant discriminator scales further than a boundary per tenant.</strong> Temporal recommends a task queue per tenant over a namespace per tenant, and suggests namespaces per tenant only below about 50 tenants. A group per tenant is the same trade.</li><li><strong>Two users of one partition name collide.</strong> Azure Durable Functions apps that share a task hub compete for messages and can get stuck, so Azure derives the default hub name from the app name. Two unrelated schedulers with one <code>SCHED_NAME</code> fail the same way.</li><li><strong>A prefix buys naming and nothing else.</strong> Sidekiq 7.0 removed <code>redis-namespace</code>: a key prefix added cost to every operation and gave no separate tuning, durability or failure domain. A per-tenant table prefix has the same shape.</li><li><strong>Ordering that falls out of an index is not fairness.</strong> Hangfire on SQL Server fetches queues in name order, so a busy <code>acme</code> can starve <code>zeta</code>. Its per-tenant throttling is in the paid Hangfire Ace set, best-effort, and scoped to its storage.</li><li><strong>Java Quartz has no tenancy guidance.</strong> Its groups are categories with no per-group concurrency limit, and its answer to different concurrency for different jobs is a second scheduler. It recommends splitting thousands of short jobs across several schedulers because the cluster-wide lock degrades beyond about three nodes.</li></ul><h2 id="the-axes-that-decide" tabindex="-1"><a class="header-anchor" href="#the-axes-that-decide"><span>The axes that decide</span></a></h2><p>Ordered roughly by how often each turns out to decide:</p><table><thead><tr><th>Axis</th><th>Ask yourself</th><th>Pushes toward shared</th><th>Pushes toward dedicated</th></tr></thead><tbody><tr><td>Onboarding cadence</td><td>Does a tenant appear while the process runs?</td><td>Runtime arrival</td><td>Tenants known at deploy time</td></tr><tr><td>Tenant count</td><td>How many, and how skewed?</td><td>Hundreds or thousands</td><td>Tens, or a few whales</td></tr><tr><td>Blast radius</td><td>What is the cost of one bad tenant taking everything down?</td><td>Tolerable</td><td>Unacceptable</td></tr><tr><td>Noisy neighbours</td><td>Can one tenant&#39;s work starve another&#39;s?</td><td>Workloads are small and similar</td><td>Workloads are bursty or heavy</td></tr><tr><td>Per-tenant quotas and SLAs</td><td>Do you sell tiers with different guarantees?</td><td>One tier</td><td>Contractual per-tenant limits</td></tr><tr><td>Data residency</td><td>Must a tenant&#39;s data live somewhere specific, or under its own key?</td><td>No constraint</td><td>Regulated or sovereign data</td></tr><tr><td>Cost per idle tenant</td><td>What does a tenant cost when it schedules nothing?</td><td>Must be ~zero</td><td>Amortised by the contract</td></tr><tr><td>Customisation</td><td>Do tenants differ in configuration, calendars, or schema?</td><td>Uniform</td><td>Divergent</td></tr><tr><td>Observability</td><td>Must you answer &quot;what is tenant X doing right now?&quot;</td><td>Group dimension is enough</td><td>Needs its own dashboards</td></tr></tbody></table><h3 id="onboarding-cadence-decides-more-than-anything-else" tabindex="-1"><a class="header-anchor" href="#onboarding-cadence-decides-more-than-anything-else"><span>Onboarding cadence decides more than anything else</span></a></h3><p>Evaluate this first: it often rules an option out. With a group per tenant, onboarding is writing a trigger. With a scheduler per tenant it is closer to a deployment. Neither version needs a <em>redeploy</em> for it (see <a href="#onboarding-a-tenant-while-the-process-runs">Onboarding a tenant while the process runs</a>), but it is far more work than a <code>ScheduleJob</code> call.</p><h3 id="tenant-count-and-the-shape-of-the-distribution" tabindex="-1"><a class="header-anchor" href="#tenant-count-and-the-shape-of-the-distribution"><span>Tenant count, and the shape of the distribution</span></a></h3><p>A scheduler per tenant costs a scheduling loop, a thread pool and, with a persistent store, a connection pool and a cluster check-in. Tens of schedulers in a process is ordinary; thousands is a different program. AWS calls 20 siloed tenants manageable and a thousand a burden to operate.</p><p>For a skewed distribution, use the bridge: the long tail on one shared scheduler with a group per tenant, and a dedicated scheduler for each large tenant. Microsoft calls this a vertically partitioned deployment, with the dedicated tier often sold at a higher rate.</p><h3 id="blast-radius-and-noisy-neighbours-are-different-problems" tabindex="-1"><a class="header-anchor" href="#blast-radius-and-noisy-neighbours-are-different-problems"><span>Blast radius and noisy neighbours are different problems</span></a></h3><table><thead><tr><th>Problem</th><th>Kind</th><th>Remedy</th></tr></thead><tbody><tr><td>Noisy neighbours</td><td>Capacity: one tenant takes a disproportionate share</td><td>Quotas, throttling, priorities</td></tr><tr><td>Blast radius</td><td>Failure: one tenant&#39;s failure takes others down</td><td>Independent failure domains: cells, stamps, separate deployments</td></tr></tbody></table><p>Moving work that is not time-sensitive to off-peak hours needs only a cron expression, and is often cheaper than any isolation mechanism. If you will ever run more than one scheduler, run two from the start, so no code assumes there is only one.</p><h3 id="cost-per-idle-tenant" tabindex="-1"><a class="header-anchor" href="#cost-per-idle-tenant"><span>Cost per idle tenant</span></a></h3><p>A tenant with one nightly job is idle 99.9% of the time. A tenant that is a group costs a row. A tenant that is a scheduler costs a scheduling loop that wakes on its own idle timer whether or not it has work, plus its pools. Multiply by the number of dormant tenants before choosing.</p><h3 id="observability" tabindex="-1"><a class="header-anchor" href="#observability"><span>Observability</span></a></h3><ul><li>With a group per tenant, the group is the tenant dimension. Make the group the raw tenant id, not a decorated string. With a scheduler per tenant, the scheduler name is the dimension.</li><li>Watch cardinality. A tenant dimension times job and trigger names is a series per tenant per trigger. Drop the name tags in a view before they reach the backend, unless you need them.</li><li>The group is a tag on execution traces on both versions, and on metrics on 4.x (3.x has no metrics). Neither version puts it into a logging scope; add per-tenant log correlation yourself.</li></ul><h2 id="mapping-the-axes-onto-quartz-net" tabindex="-1"><a class="header-anchor" href="#mapping-the-axes-onto-quartz-net"><span>Mapping the axes onto Quartz.NET</span></a></h2><p>The three separations compose, and useful designs use more than one.</p><ul><li><strong>The scheduler</strong> is the strongest boundary in the process. Each owns its job store, thread pool, listeners, plugins and calendars, and with a persistent store its connection pool and cluster check-in. A scheduler per tenant is siloed compute.</li><li><strong>The group</strong> is the group half of every <code>JobKey</code> and <code>TriggerKey</code>. It is a logical partition, not an enforced one: nothing stops a job in group <code>acme</code> from touching group <code>initech</code>. It makes every matcher-taking API (listing, pausing, resuming, deleting) tenant-scoped, and it is already a tag on the execution traces. A group per tenant is pooled compute with a tenant discriminator.</li><li><strong><code>SCHED_NAME</code></strong> is the first column of every Quartz table&#39;s primary key, and every statement filters on it. Two schedulers with different names share tables without seeing each other&#39;s rows. This is a property of the schema, not of the code paths.</li><li><strong>The table prefix</strong> gives separate table <em>sets</em> in one database. It is a backup, restore and permissions decision, not an isolation one.</li></ul><h3 id="which-mechanism-exists-on-which-version" tabindex="-1"><a class="header-anchor" href="#which-mechanism-exists-on-which-version"><span>Which mechanism exists on which version</span></a></h3><table><thead><tr><th>Mechanism</th><th>3.x</th><th>4.x</th></tr></thead><tbody><tr><td>Multiple schedulers in one process</td><td>Yes</td><td>Yes</td></tr><tr><td>Named schedulers through Microsoft DI</td><td>Yes, <code>AddQuartz(name, …)</code></td><td>Yes, <code>AddQuartz(name, …)</code></td></tr><tr><td>Resolving one by name</td><td><code>ISchedulerRepository</code></td><td>Keyed <code>IScheduler</code>, or <code>ISchedulerRepository</code></td></tr><tr><td>Groups and group matchers</td><td>Yes, <code>GroupMatcher&lt;T&gt;</code></td><td>Yes, plus the paged query API</td></tr><tr><td><code>SCHED_NAME</code> row separation</td><td>Yes</td><td>Yes</td></tr><tr><td>Per-scheduler table prefix</td><td>Yes</td><td>Yes</td></tr><tr><td>Startup schema validation</td><td>Yes, <code>PerformSchemaValidation</code> on by default</td><td>Yes, <code>SchemaProvisioning.Validate</code> by default</td></tr><tr><td>Shared database, mismatched table prefix</td><td>No: silent, each scheduler sees an empty table set</td><td>Yes, warns naming both schedulers and both prefixes</td></tr><tr><td>Listing tenants without starting them</td><td>No: the repository lists live schedulers only</td><td>Yes, <code>ISchedulerRegistry.QuerySchedulers()</code></td></tr><tr><td>Execution groups and per-node limits</td><td>Yes</td><td>Yes</td></tr><tr><td>Trigger group as the execution group</td><td>No: tag every trigger explicitly</td><td>Yes, <code>UseTriggerGroupWhenUnset()</code></td></tr><tr><td>Cluster-wide concurrency quota</td><td>No</td><td>Yes, <code>ExecutionLimitScope.Cluster</code>; approximate unless <code>AcquireTriggersWithinLock</code></td></tr><tr><td>Rate limiting (N per window)</td><td>No</td><td>No</td></tr><tr><td>Node affinity (persisted, cluster-aware)</td><td>Yes, <code>WithPreferredNode</code></td><td>Yes, <code>WithPreferredNode</code></td></tr><tr><td>Per-scheduler job type registration</td><td>No: one unkeyed registration, first wins</td><td>Yes, <code>AddJobType&lt;TJob, TImplementation&gt;()</code></td></tr><tr><td>Per-scheduler plugin instance from <code>quartz.plugin.*</code></td><td>Yes for an activated type, no for a registered one</td><td>Yes; the probe is keyed by scheduler</td></tr><tr><td>Preparing the job&#39;s DI scope</td><td>Subclass and override <code>ConfigureScope</code></td><td><code>ConfigureJobScope(…)</code> delegate</td></tr><tr><td>Reading the current firing without being handed it</td><td>No: your own <code>AsyncLocal</code></td><td>Yes, <code>IJobExecutionContextAccessor</code></td></tr><tr><td>Per-scheduler health check</td><td>No: one check, on the default scheduler</td><td>Yes, <code>q.AddQuartzHealthChecks()</code> per scheduler</td></tr><tr><td>Metrics</td><td>No</td><td>Yes</td></tr><tr><td>Runtime tenant onboarding without a container</td><td>Yes, <code>StdSchedulerFactory</code> / <code>DirectSchedulerFactory</code></td><td>Yes, <code>QuartzSchedulerBuilder</code></td></tr><tr><td>Runtime tenant onboarding into the application&#39;s container</td><td>No</td><td>Yes, <code>ISchedulerRuntime.Add</code> / <code>Remove</code></td></tr></tbody></table><h3 id="choosing" tabindex="-1"><a class="header-anchor" href="#choosing"><span>Choosing</span></a></h3><p>Stop at the first that applies:</p><ol><li><strong>Tenants&#39; data must be physically separate, or in a particular place.</strong> A database per tenant, and so a scheduler per tenant, because a job store binds to one data source. This is the silo, with the silo&#39;s onboarding cost.</li><li><strong>A few tenants need isolation and the rest do not.</strong> The bridge: one shared scheduler with a group per tenant for the long tail, and a dedicated scheduler for each tenant that bought isolation. The most common shape for SaaS, and the default when 3 and 4 do not clearly apply.</li><li><strong>Tenants arrive while the process runs, and there are many.</strong> A group per tenant. Onboarding is a <code>ScheduleJob</code> call, with no registration, no restart and no per-tenant cost beyond the rows.</li><li><strong>Tenants are few, known at deployment, and differ in configuration.</strong> A named scheduler per tenant, with per-scheduler options and health checks.</li></ol><p>Then decide the database separately:</p><ul><li>one <code>SCHED_NAME</code> per tenant is usually enough;</li><li>a table prefix per tenant if backup or permissions need separate tables;</li><li>a separate database only if the data must not sit beside another tenant&#39;s.</li></ul><h2 id="what-quartz-net-does-not-give-you" tabindex="-1"><a class="header-anchor" href="#what-quartz-net-does-not-give-you"><span>What Quartz.NET does not give you</span></a></h2><p>These apply to both 3.x and 4.x unless noted.</p><p><strong>Concurrency limits are per node unless you say otherwise; on 3.x that is the only option.</strong> By default an execution group&#39;s running count lives in memory on the scheduler thread. Nothing is persisted and nodes do not coordinate, so a group limited to 3 can run up to 3×N across an N-node cluster. On 3.x the closest approximation is dividing the cap by the node count, which is wrong whenever a node is down.</p><p>On 4.x, <code>ForGroup(&quot;acme&quot;, 8, ExecutionLimitScope.Cluster)</code> is counted from <code>QRTZ_FIRED_TRIGGERS</code>, the cluster&#39;s reservation ledger. Know its limits:</p><ul><li>The ceiling holds within one acquisition round, but can overshoot by up to <code>nodes − 1</code> (one trigger per node). The lock-free acquisition path that allows the overshoot is taken only when a round acquires a single trigger.</li><li>It fails closed: a node that cannot reach the store fires nothing rather than firing unmetered.</li></ul><p><strong>There is no rate limiting.</strong> Execution limits cap <em>concurrency</em>, not throughput. &quot;This tenant may run 100 jobs an hour&quot; cannot be expressed; build it into the job, or into what the job calls.</p><p><strong>A starved group misfires; it does not queue.</strong> A group at its limit has its triggers skipped at acquisition. They keep their original next fire time, so if the starvation outlasts the misfire threshold they misfire, and the trigger&#39;s misfire instruction, not the limit, decides whether the occurrence is skipped or rescheduled. Choose misfire instructions for triggers in limited groups with that in mind.</p><p><strong>Pausing by group prefix does not catch groups added later.</strong> Quartz.NET records the groups a prefix <em>matched</em>, and those stay paused; a group that held nothing when the prefix ran was never matched. Paused <em>job</em> groups have a second caveat: 3.x&#39;s ADO store does not persist them, and 4.x persists them but does not impose the pause on jobs added afterwards. Suspend a tenant by <em>trigger</em> group.</p><p><strong>On 3.x, job types are not keyed by scheduler.</strong> Jobs resolve from the one container by type, with no scheduler key. Two 3.x schedulers in one container cannot have different implementations or lifetimes of the same job type: whatever the application registered is what every scheduler gets. 4.x keeps the unkeyed registration as the default (<code>AddJob&lt;T&gt;</code> still registers the type with <code>TryAdd</code> semantics), but <code>AddJobType&lt;TJob, TImplementation&gt;()</code>, <code>AddJobType&lt;TJob&gt;(lifetime)</code> and <code>AddJobType&lt;TJob&gt;(factory)</code> register under one scheduler&#39;s key, and the job factory reads that key first. On either version, a job type per tenant stops scaling long before groups do. Prefer one job type that reads its tenant from the firing and resolves what it needs inside <code>Execute</code>, by key if you like.</p><p><strong>Nothing stops a job reaching another tenant&#39;s data.</strong> Groups are a naming partition. Quartz.NET gives you partitioning; isolation is your application&#39;s job: a tenant id read from the firing and passed through every query.</p><p><strong>On 3.x, dashboard authorization is per process, all or nothing.</strong> One policy, one read-only flag, no per-scheduler policy and no scheduler-name claim check. If 3.x tenants must reach only their own scheduler, enforce it outside Quartz.NET: a process per tenant, or middleware that authorizes on the scheduler-name route segment.</p><p>4.x holds each caller to its own scheduler on both surfaces. <code>QuartzDashboardOptions.SchedulerAuthorizationPolicy</code> and <code>QuartzHttpApiOptions.SchedulerAuthorizationPolicy</code> name a policy evaluated per request against a <code>SchedulerResource</code> carrying the scheduler&#39;s name, so one <code>AuthorizationHandler&lt;TRequirement, SchedulerResource&gt;</code> covers every route and page. What a caller may <em>do</em> to the scheduler it reaches is still process-wide (the dashboard&#39;s read-only flag), so &quot;this tenant may look, that one may act&quot; stays outside Quartz.NET on either version.</p>`,48),e(`p`,null,[l[3]||=e(`strong`,null,`A shut-down scheduler is not restarted in place.`,-1),l[4]||=r(),l[5]||=e(`code`,null,`Standby()`,-1),l[6]||=r(` / `,-1),l[7]||=e(`code`,null,`Start()`,-1),l[8]||=r(` pause and resume. Shutdown is terminal: the scheduler refuses to start again and every other operation throws. You can build a `,-1),l[9]||=e(`em`,null,`new`,-1),l[10]||=r(` scheduler with the same name. On 4.1 and later, `,-1),c(m,{to:`/documentation/quartz-4.x/multi-tenancy.html#restarting-a-scheduler`},{default:i(()=>[...l[2]||=[e(`code`,null,`ISchedulerRuntime.Restart`,-1)]]),_:1}),l[11]||=r(` does that: it replays the recipe the scheduler was registered with, waits for the outgoing one's jobs, and binds the result under the same name.`,-1)]),l[22]||=t(`<p><strong>The tenant does not reach your logs by itself.</strong> The job and trigger group are tags on execution traces, and on 4.x&#39;s metrics, but neither version puts them into a logging scope. A tenant carried only in an <em>execution</em> group is invisible to those signals, which is one more reason to make the trigger group the tenant.</p><h3 id="onboarding-a-tenant-while-the-process-runs" tabindex="-1"><a class="header-anchor" href="#onboarding-a-tenant-while-the-process-runs"><span>Onboarding a tenant while the process runs</span></a></h3><p>With a group per tenant, onboarding is an ordinary <code>ScheduleJob</code> call for a new group.</p><p>With a scheduler per tenant, the DI path closes once the container is built: <code>AddQuartz</code> changes <code>IServiceCollection</code>, and the hosted service enumerates schedulers once, at start. Both versions can still build a scheduler at runtime <em>outside</em> the container: 3.x through <code>StdSchedulerFactory</code> or <code>DirectSchedulerFactory</code>, 4.x through <code>QuartzSchedulerBuilder</code>, which creates and owns a container of its own. A scheduler built this way:</p><ul><li>gets no hosted-service lifetime (you start and dispose it);</li><li>resolves its jobs from its own container, not the application&#39;s, unless you give it a job factory that bridges;</li><li>is not covered by health checks registered at startup.</li></ul><p>4.1 fixes all three with <code>ISchedulerRuntime.Add(name, configure)</code>. It builds the tenant into a container of its own that resolves the application&#39;s services, jobs and options from the application&#39;s. The tenant&#39;s jobs are ordinary application components, the host drains it when it stops, and a health check registered under its name finds it. <code>Remove</code> shuts it down and releases everything built for it. The per-version guides cover the API and the trade-offs.</p><h2 id="anti-patterns" tabindex="-1"><a class="header-anchor" href="#anti-patterns"><span>Anti-patterns</span></a></h2><p><strong>Putting the tenant in a job name and parsing it back out.</strong> <code>JobKey(&quot;nightly-report-acme&quot;)</code> looks harmless until something needs every job for a tenant, and the only way is to fetch every key and split strings. Use the group half of the key for the tenant, and the name for what the job <em>is</em>.</p><p><strong>One scheduler per tenant at thousands of tenants.</strong> Each scheduler is a scheduling loop that wakes on its own timer, a thread pool, a connection pool and a cluster check-in, whether or not the tenant has anything to run. Fine at twenty, a serious operational burden at a thousand. Groups scale where schedulers do not.</p><p><strong>A shared database with a mismatched table prefix.</strong> Nothing derives the prefix from the DDL or the DDL from the prefix; you run the scripts with the prefix substituted. A prefix pointing at tables that do not exist is caught at startup (3.x <code>PerformSchemaValidation</code>, 4.x <code>SchemaProvisioning.Validate</code>, both on by default), and the error names the missing table. A prefix pointing at tables that <em>do</em> exist and belong to another tenant is not caught: it looks correct and runs on the wrong data. Derive the prefix from the tenant id in code, not from per-environment configuration.</p><p><strong>Two unrelated schedulers sharing a database with the same <code>SCHED_NAME</code>.</strong> To the schema they are two nodes of one cluster, and they will take each other&#39;s triggers. Duplicate-name checks work only within one container; across processes, keeping names distinct is up to you. Derive the scheduler name from the tenant id rather than from a configuration file someone can copy.</p><p><strong>Assuming a per-node limit is a per-cluster limit.</strong> See <a href="#what-quartz-net-does-not-give-you">above</a>. It fails only in production, after the second node is added, and it is still the default on 4.x: a limit is cluster-wide only when it says <code>ExecutionLimitScope.Cluster</code>.</p><p><strong>Letting per-tenant metrics multiply without a view.</strong> A tenant dimension times a trigger-name dimension is a series per tenant per trigger. Aggregate before the data leaves the process.</p><h2 id="see-also" tabindex="-1"><a class="header-anchor" href="#see-also"><span>See also</span></a></h2>`,14),e(`ul`,null,[e(`li`,null,[c(m,{to:`/documentation/quartz-4.x/multi-tenancy.html`},{default:i(()=>[...l[12]||=[r(`Multi-Tenancy (Quartz 4.x)`,-1)]]),_:1}),l[13]||=r(`: the 4.x mechanics in full`,-1)]),e(`li`,null,[c(m,{to:`/documentation/quartz-3.x/multi-tenancy.html`},{default:i(()=>[...l[14]||=[r(`Multi-Tenancy (Quartz 3.x)`,-1)]]),_:1}),l[15]||=r(`: the 3.x mechanics in full`,-1)]),e(`li`,null,[c(m,{to:`/documentation/best-practices.html#one-name-per-cluster-one-id-per-node`},{default:i(()=>[...l[16]||=[r(`Best Practices`,-1)]]),_:1}),l[17]||=r(`: why never to point two non-clustered schedulers at one database`,-1)]),e(`li`,null,[c(m,{to:`/documentation/troubleshooting.html`},{default:i(()=>[...l[18]||=[r(`Troubleshooting`,-1)]]),_:1})])])])}var f=s(u,[[`render`,d]]);export{l as _pageData,f as default};