import{c as e,d as t,s as n,t as r}from"./app-BsOuDHI-.js";var i=JSON.parse(`{"path":"/documentation/quartz-4.x/packages/timezoneconverter-integration.html","title":"TimeZoneConverter Integration","lang":"en-US","frontmatter":{"title":"TimeZoneConverter Integration"},"git":{"updatedTime":1788441191000,"contributors":[{"name":"Marko Lahma","username":"","email":"marko.lahma@gmail.com","commits":1},{"name":"Claude Fable 5.1","username":"","email":"noreply@anthropic.com","commits":1}],"changelog":[{"hash":"8dee9f767ead84b6e47c023533864d1d3e3178ae","time":1788441191000,"email":"marko.lahma@gmail.com","author":"Marko Lahma","message":"The release post carries the day 4.0.0 shipped","coAuthors":[{"name":"Claude Fable 5.1","email":"noreply@anthropic.com"}]}]},"filePathRelative":"documentation/quartz-4.x/packages/timezoneconverter-integration.md"}`),a={name:`timezoneconverter-integration.md`};function o(r,i,a,o,s,c){return t(),n(`div`,null,[...i[0]||=[e(`<p><a href="https://www.nuget.org/packages/Quartz.Plugins.TimeZoneConverter" target="_blank" rel="noopener noreferrer">Quartz.Plugins.TimeZoneConverter</a> plugs <a href="https://github.com/mj1856/TimeZoneConverter" target="_blank" rel="noopener noreferrer">TimeZoneConverter</a> into Quartz&#39;s time zone lookup, so that both Windows ids (<code>Central America Standard Time</code>) and IANA ids (<code>America/Guatemala</code>) resolve on either operating system.</p><h2 id="why-you-would-want-it" tabindex="-1"><a class="header-anchor" href="#why-you-would-want-it"><span>Why you would want it</span></a></h2><p>Every trigger that names a time zone names it by id, and <code>TimeZoneInfo.FindSystemTimeZoneById</code> answers with whatever ids the machine happens to know: Windows ids on Windows, IANA ids on Linux and macOS — with recent .NET able to convert between them only where the operating system has the data to do it. A schedule written on one and run on the other therefore throws <code>TimeZoneNotFoundException</code> at the point where a trigger is built or read back, and a schedule stored in a database is exactly the schedule that gets moved between them.</p><p><code>UseTimeZoneConverter</code> registers a resolver with <code>Quartz.TimeZones</code>, which is what Quartz&#39;s own lookups go through. Both spellings then resolve everywhere, and a stored trigger keeps firing after its scheduler moves host.</p><h2 id="installation" tabindex="-1"><a class="header-anchor" href="#installation"><span>Installation</span></a></h2><p>You need to add NuGet package reference to your project which uses Quartz.</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh"><pre><code class="language-bash"><span class="line">dotnet <span class="token function">add</span> package Quartz.Plugins.TimeZoneConverter</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><h2 id="using" tabindex="-1"><a class="header-anchor" href="#using"><span>Using</span></a></h2><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line">builder<span class="token punctuation">.</span>Services<span class="token punctuation">.</span><span class="token function">AddQuartz</span><span class="token punctuation">(</span>q <span class="token operator">=&gt;</span> q<span class="token punctuation">.</span><span class="token function">UseTimeZoneConverter</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p><code>UseTimeZoneConverter</code> hangs off <code>IQuartzBuilder</code>, so the same call configures a scheduler built without a host:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token keyword">await</span> <span class="token keyword">using</span> <span class="token class-name">StandaloneSchedulerFactory</span> schedulerFactory <span class="token operator">=</span> QuartzSchedulerBuilder</span>
<span class="line">    <span class="token punctuation">.</span><span class="token function">Create</span><span class="token punctuation">(</span>q <span class="token operator">=&gt;</span> q<span class="token punctuation">.</span><span class="token function">UseTimeZoneConverter</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">.</span><span class="token function">Build</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="there-is-no-plugin-and-no-key" tabindex="-1"><a class="header-anchor" href="#there-is-no-plugin-and-no-key"><span>There is no plugin, and no key</span></a></h2><p>3.x shipped this as an <code>ISchedulerPlugin</code>, named from configuration by <code>quartz.plugin.timeZoneConverter.type</code>. In 4.0 both are gone: <code>TimeZoneConverterPlugin</code> was one <code>TimeZones.AddResolver</code> call wearing a plugin&#39;s lifecycle — no per-scheduler state, no scheduler to depend on — so <code>UseTimeZoneConverter</code> performs the registration itself. A configuration file still naming the plugin type fails to load it; delete the key and call <code>UseTimeZoneConverter</code> instead.</p><p>Two things follow from that, and both are improvements:</p><ul><li><strong>It takes effect while you are configuring, not when the scheduler starts.</strong> Time zone lookup is reached from places that have no scheduler in scope — building a trigger, parsing a <code>CronExpression</code>, deserializing a trigger out of a job store — so a trigger built before the host starts now resolves its zone as well.</li><li><strong>Nothing removes it again.</strong> The plugin disposed its registration when its scheduler shut down, and had to be careful not to disturb the other schedulers in the process while doing so. One registration that outlives every scheduler is the same guarantee with none of the bookkeeping. Calling <code>UseTimeZoneConverter</code> for a second scheduler is a no-op.</li></ul>`,15)]])}var s=r(a,[[`render`,o]]);export{i as _pageData,s as default};