import{c as e,d as t,s as n,t as r}from"./app-emje7bwI.js";var i=JSON.parse(`{"path":"/documentation/quartz-4.x/packages/timezoneconverter-integration.html","title":"TimeZoneConverter Integration","lang":"en-US","frontmatter":{"title":"TimeZoneConverter Integration"},"git":{"updatedTime":1790451733000,"contributors":[{"name":"Marko Lahma","username":"","email":"marko.lahma@gmail.com","commits":1},{"name":"Claude Opus 5.5","username":"","email":"noreply@anthropic.com","commits":1}],"changelog":[{"hash":"4a29e014ef1e61227140e96264c49cf64cd3183d","time":1790451733000,"email":"marko.lahma@gmail.com","author":"Marko Lahma","message":"A captured log reaches its column whole on Oracle and SQL Server","coAuthors":[{"name":"Claude Opus 5.5","email":"noreply@anthropic.com"}]}]},"filePathRelative":"documentation/quartz-4.x/packages/timezoneconverter-integration.md"}`),a={name:`timezoneconverter-integration.md`};function o(r,i,a,o,s,c){return t(),n(`div`,null,[...i[0]||=[e(`<p><a href="https://www.nuget.org/packages/Quartz.Plugins.TimeZoneConverter" target="_blank" rel="noopener noreferrer">Quartz.Plugins.TimeZoneConverter</a> plugs <a href="https://github.com/mj1856/TimeZoneConverter" target="_blank" rel="noopener noreferrer">TimeZoneConverter</a> into Quartz&#39;s time zone lookup. Windows ids (<code>Central America Standard Time</code>) and IANA ids (<code>America/Guatemala</code>) then resolve on either operating system.</p><h2 id="why-you-would-want-it" tabindex="-1"><a class="header-anchor" href="#why-you-would-want-it"><span>Why you would want it</span></a></h2><p><code>TimeZoneInfo.FindSystemTimeZoneById</code> knows Windows ids on Windows and IANA ids on Linux and macOS. Recent .NET converts between them only where the operating system has the data. A schedule written on one and run on the other throws <code>TimeZoneNotFoundException</code> when a trigger is built or read back. Schedules stored in a database are the ones that move between hosts.</p><p><code>UseTimeZoneConverter</code> registers a resolver with <code>Quartz.TimeZones</code>, which Quartz&#39;s own lookups go through. Both spellings then resolve everywhere, and a stored trigger keeps firing after its scheduler moves host.</p><h2 id="installation" tabindex="-1"><a class="header-anchor" href="#installation"><span>Installation</span></a></h2><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh"><pre><code class="language-bash"><span class="line">dotnet <span class="token function">add</span> package Quartz.Plugins.TimeZoneConverter</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><h2 id="using" tabindex="-1"><a class="header-anchor" href="#using"><span>Using</span></a></h2><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line">builder<span class="token punctuation">.</span>Services<span class="token punctuation">.</span><span class="token function">AddQuartz</span><span class="token punctuation">(</span>q <span class="token operator">=&gt;</span> q<span class="token punctuation">.</span><span class="token function">UseTimeZoneConverter</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p><code>UseTimeZoneConverter</code> is on <code>IQuartzBuilder</code>, so the same call works without a host:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token keyword">await</span> <span class="token keyword">using</span> <span class="token class-name">StandaloneSchedulerFactory</span> schedulerFactory <span class="token operator">=</span> QuartzSchedulerBuilder</span>
<span class="line">    <span class="token punctuation">.</span><span class="token function">Create</span><span class="token punctuation">(</span>q <span class="token operator">=&gt;</span> q<span class="token punctuation">.</span><span class="token function">UseTimeZoneConverter</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">.</span><span class="token function">Build</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="there-is-no-plugin-and-no-key" tabindex="-1"><a class="header-anchor" href="#there-is-no-plugin-and-no-key"><span>There is no plugin, and no key</span></a></h2><p>3.x shipped this as an <code>ISchedulerPlugin</code> named by <code>quartz.plugin.timeZoneConverter.type</code>. 4.0 removed both the <code>TimeZoneConverterPlugin</code> type and the key; <code>UseTimeZoneConverter</code> makes the plugin&#39;s one <code>TimeZones.AddResolver</code> call itself. A configuration file that still names the plugin type fails to load it: delete the key and call <code>UseTimeZoneConverter</code> instead.</p><ul><li><strong>It takes effect during configuration, not at scheduler start.</strong> Building a trigger, parsing a <code>CronExpression</code> and deserializing a trigger from a job store all resolve zones with no scheduler in scope. A trigger built before the host starts resolves its zone too.</li><li><strong>Nothing removes it.</strong> The registration outlives every scheduler in the process. Calling <code>UseTimeZoneConverter</code> for a second scheduler is a no-op.</li></ul>`,13)]])}var s=r(a,[[`render`,o]]);export{i as _pageData,s as default};