import{a as e,c as t,d as n,l as r,m as i,p as a,s as o,t as s,u as c}from"./app-BsOuDHI-.js";var l=JSON.parse(`{"path":"/documentation/quartz-4.x/how-tos/trigger-persistence-delegate.html","title":"Persisting a Custom Trigger Type","lang":"en-US","frontmatter":{"title":"Persisting a Custom Trigger Type"},"git":{"updatedTime":1788441191000,"contributors":[{"name":"Marko Lahma","username":"","email":"marko.lahma@gmail.com","commits":1},{"name":"Claude Fable 5.1","username":"","email":"noreply@anthropic.com","commits":1}],"changelog":[{"hash":"8dee9f767ead84b6e47c023533864d1d3e3178ae","time":1788441191000,"email":"marko.lahma@gmail.com","author":"Marko Lahma","message":"The release post carries the day 4.0.0 shipped","coAuthors":[{"name":"Claude Fable 5.1","email":"noreply@anthropic.com"}]}]},"filePathRelative":"documentation/quartz-4.x/how-tos/trigger-persistence-delegate.md"}`),u={name:`trigger-persistence-delegate.md`};function d(s,l,u,d,f,p){let m=a(`RouteLink`);return n(),o(`div`,null,[l[6]||=t(`<h1 id="persisting-a-custom-trigger-type" tabindex="-1"><a class="header-anchor" href="#persisting-a-custom-trigger-type"><span>Persisting a Custom Trigger Type</span></a></h1><p>The ADO job store knows how to store the five shipped trigger families. A trigger type of your own — or one deriving from a shipped one with extra properties — needs an <code>ITriggerPersistenceDelegate</code> to say how its schedule is written and read.</p><p>Without one, the store falls back to serializing the whole trigger into <code>QRTZ_BLOB_TRIGGERS</code>. That works, and it is a blob: unqueryable, and coupled to your type&#39;s shape forever.</p><h2 id="the-easy-path-simprop-triggers" tabindex="-1"><a class="header-anchor" href="#the-easy-path-simprop-triggers"><span>The easy path: SIMPROP_TRIGGERS</span></a></h2><p><code>QRTZ_SIMPROP_TRIGGERS</code> is a generic side table with two strings, two ints, two longs, two decimals, two booleans, a third string and a time zone id. If your schedule fits in those, derive from <code>SimplePropertiesTriggerPersistenceDelegateBase</code> and write four members:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token keyword">public</span> <span class="token keyword">sealed</span> <span class="token keyword">class</span> <span class="token class-name">BusinessDayTriggerPersistenceDelegate</span> <span class="token punctuation">:</span> <span class="token type-list"><span class="token class-name">SimplePropertiesTriggerPersistenceDelegateBase</span></span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    <span class="token keyword">public</span> <span class="token keyword">override</span> <span class="token return-type class-name"><span class="token keyword">string</span></span> <span class="token function">GetHandledTriggerTypeDiscriminator</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">=&gt;</span> <span class="token string">&quot;BUSDAY&quot;</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line">    <span class="token keyword">public</span> <span class="token keyword">override</span> <span class="token return-type class-name"><span class="token keyword">bool</span></span> <span class="token function">CanHandleTriggerType</span><span class="token punctuation">(</span><span class="token class-name">IOperableTrigger</span> trigger<span class="token punctuation">)</span></span>
<span class="line">        <span class="token operator">=&gt;</span> trigger <span class="token keyword">is</span> <span class="token class-name">BusinessDayTriggerImpl</span> impl <span class="token operator">&amp;&amp;</span> <span class="token operator">!</span>impl<span class="token punctuation">.</span>HasAdditionalProperties<span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line">    <span class="token keyword">protected</span> <span class="token keyword">override</span> <span class="token return-type class-name">SimplePropertiesTriggerProperties</span> <span class="token function">GetTriggerProperties</span><span class="token punctuation">(</span><span class="token class-name">IOperableTrigger</span> trigger<span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">{</span></span>
<span class="line">        <span class="token class-name">BusinessDayTriggerImpl</span> t <span class="token operator">=</span> <span class="token punctuation">(</span>BusinessDayTriggerImpl<span class="token punctuation">)</span> trigger<span class="token punctuation">;</span></span>
<span class="line">        <span class="token keyword">return</span> <span class="token keyword">new</span> <span class="token constructor-invocation class-name">SimplePropertiesTriggerProperties</span></span>
<span class="line">        <span class="token punctuation">{</span></span>
<span class="line">            Int1 <span class="token operator">=</span> t<span class="token punctuation">.</span>SkipCount<span class="token punctuation">,</span></span>
<span class="line">            Long1 <span class="token operator">=</span> t<span class="token punctuation">.</span>TimesTriggered<span class="token punctuation">,</span></span>
<span class="line">            String1 <span class="token operator">=</span> t<span class="token punctuation">.</span>CalendarSystem<span class="token punctuation">,</span></span>
<span class="line">            TimeZoneId <span class="token operator">=</span> t<span class="token punctuation">.</span>TimeZone<span class="token punctuation">.</span>Id<span class="token punctuation">,</span></span>
<span class="line">        <span class="token punctuation">}</span><span class="token punctuation">;</span></span>
<span class="line">    <span class="token punctuation">}</span></span>
<span class="line"></span>
<span class="line">    <span class="token keyword">protected</span> <span class="token keyword">override</span> <span class="token return-type class-name">TriggerPropertyBundle</span> <span class="token function">GetTriggerPropertyBundle</span><span class="token punctuation">(</span><span class="token class-name">SimplePropertiesTriggerProperties</span> props<span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">{</span></span>
<span class="line">        <span class="token class-name">BusinessDayScheduleBuilder</span> schedule <span class="token operator">=</span> BusinessDayScheduleBuilder<span class="token punctuation">.</span><span class="token function">Create</span><span class="token punctuation">(</span><span class="token punctuation">)</span></span>
<span class="line">            <span class="token punctuation">.</span><span class="token function">SkippingDays</span><span class="token punctuation">(</span>props<span class="token punctuation">.</span>Int1<span class="token punctuation">)</span></span>
<span class="line">            <span class="token punctuation">.</span><span class="token function">InCalendarSystem</span><span class="token punctuation">(</span>props<span class="token punctuation">.</span>String1<span class="token operator">!</span><span class="token punctuation">)</span></span>
<span class="line">            <span class="token punctuation">.</span><span class="token function">InTimeZone</span><span class="token punctuation">(</span>TimeZones<span class="token punctuation">.</span><span class="token function">FindById</span><span class="token punctuation">(</span>props<span class="token punctuation">.</span>TimeZoneId<span class="token operator">!</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line">        <span class="token class-name"><span class="token keyword">long</span></span> timesTriggered <span class="token operator">=</span> props<span class="token punctuation">.</span>Long1<span class="token punctuation">;</span></span>
<span class="line">        <span class="token keyword">return</span> <span class="token keyword">new</span> <span class="token constructor-invocation class-name">TriggerPropertyBundle</span><span class="token punctuation">(</span></span>
<span class="line">            schedule<span class="token punctuation">,</span></span>
<span class="line">            t <span class="token operator">=&gt;</span> <span class="token punctuation">(</span><span class="token punctuation">(</span>BusinessDayTriggerImpl<span class="token punctuation">)</span> t<span class="token punctuation">)</span><span class="token punctuation">.</span>TimesTriggered <span class="token operator">=</span> timesTriggered<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">    <span class="token punctuation">}</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Everything else — the four SQL statements, parameter binding, the reader — is done for you. Note what is <em>not</em> virtual: <code>Initialize(TriggerPersistenceDelegateContext)</code> is a plain <code>public void</code> on the base that sets three protected properties (<code>TablePrefix</code>, <code>SchedulerName</code>, <code>DbAccessor</code>). Read those rather than overriding it. The statements are <code>private const</code> for the same reason: they name every column the base class writes, so a subclass replacing one would either write the same statement again or write one the base&#39;s parameter binding does not match.</p><h3 id="the-columns" tabindex="-1"><a class="header-anchor" href="#the-columns"><span>The columns</span></a></h3><table><thead><tr><th>Property</th><th>Column</th></tr></thead><tbody><tr><td><code>String1</code>, <code>String2</code>, <code>String3</code></td><td><code>STR_PROP_1..3</code></td></tr><tr><td><code>Int1</code>, <code>Int2</code></td><td><code>INT_PROP_1..2</code></td></tr><tr><td><code>Long1</code>, <code>Long2</code></td><td><code>LONG_PROP_1..2</code></td></tr><tr><td><code>Decimal1</code>, <code>Decimal2</code></td><td><code>DEC_PROP_1..2</code></td></tr><tr><td><code>Boolean1</code>, <code>Boolean2</code></td><td><code>BOOL_PROP_1..2</code> (through the dialect&#39;s boolean conversion)</td></tr><tr><td><code>TimeZoneId</code></td><td><code>TIME_ZONE_ID</code></td></tr></tbody></table><p>They are deliberately anonymous: the schema is fixed, and a family that needs a fourth string is out of luck rather than adding a column.</p><div class="hint-container tip"><p class="hint-container-title">Tips</p><p><code>TIME_ZONE_ID</code> got a column of its own in 2.6. A delegate reading a row written before that finds the id in <code>String2</code> instead — <code>CalendarIntervalTriggerPersistenceDelegate</code> implements exactly that fallback, and is worth copying if your table has old rows.</p></div><h3 id="the-discriminator" tabindex="-1"><a class="header-anchor" href="#the-discriminator"><span>The discriminator</span></a></h3><p><code>GetHandledTriggerTypeDiscriminator()</code> returns the value written into <code>QRTZ_TRIGGERS.TRIGGER_TYPE</code>, and read back to find the delegate again. The shipped values are <code>SIMPLE</code>, <code>CRON</code>, <code>CAL_INT</code>, <code>DAILY_I</code>, <code>RECUR</code> and <code>BLOB</code>. The column is <code>VARCHAR(8)</code>, so keep yours short — and do not collide with those six.</p><h3 id="triggerpropertybundle-and-applystate" tabindex="-1"><a class="header-anchor" href="#triggerpropertybundle-and-applystate"><span>TriggerPropertyBundle and applyState</span></a></h3><p>A trigger is rebuilt through <code>TriggerBuilder</code>, which carries a schedule but not runtime counters. That is what the second constructor parameter is for:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token keyword">new</span> <span class="token constructor-invocation class-name">TriggerPropertyBundle</span><span class="token punctuation">(</span>scheduleBuilder<span class="token punctuation">,</span> t <span class="token operator">=&gt;</span> <span class="token punctuation">(</span><span class="token punctuation">(</span>MyTriggerImpl<span class="token punctuation">)</span> t<span class="token punctuation">)</span><span class="token punctuation">.</span>TimesTriggered <span class="token operator">=</span> timesTriggered<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>Pass <code>null</code> — or use the one-argument constructor — when your delegate carries no state beyond the schedule; the cron delegate does exactly that. The store applies the fire state, then your applier, then the routing state, in that order.</p><h2 id="the-full-path-your-own-table" tabindex="-1"><a class="header-anchor" href="#the-full-path-your-own-table"><span>The full path: your own table</span></a></h2><p><code>ITriggerPersistenceDelegate</code> directly, when the schedule does not fit the generic columns:</p><table><thead><tr><th>Member</th><th></th></tr></thead><tbody><tr><td><code>void Initialize(TriggerPersistenceDelegateContext context)</code></td><td><strong>no default implementation</strong> — a delegate that does not read the context has no accessor to prepare commands with, and would fail at its first statement rather than at startup</td></tr><tr><td><code>bool CanHandleTriggerType(IOperableTrigger trigger)</code></td><td></td></tr><tr><td><code>string GetHandledTriggerTypeDiscriminator()</code></td><td></td></tr><tr><td><code>ValueTask&lt;int&gt; InsertExtendedTriggerProperties(conn, trigger, state, jobDetail, ct)</code></td><td></td></tr><tr><td><code>ValueTask&lt;int&gt; UpdateExtendedTriggerProperties(conn, trigger, state, jobDetail, ct)</code></td><td></td></tr><tr><td><code>ValueTask&lt;int&gt; DeleteExtendedTriggerProperties(conn, triggerKey, ct)</code></td><td></td></tr><tr><td><code>ValueTask&lt;TriggerPropertyBundle&gt; LoadExtendedTriggerProperties(conn, triggerKey, ct)</code></td><td></td></tr><tr><td><code>TriggerPropertyBundle ReadTriggerPropertyBundle(DbDataReader rs)</code></td><td></td></tr></tbody></table><p>There is one default interface method: the batch <code>LoadExtendedTriggerProperties(conn, IReadOnlyCollection&lt;TriggerKey&gt;, ct)</code>, which loops the single-key overload. Override it when your table can answer a whole page in one statement.</p><p><code>TriggerPersistenceDelegateContext</code> carries three things: <code>SchedulerName</code>, <code>TablePrefix</code>, and <code>DbAccessor</code> — command preparation and parameter binding for the type table this delegate owns, which is the driver delegate itself. Bind <code>SCHED_NAME</code> in every statement, and substitute the table prefix.</p><p>You will also need DDL for the table, in every dialect you support, and a migration script — see <a href="https://github.com/quartznet/quartznet/blob/main/database/README.md" target="_blank" rel="noopener noreferrer">database/README.md</a>.</p><h2 id="registering-it" tabindex="-1"><a class="header-anchor" href="#registering-it"><span>Registering it</span></a></h2><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line">builder<span class="token punctuation">.</span>Services<span class="token punctuation">.</span><span class="token function">AddQuartz</span><span class="token punctuation">(</span>q <span class="token operator">=&gt;</span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    q<span class="token punctuation">.</span><span class="token function">UsePersistentStore</span><span class="token punctuation">(</span>s <span class="token operator">=&gt;</span></span>
<span class="line">    <span class="token punctuation">{</span></span>
<span class="line">        s<span class="token punctuation">.</span><span class="token function">UseSqlServer</span><span class="token punctuation">(</span>connectionString<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">        s<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">UseTriggerPersistenceDelegate</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>BusinessDayTriggerPersistenceDelegate<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line">    <span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"><span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>There is a factory overload too, <code>UseTriggerPersistenceDelegate(Func&lt;IServiceProvider, ITriggerPersistenceDelegate&gt;)</code>, for a delegate whose constructor takes values rather than services. Either way the delegate is constructed with <code>ActivatorUtilities</code>, so constructor dependencies work and no parameterless constructor is required.</p><p>Registration is an <em>enumerable</em> — the five built-ins are always present, and yours is added to them. Registering the same type twice collapses to one.</p><div class="hint-container warning"><p class="hint-container-title">Ordering</p><p>The five built-in delegates are consulted <strong>first</strong>, and matching is first-wins. A delegate for a type deriving from a shipped trigger will therefore never be reached unless the built-in one declines it — which is what <code>HasAdditionalProperties</code> is for. See below.</p></div><p><code>Initialize</code> is called by the <em>driver delegate</em>, not the store, once at scheduler startup: the store hands the registered delegates to <code>StdAdoDelegate.Initialize</code>, which builds a <code>TriggerPersistenceDelegateContext</code> for each and calls it before adding it to the list.</p><h2 id="what-else-a-custom-trigger-type-needs" tabindex="-1"><a class="header-anchor" href="#what-else-a-custom-trigger-type-needs"><span>What else a custom trigger type needs</span></a></h2><p>A persistence delegate is one of four pieces:</p><p><strong>1. An <code>IOperableTrigger</code>.</strong> In practice derive from <code>TriggerBase</code>, which is public and abstract, and implement <code>GetScheduleBuilder()</code>.</p><div class="hint-container tip"><p class="hint-container-title">Tips</p><p>All five shipped trigger implementations are subclassable, so deriving from <code>SimpleTriggerImpl</code> or <code>CronTriggerImpl</code> — or from <code>CalendarIntervalTriggerImpl</code>, <code>DailyTimeIntervalTriggerImpl</code> or <code>RecurrenceTriggerImpl</code> — is the shortest route to a trigger that is one of those with something added. Pair it with a serializer deriving from that trigger&#39;s built-in serializer, as below.</p></div><p><strong>2. An <code>IScheduleBuilder</code>.</strong> The store rebuilds a trigger as <code>TriggerBuilder.Create()…WithSchedule(bundle.ScheduleBuilder)</code>, so the schedule has to be reproducible from a builder.</p><p><strong>3. <code>HasAdditionalProperties</code>, if you derive from a built-in trigger.</strong> <code>TriggerBase</code> declares <code>public virtual bool HasAdditionalProperties =&gt; false</code>. Override it to return <code>true</code> and the built-in delegate for the base type declines to handle your trigger, which is what lets yours be reached — and what makes the store fall back to a BLOB if you never write one.</p><p><strong>4. A serializer</strong>, for the BLOB path and for job-data round-tripping:</p><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line"><span class="token keyword">public</span> <span class="token keyword">sealed</span> <span class="token keyword">class</span> <span class="token class-name">BusinessDayTriggerSerializer</span> <span class="token punctuation">:</span> <span class="token type-list"><span class="token class-name">TriggerSerializer<span class="token punctuation">&lt;</span>BusinessDayTriggerImpl<span class="token punctuation">&gt;</span></span></span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    <span class="token keyword">public</span> <span class="token keyword">override</span> <span class="token return-type class-name"><span class="token keyword">string</span></span> TriggerTypeName <span class="token operator">=&gt;</span> <span class="token string">&quot;BusinessDayTrigger&quot;</span><span class="token punctuation">;</span></span>
<span class="line">    <span class="token comment">// CreateScheduleBuilder / SerializeFields / DeserializeFields</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><div class="language-csharp line-numbers-mode" data-highlighter="prismjs" data-ext="cs"><pre><code class="language-csharp"><span class="line">s<span class="token punctuation">.</span><span class="token function">UseSystemTextJsonSerializer</span><span class="token punctuation">(</span>registry <span class="token operator">=&gt;</span></span>
<span class="line">    registry<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">AddTriggerSerializer</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>BusinessDayTriggerImpl<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span><span class="token keyword">new</span> <span class="token constructor-invocation class-name">BusinessDayTriggerSerializer</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>The built-in serializers are public and unsealed on purpose: a trigger deriving from a built-in one pairs with a serializer deriving from the built-in one, overriding <code>SerializeFields</code> / <code>DeserializeFields</code> and calling the base so the built-in fields keep their stored shape.</p><p><code>Quartz.Serialization.Newtonsoft</code> has the same shape under the same names — <code>NewtonsoftJsonSerializerRegistry.AddTriggerSerializer&lt;T&gt;(…)</code> through <code>UseNewtonsoftJsonSerializer(…)</code> — so a custom trigger written against one package ports to the other by changing the registration. The one asymmetry to know about is job data rather than triggers: a value type of your own inside a <code>JobDataMap</code> is declared to Newtonsoft with <code>AddJobDataValueType&lt;T&gt;()</code> and to System.Text.Json with <code>AddTypeInfoResolver(…)</code>, because the two serializers ask for different things — a converter-friendly allow-list on one side, generated metadata on the other. Both refuse an undeclared type at <em>write</em> time and name the method you need.</p><div class="hint-container warning"><p class="hint-container-title">Warning</p><p><code>UseSystemTextJsonSerializer(configure)</code> with a callback captures a <strong>per-scheduler</strong> registry that is not published to the container. Called with no callback, the serializer reads the container-wide registry instead. Pick one: registering custom serializers in the callback and then expecting the HTTP client to know about them will not work.</p></div><div class="hint-container tip"><p class="hint-container-title">Tips</p><p>If you also use <code>RAMJobStore</code>, note that its trigger-type discriminator is a hard-coded switch that a custom type falls off the end of into the blob branch. That is harmless in memory, but it means a custom trigger behaves differently in the two stores — worth knowing when a test passes in memory and fails against a database.</p></div><h2 id="see-also" tabindex="-1"><a class="header-anchor" href="#see-also"><span>See also</span></a></h2>`,43),e(`ul`,null,[e(`li`,null,[c(m,{to:`/documentation/quartz-4.x/how-tos/dialect-delegate.html`},{default:i(()=>[...l[0]||=[r(`A Driver Delegate for a New Database`,-1)]]),_:1}),l[1]||=r(` — the other delegate seam`,-1)]),e(`li`,null,[c(m,{to:`/documentation/quartz-4.x/how-tos/custom-job-store.html`},{default:i(()=>[...l[2]||=[r(`A Job Store of Your Own`,-1)]]),_:1}),l[3]||=r(` — when the storage model itself is different`,-1)]),e(`li`,null,[c(m,{to:`/documentation/quartz-4.x/packages/system-text-json.html`},{default:i(()=>[...l[4]||=[r(`JSON Serialization`,-1)]]),_:1}),l[5]||=r(` — the serializer registry in full`,-1)])])])}var f=s(u,[[`render`,d]]);export{l as _pageData,f as default};